<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>为什么喜欢沫雨~</title>
  
  <subtitle> </subtitle>
  <link href="https://11111-beep.github.io/atom.xml" rel="self"/>
  
  <link href="https://11111-beep.github.io/"/>
  <updated>2026-07-10T08:59:38.265Z</updated>
  <id>https://11111-beep.github.io/</id>
  
  <author>
    <name>MoYu</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>链表</title>
    <link href="https://11111-beep.github.io/2026/07/10/%E9%93%BE%E8%A1%A8/"/>
    <id>https://11111-beep.github.io/2026/07/10/%E9%93%BE%E8%A1%A8/</id>
    <published>2026-07-10T08:52:56.000Z</published>
    <updated>2026-07-10T08:59:38.265Z</updated>
    
    <content type="html"><![CDATA[<h1 id="链表的核心概念"><a class="markdownIt-Anchor" href="#链表的核心概念"></a> 链表的核心概念</h1><p>链表是一种线性数据结构，其内部的物理存储单元非连续，非顺序，而是通过节点中的指针将所有元素串联而成</p><p>单链表的每个节点通常包含两部分：存储数据的变量和指向下一个节点的指针</p><p><strong>节点结构体定义</strong></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">ListNode</span> &#123;</span><br><span class="line">    <span class="type">int</span> val;        <span class="comment">// 数据域</span></span><br><span class="line">    ListNode* next; <span class="comment">// 指针域：指向下一个节点</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 构造函数</span></span><br><span class="line">    <span class="built_in">ListNode</span>():<span class="built_in">val</span>(<span class="number">0</span>),<span class="built_in">next</span>(<span class="literal">nullptr</span>)&#123;&#125;</span><br><span class="line">    <span class="built_in">ListNode</span>(<span class="type">int</span> x):<span class="built_in">val</span>(x),<span class="built_in">next</span>(<span class="literal">nullptr</span>)&#123;&#125;</span><br><span class="line">    <span class="built_in">ListNode</span>(<span class="type">int</span> x,ListNode* next):<span class="built_in">val</span>(x),<span class="built_in">next</span>(next)&#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><strong>链表的基本操作（创建、遍历、摧毁）</strong></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 遍历并打印链表</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">printList</span><span class="params">(ListNode* head)</span></span>&#123;</span><br><span class="line">    ListNode* cur=head;</span><br><span class="line">    <span class="keyword">while</span>(cur!=<span class="literal">nullptr</span>)&#123;</span><br><span class="line">        cout&lt;&lt;cur-&gt;val&lt;&lt;<span class="string">&quot;-&gt;&quot;</span>;</span><br><span class="line">        cur=cur-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    cout&lt;&lt;<span class="string">&quot;nullptr&quot;</span>&lt;&lt;endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 摧毁链表，释放内存</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">freeList</span><span class="params">(ListNode* head)</span></span>&#123;</span><br><span class="line">    ListNode* cur=head;</span><br><span class="line">    <span class="keyword">while</span>(cur!=<span class="literal">nullptr</span>)&#123;</span><br><span class="line">        ListNode* temp=cur;</span><br><span class="line">        cur=cur-&gt;next;</span><br><span class="line">        <span class="keyword">delete</span> temp; <span class="comment">// 释放内存，防止内存泄漏</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">// 创建一个包含节点1-&gt;2-&gt;3的单链表</span></span><br><span class="line">    ListNode* head=<span class="keyword">new</span> <span class="built_in">ListNode</span>(<span class="number">1</span>);</span><br><span class="line">    head-&gt;next=<span class="keyword">new</span> <span class="built_in">ListNode</span>(<span class="number">2</span>);</span><br><span class="line">    head-&gt;next-&gt;next=<span class="keyword">new</span> <span class="built_in">ListNode</span>(<span class="number">3</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">printList</span>(head);</span><br><span class="line">    <span class="built_in">freeList</span>(head);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h1 id="链表的题解分析"><a class="markdownIt-Anchor" href="#链表的题解分析"></a> 链表的题解分析</h1><p>处理链表问题的常用技巧：虚拟头结点、双指针和节点指针的断开与重写</p><h2 id="翻转链表"><a class="markdownIt-Anchor" href="#翻转链表"></a> 翻转链表</h2><p><em>题目描述：</em></p><p>翻转一个单链表，例如输入1-&gt;2-&gt;3-&gt;nullptr，输出3-&gt;2-&gt;1-&gt;nullptr</p><p><em>算法思路：</em></p><p>引入三个指针：<code>pre</code>(指向当前节点的前驱)、<code>cur</code>(指向当前节点)、<code>temp</code>(暂存当前节点的后继，防止断开连接后丢失)。在遍历过程中，将<code>cur-&gt;next</code>指向<code>pre</code>，然后整体向后移动。</p><img src="/2026/07/10/链表/翻转链表.png"  alt="翻转链表" style="zoom:67%;" /><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span>&#123;</span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">        <span class="function">ListNode* <span class="title">reverseList</span><span class="params">(ListNode* head)</span></span>&#123;</span><br><span class="line">            ListNode* pre=<span class="literal">nullptr</span>;</span><br><span class="line">            ListNode* cur=head;</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">while</span>(cur!=<span class="literal">nullptr</span>)&#123;</span><br><span class="line">                ListNode* temp=cur-&gt;next; <span class="comment">// 暂存后继节点</span></span><br><span class="line">                cur-&gt;next=pre;                <span class="comment">// 修改指针指向</span></span><br><span class="line">                pre=cur;                      <span class="comment">// pre暂存cur</span></span><br><span class="line">                cur=temp;                 <span class="comment">// cur访问下一节点</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> pre; </span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="删除链表的倒数第n个节点"><a class="markdownIt-Anchor" href="#删除链表的倒数第n个节点"></a> 删除链表的倒数第N个节点</h2><p><em>题目描述：</em></p><p>给你一个链表，删除链表的倒数第n个节点，并且返回链表的头结点</p><p><em>算法思路</em></p><ol><li><p>虚拟头结点：在原链表头节点前添加一个辅助节点，用于统一处理删除头结点的边界情况</p></li><li><p>快慢双指针：</p><ul><li>让<code>fast</code>指针先向前移动n+1步</li><li>随后，<code>fast</code>和<code>slow</code>指针同步向前移动，直到<code>fast</code>到达<code>nullptr</code></li><li>此时，<code>slow</code>指针恰好指向待删除节点的前驱节点，执行<code>slow-&gt;next=slow-&gt;next-&gt;next</code>即可</li></ul></li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">ListNode* <span class="title">removeNthFromEnd</span><span class="params">(ListNode* head, <span class="type">int</span> n)</span> </span>&#123;</span><br><span class="line">        ListNode* dummy = <span class="keyword">new</span> <span class="built_in">ListNode</span>(<span class="number">0</span>, head); <span class="comment">// 创建虚拟头节点</span></span><br><span class="line">        ListNode* fast = dummy;</span><br><span class="line">        ListNode* slow = dummy;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 快指针先走 n + 1 步</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt;= n; ++i) &#123;</span><br><span class="line">            fast = fast-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 快慢指针同步前移</span></span><br><span class="line">        <span class="keyword">while</span> (fast != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">            fast = fast-&gt;next;</span><br><span class="line">            slow = slow-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 删除目标节点</span></span><br><span class="line">        ListNode* target = slow-&gt;next;</span><br><span class="line">        slow-&gt;next = slow-&gt;next-&gt;next;</span><br><span class="line">        <span class="keyword">delete</span> target; <span class="comment">// 释放内存</span></span><br><span class="line"></span><br><span class="line">        ListNode* retNode = dummy-&gt;next;</span><br><span class="line">        <span class="keyword">delete</span> dummy; <span class="comment">// 释放虚拟头节点内存</span></span><br><span class="line">        <span class="keyword">return</span> retNode;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><hr />]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;链表的核心概念&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#链表的核心概念&quot;&gt;&lt;/a&gt; 链表的核心概念&lt;/h1&gt;
&lt;p&gt;链表是一种线性数据结构，其内部的物理存储单元非连续，非顺序，而是通过节点中的指针将所有元素串联而成&lt;/p&gt;
&lt;p&gt;</summary>
      
    
    
    
    
    <category term="算法" scheme="https://11111-beep.github.io/tags/%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>移动组 Android 方向学习路径</title>
    <link href="https://11111-beep.github.io/2026/06/05/%E7%A7%BB%E5%8A%A8%E7%BB%84-Android-%E6%96%B9%E5%90%91%E5%AD%A6%E4%B9%A0%E8%B7%AF%E5%BE%84/"/>
    <id>https://11111-beep.github.io/2026/06/05/%E7%A7%BB%E5%8A%A8%E7%BB%84-Android-%E6%96%B9%E5%90%91%E5%AD%A6%E4%B9%A0%E8%B7%AF%E5%BE%84/</id>
    <published>2026-06-05T07:41:09.000Z</published>
    <updated>2026-06-05T08:51:35.885Z</updated>
    
    <content type="html"><![CDATA[<h2 id="第一阶段大一上"><a class="markdownIt-Anchor" href="#第一阶段大一上"></a> 第一阶段（大一上）</h2><ol><li>学习Java面向对象部分，主要是封装、继承、多态，了解Java语法以及集合</li><li>《第一行代码》第2版（1、2、3、4、6章），不用学习 LitePal 、11章定位以及最后一章的上架</li><li>寒假制作一个简易的记事本 demo 或者其他项目</li></ol><h2 id="第二阶段大一下"><a class="markdownIt-Anchor" href="#第二阶段大一下"></a> 第二阶段（大一下）</h2><ol><li>《第一行代码》第2版所有内容，实现天气项目</li><li>学习 Kotlin 以及《第一行代码》第3版 ，不用学习11章 Apache 服务器和最后一章的开源库</li><li>思考计设项目的 idea，思考创新点与可行性</li></ol><h2 id="第三阶段大二上"><a class="markdownIt-Anchor" href="#第三阶段大二上"></a> 第三阶段（大二上）</h2><ol><li>自定义 View</li><li>重新学习 Java 的所有内容</li><li>《Android开发艺术探索》</li><li>开始做计设项目并学习相关技术栈</li></ol><h2 id="第四阶段大二下"><a class="markdownIt-Anchor" href="#第四阶段大二下"></a> 第四阶段（大二下）</h2><ol><li>完成计设项目（大创、挑杯想参加也可以）</li><li>开始背八股和计网、刷算法、找实习</li></ol><h2 id="第五阶段大三大四"><a class="markdownIt-Anchor" href="#第五阶段大三大四"></a> 第五阶段（大三大四）</h2><ol><li>数媒</li><li>实习+转正</li></ol><hr /><h2 id="学习重点"><a class="markdownIt-Anchor" href="#学习重点"></a> 学习重点</h2><ol><li>四大组件</li><li>数据库</li><li>网络通信</li><li>RecyclerView</li><li>Kotlin 协程</li><li>Jecpack 组件（Compose暂时可学可不学）</li><li>Retrofit</li><li>自定义 View</li><li>View 分发机制与手势冲突</li><li>Handler 消息机制</li><li>MVVM、MVI等</li></ol><hr /><h2 id="学习资料"><a class="markdownIt-Anchor" href="#学习资料"></a> 学习资料</h2><ul><li><p><a href="https://developers.google.com/ar/develop/java/quickstart?hl=zh-cn">Android AR</a></p></li><li><p><a href="https://github.com/SceneView/sceneview-android">Android 场景,可配合下面的 3D 模型</a></p></li><li><p><a href="https://developers.google.com/vr/develop/android/get-started">Android VR</a></p></li><li><p><a href="https://polyhaven.com/hdris/sunrise-sunset">360度的全景图</a></p></li><li><p><a href="https://sketchfab.com/feed">3D 模型库（部分免费）</a></p></li><li><p>Android 部分机器学习：<a href="https://developers.google.com/ml-kit?hl=zh-cn">ML Kit</a>、<a href="https://developers.google.com/edge/mediapipe/solutions/guide">MediaPipe</a></p></li><li><p>不推荐训练模型，如有兴趣，自己问找网址</p></li><li><p><a href="https://lottiefiles.com/">动画库（部分免费</a>）</p></li><li><p><a href="https://mastergo.com/">界面设计 AI</a></p></li><li><p><a href="https://jetpackcompose.cn/docs/">compose 学习</a></p></li><li><p><a href="https://www.pinterest.com/">UI 素材</a></p></li><li><p><a href="https://material-foundation.github.io/material-theme-builder/">主题配色</a></p></li><li><p><a href="https://getstream.io/activity-feeds/docs/android/">社区、直播等后端服务</a></p></li><li><p><a href="https://console.firebase.google.com/">Firebase 云存储、性能测试等</a></p></li><li><p><a href="https://docs.cloud.google.com/architecture/choose-design-pattern-agentic-ai-system?hl=zh-cn">Agent 设计模式</a></p></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;第一阶段大一上&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#第一阶段大一上&quot;&gt;&lt;/a&gt; 第一阶段（大一上）&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;学习Java面向对象部分，主要是封装、继承、多态，了解Java语法以及集合&lt;/li&gt;
&lt;li&gt;《第</summary>
      
    
    
    
    
    <category term="Android 方向学习路径" scheme="https://11111-beep.github.io/tags/Android-%E6%96%B9%E5%90%91%E5%AD%A6%E4%B9%A0%E8%B7%AF%E5%BE%84/"/>
    
  </entry>
  
  <entry>
    <title>Conventional Commits规范</title>
    <link href="https://11111-beep.github.io/2025/11/14/Conventional-Commits%E8%A7%84%E8%8C%83/"/>
    <id>https://11111-beep.github.io/2025/11/14/Conventional-Commits%E8%A7%84%E8%8C%83/</id>
    <published>2025-11-14T09:32:53.000Z</published>
    <updated>2025-12-14T09:50:10.964Z</updated>
    
    <content type="html"><![CDATA[<h2 id="summary"><a class="markdownIt-Anchor" href="#summary"></a> Summary</h2><p>Conventional Commits 规范是在提交消息之上建立的轻量级约定。它提供了一套简单的规则来创建明确的提交历史记录，从而简化了基于此的自动化工具的编写。该约定与语义化版本<a href="http://semver.org/">控制（SemVer）</a> 相契合，通过在提交消息中描述新增功能、修复和重大变更来实现。</p><h2 id="structure"><a class="markdownIt-Anchor" href="#structure"></a> Structure</h2><p>一个规范的 Commit Message 分为三个部分：<strong>标题 (Header)</strong>、<strong>正文 (Body)</strong> 和 <strong>页脚 (Footer)</strong>。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&lt;type&gt;[optional scope]: &lt;description&gt; // Header</span><br><span class="line"></span><br><span class="line">[optional body] // Body</span><br><span class="line"></span><br><span class="line">[optional footer(s)] // Footer</span><br></pre></td></tr></table></figure><p><em>Header</em></p><p>这是<strong>必须的</strong>，它本身包含三个部分：</p><ul><li><p><strong><code>type</code> (类型):</strong> 必填，说明 commit 的类别。</p><ul><li><code>feat</code>: 新功能 (feature)</li><li><code>fix</code>: 修复 bug</li><li><code>perf</code>: 性能优化 (performance improvement)</li><li><code>refactor</code>: 重构 (既不是修 bug 也不是加功能的代码改动)</li><li><code>style</code>: 格式调整 (不影响代码运行的变动，如空格、分号)</li><li><code>docs</code>: 文档 (documentation)</li><li><code>test</code>: 增加测试</li><li><code>chore</code>: 构建过程或辅助工具的变动 (如修改 .gitignore)</li></ul></li><li><p><strong><code>scope</code> (范围):</strong> 可选，用于说明 commit 影响的范围 (如：<code>parser</code>, <code>auth</code>, <code>ui</code>)。</p></li><li><p><strong><code>description</code> (描述):</strong> 必填，简短描述。</p><ul><li>使用动词开头，用现在时（例如，“使用XXX，修复… ”而不是“使用了**，**修复了…”）</li><li>结尾不加句号</li></ul></li></ul><p><em>Body</em></p><p>这是<strong>可选的</strong>，是你“具体做了哪些改动，为什么”。</p><ul><li>在标题行下空一行。</li><li>详细说明代码变动的<strong>动机</strong> (Why) 和<strong>具体内容</strong> (What)。</li><li>使用 <code>-</code> 或 <code>*</code> 来创建项目符号列表。</li></ul><p><em>Footer</em></p><p>这是 <strong>可选的</strong>，通常用于两种情况：</p><ol><li><strong>重大变更 (BREAKING CHANGE):</strong> 如果当前代码与上一个版本不兼容。</li><li><strong>关闭 Issue:</strong> <code>Fixes #123</code>, <code>Closes #456</code></li></ol><h2 id="specification"><a class="markdownIt-Anchor" href="#specification"></a> Specification</h2><ol><li>提交必须以类型作为前缀，类型由名词、 <code>feat</code> 、 <code>fix</code> 等组成，后跟可选的作用域、可选的感叹号 <code>!</code> 以及必需的终止冒号和空格。</li><li>类型之后可以指定作用域。作用域必须由一个名词组成，该名词描述代码库中的某个部分，并用括号括起来，例如： <code>fix(parser):</code></li><li>类型/作用域前缀后的冒号和空格后必须紧跟描述。描述是对代码更改的简短总结，例如： <em>修复：字符串中包含多个空格时数组解析出现的问题</em> 。</li><li>简短描述之后可以添加更长的提交主体，以提供有关代码变更的更多上下文信息。主体必须在描述之后的一个空行处开始。</li><li>提交内容是自由格式的，可以由任意数量的以换行符分隔的段落组成。</li><li>正文之后可以空一行提供一个或多个页脚。每个页脚必须包含一个单词标记，后跟一个 <code>:&lt;space&gt;</code> 或 <code>&lt;space&gt;#</code> 分隔符，再后跟一个字符串值（此设计灵感来源于……） <a href="https://git-scm.com/docs/git-interpret-trailers">git 尾部约定 </a>）。</li><li>页脚标记必须使用连字符 <code>-</code> 代替空格字符，例如 <code>Acked-by</code> （这有助于将页脚部分与多段落正文区分开来）。但 <code>BREAKING CHANGE</code> 除外，它也可以用作标记。</li><li>页脚的值可以包含空格和换行符，解析必须在遇到下一个有效的页脚标记/分隔符对时终止。</li><li>必须在提交的类型/范围前缀中注明重大变更，或者在页脚中注明。</li><li>如果以页脚形式包含重大变更通知，则必须以大写字母“BREAKING CHANGE”开头，后跟冒号、空格和描述，例如： <em>重大变更：环境变量现在优先于配置文件</em> 。</li><li>如果包含在类型/范围前缀中，则必须用以下方式指示重大更改： <code>!</code> 紧跟在 <code>:</code> 之前。如果使用 <code>!</code> ，则 <code>BREAKING CHANGE:</code> 可以从页脚部分省略，并且提交描述应用于描述重大变更。</li><li>提交消息中可以使用 <code>feat</code> 和 <code>fix</code> 以外的类型，例如： <em>docs: update ref docs。</em></li><li>构成常规提交的信息单元必须由实施者区分大小写，但“重大变更”必须大写。</li><li>当用作页脚标记时，BREAKING-CHANGE 必须与 BREAKING CHANGE 同义。</li></ol><h2 id="examples"><a class="markdownIt-Anchor" href="#examples"></a> Examples</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">perf(engine): 优化工作线程逻辑并改进粒子遍历</span><br><span class="line"></span><br><span class="line">- 修复了在实际工作执行前被错误计算和休眠的问题，确保线程在正确的时间点休眠。</span><br><span class="line">- 将粒子列表的操作替换为使用迭代器 (Iterator) 进行遍历和移除。</span><br><span class="line"></span><br><span class="line">这样做可以减少不必要的等待时间，并避免在遍历过程中移除元素时可能引发的 ，提升了烟花绽放时的流畅度。</span><br><span class="line"></span><br><span class="line">Refs #1</span><br></pre></td></tr></table></figure><p><strong>这个页脚 <code>Refs #15</code> 的意思是：</strong> “这次性能优化和 #15 号 Issue (关于卡顿的那个) 是相关的，但这个 commit 可能还没有 <em>完全</em> 解决它，所以先不要关闭那个 Issue。”</p><h2 id="faq"><a class="markdownIt-Anchor" href="#faq"></a> FAQ</h2><h3 id="如果提交符合多种提交类型我该怎么办"><a class="markdownIt-Anchor" href="#如果提交符合多种提交类型我该怎么办"></a> 如果提交符合多种提交类型，我该怎么办？</h3><p>返回并进行多次提交。常规提交的好处之一是它能够促使我们进行更有条理的提交和 PR。</p><h3 id="我应该如何根据常规提交规范为我的扩展添加版本号例如-jameswomackconventional-commit-spec"><a class="markdownIt-Anchor" href="#我应该如何根据常规提交规范为我的扩展添加版本号例如-jameswomackconventional-commit-spec"></a> 我应该如何根据“常规提交规范”为我的扩展添加版本号，例如 <code>@jameswomack/conventional-commit-spec</code> ？</h3><p>使用 SemVer 发布您自己对此规范的扩展。</p><h3 id="如果我意外使用了错误的提交类型该怎么办"><a class="markdownIt-Anchor" href="#如果我意外使用了错误的提交类型该怎么办"></a> 如果我意外使用了错误的提交类型该怎么办？</h3><h4 id="当你使用了符合规范但类型不正确的类型时例如使用了-fix-而不是-feat"><a class="markdownIt-Anchor" href="#当你使用了符合规范但类型不正确的类型时例如使用了-fix-而不是-feat"></a> 当你使用了符合规范但类型不正确的类型时，例如使用了 <code>fix</code> 而不是 <code>feat</code></h4><p>在合并或发布错误代码之前，使用 <code>git rebase -i</code> 编辑提交历史。发布后，清理工作会根据您使用的工具和流程而有所不同。</p><h4 id="当你使用不符合规范的类型时例如-feet-代替-feat"><a class="markdownIt-Anchor" href="#当你使用不符合规范的类型时例如-feet-代替-feat"></a> 当你使用<em>不符合</em>规范的类型时，例如 <code>feet</code> 代替 <code>feat</code></h4><p>最坏的情况下，即使提交的代码不符合常规提交规范，也并非世界末日。这仅仅意味着基于该规范的工具将无法识别该提交。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;summary&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#summary&quot;&gt;&lt;/a&gt; Summary&lt;/h2&gt;
&lt;p&gt;Conventional Commits 规范是在提交消息之上建立的轻量级约定。它提供了一套简单的规则来创建明确的</summary>
      
    
    
    
    
    <category term="Git" scheme="https://11111-beep.github.io/tags/Git/"/>
    
  </entry>
  
  <entry>
    <title>自定义view</title>
    <link href="https://11111-beep.github.io/2025/10/20/%E8%87%AA%E5%AE%9A%E4%B9%89view/"/>
    <id>https://11111-beep.github.io/2025/10/20/%E8%87%AA%E5%AE%9A%E4%B9%89view/</id>
    <published>2025-10-20T09:25:18.000Z</published>
    <updated>2025-12-14T09:29:38.734Z</updated>
    
    <content type="html"><![CDATA[<p>自定义View是一个综合的技术体系,他涉及View的层次结构,事件分发机制和View的工作原理等技术细节.</p><p><em>自定义View总体可以分为四类</em></p><ol><li>继承View重写onDraw方法</li><li>继承ViewGroup派生特殊的Layout</li><li>继承特定的View(比如TextView)</li><li>继承特定的ViewGroup(比如LinearLayout)</li></ol><p><em>注意事项</em></p><ol><li>**让View支持wrap_content: **直接继承View或ViewGroup的控件要<code>在onMeasure</code>中对<code>wrap_content</code>做特殊处理.</li><li><strong>如果有必要,让你的View支持padding:</strong> 继承View的控件在draw方法中处理<code>padding</code>,<code>padding</code>属性才能起作用.继承ViewGroup的控件要在<code>onMeasure</code>和<code>onLayout</code>中考虑<code>padding</code>和子元素的<code>margin</code>对其造成的影响( <code>Padding </code>是 View 内部的事情,而 <code>Margin</code> 是 View 外部由其父布局决定).</li><li>**尽量不要在View中使用<code>Handler</code>: **View内部提供了<code>post</code>系列方法.</li><li>**View中如果有线程或者动画,需要及时停止: **参考<code>View</code>#<code>onDetachedFromWindow</code>.</li><li><strong>View带有滑动嵌套情形时,需要处理好滑动冲突: 重写<code>onInterceptTouchEvent</code>(外部拦截法) 或<code>dispatchTouchEvent</code>(内部拦截法)</strong></li></ol><h3 id="measure过程"><a class="markdownIt-Anchor" href="#measure过程"></a> measure过程</h3><h4 id="onmeasure-的作用是什么"><a class="markdownIt-Anchor" href="#onmeasure-的作用是什么"></a> onMeasure 的作用是什么？</h4><p>在 Android 的视图体系中，每个 View 和 ViewGroup 都需要知道自己应该占据多大的空间。这个确定大小的过程就发生在 <strong>Measure (测量)</strong> 阶段。</p><p><code>onMeasure</code> 方法就是这个阶段的核心。它的职责是：</p><ol><li><strong>接收来自父容器的尺寸约束 (Constraints)。</strong></li><li><strong>根据这些约束和自身的特性 (内容、背景等)，计算出自己期望的宽度和高度。</strong></li><li><strong>通过调用 <code>setMeasuredDimension(width, height)</code> 方法，将最终计算出的尺寸保存起来。</strong></li></ol><p>整个视图树的绘制流程分为三个主要阶段：</p><ol><li><strong>Measure (测量)</strong>: 从根视图开始，递归地调用所有子视图的 <code>onMeasure</code>，确定每个视图的大小。</li><li><strong>Layout (布局)</strong>: 从根视图开始，递归地调用所有子视图的 <code>onLayout</code>，确定每个视图在屏幕上的具体位置（左、上、右、下坐标）。</li><li><strong>Draw (绘制)</strong>: 从根视图开始，递归地调用所有子视图的 <code>onDraw</code>，将它们的内容绘制到屏幕上。</li></ol><p><code>onMeasure</code> 是这三大流程的第一个，它为后续的布局和绘制奠定了基础。</p><hr /><h4 id="理解核心概念measurespec"><a class="markdownIt-Anchor" href="#理解核心概念measurespec"></a> 理解核心概念：MeasureSpec</h4><p>在深入 <code>onMeasure</code> 之前，必须先理解 <code>MeasureSpec</code>。<code>onMeasure</code> 方法接收两个参数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onMeasure</span><span class="params">(<span class="type">int</span> widthMeasureSpec, <span class="type">int</span> heightMeasureSpec)</span> &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这两个 <code>int</code> 类型的参数并不是直接的像素值，而是 <code>MeasureSpec</code>。它是一个 32 位的整数，其中高 2 位代表 <strong>模式 (Mode)</strong>，低 30 位代表 <strong>尺寸 (Size)</strong>。</p><p><code>MeasureSpec</code> 是父容器传递给子视图的“尺寸要求”或“约束条件”。它告诉子视图：“你应该在这个规则下测量自己”。</p><h4 id="measurespec-的三种模式-mode"><a class="markdownIt-Anchor" href="#measurespec-的三种模式-mode"></a> MeasureSpec 的三种模式 (Mode)</h4><ol><li><strong><code>MeasureSpec.EXACTLY</code> (精确模式)</strong><ul><li><strong>含义</strong>: 父容器已经为子视图确定了切确的尺寸。子视图的最终尺寸就是 <code>MeasureSpec</code> 中指定的 <code>size</code> 值。</li><li><strong>对应场景</strong>:<ul><li>布局文件中设置了具体的尺寸，如 <code>android:layout_width=&quot;100dp&quot;</code>。</li><li>布局文件中设置了 <code>android:layout_width=&quot;match_parent&quot;</code>，并且父容器的尺寸是确定的。</li></ul></li></ul></li><li><strong><code>MeasureSpec.AT_MOST</code> (最大模式)</strong><ul><li><strong>含义</strong>: 父容器为子视图指定了一个最大可用尺寸。子视图的尺寸不能超过这个 <code>size</code> 值，但可以根据自身内容调整为比它更小的值。</li><li><strong>对应场景</strong>:<ul><li>布局文件中设置了 <code>android:layout_width=&quot;wrap_content&quot;</code>。此时 <code>size</code> 就是父容器剩余的可用空间。</li></ul></li></ul></li><li><strong><code>MeasureSpec.UNSPECIFIED</code> (不指定模式)</strong><ul><li><strong>含义</strong>: 父容器对子视图的尺寸没有任何限制，子视图可以根据需要设置为任意大小。</li><li><strong>对应场景</strong>:<ul><li>通常用在可滚动的容器中，如 <code>ScrollView</code>、<code>RecyclerView</code>。因为这些容器可以在一个维度上无限延伸，所以它们在测量子视图时不会限制其高度（或宽度）。</li></ul></li></ul></li></ol><p>我们可以使用 <code>MeasureSpec</code> 类的静态方法来解析这两个值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">specMode</span> <span class="operator">=</span> MeasureSpec.getMode(measureSpec);</span><br><span class="line"><span class="type">int</span> <span class="variable">specSize</span> <span class="operator">=</span> MeasureSpec.getSize(measureSpec);</span><br></pre></td></tr></table></figure><hr /><h4 id="自定义-view的-onmeasure"><a class="markdownIt-Anchor" href="#自定义-view的-onmeasure"></a> 自定义 View的 onMeasure</h4><p>对于一个独立的、没有子视图的自定义 <code>View</code>（例如，继承自 <code>View</code>、<code>TextView</code>、<code>ImageView</code> 等），<code>onMeasure</code> 的实现相对简单。它的目标是根据父容器的 <code>MeasureSpec</code> 计算出自己的尺寸。</p><p><strong>基本步骤：</strong></p><ol><li>重写 <code>onMeasure(int widthMeasureSpec, int heightMeasureSpec)</code> 方法。</li><li>在方法内部，分别解析宽度和高度的 <code>mode</code> 和 <code>size</code>。</li><li>针对不同的 <code>mode</code>，计算出最终的宽度和高度。<ul><li><strong><code>EXACTLY</code></strong>: 直接使用 <code>specSize</code> 作为最终尺寸。</li><li><strong><code>AT_MOST</code></strong>: 计算出自己内容所需的尺寸（比如文本的宽度、图片的高度等），然后与 <code>specSize</code> 比较，取较小值。<code>Math.min(contentSize, specSize)</code>。</li><li><strong><code>UNSPECIFIED</code></strong>: 直接使用自己内容所需的尺寸。</li></ul></li><li><strong>【最重要的一步】</strong> 调用 <code>setMeasuredDimension(measuredWidth, measuredHeight)</code> 保存计算结果。<strong>如果不调用这个方法，系统会抛出异常。</strong></li></ol><h4 id="代码示例一个简单的正方形-view"><a class="markdownIt-Anchor" href="#代码示例一个简单的正方形-view"></a> 代码示例：一个简单的正方形 View</h4><p>假设我们要创建一个自定义 View，它总是尝试将自己渲染成一个正方形。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SquareView</span> <span class="keyword">extends</span> <span class="title class_">View</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">defaultSize</span> <span class="operator">=</span> <span class="number">200</span>; <span class="comment">// 默认尺寸，单位 px</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">SquareView</span><span class="params">(Context context, <span class="meta">@Nullable</span> AttributeSet attrs)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(context, attrs);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onMeasure</span><span class="params">(<span class="type">int</span> widthMeasureSpec, <span class="type">int</span> heightMeasureSpec)</span> &#123;</span><br><span class="line">        <span class="comment">// 直接调用父类 onMeasure 是一个好习惯，它会处理一些默认情况</span></span><br><span class="line">        <span class="comment">// super.onMeasure(widthMeasureSpec, heightMeasureSpec);</span></span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">width</span> <span class="operator">=</span> getMySize(defaultSize, widthMeasureSpec);</span><br><span class="line">        <span class="type">int</span> <span class="variable">height</span> <span class="operator">=</span> getMySize(defaultSize, heightMeasureSpec);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 为了保证是正方形，我们取宽高的最小值</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">finalSize</span> <span class="operator">=</span> Math.min(width, height);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 【关键】保存最终计算出的尺寸</span></span><br><span class="line">        setMeasuredDimension(finalSize, finalSize);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="title function_">getMySize</span><span class="params">(<span class="type">int</span> defaultSize, <span class="type">int</span> measureSpec)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">mySize</span> <span class="operator">=</span> defaultSize;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">mode</span> <span class="operator">=</span> MeasureSpec.getMode(measureSpec);</span><br><span class="line">        <span class="type">int</span> <span class="variable">size</span> <span class="operator">=</span> MeasureSpec.getSize(measureSpec);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">switch</span> (mode) &#123;</span><br><span class="line">            <span class="keyword">case</span> MeasureSpec.EXACTLY:</span><br><span class="line">                <span class="comment">// match_parent 或具体 dp 值，父容器决定了大小，我们必须遵守</span></span><br><span class="line">                mySize = size;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> MeasureSpec.AT_MOST:</span><br><span class="line">                <span class="comment">// wrap_content，我们不能超过父容器给的最大值</span></span><br><span class="line">                <span class="comment">// 在这里，我们的内容大小就是 defaultSize</span></span><br><span class="line">                mySize = Math.min(defaultSize, size);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> MeasureSpec.UNSPECIFIED:</span><br><span class="line">                <span class="comment">// 父容器不关心，我们用自己的默认值</span></span><br><span class="line">                mySize = defaultSize;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> mySize;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>小技巧</strong>: Android 提供了一个辅助方法 <code>resolveSize(size, measureSpec)</code>，它可以帮你简化 <code>AT_MOST</code> 和 <code>EXACTLY</code> 模式的处理。它等价于：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="type">int</span> <span class="title function_">resolveSize</span><span class="params">(<span class="type">int</span> size, <span class="type">int</span> measureSpec)</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> size;</span><br><span class="line">    <span class="type">int</span> <span class="variable">specMode</span> <span class="operator">=</span> MeasureSpec.getMode(measureSpec);</span><br><span class="line">    <span class="type">int</span> <span class="variable">specSize</span> <span class="operator">=</span> MeasureSpec.getSize(measureSpec);</span><br><span class="line">    <span class="keyword">switch</span> (specMode) &#123;</span><br><span class="line">    <span class="keyword">case</span> MeasureSpec.UNSPECIFIED:</span><br><span class="line">        result = size;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> MeasureSpec.AT_MOST:</span><br><span class="line">        result = Math.min(size, specSize);</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> MeasureSpec.EXACTLY:</span><br><span class="line">        result = specSize;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h4 id="自定义-viewgroup-的-onmeasure"><a class="markdownIt-Anchor" href="#自定义-viewgroup-的-onmeasure"></a> 自定义 ViewGroup 的 onMeasure</h4><p>自定义 <code>ViewGroup</code> 的 <code>onMeasure</code> 要复杂得多，因为它有双重职责：</p><ol><li><strong>测量所有子视图 (Children)</strong>: 它需要遍历自己的每一个子视图，调用它们的 <code>measure()</code> 方法，并为其生成合适的 <code>MeasureSpec</code>。</li><li><strong>确定自身尺寸</strong>: 在测量完所有子视图后，它需要根据子视图的尺寸和自身的布局逻辑（例如，是线性排列还是层叠排列），计算出自己的总尺寸。</li></ol><p><strong>基本步骤:</strong></p><ol><li>重写 <code>onMeasure(int widthMeasureSpec, int heightMeasureSpec)</code> 方法。</li><li><strong>遍历所有子视图</strong>：使用 <code>for</code> 循环和 <code>getChildCount()</code>。</li><li><strong>为每个子视图生成 <code>MeasureSpec</code></strong>: 这是最复杂的一步。你需要结合父容器传给 <code>ViewGroup</code> 的 <code>MeasureSpec</code> 和子视图自身的 <code>LayoutParams</code> (例如 <code>layout_width=&quot;match_parent&quot;</code> 或 <code>layout_width=&quot;wrap_content&quot;</code>) 来为子视图创建一个新的 <code>MeasureSpec</code>。通常使用 <code>getChildMeasureSpec()</code> 这个静态辅助方法来完成。</li><li><strong>测量子视图</strong>: 调用 <code>child.measure(childWidthMeasureSpec, childHeightMeasureSpec)</code>。这个调用会触发子视图的 <code>onMeasure</code> 方法。</li><li><strong>计算自身尺寸</strong>: 测量完所有子视图后，通过 <code>child.getMeasuredWidth()</code> 和 <code>child.getMeasuredHeight()</code> 获取它们的尺寸。然后根据你的布局逻辑（例如，将所有子视图高度相加）计算出 <code>ViewGroup</code> 自身需要的总宽度和总高度。</li><li><strong>合并尺寸和约束</strong>: 将计算出的自身所需尺寸与父容器传递的 <code>MeasureSpec</code> 进行协调（处理 <code>EXACTLY</code> 和 <code>AT_MOST</code> 模式）。</li><li><strong>保存结果</strong>: 调用 <code>setMeasuredDimension()</code> 保存最终尺寸。</li></ol><h4 id="代码示例一个简单的垂直线性布局"><a class="markdownIt-Anchor" href="#代码示例一个简单的垂直线性布局"></a> 代码示例：一个简单的垂直线性布局</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CustomVerticalLayout</span> <span class="keyword">extends</span> <span class="title class_">ViewGroup</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">CustomVerticalLayout</span><span class="params">(Context context, AttributeSet attrs)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(context, attrs);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onMeasure</span><span class="params">(<span class="type">int</span> widthMeasureSpec, <span class="type">int</span> heightMeasureSpec)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 获取自身的 MeasureSpec 约束</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">widthMode</span> <span class="operator">=</span> MeasureSpec.getMode(widthMeasureSpec);</span><br><span class="line">        <span class="type">int</span> <span class="variable">widthSize</span> <span class="operator">=</span> MeasureSpec.getSize(widthMeasureSpec);</span><br><span class="line">        <span class="type">int</span> <span class="variable">heightMode</span> <span class="operator">=</span> MeasureSpec.getMode(heightMeasureSpec);</span><br><span class="line">        <span class="type">int</span> <span class="variable">heightSize</span> <span class="operator">=</span> MeasureSpec.getSize(heightMeasureSpec);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 用于计算 ViewGroup 自身尺寸的变量</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">totalHeight</span> <span class="operator">=</span> <span class="number">0</span>; <span class="comment">// 累计所有子 View 的高度</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">maxWidth</span> <span class="operator">=</span> <span class="number">0</span>;    <span class="comment">// 找出最宽的子 View 的宽度</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 遍历子 View</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; getChildCount(); i++) &#123;</span><br><span class="line">            <span class="type">View</span> <span class="variable">child</span> <span class="operator">=</span> getChildAt(i);</span><br><span class="line">            <span class="keyword">if</span> (child.getVisibility() == GONE) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 4. 为子 View 生成 MeasureSpec 并测量它</span></span><br><span class="line">            <span class="comment">// measureChild 会自动处理 getChildMeasureSpec 的逻辑</span></span><br><span class="line">            measureChild(child, widthMeasureSpec, heightMeasureSpec);</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 5. 累加尺寸</span></span><br><span class="line">            <span class="comment">// 获取子 View 测量后的尺寸（包含 margin）</span></span><br><span class="line">            <span class="type">MarginLayoutParams</span> <span class="variable">lp</span> <span class="operator">=</span> (MarginLayoutParams) child.getLayoutParams();</span><br><span class="line">            <span class="type">int</span> <span class="variable">childHeight</span> <span class="operator">=</span> child.getMeasuredHeight() + lp.topMargin + lp.bottomMargin;</span><br><span class="line">            <span class="type">int</span> <span class="variable">childWidth</span> <span class="operator">=</span> child.getMeasuredWidth() + lp.leftMargin + lp.rightMargin;</span><br><span class="line"></span><br><span class="line">            totalHeight += childHeight;</span><br><span class="line">            maxWidth = Math.max(maxWidth, childWidth);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 6. 根据自身约束，确定最终尺寸</span></span><br><span class="line">        <span class="type">int</span> measuredWidth;</span><br><span class="line">        <span class="type">int</span> measuredHeight;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 宽度处理</span></span><br><span class="line">        <span class="keyword">if</span> (widthMode == MeasureSpec.EXACTLY) &#123;</span><br><span class="line">            measuredWidth = widthSize;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (widthMode == MeasureSpec.AT_MOST) &#123;</span><br><span class="line">            measuredWidth = Math.min(maxWidth, widthSize);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            measuredWidth = maxWidth;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 高度处理</span></span><br><span class="line">        <span class="keyword">if</span> (heightMode == MeasureSpec.EXACTLY) &#123;</span><br><span class="line">            measuredHeight = heightSize;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (heightMode == MeasureSpec.AT_MOST) &#123;</span><br><span class="line">            measuredHeight = Math.min(totalHeight, heightSize);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            measuredHeight = totalHeight;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 7. 保存最终尺寸</span></span><br><span class="line">        setMeasuredDimension(measuredWidth, measuredHeight);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 为了能正确处理 margin，需要重写以下方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> LayoutParams <span class="title function_">generateLayoutParams</span><span class="params">(AttributeSet attrs)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">MarginLayoutParams</span>(getContext(), attrs);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>注意</strong>: 在 <code>ViewGroup</code> 中，通常会使用 <code>measureChild()</code> 或 <code>measureChildWithMargins()</code> 这两个辅助方法，它们内部封装了 <code>getChildMeasureSpec()</code> 和 <code>child.measure()</code> 的调用，能简化代码，特别是处理 <code>padding</code> 和 <code>margin</code> 时。</p><hr /><h4 id="wrap_content解决方案"><a class="markdownIt-Anchor" href="#wrap_content解决方案"></a> Wrap_content解决方案</h4><h5 id="定义默认尺寸"><a class="markdownIt-Anchor" href="#定义默认尺寸"></a> 定义默认尺寸</h5><p>在你的 View 类中，最好将默认尺寸定义为成员变量，方便管理和转换。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CircleView</span> <span class="keyword">extends</span> <span class="title class_">View</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 定义默认尺寸 (以像素为单位)</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> defaultWidth;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> defaultHeight;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ... 构造函数等 ...</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">CircleView</span><span class="params">(Context context, <span class="meta">@Nullable</span> AttributeSet attrs)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(context, attrs);</span><br><span class="line">        init(context);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">init</span><span class="params">(Context context)</span> &#123;</span><br><span class="line">        <span class="comment">// 将 dp 转换为像素 (px)</span></span><br><span class="line">        <span class="type">float</span> <span class="variable">density</span> <span class="operator">=</span> context.getResources().getDisplayMetrics().density;</span><br><span class="line">        defaultWidth = (<span class="type">int</span>) (<span class="number">100</span> * density);</span><br><span class="line">        defaultHeight = (<span class="type">int</span>) (<span class="number">100</span> * density);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// ... 其他代码 ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>最佳实践</strong>: 将 <code>100</code> 这个硬编码的数值定义在 <code>res/values/dimens.xml</code> 中会是更好的做法，这样更容易维护。</li></ul><h5 id="重写-onmeasure-方法"><a class="markdownIt-Anchor" href="#重写-onmeasure-方法"></a> 重写 onMeasure() 方法</h5><p>这是最关键的一步。你需要检查测量规格中的模式，并据此计算最终尺寸。</p><p>在很多情况下，你不需要 <code>Math.min</code> 那么复杂的逻辑，可以直接在 <code>AT_MOST</code> 模式下设置默认值。很多人也会使用 <code>resolveSize()</code> 这个辅助方法来简化代码，它内部已经包含了类似的逻辑。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onMeasure</span><span class="params">(<span class="type">int</span> widthMeasureSpec, <span class="type">int</span> heightMeasureSpec)</span> &#123;</span><br><span class="line">    <span class="comment">// resolveSize() 会帮你处理 EXACTLY 和 AT_MOST 模式</span></span><br><span class="line">    <span class="comment">// 在 AT_MOST 模式下，它会返回 Math.min(defaultSize, specSize)</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">measuredWidth</span> <span class="operator">=</span> resolveSize(defaultWidth, widthMeasureSpec);</span><br><span class="line">    <span class="type">int</span> <span class="variable">measuredHeight</span> <span class="operator">=</span> resolveSize(defaultHeight, heightMeasureSpec);</span><br><span class="line">    </span><br><span class="line">    setMeasuredDimension(measuredWidth, measuredHeight);</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p><strong>注意</strong>: <code>resolveSize()</code> 在处理 <code>UNSPECIFIED</code> 模式时，会直接返回你提供的默认尺寸。这在大多数情况下是符合预期的。</p></blockquote><h4 id="总结与关键点"><a class="markdownIt-Anchor" href="#总结与关键点"></a> 总结与关键点</h4><ol><li><p><strong><code>onMeasure</code> 的目的是计算并设置 View 的尺寸。</strong></p></li><li><p><strong><code>MeasureSpec</code> 是核心</strong>，它封装了父容器对子视图的尺寸约束（模式 + 尺寸）。</p></li><li><p><strong>自定义 <code>View</code></strong>：根据 <code>MeasureSpec</code> 和自身内容计算尺寸，然后调用 <code>setMeasuredDimension()</code>。</p></li><li><p><strong>自定义 <code>ViewGroup</code></strong>：</p><ul><li>先遍历并测量所有子视图 (<code>child.measure()</code>)。</li><li>再根据子视图的尺寸和布局逻辑计算自身尺寸。</li><li>最后调用 <code>setMeasuredDimension()</code>。</li></ul></li><li><p><strong>必须调用 <code>setMeasuredDimension()</code></strong>，否则程序会崩溃。</p></li><li><p><code>onMeasure</code> 可能会被调用多次，所以其中的计算逻辑应该尽可能高效，避免重复或耗时的操作。</p></li><li><p>当你需要重新触发布局流程时（例如，View 的内容变化导致尺寸可能改变），应该调用 <code>requestLayout()</code> 方法，它会使视图树重新执行 <code>onMeasure</code> 和 <code>onLayout</code>。</p></li></ol><hr /><h3 id="layout过程"><a class="markdownIt-Anchor" href="#layout过程"></a> layout过程</h3><p><code>onLayout</code> 是 View 绘制三大流程中的第二步（Measure -&gt; <strong>Layout</strong> -&gt; Draw）。在 <code>onMeasure</code> 确定了所有 View 的尺寸之后，<code>onLayout</code> 的职责就是确定这些 View 的 <strong>具体位置</strong>。</p><hr /><h4 id="onlayout的作用和参数"><a class="markdownIt-Anchor" href="#onlayout的作用和参数"></a> onLayout的作用和参数</h4><p><code>onLayout</code> 方法的签名如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onLayout</span><span class="params">(<span class="type">boolean</span> changed, <span class="type">int</span> left, <span class="type">int</span> top, <span class="type">int</span> right, <span class="type">int</span> bottom)</span> &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它的作用是：<strong>父容器调用这个方法来确定其所有子视图（Children）在屏幕上的最终位置</strong>。</p><h4 id="参数解释"><a class="markdownIt-Anchor" href="#参数解释"></a> 参数解释</h4><ul><li><p><code>changed</code> (boolean): 表示 View 的尺寸或位置是否相较于上一次布局发生了变化。如果为 <code>true</code>，意味着需要重新布局。</p></li><li><p><code>left</code>, <code>top</code>, <code>right</code>, <code>bottom</code> (int): 这四个参数定义了 <strong>当前 View 本身</strong> 在其父容器坐标系中的位置和区域。它们是由当前 View 的父容器在调用 <code>child.layout()</code> 时传递过来的。</p><ul><li><code>left</code>: View 左边缘相对于父容器左边缘的距离。</li><li><code>top</code>: View 上边缘相对于父容器上边缘的距离。</li><li><code>right</code>: View 右边缘相对于父容器左边缘的距离。</li><li><code>bottom</code>: View 下边缘相对于父容器上边缘的距离。</li></ul><p>你可以通过 <code>getWidth()</code> (<code>right - left</code>) 和 <code>getHeight()</code> (<code>bottom - top</code>) 获取到 View 在 <code>onMeasure</code> 后并经过布局确定的最终尺寸。</p></li></ul><hr /><h4 id="自定义-view的-onlayout"><a class="markdownIt-Anchor" href="#自定义-view的-onlayout"></a> 自定义 View的 onLayout</h4><p>对于一个不包含任何子视图的自定义 <code>View</code> (继承自 <code>View</code> 或 <code>TextView</code> 等)，<strong>通常你不需要重写 <code>onLayout</code> 方法</strong>。</p><p><strong>为什么？</strong></p><p>因为 <code>onLayout</code> 的核心职责是 <strong>布局它的子视图</strong>。一个普通的 <code>View</code> 没有子视图，所以它没有东西需要布局。它的位置完全由它的父 <code>ViewGroup</code> 来决定。</p><p>父 <code>ViewGroup</code> 在自己的 <code>onLayout</code> 方法中，会计算出这个 <code>View</code> 应该放在哪里，然后调用这个 <code>View</code> 的 <code>layout(l, t, r, b)</code> 方法。<code>View</code> 类中 <code>final</code> 的 <code>layout()</code> 方法会负责将这四个坐标值保存到 View 的内部变量中（<code>mLeft</code>, <code>mTop</code>, <code>mRight</code>, <code>mBottom</code>），然后它会调用 <code>onLayout()</code>。</p><p><code>View.java</code> 中 <code>onLayout()</code> 的默认实现是空的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// in View.java</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onLayout</span><span class="params">(<span class="type">boolean</span> changed, <span class="type">int</span> left, <span class="type">int</span> top, <span class="type">int</span> right, <span class="type">int</span> bottom)</span> &#123;</span><br><span class="line">    <span class="comment">// Empty by default</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>所以，对于一个简单的自定义 <code>View</code>，由于没有子 View 需要排列，我们让这个空方法被调用就可以了，无需重写。</p><hr /><h4 id="自定义-viewgroup的-onlayout"><a class="markdownIt-Anchor" href="#自定义-viewgroup的-onlayout"></a> 自定义 ViewGroup的 onLayout</h4><p><code>onLayout</code> 对于自定义 <code>ViewGroup</code> 来说是 <strong>必须实现的核心方法</strong>。这正是 <code>ViewGroup</code> 与普通 <code>View</code> 的本质区别所在——它需要管理和排列它的孩子们。</p><p><strong>核心职责：</strong></p><p>遍历所有的子视图，并为每一个子视图调用其 <code>layout(l, t, r, b)</code> 方法，从而将它们放置在 <code>ViewGroup</code> 内部的正确位置。</p><p><strong>基本步骤：</strong></p><ol><li>重写 <code>onLayout(boolean changed, int left, int top, int right, int bottom)</code> 方法。</li><li>在方法内部，通常需要先考虑自身的 <code>padding</code>。子视图的布局区域应该在 <code>ViewGroup</code> 的 <code>padding</code> 内部。</li><li><strong>遍历所有子视图</strong>：使用 <code>for</code> 循环和 <code>getChildCount()</code>。</li><li><strong>获取子视图的测量尺寸</strong>：在 <code>onMeasure</code> 阶段结束后，可以通过 <code>child.getMeasuredWidth()</code> 和 <code>child.getMeasuredHeight()</code> 来获取每个子视图希望的尺寸。</li><li><strong>计算每个子视图的位置</strong>：根据你的布局逻辑（例如垂直线性排列、水平排列、或者像 <code>FrameLayout</code> 一样层叠），计算出每个子视图的 <code>left</code>, <code>top</code>, <code>right</code>, <code>bottom</code> 坐标。<strong>注意：这些坐标是相对于当前 <code>ViewGroup</code> 的，而不是相对于屏幕的。</strong></li><li><strong>【最重要的一步】</strong> 为每个子视图调用 <code>child.layout(childLeft, childTop, childRight, childBottom)</code>，将计算出的位置应用到子视图上。这个调用会递归地触发子视图的布局流程。</li></ol><h4 id="代码示例继续完善-customverticallayout"><a class="markdownIt-Anchor" href="#代码示例继续完善-customverticallayout"></a> 代码示例：继续完善 CustomVerticalLayout</h4><p>我们接着 <code>onMeasure</code> 的例子，为 <code>CustomVerticalLayout</code> 实现 <code>onLayout</code> 方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CustomVerticalLayout</span> <span class="keyword">extends</span> <span class="title class_">ViewGroup</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ... 构造函数和 onMeasure 方法 ...</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onLayout</span><span class="params">(<span class="type">boolean</span> changed, <span class="type">int</span> l, <span class="type">int</span> t, <span class="type">int</span> r, <span class="type">int</span> b)</span> &#123; <span class="comment">// 定义了 getWidth() 和 getHeight() 的值。</span></span><br><span class="line">        <span class="comment">// 1. 考虑自身的 padding作为初始坐标</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">currentTop</span> <span class="operator">=</span> getPaddingTop();</span><br><span class="line">        <span class="type">int</span> <span class="variable">currentLeft</span> <span class="operator">=</span> getPaddingLeft();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 遍历子 View</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; getChildCount(); i++) &#123;</span><br><span class="line">            <span class="type">View</span> <span class="variable">child</span> <span class="operator">=</span> getChildAt(i);</span><br><span class="line">            <span class="keyword">if</span> (child.getVisibility() == GONE) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 3. 获取子 View 的测量尺寸和 margin</span></span><br><span class="line">            <span class="type">MarginLayoutParams</span> <span class="variable">lp</span> <span class="operator">=</span> (MarginLayoutParams) child.getLayoutParams();</span><br><span class="line">            <span class="type">int</span> <span class="variable">childWidth</span> <span class="operator">=</span> child.getMeasuredWidth();</span><br><span class="line">            <span class="type">int</span> <span class="variable">childHeight</span> <span class="operator">=</span> child.getMeasuredHeight();</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 4. 计算子 View 的 left, top, right, bottom 坐标</span></span><br><span class="line">            <span class="comment">// 坐标系是相对于当前 ViewGroup 的,包括内边距</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">left</span> <span class="operator">=</span> currentLeft + lp.leftMargin; <span class="comment">// 左坐标+左外边距</span></span><br><span class="line">            <span class="type">int</span> <span class="variable">top</span> <span class="operator">=</span> currentTop + lp.topMargin;</span><br><span class="line">            <span class="type">int</span> <span class="variable">right</span> <span class="operator">=</span> left + childWidth;</span><br><span class="line">            <span class="type">int</span> <span class="variable">bottom</span> <span class="operator">=</span> top + childHeight;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 5. 【关键】调用 child.layout() 来放置子 View</span></span><br><span class="line">            child.layout(left, top, right, bottom);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 6. 更新下一个子 View 的起始 top 位置,包括外边距</span></span><br><span class="line">            currentTop += childHeight + lp.topMargin + lp.bottomMargin;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// ... generateLayoutParams 方法 ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中：</p><ul><li>我们维护一个 <code>currentTop</code> 变量，用于记录当前应当放置子视图的纵向位置。</li><li>对于每个子视图，我们从 <code>currentTop</code> 开始，加上它的 <code>topMargin</code>，确定它的 <code>top</code> 位置。</li><li>它的 <code>bottom</code> 位置就是 <code>top + childHeight</code>。</li><li>它的 <code>left</code> 和 <code>right</code> 位置则由 <code>ViewGroup</code> 的 <code>paddingLeft</code>、子视图的 <code>leftMargin</code> 和子视图的宽度共同决定。</li><li>放置好一个子视图后，我们将 <code>currentTop</code> 增加这个子视图的总高度（测量高度 + 上下 margin），为下一个子视图的放置做准备。</li></ul><hr /><h4 id="onmeasurevsonlayout-核心区别对比"><a class="markdownIt-Anchor" href="#onmeasurevsonlayout-核心区别对比"></a> onMeasure<code>vs</code>onLayout 核心区别对比</h4><p>为了更清晰地理解，我们将这两个方法进行对比：</p><table><thead><tr><th>特性</th><th><code>onMeasure</code></th><th><code>onLayout</code></th></tr></thead><tbody><tr><td><strong>核心职责</strong></td><td>计算视图的 <strong>尺寸</strong> (Size)</td><td>确定视图的 <strong>位置</strong> (Position)</td></tr><tr><td><strong>工作对象</strong></td><td><strong><code>View</code> 和 <code>ViewGroup</code> 都需要</strong>。<code>ViewGroup</code> 测量子视图，并基于此计算自身尺寸。</td><td><strong>主要是 <code>ViewGroup</code> 的职责</strong>。<code>View</code> 通常不需要实现它。</td></tr><tr><td><strong>关键调用</strong></td><td>计算完尺寸后，必须调用 <code>setMeasuredDimension()</code> 来保存结果。</td><td>遍历子视图，为每个子视图调用 <code>child.layout()</code> 来应用位置。</td></tr><tr><td><strong>输入参数</strong></td><td><code>widthMeasureSpec</code>, <code>heightMeasureSpec</code> (父容器的尺寸约束)</td><td><code>changed</code>, <code>left</code>, <code>top</code>, <code>right</code>, <code>bottom</code> (自身在父容器中的位置)</td></tr><tr><td><strong>数据来源</strong></td><td>依赖父容器的约束和自身内容。</td><td>依赖 <code>onMeasure</code> 的结果 (<code>getMeasuredWidth()</code>) 和自身的布局逻辑。</td></tr><tr><td><strong>触发时机</strong></td><td>在 <code>layout</code> 之前。</td><td>在 <code>measure</code> 之后，<code>draw</code> 之前。</td></tr></tbody></table><h4 id="总结与关键点-2"><a class="markdownIt-Anchor" href="#总结与关键点-2"></a> 总结与关键点</h4><ol><li><strong><code>onLayout</code> 的核心使命是“放位置”</strong>。它在 <code>onMeasure</code> 算出“有多大”之后，决定“放哪里”。</li><li>对于<strong>自定义 <code>View</code></strong>，因为没有子视图，所以<strong>基本不用管 <code>onLayout</code></strong>。</li><li>对于<strong>自定义 <code>ViewGroup</code></strong>，<code>onLayout</code> 是你实现<strong>自定义布局逻辑</strong>的地方，<strong>必须重写</strong>。</li><li>在 <code>onLayout</code> 中，你需要遍历子视图，获取它们测量好的尺寸，然后调用 <code>child.layout()</code> 将它们一一安放。</li><li><code>onLayout</code> 中计算的坐标是 <strong>相对于父容器（也就是当前这个 <code>ViewGroup</code>）的</strong>。</li><li><code>requestLayout()</code> 会触发布局的重新计算，它会依次调用 <code>onMeasure</code> 和 <code>onLayout</code>。而 <code>invalidate()</code> 只会触发重绘，调用 <code>onDraw</code>。如果你的 View 只是内容变了但尺寸和位置不变（比如改变画笔颜色），调用 <code>invalidate()</code>；如果尺寸可能发生变化，调用 <code>requestLayout()</code>。</li></ol><hr /><h3 id="draw过程"><a class="markdownIt-Anchor" href="#draw过程"></a> draw过程</h3><p><code>onDraw</code> 是 View 绘制三大流程中的最后一步（Measure -&gt; Layout -&gt; <strong>Draw</strong>）。在 <code>onMeasure</code> 确定了尺寸、<code>onLayout</code> 确定了位置之后，<code>onDraw</code> 的职责就是 <strong>将视图的内容实际绘制到屏幕上</strong>。</p><hr /><h4 id="ondraw-的作用和核心工具"><a class="markdownIt-Anchor" href="#ondraw-的作用和核心工具"></a> onDraw 的作用和核心工具</h4><p><code>onDraw</code> 方法的签名如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onDraw</span><span class="params">(Canvas canvas)</span> &#123;</span><br><span class="line">    <span class="built_in">super</span>.onDraw(canvas);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>它的作用是：<strong>提供一个 <code>Canvas</code> (画布) 对象，让你可以在上面进行任意的 2D 绘制操作。</strong></p><h5 id="核心工具"><a class="markdownIt-Anchor" href="#核心工具"></a> 核心工具</h5><p>要进行绘制，通常需要两个核心类的配合：</p><ol><li><strong><code>Canvas</code> (画布)</strong><ul><li><code>onDraw</code> 方法会传给你一个 <code>Canvas</code> 对象。你可以把它想象成一块指定了尺寸和位置的画布。</li><li>它提供了一系列 <code>draw...()</code> 方法，比如：<ul><li><code>drawRect(float left, float top, float right, float bottom, Paint paint)</code>: 绘制矩形。</li><li><code>drawCircle(float cx, float cy, float radius, Paint paint)</code>: 绘制圆形。</li><li><code>drawLine(float startX, float startY, float stopX, float stopY, Paint paint)</code>: 绘制线条。</li><li><code>drawText(String text, float x, float y, Paint paint)</code>: 绘制文本。</li><li><code>drawBitmap(Bitmap bitmap, float left, float top, Paint paint)</code>: 绘制位图 (Bitmap)。</li><li><code>drawPath(Path path, Paint paint)</code>: 绘制自定义路径。</li></ul></li><li><code>Canvas</code> 定义了 <strong>“画什么”</strong> 和 <strong>“在哪里画”</strong>。</li></ul></li><li><strong><code>Paint</code> (画笔)</strong><ul><li><code>Paint</code> 对象定义了绘制的样式和效果。你可以把它想象成一支画笔。</li><li>它控制着各种属性，比如：<ul><li><code>setColor(int color)</code>: 设置颜色。</li><li><code>setStyle(Paint.Style style)</code>: 设置样式（<code>FILL</code> 填充、<code>STROKE</code> 描边、<code>FILL_AND_STROKE</code> 填充并描边）。</li><li><code>setStrokeWidth(float width)</code>: 设置线条或描边的宽度。</li><li><code>setTextSize(float textSize)</code>: 设置文本大小。</li><li><code>setAntiAlias(boolean aa)</code>: 设置是否开启抗锯齿，能让图形边缘更平滑。</li></ul></li><li><code>Paint</code> 定义了 <strong>“怎么画”</strong> (颜色、粗细、样式等)。</li></ul></li></ol><p><strong>坐标系</strong>：在 <code>onDraw</code> 方法中，画布 <code>Canvas</code> 的坐标系原点 <code>(0, 0)</code> 位于当前 <code>View</code> 的 <strong>左上角</strong>。</p><hr /><h4 id="自定义-view的-ondraw"><a class="markdownIt-Anchor" href="#自定义-view的-ondraw"></a> 自定义 View的 onDraw</h4><p>对于一个不包含子视图的自定义 <code>View</code>，<code>onDraw</code> 是它的灵魂。这是你将所有视觉元素呈现给用户的地方。</p><p><strong>核心职责：</strong> 使用 <code>Canvas</code> 和 <code>Paint</code> 对象，在 <code>View</code> 的边界内绘制出你想要的任何内容。</p><p><strong>基本步骤：</strong></p><ol><li><strong>初始化 <code>Paint</code> 对象</strong>：<strong>这是一个至关重要的性能优化点</strong>。<code>onDraw</code> 方法会被频繁调用（例如，在动画期间每秒调用 60 次）。如果在 <code>onDraw</code> 内部创建 <code>Paint</code> 等对象，会引发大量的内存分配和垃圾回收（GC），导致界面卡顿。因此，<strong>务必在构造函数或其他一次性初始化方法中创建 <code>Paint</code> 对象</strong>。</li><li>重写 <code>onDraw(Canvas canvas)</code> 方法。</li><li>在 <code>onDraw</code> 内部，你可以通过 <code>getWidth()</code> 和 <code>getHeight()</code> 获取当前 <code>View</code> 的最终宽高。</li><li>使用 <code>canvas.draw...()</code> 方法和预先初始化好的 <code>Paint</code> 对象进行绘制。</li></ol><h5 id="代码示例一个绘制了背景和圆形的-squareview"><a class="markdownIt-Anchor" href="#代码示例一个绘制了背景和圆形的-squareview"></a> 代码示例：一个绘制了背景和圆形的 SquareView</h5><p>我们继续之前的 <code>SquareView</code> 例子，让它不再是透明的，而是在内部绘制内容。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SquareView</span> <span class="keyword">extends</span> <span class="title class_">View</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Paint backgroundPaint;</span><br><span class="line">    <span class="keyword">private</span> Paint circlePaint;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">SquareView</span><span class="params">(Context context, <span class="meta">@Nullable</span> AttributeSet attrs)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(context, attrs);</span><br><span class="line">        initPaints();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 在构造函数中初始化画笔</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">initPaints</span><span class="params">()</span> &#123;</span><br><span class="line">        backgroundPaint = <span class="keyword">new</span> <span class="title class_">Paint</span>();</span><br><span class="line">        backgroundPaint.setColor(Color.parseColor(<span class="string">&quot;#8888FF&quot;</span>)); <span class="comment">// 淡蓝色背景</span></span><br><span class="line">        backgroundPaint.setStyle(Paint.Style.FILL); <span class="comment">// 填充样式</span></span><br><span class="line"></span><br><span class="line">        circlePaint = <span class="keyword">new</span> <span class="title class_">Paint</span>(Paint.ANTI_ALIAS_FLAG); <span class="comment">// 创建时就开启抗锯齿</span></span><br><span class="line">        circlePaint.setColor(Color.RED);</span><br><span class="line">        circlePaint.setStyle(Paint.Style.FILL);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onMeasure</span><span class="params">(<span class="type">int</span> widthMeasureSpec, <span class="type">int</span> heightMeasureSpec)</span> &#123;</span><br><span class="line">        <span class="comment">// ... onMeasure 逻辑保持不变 ...</span></span><br><span class="line">        <span class="comment">// 假设它最终测量自己为一个正方形</span></span><br><span class="line">        <span class="built_in">super</span>.onMeasure(widthMeasureSpec, heightMeasureSpec); <span class="comment">// 简化处理</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onDraw</span><span class="params">(Canvas canvas)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onDraw(canvas); <span class="comment">// 绘制 View 自身的背景等</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 获取 View 的尺寸</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">width</span> <span class="operator">=</span> getWidth();</span><br><span class="line">        <span class="type">int</span> <span class="variable">height</span> <span class="operator">=</span> getHeight();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 3. 绘制背景</span></span><br><span class="line">        <span class="comment">// 绘制一个覆盖整个 View 的矩形</span></span><br><span class="line">        canvas.drawRect(<span class="number">0</span>, <span class="number">0</span>, width, height, backgroundPaint);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 在中心绘制一个红色的圆</span></span><br><span class="line">        <span class="type">float</span> <span class="variable">centerX</span> <span class="operator">=</span> width / <span class="number">2f</span>;</span><br><span class="line">        <span class="type">float</span> <span class="variable">centerY</span> <span class="operator">=</span> height / <span class="number">2f</span>;</span><br><span class="line">        <span class="type">float</span> <span class="variable">radius</span> <span class="operator">=</span> Math.min(width, height) / <span class="number">4f</span>; <span class="comment">// 半径为宽/高中较小值的 1/4</span></span><br><span class="line">        canvas.drawCircle(centerX, centerY, radius, circlePaint);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h4 id="自定义-viewgroup-的-ondraw"><a class="markdownIt-Anchor" href="#自定义-viewgroup-的-ondraw"></a> 自定义 ViewGroup 的 onDraw</h4><p>默认情况下，<code>ViewGroup</code> 是不会调用 <code>onDraw</code> 方法的。</p><p><strong>为什么？</strong></p><p>因为 <code>ViewGroup</code> 的主要职责是管理和布局子 <code>View</code>，它本身通常被认为是透明的、不可见的容器。为了进行性能优化，系统假设它不需要绘制任何内容，从而跳过它的 <code>onDraw</code> 流程。子 <code>View</code> 的绘制是由系统在 <code>dispatchDraw</code> 方法中管理的。</p><p><strong>如何让 <code>ViewGroup</code> 调用 <code>onDraw</code>？</strong></p><p>如果你希望 <code>ViewGroup</code> 也绘制一些内容（例如，一个特殊的背景、子 <code>View</code> 之间的分割线），你必须明确告诉系统。有两种常用方法：</p><ol><li><p><strong>在构造函数中调用 <code>setWillNotDraw(false)</code></strong> <code>ViewGroup</code> 内部有一个标志位，默认为 <code>true</code>，表示“我不会绘制任何东西”。将其设置为 <code>false</code> 后，系统在绘制流程中就会调用该 <code>ViewGroup</code> 的 <code>onDraw</code> 方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="title function_">CustomVerticalLayout</span><span class="params">(Context context, AttributeSet attrs)</span> &#123;</span><br><span class="line">    <span class="built_in">super</span>(context, attrs);</span><br><span class="line">    <span class="comment">// 告诉系统，这个 ViewGroup 需要执行 onDraw</span></span><br><span class="line">    setWillNotDraw(<span class="literal">false</span>); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>在 XML 中设置 <code>android:background</code> 属性</strong> 只要你为 <code>ViewGroup</code> 设置了任何背景（无论是颜色还是 <code>Drawable</code>），系统会自动为你调用 <code>setWillNotDraw(false)</code>，因为绘制背景本身就需要 <code>onDraw</code> 流程的参与。</p></li></ol><h4 id="viewgroup-的绘制顺序"><a class="markdownIt-Anchor" href="#viewgroup-的绘制顺序"></a> ViewGroup 的绘制顺序</h4><p>当一个 <code>ViewGroup</code> 需要绘制时，其绘制层级如下：</p><ol><li><strong>绘制背景</strong>: 如果设置了 <code>background</code>，先绘制它。</li><li><strong>绘制自身内容 (<code>onDraw</code>)</strong>: 调用 <code>onDraw</code> 方法，绘制你在其中指定的任何内容（例如分割线）。</li><li><strong>绘制子视图 (<code>dispatchDraw</code>)</strong>: <code>onDraw</code> 执行完毕后，系统会调用 <code>dispatchDraw(Canvas canvas)</code> 方法。这个方法会遍历所有子 <code>View</code>，并调用每个子 <code>View</code> 的 <code>draw()</code> 方法，从而让子 <code>View</code> 绘制自己。</li><li><strong>绘制前景和滚动条</strong>: 最后，绘制滑动边缘效果、滚动条和前景（<code>Foreground</code>）。</li></ol><p>这意味着，在 <code>onDraw</code> 中绘制的内容会被 **“压在”**所有子 <code>View</code> 的 <strong>下面</strong>。如果你想在子 <code>View</code> 的上面绘制内容，你需要重写 <code>dispatchDraw</code> 方法，并在调用 <code>super.dispatchDraw(canvas)</code> 之后进行绘制。</p><hr /><h4 id="代码示例继续完善-customverticallayout-2"><a class="markdownIt-Anchor" href="#代码示例继续完善-customverticallayout-2"></a> 代码示例：继续完善 CustomVerticalLayout</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CustomVerticalLayout</span> <span class="keyword">extends</span> <span class="title class_">ViewGroup</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 定义一个画笔用于绘制分割线</span></span><br><span class="line">    <span class="keyword">private</span> Paint mDividerPaint;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">CustomVerticalLayout</span><span class="params">(Context context, AttributeSet attrs)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(context, attrs);</span><br><span class="line">        init();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">init</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 2. 初始化画笔</span></span><br><span class="line">        mDividerPaint = <span class="keyword">new</span> <span class="title class_">Paint</span>(Paint.ANTI_ALIAS_FLAG);</span><br><span class="line">        mDividerPaint.setColor(Color.RED);</span><br><span class="line">        mDividerPaint.setStyle(Paint.Style.STROKE);</span><br><span class="line">        mDividerPaint.setStrokeWidth(<span class="number">4</span>); <span class="comment">// 设置分割线厚度为 4px</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 【关键】告诉系统这个 ViewGroup 需要执行 onDraw 方法</span></span><br><span class="line">        setWillNotDraw(<span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// onMeasure 和 onLayout 方法与之前的例子完全相同...</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onMeasure</span><span class="params">(<span class="type">int</span> widthMeasureSpec, <span class="type">int</span> heightMeasureSpec)</span> &#123;</span><br><span class="line">        <span class="comment">// ... 此处省略 onMeasure 的完整代码 ...</span></span><br><span class="line">        <span class="comment">// 关键是调用 measureChildWithMargins() 或者类似逻辑</span></span><br><span class="line">        <span class="comment">// 并最终调用 setMeasuredDimension()</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onLayout</span><span class="params">(<span class="type">boolean</span> changed, <span class="type">int</span> l, <span class="type">int</span> t, <span class="type">int</span> r, <span class="type">int</span> b)</span> &#123;</span><br><span class="line">        <span class="comment">// ... 此处省略 onLayout 的完整代码 ...</span></span><br><span class="line">        <span class="comment">// 关键是循环调用 child.layout()</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. 实现 onDraw 方法来绘制内容</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onDraw</span><span class="params">(Canvas canvas)</span> &#123;</span><br><span class="line">        <span class="comment">// 先调用父类的方法，这是一个好习惯</span></span><br><span class="line">        <span class="built_in">super</span>.onDraw(canvas);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 5. 遍历子视图，在它们之间画线</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; getChildCount() - <span class="number">1</span>; i++) &#123; <span class="comment">// 注意是 getChildCount() - 1</span></span><br><span class="line">            <span class="type">View</span> <span class="variable">child</span> <span class="operator">=</span> getChildAt(i);</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 如果子视图是 GONE，则跳过</span></span><br><span class="line">            <span class="keyword">if</span> (child.getVisibility() == GONE) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 6. 计算分割线的 Y 坐标</span></span><br><span class="line">            <span class="type">MarginLayoutParams</span> <span class="variable">lp</span> <span class="operator">=</span> (MarginLayoutParams) child.getLayoutParams();</span><br><span class="line">            <span class="comment">// 将线画在子视图底部和其 bottomMargin 之间的区域</span></span><br><span class="line">            <span class="type">float</span> <span class="variable">dividerY</span> <span class="operator">=</span> child.getBottom() + lp.bottomMargin / <span class="number">2.0f</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 7. 计算分割线的 X 坐标范围</span></span><br><span class="line">            <span class="type">float</span> <span class="variable">startX</span> <span class="operator">=</span> getPaddingLeft();</span><br><span class="line">            <span class="type">float</span> <span class="variable">stopX</span> <span class="operator">=</span> getWidth() - getPaddingRight();</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 8. 【关键】使用 canvas 画线</span></span><br><span class="line">            canvas.drawLine(startX, dividerY, stopX, dividerY, mDividerPaint);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">     <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">dispatchDraw</span><span class="params">(Canvas canvas)</span> &#123;</span><br><span class="line">        <span class="comment">// 首先，让系统完成所有子视图的绘制</span></span><br><span class="line">        <span class="built_in">super</span>.dispatchDraw(canvas);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 然后，在所有子视图都画完之后，我们再画上我们的顶层内容</span></span><br><span class="line">        <span class="keyword">if</span> (mSelectedIndex &gt;= <span class="number">0</span> &amp;&amp; mSelectedIndex &lt; getChildCount()) &#123;</span><br><span class="line">            <span class="type">View</span> <span class="variable">selectedChild</span> <span class="operator">=</span> getChildAt(mSelectedIndex);</span><br><span class="line">            <span class="keyword">if</span> (selectedChild != <span class="literal">null</span> &amp;&amp; selectedChild.getVisibility() != GONE) &#123;</span><br><span class="line">                <span class="comment">// 获取选中子视图的边界</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">left</span> <span class="operator">=</span> selectedChild.getLeft();</span><br><span class="line">                <span class="type">int</span> <span class="variable">top</span> <span class="operator">=</span> selectedChild.getTop();</span><br><span class="line">                <span class="type">int</span> <span class="variable">right</span> <span class="operator">=</span> selectedChild.getRight();</span><br><span class="line">                <span class="type">int</span> <span class="variable">bottom</span> <span class="operator">=</span> selectedChild.getBottom();</span><br><span class="line">                <span class="comment">// 在其上层绘制一个半透明的红色矩形作为遮罩</span></span><br><span class="line">                canvas.drawRect(left, top, right, bottom, mOverlayPaint);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ... generateLayoutParams 方法 ...</span></span><br><span class="line">    <span class="comment">// 为了能正确处理 margin，需要重写 generateLayoutParams 相关方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> LayoutParams <span class="title function_">generateLayoutParams</span><span class="params">(AttributeSet attrs)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">MarginLayoutParams</span>(getContext(), attrs);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ... 其他 generateLayoutParams 重载 ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中：</p><ul><li><strong><code>Paint mDividerPaint</code></strong>: 我们首先定义了一个 <code>Paint</code> 对象作为成员变量。这个“画笔”将专门用来绘制我们的分割线。</li><li><strong><code>init()</code> 方法</strong>: 我们在构造函数中调用 <code>init()</code> 来进行初始化。将初始化逻辑集中在一起是个好习惯。在这里，我们创建了 <code>Paint</code> 对象并设置了它的颜色、样式（描边）和粗细。</li><li><strong><code>setWillNotDraw(false)</code></strong>: 这是让 <code>ViewGroup</code> 能够绘制的关键一步。默认情况下，为了优化性能，<code>ViewGroup</code> 不会调用 <code>onDraw</code> 方法。我们通过这个调用来告诉系统：“这个 <code>ViewGroup</code> 有自己的内容需要绘制，请调用我的 <code>onDraw</code> 方法”。</li><li><strong><code>onDraw(Canvas canvas)</code></strong>: 这是实际进行绘制的地方。系统会传入一个 <code>Canvas</code>（画布）对象，我们所有的绘制操作都在这个画布上进行。</li><li><strong>遍历子视图</strong>: 我们使用一个 <code>for</code> 循环来遍历子视图。请注意循环条件是 <code>i &lt; getChildCount() - 1</code>，因为我们只需要在子视图“之间”画线，所以最后一个子视图下面不需要再画。</li><li><strong>计算Y坐标</strong>: 我们需要确定水平分割线应该画在哪里。一个比较好的位置是在当前子视图的底部（<code>child.getBottom()</code>）与其下外边距（<code>lp.bottomMargin</code>）所构成的空间内。这里我们取其中点，使得视觉上更均衡。</li><li><strong>计算X坐标</strong>: 我们希望分割线能横跨整个 <code>ViewGroup</code> 的内容区域，所以线的起点是 <code>ViewGroup</code> 的左内边距（<code>getPaddingLeft()</code>），终点是 <code>ViewGroup</code> 的宽度减去右内边距（<code>getWidth() - getPaddingRight()</code>）。</li><li><strong><code>canvas.drawLine(...)</code></strong>: 最后，我们调用 <code>canvas</code> 的 <code>drawLine</code> 方法，传入计算好的起点和终点坐标，并指定使用我们预设好的 <code>mDividerPaint</code> 画笔，从而在屏幕上绘制出红色的分割线。</li></ul><h4 id="如何触发重绘"><a class="markdownIt-Anchor" href="#如何触发重绘"></a> 如何触发重绘</h4><p>当你的 <code>View</code> 的外观需要更新时（例如，用户点击后改变颜色），你需要通知系统重新绘制它。这通过调用 <code>invalidate()</code> 方法来完成。</p><ul><li><code>invalidate()</code>: 请求重绘当前 <code>View</code>。它会使 <code>View</code> 的 <code>onDraw</code> 方法在未来的某个时间点（通常是下一帧）被UI线程调用。它只会触发绘制流程，不会触发布局和测量。</li><li><code>postInvalidate()</code>: <code>invalidate()</code> 的线程安全版本。如果你在非 UI 线程想刷新 <code>View</code>，应调用此方法。</li></ul><p><strong>再次对比 <code>requestLayout()</code> 和 <code>invalidate()</code></strong></p><ul><li><strong><code>requestLayout()</code></strong>: 当 <code>View</code> 的 <strong>尺寸或位置</strong> 可能发生变化时调用。它会触发 <code>onMeasure</code>, <code>onLayout</code>, <code>onDraw</code> 的完整流程。这是一个重量级操作。</li><li><strong><code>invalidate()</code></strong>: 当 <code>View</code> 的 <strong>外观</strong> 发生变化，但尺寸和位置不变时调用。它只触发 <code>onDraw</code>。这是一个相对轻量级的操作。</li></ul><h4 id="总结与关键点-3"><a class="markdownIt-Anchor" href="#总结与关键点-3"></a> 总结与关键点</h4><ol><li><strong><code>onDraw</code> 是绘制流程的最后一步，负责将视图渲染到屏幕上。</strong></li><li><strong>自定义 <code>View</code></strong> 的核心就是实现 <code>onDraw</code>，使用 <code>Canvas</code>(画什么,在哪画) 和 <code>Paint</code>(怎么画) 来创造视觉内容。</li><li><strong>自定义 <code>ViewGroup</code></strong> 默认不执行 <code>onDraw</code>。若要绘制，需调用 <code>setWillNotDraw(false)</code> 或设置背景。<code>ViewGroup</code> 的 <code>onDraw</code> 内容会显示在所有子 <code>View</code> 的下方。</li><li><strong>性能至上</strong>: <strong>绝对不要</strong> 在 <code>onDraw</code> 方法中创建新对象（<code>new Paint()</code>, <code>new Path()</code> 等），应在构造函数中预先创建并复用。</li><li><strong>触发重绘</strong>: 使用 <code>invalidate()</code> 或 <code>postInvalidate()</code> 来请求系统重新调用 <code>onDraw</code>。</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;自定义View是一个综合的技术体系,他涉及View的层次结构,事件分发机制和View的工作原理等技术细节.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;自定义View总体可以分为四类&lt;/em&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;继承View重写onDraw方法&lt;/li&gt;
&lt;li&gt;继承ViewGroup派</summary>
      
    
    
    
    
    <category term="Android" scheme="https://11111-beep.github.io/tags/Android/"/>
    
  </entry>
  
  <entry>
    <title>第一行代码-Kotlin</title>
    <link href="https://11111-beep.github.io/2025/10/06/%E7%AC%AC%E4%B8%80%E8%A1%8C%E4%BB%A3%E7%A0%81-Kotlin/"/>
    <id>https://11111-beep.github.io/2025/10/06/%E7%AC%AC%E4%B8%80%E8%A1%8C%E4%BB%A3%E7%A0%81-Kotlin/</id>
    <published>2025-10-06T09:20:39.000Z</published>
    <updated>2025-12-14T09:24:21.733Z</updated>
    
    <content type="html"><![CDATA[<h1 id="kfc疯狂星期四"><a class="markdownIt-Anchor" href="#kfc疯狂星期四"></a> <strong>KFC疯狂星期四</strong></h1><h2 id="1探究activity"><a class="markdownIt-Anchor" href="#1探究activity"></a> 1.探究Activity</h2><h3 id="在activity中使用toast"><a class="markdownIt-Anchor" href="#在activity中使用toast"></a> 在Activity中使用Toast</h3><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> button3 = findViewById&lt;Button&gt;(R.id.button3)</span><br><span class="line">        button3.setOnClickListener &#123;</span><br><span class="line">            Toast.makeText(<span class="keyword">this</span>, <span class="string">&quot;ThirdActivity&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure><h3 id="viewbinding的使用"><a class="markdownIt-Anchor" href="#viewbinding的使用"></a> ViewBinding的使用</h3><ol><li>在<code>build.gradle.kts</code>导入<code>ViewBinding</code></li><li>在活动中导入自动生成的 <code>ViewBinding </code>类并对其进行声明(根据活动)</li><li>绑定布局</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">android &#123;</span><br><span class="line">    <span class="comment">// 启用 ViewBinding</span></span><br><span class="line">    viewBinding &#123;</span><br><span class="line">        enable = <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> com.example.activitytest.databinding.FirstLayoutBinding  <span class="comment">// 自动生成的 ViewBinding 类(根据该活动)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">FirstActivity</span> : <span class="type">BaseActivity</span>() &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> binding: FirstLayoutBinding  <span class="comment">// 声明 ViewBinding 变量</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> TAG = <span class="string">&quot;FirstActivity&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        Log.d(TAG, <span class="string">&quot;Task id is <span class="variable">$taskId</span>&quot;</span>)</span><br><span class="line">        <span class="comment">// 绑定布局</span></span><br><span class="line">        binding = FirstLayoutBinding.inflate(layoutInflater)</span><br><span class="line">        setContentView(binding.root)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置按钮点击事件</span></span><br><span class="line">        binding.button1.setOnClickListener &#123;</span><br><span class="line">          </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h3 id="在布局中直接调用图像"><a class="markdownIt-Anchor" href="#在布局中直接调用图像"></a> 在布局中直接调用图像</h3><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">android:src=&quot;@android:drawable/ic_menu_call&quot;</span><br><span class="line">app:srcCompat=&quot;@android:drawable/ic_menu_edit&quot;</span><br></pre></td></tr></table></figure><h3 id="在活动中使用menu"><a class="markdownIt-Anchor" href="#在活动中使用menu"></a> 在活动中使用Menu</h3><ol><li>在<code>res</code>目录下新建<code>menu</code>文件夹并生成<code>main</code>文件</li><li>在活动中重写<code>onCreateOptionsMenu()</code>方法与<code>onOptionsItemSelected()</code>方法</li><li>修改主题</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreateOptionsMenu</span><span class="params">(menu: <span class="type">Menu</span>?)</span></span>: <span class="built_in">Boolean</span> &#123;</span><br><span class="line">        menuInflater.inflate(R.menu.main, menu)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onOptionsItemSelected</span><span class="params">(item: <span class="type">MenuItem</span>)</span></span>: <span class="built_in">Boolean</span> &#123;</span><br><span class="line">        <span class="keyword">when</span> (item.itemId) &#123;</span><br><span class="line">            R.id.add_item -&gt; Toast.makeText(<span class="keyword">this</span>, <span class="string">&quot;You clicked Add&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">            R.id.remove_item -&gt; Toast.makeText(<span class="keyword">this</span>, <span class="string">&quot;You clicked Remove&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">&lt;resources xmlns:tools=<span class="string">&quot;http://schemas.android.com/tools&quot;</span>&gt;</span><br><span class="line">    &lt;!-- Base application theme. --&gt;</span><br><span class="line">    &lt;style name=<span class="string">&quot;Theme.ActivityTest&quot;</span> parent=<span class="string">&quot;Theme.MaterialComponents.DayNight.DarkActionBar&quot;</span>&gt;</span><br><span class="line">        &lt;!-- Primary brand color. --&gt;</span><br><span class="line">        &lt;item name=<span class="string">&quot;colorPrimary&quot;</span>&gt;#FDD3E1&lt;/item&gt;</span><br><span class="line">        &lt;item name=<span class="string">&quot;colorPrimaryVariant&quot;</span>&gt;#EBB7C9&lt;/item&gt;</span><br><span class="line">        &lt;item name=<span class="string">&quot;colorOnPrimary&quot;</span>&gt;#F8EBEF&lt;/item&gt;</span><br><span class="line">        &lt;!-- Secondary brand color. --&gt;</span><br><span class="line">        &lt;item name=<span class="string">&quot;colorSecondary&quot;</span>&gt;#E2B5C4&lt;/item&gt;</span><br><span class="line">        &lt;item name=<span class="string">&quot;colorSecondaryVariant&quot;</span>&gt;#F29AB8&lt;/item&gt;</span><br><span class="line">        &lt;item name=<span class="string">&quot;colorOnSecondary&quot;</span>&gt;#D95D88&lt;/item&gt;</span><br><span class="line">        &lt;!-- Status bar color. --&gt;</span><br><span class="line">        &lt;item name=<span class="string">&quot;android:statusBarColor&quot;</span>&gt;?attr/colorPrimaryVariant&lt;/item&gt;</span><br><span class="line">        &lt;!-- Customize your theme here. --&gt;</span><br><span class="line">    &lt;/style&gt;</span><br><span class="line">&lt;/resources&gt;</span><br></pre></td></tr></table></figure><h3 id="使用intent在activity之间穿梭"><a class="markdownIt-Anchor" href="#使用intent在activity之间穿梭"></a> 使用Intent在Activity之间穿梭</h3><h4 id="显式intent"><a class="markdownIt-Anchor" href="#显式intent"></a> 显式Intent</h4><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">binding.button1.setOnClickListener &#123;</span><br><span class="line">           <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, SecondActivity::<span class="keyword">class</span>.java)</span><br><span class="line">           startActivity(intent)</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure><h4 id="隐式intent指定对应action和category等信息去响应"><a class="markdownIt-Anchor" href="#隐式intent指定对应action和category等信息去响应"></a> 隐式Intent(指定对应action和category等信息去响应)</h4><ol><li>指定当前<code>Activity</code>能够响应的<code>action</code>和<code>category</code></li><li>调用<code>addCategory()</code>方法添加一个category</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">&lt;activity</span><br><span class="line">            android:name=<span class="string">&quot;.SecondActivity&quot;</span></span><br><span class="line">            android:launchMode=<span class="string">&quot;singleInstance&quot;</span></span><br><span class="line">            android:exported=<span class="string">&quot;false&quot;</span>&gt;</span><br><span class="line">            &lt;intent-filter&gt;</span><br><span class="line">                &lt;action android:name=<span class="string">&quot;com.example.activitytest.ACTION_START&quot;</span> /&gt;</span><br><span class="line">                &lt;category android:name=<span class="string">&quot;android.intent.category.DEFAULT&quot;</span> /&gt;</span><br><span class="line">                &lt;category android:name=<span class="string">&quot;com.example.activitytest.MY_CATEGORY&quot;</span> /&gt;</span><br><span class="line">            &lt;/intent-filter&gt;</span><br><span class="line">        &lt;/activity&gt;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">binding.button1.setOnClickListener &#123;</span><br><span class="line">          <span class="keyword">val</span> intent = Intent(<span class="string">&quot;com.example.activitytest.ACTION_START&quot;</span>)</span><br><span class="line">          intent.addCategory(<span class="string">&quot;com.example.activitytest.MY_CATEGORY&quot;</span>)</span><br><span class="line">          startActivity(intent)</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure><p><strong>打开百度</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">binding.button1.setOnClickListener &#123;</span><br><span class="line">          <span class="keyword">val</span> intent = Intent(Intent.ACTION_VIEW)</span><br><span class="line">          intent.<span class="keyword">data</span> = Uri.parse(<span class="string">&quot;https://www.baidu.com&quot;</span>)</span><br><span class="line">          startActivity(intent)</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure><p><strong>打开电话</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">binding.button1.setOnClickListener &#123;</span><br><span class="line">           <span class="keyword">val</span> intent = Intent(Intent.ACTION_DIAL)</span><br><span class="line">           intent.<span class="keyword">data</span> = Uri.parse(<span class="string">&quot;tel:10086&quot;</span>)</span><br><span class="line">           startActivity(intent)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h4 id="向下一个activity传递数据"><a class="markdownIt-Anchor" href="#向下一个activity传递数据"></a> 向下一个Activity传递数据</h4><p>运用<code>putExtra()</code>方法的重载</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">binding.button1.setOnClickListener &#123;</span><br><span class="line">           <span class="keyword">val</span> <span class="keyword">data</span> = <span class="string">&quot;Hello SecondActivity&quot;</span></span><br><span class="line">           <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, SecondActivity::<span class="keyword">class</span>.java)</span><br><span class="line">           intent.putExtra(<span class="string">&quot;extra_data&quot;</span>, <span class="keyword">data</span>) <span class="comment">// 第一个参数是键,第二个是要传递的数据</span></span><br><span class="line">           startActivity(intent)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.second_layout)</span><br><span class="line">        <span class="keyword">val</span> extraData = intent.getStringExtra(<span class="string">&quot;extra_data&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="返回数据给上一个activity"><a class="markdownIt-Anchor" href="#返回数据给上一个activity"></a> 返回数据给上一个Activity</h4><ol><li>用<code>startActivityForResult()</code>启动活动</li><li>用<code>setResult()</code>方法向上一个Activity返回数据</li><li>在上一个活动重写<code>onActivityResult</code>方法</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">binding.button1.setOnClickListener &#123;</span><br><span class="line">           <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, SecondActivity::<span class="keyword">class</span>.java)</span><br><span class="line">           startActivityForResult(intent, <span class="number">1</span>) <span class="comment">// 第二个是请求码</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">SecondActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> binding: SecondActivityBinding  <span class="comment">// 使用生成的绑定类</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> TAG = <span class="string">&quot;SecondActivity&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        Log.d(TAG, <span class="string">&quot;Task id is <span class="variable">$taskId</span>&quot;</span>)</span><br><span class="line">        <span class="comment">// 绑定布局</span></span><br><span class="line">        binding = SecondActivityBinding.inflate(layoutInflater)</span><br><span class="line">        setContentView(binding.root)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 使用 binding.button2 访问视图（确保在布局中存在 id 为 button2 的控件）</span></span><br><span class="line">        binding.button2.setOnClickListener &#123;</span><br><span class="line">           <span class="keyword">val</span> intent = Intent().apply &#123;</span><br><span class="line">                putExtra(<span class="string">&quot;data_return&quot;</span>, <span class="string">&quot;Hello FirstActivity&quot;</span>)</span><br><span class="line">            &#125;</span><br><span class="line">            setResult(RESULT_OK, intent)</span><br><span class="line">            finish()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onActivityResult</span><span class="params">(requestCode: <span class="type">Int</span>, resultCode: <span class="type">Int</span>, <span class="keyword">data</span>: <span class="type">Intent</span>?)</span></span> &#123;</span><br><span class="line">    <span class="comment">// 首先调用父类的onActivityResult方法，确保默认行为得到执行</span></span><br><span class="line">    <span class="keyword">super</span>.onActivityResult(requestCode, resultCode, <span class="keyword">data</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 根据requestCode判断是哪个Activity返回的结果</span></span><br><span class="line">    <span class="keyword">when</span> (requestCode) &#123;</span><br><span class="line">        <span class="comment">// requestCode为1时，表示是特定的Activity返回结果</span></span><br><span class="line">        <span class="number">1</span> -&gt; &#123;</span><br><span class="line">            <span class="comment">// 如果返回结果为成功（RESULT_OK）</span></span><br><span class="line">            <span class="keyword">if</span> (resultCode == RESULT_OK) &#123;</span><br><span class="line">                <span class="comment">// 从Intent中获取返回的字符串数据，key为&quot;data_return&quot;</span></span><br><span class="line">                <span class="keyword">val</span> returnedData = <span class="keyword">data</span>?.getStringExtra(<span class="string">&quot;data_return&quot;</span>)</span><br><span class="line">                </span><br><span class="line">                <span class="comment">// 使用Log.d输出日志，打印返回的数据，TAG为类标识</span></span><br><span class="line">                Log.d(TAG, <span class="string">&quot;returned data is <span class="variable">$returnedData</span>&quot;</span>)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="activity的生命周期"><a class="markdownIt-Anchor" href="#activity的生命周期"></a> Activity的生命周期</h3><h4 id="1-oncreate"><a class="markdownIt-Anchor" href="#1-oncreate"></a> 1. onCreate()</h4><ul><li><p>作用</p><p>：当 Activity 第一次被创建时调用。在这个方法中，你通常会完成以下工作：</p><ul><li>设置布局文件（例如调用 <code>setContentView()</code>）</li><li>初始化控件、变量和数据</li><li>配置基本组件（如 Toolbar、Fragment 等）</li></ul></li><li><p>示例代码</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">    <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">    setContentView(R.layout.activity_main)</span><br><span class="line">    <span class="comment">// 初始化操作</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h4 id="2-onstart"><a class="markdownIt-Anchor" href="#2-onstart"></a> 2. onStart()</h4><ul><li><p><strong>作用</strong>：在 <code>onCreate()</code> 之后调用，此时 Activity 已经对用户可见，但还未获得焦点，也就是还不能进行用户交互。通常在这里启动一些即将进入前台的操作。</p></li><li><p>示例代码</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onStart</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">super</span>.onStart()</span><br><span class="line">    <span class="comment">// Activity 对用户可见，但尚未获得焦点</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h4 id="3-onresume"><a class="markdownIt-Anchor" href="#3-onresume"></a> 3. onResume()</h4><ul><li><p><strong>作用</strong>：在 <code>onStart()</code> 之后调用，此时 Activity 已经位于前台，并且可以接收用户的输入和交互。这也是应用处于“运行”状态的主要阶段。</p></li><li><p>示例代码</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onResume</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">super</span>.onResume()</span><br><span class="line">    <span class="comment">// 开始处理用户交互，如启动动画、注册传感器等</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h4 id="4-onpause"><a class="markdownIt-Anchor" href="#4-onpause"></a> 4. onPause()</h4><ul><li><p><strong>作用</strong>：当系统准备启动或恢复另一个 Activity 时调用，此时当前 Activity 仍然部分可见，但失去了用户焦点。通常在这里进行轻量级的保存工作，比如暂停动画、保存数据或释放一些占用较多资源的操作。</p></li><li><p><strong>注意</strong>：此方法的执行时间应该尽可能短，以免阻塞其他 Activity 的启动。</p></li><li><p>示例代码</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onPause</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">super</span>.onPause()</span><br><span class="line">    <span class="comment">// 暂停动画、保存数据、释放资源等</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h4 id="5-onstop"><a class="markdownIt-Anchor" href="#5-onstop"></a> 5. onStop()</h4><ul><li><p><strong>作用</strong>：当 Activity 完全对用户不可见时调用，可能是因为新的 Activity 覆盖在上面或 Activity 被销毁。通常在这里释放不再需要的资源，并执行较重的保存工作。</p></li><li><p>示例代码</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onStop</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">super</span>.onStop()</span><br><span class="line">    <span class="comment">// 释放资源、保存数据等</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h4 id="6-ondestroy"><a class="markdownIt-Anchor" href="#6-ondestroy"></a> 6. onDestroy()</h4><ul><li><p><strong>作用</strong>：在 Activity 被完全销毁之前调用。这可能是因为用户主动关闭 Activity 或系统由于资源不足而销毁 Activity。在此方法中需要清理所有占用的资源，确保内存没有泄漏。</p></li><li><p>示例代码</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">super</span>.onDestroy()</span><br><span class="line">    <span class="comment">// 清理资源、取消注册监听器等</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h4 id="7-onrestart"><a class="markdownIt-Anchor" href="#7-onrestart"></a> 7. onRestart()</h4><ul><li><p><strong>作用</strong>：当一个已停止的 Activity 将要重新启动时调用，此方法紧接在 <code>onStop()</code> 之后调用，然后进入 <code>onStart()</code>。常用于重新初始化在 <code>onStop()</code> 中释放的资源或状态恢复。</p></li><li><p>示例代码</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onRestart</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">super</span>.onRestart()</span><br><span class="line">    <span class="comment">// 准备 Activity 重新对用户可见</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h4 id="生命周期的常见调用顺序"><a class="markdownIt-Anchor" href="#生命周期的常见调用顺序"></a> 生命周期的常见调用顺序</h4><ul><li><strong>初次启动 Activity</strong>：<br /><code>onCreate()</code> → <code>onStart()</code> → <code>onResume()</code></li><li><strong>用户离开 Activity（例如启动另一个 Activity 或按下 Home 键）</strong>：<br /><code>onPause()</code> → <code>onStop()</code></li><li><strong>用户返回到 Activity</strong>：<br /><code>onRestart()</code> → <code>onStart()</code> → <code>onResume()</code></li><li><strong>销毁 Activity</strong>：<br /><code>onPause()</code> → <code>onStop()</code> → <code>onDestroy()</code></li></ul><h3 id="如果activity被回收了怎么办"><a class="markdownIt-Anchor" href="#如果activity被回收了怎么办"></a> 如果Activity被回收了怎么办</h3><ol><li>使用<code>onSaveInstanceState</code>回调方法保存数据</li><li>在<code>onCreate()</code>方法里面对其进行恢复</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义一个需要保存的变量</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">var</span> counter: <span class="built_in">Int</span> = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果 savedInstanceState 不为 null，则恢复数据</span></span><br><span class="line">        savedInstanceState?.let &#123;</span><br><span class="line">            counter = it.getInt(<span class="string">&quot;counter_key&quot;</span>, <span class="number">0</span>)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 其他初始化代码</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在此回调中保存需要持久化的数据</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onSaveInstanceState</span><span class="params">(outState: <span class="type">Bundle</span>)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onSaveInstanceState(outState)</span><br><span class="line">        <span class="comment">// 将 counter 的值存入 Bundle 中</span></span><br><span class="line">        outState.putInt(<span class="string">&quot;counter_key&quot;</span>, counter)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="activity的启动方式"><a class="markdownIt-Anchor" href="#activity的启动方式"></a> Activity的启动方式</h3><h4 id="1-androidmanifestxml-配置"><a class="markdownIt-Anchor" href="#1-androidmanifestxml-配置"></a> 1. AndroidManifest.xml 配置</h4><p>在 <code>AndroidManifest.xml</code> 中，为每个 Activity 配置不同的启动模式：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">manifest</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">package</span>=<span class="string">&quot;com.example.launchmode&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">application</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:allowBackup</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:label</span>=<span class="string">&quot;@string/app_name&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:theme</span>=<span class="string">&quot;@style/AppTheme&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!-- Standard 模式：每次启动都会创建一个新实例 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">activity</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.StandardActivity&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:launchMode</span>=<span class="string">&quot;standard&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">&quot;android.intent.action.MAIN&quot;</span> /&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">category</span> <span class="attr">android:name</span>=<span class="string">&quot;android.intent.category.LAUNCHER&quot;</span> /&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">activity</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!-- SingleTop 模式：如果该 Activity 已经处于任务栈顶部，则复用当前实例，</span></span><br><span class="line"><span class="comment">             否则创建新实例 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">activity</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.SingleTopActivity&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:launchMode</span>=<span class="string">&quot;singleTop&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">activity</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!-- SingleTask 模式：系统保证全局只有一个该 Activity 实例，</span></span><br><span class="line"><span class="comment">             如果已存在，则清除它之上的 Activity，并通过 onNewIntent() 传递新的 Intent --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">activity</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.SingleTaskActivity&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:launchMode</span>=<span class="string">&quot;singleTask&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">activity</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!-- SingleInstance 模式：该 Activity 独占一个任务栈，</span></span><br><span class="line"><span class="comment">             栈中只有它自己，其他 Activity 将在其它任务栈中运行 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">activity</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.SingleInstanceActivity&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:launchMode</span>=<span class="string">&quot;singleInstance&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">activity</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;/<span class="name">application</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">manifest</span>&gt;</span></span><br></pre></td></tr></table></figure><h4 id="2-kotlin-代码示例与注释"><a class="markdownIt-Anchor" href="#2-kotlin-代码示例与注释"></a> 2. Kotlin 代码示例与注释</h4><h5 id="1-standardactivity"><a class="markdownIt-Anchor" href="#1-standardactivity"></a> (1) StandardActivity</h5><p><strong>说明</strong>：Standard 模式是 Android 的默认模式，每次启动该 Activity 都会新建一个实例，即使之前已有相同类型的 Activity 存在于任务栈中。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * StandardActivity 使用 standard 模式，</span></span><br><span class="line"><span class="comment"> * 每次启动都会创建一个新的实例。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">StandardActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_standard)</span><br><span class="line">        <span class="comment">// 此处执行初始化工作，每次启动该 Activity 都是一个全新的状态</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h5 id="2-singletopactivity"><a class="markdownIt-Anchor" href="#2-singletopactivity"></a> (2) SingleTopActivity</h5><p><strong>说明</strong>：SingleTop 模式下，如果目标 Activity 已经处于任务栈的顶部，则不会创建新的实例，而是调用 onNewIntent() 方法传递新的 Intent；如果不在栈顶，则会创建新实例。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * SingleTopActivity 使用 singleTop 模式，</span></span><br><span class="line"><span class="comment"> * 当该 Activity 已经在任务栈顶部时，新的启动请求不会创建新实例，</span></span><br><span class="line"><span class="comment"> * 而是通过 onNewIntent() 方法传递新的 Intent。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">SingleTopActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_single_top)</span><br><span class="line">        <span class="comment">// 初始化工作</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当 SingleTopActivity 已经在任务栈顶部时，</span></span><br><span class="line"><span class="comment">     * 再次启动该 Activity 会调用此方法，传入新的 Intent。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onNewIntent</span><span class="params">(intent: <span class="type">Intent</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onNewIntent(intent)</span><br><span class="line">        <span class="comment">// 处理新的 Intent，例如更新界面数据或状态</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h5 id="3-singletaskactivity"><a class="markdownIt-Anchor" href="#3-singletaskactivity"></a> (3) SingleTaskActivity</h5><p><strong>说明</strong>：SingleTask 模式确保在系统中只存在一个该 Activity 的实例。当再次启动时，如果实例已存在，系统会将其上面的所有 Activity 清除，并调用 onNewIntent() 方法传递 Intent。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * SingleTaskActivity 使用 singleTask 模式，</span></span><br><span class="line"><span class="comment"> * 系统中全局只有一个该 Activity 的实例。</span></span><br><span class="line"><span class="comment"> * 如果该实例已存在，再次启动时会清除它之上的所有 Activity，</span></span><br><span class="line"><span class="comment"> * 并通过 onNewIntent() 传递新的启动 Intent。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">SingleTaskActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_single_task)</span><br><span class="line">        <span class="comment">// 初始化代码</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当 SingleTaskActivity 已存在且收到新的启动请求时，</span></span><br><span class="line"><span class="comment">     * 系统会调用此方法，并传递新的 Intent。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onNewIntent</span><span class="params">(intent: <span class="type">Intent</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onNewIntent(intent)</span><br><span class="line">        <span class="comment">// 处理新的 Intent，例如刷新界面或更新数据</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h5 id="4-singleinstanceactivity"><a class="markdownIt-Anchor" href="#4-singleinstanceactivity"></a> (4) SingleInstanceActivity</h5><p><strong>说明</strong>：SingleInstance 模式是最严格的启动模式，该 Activity 所在的任务栈中只允许存在它本身，其它 Activity 会在其他任务栈中运行，从而实现高度隔离。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * SingleInstanceActivity 使用 singleInstance 模式，</span></span><br><span class="line"><span class="comment"> * 该模式下，该 Activity 独占一个任务栈，</span></span><br><span class="line"><span class="comment"> * 任务栈中只包含它自己，其他 Activity 将不会进入此任务栈。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">SingleInstanceActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_single_instance)</span><br><span class="line">        <span class="comment">// 初始化代码</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h4 id="3-总结"><a class="markdownIt-Anchor" href="#3-总结"></a> 3. 总结</h4><ul><li><strong>standard</strong><br />每次启动时都会创建新的 Activity 实例，适用于不需要保留之前状态的场景。</li><li><strong>singleTop</strong><br />如果目标 Activity 已位于任务栈顶部，则复用实例（调用 onNewIntent()），否则创建新实例。适合频繁接收新 Intent 的场景。</li><li><strong>singleTask</strong><br />保证全局只有一个实例，启动时会清除目标实例之上的所有 Activity，并调用 onNewIntent()。适用于需要全局唯一实例的核心界面。</li><li><strong>singleInstance</strong><br />该 Activity 独占任务栈，其他 Activity 不会与其共存。适用于需要高度隔离的场景，如系统级界面或特殊业务场景。</li></ul><h3 id="activity的最佳实践"><a class="markdownIt-Anchor" href="#activity的最佳实践"></a> Activity的最佳实践</h3><h4 id="知晓当前是在哪一个活动"><a class="markdownIt-Anchor" href="#知晓当前是在哪一个活动"></a> 知晓当前是在哪一个活动</h4><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">open</span> <span class="keyword">class</span> <span class="title class_">BaseActivity</span> : <span class="type">AppCompatActivity</span>()&#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        Log.d(<span class="string">&quot;BaseActivity&quot;</span>, javaClass.simpleName)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="随时随地退出程序"><a class="markdownIt-Anchor" href="#随时随地退出程序"></a> 随时随地退出程序</h4><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.activitytest</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> android.app.Activity</span><br><span class="line"></span><br><span class="line"><span class="keyword">object</span> ActivityCollector &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> activities = ArrayList&lt;Activity&gt;()</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">addActivity</span><span class="params">(activity: <span class="type">Activity</span>)</span></span> &#123;</span><br><span class="line">        activities.add(activity)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">removeActivity</span><span class="params">(activity: <span class="type">Activity</span>)</span></span> &#123;</span><br><span class="line">        activities.remove(activity)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">finishAll</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (activity <span class="keyword">in</span> activities) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!activity.isFinishing) &#123;</span><br><span class="line">                activity.finish()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        activities.clear()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">open</span> <span class="keyword">class</span> <span class="title class_">BaseActivity</span> : <span class="type">AppCompatActivity</span>()&#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        Log.d(<span class="string">&quot;BaseActivity&quot;</span>, javaClass.simpleName)</span><br><span class="line">        ActivityCollector.addActivity(<span class="keyword">this</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onDestroy()</span><br><span class="line">        ActivityCollector.removeActivity(<span class="keyword">this</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> button3 = findViewById&lt;Button&gt;(R.id.button3)</span><br><span class="line">        button3.setOnClickListener &#123;</span><br><span class="line">            ActivityCollector.finishAll()</span><br><span class="line">            android.os.Process.killProcess(android.os.Process.myPid())</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure><h4 id="启动activity的最佳写法"><a class="markdownIt-Anchor" href="#启动activity的最佳写法"></a> 启动Activity的最佳写法</h4><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 在要启动的活动内添加该静态方法</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">companion</span> <span class="keyword">object</span> &#123;</span><br><span class="line">    <span class="comment">// 封装启动 SecondActivity 的静态方法</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">actionStart</span><span class="params">(context: <span class="type">Context</span>, <span class="keyword">data</span>: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 创建一个 Intent 对象，指定从当前上下文启动 SecondActivity</span></span><br><span class="line">        <span class="keyword">val</span> intent = Intent(context, SecondActivity::<span class="keyword">class</span>.java)</span><br><span class="line">        <span class="comment">// 将传入的字符串数据作为额外参数存入 Intent，</span></span><br><span class="line">        <span class="comment">// &quot;extra_data&quot; 为键，用于在 SecondActivity 中提取该数据</span></span><br><span class="line">        intent.putExtra(<span class="string">&quot;extra_data&quot;</span>, <span class="keyword">data</span>)</span><br><span class="line">        <span class="comment">// 通过上下文启动 SecondActivity</span></span><br><span class="line">        context.startActivity(intent)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">binding.button1.setOnClickListener &#123;</span><br><span class="line">        SecondActivity.actionStart(<span class="keyword">this</span>, <span class="string">&quot;data&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="2ui开发"><a class="markdownIt-Anchor" href="#2ui开发"></a> 2.UI开发</h2><h3 id="布局"><a class="markdownIt-Anchor" href="#布局"></a> 布局</h3><h3 id="listview控件"><a class="markdownIt-Anchor" href="#listview控件"></a> ListView控件</h3><p>1.定义 <code>Fruit</code> 类，并在 <code>initFruits()</code> 方法中构建一个包含多个水果对象的数据列表。</p><p>2在 <code>activity_main.xml</code> 中定义 ListView，接着在 <code>MainActivity</code> 的 <code>onCreate()</code> 方法中设置布局、初始化数据、获取 ListView 控件。</p><p>3.实现 <code>FruitAdapter</code> 类，通过重写 <code>getView()</code> 方法和使用 ViewHolder 模式，将数据与列表项布局（fruit_item.xml）绑定，并返回每一项的视图。</p><p>4.在 <code>fruit_item.xml</code> 中定义每个列表项的视图结构，包括图片和文本。</p><p>5.在 <code>MainActivity</code> 中创建适配器、绑定 ListView 并设置点击事件，实现用户点击列表项时显示相应水果名称的 Toast 提示。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 水果数据列表，用于存储水果对象</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> fruitList = ArrayList&lt;Fruit&gt;()</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Activity创建时的回调方法</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> savedInstanceState Activity保存的状态</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 调用父类的onCreate方法</span></span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 设置Activity的布局</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line">        <span class="comment">// 初始化水果数据</span></span><br><span class="line">        initFruits()</span><br><span class="line">        <span class="comment">// 创建适配器，将水果数据绑定到ListView</span></span><br><span class="line">        <span class="keyword">val</span> adapter = FruitAdapter(<span class="keyword">this</span>, R.layout.fruit_item, fruitList)</span><br><span class="line">        <span class="comment">// 获取ListView控件</span></span><br><span class="line">        <span class="keyword">val</span> listView = findViewById&lt;ListView&gt;(R.id.listView)</span><br><span class="line">        <span class="comment">// 将适配器设置给ListView</span></span><br><span class="line">        listView.adapter = adapter</span><br><span class="line">        <span class="comment">// 设置ListView的点击事件监听器</span></span><br><span class="line">        listView.setOnItemClickListener &#123; _, _, position, _ -&gt;</span><br><span class="line">            <span class="comment">// 获取点击位置的水果</span></span><br><span class="line">            <span class="keyword">val</span> fruit = fruitList[position]</span><br><span class="line">            <span class="comment">// 显示水果名称的Toast消息</span></span><br><span class="line">            Toast.makeText(<span class="keyword">this</span>, fruit.name, Toast.LENGTH_SHORT).show()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 初始化水果数据的方法</span></span><br><span class="line"><span class="comment">     * 重复20次，添加三种水果（苹果、香蕉、橙子）</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">initFruits</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="comment">// 重复20次，用于添加多个水果项</span></span><br><span class="line">        repeat(<span class="number">20</span>) &#123;</span><br><span class="line">            <span class="comment">// 添加苹果</span></span><br><span class="line">            fruitList.add(Fruit(<span class="string">&quot;Apple&quot;</span>, R.drawable.b1))</span><br><span class="line">            <span class="comment">// 添加香蕉</span></span><br><span class="line">            fruitList.add(Fruit(<span class="string">&quot;Banana&quot;</span>, R.drawable.b2))</span><br><span class="line">            <span class="comment">// 添加橙子</span></span><br><span class="line">            fruitList.add(Fruit(<span class="string">&quot;Orange&quot;</span>, R.drawable.b3))</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">FruitAdapter</span>(activity: Activity, <span class="keyword">val</span> resourceId: <span class="built_in">Int</span>, <span class="keyword">val</span> <span class="keyword">data</span>: List&lt;Fruit&gt;) : ArrayAdapter&lt;Fruit&gt;(activity, resourceId, <span class="keyword">data</span>) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * ViewHolder类用于缓存每个列表项中的视图，以提高列表滚动时的性能。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">inner</span> <span class="keyword">class</span> <span class="title class_">ViewHolder</span>(<span class="keyword">val</span> fruitImage: ImageView, <span class="keyword">val</span> fruitName: TextView)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">getView</span><span class="params">(position: <span class="type">Int</span>, convertView: <span class="type">View</span>?, parent: <span class="type">ViewGroup</span>)</span></span>: View &#123;</span><br><span class="line">        <span class="keyword">val</span> view: View</span><br><span class="line">        <span class="keyword">val</span> viewHolder: ViewHolder</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果convertView为空，创建一个新的视图，否则复用convertView</span></span><br><span class="line">        <span class="keyword">if</span> (convertView == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 从XML布局文件中加载视图</span></span><br><span class="line">            view = LayoutInflater.from(context).inflate(resourceId, parent, <span class="literal">false</span>)</span><br><span class="line">            <span class="comment">// 初始化ImageView和TextView</span></span><br><span class="line">            <span class="keyword">val</span> fruitImage: ImageView = view.findViewById(R.id.fruit_image)</span><br><span class="line">            <span class="keyword">val</span> fruitName: TextView = view.findViewById(R.id.fruit_name)</span><br><span class="line">            <span class="comment">// 创建ViewHolder并绑定到view的tag中</span></span><br><span class="line">            viewHolder = ViewHolder(fruitImage, fruitName)</span><br><span class="line">            view.tag = viewHolder</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            view = convertView</span><br><span class="line">            <span class="comment">// 从convertView的tag中获取缓存的ViewHolder</span></span><br><span class="line">            viewHolder = view.tag <span class="keyword">as</span> ViewHolder</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取当前位置的Fruit对象</span></span><br><span class="line">        <span class="keyword">val</span> fruit = getItem(position)</span><br><span class="line">        <span class="keyword">if</span> (fruit != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 设置水果的图片</span></span><br><span class="line">            viewHolder.fruitImage.setImageResource(fruit.imageId)</span><br><span class="line">            <span class="comment">// 设置水果的名称</span></span><br><span class="line">            viewHolder.fruitName.text = fruit.name</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 返回准备好的视图</span></span><br><span class="line">        <span class="keyword">return</span> view</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Fruit</span>(<span class="keyword">val</span> name: String, <span class="keyword">val</span> imageId: <span class="built_in">Int</span>)</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/main&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">tools:context</span>=<span class="string">&quot;.MainActivity&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">ListView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/listView&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;60dp&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">ImageView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/fruit_image&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;40dp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;40dp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_marginLeft</span>=<span class="string">&quot;10dp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_gravity</span>=<span class="string">&quot;center_vertical&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/fruit_name&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_gravity</span>=<span class="string">&quot;center_vertical&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_marginLeft</span>=<span class="string">&quot;10dp&quot;</span></span></span><br><span class="line"><span class="tag">    /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="recyclerview控件"><a class="markdownIt-Anchor" href="#recyclerview控件"></a> RecyclerView控件</h3><p>1.<strong>添加依赖库</strong></p><p>2.<strong>数据模型定义</strong><br /><code>Fruit</code> 类定义了水果的数据结构。</p><p>3.<strong>适配器实现</strong><br />在 <code>FruitAdapter</code> 中：</p><ul><li>定义 ViewHolder 缓存 item 内的控件；</li><li>在 <code>onCreateViewHolder</code> 中加载 item 布局、创建 ViewHolder 并设置点击事件；</li><li>在 <code>onBindViewHolder</code> 中绑定数据；</li><li><code>getItemCount</code> 返回数据总数。</li></ul><p>4.<strong>item 布局定义</strong><br /><code>fruit_item.xml</code> 描述了每个列表项的外观（图片和名称）。</p><p>5.<strong>MainActivity 配置</strong><br />在 <code>onCreate()</code> 中：</p><ul><li><p>调用 <code>initFruits()</code> 初始化数据；</p></li><li><p>设置 RecyclerView 的布局管理器（瀑布流效果）；</p></li><li><p>绑定适配器，实现数据展示。</p></li></ul><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">implementation(&quot;androidx.recyclerview:recyclerview:1.3.2&quot;)</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义一个 ArrayList 用于存储水果数据</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> fruitList = ArrayList&lt;Fruit&gt;()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 设置当前 Activity 的布局为 activity_main.xml</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 初始化水果数据</span></span><br><span class="line">        initFruits()</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 使用 StaggeredGridLayoutManager 设置 RecyclerView 为瀑布流布局，3列垂直排列</span></span><br><span class="line">        <span class="keyword">val</span> layoutManager = StaggeredGridLayoutManager(<span class="number">3</span>, LinearLayoutManager.VERTICAL)</span><br><span class="line">        <span class="comment">// 获取布局中的 RecyclerView 控件</span></span><br><span class="line">        <span class="keyword">val</span> recyclerView = findViewById&lt;RecyclerView&gt;(R.id.recyclerView)</span><br><span class="line">        <span class="comment">// 设置 RecyclerView 的布局管理器</span></span><br><span class="line">        recyclerView.layoutManager = layoutManager</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建自定义适配器，并传入水果数据列表</span></span><br><span class="line">        <span class="keyword">val</span> adapter = FruitAdapter(fruitList)</span><br><span class="line">        <span class="comment">// 将适配器设置给 RecyclerView</span></span><br><span class="line">        recyclerView.adapter = adapter</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化水果数据的方法</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">initFruits</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="comment">// 重复 50 次，每次添加三种水果数据（Apple、Banana、Orange）</span></span><br><span class="line">        repeat(<span class="number">50</span>) &#123;</span><br><span class="line">            <span class="comment">// 添加 Apple，名称通过 getRandomLengthString() 方法随机生成长度，图片资源为 R.drawable.b1</span></span><br><span class="line">            fruitList.add(Fruit(getRandomLengthString(<span class="string">&quot;Apple&quot;</span>), R.drawable.b1))</span><br><span class="line">            <span class="comment">// 添加 Banana，图片资源为 R.drawable.b2</span></span><br><span class="line">            fruitList.add(Fruit(getRandomLengthString(<span class="string">&quot;Banana&quot;</span>), R.drawable.b2))</span><br><span class="line">            <span class="comment">// 添加 Orange，图片资源为 R.drawable.b3</span></span><br><span class="line">            fruitList.add(Fruit(getRandomLengthString(<span class="string">&quot;Orange&quot;</span>), R.drawable.b3))</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 根据传入的字符串随机重复生成一个新的字符串</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">getRandomLengthString</span><span class="params">(str: <span class="type">String</span>)</span></span>: String &#123;</span><br><span class="line">        <span class="comment">// 随机生成重复次数（1 到 20 之间）</span></span><br><span class="line">        <span class="keyword">val</span> n = (<span class="number">1.</span><span class="number">.20</span>).random()</span><br><span class="line">        <span class="keyword">val</span> builder = StringBuilder()</span><br><span class="line">        <span class="comment">// 重复追加字符串 n 次</span></span><br><span class="line">        repeat(n) &#123;</span><br><span class="line">            builder.append(str)</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 返回生成的字符串</span></span><br><span class="line">        <span class="keyword">return</span> builder.toString()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Fruit</span>(<span class="keyword">val</span> name: String, <span class="keyword">val</span> imageId: <span class="built_in">Int</span>)</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">FruitAdapter</span>(<span class="keyword">val</span> fruitList: List&lt;Fruit&gt;) : RecyclerView.Adapter&lt;FruitAdapter.ViewHolder&gt;() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 内部类 ViewHolder 用于缓存 item 视图中的控件，提高滚动性能</span></span><br><span class="line">    <span class="keyword">inner</span> <span class="keyword">class</span> <span class="title class_">ViewHolder</span>(view: View) : RecyclerView.ViewHolder(view) &#123;</span><br><span class="line">        <span class="comment">// 获取 item 布局中的 ImageView 控件</span></span><br><span class="line">        <span class="keyword">val</span> fruitImage: ImageView = view.findViewById(R.id.fruit_image)</span><br><span class="line">        <span class="comment">// 获取 item 布局中的 TextView 控件</span></span><br><span class="line">        <span class="keyword">val</span> fruitName: TextView = view.findViewById(R.id.fruit_name)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建新的 ViewHolder 时调用该方法</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreateViewHolder</span><span class="params">(parent: <span class="type">ViewGroup</span>, viewType: <span class="type">Int</span>)</span></span>: ViewHolder &#123;</span><br><span class="line">        <span class="comment">// 通过 LayoutInflater 加载 item 布局文件 fruit_item.xml</span></span><br><span class="line">        <span class="keyword">val</span> view = LayoutInflater.from(parent.context).inflate(R.layout.fruit_item, parent, <span class="literal">false</span>)</span><br><span class="line">        <span class="comment">// 创建 ViewHolder 对象，传入加载好的视图</span></span><br><span class="line">        <span class="keyword">val</span> viewHolder = ViewHolder(view)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置整个 item 视图的点击事件</span></span><br><span class="line">        view.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 通过 adapterPosition 获取当前点击的 item 位置</span></span><br><span class="line">            <span class="keyword">val</span> position = viewHolder.adapterPosition</span><br><span class="line">            <span class="comment">// 根据位置从数据列表中获取对应的水果对象</span></span><br><span class="line">            <span class="keyword">val</span> fruit = fruitList[position]</span><br><span class="line">            <span class="comment">// 显示点击提示，提示中包含水果名称</span></span><br><span class="line">            Toast.makeText(parent.context, <span class="string">&quot;you clicked view <span class="subst">$&#123;fruit.name&#125;</span>&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 为 item 中的 ImageView 设置点击事件</span></span><br><span class="line">        viewHolder.fruitImage.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 同样通过 adapterPosition 获取点击项的位置</span></span><br><span class="line">            <span class="keyword">val</span> position = viewHolder.adapterPosition</span><br><span class="line">            <span class="comment">// 根据位置获取对应的水果对象</span></span><br><span class="line">            <span class="keyword">val</span> fruit = fruitList[position]</span><br><span class="line">            <span class="comment">// 显示点击提示，提示中包含水果名称</span></span><br><span class="line">            Toast.makeText(parent.context, <span class="string">&quot;you clicked image <span class="subst">$&#123;fruit.name&#125;</span>&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 返回创建好的 ViewHolder 对象</span></span><br><span class="line">        <span class="keyword">return</span> viewHolder</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将数据绑定到 ViewHolder 上时调用</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onBindViewHolder</span><span class="params">(holder: <span class="type">ViewHolder</span>, position: <span class="type">Int</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 根据 position 获取对应的水果对象</span></span><br><span class="line">        <span class="keyword">val</span> fruit = fruitList[position]</span><br><span class="line">        <span class="comment">// 设置 ImageView 显示水果的图片资源</span></span><br><span class="line">        holder.fruitImage.setImageResource(fruit.imageId)</span><br><span class="line">        <span class="comment">// 设置 TextView 显示水果的名称</span></span><br><span class="line">        holder.fruitName.text = fruit.name</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 返回数据列表的大小，决定 RecyclerView 的 item 个数</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">getItemCount</span><span class="params">()</span></span> = fruitList.size</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/main&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">tools:context</span>=<span class="string">&quot;.MainActivity&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">androidx.recyclerview.widget.RecyclerView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/recyclerView&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_margin</span>=<span class="string">&quot;5dp&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">ImageView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/fruit_image&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;40dp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;40dp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_gravity</span>=<span class="string">&quot;center_horizontal&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_marginTop</span>=<span class="string">&quot;10dp&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/fruit_name&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_gravity</span>=<span class="string">&quot;left&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_marginTop</span>=<span class="string">&quot;10dp&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><hr /><h3 id="fragment碎片"><a class="markdownIt-Anchor" href="#fragment碎片"></a> Fragment碎片</h3><p>Fragment是一种可以嵌入在Activity的UI片段</p><h3 id="fragment的简单用法"><a class="markdownIt-Anchor" href="#fragment的简单用法"></a> Fragment的简单用法</h3><ol><li>写左右两个Fragment布局</li><li>新建左右两个Fragment类</li><li>在<code>activity_main</code>中引入两个Fragment</li></ol><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">Button</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/button&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_gravity</span>=<span class="string">&quot;center_horizontal&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;Button&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:background</span>=<span class="string">&quot;#F2CAD8&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_gravity</span>=<span class="string">&quot;center_horizontal&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:textSize</span>=<span class="string">&quot;24sp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;This is right fragment&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">LeftFragment</span> : <span class="type">Fragment</span>() &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreateView</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        inflater: <span class="type">LayoutInflater</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">        container: <span class="type">ViewGroup</span>?,</span></span></span><br><span class="line"><span class="params"><span class="function">        savedInstanceState: <span class="type">Bundle</span>?</span></span></span><br><span class="line"><span class="params"><span class="function">    )</span></span>: View? &#123;</span><br><span class="line">        <span class="keyword">return</span> inflater.inflate(R.layout.left_fragment, container, <span class="literal">false</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">RightFragment</span>: <span class="type">Fragment</span>() &#123;</span><br><span class="line">    <span class="keyword">companion</span> <span class="keyword">object</span> &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">val</span> TAG = <span class="string">&quot;RightFragment&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreateView</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        inflater: <span class="type">LayoutInflater</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">        container: <span class="type">ViewGroup</span>?,</span></span></span><br><span class="line"><span class="params"><span class="function">        savedInstanceState: <span class="type">Bundle</span>?</span></span></span><br><span class="line"><span class="params"><span class="function">    )</span></span>: View? &#123;</span><br><span class="line">        <span class="keyword">return</span> inflater.inflate(R.layout.right_fragment, container, <span class="literal">false</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/main&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;horizontal&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">tools:context</span>=<span class="string">&quot;.MainActivity&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:fitsSystemWindows</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">fragment</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/leftFrag&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:name</span>=<span class="string">&quot;com.example.fragmenttest.LeftFragment&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">tools:layout</span>=<span class="string">&quot;@layout/left_fragment&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">frameLayout</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/rightLayout&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:name</span>=<span class="string">&quot;com.example.fragmenttest.RightFragment&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">tools:layout</span>=<span class="string">&quot;@layout/right_fragment&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="动态添加fragment"><a class="markdownIt-Anchor" href="#动态添加fragment"></a> 动态添加Fragment</h3><ol><li><p>新建<code>another_right_fragmen</code>t和<code>AnotherRightFragment</code></p></li><li><p>动态添加到<code>activity_main</code>中</p></li><li><p>修改<code>MainActivity</code></p><p>(1) 创建待添加 Fragment 的实例。<br />(2) 获取 <code>getSupportFragmentManager() </code>方法。<br />(3) 开启一个事务，通过调用<code>beginTransaction()</code>方法开启。<br />(4) 向容器内添加或替换 Fragment，一般使用 <code>replace() </code>方法实现，需要传入容器的 id 和待添加的 Fragment 实例。<br />(5) 提交事务，调用<code> commit()</code>方法来完成。</p></li></ol><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:background</span>=<span class="string">&quot;#ffff00&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_gravity</span>=<span class="string">&quot;center_horizontal&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:textSize</span>=<span class="string">&quot;24sp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;This is another right fragment&quot;</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">AnotherRightFragment</span> : <span class="type">Fragment</span>()&#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreateView</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        inflater: <span class="type">LayoutInflater</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">        container: <span class="type">ViewGroup</span>?,</span></span></span><br><span class="line"><span class="params"><span class="function">        savedInstanceState: <span class="type">Bundle</span>?</span></span></span><br><span class="line"><span class="params"><span class="function">    )</span></span>: View? &#123;</span><br><span class="line">        <span class="keyword">return</span> inflater.inflate(R.layout.another_right_fragment, container, <span class="literal">false</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/main&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;horizontal&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">tools:context</span>=<span class="string">&quot;.MainActivity&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:fitsSystemWindows</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">fragment</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/leftFrag&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:name</span>=<span class="string">&quot;com.example.fragmenttest.LeftFragment&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">tools:layout</span>=<span class="string">&quot;@layout/left_fragment&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">FrameLayout</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/rightLayout&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;0dp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_weight</span>=<span class="string">&quot;1&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">FrameLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * onCreate是Activity的生命周期回调方法，用于初始化组件和布局</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> savedInstanceState 如果活动是被重建，保存状态的Bundle对象，否则为null</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@SuppressLint(<span class="string">&quot;MissingInflatedId&quot;</span>)</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 设置活动的用户界面布局</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取按钮控件</span></span><br><span class="line">        <span class="keyword">val</span> button = findViewById&lt;Button&gt;(R.id.button)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 设置按钮点击监听器</span></span><br><span class="line">        button.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 当按钮被点击时，替换右侧布局为AnotherRightFragment</span></span><br><span class="line">            replaceFragment(AnotherRightFragment())</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 初始时显示RightFragment</span></span><br><span class="line">        replaceFragment(RightFragment())</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 替换右侧布局中的Fragment</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> fragment 要替换的Fragment实例</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">replaceFragment</span><span class="params">(fragment: <span class="type">Fragment</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 获取SupportFragmentManager实例</span></span><br><span class="line">        <span class="keyword">val</span> fragmentManager = supportFragmentManager</span><br><span class="line">        <span class="comment">// 开始一个事务，用于执行Fragment替换操作</span></span><br><span class="line">        <span class="keyword">val</span> fragmentTransaction = fragmentManager.beginTransaction()</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 将指定的Fragment替换到右侧布局容器中</span></span><br><span class="line">        fragmentTransaction.replace(R.id.rightLayout, fragment)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 如果替换的Fragment是AnotherRightFragment，则将其添加到回退栈中</span></span><br><span class="line">        <span class="keyword">if</span> (fragment <span class="keyword">is</span> AnotherRightFragment) &#123;</span><br><span class="line">            fragmentTransaction.addToBackStack(<span class="literal">null</span>)</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 提交事务，执行Fragment替换操作</span></span><br><span class="line">        fragmentTransaction.commit()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="在fragment中实现返回栈"><a class="markdownIt-Anchor" href="#在fragment中实现返回栈"></a> 在Fragment中实现返回栈</h3><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"> <span class="keyword">val</span> fragmentTransaction = fragmentManager.beginTransaction()</span><br><span class="line">fragmentTransaction.addToBackStack(<span class="literal">null</span>)</span><br></pre></td></tr></table></figure><h3 id="fragment和activity之间的交互"><a class="markdownIt-Anchor" href="#fragment和activity之间的交互"></a> Fragment和Activity之间的交互</h3><h4 id="1在activity调用fragment"><a class="markdownIt-Anchor" href="#1在activity调用fragment"></a> 1.在Activity调用Fragment</h4><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> fragment = leftFrag <span class="keyword">as</span> LeftFragment</span><br></pre></td></tr></table></figure><h4 id="2在fragment调用activity"><a class="markdownIt-Anchor" href="#2在fragment调用activity"></a> 2.在Fragment调用Activity</h4><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (activity!=<span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">val</span> mainActivity = activity <span class="keyword">as</span> MainActivity</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="fragment的生命周期"><a class="markdownIt-Anchor" href="#fragment的生命周期"></a> Fragment的生命周期</h3><h4 id="1-onattachcontext-context"><a class="markdownIt-Anchor" href="#1-onattachcontext-context"></a> 1. onAttach(Context context)</h4><ul><li><strong>作用：</strong> 当 Fragment 被添加到 Activity 时，会首先调用该方法。这时 Fragment 与宿主 Activity 建立关联，但还没有创建自身的视图。</li><li><strong>注意：</strong> 此时你可以获取宿主 Activity 的上下文，进行一些必要的初始化工作。</li></ul><h4 id="2-oncreatebundle-savedinstancestate"><a class="markdownIt-Anchor" href="#2-oncreatebundle-savedinstancestate"></a> 2. onCreate(Bundle savedInstanceState)</h4><ul><li><strong>作用：</strong> 在 Fragment 创建时调用，用于进行一些非视图相关的初始化操作，比如初始化数据、设置选项等。</li><li><strong>注意：</strong> 此阶段不适合初始化界面相关的内容，因为视图尚未创建。</li></ul><h4 id="3-oncreateviewlayoutinflater-inflater-viewgroup-container-bundle-savedinstancestate"><a class="markdownIt-Anchor" href="#3-oncreateviewlayoutinflater-inflater-viewgroup-container-bundle-savedinstancestate"></a> 3. onCreateView(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState)</h4><ul><li><strong>作用：</strong> 用于创建并返回 Fragment 的视图层次结构（通常通过 XML 布局文件膨胀得到）。</li><li><strong>注意：</strong> 你可以在这里绑定视图，设置控件的属性或事件监听。</li></ul><h4 id="4-onviewcreatedview-view-bundle-savedinstancestate"><a class="markdownIt-Anchor" href="#4-onviewcreatedview-view-bundle-savedinstancestate"></a> 4. onViewCreated(View view, Bundle savedInstanceState)</h4><ul><li><strong>作用：</strong> 当视图被创建后立即调用，此时可以对视图进行进一步的初始化（例如查找子视图、设置适配器等）。</li><li><strong>注意：</strong> 虽然这是可选的，但有助于将视图相关的逻辑与视图创建分离。</li></ul><h4 id="5-onactivitycreatedbundle-savedinstancestate"><a class="markdownIt-Anchor" href="#5-onactivitycreatedbundle-savedinstancestate"></a> 5. onActivityCreated(Bundle savedInstanceState)</h4><ul><li><strong>作用：</strong> 当宿主 Activity 的 onCreate() 方法执行完毕后调用，这时 Fragment 的宿主 Activity 已经完全初始化，可以安全地与 Activity 进行交互。</li><li><strong>注意：</strong> 可以在此阶段恢复状态或进行依赖于 Activity 的操作。</li></ul><h4 id="6-onstart"><a class="markdownIt-Anchor" href="#6-onstart"></a> 6. onStart()</h4><ul><li><strong>作用：</strong> Fragment 对用户可见。此时界面已经呈现，但还没有获得用户焦点。</li><li><strong>注意：</strong> 通常在这里开始一些较轻的 UI 刷新工作。</li></ul><h4 id="7-onresume"><a class="markdownIt-Anchor" href="#7-onresume"></a> 7. onResume()</h4><ul><li><strong>作用：</strong> Fragment 进入活动状态，此时它与用户进行交互。界面完全处于前台，响应用户输入。</li><li><strong>注意：</strong> 可以在这里启动动画、开始监听传感器数据等与用户交互密切相关的操作。</li></ul><h4 id="8-onpause"><a class="markdownIt-Anchor" href="#8-onpause"></a> 8. onPause()</h4><ul><li><strong>作用：</strong> 当 Fragment 失去焦点，用户无法与之交互时调用（例如，用户离开应用或启动新的 Activity）。</li><li><strong>注意：</strong> 在这里应停止可能影响性能或不必要的操作，比如暂停动画、释放资源等。</li></ul><h4 id="9-onstop"><a class="markdownIt-Anchor" href="#9-onstop"></a> 9. onStop()</h4><ul><li><strong>作用：</strong> Fragment 不再对用户可见，但仍保留在内存中。</li><li><strong>注意：</strong> 此阶段可进行资源释放、停止更新 UI 等操作，确保不会浪费资源。</li></ul><h4 id="10-ondestroyview"><a class="markdownIt-Anchor" href="#10-ondestroyview"></a> 10. onDestroyView()</h4><ul><li><strong>作用：</strong> Fragment 的视图层次结构被销毁。此时与视图相关的资源（如绑定的视图、适配器等）应当被清理。</li><li><strong>注意：</strong> 如果需要保存视图状态，可以在此方法之前进行处理。</li></ul><h4 id="11-ondestroy"><a class="markdownIt-Anchor" href="#11-ondestroy"></a> 11. onDestroy()</h4><ul><li><strong>作用：</strong> Fragment 自身被销毁，用于释放所有剩余资源和进行最终清理工作。</li><li><strong>注意：</strong> 此阶段与 onCreate() 相对，用于做一些彻底的清理工作。</li></ul><h4 id="12-ondetach"><a class="markdownIt-Anchor" href="#12-ondetach"></a> 12. onDetach()</h4><ul><li><strong>作用：</strong> Fragment 与宿主 Activity 的关联被断开，生命周期结束。</li><li><strong>注意：</strong> 此时所有与 Activity 相关的引用都应该被释放，以防止内存泄漏</li></ul><hr /><h2 id="3广播broadcast"><a class="markdownIt-Anchor" href="#3广播broadcast"></a> 3.广播(Broadcast)</h2><p>广播机制（Broadcasting）指的是将消息、数据或信号从一个源同时发送给网络中所有或一部分节点或设备。它与单播（Unicast，一对一通信）和多播（Multicast，一对多通信）不同，广播主要面向整个网络或一个较大的受众群体。</p><h3 id="1-标准广播"><a class="markdownIt-Anchor" href="#1-标准广播"></a> 1. 标准广播</h3><p><strong>定义与特点</strong></p><ul><li><strong>无序性</strong>：标准广播在消息传输时不对接收顺序做严格保证。不同接收者收到消息的顺序可能不一致，这种特性使其实现相对简单。</li><li><strong>高效性</strong>：由于不需要额外的排序和同步机制，标准广播通常具有较低的延时和较高的传输效率。</li><li>适用场景：<ul><li>用于事件通知、状态更新或日志广播等场景，这些场景对消息顺序没有严格要求。</li><li>在对实时性要求高、能够容忍一定顺序差异的应用中，标准广播是常用的选择。</li></ul></li></ul><hr /><h3 id="2-有序广播"><a class="markdownIt-Anchor" href="#2-有序广播"></a> 2. 有序广播</h3><p><strong>定义与特点</strong></p><ul><li><strong>顺序一致性</strong>：有序广播要求所有接收者必须按照相同的顺序接收到所有消息。也就是说，无论消息是从哪个节点发送，所有节点在消息的交付顺序上都必须保持一致。</li><li><strong>实现复杂性</strong>：为了确保全局顺序一致，常需要采用额外的排序机制或共识算法（如逻辑时钟、矢量时钟或专门的总排序服务）。这种机制虽然能够确保系统状态的一致性，但同时也会增加系统的复杂性和延时。</li><li>适用场景：<ul><li>在需要高度一致性的分布式系统中，如分布式数据库复制、一致性协议、金融交易系统等，保证操作顺序一致是关键。</li><li>有序广播有助于防止由于消息乱序而引发的数据冲突或状态不一致问题，从而提高系统的容错性和数据一致性。</li></ul></li></ul><hr /><h3 id="3-对比与选择"><a class="markdownIt-Anchor" href="#3-对比与选择"></a> 3. 对比与选择</h3><table><thead><tr><th>特性</th><th>标准广播</th><th>有序广播</th></tr></thead><tbody><tr><td><strong>消息顺序</strong></td><td>无严格顺序保证</td><td>全局顺序一致</td></tr><tr><td><strong>实现难度</strong></td><td>实现简单，效率高</td><td>需要额外排序/同步机制，复杂度高</td></tr><tr><td><strong>延时</strong></td><td>较低</td><td>可能因排序/同步而增加延时</td></tr><tr><td><strong>适用场景</strong></td><td>实时性要求高、顺序要求不严格</td><td>状态一致性要求高、需要全局顺序一致</td></tr></tbody></table><hr /><h3 id="4-静态注册广播"><a class="markdownIt-Anchor" href="#4-静态注册广播"></a> 4 静态注册广播</h3><p><strong>定义</strong><br />静态注册广播是在系统配置或设计阶段就预先确定好所有参与广播的节点，广播目标在系统运行过程中保持不变。</p><p><strong>特点与优势</strong></p><ul><li><strong>实现简单</strong>：广播目标在配置文件或代码中硬编码，开发和调试过程中较为直接。</li><li><strong>低开销</strong>：无需额外的注册和管理机制，消息直接发送给预定的目标。</li><li><strong>确定性高</strong>：由于广播组成员固定，网络拓扑和消息传递路径相对固定，有助于预期性能分析。</li></ul><p><strong>局限性</strong></p><ul><li><strong>灵活性不足</strong>：如果系统中的节点发生增减或迁移，必须手动更新配置，无法自动适应环境变化。</li><li><strong>扩展性受限</strong>：适用于节点较少且变化不频繁的系统，对于大规模动态环境来说不够高效。</li></ul><hr /><h3 id="5-动态注册广播"><a class="markdownIt-Anchor" href="#5-动态注册广播"></a> 5. 动态注册广播</h3><p><strong>定义</strong><br />动态注册广播允许系统中节点在运行时主动注册成为广播组成员或退出广播组。此类机制通常依赖一个注册中心或服务发现模块，实时维护当前可用的广播目标列表。</p><p><strong>特点与优势</strong></p><ul><li><strong>高灵活性</strong>：节点上线、下线、扩容或故障恢复时，注册中心会自动更新广播组成员，保证广播消息能够覆盖最新的系统状态。</li><li><strong>自动管理</strong>：利用心跳检测或健康检查机制，动态剔除不可用节点，提升系统整体的容错能力。</li><li><strong>便于扩展</strong>：适合大规模、动态变化频繁的分布式系统，能够根据实际负载和环境变化自动调整广播对象。</li></ul><p><strong>局限性</strong></p><ul><li><strong>实现复杂</strong>：需要部署和维护额外的注册服务，涉及网络延时、状态一致性等问题。</li><li><strong>管理开销</strong>：动态更新广播组可能带来短暂的不一致或额外的网络通信开销，需仔细设计以减少延迟和资源消耗。</li></ul><hr /><h3 id="6-对比与适用场景"><a class="markdownIt-Anchor" href="#6-对比与适用场景"></a> 6. 对比与适用场景</h3><table><thead><tr><th>特性</th><th>静态注册广播</th><th>动态注册广播</th></tr></thead><tbody><tr><td><strong>注册方式</strong></td><td>在配置阶段固定节点列表</td><td>节点可在运行时自动注册或注销</td></tr><tr><td><strong>实现复杂度</strong></td><td>实现简单，依赖硬编码或配置文件</td><td>需要额外的注册中心和健康检查机制</td></tr><tr><td><strong>灵活性与扩展性</strong></td><td>节点变化需要人工干预，适合节点稳定的小规模系统</td><td>适应节点频繁变动，大规模系统中自动维护广播组成员</td></tr><tr><td><strong>系统管理开销</strong></td><td>管理和维护简单，但缺乏自动适应性</td><td>运维自动化，但引入额外通信和状态同步的开销</td></tr></tbody></table><h3 id="动态注册监听时间变化记得取消注册"><a class="markdownIt-Anchor" href="#动态注册监听时间变化记得取消注册"></a> 动态注册监听时间变化(记得取消注册)</h3><ol><li><p>新建一个类继承自<code>BroadcastReceiver</code></p></li><li><p>重写<code>onReceiver()</code>方法,执行具体逻辑</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="keyword">lateinit</span> <span class="keyword">var</span> timeChangeReceiver: TimeChangeReceiver</span><br><span class="line">    <span class="meta">@SuppressLint(<span class="string">&quot;WrongViewCast&quot;</span>)</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line">        <span class="keyword">val</span> intentFilter = IntentFilter()</span><br><span class="line">        intentFilter.addAction(<span class="string">&quot;android.intent.action.TIME_TICK&quot;</span>)</span><br><span class="line">        timeChangeReceiver = TimeChangeReceiver()</span><br><span class="line">        registerReceiver(timeChangeReceiver, intentFilter)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onDestroy()</span><br><span class="line">        unregisterReceiver(timeChangeReceiver) <span class="comment">//取消注册</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">inner</span> <span class="keyword">class</span> <span class="title class_">TimeChangeReceiver</span> : <span class="type">BroadcastReceiver</span>() &#123;</span><br><span class="line">        <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onReceive</span><span class="params">(context: <span class="type">Context</span>, intent: <span class="type">Intent</span>)</span></span> &#123;</span><br><span class="line">            Toast.makeText(context, <span class="string">&quot;Time has changed&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><h3 id="静态注册实现开机启动"><a class="markdownIt-Anchor" href="#静态注册实现开机启动"></a> 静态注册实现开机启动</h3><ol><li>用快捷方式创建一个广播接收器</li><li>在<code>AndroidManifest</code>添加对应权限与<code>action</code></li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">BootCompleteReceiver</span> : <span class="type">BroadcastReceiver</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onReceive</span><span class="params">(context: <span class="type">Context</span>, intent: <span class="type">Intent</span>)</span></span> &#123;</span><br><span class="line">        Toast.makeText(context, <span class="string">&quot;Boot Complete&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.RECEIVE_BOOT_COMPLETED&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">receiver</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.BootCompleteReceiver&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:enabled</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:exported</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">&quot;android.intent.action.BOOT_COMPLETED&quot;</span> /&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">receiver</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="发送标准广播"><a class="markdownIt-Anchor" href="#发送标准广播"></a> 发送标准广播</h3><ol><li>定义一个广播接收器</li><li>在AndroidManifest添加自定义广播</li><li>在MainActivity发送广播</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyBroadcastReceiver</span> : <span class="type">BroadcastReceiver</span>() &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onReceive</span><span class="params">(context: <span class="type">Context</span>, intent: <span class="type">Intent</span>)</span></span> &#123;</span><br><span class="line">        Toast.makeText(context, <span class="string">&quot;received in MyBroadcastReceiver&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">        abortBroadcast()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">receiver</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.MyBroadcastReceiver&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:enabled</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:exported</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">intent-filter</span> <span class="attr">android:priority</span>=<span class="string">&quot;100&quot;</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">&quot;com.example.broadcasttest.MY_BROADCAST&quot;</span> /&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">receiver</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> button = findViewById&lt;androidx.appcompat.widget.AppCompatButton&gt;(R.id.button)</span><br><span class="line">        button.setOnClickListener &#123;</span><br><span class="line">            <span class="keyword">val</span> intent = Intent(<span class="string">&quot;com.example.broadcasttest.MY_BROADCAST&quot;</span>)</span><br><span class="line">            intent.setPackage(packageName)</span><br><span class="line">            sendOrderedBroadcast(intent, <span class="literal">null</span>)</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure><h3 id="发送有序广播"><a class="markdownIt-Anchor" href="#发送有序广播"></a> 发送有序广播</h3><ol><li>新建<code>AnotherBroadcastReceiver</code></li><li>在AndroidManifest添加自定义广播</li><li>在<code>MainActivity</code>利用<code>sendOrderedBroadcast</code>发送广播</li><li>在<code>AndroidManifest</code>设置优先级</li><li>在<code>MyBroadcastReceiver</code>利用<code>abortBroadcast</code>截断广播</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">AnotherBroadcastReceiver</span> : <span class="type">BroadcastReceiver</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onReceive</span><span class="params">(context: <span class="type">Context</span>, intent: <span class="type">Intent</span>)</span></span> &#123;</span><br><span class="line">        Toast.makeText(context, <span class="string">&quot;received in AnotherBroadcastReceiver&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">receiver</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.AnotherBroadcastReceiver&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:enabled</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:exported</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">&quot;com.example.broadcasttest.MY_BROADCAST&quot;</span> /&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">receiver</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> button = findViewById&lt;androidx.appcompat.widget.AppCompatButton&gt;(R.id.button)</span><br><span class="line">        button.setOnClickListener &#123;</span><br><span class="line">            <span class="keyword">val</span> intent = Intent(<span class="string">&quot;com.example.broadcasttest.MY_BROADCAST&quot;</span>)</span><br><span class="line">            intent.setPackage(packageName)</span><br><span class="line">            sendOrderedBroadcast(intent, <span class="literal">null</span>)</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">intent-filter</span> <span class="attr">android:priority</span>=<span class="string">&quot;100&quot;</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">&quot;com.example.broadcasttest.MY_BROADCAST&quot;</span> /&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyBroadcastReceiver</span> : <span class="type">BroadcastReceiver</span>() &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onReceive</span><span class="params">(context: <span class="type">Context</span>, intent: <span class="type">Intent</span>)</span></span> &#123;</span><br><span class="line">        Toast.makeText(context, <span class="string">&quot;received in MyBroadcastReceiver&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">        abortBroadcast()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="4持久化技术"><a class="markdownIt-Anchor" href="#4持久化技术"></a> 4.持久化技术</h2><p>数据持久化技术就是指将那些内存中的瞬时数据保存到存储设备中,保证即使在手机或计算机关机的情况下,这些数据仍然不会消失.</p><h3 id="文件存储"><a class="markdownIt-Anchor" href="#文件存储"></a> 文件存储</h3><ol><li>新建<code>save</code>函数,通过<code>openFileOutput()</code>方法存入数据</li><li>让Activity在<code>Destroy</code>之前掉用<code>save</code>方法</li><li>在<code>load</code>中通过<code>openFileInput</code>读取数据</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Activity创建时的回调方法</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 从文件中加载数据</span></span><br><span class="line">        <span class="keyword">val</span> inputText = load()</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 如果加载的文字不是空的，则将其显示在EditText中</span></span><br><span class="line">        <span class="keyword">if</span> (inputText.isNotEmpty()) &#123;</span><br><span class="line">            <span class="keyword">val</span> editText = findViewById&lt;EditText&gt;(R.id.editText)</span><br><span class="line">            editText.setText(inputText)</span><br><span class="line">            editText.setSelection(inputText.length)</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 显示一个toast提示，表示数据已恢复</span></span><br><span class="line">            Toast.makeText(<span class="keyword">this</span>, <span class="string">&quot;Restoring saved data&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 从文件中加载数据的方法</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 返回加载的字符串内容，如果出错则返回空字符串</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">load</span><span class="params">()</span></span>: String &#123;</span><br><span class="line">        <span class="keyword">val</span> content = StringBuilder()</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 打开文件输入流，文件名为“data”</span></span><br><span class="line">            <span class="keyword">val</span> input = openFileInput(<span class="string">&quot;data&quot;</span>)</span><br><span class="line">            <span class="comment">// 创建缓冲读取器</span></span><br><span class="line">            <span class="keyword">val</span> reader = BufferedReader(InputStreamReader(input))</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 使用reader.forEachLine读取每一行内容并追加到content中</span></span><br><span class="line">            reader.use &#123;</span><br><span class="line">                reader.forEachLine &#123;</span><br><span class="line">                    content.append(it)</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (e: Exception) &#123;</span><br><span class="line">            <span class="comment">// 如果出错，打印异常堆栈信息</span></span><br><span class="line">            e.printStackTrace()</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 返回读取到的内容</span></span><br><span class="line">        <span class="keyword">return</span> content.toString()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Activity被销毁时的回调方法</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onDestroy()</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取EditText实例</span></span><br><span class="line">        <span class="keyword">val</span> editText = findViewById&lt;EditText&gt;(R.id.editText)</span><br><span class="line">        <span class="comment">// 获取EditText中的文字内容</span></span><br><span class="line">        <span class="keyword">val</span> inputText = editText.text.toString()</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 调用save方法保存内容</span></span><br><span class="line">        save(inputText)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 将输入的文字内容保存到文件中的方法</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> inputText 需要保存的字符串内容</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">save</span><span class="params">(inputText: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 打开文件输出流，文件名为“data”，模式为私有（只有本应用可以访问）</span></span><br><span class="line">            <span class="keyword">val</span> output = openFileOutput(<span class="string">&quot;data&quot;</span>, MODE_PRIVATE)</span><br><span class="line">            <span class="comment">// 创建缓冲写入器</span></span><br><span class="line">            <span class="keyword">val</span> writer = BufferedWriter(OutputStreamWriter(output))</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 使用writer写入内容</span></span><br><span class="line">            writer.use &#123;</span><br><span class="line">                it.write(inputText)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (e: Exception) &#123;</span><br><span class="line">            <span class="comment">// 如果出错，打印异常堆栈信息</span></span><br><span class="line">            e.printStackTrace()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="sharedpreferences存储"><a class="markdownIt-Anchor" href="#sharedpreferences存储"></a> SharedPreferences存储</h3><ol><li>通过<code>getSharedPreferences()</code>指定文件名并得到<code>SharedPreferences.Editor</code>对象</li><li>添加新数据,用<code>apple()</code>进行提交</li><li>运用一系列<code>get</code>方法提取数据</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="comment">// 用于日志记录的标签，标识这个类的日志信息</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> TAG = <span class="string">&quot;MainActivity&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Activity创建时的回调方法</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 设置主界面为activity_main布局</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取保存按钮实例</span></span><br><span class="line">        <span class="keyword">val</span> saveButton = findViewById&lt;Button&gt;(R.id.saveButton)</span><br><span class="line">        <span class="comment">// 获取恢复按钮实例</span></span><br><span class="line">        <span class="keyword">val</span> restoreButton = findViewById&lt;Button&gt;(R.id.restoreButton)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置保存按钮的点击事件监听器</span></span><br><span class="line">        saveButton.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 获取SharedPreferences的编辑器，文件名为&quot;sharedPreferences&quot;，模式为私有（仅本应用访问）</span></span><br><span class="line">            <span class="keyword">val</span> editor = getSharedPreferences(<span class="string">&quot;sharedPreferences&quot;</span>, MODE_PRIVATE).edit()</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 保存字符串类型的数据，键为&quot;name&quot;，值为&quot;John Doe&quot;</span></span><br><span class="line">            editor.putString(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;John Doe&quot;</span>)</span><br><span class="line">            <span class="comment">// 保存整数类型的数据，键为&quot;age&quot;，值为30</span></span><br><span class="line">            editor.putInt(<span class="string">&quot;age&quot;</span>, <span class="number">30</span>)</span><br><span class="line">            <span class="comment">// 保存布尔类型的数据，键为&quot;isStudent&quot;，值为true</span></span><br><span class="line">            editor.putBoolean(<span class="string">&quot;isStudent&quot;</span>, <span class="literal">true</span>)</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 提交修改，保存数据到SharedPreferences文件中</span></span><br><span class="line">            editor.apply()</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置恢复按钮的点击事件监听器</span></span><br><span class="line">        restoreButton.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 获取SharedPreferences文件，文件名为&quot;data&quot;，模式为私有（仅本应用访问）</span></span><br><span class="line">            <span class="keyword">val</span> sharedPreferences = getSharedPreferences(<span class="string">&quot;data&quot;</span>, Context.MODE_PRIVATE)</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 读取字符串类型的数据，键为&quot;name&quot;，默认值为空字符串</span></span><br><span class="line">            <span class="keyword">val</span> name = sharedPreferences.getString(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;&quot;</span>)</span><br><span class="line">            <span class="comment">// 读取整数类型的数据，键为&quot;age&quot;，默认值为0</span></span><br><span class="line">            <span class="keyword">val</span> age = sharedPreferences.getInt(<span class="string">&quot;age&quot;</span>, <span class="number">0</span>)</span><br><span class="line">            <span class="comment">// 读取布尔类型的数据，键为&quot;isStudent&quot;，默认值为false</span></span><br><span class="line">            <span class="keyword">val</span> isStudent = sharedPreferences.getBoolean(<span class="string">&quot;isStudent&quot;</span>, <span class="literal">false</span>)</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 打印日志，显示读取的数据</span></span><br><span class="line">            Log.d(TAG, <span class="string">&quot;name is <span class="variable">$name</span>&quot;</span>)</span><br><span class="line">            Log.d(TAG, <span class="string">&quot;age is <span class="variable">$age</span>&quot;</span>)</span><br><span class="line">            Log.d(TAG, <span class="string">&quot;isStudent is <span class="variable">$isStudent</span>&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="sqlite数据库存储"><a class="markdownIt-Anchor" href="#sqlite数据库存储"></a> SQLite数据库存储</h3><p>SQLite是一个轻量级的关系型数据库，嵌入在Android系统中，适合存储结构化数据。与传统的客户端-服务器数据库不同，SQLite是一个内嵌式数据库，所有的数据操作都是在本地进行，非常适合需要离线存储的小型或中型数据应用。</p><ol><li>通过SOL语句建表,在<code>onCreate</code>方法里面调用<code>db.execSQL()</code>创建数据库</li><li>在<code>onUpgrade()</code>执行DROP语句对数据库进行更新</li><li>增加数据库帮助类的版本号</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="meta">@SuppressLint(<span class="string">&quot;MissingInflatedId&quot;</span>, <span class="string">&quot;Range&quot;</span>)</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 设置主界面为activity_main布局</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取创建数据库按钮实例</span></span><br><span class="line">        <span class="keyword">val</span> createDatabase = findViewById&lt;Button&gt;(R.id.createDatabase)</span><br><span class="line">        <span class="comment">// 初始化数据库帮助类，参数依次为上下文、数据库名、版本号</span></span><br><span class="line">        <span class="keyword">val</span> dbHelper = MyDatabaseHelper(<span class="keyword">this</span>, <span class="string">&quot;BookStore.db&quot;</span>, <span class="number">2</span>)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 设置创建数据库按钮的点击事件监听器</span></span><br><span class="line">        createDatabase.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 获取可写的数据库实例，确保数据库和表被创建</span></span><br><span class="line">            dbHelper.writableDatabase</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取添加数据按钮实例</span></span><br><span class="line">        <span class="keyword">val</span> addDate = findViewById&lt;Button&gt;(R.id.addData)</span><br><span class="line">        <span class="comment">// 设置添加数据按钮的点击事件监听器</span></span><br><span class="line">        addDate.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 获取可写的数据库实例</span></span><br><span class="line">            <span class="keyword">val</span> db = dbHelper.writableDatabase</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 准备要插入的数据1</span></span><br><span class="line">            <span class="keyword">val</span> values1 = ContentValues().apply &#123;</span><br><span class="line">                put(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;The Da Vinci Code&quot;</span>)</span><br><span class="line">                put(<span class="string">&quot;author&quot;</span>, <span class="string">&quot;Dan Brown&quot;</span>)</span><br><span class="line">                put(<span class="string">&quot;pages&quot;</span>, <span class="number">454</span>)</span><br><span class="line">                put(<span class="string">&quot;price&quot;</span>, <span class="number">16.96</span>)</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 将数据插入到&quot;Book&quot;表中，返回新行的ID</span></span><br><span class="line">            db.insert(<span class="string">&quot;Book&quot;</span>, <span class="literal">null</span>, values1)</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 准备要插入的数据2</span></span><br><span class="line">            <span class="keyword">val</span> values2 = ContentValues().apply &#123;</span><br><span class="line">                put(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;The Lost Symbol&quot;</span>)</span><br><span class="line">                put(<span class="string">&quot;author&quot;</span>, <span class="string">&quot;Dan Brown&quot;</span>)</span><br><span class="line">                put(<span class="string">&quot;pages&quot;</span>, <span class="number">510</span>)</span><br><span class="line">                put(<span class="string">&quot;price&quot;</span>, <span class="number">19.95</span>)</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 将数据插入到&quot;Book&quot;表中，返回新行的ID</span></span><br><span class="line">            db.insert(<span class="string">&quot;Book&quot;</span>, <span class="literal">null</span>, values2)</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取更新数据按钮实例</span></span><br><span class="line">        <span class="keyword">val</span> updateData = findViewById&lt;Button&gt;(R.id.updateData)</span><br><span class="line">        <span class="comment">// 设置更新数据按钮的点击事件监听器</span></span><br><span class="line">        updateData.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 获取可写的数据库实例</span></span><br><span class="line">            <span class="keyword">val</span> db = dbHelper.writableDatabase</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 准备要更新的数据</span></span><br><span class="line">            <span class="keyword">val</span> values = ContentValues()</span><br><span class="line">            values.put(<span class="string">&quot;price&quot;</span>, <span class="number">10.99</span>)</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 更新&quot;Book&quot;表中满足条件的记录</span></span><br><span class="line">            <span class="comment">// 条件：name等于&quot;The Da Vinci Code&quot;</span></span><br><span class="line">            db.update(<span class="string">&quot;Book&quot;</span>, values, <span class="string">&quot;name = ?&quot;</span>, arrayOf(<span class="string">&quot;The Da Vinci Code&quot;</span>))</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取删除数据按钮实例</span></span><br><span class="line">        <span class="keyword">val</span> deleteData = findViewById&lt;Button&gt;(R.id.deleteData)</span><br><span class="line">        <span class="comment">// 设置删除数据按钮的点击事件监听器</span></span><br><span class="line">        deleteData.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 获取可写的数据库实例</span></span><br><span class="line">            <span class="keyword">val</span> db = dbHelper.writableDatabase</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 删除&quot;Book&quot;表中满足条件的记录</span></span><br><span class="line">            <span class="comment">// 条件：pages大于500</span></span><br><span class="line">            db.delete(<span class="string">&quot;Book&quot;</span>, <span class="string">&quot;pages &gt; ?&quot;</span>, arrayOf(<span class="string">&quot;500&quot;</span>))</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取查询数据按钮实例</span></span><br><span class="line">        <span class="keyword">val</span> queryData = findViewById&lt;Button&gt;(R.id.queryData)</span><br><span class="line">        <span class="comment">// 设置查询数据按钮的点击事件监听器</span></span><br><span class="line">        queryData.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 获取可写的数据库实例</span></span><br><span class="line">            <span class="keyword">val</span> db = dbHelper.writableDatabase</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 查询&quot;Book&quot;表中的所有数据</span></span><br><span class="line">            <span class="keyword">val</span> cursor = db.query(<span class="string">&quot;Book&quot;</span>, <span class="literal">null</span>, <span class="literal">null</span>, <span class="literal">null</span>, <span class="literal">null</span>, <span class="literal">null</span>, <span class="literal">null</span>)</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 遍历查询结果</span></span><br><span class="line">            <span class="keyword">if</span> (cursor.moveToFirst()) &#123;</span><br><span class="line">                <span class="keyword">do</span> &#123;</span><br><span class="line">                    <span class="comment">// 获取每一列的数据</span></span><br><span class="line">                    <span class="keyword">val</span> name = cursor.getString(cursor.getColumnIndex(<span class="string">&quot;name&quot;</span>))</span><br><span class="line">                    <span class="keyword">val</span> author = cursor.getString(cursor.getColumnIndex(<span class="string">&quot;author&quot;</span>))</span><br><span class="line">                    <span class="keyword">val</span> pages = cursor.getInt(cursor.getColumnIndex(<span class="string">&quot;pages&quot;</span>))</span><br><span class="line">                    <span class="keyword">val</span> price = cursor.getDouble(cursor.getColumnIndex(<span class="string">&quot;price&quot;</span>))</span><br><span class="line">                    </span><br><span class="line">                    <span class="comment">// 打印日志，显示查询到的数据</span></span><br><span class="line">                    println(<span class="string">&quot;name is <span class="variable">$name</span>, author is <span class="variable">$author</span>, pages is <span class="variable">$pages</span>, price is <span class="variable">$price</span>&quot;</span>)</span><br><span class="line">                &#125; <span class="keyword">while</span> (cursor.moveToNext())</span><br><span class="line">            &#125;</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 关闭游标，释放资源</span></span><br><span class="line">            cursor.close()</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取替换数据按钮实例</span></span><br><span class="line">        <span class="keyword">val</span> replaceData = findViewById&lt;Button&gt;(R.id.replaceData)</span><br><span class="line">        <span class="comment">// 设置替换数据按钮的点击事件监听器</span></span><br><span class="line">        replaceData.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 获取可写的数据库实例</span></span><br><span class="line">            <span class="keyword">val</span> db = dbHelper.writableDatabase</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 开始一个事务</span></span><br><span class="line">            db.beginTransaction()</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 删除&quot;Book&quot;表中的所有记录</span></span><br><span class="line">                db.delete(<span class="string">&quot;Book&quot;</span>, <span class="literal">null</span>, <span class="literal">null</span>)</span><br><span class="line">                </span><br><span class="line">                <span class="comment">// 这里可以手动抛出异常进行事务回滚测试</span></span><br><span class="line">                <span class="comment">// if (true) &#123;</span></span><br><span class="line">                <span class="comment">//     throw NullPointerException()</span></span><br><span class="line">                <span class="comment">// &#125;</span></span><br><span class="line">                </span><br><span class="line">                <span class="comment">// 准备要插入的数据</span></span><br><span class="line">                <span class="keyword">val</span> values = ContentValues().apply &#123;</span><br><span class="line">                    put(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;Game of Thrones&quot;</span>)</span><br><span class="line">                    put(<span class="string">&quot;author&quot;</span>, <span class="string">&quot;George Martin&quot;</span>)</span><br><span class="line">                    put(<span class="string">&quot;pages&quot;</span>, <span class="number">720</span>)</span><br><span class="line">                    put(<span class="string">&quot;price&quot;</span>, <span class="number">20.85</span>)</span><br><span class="line">                &#125;</span><br><span class="line">                </span><br><span class="line">                <span class="comment">// 将数据插入到&quot;Book&quot;表中</span></span><br><span class="line">                db.insert(<span class="string">&quot;Book&quot;</span>, <span class="literal">null</span>, values)</span><br><span class="line">                </span><br><span class="line">                <span class="comment">// 标记事务为成功</span></span><br><span class="line">                db.setTransactionSuccessful()</span><br><span class="line">            &#125; <span class="keyword">catch</span> (e: Exception) &#123;</span><br><span class="line">                <span class="comment">// 捕获异常并打印异常信息</span></span><br><span class="line">                e.printStackTrace()</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                <span class="comment">// 结束事务</span></span><br><span class="line">                db.endTransaction()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="1-添加插入数据"><a class="markdownIt-Anchor" href="#1-添加插入数据"></a> 1. 添加（插入）数据</h4><p><strong>核心步骤</strong></p><ul><li>获取数据库实例（通常使用<code>writableDatabase</code>）。</li><li>使用<code>ContentValues</code>封装待插入的数据。</li><li>调用<code>insert()</code>方法将数据插入表中，并返回新记录的ID。</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">insertCity</span><span class="params">(dbHelper: <span class="type">DBHelper</span>, name: <span class="type">String</span>, code: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">    <span class="comment">// 获取可写数据库</span></span><br><span class="line">    <span class="keyword">val</span> db = dbHelper.writableDatabase</span><br><span class="line">    <span class="comment">// 使用ContentValues封装要插入的数据</span></span><br><span class="line">    <span class="keyword">val</span> values = ContentValues().apply &#123;</span><br><span class="line">        put(<span class="string">&quot;name&quot;</span>, name)</span><br><span class="line">        put(<span class="string">&quot;code&quot;</span>, code)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 调用insert方法，将数据插入City表</span></span><br><span class="line">    <span class="keyword">val</span> newRowId = db.insert(<span class="string">&quot;City&quot;</span>, <span class="literal">null</span>, values)</span><br><span class="line">    <span class="comment">// 判断插入是否成功</span></span><br><span class="line">    <span class="keyword">if</span> (newRowId != -<span class="number">1L</span>) &#123;</span><br><span class="line">        println(<span class="string">&quot;插入成功，Row ID: <span class="variable">$newRowId</span>&quot;</span>)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        println(<span class="string">&quot;插入失败&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 操作结束后关闭数据库</span></span><br><span class="line">    db.close()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>注意事项</strong></p><ul><li>插入操作一般要在子线程中执行，避免阻塞主线程。可以使用Kotlin协程或其它异步处理方式。</li><li>可以通过事务优化批量插入的性能。</li></ul><h4 id="2-更新数据"><a class="markdownIt-Anchor" href="#2-更新数据"></a> 2. 更新数据</h4><p><strong>核心步骤</strong></p><ul><li>获取数据库实例（使用<code>writableDatabase</code>）。</li><li>构造一个<code>ContentValues</code>对象来表示要更新的字段及其新值。</li><li>使用<code>update()</code>方法，传入更新条件和条件参数。</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">updateCityName</span><span class="params">(dbHelper: <span class="type">DBHelper</span>, cityId: <span class="type">Int</span>, newName: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> db = dbHelper.writableDatabase</span><br><span class="line">    <span class="comment">// 构造ContentValues更新记录</span></span><br><span class="line">    <span class="keyword">val</span> values = ContentValues().apply &#123;</span><br><span class="line">        put(<span class="string">&quot;name&quot;</span>, newName)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 构造更新条件</span></span><br><span class="line">    <span class="keyword">val</span> selection = <span class="string">&quot;id = ?&quot;</span></span><br><span class="line">    <span class="keyword">val</span> selectionArgs = arrayOf(cityId.toString())</span><br><span class="line">    <span class="comment">// 调用update方法更新数据</span></span><br><span class="line">    <span class="keyword">val</span> count = db.update(<span class="string">&quot;City&quot;</span>, values, selection, selectionArgs)</span><br><span class="line">    println(<span class="string">&quot;更新了<span class="variable">$count</span>条记录&quot;</span>)</span><br><span class="line">    db.close()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>注意事项</strong></p><ul><li>更新操作需要谨慎构造条件（WHERE子句），避免误更新多条记录。</li><li>同样建议在子线程中执行更新操作以避免主线程阻塞。</li></ul><h4 id="3-删除数据"><a class="markdownIt-Anchor" href="#3-删除数据"></a> 3. 删除数据</h4><p><strong>核心步骤</strong></p><ul><li>获取可写数据库实例。</li><li>构造删除条件（WHERE子句）及其参数。</li><li>调用<code>delete()</code>方法删除记录。</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">deleteCity</span><span class="params">(dbHelper: <span class="type">DBHelper</span>, cityId: <span class="type">Int</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> db = dbHelper.writableDatabase</span><br><span class="line">    <span class="comment">// 构造删除条件</span></span><br><span class="line">    <span class="keyword">val</span> selection = <span class="string">&quot;id = ?&quot;</span></span><br><span class="line">    <span class="keyword">val</span> selectionArgs = arrayOf(cityId.toString())</span><br><span class="line">    <span class="comment">// 调用delete方法删除记录</span></span><br><span class="line">    <span class="keyword">val</span> deletedRows = db.delete(<span class="string">&quot;City&quot;</span>, selection, selectionArgs)</span><br><span class="line">    println(<span class="string">&quot;删除了<span class="variable">$deletedRows</span>条记录&quot;</span>)</span><br><span class="line">    db.close()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>注意事项</strong></p><ul><li>删除数据时一定要构造准确的删除条件，避免误删数据。</li><li>如需删除所有数据，可以传入<code>null</code>作为条件，但要慎重使用。</li></ul><h4 id="4-查询数据"><a class="markdownIt-Anchor" href="#4-查询数据"></a> 4. 查询数据</h4><p><strong>核心步骤</strong></p><ul><li>获取数据库实例（通常使用<code>readableDatabase</code>）。</li><li>构造要查询的列数组、WHERE条件（可选）以及排序等参数。</li><li>调用<code>query()</code>方法或者使用<code>rawQuery()</code>执行SQL语句。</li><li>使用<code>Cursor</code>遍历查询结果，并通过<code>getColumnIndexOrThrow</code>获取列数据。</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">queryCities</span><span class="params">(dbHelper: <span class="type">DBHelper</span>)</span></span>: List&lt;Triple&lt;<span class="built_in">Int</span>, String, String&gt;&gt; &#123;</span><br><span class="line">    <span class="keyword">val</span> db = dbHelper.readableDatabase</span><br><span class="line">    <span class="comment">// 指定要返回的列</span></span><br><span class="line">    <span class="keyword">val</span> projection = arrayOf(<span class="string">&quot;id&quot;</span>, <span class="string">&quot;name&quot;</span>, <span class="string">&quot;code&quot;</span>)</span><br><span class="line">    <span class="comment">// 查询City表中的所有数据</span></span><br><span class="line">    <span class="keyword">val</span> cursor: Cursor = db.query(</span><br><span class="line">        <span class="string">&quot;City&quot;</span>,        <span class="comment">// 表名</span></span><br><span class="line">        projection,    <span class="comment">// 返回的列</span></span><br><span class="line">        <span class="literal">null</span>,          <span class="comment">// WHERE子句</span></span><br><span class="line">        <span class="literal">null</span>,          <span class="comment">// WHERE参数</span></span><br><span class="line">        <span class="literal">null</span>,          <span class="comment">// GROUP BY子句</span></span><br><span class="line">        <span class="literal">null</span>,          <span class="comment">// HAVING子句</span></span><br><span class="line">        <span class="string">&quot;id ASC&quot;</span>       <span class="comment">// ORDER BY子句：按id升序排列</span></span><br><span class="line">    )</span><br><span class="line">    <span class="comment">// 存储查询结果的集合</span></span><br><span class="line">    <span class="keyword">val</span> cities = mutableListOf&lt;Triple&lt;<span class="built_in">Int</span>, String, String&gt;&gt;()</span><br><span class="line">    with(cursor) &#123;</span><br><span class="line">        <span class="keyword">while</span> (moveToNext()) &#123;</span><br><span class="line">            <span class="comment">// 根据列名获取数据</span></span><br><span class="line">            <span class="keyword">val</span> id = getInt(getColumnIndexOrThrow(<span class="string">&quot;id&quot;</span>))</span><br><span class="line">            <span class="keyword">val</span> name = getString(getColumnIndexOrThrow(<span class="string">&quot;name&quot;</span>))</span><br><span class="line">            <span class="keyword">val</span> code = getString(getColumnIndexOrThrow(<span class="string">&quot;code&quot;</span>))</span><br><span class="line">            cities.add(Triple(id, name, code))</span><br><span class="line">        &#125;</span><br><span class="line">        close()  <span class="comment">// 用完Cursor后关闭，防止内存泄漏</span></span><br><span class="line">    &#125;</span><br><span class="line">    db.close()</span><br><span class="line">    <span class="keyword">return</span> cities</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>注意事项</strong></p><ul><li>查询操作后要及时关闭<code>Cursor</code>和数据库。</li><li>当需要查询大量数据时，可考虑使用分页查询以提高性能。</li><li>对于复杂查询，可以使用<code>rawQuery()</code>方法执行自定义的SQL语句。</li></ul><h4 id="5-事务"><a class="markdownIt-Anchor" href="#5-事务"></a> 5. 事务</h4><ol><li><strong>开启事务</strong>：调用<code>beginTransaction()</code>开始一个事务。</li><li><strong>执行数据库操作</strong>：在事务中执行多条数据库操作（如插入、更新等）。</li><li><strong>标记事务成功</strong>：当所有操作执行成功后，调用<code>setTransactionSuccessful()</code>标记事务成功。如果没有调用这一方法，后续<code>endTransaction()</code>时将会回滚事务。</li><li><strong>结束事务</strong>：调用<code>endTransaction()</code>结束事务。如果事务成功，则提交所有操作，否则回滚所有操作。</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">insertMultipleCities</span><span class="params">(dbHelper: <span class="type">DBHelper</span>, cities: <span class="type">List</span>&lt;<span class="type">Pair</span>&lt;<span class="type">String</span>, String&gt;&gt;)</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> db = dbHelper.writableDatabase</span><br><span class="line">    <span class="comment">// 开始事务</span></span><br><span class="line">    db.beginTransaction()</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        cities.forEach &#123; city -&gt;</span><br><span class="line">            <span class="keyword">val</span> values = ContentValues().apply &#123;</span><br><span class="line">                put(<span class="string">&quot;name&quot;</span>, city.first)</span><br><span class="line">                put(<span class="string">&quot;code&quot;</span>, city.second)</span><br><span class="line">            &#125;</span><br><span class="line">            db.insert(<span class="string">&quot;City&quot;</span>, <span class="literal">null</span>, values)</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 标记事务成功</span></span><br><span class="line">        db.setTransactionSuccessful()</span><br><span class="line">        println(<span class="string">&quot;批量插入成功&quot;</span>)</span><br><span class="line">    &#125; <span class="keyword">catch</span> (e: Exception) &#123;</span><br><span class="line">        e.printStackTrace()</span><br><span class="line">        println(<span class="string">&quot;批量插入失败，事务回滚&quot;</span>)</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="comment">// 结束事务，如果未调用setTransactionSuccessful，则会自动回滚</span></span><br><span class="line">        db.endTransaction()</span><br><span class="line">        db.close()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="5探究contentprovider"><a class="markdownIt-Anchor" href="#5探究contentprovider"></a> 5.探究ContentProvider</h2><ul><li><strong>定义</strong>：ContentProvider 是一种Android组件，允许应用程序以标准化接口对数据进行访问，无论数据存储在 SQLite 数据库、文件系统还是其他位置。</li><li><strong>数据共享</strong>：它主要用于跨进程或跨应用间的数据共享，通过定义 URI（统一资源标识符）来标识数据资源，并提供增删改查（CRUD）的接口。</li></ul><h3 id="申请权限"><a class="markdownIt-Anchor" href="#申请权限"></a> 申请权限</h3><ol><li>借助<code>ContextCompat.checkSelfPermission()</code>方法判断是否授予权限</li><li>调用<code>ActivityCompat.requestPermissions()</code>方法向用户申请权限</li><li>在<code>AndroidManifest</code>申请权限</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// onCreate是Activity的生命周期回调，用于初始化视图和绑定事件</span></span><br><span class="line">    <span class="meta">@SuppressLint(<span class="string">&quot;MissingInflatedId&quot;</span>)</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 加载主布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取按钮控件</span></span><br><span class="line">        <span class="keyword">val</span> makeCall = findViewById&lt;Button&gt;(R.id.makeCall)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 设置按钮的点击事件监听器</span></span><br><span class="line">        makeCall.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 检查CALL_PHONE权限是否已授予</span></span><br><span class="line">            <span class="keyword">if</span> (ContextCompat.checkSelfPermission(<span class="keyword">this</span>, Manifest.permission.CALL_PHONE) != PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                <span class="comment">// 如果权限未授予，请求权限</span></span><br><span class="line">                ActivityCompat.requestPermissions(<span class="keyword">this</span>, arrayOf(Manifest.permission.CALL_PHONE), <span class="number">1</span>)</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 如果权限已授予，执行拨打电话操作</span></span><br><span class="line">                call()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// onRequestPermissionsResult用于处理权限请求的结果</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onRequestPermissionsResult</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        requestCode: <span class="type">Int</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">        permissions: <span class="type">Array</span>&lt;<span class="type">out</span> <span class="type">String</span>&gt;,</span></span></span><br><span class="line"><span class="params"><span class="function">        grantResults: <span class="type">IntArray</span></span></span></span><br><span class="line"><span class="params"><span class="function">    )</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onRequestPermissionsResult(requestCode, permissions, grantResults)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 根据requestCode判断是哪个权限请求</span></span><br><span class="line">        <span class="keyword">when</span> (requestCode) &#123;</span><br><span class="line">            <span class="number">1</span> -&gt; &#123;</span><br><span class="line">                <span class="comment">// 如果权限请求结果不为空，并且第一个权限被授予</span></span><br><span class="line">                <span class="keyword">if</span> (grantResults.isNotEmpty() &amp;&amp; grantResults[<span class="number">0</span>] == PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                    <span class="comment">// 执行拨打电话操作</span></span><br><span class="line">                    call()</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 显示权限被拒绝的提示信息</span></span><br><span class="line">                    Toast.makeText(<span class="keyword">this</span>, <span class="string">&quot;You denied the permission&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 封装的拨打电话功能函数</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">call</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 创建一个Intent，用于执行拨打电话操作</span></span><br><span class="line">            <span class="keyword">val</span> intent = Intent(Intent.ACTION_CALL)</span><br><span class="line">            <span class="comment">// 设置拨打的电话号码，这里使用的是10086</span></span><br><span class="line">            intent.<span class="keyword">data</span> = Uri.parse(<span class="string">&quot;tel:10086&quot;</span>)</span><br><span class="line">            <span class="comment">// 启动Activity，执行拨打操作</span></span><br><span class="line">            startActivity(intent)</span><br><span class="line">        &#125; <span class="keyword">catch</span> (e: SecurityException) &#123;</span><br><span class="line">            <span class="comment">// 捕获并处理安全异常</span></span><br><span class="line">            e.printStackTrace()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.CALL_PHONE&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><h3 id="contentresolver的基本用法"><a class="markdownIt-Anchor" href="#contentresolver的基本用法"></a> ContentResolver的基本用法</h3><p><code>ContentResolver</code> 是 Android 提供的一个接口，用于与 <code>ContentProvider</code> 交互，从而实现跨进程或跨应用的数据访问。通过 ContentResolver，你可以对 ContentProvider 中的数据执行查询、插入、更新和删除等操作.</p><h4 id="1-获取-contentresolver-实例"><a class="markdownIt-Anchor" href="#1-获取-contentresolver-实例"></a> 1. 获取 ContentResolver 实例</h4><p>在 Android 中，通常通过上下文（Context）来获取 ContentResolver 实例：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> contentResolver = context.contentResolver</span><br></pre></td></tr></table></figure><h4 id="2查询数据"><a class="markdownIt-Anchor" href="#2查询数据"></a> 2.查询数据</h4><p><code>query()</code> 方法用于从 ContentProvider 查询数据，参数说明如下：</p><ul><li><strong>uri</strong>：需要访问的数据的 URI，例如：<code>content://com.example.app.provider/city</code></li><li><strong>projection</strong>：需要查询的列数组，如果传入 null，则返回所有列</li><li><strong>selection</strong>：过滤条件（WHERE 子句），可使用 ? 占位符</li><li><strong>selectionArgs</strong>：过滤条件对应的参数数组</li><li><strong>sortOrder</strong>：排序规则（例如 <code>&quot;name ASC&quot;</code>）</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> uri = Uri.parse(<span class="string">&quot;content://com.example.app.provider/city&quot;</span>)</span><br><span class="line"><span class="keyword">val</span> projection = arrayOf(<span class="string">&quot;id&quot;</span>, <span class="string">&quot;name&quot;</span>, <span class="string">&quot;code&quot;</span>)</span><br><span class="line"><span class="keyword">val</span> cursor = contentResolver.query(uri, projection, <span class="literal">null</span>, <span class="literal">null</span>, <span class="string">&quot;id ASC&quot;</span>)</span><br><span class="line">cursor?.use &#123;</span><br><span class="line">    <span class="keyword">while</span> (it.moveToNext()) &#123;</span><br><span class="line">        <span class="keyword">val</span> id = it.getInt(it.getColumnIndexOrThrow(<span class="string">&quot;id&quot;</span>))</span><br><span class="line">        <span class="keyword">val</span> name = it.getString(it.getColumnIndexOrThrow(<span class="string">&quot;name&quot;</span>))</span><br><span class="line">        <span class="keyword">val</span> code = it.getString(it.getColumnIndexOrThrow(<span class="string">&quot;code&quot;</span>))</span><br><span class="line">        println(<span class="string">&quot;City: id=<span class="variable">$id</span>, name=<span class="variable">$name</span>, code=<span class="variable">$code</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><blockquote><p>注意：查询结束后需要关闭 Cursor（使用 <code>use &#123;&#125;</code> 块可自动关闭）。</p></blockquote><h4 id="3-插入数据"><a class="markdownIt-Anchor" href="#3-插入数据"></a> 3. 插入数据</h4><p><code>insert()</code> 方法用于在 ContentProvider 中插入新数据，参数说明如下：</p><ul><li><strong>uri</strong>：目标数据的 URI</li><li><strong>ContentValues</strong>：存储键值对数据</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> uri = Uri.parse(<span class="string">&quot;content://com.example.app.provider/city&quot;</span>)</span><br><span class="line"><span class="keyword">val</span> values = ContentValues().apply &#123;</span><br><span class="line">    put(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;Beijing&quot;</span>)</span><br><span class="line">    put(<span class="string">&quot;code&quot;</span>, <span class="string">&quot;BJ&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">val</span> newUri = contentResolver.insert(uri, values)</span><br><span class="line">println(<span class="string">&quot;插入成功，新记录URI：<span class="variable">$newUri</span>&quot;</span>)</span><br></pre></td></tr></table></figure><h4 id="4更新数据"><a class="markdownIt-Anchor" href="#4更新数据"></a> 4.更新数据</h4><p><code>update()</code> 方法用于更新 ContentProvider 中的数据，参数说明如下：</p><ul><li><strong>uri</strong>：目标数据的 URI</li><li><strong>ContentValues</strong>：存储需要更新的字段和值</li><li><strong>selection</strong>：指定更新条件</li><li><strong>selectionArgs</strong>：条件参数数组</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> uri = Uri.parse(<span class="string">&quot;content://com.example.app.provider/city&quot;</span>)</span><br><span class="line"><span class="keyword">val</span> values = ContentValues().apply &#123;</span><br><span class="line">    put(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;Shanghai&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">val</span> selection = <span class="string">&quot;id = ?&quot;</span></span><br><span class="line"><span class="keyword">val</span> selectionArgs = arrayOf(<span class="string">&quot;1&quot;</span>)</span><br><span class="line"><span class="keyword">val</span> updateCount = contentResolver.update(uri, values, selection, selectionArgs)</span><br><span class="line">println(<span class="string">&quot;更新了 <span class="variable">$updateCount</span> 条记录&quot;</span>)</span><br></pre></td></tr></table></figure><h4 id="5删除数据"><a class="markdownIt-Anchor" href="#5删除数据"></a> 5.删除数据</h4><p><code>delete()</code> 方法用于删除 ContentProvider 中的数据，参数说明如下：</p><ul><li><strong>uri</strong>：目标数据的 URI</li><li><strong>selection</strong>：删除条件</li><li><strong>selectionArgs</strong>：条件参数数组</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> uri = Uri.parse(<span class="string">&quot;content://com.example.app.provider/city&quot;</span>)</span><br><span class="line"><span class="keyword">val</span> selection = <span class="string">&quot;id = ?&quot;</span></span><br><span class="line"><span class="keyword">val</span> selectionArgs = arrayOf(<span class="string">&quot;1&quot;</span>)</span><br><span class="line"><span class="keyword">val</span> deleteCount = contentResolver.delete(uri, selection, selectionArgs)</span><br><span class="line">println(<span class="string">&quot;删除了 <span class="variable">$deleteCount</span> 条记录&quot;</span>)</span><br></pre></td></tr></table></figure><h3 id="创建contentprovider的步骤"><a class="markdownIt-Anchor" href="#创建contentprovider的步骤"></a> 创建ContentProvider的步骤</h3><p><strong>1.继承 ContentProvider 类</strong><br />创建一个新的类，继承自 <code>ContentProvider</code>，并重写所有抽象方法（如 <code>onCreate()</code>、<code>query()</code>、<code>insert()</code>、<code>update()</code>、<code>delete()</code>、<code>getType()</code>）。</p><p><strong>实现 onCreate() 方法</strong><br />在 <code>onCreate()</code> 中进行必要的初始化操作，比如数据库连接的创建或其他资源的初始化。此方法在 ContentProvider 实例被创建时调用。</p><p><strong>2.实现数据操作方法</strong></p><ul><li><strong>query()</strong>：根据传入的 URI 查询数据，并返回一个 Cursor 对象。</li><li><strong>insert()</strong>：根据传入的 URI 插入数据，返回新插入数据的 URI。</li><li><strong>update()</strong>：根据 URI 更新数据，返回受影响的行数。</li><li><strong>delete()</strong>：根据 URI 删除数据，返回删除的行数。</li><li><strong>getType()</strong>：返回指定 URI 对应的数据 MIME 类型。</li></ul><p><strong>3.配置 URI 匹配器</strong><br />使用 <code>UriMatcher</code> 对传入的 URI 进行匹配，以便确定请求类型并执行相应操作。可以在静态代码块中添加匹配规则，例如：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> static <span class="keyword">final</span> UriMatcher uriMatcher = new UriMatcher(UriMatcher.NO_MATCH);</span><br><span class="line">static &#123;</span><br><span class="line">    uriMatcher.addURI(<span class="string">&quot;your.authority&quot;</span>, <span class="string">&quot;your_path&quot;</span>, CODE_CONSTANT);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>4.在 AndroidManifest.xml 中注册 ContentProvider</strong><br />在清单文件中注册你的 ContentProvider：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">provider</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:name</span>=<span class="string">&quot;.YourContentProvider&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:authorities</span>=<span class="string">&quot;your.authority&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:exported</span>=<span class="string">&quot;true&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><p>注意：<code>android:exported</code> 的值根据你的需求设置为 <code>true</code> 或 <code>false</code>。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">DatabaseProvider</span> : <span class="type">ContentProvider</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Uri匹配器的代码，用于区分不同的Uri路径</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> bookDir = <span class="number">0</span>    <span class="comment">// Uri对应书籍目录</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> bookItem = <span class="number">1</span>   <span class="comment">// Uri对应单个书籍项</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> categoryDir = <span class="number">2</span> <span class="comment">// Uri对应类别目录</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> categoryItem = <span class="number">3</span> <span class="comment">// Uri对应单个类别项</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> authority = <span class="string">&quot;com.example.databasetest.provider&quot;</span> <span class="comment">// 内容提供者的唯一标识</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">var</span> dbHelper: MyDatabaseHelper? = <span class="literal">null</span> <span class="comment">// 数据库帮助器实例</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 懒加载的Uri匹配器，用于根据Uri路径匹配不同的数据操作</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> uriMatcher <span class="keyword">by</span> lazy &#123;</span><br><span class="line">        <span class="keyword">val</span> matcher = UriMatcher(UriMatcher.NO_MATCH)</span><br><span class="line">        <span class="comment">// 添加Uri匹配规则</span></span><br><span class="line">        matcher.addURI(authority, <span class="string">&quot;book&quot;</span>, bookDir) <span class="comment">// 匹配书籍目录</span></span><br><span class="line">        matcher.addURI(authority, <span class="string">&quot;book/#&quot;</span>, bookItem) <span class="comment">// 匹配单个书籍项（#表示数字）</span></span><br><span class="line">        matcher.addURI(authority, <span class="string">&quot;category&quot;</span>, categoryDir) <span class="comment">// 匹配类别目录</span></span><br><span class="line">        matcher.addURI(authority, <span class="string">&quot;category/#&quot;</span>, categoryItem) <span class="comment">// 匹配单个类别项</span></span><br><span class="line">        matcher</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 删除数据</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> uri         要删除的Uri</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> selection   删除条件</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> selectionArgs 条件中的参数</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 删除的行数</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">delete</span><span class="params">(uri: <span class="type">Uri</span>, selection: <span class="type">String</span>?, selectionArgs: <span class="type">Array</span>&lt;<span class="type">String</span>&gt;?)</span></span> = dbHelper?.let &#123;</span><br><span class="line">        <span class="keyword">val</span> db = it.writableDatabase <span class="comment">// 获取可写数据库实例</span></span><br><span class="line">        <span class="keyword">val</span> deletedRows = <span class="keyword">when</span> (uriMatcher.match(uri)) &#123; <span class="comment">// 根据Uri匹配不同的删除操作</span></span><br><span class="line">            bookDir -&gt; db.delete(<span class="string">&quot;Book&quot;</span>, selection, selectionArgs) <span class="comment">// 删除书籍目录下的记录</span></span><br><span class="line">            bookItem -&gt; &#123;</span><br><span class="line">                <span class="keyword">val</span> bookId = uri.pathSegments[<span class="number">1</span>] <span class="comment">// 获取书籍ID</span></span><br><span class="line">                db.delete(<span class="string">&quot;Book&quot;</span>, <span class="string">&quot;id = ?&quot;</span>, arrayOf(bookId)) <span class="comment">// 删除指定书籍</span></span><br><span class="line">            &#125;</span><br><span class="line">            categoryDir -&gt; db.delete(<span class="string">&quot;Category&quot;</span>, selection, selectionArgs) <span class="comment">// 删除类别目录下的记录</span></span><br><span class="line">            categoryItem -&gt; &#123;</span><br><span class="line">                <span class="keyword">val</span> categoryId = uri.pathSegments[<span class="number">1</span>] <span class="comment">// 获取类别ID</span></span><br><span class="line">                db.delete(<span class="string">&quot;Category&quot;</span>, <span class="string">&quot;id = ?&quot;</span>, arrayOf(categoryId)) <span class="comment">// 删除指定类别</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> -&gt; <span class="number">0</span> <span class="comment">// Uri不匹配，删除0行</span></span><br><span class="line">        &#125;</span><br><span class="line">        deletedRows</span><br><span class="line">    &#125; ?: <span class="number">0</span> <span class="comment">// 如果dbHelper为空，返回0</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 获取Uri对应的MIME类型</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> uri 要获取类型的Uri</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> MIME类型字符串</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">getType</span><span class="params">(uri: <span class="type">Uri</span>)</span></span> = <span class="keyword">when</span> (uriMatcher.match(uri)) &#123;</span><br><span class="line">        bookDir -&gt; <span class="string">&quot;vnd.android.cursor.dir/vnd.com.example.databasetest.provider.book&quot;</span> <span class="comment">// 书籍目录MIME类型</span></span><br><span class="line">        bookItem -&gt; <span class="string">&quot;vnd.android.cursor.item/vnd.com.example.databasetest.provider.book&quot;</span> <span class="comment">// 单个书籍项MIME类型</span></span><br><span class="line">        categoryDir -&gt; <span class="string">&quot;vnd.android.cursor.dir/vnd.com.example.databasetest.provider.category&quot;</span> <span class="comment">// 类别目录MIME类型</span></span><br><span class="line">        categoryItem -&gt; <span class="string">&quot;vnd.android.cursor.item/vnd.com.example.databasetest.provider.category&quot;</span> <span class="comment">// 单个类别项MIME类型</span></span><br><span class="line">        <span class="keyword">else</span> -&gt; <span class="literal">null</span> <span class="comment">// 不匹配的Uri返回null</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 插入新的数据</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> uri     要插入的Uri</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> values  要插入的数据</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 新插入记录的Uri</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">insert</span><span class="params">(uri: <span class="type">Uri</span>, values: <span class="type">ContentValues</span>?)</span></span> = dbHelper?.let &#123;</span><br><span class="line">        <span class="keyword">val</span> db = it.writableDatabase <span class="comment">// 获取可写数据库实例</span></span><br><span class="line">        <span class="keyword">val</span> uriReturn = <span class="keyword">when</span> (uriMatcher.match(uri)) &#123; <span class="comment">// 根据Uri匹配不同的插入操作</span></span><br><span class="line">            bookDir -&gt; &#123; <span class="comment">// 插入书籍目录</span></span><br><span class="line">                <span class="keyword">val</span> newBookId = db.insert(<span class="string">&quot;Book&quot;</span>, <span class="literal">null</span>, values) <span class="comment">// 插入新书籍</span></span><br><span class="line">                Uri.parse(<span class="string">&quot;content://<span class="variable">$authority</span>/book/<span class="variable">$newBookId</span>&quot;</span>) <span class="comment">// 返回新书籍的Uri</span></span><br><span class="line">            &#125;</span><br><span class="line">            categoryDir, categoryItem -&gt; &#123; <span class="comment">// 插入类别目录或单个类别</span></span><br><span class="line">                <span class="keyword">val</span> newCategoryId = db.insert(<span class="string">&quot;Category&quot;</span>, <span class="literal">null</span>, values) <span class="comment">// 插入新类别</span></span><br><span class="line">                Uri.parse(<span class="string">&quot;content://<span class="variable">$authority</span>/category/<span class="variable">$newCategoryId</span>&quot;</span>) <span class="comment">// 返回新类别的Uri</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> -&gt; <span class="literal">null</span> <span class="comment">// Uri不匹配，返回null</span></span><br><span class="line">        &#125;</span><br><span class="line">        uriReturn</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 创建内容提供者时初始化数据库</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 是否成功创建</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">()</span></span> = context?.let &#123;</span><br><span class="line">        dbHelper = MyDatabaseHelper(it, <span class="string">&quot;BookStore.db&quot;</span>, <span class="number">2</span>) <span class="comment">// 初始化数据库帮助器</span></span><br><span class="line">        <span class="literal">true</span> <span class="comment">// 创建成功</span></span><br><span class="line">    &#125; ?: <span class="literal">false</span> <span class="comment">// 创建失败</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 查询数据</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> uri          查询的Uri</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> projection   要查询的列</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> selection    查询条件</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> selectionArgs 条件中的参数</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> sortOrder    排序方式</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 查询结果的Cursor对象</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">query</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        uri: <span class="type">Uri</span>, projection: <span class="type">Array</span>&lt;<span class="type">String</span>&gt;?, selection: <span class="type">String</span>?,</span></span></span><br><span class="line"><span class="params"><span class="function">        selectionArgs: <span class="type">Array</span>&lt;<span class="type">String</span>&gt;?, sortOrder: <span class="type">String</span>?</span></span></span><br><span class="line"><span class="params"><span class="function">    )</span></span> = dbHelper?.let &#123;</span><br><span class="line">        <span class="keyword">val</span> db = it.readableDatabase <span class="comment">// 获取可读数据库实例</span></span><br><span class="line">        <span class="keyword">val</span> cursor = <span class="keyword">when</span> (uriMatcher.match(uri)) &#123; <span class="comment">// 根据Uri匹配不同的查询操作</span></span><br><span class="line">            bookDir -&gt; db.query(<span class="string">&quot;Book&quot;</span>, projection, selection, selectionArgs, <span class="literal">null</span>, <span class="literal">null</span>, sortOrder) <span class="comment">// 查询书籍目录</span></span><br><span class="line">            bookItem -&gt; &#123;</span><br><span class="line">                <span class="keyword">val</span> bookId = uri.pathSegments[<span class="number">1</span>] <span class="comment">// 获取书籍ID</span></span><br><span class="line">                db.query(<span class="string">&quot;Book&quot;</span>, projection, <span class="string">&quot;id = ?&quot;</span>, arrayOf(bookId), <span class="literal">null</span>, <span class="literal">null</span>, sortOrder) <span class="comment">// 查询指定书籍</span></span><br><span class="line">            &#125;</span><br><span class="line">            categoryDir -&gt; db.query(<span class="string">&quot;Category&quot;</span>, projection, selection, selectionArgs, <span class="literal">null</span>, <span class="literal">null</span>, sortOrder) <span class="comment">// 查询类别目录</span></span><br><span class="line">            categoryItem -&gt; &#123;</span><br><span class="line">                <span class="keyword">val</span> categoryId = uri.pathSegments[<span class="number">1</span>] <span class="comment">// 获取类别ID</span></span><br><span class="line">                db.query(<span class="string">&quot;Category&quot;</span>, projection, <span class="string">&quot;id = ?&quot;</span>, arrayOf(categoryId), <span class="literal">null</span>, <span class="literal">null</span>, sortOrder) <span class="comment">// 查询指定类别</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> -&gt; <span class="literal">null</span> <span class="comment">// Uri不匹配，返回null</span></span><br><span class="line">        &#125;</span><br><span class="line">        cursor</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 更新数据</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> uri          要更新的Uri</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> values      更新的值</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> selection   更新条件</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> selectionArgs 条件中的参数</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 更新的行数</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">update</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">        uri: <span class="type">Uri</span>, values: <span class="type">ContentValues</span>?, selection: <span class="type">String</span>?,</span></span></span><br><span class="line"><span class="params"><span class="function">        selectionArgs: <span class="type">Array</span>&lt;<span class="type">String</span>&gt;?</span></span></span><br><span class="line"><span class="params"><span class="function">    )</span></span> = dbHelper?.let &#123;</span><br><span class="line">        <span class="keyword">val</span> db = it.writableDatabase <span class="comment">// 获取可写数据库实例</span></span><br><span class="line">        <span class="keyword">val</span> updatedRows = <span class="keyword">when</span> (uriMatcher.match(uri)) &#123; <span class="comment">// 根据Uri匹配不同的更新操作</span></span><br><span class="line">            bookDir -&gt; db.update(<span class="string">&quot;Book&quot;</span>, values, selection, selectionArgs) <span class="comment">// 更新书籍目录下的记录</span></span><br><span class="line">            bookItem -&gt; &#123;</span><br><span class="line">                <span class="keyword">val</span> bookId = uri.pathSegments[<span class="number">1</span>] <span class="comment">// 获取书籍ID</span></span><br><span class="line">                db.update(<span class="string">&quot;Book&quot;</span>, values, <span class="string">&quot;id = ?&quot;</span>, arrayOf(bookId)) <span class="comment">// 更新指定书籍</span></span><br><span class="line">            &#125;</span><br><span class="line">            categoryDir -&gt; db.update(<span class="string">&quot;Category&quot;</span>, values, selection, selectionArgs) <span class="comment">// 更新类别目录下的记录</span></span><br><span class="line">            categoryItem -&gt; &#123;</span><br><span class="line">                <span class="keyword">val</span> categoryId = uri.pathSegments[<span class="number">1</span>] <span class="comment">// 获取类别ID</span></span><br><span class="line">                db.update(<span class="string">&quot;Category&quot;</span>, values, <span class="string">&quot;id = ?&quot;</span>, arrayOf(categoryId)) <span class="comment">// 更新指定类别</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> -&gt; <span class="number">0</span> <span class="comment">// Uri不匹配，更新0行</span></span><br><span class="line">        &#125;</span><br><span class="line">        updatedRows</span><br><span class="line">    &#125; ?: <span class="number">0</span> <span class="comment">// 如果dbHelper为空，返回0</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><hr /><h2 id="6运用手机多媒体"><a class="markdownIt-Anchor" href="#6运用手机多媒体"></a> 6.运用手机多媒体</h2><h3 id="使用通知"><a class="markdownIt-Anchor" href="#使用通知"></a> 使用通知</h3><ol><li><p>获取<code>NotificationManager</code>实例</p></li><li><p>使用<code>NotificationChannel</code>创建一个通知渠道(通道ID,通道名称,重要等级)</p></li><li><p>动态请求检查<code>POST_NOTIFICATIONS</code>权限</p></li><li><p>创建<code>NotificationBuilder</code>，用于构建通知</p></li><li><p>调用<code>notify()</code>方法发送通知</p></li><li><p>在<code>AndroidManifest</code>申请权限</p></li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="comment">// onCreate方法，用于初始化界面</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 设置界面的布局</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取sendNotice按钮</span></span><br><span class="line">        <span class="keyword">val</span> sendNotice = findViewById&lt;Button&gt;(R.id.sendNotice)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取NotificationManager实例</span></span><br><span class="line">        <span class="keyword">val</span> manager = getSystemService(NOTIFICATION_SERVICE) <span class="keyword">as</span> NotificationManager</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 检查是否有POST_NOTIFICATIONS权限</span></span><br><span class="line">        <span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.TIRAMISU) &#123;</span><br><span class="line">            <span class="comment">// 如果没有权限，则请求权限</span></span><br><span class="line">            <span class="keyword">if</span> (ContextCompat.checkSelfPermission(<span class="keyword">this</span>, Manifest.permission.POST_NOTIFICATIONS) != PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                <span class="comment">// 请求POST_NOTIFICATIONS权限</span></span><br><span class="line">                ActivityCompat.requestPermissions(<span class="keyword">this</span>, arrayOf(Manifest.permission.POST_NOTIFICATIONS), <span class="number">1</span>)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 检查是否是Android 8.0以上版本</span></span><br><span class="line">        <span class="keyword">if</span>(Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.O) &#123;</span><br><span class="line">            <span class="comment">// 创建通知渠道</span></span><br><span class="line">            <span class="keyword">val</span> channel = NotificationChannel(<span class="string">&quot;normal&quot;</span>, <span class="string">&quot;Normal&quot;</span>, NotificationManager.IMPORTANCE_HIGH)</span><br><span class="line">            <span class="comment">// 创建通知渠道</span></span><br><span class="line">            <span class="keyword">val</span> channel2 = NotificationChannel(<span class="string">&quot;important&quot;</span>, <span class="string">&quot;Important&quot;</span>, NotificationManager.IMPORTANCE_HIGH)</span><br><span class="line">            <span class="comment">// 创建通知渠道</span></span><br><span class="line">            manager.createNotificationChannel(channel)</span><br><span class="line">            <span class="comment">// 创建通知渠道</span></span><br><span class="line">            manager.createNotificationChannel(channel2)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置sendNotice按钮的点击事件</span></span><br><span class="line">        sendNotice.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 创建Intent，用于跳转到NotificationActivity</span></span><br><span class="line">            <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, NotificationActivity::<span class="keyword">class</span>.java)</span><br><span class="line">            <span class="comment">// 创建PendingIntent，用于跳转到NotificationActivity</span></span><br><span class="line">            <span class="keyword">val</span> pi = PendingIntent.getActivity(<span class="keyword">this</span>, <span class="number">0</span>, intent, PendingIntent.FLAG_IMMUTABLE)</span><br><span class="line">            <span class="comment">// 创建NotificationBuilder，用于构建通知</span></span><br><span class="line">            <span class="keyword">val</span> notification = NotificationCompat.Builder(<span class="keyword">this</span>, <span class="string">&quot;normal&quot;</span>)</span><br><span class="line">                <span class="comment">// 设置通知的标题</span></span><br><span class="line">                .setContentTitle(<span class="string">&quot;This is content title&quot;</span>)</span><br><span class="line">                <span class="comment">// 设置通知的内容</span></span><br><span class="line">                .setContentText(<span class="string">&quot;This is content text&quot;</span>)</span><br><span class="line">                <span class="comment">// 设置通知的小图标</span></span><br><span class="line">                .setSmallIcon(R.drawable.ic_launcher_foreground)</span><br><span class="line">                <span class="comment">// 设置通知的大图标</span></span><br><span class="line">                .setLargeIcon(BitmapFactory.decodeResource(resources, R.drawable.ic_launcher_foreground))</span><br><span class="line">                <span class="comment">// 设置通知的内容意图</span></span><br><span class="line">                .setContentIntent(pi)</span><br><span class="line">                <span class="comment">// 设置通知是否自动取消</span></span><br><span class="line">                .setAutoCancel(<span class="literal">true</span>)</span><br><span class="line">                <span class="comment">// 设置通知的样式</span></span><br><span class="line">                .setStyle(</span><br><span class="line">                    NotificationCompat.BigPictureStyle()</span><br><span class="line">                        <span class="comment">// 设置通知的大图</span></span><br><span class="line">                        .bigPicture(BitmapFactory.decodeResource(resources, R.drawable.big_image))</span><br><span class="line">                )</span><br><span class="line">                <span class="comment">// 显示横幅通知所需的设置</span></span><br><span class="line">                .setPriority(NotificationCompat.PRIORITY_HIGH)</span><br><span class="line">                <span class="comment">// 设置通知的默认行为</span></span><br><span class="line">                .setDefaults(NotificationCompat.DEFAULT_ALL)</span><br><span class="line">                <span class="comment">// 构建通知</span></span><br><span class="line">                .build()</span><br><span class="line">            <span class="comment">// 发送通知</span></span><br><span class="line">            manager.notify(<span class="number">1</span>, notification)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.POST_NOTIFICATIONS&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><h3 id="调用摄像头和相册"><a class="markdownIt-Anchor" href="#调用摄像头和相册"></a> 调用摄像头和相册</h3><ol><li><p>创建一个File对象,用于存放图片</p></li><li><p>创建一个新的图片文件 <code>output_image.jpg</code> 并生成 <code>Uri</code>,创建 <code>Intent</code> 启动图库应用，指定 <code>image/*</code> 类型，允许用户选择图片</p></li><li><p>处理从图库返回的图片：获取图片的 <code>Uri</code></p></li><li><p><code>getBitmapFromUri()</code> 方法从 <code>Uri</code> 获取文件描述符并解码为 <code>Bitmap</code> 对象。</p></li><li><p>处理从相机返回的图片：解码图片并显示到 <code>ImageView</code> 上，使用 <code>rotateIfRequired()</code> 检查是否需要旋转图片</p></li><li><p>注册内容提供器并在xml目录下创建<code>file_paths</code>文件</p></li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="comment">// 定义两个常量，分别代表从相机获取图片和从图库获取图片</span></span><br><span class="line">    <span class="keyword">val</span> takePhoto = <span class="number">1</span></span><br><span class="line">    <span class="keyword">val</span> fromAlbum = <span class="number">2</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// Uri类型的变量，用于存储图片的Uri</span></span><br><span class="line">    <span class="keyword">lateinit</span> <span class="keyword">var</span> imageUri: Uri</span><br><span class="line"></span><br><span class="line">    <span class="comment">// File类型的变量，用于存储图片的文件</span></span><br><span class="line">    <span class="keyword">lateinit</span> <span class="keyword">var</span> outputImage: File</span><br><span class="line"></span><br><span class="line">    <span class="comment">// onCreate方法，用于初始化界面</span></span><br><span class="line">    <span class="meta">@SuppressLint(<span class="string">&quot;MissingInflatedId&quot;</span>)</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 设置界面的布局</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取takePhotoBtn按钮</span></span><br><span class="line">        <span class="keyword">val</span> takePhotoBtn = findViewById&lt;Button&gt;(R.id.takePhotoBtn)</span><br><span class="line">        <span class="comment">// 设置takePhotoBtn按钮的点击事件</span></span><br><span class="line">        takePhotoBtn.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 获取输出图片的文件</span></span><br><span class="line">            outputImage = File(externalCacheDir, <span class="string">&quot;output_image.jpg&quot;</span>)</span><br><span class="line">            <span class="comment">// 如果文件存在，则删除它</span></span><br><span class="line">            <span class="keyword">if</span> (outputImage.exists()) &#123;</span><br><span class="line">                outputImage.delete()</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 创建新文件</span></span><br><span class="line">            outputImage.createNewFile()</span><br><span class="line">            <span class="comment">// 根据系统版本获取Uri</span></span><br><span class="line">            imageUri = <span class="keyword">if</span> (android.os.Build.VERSION.SDK_INT &gt;= android.os.Build.VERSION_CODES.N) &#123;</span><br><span class="line">                <span class="comment">// Android 7.0以上使用FileProvider获取Uri</span></span><br><span class="line">                FileProvider.getUriForFile(<span class="keyword">this</span>, <span class="string">&quot;com.example.cameraalbumtest.fileprovider&quot;</span>, outputImage)</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// Android 7.0以下使用Uri.fromFile获取Uri</span></span><br><span class="line">                Uri.fromFile(outputImage)</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 创建Intent，用于启动相机应用</span></span><br><span class="line">            <span class="keyword">val</span> intent = Intent(<span class="string">&quot;android.media.action.IMAGE_CAPTURE&quot;</span>)</span><br><span class="line">            <span class="comment">// 将图片的Uri传递给相机应用</span></span><br><span class="line">            intent.putExtra(MediaStore.EXTRA_OUTPUT, imageUri)</span><br><span class="line">            <span class="comment">// 启动相机应用</span></span><br><span class="line">            startActivityForResult(intent, takePhoto)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取fromAlbumBtn按钮</span></span><br><span class="line">        <span class="keyword">val</span> fromAlbumBtn = findViewById&lt;Button&gt;(R.id.fromAlbumBtn)</span><br><span class="line">        <span class="comment">// 设置fromAlbumBtn按钮的点击事件</span></span><br><span class="line">        fromAlbumBtn.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 创建Intent，用于启动图库应用</span></span><br><span class="line">            <span class="keyword">val</span> intent = Intent(Intent.ACTION_OPEN_DOCUMENT)</span><br><span class="line">            <span class="comment">// 添加图库应用的分类</span></span><br><span class="line">            intent.addCategory(Intent.CATEGORY_OPENABLE)</span><br><span class="line">            <span class="comment">// 设置图库应用的类型</span></span><br><span class="line">            intent.type = <span class="string">&quot;image/*&quot;</span></span><br><span class="line">            <span class="comment">// 启动图库应用</span></span><br><span class="line">            startActivityForResult(intent, fromAlbum)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// onActivityResult方法，用于处理从相机或图库获取的图片</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onActivityResult</span><span class="params">(requestCode: <span class="type">Int</span>, resultCode: <span class="type">Int</span>, <span class="keyword">data</span>: <span class="type">Intent</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onActivityResult(requestCode, resultCode, <span class="keyword">data</span>)</span><br><span class="line">        <span class="comment">// 根据requestCode判断是从相机获取图片还是从图库获取图片</span></span><br><span class="line">        <span class="keyword">when</span> (requestCode) &#123;</span><br><span class="line">            takePhoto -&gt; &#123;</span><br><span class="line">                <span class="comment">// 如果获取图片成功</span></span><br><span class="line">                <span class="keyword">if</span> (resultCode == RESULT_OK) &#123;</span><br><span class="line">                    <span class="comment">// 获取图片的选项</span></span><br><span class="line">                    <span class="keyword">val</span> options = BitmapFactory.Options()</span><br><span class="line">                    <span class="comment">// 设置图片的采样率</span></span><br><span class="line">                    options.inSampleSize = <span class="number">5</span></span><br><span class="line">                    <span class="comment">// 解析图片</span></span><br><span class="line">                    <span class="keyword">val</span> bitmap = BitmapFactory.decodeStream(contentResolver.openInputStream(imageUri))</span><br><span class="line">                    <span class="comment">// 获取imageView</span></span><br><span class="line">                    <span class="keyword">val</span> imageView = findViewById&lt;ImageView&gt;(R.id.imageView)</span><br><span class="line">                    <span class="comment">// 将图片设置到imageView上</span></span><br><span class="line">                    imageView.setImageBitmap(rotateIfRequired(bitmap))</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            fromAlbum -&gt; &#123;</span><br><span class="line">                <span class="comment">// 如果获取图片成功且data不为空</span></span><br><span class="line">                <span class="keyword">if</span> (resultCode == RESULT_OK &amp;&amp; <span class="keyword">data</span> != <span class="literal">null</span>) &#123;</span><br><span class="line">                    <span class="comment">// 获取图片的选项</span></span><br><span class="line">                    <span class="keyword">val</span> options = BitmapFactory.Options()</span><br><span class="line">                    <span class="comment">// 设置图片的采样率</span></span><br><span class="line">                    options.inSampleSize = <span class="number">5</span></span><br><span class="line">                    <span class="comment">// 获取图片的Uri</span></span><br><span class="line">                    <span class="keyword">data</span>.<span class="keyword">data</span>?.let &#123; uri -&gt;</span><br><span class="line">                        <span class="comment">// 解析图片</span></span><br><span class="line">                        <span class="keyword">val</span> bitmap = getBitmapFromUri(uri)</span><br><span class="line">                        <span class="comment">// 获取imageView</span></span><br><span class="line">                        <span class="keyword">val</span> imageView = findViewById&lt;ImageView&gt;(R.id.imageView)</span><br><span class="line">                        <span class="comment">// 将图片设置到imageView上</span></span><br><span class="line">                        imageView.setImageBitmap(bitmap)</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// getBitmapFromUri方法，用于从Uri获取Bitmap</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">getBitmapFromUri</span><span class="params">(uri: <span class="type">Uri</span>)</span></span>: Bitmap? &#123;</span><br><span class="line">        <span class="comment">// 使用contentResolver.openFileDescriptor获取文件描述符</span></span><br><span class="line">        <span class="keyword">return</span> contentResolver.openFileDescriptor(uri, <span class="string">&quot;r&quot;</span>)?.use &#123;</span><br><span class="line">            <span class="comment">// 使用BitmapFactory.decodeFileDescriptor解析图片</span></span><br><span class="line">            BitmapFactory.decodeFileDescriptor(it.fileDescriptor)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// rotateIfRequired方法，用于判断图片是否需要旋转</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">rotateIfRequired</span><span class="params">(bitmap: <span class="type">Bitmap</span>)</span></span>: Bitmap &#123;</span><br><span class="line">        <span class="comment">// 获取图片的Exif信息</span></span><br><span class="line">        <span class="keyword">val</span> exif = ExifInterface(outputImage.path)</span><br><span class="line">        <span class="comment">// 获取图片的方向</span></span><br><span class="line">        <span class="keyword">val</span> orientation = exif.getAttributeInt(ExifInterface.TAG_ORIENTATION, ExifInterface.ORIENTATION_NORMAL)</span><br><span class="line">        <span class="comment">// 根据方向旋转图片</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">when</span> (orientation) &#123;</span><br><span class="line">            ExifInterface.ORIENTATION_ROTATE_90 -&gt; rotateBitmap(bitmap, <span class="number">90</span>)</span><br><span class="line">            ExifInterface.ORIENTATION_ROTATE_180 -&gt; rotateBitmap(bitmap, <span class="number">180</span>)</span><br><span class="line">            ExifInterface.ORIENTATION_ROTATE_270 -&gt; rotateBitmap(bitmap, <span class="number">270</span>)</span><br><span class="line">            <span class="keyword">else</span> -&gt; bitmap</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// rotateBitmap方法，用于旋转Bitmap</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">rotateBitmap</span><span class="params">(bitmap: <span class="type">Bitmap</span>, degree: <span class="type">Int</span>)</span></span>: Bitmap &#123;</span><br><span class="line">        <span class="comment">// 创建一个旋转矩阵</span></span><br><span class="line">        <span class="keyword">val</span> matrix = android.graphics.Matrix()</span><br><span class="line">        <span class="comment">// 将旋转角度添加到矩阵中</span></span><br><span class="line">        matrix.postRotate(degree.toFloat())</span><br><span class="line">        <span class="comment">// 使用Bitmap.createBitmap旋转图片</span></span><br><span class="line">        <span class="keyword">val</span> rotatedImg = Bitmap.createBitmap(bitmap, <span class="number">0</span>, <span class="number">0</span>, bitmap.width, bitmap.height, matrix, <span class="literal">true</span>)</span><br><span class="line">        <span class="comment">// 回收原图片</span></span><br><span class="line">        bitmap.recycle()</span><br><span class="line">        <span class="comment">// 返回旋转后的图片</span></span><br><span class="line">        <span class="keyword">return</span> rotatedImg</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">paths</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">external-path</span> <span class="attr">name</span>=<span class="string">&quot;my_images&quot;</span> <span class="attr">path</span>=<span class="string">&quot;.&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">paths</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">provider</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:authorities</span>=<span class="string">&quot;com.example.cameraalbumtest.fileprovider&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;androidx.core.content.FileProvider&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:exported</span>=<span class="string">&quot;false&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:grantUriPermissions</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">meta-data</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:name</span>=<span class="string">&quot;android.support.FILE_PROVIDER_PATHS&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:resource</span>=<span class="string">&quot;@xml/file_paths&quot;</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">provider</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="播放音频"><a class="markdownIt-Anchor" href="#播放音频"></a> 播放音频</h3><p><strong>MediaPlayer类中常用控制方法:</strong></p><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td><code>setDataSource(String path)</code></td><td>设置数据源，指定要播放的音频或视频文件的路径。</td></tr><tr><td><code>setDataSource(Context context, Uri uri)</code></td><td>设置数据源，指定要播放的音频或视频文件的 URI。</td></tr><tr><td><code>prepare()</code></td><td>准备 <code>MediaPlayer</code>，使其可以开始播放。该方法在播放前必须调用。</td></tr><tr><td><code>prepareAsync()</code></td><td>异步准备 <code>MediaPlayer</code>，用于播放较大的文件或需要较长时间准备的文件。</td></tr><tr><td><code>start()</code></td><td>启动播放音频或视频。</td></tr><tr><td><code>pause()</code></td><td>暂停音频或视频的播放。</td></tr><tr><td><code>stop()</code></td><td>停止播放音频或视频，重置播放器状态。</td></tr><tr><td><code>seekTo(int msec)</code></td><td>跳转到音频或视频的指定时间位置（毫秒）。</td></tr><tr><td><code>getCurrentPosition()</code></td><td>获取当前播放的时间位置（毫秒）。</td></tr><tr><td><code>getDuration()</code></td><td>获取音频或视频的总时长（毫秒）。</td></tr><tr><td><code>isPlaying()</code></td><td>检查 <code>MediaPlayer</code> 是否正在播放。返回 <code>true</code> 或 <code>false</code>。</td></tr><tr><td><code>setLooping(boolean looping)</code></td><td>设置是否循环播放。<code>true</code> 为循环播放，<code>false</code> 为正常播放。</td></tr><tr><td><code>setVolume(float leftVolume, float rightVolume)</code></td><td>设置音量，<code>leftVolume</code> 和 <code>rightVolume</code> 的值范围为 0.0 到 1.0。</td></tr><tr><td><code>reset()</code></td><td>重置 <code>MediaPlayer</code> 到初始状态，释放所有资源。</td></tr><tr><td><code>release()</code></td><td>释放 <code>MediaPlayer</code> 所占用的所有资源，调用后无法再使用该实例</td></tr></tbody></table><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="comment">// 定义一个MediaPlayer实例</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> mediaPlayer = MediaPlayer()</span><br><span class="line"></span><br><span class="line">    <span class="comment">// onCreate方法，用于初始化界面</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 设置界面的布局</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取play按钮</span></span><br><span class="line">        <span class="keyword">val</span> play = findViewById&lt;Button&gt;(R.id.play)</span><br><span class="line">        <span class="comment">// 获取pause按钮</span></span><br><span class="line">        <span class="keyword">val</span> pause = findViewById&lt;Button&gt;(R.id.pause)</span><br><span class="line">        <span class="comment">// 获取stop按钮</span></span><br><span class="line">        <span class="keyword">val</span> stop = findViewById&lt;Button&gt;(R.id.stop)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 初始化MediaPlayer</span></span><br><span class="line">        initMediaPlayer()</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置play按钮的点击事件</span></span><br><span class="line">        play.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 检查是否正在播放</span></span><br><span class="line">            <span class="keyword">if</span> (!mediaPlayer.isPlaying)&#123;</span><br><span class="line">                <span class="comment">// 开始播放</span></span><br><span class="line">                mediaPlayer.start()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置pause按钮的点击事件</span></span><br><span class="line">        pause.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 检查是否正在播放</span></span><br><span class="line">            <span class="keyword">if</span> (mediaPlayer.isPlaying)&#123;</span><br><span class="line">                <span class="comment">// 暂停播放</span></span><br><span class="line">                mediaPlayer.pause()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置stop按钮的点击事件</span></span><br><span class="line">        stop.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 检查是否正在播放</span></span><br><span class="line">            <span class="keyword">if</span> (mediaPlayer.isPlaying)&#123;</span><br><span class="line">                <span class="comment">// 重置MediaPlayer</span></span><br><span class="line">                mediaPlayer.reset()</span><br><span class="line">                <span class="comment">// 重新初始化MediaPlayer</span></span><br><span class="line">                initMediaPlayer()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// initMediaPlayer方法，用于初始化MediaPlayer</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">initMediaPlayer</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">// 获取AssetManager实例</span></span><br><span class="line">        <span class="keyword">val</span> assetManager = assets</span><br><span class="line">        <span class="comment">// 获取文件描述符</span></span><br><span class="line">        <span class="keyword">val</span> fd = assetManager.openFd(<span class="string">&quot;music.mp3&quot;</span>)</span><br><span class="line">        <span class="comment">// 设置MediaPlayer的数据源</span></span><br><span class="line">        mediaPlayer.setDataSource(fd.fileDescriptor,fd.startOffset,fd.length)</span><br><span class="line">        <span class="comment">// 准备MediaPlayer</span></span><br><span class="line">        mediaPlayer.prepare()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// onDestroy方法，用于销毁界面</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onDestroy()</span><br><span class="line">        <span class="comment">// 停止MediaPlayer</span></span><br><span class="line">        mediaPlayer.stop()</span><br><span class="line">        <span class="comment">// 释放MediaPlayer</span></span><br><span class="line">        mediaPlayer.release()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="播放视频"><a class="markdownIt-Anchor" href="#播放视频"></a> 播放视频</h3><p><strong><code>VideoView</code> 类中常用的控制方法及其功能：</strong></p><table><thead><tr><th>方法</th><th>描述</th></tr></thead><tbody><tr><td><code>setVideoPath(String path)</code></td><td>设置视频文件的路径，通过文件路径指定要播放的视频文件。</td></tr><tr><td><code>setVideoURI(Uri uri)</code></td><td>设置视频的 URI，指定要播放的视频的 URI。</td></tr><tr><td><code>start()</code></td><td>开始播放视频。</td></tr><tr><td><code>pause()</code></td><td>暂停视频播放。</td></tr><tr><td><code>stopPlayback()</code></td><td>停止视频播放，并释放播放器资源。</td></tr><tr><td><code>seekTo(int msec)</code></td><td>跳转到指定时间位置（毫秒）。</td></tr><tr><td><code>getCurrentPosition()</code></td><td>获取当前播放进度（毫秒）。</td></tr><tr><td><code>getDuration()</code></td><td>获取视频的总时长（毫秒）。</td></tr><tr><td><code>isPlaying()</code></td><td>检查视频是否正在播放。返回 <code>true</code> 或 <code>false</code>。</td></tr><tr><td><code>setLooping(boolean looping)</code></td><td>设置是否循环播放视频，<code>true</code> 表示循环播放，<code>false</code> 表示不循环播放。</td></tr></tbody></table><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="comment">// onCreate方法，用于初始化界面</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 设置界面的布局</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取视频URI</span></span><br><span class="line">        <span class="keyword">val</span> uri = Uri.parse(<span class="string">&quot;android.resource://<span class="variable">$packageName</span>/<span class="subst">$&#123;R.raw.video&#125;</span>&quot;</span>)</span><br><span class="line">        <span class="comment">// 获取VideoView实例</span></span><br><span class="line">        <span class="keyword">val</span> videoView = findViewById&lt;VideoView&gt;(R.id.videoView)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置VideoView的视频URI</span></span><br><span class="line">        videoView.setVideoURI(uri)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取play按钮</span></span><br><span class="line">        <span class="keyword">val</span> play = findViewById&lt;Button&gt;(R.id.play)</span><br><span class="line">        <span class="comment">// 获取pause按钮</span></span><br><span class="line">        <span class="keyword">val</span> pause = findViewById&lt;Button&gt;(R.id.pause)</span><br><span class="line">        <span class="comment">// 获取replay按钮</span></span><br><span class="line">        <span class="keyword">val</span> replay = findViewById&lt;Button&gt;(R.id.replay)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置play按钮的点击事件</span></span><br><span class="line">        play.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 检查是否正在播放</span></span><br><span class="line">            <span class="keyword">if</span> (!videoView.isPlaying)&#123;</span><br><span class="line">                <span class="comment">// 开始播放</span></span><br><span class="line">                videoView.start()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置pause按钮的点击事件</span></span><br><span class="line">        pause.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 检查是否正在播放</span></span><br><span class="line">            <span class="keyword">if</span> (videoView.isPlaying)&#123;</span><br><span class="line">                <span class="comment">// 暂停播放</span></span><br><span class="line">                videoView.pause()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置replay按钮的点击事件</span></span><br><span class="line">        replay.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 检查是否正在播放</span></span><br><span class="line">            <span class="keyword">if</span> (videoView.isPlaying)&#123;</span><br><span class="line">                <span class="comment">// 恢复播放</span></span><br><span class="line">                videoView.resume()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// onDestroy方法，用于销毁界面</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onDestroy()</span><br><span class="line">        <span class="comment">// 获取VideoView实例</span></span><br><span class="line">        <span class="keyword">val</span> videoView = findViewById&lt;VideoView&gt;(R.id.videoView)</span><br><span class="line">        <span class="comment">// 暂停播放</span></span><br><span class="line">        videoView.pause()</span><br><span class="line">        <span class="comment">// 释放VideoView</span></span><br><span class="line">        videoView.<span class="keyword">suspend</span>()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="7service"><a class="markdownIt-Anchor" href="#7service"></a> 7.Service</h2><p>Service是Android中实现程序后台运行的解决方案,它非常适合执行那些不需要和用户交互而且还要长期运行的任务,要记得在Service内部创建子线程,并在这里执行具体的任务</p><h3 id="在子线程更新ui异步处理"><a class="markdownIt-Anchor" href="#在子线程更新ui异步处理"></a> 在子线程更新UI(异步处理)</h3><p>在 Android 开发中，主线程（也称为 UI 线程）负责处理用户界面操作和事件分发。如果在主线程上执行耗时操作，会导致 UI 阻塞，应用程序看起来会“冻结”甚至出现“应用程序无响应”（ANR）错误。因此，<strong>当需要执行任何可能耗时的操作时，都应该开启新的线程（或使用其他并发机制）</strong>。</p><p>以下是一些 Android 中需要开启线程的常见例子：</p><ol><li><strong>网络请求 (Network Requests) 网络请求 （Network Requests）</strong><ul><li><strong>例子：</strong> 从服务器下载图片、获取 JSON 数据、上传文件等。</li><li><strong>原因：</strong> 网络操作需要等待服务器响应，耗时不可预测，可能几秒甚至更长。如果在主线程上执行，会导致 UI 卡顿。</li></ul></li><li><strong>数据库操作 (Database Operations)</strong><ul><li><strong>例子：</strong> 查询大量数据、插入/更新/删除大量记录、进行复杂的数据关联操作。</li><li><strong>原因：</strong> 数据库操作，尤其是涉及大量数据的操作，可能会非常耗时。</li></ul></li><li><strong>文件 I/O 操作 (File I/O Operations)</strong><ul><li><strong>例子：</strong> 读取或写入大文件、处理图片或视频文件、进行文件压缩/解压缩。</li><li><strong>原因：</strong> 磁盘读写速度相对较慢，耗时操作可能阻塞 UI。</li></ul></li><li><strong>图片处理 (Image Processing) 图片处理 （Image processing）</strong><ul><li><strong>例子：</strong> 对图片进行缩放、旋转、裁剪、滤镜处理，或从网络加载并解码大图。</li><li><strong>原因：</strong> 图片处理通常涉及大量的计算和内存操作，非常耗时。</li></ul></li><li><strong>耗时计算 (Heavy Computations)<br />耗时计算 （Heavy Computations）</strong><ul><li><strong>例子：</strong> 复杂的数学计算、算法执行、数据排序、数据分析。</li><li><strong>原因：</strong> 任何需要大量 CPU 时间的计算都应在后台线程进行，以避免阻塞主线程。</li></ul></li><li><strong>音视频处理 (Audio/Video Processing)</strong><ul><li><strong>例子：</strong> 编码/解码音视频、录音、播放媒体流。</li><li><strong>原因：</strong> 这些操作通常是实时且计算密集型的。</li></ul></li><li><strong>传感器数据处理 (Sensor Data Processing)</strong><ul><li><strong>例子：</strong> 持续获取和处理大量的传感器数据，例如加速度计或陀螺仪数据。</li><li><strong>原因：</strong> 虽然单个传感器事件可能不耗时，但持续、高频率的处理可能导致性能问题。</li></ul></li></ol><p><strong>Android 中实现多线程的常见方式：</strong></p><p>虽然可以直接使用 Java 的 <code>Thread</code> 和 <code>Runnable</code>，但在 Android 中，通常会使用更高级别的抽象和框架来简化多线程开发，并更好地处理线程间通信（特别是将结果更新到 UI）：</p><ul><li><strong>Kotlin Coroutines (协程)：</strong> 推荐用于 Kotlin 项目，提供更简洁、安全的异步编程方式。</li><li><strong>Java <code>Executor</code> 框架/<code>ThreadPoolExecutor</code>：</strong> 用于管理线程池，避免频繁创建和销毁线程。</li><li><strong><code>AsyncTask</code> (已废弃/不推荐)：</strong> 早期 Android 提供的一种简化异步任务的类，但现在已不推荐使用，因为其内部实现存在一些问题，且容易导致内存泄漏和行为不一致。</li><li><strong><code>Handler</code> 和 <code>Looper</code>：</strong> 用于在不同线程之间发送和处理消息，特别是在后台线程完成任务后更新 UI。</li><li><strong><code>Service</code> 和 <code>IntentService</code> (已废弃/不推荐使用)：</strong> 用于执行不需要 UI 的长时间运行操作。<code>IntentService</code> 会在任务完成后自动停止。</li><li><strong>Jetpack WorkManager：</strong> 推荐用于可延迟、保证执行的任务，即使应用程序退出或设备重启也能完成。</li></ul><img src="/2025/10/06/%E7%AC%AC%E4%B8%80%E8%A1%8C%E4%BB%A3%E7%A0%81-Kotlin/9960da4c00286f063dacb91438ebccd9.png" class="" title="在这里插入图片描述"><ol><li>新增一个<code>Handler</code>对象,并重写一个<code>handleMessage()</code>方法</li><li>设定一个匹配<code>what</code>的值<code>updateText</code></li><li>在主线程调用<code>Handler</code></li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="comment">// 用于标识更新文本的消息类型</span></span><br><span class="line">    <span class="keyword">val</span> updateText = <span class="number">1</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 使用lateinit进行延迟初始化，因为该视图在onCreate中会被赋值</span></span><br><span class="line">    <span class="keyword">lateinit</span> <span class="keyword">var</span> textView: TextView</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Handler实例，用于跨线程发送消息并更新UI</span></span><br><span class="line">    <span class="keyword">val</span> handler = <span class="keyword">object</span> : android.os.Handler() &#123;</span><br><span class="line">        <span class="comment">// 处理接收到的消息</span></span><br><span class="line">        <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">handleMessage</span><span class="params">(msg: <span class="type">android</span>.<span class="type">os</span>.<span class="type">Message</span>)</span></span> &#123;</span><br><span class="line">            <span class="keyword">super</span>.handleMessage(msg)</span><br><span class="line">            <span class="comment">// 根据消息类型执行相应操作</span></span><br><span class="line">            <span class="keyword">when</span> (msg.what) &#123;</span><br><span class="line">                updateText -&gt; &#123;</span><br><span class="line">                    <span class="comment">// 更新TextView的文本</span></span><br><span class="line">                    textView.text = <span class="string">&quot;Nice to meet you!&quot;</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Activity创建时的生命周期回调</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 设置活动的布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 初始化视图，必须在onCreate中进行，因为此时视图已经创建</span></span><br><span class="line">        textView = findViewById(R.id.textView)</span><br><span class="line">        <span class="keyword">val</span> changeTextBtn = findViewById&lt;Button&gt;(R.id.changeTextBtn)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置按钮的点击事件监听器</span></span><br><span class="line">        changeTextBtn.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 启动一个新线程，因为Android不允许在子线程直接更新UI</span></span><br><span class="line">            Thread &#123;</span><br><span class="line">                <span class="comment">// 创建一个消息对象</span></span><br><span class="line">                <span class="keyword">val</span> msg = Message()</span><br><span class="line">                <span class="comment">// 设置消息类型为updateText</span></span><br><span class="line">                msg.what = updateText</span><br><span class="line">                <span class="comment">// 通过Handler将消息发送到主线程，安全更新UI</span></span><br><span class="line">                handler.sendMessage(msg)</span><br><span class="line">            &#125;.start() <span class="comment">// 启动线程</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="解析异步消息处理机制"><a class="markdownIt-Anchor" href="#解析异步消息处理机制"></a> 解析异步消息处理机制</h3><h4 id="1-message"><a class="markdownIt-Anchor" href="#1-message"></a> 1. Message</h4><ul><li><strong>作用</strong><br />Message作为数据载体，用于封装需要传递的信息。它包含的主要字段有：<ul><li><strong>what</strong>：标识消息类型，便于在处理时区分不同的业务逻辑。</li><li><strong>arg1/arg2</strong>：用于传递简单的数值数据。</li><li><strong>obj</strong>：可以传递复杂的数据对象。</li><li><strong>Runnable</strong>：如果设置了Runnable对象，则在消息处理时直接执行该任务。</li></ul></li><li><strong>复用机制</strong><br />通过<code>Message.obtain()</code>获取消息对象，可以复用系统内部的消息池，降低内存分配开销。</li></ul><hr /><h4 id="2-handler"><a class="markdownIt-Anchor" href="#2-handler"></a> 2. Handler</h4><ul><li><strong>作用</strong><br />Handler主要负责发送和处理消息。它将消息放入MessageQueue中，并在消息被取出后执行相应的逻辑。</li><li><strong>主要方法</strong><ul><li><strong>发送消息</strong>：<code>sendMessage()</code>、<code>sendMessageDelayed()</code>、<code>post()</code>等方法用于将消息或Runnable提交到消息队列。</li><li><strong>处理消息</strong>：通过重写<code>handleMessage(Message msg)</code>方法，对不同类型的消息（根据msg.what）进行处理。</li></ul></li><li><strong>线程关联</strong><br />每个Handler都与创建它时所在的线程的Looper绑定，这确保了消息在创建Handler的线程中被处理，从而避免线程安全问题。</li></ul><hr /><h4 id="3-messagequeue"><a class="markdownIt-Anchor" href="#3-messagequeue"></a> 3. MessageQueue</h4><ul><li><strong>作用</strong><br />MessageQueue是消息存储和调度的数据结构，负责保存待处理的消息。</li><li><strong>特点</strong><ul><li>消息按先进先出（FIFO）的顺序（结合延时机制）排列。</li><li>保证消息的顺序性与调度的及时性，确保Looper能依次处理每个消息。</li><li>实际上，每个拥有消息循环的线程都会对应一个MessageQueue。</li></ul></li></ul><hr /><h4 id="4-looper"><a class="markdownIt-Anchor" href="#4-looper"></a> 4. Looper</h4><ul><li><strong>作用</strong><br />Looper为线程提供一个持续运行的消息循环环境，其主要任务是不断从MessageQueue中取出消息，并交由相应的Handler处理。</li><li><strong>工作流程</strong><ol><li><strong>初始化</strong>：在线程启动时调用<code>Looper.prepare()</code>为当前线程创建Looper和MessageQueue（例如主线程在应用启动时自动初始化）。</li><li><strong>循环处理</strong>：调用<code>Looper.loop()</code>进入消息循环，不断取出消息并分发到目标Handler。</li></ol></li><li><strong>线程绑定</strong><br />每个线程只能有一个Looper，这样确保了同一线程内消息的有序执行，避免并发冲突。</li></ul><hr /><h4 id="5-协同工作流程"><a class="markdownIt-Anchor" href="#5-协同工作流程"></a> 5. 协同工作流程</h4><ol><li><strong>初始化消息循环</strong><ul><li>当线程（如UI线程或自定义线程）启动时，调用<code>Looper.prepare()</code>初始化Looper和MessageQueue。</li><li>之后，通过<code>Looper.loop()</code>进入无限循环状态，等待并分发消息。</li></ul></li><li><strong>发送消息</strong><ul><li>通过Handler的<code>sendMessage()</code>或<code>post()</code>方法，将消息或Runnable对象封装成Message后加入到MessageQueue中。</li><li>可设置延时或定时任务（例如<code>sendMessageDelayed()</code>），让消息在指定时间后被处理。</li></ul></li><li><strong>消息分发与处理</strong><ul><li>Looper不断从MessageQueue中取出最前面的消息。</li><li>根据消息中绑定的目标Handler，将消息交由Handler的<code>handleMessage(Message msg)</code>方法处理。</li><li>如果Message中包含Runnable，则直接执行对应的任务。</li></ul></li><li><strong>线程安全与解耦</strong><ul><li>通过Handler与Looper的绑定，确保消息在创建Handler的线程中执行，无需额外的线程同步。</li><li>各组件之间松耦合，生产者只需将任务封装成消息，消费者（Handler）负责执行处理逻辑。</li></ul></li></ol><h3 id="asynctask简单线程切换"><a class="markdownIt-Anchor" href="#asynctask简单线程切换"></a> AsyncTask(简单线程切换)</h3><ul><li><strong>异步任务执行</strong><br />AsyncTask 允许开发者在后台线程中运行耗时任务，并在任务执行过程中或完成后与 UI 线程进行交互，无需手动处理线程间通信。</li><li><strong>轻量级任务</strong><br />它适用于执行短时间任务（一般几秒以内），而不适合长期运行的任务，因为 AsyncTask 内部是通过线程池来管理任务的，长时间运行任务可能导致线程池饱和。</li><li><strong>生命周期方法</strong><br />AsyncTask 提供了四个关键的回调方法，用以控制任务的不同阶段：<ul><li><strong>onPreExecute()</strong>：在任务开始前调用，一般用于在 UI 上展示进度条或进行初始化操作。</li><li><strong>doInBackground(Params… params)</strong>：在后台线程中执行耗时任务，是整个任务的核心逻辑所在。该方法运行在子线程中，不能直接更新 UI。</li><li><strong>onProgressUpdate(Progress… values)</strong>：在任务执行期间调用，用于更新任务进度，可以在 UI 线程中更新进度条等视图。</li><li><strong>onPostExecute(Result result)</strong>：任务执行完毕后调用，此时可以将结果传递给 UI 线程，进行界面更新。</li></ul></li></ul><h4 id="asynctask-运行流程"><a class="markdownIt-Anchor" href="#asynctask-运行流程"></a> AsyncTask 运行流程</h4><ol><li><strong>任务准备阶段：onPreExecute()</strong><br />在调用 execute() 方法后，AsyncTask 会先在 UI 线程中执行 onPreExecute() 方法。此阶段常用于进行初始化操作，比如在 UI 上显示加载提示或初始化进度条。</li><li><strong>后台任务执行阶段：doInBackground()</strong><br />onPreExecute() 执行完成后，AsyncTask 会启动一个后台线程，在该线程中调用 doInBackground(Params… params) 方法。<ul><li>该方法中执行耗时操作，不能直接更新 UI。</li><li>如果需要在任务执行过程中更新进度，可以调用 publishProgress()，从而触发 onProgressUpdate()。</li></ul></li><li><strong>进度更新阶段：onProgressUpdate()</strong><br />当后台任务中调用 publishProgress() 后，onProgressUpdate(Progress… values) 方法会在 UI 线程中被调用。<ul><li>这个方法用于更新界面，比如更新进度条、显示当前进度等。</li></ul></li><li><strong>任务完成阶段：onPostExecute()</strong><br />当 doInBackground() 执行完毕后，返回的结果会传递给 onPostExecute(Result result) 方法，该方法也运行在 UI 线程中。<ul><li>常用于将后台任务的结果展示到 UI 上，或者隐藏加载提示等。</li></ul></li><li><strong>任务取消：onCancelled()</strong><br />如果在任务执行过程中调用 cancel() 方法取消任务，则会触发 onCancelled() 方法（或 onCancelled(Result result)），用于处理任务取消后的逻辑。</li></ol><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">inner</span> <span class="keyword">class</span> <span class="title class_">DownloadTask</span> : <span class="type">AsyncTask</span>&lt;<span class="type">String, Int, String</span>&gt;() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在任务开始前执行，在 UI 线程中调用</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onPreExecute</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onPreExecute()</span><br><span class="line">        <span class="comment">// 显示进度条并初始化</span></span><br><span class="line">        progressBar.visibility = View.VISIBLE</span><br><span class="line">        progressBar.progress = <span class="number">0</span></span><br><span class="line">        tvStatus.text = <span class="string">&quot;开始下载...&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在后台线程中执行耗时任务，接收多个 URL 参数</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">doInBackground</span><span class="params">(<span class="keyword">vararg</span> urls: <span class="type">String</span>)</span></span>: String &#123;</span><br><span class="line">        <span class="keyword">val</span> count = urls.size</span><br><span class="line">        <span class="keyword">for</span> (i <span class="keyword">in</span> <span class="number">0</span> until count) &#123;</span><br><span class="line">            <span class="comment">// 模拟下载耗时，每个文件耗时1秒</span></span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">1000</span>)</span><br><span class="line">            &#125; <span class="keyword">catch</span> (e: InterruptedException) &#123;</span><br><span class="line">                e.printStackTrace()</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 发布当前进度</span></span><br><span class="line">            publishProgress(((i + <span class="number">1</span>) * <span class="number">100</span>) / count)</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 返回任务执行结果</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;下载完成&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在 UI 线程中更新任务进度</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onProgressUpdate</span><span class="params">(<span class="keyword">vararg</span> values: <span class="type">Int</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onProgressUpdate(*values)</span><br><span class="line">        <span class="keyword">val</span> progress = values[<span class="number">0</span>] ?: <span class="number">0</span></span><br><span class="line">        progressBar.progress = progress</span><br><span class="line">        tvStatus.text = <span class="string">&quot;下载进度: <span class="variable">$progress</span>%&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在任务完成后执行，在 UI 线程中调用</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onPostExecute</span><span class="params">(result: <span class="type">String</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onPostExecute(result)</span><br><span class="line">        progressBar.visibility = View.GONE</span><br><span class="line">        tvStatus.text = result ?: <span class="string">&quot;&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 处理任务取消的逻辑</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCancelled</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCancelled()</span><br><span class="line">        tvStatus.text = <span class="string">&quot;任务已取消&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="service的基本用法"><a class="markdownIt-Anchor" href="#service的基本用法"></a> Service的基本用法</h3><h4 id="第一步mainactivity-的整体结构"><a class="markdownIt-Anchor" href="#第一步mainactivity-的整体结构"></a> 第一步：MainActivity 的整体结构</h4><ul><li><strong>继承与初始化</strong><ul><li><code>MainActivity</code> 继承自 <code>AppCompatActivity</code>，是 Android 应用中的主界面。</li><li>使用 <code>lateinit var downloadBinder: MyService.DownloadBinder</code> 声明一个延迟初始化的变量，用于在服务绑定成功后接收服务返回的 Binder 实例。这样可以在绑定服务后调用服务内部的方法。</li></ul></li><li><strong>作用</strong><ul><li>主要负责展示 UI、处理用户点击事件，并通过按钮控制服务的启动、停止、绑定和解绑。</li></ul></li></ul><hr /><h4 id="第二步serviceconnection-实现"><a class="markdownIt-Anchor" href="#第二步serviceconnection-实现"></a> 第二步：ServiceConnection 实现</h4><ul><li><strong>定义 ServiceConnection 对象</strong><ul><li>代码中创建了一个匿名内部类 <code>connection</code> 实现了 <code>ServiceConnection</code> 接口，其主要方法为：<ul><li><code>onServiceConnected</code>：当与服务连接成功后，该方法被调用。这里将传入的 <code>IBinder</code> 强制转换为 <code>MyService.DownloadBinder</code> 类型，然后调用服务中的方法：<ul><li><code>downloadBinder.startDownload()</code>：启动下载操作。</li><li><code>downloadBinder.getProgress()</code>：获取当前下载进度。</li></ul></li><li><code>onServiceDisconnected</code>：当服务意外断开时调用（例如服务崩溃），此处暂未实现具体逻辑。</li></ul></li></ul></li><li><strong>作用</strong><ul><li>该模块负责与 <code>MyService</code> 进行连接，获取服务中的接口，从而使 Activity 能调用服务的方法进行业务操作。</li></ul></li></ul><hr /><h4 id="第三步mainactivity-的-oncreate-方法"><a class="markdownIt-Anchor" href="#第三步mainactivity-的-oncreate-方法"></a> 第三步：MainActivity 的 onCreate 方法</h4><ul><li><strong>UI 初始化与事件绑定</strong><ul><li>在 <code>onCreate</code> 方法中，首先调用 <code>setContentView</code> 加载布局文件 <code>activity_main</code>。</li><li>通过 <code>findViewById</code> 获取多个按钮：<ul><li><strong>启动服务按钮</strong> (<code>startServiceBtn</code>)：点击后创建 Intent 指定 <code>MyService</code>，调用 <code>startService(intent)</code> 启动服务。</li><li><strong>停止服务按钮</strong> (<code>stopServiceBtn</code>)：点击后创建 Intent 指定 <code>MyService</code>，调用 <code>stopService(intent)</code> 停止服务。</li><li><strong>绑定服务按钮</strong> (<code>bindServiceBtn</code>)：点击后创建 Intent 指定 <code>MyService</code>，调用 <code>bindService(intent, connection, Context.BIND_AUTO_CREATE)</code> 绑定服务，并在绑定成功后自动创建服务。</li><li><strong>解绑服务按钮</strong> (<code>unbindServiceBtn</code>)：点击后调用 <code>unbindService(connection)</code> 解除服务绑定。</li><li><strong>启动 IntentService 按钮</strong> (<code>startIntentServiceBtn</code>)：点击后创建 Intent 指定 <code>MyIntentService</code>，调用 <code>startService(intent)</code> 启动 IntentService。</li></ul></li></ul></li><li><strong>作用</strong><ul><li>该部分代码实现了与用户交互的界面逻辑，不同按钮对应不同的服务操作，从而使用户可以控制服务的生命周期和行为。</li></ul></li></ul><hr /><h4 id="第四步myservice-的详细解析"><a class="markdownIt-Anchor" href="#第四步myservice-的详细解析"></a> 第四步：MyService 的详细解析</h4><ul><li><strong>继承与结构</strong><ul><li><code>MyService</code> 继承自 <code>Service</code>，用于实现后台任务，并且通过 Binder 提供给客户端接口。</li><li>内部定义了 <code>DownloadBinder</code> 类，继承自 <code>Binder</code>，该内部类包含两个方法：<ul><li><code>startDownload()</code>：用于模拟开始下载操作，同时在日志中记录该操作。</li><li><code>getProgress()</code>：用于获取下载进度，当前返回值为 0，并在日志中记录调用过程。</li></ul></li></ul></li><li><strong>onBind 方法</strong><ul><li>当客户端调用 <code>bindService</code> 后，会触发 <code>onBind</code> 方法，返回 <code>mBinder</code> 对象，供客户端与服务交互。</li></ul></li><li><strong>onCreate 方法与前台服务设置</strong><ul><li>在 <code>onCreate</code> 中：<ul><li>先通过日志记录服务的创建过程。</li><li>获取系统的通知管理器，并在 Android 8.0（API 26）及以上版本创建通知渠道，确保通知能正确显示。</li><li>构建一个前台通知（包含标题、内容、图标、点击事件等），并调用 <code>startForeground(1, notification)</code> 将服务置于前台运行，从而降低被系统杀死的风险。</li></ul></li></ul></li><li><strong>onStartCommand 方法</strong><ul><li>记录服务启动的日志，允许服务通过 Intent 启动，并返回父类方法的返回值。</li></ul></li><li><strong>onDestroy 方法</strong><ul><li>当服务销毁时，记录销毁日志，可用于调试和资源清理。</li></ul></li><li><strong>作用</strong><ul><li><code>MyService</code> 模块主要用于模拟一个后台下载服务，通过前台通知确保长时间运行，同时通过 Binder 提供下载控制和进度查询的接口给绑定的客户端。</li></ul></li></ul><hr /><h4 id="第五步myintentservice-的详细解析"><a class="markdownIt-Anchor" href="#第五步myintentservice-的详细解析"></a> 第五步：MyIntentService 的详细解析</h4><ul><li><strong>继承与构造</strong><ul><li><code>MyIntentService</code> 继承自 <code>IntentService</code>，在构造函数中传入服务名称 <code>&quot;MyIntentService&quot;</code>，用于标识服务线程的名称。</li></ul></li><li><strong>onHandleIntent 方法</strong><ul><li>该方法在工作线程中运行，用于处理传递进来的 Intent。这里简单记录了当前线程的名称，表明任务在子线程中执行，不会阻塞主线程。</li></ul></li><li><strong>onDestroy 方法</strong><ul><li>重写了 <code>onDestroy</code> 方法，用于在服务销毁时记录日志，方便调试和资源释放。</li></ul></li><li><strong>作用</strong><ul><li><code>MyIntentService</code> 主要用于处理异步任务。由于它内置了工作线程，因此可以在不阻塞 UI 的前提下执行长时间任务。任务处理完成后，服务会自动停止。</li></ul></li></ul><hr /><h4 id="总结"><a class="markdownIt-Anchor" href="#总结"></a> 总结</h4><ul><li><strong>MainActivity</strong>：作为用户界面控制中心，通过按钮触发对服务的启动、停止、绑定、解绑和异步任务的执行。</li><li><strong>MyService</strong>：实现一个前台服务，主要用于模拟下载任务，通过 Binder 提供接口供 Activity 调用，并使用前台通知来提升服务的优先级。</li><li><strong>MyIntentService</strong>：用于在后台线程中处理异步任务，确保耗时操作不会影响 UI 线程，且任务完成后自动停止服务。</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 使用 lateinit 进行延迟初始化，因为该 Binder 在服务连接时会被赋值。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">lateinit</span> <span class="keyword">var</span> downloadBinder: MyService.DownloadBinder</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * ServiceConnection 实例，处理与服务的连接和断开。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> connection = <span class="keyword">object</span> : ServiceConnection &#123;</span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 当服务连接成功时调用，返回服务的 IBinder 接口。</span></span><br><span class="line"><span class="comment">         * <span class="doctag">@param</span> name ComponentName：服务的组件名称。</span></span><br><span class="line"><span class="comment">         * <span class="doctag">@param</span> service IBinder：服务提供的接口。</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onServiceConnected</span><span class="params">(name: <span class="type">ComponentName</span>, service: <span class="type">IBinder</span>)</span></span> &#123;</span><br><span class="line">            <span class="comment">// 将 IBinder 转换为 MyService.DownloadBinder 类型</span></span><br><span class="line">            downloadBinder = service <span class="keyword">as</span> MyService.DownloadBinder</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 调用服务提供的方法开始下载</span></span><br><span class="line">            downloadBinder.startDownload()</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 调用服务提供的方法获取下载进度</span></span><br><span class="line">            downloadBinder.getProgress()</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 当服务断开连接时调用。</span></span><br><span class="line"><span class="comment">         * <span class="doctag">@param</span> name ComponentName：服务的组件名称。</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onServiceDisconnected</span><span class="params">(name: <span class="type">ComponentName</span>)</span></span> &#123;</span><br><span class="line">            <span class="comment">// 目前没有实现断开连接的逻辑</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Activity 创建时的生命周期回调，用于初始化界面和设置事件监听器。</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> savedInstanceState Bundle：保存的实例状态，可能为 null。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        <span class="comment">// 设置活动的布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取启动服务的按钮</span></span><br><span class="line">        <span class="keyword">val</span> startServiceBtn = findViewById&lt;Button&gt;(R.id.startServiceBtn)</span><br><span class="line">        <span class="comment">// 获取停止服务的按钮</span></span><br><span class="line">        <span class="keyword">val</span> stopServiceBtn = findViewById&lt;Button&gt;(R.id.stopServiceBtn)</span><br><span class="line">        <span class="comment">// 获取绑定服务的按钮</span></span><br><span class="line">        <span class="keyword">val</span> bindServiceBtn = findViewById&lt;Button&gt;(R.id.bindServiceBtn)</span><br><span class="line">        <span class="comment">// 获取解绑服务的按钮</span></span><br><span class="line">        <span class="keyword">val</span> unbindServiceBtn = findViewById&lt;Button&gt;(R.id.unbindServiceBtn)</span><br><span class="line">        <span class="comment">// 获取启动 IntentService 的按钮</span></span><br><span class="line">        <span class="keyword">val</span> startIntentServiceBtn = findViewById&lt;Button&gt;(R.id.startIntentServiceBtn)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置启动服务按钮的点击监听器</span></span><br><span class="line">        startServiceBtn.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 创建启动服务的 Intent</span></span><br><span class="line">            <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, MyService::<span class="keyword">class</span>.java)</span><br><span class="line">            <span class="comment">// 启动服务</span></span><br><span class="line">            startService(intent)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置停止服务按钮的点击监听器</span></span><br><span class="line">        stopServiceBtn.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 创建停止服务的 Intent</span></span><br><span class="line">            <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, MyService::<span class="keyword">class</span>.java)</span><br><span class="line">            <span class="comment">// 停止服务</span></span><br><span class="line">            stopService(intent)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置绑定服务按钮的点击监听器</span></span><br><span class="line">        bindServiceBtn.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 创建绑定服务的 Intent</span></span><br><span class="line">            <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, MyService::<span class="keyword">class</span>.java)</span><br><span class="line">            <span class="comment">// 绑定服务，使用 BIND_AUTO_CREATE 标志自动创建服务</span></span><br><span class="line">            bindService(intent, connection, Context.BIND_AUTO_CREATE)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置解绑服务按钮的点击监听器</span></span><br><span class="line">        unbindServiceBtn.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 解绑服务</span></span><br><span class="line">            unbindService(connection)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置启动 IntentService 按钮的点击监听器</span></span><br><span class="line">        startIntentServiceBtn.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 创建启动 IntentService 的 Intent</span></span><br><span class="line">            <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, MyIntentService::<span class="keyword">class</span>.java)</span><br><span class="line">            <span class="comment">// 启动 IntentService</span></span><br><span class="line">            startService(intent)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyService</span> : <span class="type">Service</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">companion</span> <span class="keyword">object</span> &#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">const</span> <span class="keyword">val</span> TAG = <span class="string">&quot;MyService&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 自定义的 Binder 类，提供服务的接口方法。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> mBinder = DownloadBinder()</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Binder 类，继承自 android.os.Binder，定义服务提供给客户端的接口。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">DownloadBinder</span> : <span class="type">Binder</span>() &#123;</span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 开始下载的方法。</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="function"><span class="keyword">fun</span> <span class="title">startDownload</span><span class="params">()</span></span> &#123;</span><br><span class="line">            Log.d(TAG, <span class="string">&quot;startDownload: 执行了开始下载操作&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 获取下载进度的方法，目前返回 0。</span></span><br><span class="line"><span class="comment">         *</span></span><br><span class="line"><span class="comment">         * <span class="doctag">@return</span> 下载进度，范围为 0 到 100。</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="function"><span class="keyword">fun</span> <span class="title">getProgress</span><span class="params">()</span></span>: <span class="built_in">Int</span> &#123;</span><br><span class="line">            Log.d(TAG, <span class="string">&quot;getProgress: 执行了获取进度操作&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当客户端调用 bind() 时，返回一个 IBinder 接口。</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> intent Intent：用于绑定服务的意图。</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> IBinder：提供给客户端用于与服务通信的接口。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onBind</span><span class="params">(intent: <span class="type">Intent</span>)</span></span>: IBinder &#123;</span><br><span class="line">        <span class="keyword">return</span> mBinder</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 安全忽略 ForegroundServiceType 的警告，因为我们确实需要在前台运行服务。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@SuppressLint(<span class="string">&quot;ForegroundServiceType&quot;</span>)</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate()</span><br><span class="line">        Log.d(TAG, <span class="string">&quot;onCreate: 执行了服务创建&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取通知管理器</span></span><br><span class="line">        <span class="keyword">val</span> manager = getSystemService(NOTIFICATION_SERVICE) <span class="keyword">as</span> NotificationManager</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果是 Android 8.0 及以上版本，创建通知渠道</span></span><br><span class="line">        <span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.O) &#123;</span><br><span class="line">            <span class="keyword">val</span> channel = NotificationChannel(</span><br><span class="line">                <span class="string">&quot;my_service&quot;</span>, <span class="comment">// 通知渠道 ID</span></span><br><span class="line">                <span class="string">&quot;My Service&quot;</span>, <span class="comment">// 通知渠道名称</span></span><br><span class="line">                NotificationManager.IMPORTANCE_DEFAULT <span class="comment">// 通知重要性级别</span></span><br><span class="line">            )</span><br><span class="line">            manager.createNotificationChannel(channel)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建打开 MainActivity 的 PendingIntent</span></span><br><span class="line">        <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, MainActivity::<span class="keyword">class</span>.java)</span><br><span class="line">        <span class="keyword">val</span> pi = PendingIntent.getActivity(</span><br><span class="line">            <span class="keyword">this</span>,</span><br><span class="line">            <span class="number">0</span>,</span><br><span class="line">            intent,</span><br><span class="line">            PendingIntent.FLAG_IMMUTABLE</span><br><span class="line">        )</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 构建前台通知</span></span><br><span class="line">        <span class="keyword">val</span> notification = androidx.core.app.NotificationCompat.Builder(<span class="keyword">this</span>, <span class="string">&quot;my_service&quot;</span>)</span><br><span class="line">            .setContentTitle(<span class="string">&quot;这是内容标题&quot;</span>) <span class="comment">// 通知标题</span></span><br><span class="line">            .setContentText(<span class="string">&quot;这是内容文本&quot;</span>) <span class="comment">// 通知内容</span></span><br><span class="line">            .setWhen(System.currentTimeMillis()) <span class="comment">// 通知时间</span></span><br><span class="line">            .setSmallIcon(R.mipmap.ic_launcher) <span class="comment">// 通知小图标</span></span><br><span class="line">            .setLargeIcon(BitmapFactory.decodeResource(resources, R.mipmap.ic_launcher)) <span class="comment">// 通知大图标</span></span><br><span class="line">            .setContentIntent(pi) <span class="comment">// 通知点击意图</span></span><br><span class="line">            .build()</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 将服务置于前台，并显示通知</span></span><br><span class="line">        startForeground(<span class="number">1</span>, notification)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当服务启动时被调用。</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> intent Intent：启动服务的意图。</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> flags Int：标志。</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> startId Int：启动 ID。</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> Int：表示服务启动的方式。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onStartCommand</span><span class="params">(intent: <span class="type">Intent</span>?, flags: <span class="type">Int</span>, startId: <span class="type">Int</span>)</span></span>: <span class="built_in">Int</span> &#123;</span><br><span class="line">        Log.d(TAG, <span class="string">&quot;onStartCommand: 执行了服务启动&quot;</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">super</span>.onStartCommand(intent, flags, startId)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当服务被销毁时被调用。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onDestroy()</span><br><span class="line">        Log.d(TAG, <span class="string">&quot;onDestroy: 执行了服务销毁&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyIntentService</span> : <span class="type">IntentService</span>(<span class="string">&quot;MyIntentService&quot;</span>) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 用于日志记录的 TAG，标识该服务的日志信息。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> TAG = <span class="string">&quot;MyIntentService&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * IntentService 的回调方法，用于处理传递过来的 Intent。</span></span><br><span class="line"><span class="comment">     * 该方法在单独的 worker 线程中执行，避免阻塞主线程。</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> intent Intent：传递给服务的意图，可能为 null。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onHandleIntent</span><span class="params">(intent: <span class="type">Intent</span>?)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 记录当前线程的 ID，用于确认是否在正确的线程中执行任务</span></span><br><span class="line">        Log.d(TAG, <span class="string">&quot;Thread id is <span class="subst">$&#123;Thread.currentThread().name&#125;</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 服务被销毁时的回调方法，用于执行清理操作。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onDestroy()</span><br><span class="line">        <span class="comment">// 记录服务销毁的日志</span></span><br><span class="line">        Log.d(TAG, <span class="string">&quot;onDestroy executed&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="activty与service进行通信"><a class="markdownIt-Anchor" href="#activty与service进行通信"></a> Activty与Service进行通信</h3><h4 id="1-基本原理"><a class="markdownIt-Anchor" href="#1-基本原理"></a> 1. 基本原理</h4><p>在 Android 开发中，Activity 与 Service 之间的通信主要有两种方式：</p><ul><li><strong>启动服务（Start Service）：</strong><br />通过调用 <code>startService()</code> 启动服务，服务在后台独立运行，适用于执行长时间任务。但这种方式下，Activity 与服务之间无法直接交互。</li><li><strong>绑定服务（Bind Service）：</strong><br />通过调用 <code>bindService()</code>，Activity 与 Service 建立绑定连接，此时双方可以通过 Binder 机制直接通信。Activity 可以调用 Service 内的方法，而 Service 也可以向 Activity 返回数据或状态更新。</li></ul><p>在我们的示例中，主要采用了<strong>绑定服务</strong>的方式实现 Activity 与 Service 的通信。</p><hr /><h4 id="2-binder-机制"><a class="markdownIt-Anchor" href="#2-binder-机制"></a> 2. Binder 机制</h4><p>Binder 是 Android 系统提供的一种高效进程间通信（IPC）机制。在同一进程内使用 Binder 也非常简单，只需要通过继承 <code>Binder</code> 类实现具体的业务接口。</p><h5 id="21-在-service-端实现-binder"><a class="markdownIt-Anchor" href="#21-在-service-端实现-binder"></a> 2.1 在 Service 端实现 Binder</h5><ul><li><strong>自定义 Binder 类：</strong><br />在 <code>MyService</code> 中，我们定义了内部类 <code>DownloadBinder</code>，它继承自 <code>Binder</code>，并提供了两个方法：<ul><li><code>startDownload()</code>：模拟开始下载任务。</li><li><code>getProgress()</code>：获取下载进度（当前示例返回 0）。</li></ul></li><li><strong>返回 Binder 对象：</strong><br />当 Activity 调用 <code>bindService()</code> 时，系统会调用 Service 的 <code>onBind()</code> 方法，此时返回 <code>DownloadBinder</code> 对象，让客户端（Activity）可以通过该对象调用相应方法。</li></ul><h5 id="22-在-activity-端使用-binder"><a class="markdownIt-Anchor" href="#22-在-activity-端使用-binder"></a> 2.2 在 Activity 端使用 Binder</h5><ul><li><strong>创建 ServiceConnection：</strong><br />在 <code>MainActivity</code> 中，通过匿名内部类实现 <code>ServiceConnection</code> 接口：<ul><li>在 <code>onServiceConnected</code> 方法中，将系统返回的 <code>IBinder</code> 强制转换为 <code>MyService.DownloadBinder</code> 类型，并保存到 <code>downloadBinder</code> 变量中。</li><li>之后，就可以通过 <code>downloadBinder</code> 调用 Service 中的方法，如 <code>startDownload()</code> 和 <code>getProgress()</code>。</li></ul></li><li><strong>绑定和解绑服务：</strong><br />通过调用 <code>bindService(intent, connection, Context.BIND_AUTO_CREATE)</code> 建立连接；完成任务或退出 Activity 后，调用 <code>unbindService(connection)</code> 断开绑定，避免内存泄漏。</li></ul><hr /><h4 id="3-详细实现步骤"><a class="markdownIt-Anchor" href="#3-详细实现步骤"></a> 3. 详细实现步骤</h4><h5 id="1在-service-中定义-binder"><a class="markdownIt-Anchor" href="#1在-service-中定义-binder"></a> (1)：在 Service 中定义 Binder</h5><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyService</span> : <span class="type">Service</span>() &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> mBinder = DownloadBinder()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">DownloadBinder</span> : <span class="type">Binder</span>() &#123;</span><br><span class="line">        <span class="function"><span class="keyword">fun</span> <span class="title">startDownload</span><span class="params">()</span></span> &#123;</span><br><span class="line">            Log.d(TAG, <span class="string">&quot;startDownload: 执行了开始下载操作&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">fun</span> <span class="title">getProgress</span><span class="params">()</span></span>: <span class="built_in">Int</span> &#123;</span><br><span class="line">            Log.d(TAG, <span class="string">&quot;getProgress: 执行了获取进度操作&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onBind</span><span class="params">(intent: <span class="type">Intent</span>)</span></span>: IBinder &#123;</span><br><span class="line">        <span class="keyword">return</span> mBinder</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>作用：</strong><br />通过自定义 Binder 类，将 Service 中的方法封装起来供 Activity 调用。</li></ul><h5 id="2在-activity-中实现-serviceconnection"><a class="markdownIt-Anchor" href="#2在-activity-中实现-serviceconnection"></a> (2)：在 Activity 中实现 ServiceConnection</h5><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">val</span> connection = <span class="keyword">object</span> : ServiceConnection &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onServiceConnected</span><span class="params">(name: <span class="type">ComponentName</span>, service: <span class="type">IBinder</span>)</span></span> &#123;</span><br><span class="line">        downloadBinder = service <span class="keyword">as</span> MyService.DownloadBinder</span><br><span class="line">        downloadBinder.startDownload()</span><br><span class="line">        downloadBinder.getProgress()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onServiceDisconnected</span><span class="params">(name: <span class="type">ComponentName</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 断开连接时的处理逻辑</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>作用：</strong><br />当绑定成功时，获取 Binder 对象，并调用 Service 中的方法，实现通信。</li></ul><h5 id="3绑定与解绑服务"><a class="markdownIt-Anchor" href="#3绑定与解绑服务"></a> (3)：绑定与解绑服务</h5><ul><li><p><strong>绑定服务：</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">bindServiceBtn.setOnClickListener &#123;</span><br><span class="line">    <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, MyService::<span class="keyword">class</span>.java)</span><br><span class="line">    bindService(intent, connection, Context.BIND_AUTO_CREATE)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>解绑服务：</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">unbindServiceBtn.setOnClickListener &#123;</span><br><span class="line">    unbindService(connection)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>作用：</strong><br />通过点击不同按钮，实现与 Service 的建立连接和断开连接，保证在需要时可以调用 Service 中的功能，同时避免资源泄漏。</p></li></ul><h3 id="前台service"><a class="markdownIt-Anchor" href="#前台service"></a> 前台Service</h3><p>前台 Service（Foreground Service）是 Android 提供的一种特殊 Service，它在运行时会在<strong>通知栏显示一个持久化通知</strong>，以提高应用的存活能力，并避免被系统回收。适用于：</p><ul><li>长时间运行的任务，如音乐播放、文件下载、GPS 追踪等。</li><li>需要用户感知的任务，避免因后台优化机制被杀死。</li></ul><h4 id="1-创建前台-service-的步骤"><a class="markdownIt-Anchor" href="#1-创建前台-service-的步骤"></a> <strong>1. 创建前台 Service 的步骤</strong></h4><h5 id="创建-service-并继承-service"><a class="markdownIt-Anchor" href="#创建-service-并继承-service"></a> <strong>创建 Service 并继承 <code>Service</code></strong></h5><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyService</span> : <span class="type">Service</span>() &#123;</span><br><span class="line">    <span class="keyword">companion</span> <span class="keyword">object</span> &#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">const</span> <span class="keyword">val</span> TAG = <span class="string">&quot;MyService&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> mBinder = DownloadBinder()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">DownloadBinder</span> : <span class="type">Binder</span>() &#123;</span><br><span class="line">        <span class="function"><span class="keyword">fun</span> <span class="title">startDownload</span><span class="params">()</span></span> &#123;</span><br><span class="line">            Log.d(TAG, <span class="string">&quot;startDownload: 执行了开始下载操作&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">fun</span> <span class="title">getProgress</span><span class="params">()</span></span>: <span class="built_in">Int</span> &#123;</span><br><span class="line">            Log.d(TAG, <span class="string">&quot;getProgress: 执行了获取进度操作&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onBind</span><span class="params">(intent: <span class="type">Intent</span>)</span></span>: IBinder &#123;</span><br><span class="line">        <span class="keyword">return</span> mBinder</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>作用：</strong><br />该 Service 继承 <code>Service</code>，并定义了 <code>DownloadBinder</code>，用于提供服务端的方法。</li></ul><hr /><h5 id="在-oncreate-中启动前台-service"><a class="markdownIt-Anchor" href="#在-oncreate-中启动前台-service"></a> <strong>在 <code>onCreate()</code> 中启动前台 Service</strong></h5><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@SuppressLint(<span class="string">&quot;ForegroundServiceType&quot;</span>)</span></span><br><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">super</span>.onCreate()</span><br><span class="line">    Log.d(TAG, <span class="string">&quot;onCreate: 执行了服务创建&quot;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">val</span> manager = getSystemService(NOTIFICATION_SERVICE) <span class="keyword">as</span> NotificationManager</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.O) &#123;</span><br><span class="line">        <span class="keyword">val</span> channel = NotificationChannel(</span><br><span class="line">            <span class="string">&quot;my_service&quot;</span>,</span><br><span class="line">            <span class="string">&quot;My Service&quot;</span>,</span><br><span class="line">            NotificationManager.IMPORTANCE_DEFAULT</span><br><span class="line">        )</span><br><span class="line">        manager.createNotificationChannel(channel)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, MainActivity::<span class="keyword">class</span>.java)</span><br><span class="line">    <span class="keyword">val</span> pi = PendingIntent.getActivity(<span class="keyword">this</span>, <span class="number">0</span>, intent, PendingIntent.FLAG_IMMUTABLE)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">val</span> notification = NotificationCompat.Builder(<span class="keyword">this</span>, <span class="string">&quot;my_service&quot;</span>)</span><br><span class="line">        .setContentTitle(<span class="string">&quot;前台服务运行中&quot;</span>)</span><br><span class="line">        .setContentText(<span class="string">&quot;点击打开应用&quot;</span>)</span><br><span class="line">        .setWhen(System.currentTimeMillis())</span><br><span class="line">        .setSmallIcon(R.mipmap.ic_launcher)</span><br><span class="line">        .setLargeIcon(BitmapFactory.decodeResource(resources, R.mipmap.ic_launcher))</span><br><span class="line">        .setContentIntent(pi)</span><br><span class="line">        .build()</span><br><span class="line"></span><br><span class="line">    startForeground(<span class="number">1</span>, notification) <span class="comment">// 启动前台服务</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>作用：</strong><ul><li>创建通知渠道（Android 8.0 及以上必须创建）。</li><li>使用 <code>startForeground(1, notification)</code> 将服务提升为前台服务。</li><li>让服务在后台运行时不会被系统杀死。</li></ul></li></ul><hr /><h5 id="在-androidmanifestxml-声明权限"><a class="markdownIt-Anchor" href="#在-androidmanifestxml-声明权限"></a> <strong>在 <code>AndroidManifest.xml</code> 声明权限</strong></h5><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.FOREGROUND_SERVICE&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure><ul><li><strong>作用：</strong><br />申请运行前台服务的权限，否则 Android 9.0 及以上版本无法正常运行。</li></ul><hr /><h4 id="2-启动与停止前台-service"><a class="markdownIt-Anchor" href="#2-启动与停止前台-service"></a> <strong>2. 启动与停止前台 Service</strong></h4><h5 id="启动前台-service"><a class="markdownIt-Anchor" href="#启动前台-service"></a> <strong>启动前台 Service</strong></h5><p>在 <code>MainActivity</code> 中，添加启动前台 Service 的代码：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> startServiceBtn = findViewById&lt;Button&gt;(R.id.startServiceBtn)</span><br><span class="line">startServiceBtn.setOnClickListener &#123;</span><br><span class="line">    <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, MyService::<span class="keyword">class</span>.java)</span><br><span class="line">    startService(intent) <span class="comment">// 启动 Service</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>作用：</strong><br />通过 <code>startService()</code> 启动 <code>MyService</code>，并触发 <code>onCreate()</code> 方法，使其进入前台模式。</li></ul><h5 id="停止前台-service"><a class="markdownIt-Anchor" href="#停止前台-service"></a> <strong>停止前台 Service</strong></h5><p>在 <code>MyService</code> 中，添加 <code>onDestroy()</code> 方法：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">super</span>.onDestroy()</span><br><span class="line">    Log.d(TAG, <span class="string">&quot;onDestroy: 执行了服务销毁&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 <code>MainActivity</code> 里：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> stopServiceBtn = findViewById&lt;Button&gt;(R.id.stopServiceBtn)</span><br><span class="line">stopServiceBtn.setOnClickListener &#123;</span><br><span class="line">    <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, MyService::<span class="keyword">class</span>.java)</span><br><span class="line">    stopService(intent) <span class="comment">// 停止 Service</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>作用：</strong><br />通过 <code>stopService()</code> 停止前台 Service，并销毁通知。</li></ul><hr /><h4 id="3-停止前台-service-但不销毁"><a class="markdownIt-Anchor" href="#3-停止前台-service-但不销毁"></a> <strong>3. 停止前台 Service 但不销毁</strong></h4><p>有时，我们希望前台 Service 转为后台 Service，而不是完全销毁。可以调用：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">stopForeground(<span class="literal">true</span>)  <span class="comment">// true 表示移除通知</span></span><br></pre></td></tr></table></figure><ul><li><strong>作用：</strong><br />让 Service 退出前台模式，但仍然保持后台运行。</li></ul><p>示例：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">stopForegroundService</span><span class="params">()</span></span> &#123;</span><br><span class="line">    stopForeground(<span class="literal">true</span>) <span class="comment">// 移除通知</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h4 id="4-适配-android-11前台服务类型"><a class="markdownIt-Anchor" href="#4-适配-android-11前台服务类型"></a> <strong>4. 适配 Android 11+（前台服务类型）</strong></h4><p>在 <code>AndroidManifest.xml</code> 中，Android 11+ 需要指定 Service 的类型：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">service</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:name</span>=<span class="string">&quot;.MyService&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:foregroundServiceType</span>=<span class="string">&quot;mediaPlayback&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure><ul><li><code>mediaPlayback</code>：适用于音乐播放。</li><li><code>location</code>：适用于 GPS 追踪。</li><li><code>dataSync</code>：适用于数据同步任务。</li></ul><h4 id="jobintentservice-及其用法"><a class="markdownIt-Anchor" href="#jobintentservice-及其用法"></a> <strong>JobIntentService 及其用法</strong></h4><p><code>JobIntentService</code> 是 Android 提供的一个特殊 <code>Service</code>，用于替代 <code>IntentService</code>，<strong>主要用于适配 Android 8.0+（API 26+）的后台限制</strong>。</p><p>从 Android 8.0 开始，普通后台 <code>Service</code> 可能会被系统<strong>强制终止</strong>，而 <code>JobIntentService</code> 结合了 <code>JobScheduler</code>，可在合适的时间<strong>自动执行后台任务</strong>，确保任务不会因后台限制而被杀死。</p><h4 id="jobintentservice-的特点"><a class="markdownIt-Anchor" href="#jobintentservice-的特点"></a> <strong>JobIntentService 的特点</strong></h4><p>✅ <strong>自动创建子线程</strong>：不需要手动创建 <code>Thread</code> 或 <code>AsyncTask</code>，不会阻塞主线程。<br />✅ <strong>按队列依次执行任务</strong>：如果多次调用 <code>enqueueWork()</code>，任务会排队顺序执行。<br />✅ <strong>自动停止</strong>：任务执行完毕后，<code>JobIntentService</code> 会<strong>自动停止</strong>，无需手动 <code>stopSelf()</code>。<br />✅ <strong>兼容 Android 5.0 及以上</strong>：即使在 Android 8.0 之后，也能正常运行。<br />✅ <strong>适合短时任务</strong>：适用于数据同步、文件上传等<strong>短时间后台任务</strong>。</p><hr /><h4 id="自定义-jobintentservice"><a class="markdownIt-Anchor" href="#自定义-jobintentservice"></a> <strong>自定义 JobIntentService</strong></h4><p>创建一个 <code>MyJobIntentService</code> 继承 <code>JobIntentService</code>，并实现 <code>onHandleWork()</code> 方法：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyJobIntentService</span> : <span class="type">JobIntentService</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">companion</span> <span class="keyword">object</span> &#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">const</span> <span class="keyword">val</span> JOB_ID = <span class="number">1000</span>  <span class="comment">// 定义唯一 Job ID</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 用于启动 JobIntentService 的方法，确保在 Android 8.0+ 也能正常运行</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="function"><span class="keyword">fun</span> <span class="title">enqueueWork</span><span class="params">(context: <span class="type">Context</span>, work: <span class="type">Intent</span>)</span></span> &#123;</span><br><span class="line">            enqueueWork(context, MyJobIntentService::<span class="keyword">class</span>.java, JOB_ID, work)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理后台任务的方法，会自动在子线程执行</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onHandleWork</span><span class="params">(intent: <span class="type">Intent</span>)</span></span> &#123;</span><br><span class="line">        <span class="keyword">val</span> task = intent.getStringExtra(<span class="string">&quot;task_data&quot;</span>) ?: <span class="string">&quot;默认任务&quot;</span></span><br><span class="line"></span><br><span class="line">        Log.d(<span class="string">&quot;MyJobIntentService&quot;</span>, <span class="string">&quot;任务开始: <span class="variable">$task</span>, 线程名: <span class="subst">$&#123;Thread.currentThread().name&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 模拟耗时操作（如下载文件、上传数据）</span></span><br><span class="line">        <span class="keyword">for</span> (i <span class="keyword">in</span> <span class="number">1.</span><span class="number">.5</span>) &#123;</span><br><span class="line">            Log.d(<span class="string">&quot;MyJobIntentService&quot;</span>, <span class="string">&quot;执行任务 <span class="variable">$task</span> - 第 <span class="variable">$i</span> 步&quot;</span>)</span><br><span class="line">            Thread.sleep(<span class="number">1000</span>) <span class="comment">// 模拟任务执行时间</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        Log.d(<span class="string">&quot;MyJobIntentService&quot;</span>, <span class="string">&quot;任务完成: <span class="variable">$task</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当任务执行完毕且没有更多任务时，JobIntentService 会自动停止</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onDestroy()</span><br><span class="line">        Log.d(<span class="string">&quot;MyJobIntentService&quot;</span>, <span class="string">&quot;服务已销毁&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>🔹 <strong>代码解析</strong>：</p><ul><li><strong><code>onHandleWork(intent: Intent)</code></strong>：<ul><li>这个方法在<strong>子线程</strong>执行，避免阻塞主线程。</li><li>通过 <code>intent.getStringExtra(&quot;task_data&quot;)</code> 获取任务参数。</li><li>使用 <code>Thread.sleep(1000)</code> 模拟任务执行，表示任务进度。</li></ul></li><li><strong><code>enqueueWork(context, work)</code></strong>：<ul><li>这个方法用于启动 <code>JobIntentService</code>，<strong>代替</strong> <code>startService()</code>，<strong>确保兼容 Android 8.0+</strong>。</li><li>任务会按照队列顺序执行，不会并发。</li></ul></li></ul><hr /><h4 id="在-androidmanifestxml-注册-service"><a class="markdownIt-Anchor" href="#在-androidmanifestxml-注册-service"></a> <strong>在 AndroidManifest.xml 注册 Service</strong></h4><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">service</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:name</span>=<span class="string">&quot;.MyJobIntentService&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:permission</span>=<span class="string">&quot;android.permission.BIND_JOB_SERVICE&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:exported</span>=<span class="string">&quot;false&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><p>🔹 <strong>注意</strong>：</p><ul><li><code>android:permission=&quot;android.permission.BIND_JOB_SERVICE&quot;</code> 允许 <code>JobScheduler</code> 绑定 Service。</li><li><code>android:exported=&quot;false&quot;</code> 让 Service 只能被<strong>应用内</strong>调用，保证安全性。</li></ul><hr /><h4 id="启动-jobintentservice"><a class="markdownIt-Anchor" href="#启动-jobintentservice"></a> <strong>启动 JobIntentService</strong></h4><p>在 <code>MainActivity</code> 中，创建按钮来启动 <code>MyJobIntentService</code>：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> startJobIntentServiceBtn = findViewById&lt;Button&gt;(R.id.startJobIntentServiceBtn)</span><br><span class="line">startJobIntentServiceBtn.setOnClickListener &#123;</span><br><span class="line">    <span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, MyJobIntentService::<span class="keyword">class</span>.java)</span><br><span class="line">    intent.putExtra(<span class="string">&quot;task_data&quot;</span>, <span class="string">&quot;下载任务&quot;</span>) <span class="comment">// 传递任务信息</span></span><br><span class="line">    MyJobIntentService.enqueueWork(<span class="keyword">this</span>, intent) <span class="comment">// 启动 JobIntentService</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>🔹 <strong>这里不使用 <code>startService()</code>，而是用 <code>enqueueWork()</code></strong>，确保兼容 Android 8.0+。</p><hr /><h4 id="jobintentservice-处理多个任务"><a class="markdownIt-Anchor" href="#jobintentservice-处理多个任务"></a> <strong>JobIntentService 处理多个任务</strong></h4><p>与 <code>IntentService</code> 类似，<strong><code>JobIntentService</code> 不会并行执行任务，而是按顺序排队执行</strong>：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">MyJobIntentService.enqueueWork(<span class="keyword">this</span>, Intent(<span class="keyword">this</span>, MyJobIntentService::<span class="keyword">class</span>.java).putExtra(<span class="string">&quot;task_data&quot;</span>, <span class="string">&quot;任务 1&quot;</span>))</span><br><span class="line">MyJobIntentService.enqueueWork(<span class="keyword">this</span>, Intent(<span class="keyword">this</span>, MyJobIntentService::<span class="keyword">class</span>.java).putExtra(<span class="string">&quot;task_data&quot;</span>, <span class="string">&quot;任务 2&quot;</span>))</span><br><span class="line">MyJobIntentService.enqueueWork(<span class="keyword">this</span>, Intent(<span class="keyword">this</span>, MyJobIntentService::<span class="keyword">class</span>.java).putExtra(<span class="string">&quot;task_data&quot;</span>, <span class="string">&quot;任务 3&quot;</span>))</span><br></pre></td></tr></table></figure><p><strong>执行顺序</strong>：</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">任务 <span class="number">1</span> 开始执行</span><br><span class="line">任务 <span class="number">1</span> 结束</span><br><span class="line">任务 <span class="number">2</span> 开始执行</span><br><span class="line">任务 <span class="number">2</span> 结束</span><br><span class="line">任务 <span class="number">3</span> 开始执行</span><br><span class="line">任务 <span class="number">3</span> 结束</span><br></pre></td></tr></table></figure><ul><li><strong>多个任务不会并发执行</strong>，而是<strong>按顺序执行</strong>。</li><li><strong>任务执行完毕后，Service 会自动停止</strong>。</li></ul><hr /><h4 id="jobintentservice-与-intentservice-对比"><a class="markdownIt-Anchor" href="#jobintentservice-与-intentservice-对比"></a> <strong>JobIntentService 与 IntentService 对比</strong></h4><table><thead><tr><th><strong>特性</strong></th><th><strong>JobIntentService</strong></th><th><strong>IntentService</strong></th></tr></thead><tbody><tr><td><strong>运行线程</strong></td><td>自动创建子线程</td><td>自动创建子线程</td></tr><tr><td><strong>任务执行方式</strong></td><td><strong>按队列顺序执行</strong></td><td><strong>按队列顺序执行</strong></td></tr><tr><td><strong>任务执行完是否自动停止</strong></td><td>✅ 是</td><td>✅ 是</td></tr><tr><td><strong>是否兼容 Android 8.0+</strong></td><td>✅ 是</td><td>❌ 可能受限制</td></tr><tr><td><strong>适用于长时间任务</strong></td><td>❌ 不适合</td><td>❌ 不适合</td></tr><tr><td><strong>适用于短时任务</strong></td><td>✅ 适合</td><td>✅ 适合</td></tr></tbody></table><p>🔹 <strong>总结</strong>：</p><ul><li><strong><code>JobIntentService</code> 适用于 Android 8.0+，替代 <code>IntentService</code></strong>。</li><li><strong>如果需要后台执行短时间任务（如上传数据、数据库操作），建议使用 <code>JobIntentService</code></strong>。</li></ul><hr /><h4 id="适用场景"><a class="markdownIt-Anchor" href="#适用场景"></a> <strong>适用场景</strong></h4><table><thead><tr><th><strong>应用场景</strong></th><th><strong>建议的 Service 方式</strong></th></tr></thead><tbody><tr><td>后台持续运行的任务（如音乐播放）</td><td><strong>前台 Service</strong></td></tr><tr><td>短时间后台任务（如数据同步）</td><td><strong>JobIntentService</strong></td></tr><tr><td>需要兼容 Android 8.0+ 的后台任务</td><td><strong>JobIntentService</strong></td></tr><tr><td>需要并行执行多个任务</td><td><strong>普通 Service + 线程池</strong></td></tr></tbody></table><hr /><h2 id="8使用网络技术"><a class="markdownIt-Anchor" href="#8使用网络技术"></a> 8.使用网络技术</h2><h3 id="webview用法网页浏览"><a class="markdownIt-Anchor" href="#webview用法网页浏览"></a> WebView用法(网页浏览)</h3><ol><li>添加<code>WebView</code>控件</li><li>调佣<code>javaScriptEnabled</code>与<code>loadUrl</code>方法</li><li>申请权限</li></ol><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span> &gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">WebView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/webview&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="comment">// 当Activity创建时被调用</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 调用父类的onCreate方法</span></span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 设置当前Activity的布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取当前Activity中id为R.id.webview的WebView控件</span></span><br><span class="line">        <span class="keyword">val</span> webView = findViewById&lt;WebView&gt;(R.id.webview)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 启用JavaScript</span></span><br><span class="line">        webView.settings.javaScriptEnabled = <span class="literal">true</span></span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 设置WebView的客户端</span></span><br><span class="line">        webView.webViewClient = WebViewClient()</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 加载网页</span></span><br><span class="line">        webView.loadUrl(<span class="string">&quot;https://www.baidu.com&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.INTERNET&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><h3 id="使用http访问网络okhttp传输数据和调用api"><a class="markdownIt-Anchor" href="#使用http访问网络okhttp传输数据和调用api"></a> 使用HTTP访问网络(OkHttp:传输数据和调用API)</h3><ol><li>导入依赖库</li><li>创建<code>OkHttp</code>实例,获取<code>Request</code>对象与<code>Call</code>对象,并调用<code>execute()</code>方法获取实例</li></ol><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">implementation(&quot;com.squareup.okhttp3:okhttp:4.9.3&quot;)</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line">    <span class="comment">// 当Activity创建时被调用</span></span><br><span class="line">    <span class="meta">@SuppressLint(<span class="string">&quot;MissingInflatedId&quot;</span>)</span> <span class="comment">// 这个注解是为了忽略Android Studio的警告，表示id为R.id.sendRequestBtn的Button控件可能没有被初始化</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 调用父类的onCreate方法</span></span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 设置当前Activity的布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取当前Activity中id为R.id.sendRequestBtn的Button控件</span></span><br><span class="line">        <span class="keyword">val</span> sendRequestBtn = findViewById&lt;Button&gt;(R.id.sendRequestBtn)</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 为Button控件设置点击事件</span></span><br><span class="line">        sendRequestBtn.setOnClickListener &#123;</span><br><span class="line">            <span class="comment">// 当Button控件被点击时，调用sendRequestWithHttpURLConnection函数</span></span><br><span class="line">            sendRequestWithHttpURLConnection()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">sendRequestWithHttpURLConnection</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="comment">// 在新线程中执行网络请求</span></span><br><span class="line">        Thread &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 创建OkHttpClient对象</span></span><br><span class="line">                <span class="keyword">val</span> client = OkHttpClient()</span><br><span class="line">                </span><br><span class="line">                <span class="comment">// 构建请求</span></span><br><span class="line">                <span class="keyword">val</span> request = okhttp3.Request.Builder()</span><br><span class="line">                    <span class="comment">// 设置请求的URL</span></span><br><span class="line">                    .url(<span class="string">&quot;https://www.baidu.com&quot;</span>)</span><br><span class="line">                    .build()</span><br><span class="line">                </span><br><span class="line">                <span class="comment">// 执行请求</span></span><br><span class="line">                <span class="keyword">val</span> response = client.newCall(request).execute()</span><br><span class="line">                </span><br><span class="line">                <span class="comment">// 获取响应的数据</span></span><br><span class="line">                <span class="keyword">val</span> responseData = response.body?.string()</span><br><span class="line">                </span><br><span class="line">                <span class="comment">// 如果响应的数据不为空，则显示响应的数据</span></span><br><span class="line">                <span class="keyword">if</span> (responseData != <span class="literal">null</span>) &#123;</span><br><span class="line">                    showResponse(responseData)</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (e: Exception) &#123;</span><br><span class="line">                <span class="comment">// 如果发生异常，则打印异常信息</span></span><br><span class="line">                e.printStackTrace()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;.start()</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 显示响应的数据</span></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">showResponse</span><span class="params">(response: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 在主线程中更新UI</span></span><br><span class="line">        runOnUiThread &#123;</span><br><span class="line">            <span class="comment">// 获取当前Activity中id为R.id.responseText的TextView控件</span></span><br><span class="line">            <span class="keyword">val</span> responseText = findViewById&lt;TextView&gt;(R.id.responseText)</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 更新TextView控件的文本</span></span><br><span class="line">            responseText.text = response</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="解析xml格式数据"><a class="markdownIt-Anchor" href="#解析xml格式数据"></a> 解析XML格式数据</h3><h4 id="1pull解析方式"><a class="markdownIt-Anchor" href="#1pull解析方式"></a> 1.Pull解析方式</h4><p>• <strong>创建<code>XmlPullParser</code>对象</strong><br />需要先创建一个<code>XmlPullParser</code>对象，这个对象负责解析XML数据。可以通过<code>Xml.newPullParser()</code>来创建。</p><p>• <strong>设置输入源</strong><br />可以设置文件、字符串、或<code>InputStream</code>作为输入源，通常通过<code>setInput</code>方法来绑定数据源。</p><p>• <strong>开始解析</strong><br />通过<code>next()</code>方法进行解析，返回的值表示当前解析到的XML元素的类型，例如：<code>START_TAG</code>、<code>END_TAG</code>、<code>TEXT</code>等。</p><p>• <strong>处理解析事件</strong><br />根据<code>next()</code>返回的事件类型进行不同的处理。例如，遇到<code>START_TAG</code>时，可以获取标签的名称和属性；遇到<code>TEXT</code>时，处理标签中的内容。</p><p>• <strong>结束解析</strong><br />解析完成后，需要确保关闭<code>XmlPullParser</code>对象，释放资源。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">book</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Java Programming<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">author</span>&gt;</span>John Doe<span class="tag">&lt;/<span class="name">author</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">book</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">XmlPullParserExample</span> &#123;</span><br><span class="line">    <span class="comment">// 主函数</span></span><br><span class="line">    <span class="keyword">companion</span> <span class="keyword">object</span> &#123;</span><br><span class="line">        <span class="meta">@JvmStatic</span></span><br><span class="line">        <span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">(args: <span class="type">Array</span>&lt;<span class="type">String</span>&gt;)</span></span> &#123;</span><br><span class="line">            <span class="comment">// 定义XML数据</span></span><br><span class="line">            <span class="keyword">val</span> xmlData = <span class="string">&quot;&lt;book&gt;&lt;title&gt;Java Programming&lt;/title&gt;&lt;author&gt;John Doe&lt;/author&gt;&lt;/book&gt;&quot;</span></span><br><span class="line"></span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 创建XmlPullParserFactory实例</span></span><br><span class="line">                <span class="keyword">val</span> factory = XmlPullParserFactory.newInstance()</span><br><span class="line">                <span class="comment">// 创建XmlPullParser实例</span></span><br><span class="line">                <span class="keyword">val</span> parser = factory.newPullParser()</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 设置输入源</span></span><br><span class="line">                parser.setInput(StringReader(xmlData))</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 开始解析</span></span><br><span class="line">                <span class="keyword">var</span> eventType = parser.eventType</span><br><span class="line">                <span class="keyword">while</span> (eventType != XmlPullParser.END_DOCUMENT) &#123;</span><br><span class="line">                    <span class="comment">// 获取当前标签的名称</span></span><br><span class="line">                    <span class="keyword">val</span> tagName = parser.name</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 根据事件类型进行处理</span></span><br><span class="line">                    <span class="keyword">when</span> (eventType) &#123;</span><br><span class="line">                        XmlPullParser.START_TAG -&gt; &#123;</span><br><span class="line">                            <span class="comment">// 如果当前标签是title，则读取title标签中的文本</span></span><br><span class="line">                            <span class="keyword">if</span> (<span class="string">&quot;title&quot;</span>.equals(tagName, ignoreCase = <span class="literal">true</span>)) &#123;</span><br><span class="line">                                <span class="comment">// 读取title标签中的文本</span></span><br><span class="line">                                System.<span class="keyword">out</span>.println(<span class="string">&quot;Title: &quot;</span> + parser.nextText())</span><br><span class="line">                            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;author&quot;</span>.equals(tagName, ignoreCase = <span class="literal">true</span>)) &#123;</span><br><span class="line">                                <span class="comment">// 如果当前标签是author，则读取author标签中的文本</span></span><br><span class="line">                                System.<span class="keyword">out</span>.println(<span class="string">&quot;Author: &quot;</span> + parser.nextText())</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                        XmlPullParser.END_TAG -&gt; &#123;</span><br><span class="line">                            <span class="comment">// 如果当前事件是结束标签，则不进行处理</span></span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 解析下一个事件</span></span><br><span class="line">                    eventType = parser.next()</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (e: Exception) &#123;</span><br><span class="line">                <span class="comment">// 如果发生异常，则打印异常信息</span></span><br><span class="line">                e.printStackTrace()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="2sax解析方式"><a class="markdownIt-Anchor" href="#2sax解析方式"></a> 2.SAX解析方式</h4><ol><li><strong>初始化解析器</strong><br />创建SAX解析器，并设置一个<code>ContentHandler</code>接口的实现类来处理不同的解析事件。</li><li><strong>解析XML文档</strong><br />使用<code>parse()</code>方法启动解析过程，解析器会按照文档顺序处理XML内容。</li><li><strong>处理事件</strong><br />在解析过程中，SAX解析器会触发相应的事件（如开始标签、结束标签、文本节点等），应用程序可以通过实现<code>ContentHandler</code>接口的不同方法来响应这些事件。</li><li><strong>结束解析</strong><br />解析完成后，解析器会关闭并释放资源。</li></ol><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">book</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Java Programming<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">author</span>&gt;</span>John Doe<span class="tag">&lt;/<span class="name">author</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">book</span>&gt;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// SaxExample类，用于演示SAX解析的使用</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">SaxExample</span> &#123;</span><br><span class="line">    <span class="comment">// 主函数</span></span><br><span class="line">    <span class="keyword">companion</span> <span class="keyword">object</span> &#123;</span><br><span class="line">        <span class="meta">@JvmStatic</span></span><br><span class="line">        <span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">(args: <span class="type">Array</span>&lt;<span class="type">String</span>&gt;)</span></span> &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 创建SAX解析器工厂</span></span><br><span class="line">                <span class="keyword">val</span> factory = SAXParserFactory.newInstance()</span><br><span class="line">                <span class="comment">// 创建SAX解析器</span></span><br><span class="line">                <span class="keyword">val</span> parser = factory.newSAXParser()</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 创建自定义的ContentHandler</span></span><br><span class="line">                <span class="keyword">val</span> handler = <span class="keyword">object</span> : DefaultHandler() &#123;</span><br><span class="line">                    <span class="comment">// 开始标签时触发</span></span><br><span class="line">                    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">startElement</span><span class="params">(uri: <span class="type">String</span>?, localName: <span class="type">String</span>?, qName: <span class="type">String</span>?, attributes: <span class="type">Attributes</span>?)</span></span> &#123;</span><br><span class="line">                        <span class="comment">// 输出开始标签的名称</span></span><br><span class="line">                        System.<span class="keyword">out</span>.println(<span class="string">&quot;Start Element: <span class="variable">$qName</span>&quot;</span>)</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 结束标签时触发</span></span><br><span class="line">                    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">endElement</span><span class="params">(uri: <span class="type">String</span>?, localName: <span class="type">String</span>?, qName: <span class="type">String</span>?)</span></span> &#123;</span><br><span class="line">                        <span class="comment">// 输出结束标签的名称</span></span><br><span class="line">                        System.<span class="keyword">out</span>.println(<span class="string">&quot;End Element: <span class="variable">$qName</span>&quot;</span>)</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 处理文本节点时触发</span></span><br><span class="line">                    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">characters</span><span class="params">(ch: <span class="type">CharArray</span>?, start: <span class="type">Int</span>, length: <span class="type">Int</span>)</span></span> &#123;</span><br><span class="line">                        <span class="comment">// 输出文本内容</span></span><br><span class="line">                        System.<span class="keyword">out</span>.println(<span class="string">&quot;Text: <span class="subst">$&#123;String(ch, start, length)&#125;</span>&quot;</span>)</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 解析XML文档</span></span><br><span class="line">                <span class="keyword">val</span> xmlData = <span class="string">&quot;&lt;book&gt;&lt;title&gt;Java Programming&lt;/title&gt;&lt;author&gt;John Doe&lt;/author&gt;&lt;/book&gt;&quot;</span></span><br><span class="line">                parser.parse(InputSource(StringReader(xmlData)), handler)</span><br><span class="line"></span><br><span class="line">            &#125; <span class="keyword">catch</span> (e: Exception) &#123;</span><br><span class="line">                <span class="comment">// 如果发生异常，则打印异常信息</span></span><br><span class="line">                e.printStackTrace()</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="解析json格式数据"><a class="markdownIt-Anchor" href="#解析json格式数据"></a> 解析JSON格式数据</h3><h4 id="1使用jsonobject"><a class="markdownIt-Anchor" href="#1使用jsonobject"></a> 1.使用JSONObject</h4><p>• <strong>创建 <code>JSONObject</code> 对象</strong><br />通过 <code>new JSONObject(jsonString)</code> 将 JSON 字符串转换为 <code>JSONObject</code> 对象。</p><p>• <strong>获取 <code>book</code> 对象</strong><br />使用 <code>jsonObject.getJSONObject(&quot;book&quot;)</code> 获取嵌套的 <code>book</code> 对象。</p><p>• <strong>获取字段值</strong><br />使用 <code>getString()</code>、<code>getDouble()</code> 和 <code>getBoolean()</code> 等方法从 <code>book</code> 对象中提取书籍的标题、作者、价格和可用性。</p><p>• <strong>处理 <code>genres</code> 数组</strong><br />使用 <code>getJSONArray(&quot;genres&quot;)</code> 获取嵌套的数组，并遍历数组中的元素。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="comment">// 示例 JSON 字符串</span></span><br><span class="line">    <span class="keyword">val</span> jsonString = <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">        &#123;</span></span><br><span class="line"><span class="string">          &quot;book&quot;: &#123;</span></span><br><span class="line"><span class="string">            &quot;title&quot;: &quot;Java Programming&quot;,</span></span><br><span class="line"><span class="string">            &quot;author&quot;: &quot;John Doe&quot;,</span></span><br><span class="line"><span class="string">            &quot;price&quot;: 29.99,</span></span><br><span class="line"><span class="string">            &quot;available&quot;: true</span></span><br><span class="line"><span class="string">          &#125;,</span></span><br><span class="line"><span class="string">          &quot;genres&quot;: [&quot;Programming&quot;, &quot;Technology&quot;]</span></span><br><span class="line"><span class="string">        &#125;</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 创建 JSONObject 对象</span></span><br><span class="line">        <span class="keyword">val</span> jsonObject = JSONObject(jsonString)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取嵌套的 &quot;book&quot; 对象</span></span><br><span class="line">        <span class="keyword">val</span> bookObject = jsonObject.getJSONObject(<span class="string">&quot;book&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取书籍的各个字段</span></span><br><span class="line">        <span class="keyword">val</span> title = bookObject.getString(<span class="string">&quot;title&quot;</span>)</span><br><span class="line">        <span class="keyword">val</span> author = bookObject.getString(<span class="string">&quot;author&quot;</span>)</span><br><span class="line">        <span class="keyword">val</span> price = bookObject.getDouble(<span class="string">&quot;price&quot;</span>)</span><br><span class="line">        <span class="keyword">val</span> available = bookObject.getBoolean(<span class="string">&quot;available&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取 &quot;genres&quot; 数组</span></span><br><span class="line">        <span class="keyword">val</span> genresArray = jsonObject.getJSONArray(<span class="string">&quot;genres&quot;</span>)</span><br><span class="line">        <span class="keyword">for</span> (i <span class="keyword">in</span> <span class="number">0</span> until genresArray.length()) &#123;</span><br><span class="line">            println(<span class="string">&quot;Genre: <span class="subst">$&#123;genresArray.getString(i)&#125;</span>&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 输出解析的数据</span></span><br><span class="line">        println(<span class="string">&quot;Title: <span class="variable">$title</span>&quot;</span>)</span><br><span class="line">        println(<span class="string">&quot;Author: <span class="variable">$author</span>&quot;</span>)</span><br><span class="line">        println(<span class="string">&quot;Price: <span class="variable">$price</span>&quot;</span>)</span><br><span class="line">        println(<span class="string">&quot;Available: <span class="variable">$available</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">catch</span> (e: JSONException) &#123;</span><br><span class="line">        e.printStackTrace()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="2使用gson"><a class="markdownIt-Anchor" href="#2使用gson"></a> 2.使用GSON</h4><p>• 在项目中使用 Gson 之前，确保在 <code>build.gradle</code> 中添加 Gson 库的依赖</p><p>• 使用 <code>Gson()</code> 创建 Gson 实例。</p><p>• 使用 <code>fromJson()</code> 方法将 JSON 字符串转换为 Kotlin 对象。</p><p>• 使用 <code>toJson()</code> 方法将 Kotlin 对象转换为 JSON 字符串。</p><p>• 处理 JSON 数组，使用 <code>Array&lt;Book&gt;::class.java</code> 解析为列表。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">implementation &#x27;com.google.code.gson:gson:2.8.8&#x27;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> jsonString = <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">        &#123;</span></span><br><span class="line"><span class="string">          &quot;title&quot;: &quot;Kotlin Programming&quot;,</span></span><br><span class="line"><span class="string">          &quot;author&quot;: &#123;</span></span><br><span class="line"><span class="string">            &quot;name&quot;: &quot;Jane Doe&quot;,</span></span><br><span class="line"><span class="string">            &quot;age&quot;: 30</span></span><br><span class="line"><span class="string">          &#125;,</span></span><br><span class="line"><span class="string">          &quot;price&quot;: 39.99,</span></span><br><span class="line"><span class="string">          &quot;available&quot;: true</span></span><br><span class="line"><span class="string">        &#125;</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">val</span> gson = Gson()</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将 JSON 字符串转换为 Book 对象</span></span><br><span class="line">    <span class="keyword">val</span> book = gson.fromJson(jsonString, Book::<span class="keyword">class</span>.java)</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 输出 Book 对象和嵌套的 Author 对象</span></span><br><span class="line">    println(<span class="string">&quot;Title: <span class="subst">$&#123;book.title&#125;</span>&quot;</span>)</span><br><span class="line">    println(<span class="string">&quot;Author Name: <span class="subst">$&#123;book.author.name&#125;</span>&quot;</span>)</span><br><span class="line">    println(<span class="string">&quot;Author Age: <span class="subst">$&#123;book.author.age&#125;</span>&quot;</span>)</span><br><span class="line">    println(<span class="string">&quot;Price: <span class="subst">$&#123;book.price&#125;</span>&quot;</span>)</span><br><span class="line">    println(<span class="string">&quot;Available: <span class="subst">$&#123;book.available&#125;</span>&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="网络请求回调的实现方式避免阻塞主线程"><a class="markdownIt-Anchor" href="#网络请求回调的实现方式避免阻塞主线程"></a> 网络请求回调的实现方式(避免阻塞主线程)</h3><ol><li>定义回调接口。</li><li>在发起网络请求的类中，接受该接口并在适当时机调用回调方法。</li><li>在需要接收回调的地方实现该接口。</li></ol><h5 id="1-定义回调接口"><a class="markdownIt-Anchor" href="#1-定义回调接口"></a> <strong>1. 定义回调接口：</strong></h5><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">NetworkCallback</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">onSuccess</span><span class="params">(response: <span class="type">String</span>)</span></span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">onFailure</span><span class="params">(error: <span class="type">String</span>)</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="2-发起网络请求的类"><a class="markdownIt-Anchor" href="#2-发起网络请求的类"></a> <strong>2. 发起网络请求的类：</strong></h5><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">NetworkRequest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 模拟网络请求</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">makeRequest</span><span class="params">(url: <span class="type">String</span>, callback: <span class="type">NetworkCallback</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 使用线程来进行网络请求</span></span><br><span class="line">        thread &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">val</span> connection = URL(url).openConnection() <span class="keyword">as</span> HttpURLConnection</span><br><span class="line">                connection.requestMethod = <span class="string">&quot;GET&quot;</span></span><br><span class="line">                connection.connect()</span><br><span class="line"></span><br><span class="line">                <span class="keyword">val</span> responseCode = connection.responseCode</span><br><span class="line">                <span class="keyword">if</span> (responseCode == HttpURLConnection.HTTP_OK) &#123;</span><br><span class="line">                    <span class="keyword">val</span> response = connection.inputStream.bufferedReader().use &#123; it.readText() &#125;</span><br><span class="line">                    <span class="comment">// 请求成功，回调 onSuccess</span></span><br><span class="line">                    callback.onSuccess(response)</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 请求失败，回调 onFailure</span></span><br><span class="line">                    callback.onFailure(<span class="string">&quot;Error: <span class="variable">$responseCode</span>&quot;</span>)</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (e: Exception) &#123;</span><br><span class="line">                callback.onFailure(<span class="string">&quot;Error: <span class="subst">$&#123;e.message&#125;</span>&quot;</span>)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="3-在-activity-或-fragment-中实现回调接口"><a class="markdownIt-Anchor" href="#3-在-activity-或-fragment-中实现回调接口"></a> <strong>3. 在 Activity 或 Fragment 中实现回调接口：</strong></h5><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>(), NetworkCallback &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="keyword">val</span> networkRequest = NetworkRequest()</span><br><span class="line">        networkRequest.makeRequest(<span class="string">&quot;https://jsonplaceholder.typicode.com/posts&quot;</span>, <span class="keyword">this</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 实现接口中的方法</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onSuccess</span><span class="params">(response: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 请求成功，处理响应数据</span></span><br><span class="line">        println(<span class="string">&quot;Response: <span class="variable">$response</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onFailure</span><span class="params">(error: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 请求失败，处理错误信息</span></span><br><span class="line">        println(<span class="string">&quot;Error: <span class="variable">$error</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="最好用的网络库retrofit"><a class="markdownIt-Anchor" href="#最好用的网络库retrofit"></a> 最好用的网络库:Retrofit</h3><p><strong>Retrofit</strong> 是一个类型安全的 HTTP 客户端，用于简化与 RESTful API 的通信。它由 <strong>Square</strong> 公司开发，广泛用于 Android 和 Java 项目中。通过 Retrofit，我们可以轻松地将 HTTP 请求转换为方法调用，处理网络请求和响应时，自动将 JSON 数据转换为 Java 对象，提供简洁而强大的 API。</p><h4 id="1-retrofit-的特点"><a class="markdownIt-Anchor" href="#1-retrofit-的特点"></a> <strong>1. Retrofit 的特点</strong></h4><ul><li><strong>简化 HTTP 请求</strong>：通过注解和接口的方式将 HTTP 请求转换为方法调用，避免了手动编写大量网络请求代码。</li><li><strong>类型安全</strong>：通过返回特定的 Java 对象，避免了直接操作 JSON 字符串的复杂性。</li><li><strong>与 Gson、Moshi 等库集成</strong>：支持 Gson 等 JSON 转换器，自动将 JSON 数据转换为 Java 对象。</li><li><strong>异步与同步请求支持</strong>：Retrofit 支持异步请求和同步请求，方便根据需求选择合适的方式。</li><li><strong>支持文件上传和下载</strong>：支持上传和下载文件，非常适合处理文件传输任务。</li></ul><h4 id="2-添加-retrofit-依赖"><a class="markdownIt-Anchor" href="#2-添加-retrofit-依赖"></a> <strong>2. 添加 Retrofit 依赖</strong></h4><p>首先，在项目中添加 Retrofit 依赖。你可以在 <code>build.gradle</code> 文件中添加如下代码：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">// Retrofit 依赖</span><br><span class="line">implementation &#x27;com.squareup.retrofit2:retrofit:2.9.0&#x27;</span><br><span class="line"></span><br><span class="line">// Gson 转换器依赖</span><br><span class="line">implementation &#x27;com.squareup.retrofit2:converter-gson:2.9.0&#x27;</span><br></pre></td></tr></table></figure><p>如果你需要支持其他数据转换格式（如 Moshi），可以更改转换器的依赖。例如，使用 <strong>Moshi</strong> 作为 JSON 转换器：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">implementation &#x27;com.squareup.retrofit2:converter-moshi:2.9.0&#x27;</span><br></pre></td></tr></table></figure><h4 id="3-使用-retrofit-的基本步骤"><a class="markdownIt-Anchor" href="#3-使用-retrofit-的基本步骤"></a> <strong>3. 使用 Retrofit 的基本步骤</strong></h4><h5 id="1创建-api-接口"><a class="markdownIt-Anchor" href="#1创建-api-接口"></a> <strong>(1)：创建 API 接口</strong></h5><p>在 Retrofit 中，网络请求通过定义接口方法来实现。每个接口方法对应一个 HTTP 请求。你可以通过注解定义请求的类型（如 <code>GET</code>、<code>POST</code>、<code>PUT</code> 等）。</p><p>例如，假设我们要请求一个返回用户信息的 API：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">ApiService</span> &#123;</span><br><span class="line">    <span class="comment">// GET 请求，获取用户数据</span></span><br><span class="line">    <span class="meta">@GET(<span class="string">&quot;users/&#123;userId&#125;&quot;</span>)</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">getUser</span><span class="params">(<span class="meta">@Path(<span class="string">&quot;userId&quot;</span>)</span> userId: <span class="type">String</span>)</span></span>: Call&lt;User&gt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中，<code>getUser</code> 方法通过 <code>@GET</code> 注解定义了一个 GET 请求，并且通过 <code>@Path</code> 注解动态传入 <code>userId</code>。</p><h5 id="2创建数据模型类"><a class="markdownIt-Anchor" href="#2创建数据模型类"></a> <strong>(2)：创建数据模型类</strong></h5><p>假设我们收到的 JSON 响应是：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">  <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;John Doe&quot;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="attr">&quot;email&quot;</span><span class="punctuation">:</span> <span class="string">&quot;john.doe@example.com&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><p>你需要创建一个 Java 或 Kotlin 数据类来映射 JSON 响应：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">data</span> <span class="keyword">class</span> <span class="title class_">User</span>(</span><br><span class="line">    <span class="keyword">val</span> id: <span class="built_in">Int</span>,</span><br><span class="line">    <span class="keyword">val</span> name: String,</span><br><span class="line">    <span class="keyword">val</span> email: String</span><br><span class="line">)</span><br></pre></td></tr></table></figure><h5 id="3创建-retrofit-实例"><a class="markdownIt-Anchor" href="#3创建-retrofit-实例"></a> <strong>(3)：创建 Retrofit 实例</strong></h5><p>Retrofit 需要一个 <code>Retrofit</code> 实例，你可以配置 Retrofit 的基本 URL 和转换器（如 Gson）：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> retrofit = Retrofit.Builder()</span><br><span class="line">    .baseUrl(<span class="string">&quot;https://api.example.com/&quot;</span>)  <span class="comment">// 设置基础 URL</span></span><br><span class="line">    .addConverterFactory(GsonConverterFactory.create())  <span class="comment">// 使用 Gson 转换器</span></span><br><span class="line">    .build()</span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> apiService = retrofit.create(ApiService::<span class="keyword">class</span>.java)</span><br></pre></td></tr></table></figure><ul><li><strong><code>baseUrl</code></strong>：指定服务器的基本 URL。注意，URL 必须以 <code>/</code> 结尾。</li><li><strong><code>addConverterFactory</code></strong>：指定数据转换器，告诉 Retrofit 如何将响应转换为相应的对象。</li></ul><h5 id="4执行网络请求"><a class="markdownIt-Anchor" href="#4执行网络请求"></a> <strong>(4)：执行网络请求</strong></h5><p>Retrofit 支持同步和异步请求。如果你希望同步地执行网络请求，可以使用 <code>execute()</code> 方法。如果你需要异步执行请求并处理回调，可以使用 <code>enqueue()</code> 方法。</p><p><strong>1. 异步请求：</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> call = apiService.getUser(<span class="string">&quot;123&quot;</span>)  <span class="comment">// 发起网络请求</span></span><br><span class="line"></span><br><span class="line">call.enqueue(<span class="keyword">object</span> : Callback&lt;User&gt; &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onResponse</span><span class="params">(call: <span class="type">Call</span>&lt;<span class="type">User</span>&gt;, response: <span class="type">Response</span>&lt;<span class="type">User</span>&gt;)</span></span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (response.isSuccessful) &#123;</span><br><span class="line">            <span class="comment">// 请求成功，处理响应</span></span><br><span class="line">            <span class="keyword">val</span> user = response.body()</span><br><span class="line">            println(<span class="string">&quot;User: <span class="variable">$user</span>&quot;</span>)</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 请求失败</span></span><br><span class="line">            println(<span class="string">&quot;Error: <span class="subst">$&#123;response.message()&#125;</span>&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onFailure</span><span class="params">(call: <span class="type">Call</span>&lt;<span class="type">User</span>&gt;, t: <span class="type">Throwable</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 请求失败，处理错误</span></span><br><span class="line">        println(<span class="string">&quot;Error: <span class="subst">$&#123;t.message&#125;</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p><strong>2. 同步请求：</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> call = apiService.getUser(<span class="string">&quot;123&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">val</span> response = call.execute()  <span class="comment">// 执行同步请求</span></span><br><span class="line">    <span class="keyword">if</span> (response.isSuccessful) &#123;</span><br><span class="line">        <span class="comment">// 请求成功，处理响应</span></span><br><span class="line">        <span class="keyword">val</span> user = response.body()</span><br><span class="line">        println(<span class="string">&quot;User: <span class="variable">$user</span>&quot;</span>)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 请求失败</span></span><br><span class="line">        println(<span class="string">&quot;Error: <span class="subst">$&#123;response.message()&#125;</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (e: Exception) &#123;</span><br><span class="line">    <span class="comment">// 异常处理</span></span><br><span class="line">    println(<span class="string">&quot;Error: <span class="subst">$&#123;e.message&#125;</span>&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="5处理请求结果"><a class="markdownIt-Anchor" href="#5处理请求结果"></a> <strong>(5)：处理请求结果</strong></h5><p>在响应成功的情况下，<code>response.body()</code> 将返回解析后的对象（此例中是 <code>User</code> 对象）。如果响应失败，可以通过 <code>response.message()</code> 或 <code>response.code()</code> 来获取错误信息。</p><h4 id="4-其他功能"><a class="markdownIt-Anchor" href="#4-其他功能"></a> <strong>4. 其他功能</strong></h4><h5 id="支持请求体和请求参数"><a class="markdownIt-Anchor" href="#支持请求体和请求参数"></a> <strong>支持请求体和请求参数</strong></h5><p>你可以使用 Retrofit 支持的注解来处理请求体和请求参数，例如：</p><ul><li><strong><code>@Body</code></strong>：将对象作为请求体。</li><li><strong><code>@Query</code></strong>：将参数作为 URL 查询字符串。</li><li><strong><code>@Field</code></strong>：用于 <code>POST</code> 请求，将字段作为表单数据发送。</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@POST(<span class="string">&quot;users&quot;</span>)</span></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">createUser</span><span class="params">(<span class="meta">@Body</span> user: <span class="type">User</span>)</span></span>: Call&lt;User&gt;</span><br><span class="line"></span><br><span class="line"><span class="meta">@GET(<span class="string">&quot;users&quot;</span>)</span></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">getUsers</span><span class="params">(<span class="meta">@Query(<span class="string">&quot;page&quot;</span>)</span> page: <span class="type">Int</span>)</span></span>: Call&lt;List&lt;User&gt;&gt;</span><br></pre></td></tr></table></figure><h5 id="上传文件"><a class="markdownIt-Anchor" href="#上传文件"></a> <strong>上传文件</strong></h5><p>Retrofit 支持通过 <code>@Multipart</code> 注解上传文件，结合 <code>@Part</code> 注解上传文件和其他表单字段。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Multipart</span></span><br><span class="line"><span class="meta">@POST(<span class="string">&quot;upload&quot;</span>)</span></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">uploadFile</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="meta">@Part</span> file: <span class="type">MultipartBody</span>.<span class="type">Part</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="meta">@Part(<span class="string">&quot;description&quot;</span>)</span> description: <span class="type">RequestBody</span></span></span></span><br><span class="line"><span class="params"><span class="function">)</span></span>: Call&lt;ResponseBody&gt;</span><br></pre></td></tr></table></figure><h5 id="下载文件"><a class="markdownIt-Anchor" href="#下载文件"></a> <strong>下载文件</strong></h5><p>通过 Retrofit 也可以轻松地下载文件：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@GET(<span class="string">&quot;path/to/file&quot;</span>)</span></span><br><span class="line"><span class="meta">@Streaming</span></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">downloadFile</span><span class="params">(<span class="meta">@Url</span> fileUrl: <span class="type">String</span>)</span></span>: Call&lt;ResponseBody&gt;</span><br></pre></td></tr></table></figure><hr /><h2 id="9最佳ui体验material-design"><a class="markdownIt-Anchor" href="#9最佳ui体验material-design"></a> 9.最佳UI体验,Material Design</h2><p>Material Design 是由 Google 于 2014 年推出的一种设计语言，旨在为跨平台的应用程序提供一致的用户体验。它不仅是一种视觉设计规范，还结合了交互、动画和功能设计的原则，适用于 Android 应用开发以及其他平台（如 Web 和 iOS）。Material Design 的核心理念是通过模拟现实世界中的物理材质（如纸张和墨水）来创建直观且美观的用户界面。</p><h3 id="1-toolbar"><a class="markdownIt-Anchor" href="#1-toolbar"></a> 1. Toolbar</h3><p><strong>作用：</strong><br />Toolbar 是应用程序的操作栏，可替换传统的 ActionBar，支持自定义布局、菜单和导航图标。</p><p><strong>XML 示例：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">androidx.appcompat.widget.Toolbar</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/toolbar&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;?attr/actionBarSize&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:background</span>=<span class="string">&quot;?attr/colorPrimary&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">app:popupTheme</span>=<span class="string">&quot;@style/ThemeOverlay.AppCompat.Light&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><hr /><h3 id="2-drawerlayout"><a class="markdownIt-Anchor" href="#2-drawerlayout"></a> 2. DrawerLayout</h3><p><strong>作用：</strong><br />DrawerLayout 用于实现侧滑菜单（导航抽屉）的根布局，它允许在主内容之外滑出一个侧边栏。</p><p><strong>XML 示例：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">androidx.drawerlayout.widget.DrawerLayout</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/drawer_layout&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 主内容区域 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">FrameLayout</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/content_frame&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 侧滑菜单区域（NavigationView 在 DrawerLayout 中）--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">com.google.android.material.navigation.NavigationView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/nav_view&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_gravity</span>=<span class="string">&quot;start&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">app:menu</span>=<span class="string">&quot;@menu/drawer_menu&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">androidx.drawerlayout.widget.DrawerLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><hr /><h3 id="3-navigationview"><a class="markdownIt-Anchor" href="#3-navigationview"></a> 3. NavigationView</h3><p><strong>作用：</strong><br />NavigationView 用于在侧滑菜单中显示导航菜单项，可方便地配置菜单资源、头部布局等。</p><p><strong>XML 示例（已嵌入在 DrawerLayout 中）：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">com.google.android.material.navigation.NavigationView</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/nav_view&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_gravity</span>=<span class="string">&quot;start&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">app:headerLayout</span>=<span class="string">&quot;@layout/nav_header&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">app:menu</span>=<span class="string">&quot;@menu/drawer_menu&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">implementation(&quot;com.google.android.material:material:1.12.0&quot;)</span><br><span class="line">implementation(&quot;de.hdodenhof:circleimageview:3.1.0&quot;)</span><br></pre></td></tr></table></figure><hr /><h3 id="4-floatingactionbutton"><a class="markdownIt-Anchor" href="#4-floatingactionbutton"></a> 4. FloatingActionButton</h3><p><strong>作用：</strong><br />FloatingActionButton（浮动操作按钮）是一种圆形按钮，常用于突出主要的交互操作。</p><p><strong>XML 示例：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">com.google.android.material.floatingactionbutton.FloatingActionButton</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/fab&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_margin</span>=<span class="string">&quot;16dp&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:src</span>=<span class="string">&quot;@drawable/ic_add&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">app:backgroundTint</span>=<span class="string">&quot;@color/colorAccent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">app:layout_anchor</span>=<span class="string">&quot;@id/coordinator_layout&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">app:layout_anchorGravity</span>=<span class="string">&quot;bottom|end&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><hr /><h3 id="5-snackbar"><a class="markdownIt-Anchor" href="#5-snackbar"></a> 5. Snackbar</h3><p><strong>作用：</strong><br />Snackbar 用于短暂地在屏幕底部显示信息反馈。虽然 Snackbar 主要在代码中调用显示，但其依赖于包含在布局中的 <code>CoordinatorLayout</code> 来获得自动动画和交互。</p><p><strong>代码示例（Kotlin 代码中调用）：</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Snackbar.make(findViewById(R.id.coordinator_layout), <span class="string">&quot;这是一个Snackbar&quot;</span>, Snackbar.LENGTH_LONG)</span><br><span class="line">    .setAction(<span class="string">&quot;撤销&quot;</span>) &#123;</span><br><span class="line">        <span class="comment">// 处理点击事件</span></span><br><span class="line">    &#125;</span><br><span class="line">    .show()</span><br></pre></td></tr></table></figure><blockquote><p><strong>说明：</strong> Snackbar 本身不需要在 XML 中声明，而是依赖于存在的布局容器（例如 CoordinatorLayout）。</p></blockquote><hr /><h3 id="6-coordinatorlayout"><a class="markdownIt-Anchor" href="#6-coordinatorlayout"></a> 6. CoordinatorLayout</h3><p><strong>作用：</strong><br />CoordinatorLayout 是一个高级布局容器，可以协调其内部子控件的交互，如 FloatingActionButton 的自动隐藏、Snackbar 的自动上移等。</p><p><strong>XML 示例：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">androidx.coordinatorlayout.widget.CoordinatorLayout</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/coordinator_layout&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 这里可以嵌入 AppBarLayout、FloatingActionButton 等 --&gt;</span></span><br><span class="line">    </span><br><span class="line"><span class="tag">&lt;/<span class="name">androidx.coordinatorlayout.widget.CoordinatorLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><hr /><h3 id="7-materialcardview"><a class="markdownIt-Anchor" href="#7-materialcardview"></a> 7. MaterialCardView</h3><p><strong>作用：</strong><br />MaterialCardView 是基于 CardView 的扩展，提供更多 Material Design 风格的卡片效果，例如圆角、阴影以及状态变化。</p><p><strong>XML 示例：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">com.google.android.material.card.MaterialCardView</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/material_card&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_margin</span>=<span class="string">&quot;16dp&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">app:cardCornerRadius</span>=<span class="string">&quot;8dp&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">app:cardElevation</span>=<span class="string">&quot;4dp&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 卡片内部内容 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:padding</span>=<span class="string">&quot;16dp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;这是一个MaterialCardView&quot;</span> /&gt;</span></span><br><span class="line">        </span><br><span class="line"><span class="tag">&lt;/<span class="name">com.google.android.material.card.MaterialCardView</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">implementation(&quot;androidx.recyclerview:recyclerview:1.3.1&quot;)</span><br><span class="line">implementation(&quot;com.github.bumptech.glide:glide:4.15.0&quot;)</span><br></pre></td></tr></table></figure><hr /><h3 id="8-appbarlayout"><a class="markdownIt-Anchor" href="#8-appbarlayout"></a> 8. AppBarLayout</h3><p><strong>作用：</strong><br />AppBarLayout 是用于实现应用栏（如 Toolbar、Tabs）的容器，它通常配合 CollapsingToolbarLayout 实现折叠效果。</p><p><strong>XML 示例：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">com.google.android.material.appbar.AppBarLayout</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/appbar_layout&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:theme</span>=<span class="string">&quot;@style/AppTheme.AppBarOverlay&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">androidx.appcompat.widget.Toolbar</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/toolbar&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;?attr/actionBarSize&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:background</span>=<span class="string">&quot;?attr/colorPrimary&quot;</span> /&gt;</span></span><br><span class="line">        </span><br><span class="line"><span class="tag">&lt;/<span class="name">com.google.android.material.appbar.AppBarLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><hr /><h3 id="9-swiperefreshlayout"><a class="markdownIt-Anchor" href="#9-swiperefreshlayout"></a> 9. SwipeRefreshLayout</h3><p><strong>作用：</strong><br />SwipeRefreshLayout 用于实现下拉刷新效果，当用户下拉时可以触发刷新操作。</p><p><strong>XML 示例：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">androidx.swiperefreshlayout.widget.SwipeRefreshLayout</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/swipe_refresh&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 包含可刷新的内容区域，例如 RecyclerView --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">androidx.recyclerview.widget.RecyclerView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/recycler_view&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">androidx.swiperefreshlayout.widget.SwipeRefreshLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">implementation(&quot;androidx.swiperefreshlayout:swiperefreshlayout:1.1.0&quot;)</span><br></pre></td></tr></table></figure><hr /><h3 id="10-collapsingtoolbarlayout"><a class="markdownIt-Anchor" href="#10-collapsingtoolbarlayout"></a> 10. CollapsingToolbarLayout</h3><p><strong>作用：</strong><br />CollapsingToolbarLayout 用于实现可折叠的 Toolbar 效果，在滚动时可以收缩标题区域，与 AppBarLayout 协同工作。</p><p><strong>XML 示例：</strong></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">com.google.android.material.appbar.CollapsingToolbarLayout</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/collapsing_toolbar&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;300dp&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">app:layout_scrollFlags</span>=<span class="string">&quot;scroll|exitUntilCollapsed&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">app:contentScrim</span>=<span class="string">&quot;?attr/colorPrimary&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">ImageView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/header_image&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:scaleType</span>=<span class="string">&quot;centerCrop&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:src</span>=<span class="string">&quot;@drawable/header_image&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">app:layout_collapseMode</span>=<span class="string">&quot;parallax&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">androidx.appcompat.widget.Toolbar</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/toolbar&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;?attr/actionBarSize&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">app:layout_collapseMode</span>=<span class="string">&quot;pin&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">com.google.android.material.appbar.CollapsingToolbarLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><hr /><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 根布局：DrawerLayout 是实现侧滑菜单的容器 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">androidx.drawerlayout.widget.DrawerLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/drawerLayout&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">DrawerLayout</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中引用</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">    android:layout_width=&quot;match_parent&quot; <span class="comment">&lt;!-- 占据整个屏幕宽度 --&gt;</span></span><br><span class="line">    android:layout_height=&quot;match_parent&quot; <span class="comment">&lt;!-- 占据整个屏幕高度 --&gt;</span></span><br><span class="line">    tools:context=&quot;.MainActivity&quot;&gt; <span class="comment">&lt;!-- 指定与该布局关联的 Activity --&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- CoordinatorLayout 是一个增强型布局容器，用于协调子视图的交互和动画 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">androidx.coordinatorlayout.widget.CoordinatorLayout</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!-- AppBarLayout 用于管理 Toolbar 和其他滚动相关的组件 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">com.google.android.material.appbar.AppBarLayout</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">&lt;!-- Toolbar 替代传统的 ActionBar，作为应用的顶部导航栏 --&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">androidx.appcompat.widget.Toolbar</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/toolbar&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">Toolbar</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中设置支持操作栏</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">                android:layout_width=&quot;match_parent&quot;</span><br><span class="line">                android:layout_height=&quot;?attr/actionBarSize&quot; <span class="comment">&lt;!-- 使用系统定义的动作栏高度 --&gt;</span></span><br><span class="line">                android:background=&quot;@color/black&quot; <span class="comment">&lt;!-- 设置背景颜色为黑色 --&gt;</span></span><br><span class="line">                android:theme=&quot;@style/ThemeOverlay.AppCompat.Dark.ActionBar&quot; <span class="comment">&lt;!-- 设置深色主题 --&gt;</span></span><br><span class="line">                app:popupTheme=&quot;@style/ThemeOverlay.AppCompat.Light&quot; <span class="comment">&lt;!-- 弹出菜单使用浅色主题 --&gt;</span></span><br><span class="line">                app:layout_scrollFlags=&quot;scroll|enterAlways|snap&quot;&gt; <span class="comment">&lt;!-- 滚动时隐藏/显示 Toolbar --&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">androidx.appcompat.widget.Toolbar</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;/<span class="name">com.google.android.material.appbar.AppBarLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!-- SwipeRefreshLayout 实现下拉刷新功能 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">androidx.swiperefreshlayout.widget.SwipeRefreshLayout</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/swipeRefresh&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">SwipeRefreshLayout</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中控制刷新状态</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">            android:layout_width=&quot;match_parent&quot;</span><br><span class="line">            android:layout_height=&quot;match_parent&quot;</span><br><span class="line">            app:layout_behavior=&quot;@string/appbar_scrolling_view_behavior&quot;&gt; <span class="comment">&lt;!-- 与 AppBarLayout 配合使用，确保滚动行为一致 --&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">&lt;!-- RecyclerView 用于显示列表数据 --&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">androidx.recyclerview.widget.RecyclerView</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/recyclerView&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">RecyclerView</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中绑定适配器</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">                android:layout_width=&quot;match_parent&quot;</span><br><span class="line">                android:layout_height=&quot;match_parent&quot; /&gt;</span><br><span class="line">        <span class="tag">&lt;/<span class="name">androidx.swiperefreshlayout.widget.SwipeRefreshLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!-- FloatingActionButton 是一个悬浮按钮，用于执行核心操作 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">com.google.android.material.floatingactionbutton.FloatingActionButton</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/fab&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">FloatingActionButton</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中设置点击事件</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">            android:layout_width=&quot;wrap_content&quot;</span><br><span class="line">            android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">            android:layout_gravity=&quot;bottom|end&quot; <span class="comment">&lt;!-- 将按钮放置在右下角 --&gt;</span></span><br><span class="line">            android:layout_margin=&quot;16dp&quot; <span class="comment">&lt;!-- 设置按钮与屏幕边缘的间距 --&gt;</span></span><br><span class="line">            app:srcCompat=&quot;@android:drawable/ic_menu_edit&quot; <span class="comment">&lt;!-- 设置按钮图标 --&gt;</span></span><br><span class="line">            android:elevation=&quot;8dp&quot; <span class="comment">&lt;!-- 设置阴影效果，提升视觉层次感 --&gt;</span></span><br><span class="line">            /&gt;</span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;/<span class="name">androidx.coordinatorlayout.widget.CoordinatorLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- NavigationView 是侧滑菜单的内容部分 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">com.google.android.material.navigation.NavigationView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/navView&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">NavigationView</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中处理菜单点击事件</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">        android:layout_width=&quot;match_parent&quot; <span class="comment">&lt;!-- 菜单占据整个侧滑区域的宽度 --&gt;</span></span><br><span class="line">        android:layout_height=&quot;match_parent&quot; <span class="comment">&lt;!-- 菜单占据整个侧滑区域的高度 --&gt;</span></span><br><span class="line">        android:layout_gravity=&quot;start&quot; <span class="comment">&lt;!-- 指定菜单从左侧滑出（start 表示从左到右的语言环境） --&gt;</span></span><br><span class="line">        app:headerLayout=&quot;@layout/nav_header&quot; <span class="comment">&lt;!-- 设置菜单头部布局 --&gt;</span></span><br><span class="line">        app:menu=&quot;@menu/nav_menu&quot; /&gt; <span class="comment">&lt;!-- 设置菜单项资源文件 --&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">androidx.drawerlayout.widget.DrawerLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 根布局：CoordinatorLayout 是一个增强型布局容器，用于协调子视图的交互和动画 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">androidx.coordinatorlayout.widget.CoordinatorLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/main&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">CoordinatorLayout</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中引用</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">    android:layout_width=&quot;match_parent&quot; <span class="comment">&lt;!-- 占据整个屏幕宽度 --&gt;</span></span><br><span class="line">    android:layout_height=&quot;match_parent&quot; <span class="comment">&lt;!-- 占据整个屏幕高度 --&gt;</span></span><br><span class="line">    tools:context=&quot;.FruitActivity&quot; <span class="comment">&lt;!-- 指定与该布局关联的 Activity --&gt;</span></span><br><span class="line">    android:fitsSystemWindows=&quot;true&quot;&gt; <span class="comment">&lt;!-- 确保布局不会被系统窗口（如状态栏）遮挡 --&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- AppBarLayout 用于管理顶部应用栏的滚动行为 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">com.google.android.material.appbar.AppBarLayout</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/appBar&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">AppBarLayout</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中引用</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">        android:layout_width=&quot;match_parent&quot; <span class="comment">&lt;!-- 占据整个屏幕宽度 --&gt;</span></span><br><span class="line">        android:layout_height=&quot;250dp&quot; <span class="comment">&lt;!-- 固定高度为 250dp --&gt;</span></span><br><span class="line">        android:fitsSystemWindows=&quot;true&quot;&gt; <span class="comment">&lt;!-- 确保内容不会被系统窗口遮挡 --&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!-- CollapsingToolbarLayout 实现折叠效果的标题栏 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">com.google.android.material.appbar.CollapsingToolbarLayout</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/collapsingToolbar&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">CollapsingToolbarLayout</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中设置标题</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">            android:layout_width=&quot;match_parent&quot;</span><br><span class="line">            android:layout_height=&quot;match_parent&quot;</span><br><span class="line">            android:theme=&quot;@style/ThemeOverlay.AppCompat.Dark.ActionBar&quot; <span class="comment">&lt;!-- 设置深色主题 --&gt;</span></span><br><span class="line">            android:fitsSystemWindows=&quot;true&quot;</span><br><span class="line">            app:contentScrim=&quot;?attr/colorPrimary&quot; <span class="comment">&lt;!-- 折叠时的背景颜色 --&gt;</span></span><br><span class="line">            app:layout_scrollFlags=&quot;scroll|exitUntilCollapsed&quot;&gt; <span class="comment">&lt;!-- 滚动时折叠效果 --&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">&lt;!-- ImageView 显示顶部大图，支持折叠动画 --&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">ImageView</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/fruitImageView&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">ImageView</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中设置图片</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">                android:layout_width=&quot;match_parent&quot;</span><br><span class="line">                android:layout_height=&quot;match_parent&quot;</span><br><span class="line">                android:scaleType=&quot;centerCrop&quot; <span class="comment">&lt;!-- 图片缩放方式，确保填满整个区域 --&gt;</span></span><br><span class="line">                android:fitsSystemWindows=&quot;true&quot;</span><br><span class="line">                app:layout_collapseMode=&quot;parallax&quot; /&gt; <span class="comment">&lt;!-- 图片在折叠时产生视差效果 --&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">&lt;!-- Toolbar 替代传统的 ActionBar，作为应用的顶部导航栏 --&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">androidx.appcompat.widget.Toolbar</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/toolbar&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">Toolbar</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中设置支持操作栏</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">                android:layout_width=&quot;match_parent&quot;</span><br><span class="line">                android:layout_height=&quot;?attr/actionBarSize&quot; <span class="comment">&lt;!-- 使用系统定义的动作栏高度 --&gt;</span></span><br><span class="line">                app:layout_collapseMode=&quot;pin&quot; /&gt; <span class="comment">&lt;!-- 在折叠时固定显示 --&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;/<span class="name">com.google.android.material.appbar.CollapsingToolbarLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;/<span class="name">com.google.android.material.appbar.AppBarLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- NestedScrollView 实现可滚动的内容区域 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">androidx.core.widget.NestedScrollView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">app:layout_behavior</span>=<span class="string">&quot;@string/appbar_scrolling_view_behavior&quot;</span>&gt;</span> <span class="comment">&lt;!-- 与 AppBarLayout 配合使用，确保滚动行为一致 --&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!-- LinearLayout 垂直排列内容 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">LinearLayout</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">垂直方向排列子视图</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">            android:layout_width=&quot;match_parent&quot;</span><br><span class="line">            android:layout_height=&quot;wrap_content&quot;&gt;</span><br><span class="line"></span><br><span class="line">            <span class="comment">&lt;!-- MaterialCardView 显示内容卡片 --&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">com.google.android.material.card.MaterialCardView</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_marginBottom</span>=<span class="string">&quot;15dp&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">底部外边距</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">                android:layout_marginLeft=&quot;15dp&quot; <span class="comment">&lt;!-- 左侧外边距 --&gt;</span></span><br><span class="line">                android:layout_marginRight=&quot;15dp&quot; <span class="comment">&lt;!-- 右侧外边距 --&gt;</span></span><br><span class="line">                android:layout_marginTop=&quot;35dp&quot; <span class="comment">&lt;!-- 顶部外边距 --&gt;</span></span><br><span class="line">                app:cardCornerRadius=&quot;4dp&quot;&gt; <span class="comment">&lt;!-- 设置圆角半径 --&gt;</span></span><br><span class="line"></span><br><span class="line">                <span class="comment">&lt;!-- TextView 显示主要内容 --&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">                    <span class="attr">android:id</span>=<span class="string">&quot;@+id/fruitContentText&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">TextView</span> <span class="attr">的</span> <span class="attr">ID</span>，<span class="attr">用于在代码中设置文本</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">                    android:layout_width=&quot;wrap_content&quot;</span><br><span class="line">                    android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">                    android:layout_margin=&quot;10dp&quot; /&gt; <span class="comment">&lt;!-- 内边距 --&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="tag">&lt;/<span class="name">com.google.android.material.card.MaterialCardView</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">androidx.core.widget.NestedScrollView</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- FloatingActionButton 是一个悬浮按钮，用于执行核心操作 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">com.google.android.material.floatingactionbutton.FloatingActionButton</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_margin</span>=<span class="string">&quot;16dp&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">设置按钮与屏幕边缘的间距</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">        app:srcCompat=&quot;@android:drawable/ic_menu_edit&quot; <span class="comment">&lt;!-- 设置按钮图标 --&gt;</span></span><br><span class="line">        app:layout_anchor=&quot;@id/appBar&quot; <span class="comment">&lt;!-- 将按钮锚定到 AppBarLayout --&gt;</span></span><br><span class="line">        app:pressedTranslationZ=&quot;12dp&quot; <span class="comment">&lt;!-- 按下时的阴影深度 --&gt;</span></span><br><span class="line">        app:layout_anchorGravity=&quot;bottom|end&quot; /&gt; <span class="comment">&lt;!-- 将按钮放置在右下角 --&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">androidx.coordinatorlayout.widget.CoordinatorLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><hr /><h2 id="10高级程序开发组件jetpack"><a class="markdownIt-Anchor" href="#10高级程序开发组件jetpack"></a> 10.高级程序开发组件Jetpack</h2><p>Jetpack 是 Android 官方推出的一套组件库，旨在简化 Android 应用的开发，提升开发效率并保证代码质量。Jetpack 提供了大量的功能组件，可以帮助开发者轻松实现常见的 Android 开发任务，比如 UI 组件、数据存储、生命周期管理等。</p><table><thead><tr><th><strong>分类</strong></th><th><strong>组件</strong></th><th><strong>描述</strong></th></tr></thead><tbody><tr><td><strong>架构组件 (Architecture Components)</strong></td><td><strong>LiveData</strong></td><td>用于持有和管理 UI 相关的数据，自动观察数据变化，适用于需要更新 UI 的情况。</td></tr><tr><td></td><td><strong>ViewModel</strong></td><td>用于管理和持有 UI 相关的数据，能够在配置变化时（如屏幕旋转）保持数据不丢失。</td></tr><tr><td></td><td><strong>Room</strong></td><td>SQLite 数据库的高级封装，提供 ORM（对象关系映射）功能，简化数据库操作。</td></tr><tr><td></td><td><strong>WorkManager</strong></td><td>管理后台任务，支持定期任务和长期任务，确保任务在适当时机执行，并考虑任务的依赖和约束条件。</td></tr><tr><td><strong>UI 组件 (UI Components)</strong></td><td><strong>Navigation</strong></td><td>管理页面导航，避免 Fragment 事务和 Intent 操作，使用声明式方式管理页面跳转。</td></tr><tr><td></td><td><strong>ConstraintLayout</strong></td><td>灵活高效的布局组件，允许通过设置约束关系实现复杂的布局，支持动态调整。</td></tr><tr><td></td><td><strong>Paging</strong></td><td>高效加载大量数据，支持分页加载，在滑动列表时异步加载数据，避免卡顿。</td></tr><tr><td></td><td><strong>Fragment</strong></td><td>用于构建可复用的 UI 组件，支持动态加载内容，并且能够更好地管理多屏幕、设备和生命周期。</td></tr><tr><td><strong>行为组件 (Behavior Components)</strong></td><td><strong>Permissions</strong></td><td>简化权限请求管理，支持 Android 6.0+ 运行时权限请求规范，确保应用符合最佳实践。</td></tr><tr><td></td><td><strong>Broadcast</strong></td><td>用于发送和接收广播消息，简化了广播机制的使用，提供统一的接口。</td></tr><tr><td><strong>基础组件 (Foundation Components)</strong></td><td><strong>AppCompat</strong></td><td>提供对旧版本 Android 设备的支持，使得应用能够在不同 Android 版本上保持一致的外观。</td></tr><tr><td></td><td><strong>Android KTX</strong></td><td>提供 Kotlin 扩展函数，简化 Android 开发中的代码，使其更加简洁和易用。</td></tr><tr><td></td><td><strong>Test</strong></td><td>提供了 Android 应用的测试框架，帮助开发者更容易编写和运行单元测试、UI 测试等。</td></tr></tbody></table><hr /><h3 id="viewmodel"><a class="markdownIt-Anchor" href="#viewmodel"></a> **ViewModel **</h3><p><strong>ViewModel</strong> 是 Jetpack 架构组件之一，它用于存储和管理 UI 相关的数据，并能够在配置变化（如屏幕旋转）时持久化这些数据。通过使用 ViewModel，可以确保即使 Activity 或 Fragment 被销毁和重建，UI 数据也不会丢失，避免了因重新加载数据而造成的性能问题。</p><ul><li><strong>生命周期感知</strong>：ViewModel 与 Activity 或 Fragment 的生命周期绑定，但不会随着其被销毁而销毁。它的生命周期是与 UI 控件（如 Activity 和 Fragment）不同的。</li><li><strong>数据持久化</strong>：即使发生配置变化（如屏幕旋转），ViewModel 中的数据依然保持不变。</li><li><strong>轻松管理 UI 数据</strong>：可以集中处理 UI 相关的数据和逻辑，使 UI 代码更加简洁。</li></ul><hr /><h4 id="1-添加依赖"><a class="markdownIt-Anchor" href="#1-添加依赖"></a> 1. <strong>添加依赖</strong></h4><p>首先，确保你的项目中已经加入了 Jetpack 的 ViewModel 依赖。通常情况下，这些依赖已经包含在 Jetpack 中，但需要显式声明。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">implementation &quot;androidx.lifecycle:lifecycle-viewmodel-ktx:2.5.1&quot;</span><br></pre></td></tr></table></figure><h4 id="2-创建-viewmodel-类"><a class="markdownIt-Anchor" href="#2-创建-viewmodel-类"></a> 2. <strong>创建 ViewModel 类</strong></h4><p>你需要继承 <code>ViewModel</code> 类来创建自定义的 ViewModel。通常，它用于持有和管理 UI 数据。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyViewModel</span> : <span class="type">ViewModel</span>() &#123;</span><br><span class="line">    <span class="comment">// 这里是 UI 数据</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> _counter = MutableLiveData&lt;<span class="built_in">Int</span>&gt;()</span><br><span class="line">    <span class="keyword">val</span> counter: LiveData&lt;<span class="built_in">Int</span>&gt; <span class="keyword">get</span>() = _counter</span><br><span class="line"></span><br><span class="line">    <span class="keyword">init</span> &#123;</span><br><span class="line">        _counter.value = <span class="number">0</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">incrementCounter</span><span class="params">()</span></span> &#123;</span><br><span class="line">        _counter.value = (_counter.value ?: <span class="number">0</span>) + <span class="number">1</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="3-在-activity-或-fragment-中使用-viewmodel"><a class="markdownIt-Anchor" href="#3-在-activity-或-fragment-中使用-viewmodel"></a> 3. <strong>在 Activity 或 Fragment 中使用 ViewModel</strong></h4><p>在你的 Activity 或 Fragment 中，通过 <code>ViewModelProvider</code> 来获取 ViewModel 实例。</p><p><strong>在 Activity 中使用：</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用 viewModels() 委托来获取 ViewModel 实例</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> myViewModel: MyViewModel <span class="keyword">by</span> viewModels()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 观察 LiveData 数据</span></span><br><span class="line">        myViewModel.counter.observe(<span class="keyword">this</span>, Observer &#123; count -&gt;</span><br><span class="line">            <span class="comment">// 更新 UI</span></span><br><span class="line">            counterText.text = count.toString()</span><br><span class="line">        &#125;)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置按钮点击事件</span></span><br><span class="line">        incrementButton.setOnClickListener &#123;</span><br><span class="line">            myViewModel.incrementCounter()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="4-使用-livedata-观察数据变化"><a class="markdownIt-Anchor" href="#4-使用-livedata-观察数据变化"></a> 4. <strong>使用 LiveData 观察数据变化</strong></h4><p>在 ViewModel 中，通常使用 <code>LiveData</code> 来封装数据，Activity 或 Fragment 通过观察 <code>LiveData</code> 来更新 UI。当数据变化时，UI 会自动更新。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// LiveData 观察</span></span><br><span class="line">myViewModel.counter.observe(<span class="keyword">this</span>, Observer &#123; newCount -&gt;</span><br><span class="line">    <span class="comment">// 数据变化时更新 UI</span></span><br><span class="line">    counterText.text = newCount.toString()</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><h4 id="5-向-viewmodel-传递参数并保存数据"><a class="markdownIt-Anchor" href="#5-向-viewmodel-传递参数并保存数据"></a> 5. <strong>向 ViewModel 传递参数并保存数据</strong></h4><p>有时，你可能需要在创建 ViewModel 时向其传递参数。例如，可以通过构造函数传递一个参数并在 ViewModel 中使用。</p><p>为了向 <code>ViewModel</code> 传递参数，你可以使用 <code>ViewModelProvider.Factory</code> 来提供自定义的 ViewModel 创建逻辑。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个需要参数的 ViewModel</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyViewModel</span>(<span class="keyword">private</span> <span class="keyword">val</span> initialCounter: <span class="built_in">Int</span>) : ViewModel() &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> _counter = MutableLiveData&lt;<span class="built_in">Int</span>&gt;()</span><br><span class="line">    <span class="keyword">val</span> counter: LiveData&lt;<span class="built_in">Int</span>&gt; <span class="keyword">get</span>() = _counter</span><br><span class="line"></span><br><span class="line">    <span class="keyword">init</span> &#123;</span><br><span class="line">        _counter.value = initialCounter</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">incrementCounter</span><span class="params">()</span></span> &#123;</span><br><span class="line">        _counter.value = (_counter.value ?: <span class="number">0</span>) + <span class="number">1</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ViewModelProvider.Factory 实现</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyViewModelFactory</span>(<span class="keyword">private</span> <span class="keyword">val</span> initialCounter: <span class="built_in">Int</span>) : ViewModelProvider.Factory &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="type">&lt;T : ViewModel?&gt;</span> <span class="title">create</span><span class="params">(modelClass: <span class="type">Class</span>&lt;<span class="type">T</span>&gt;)</span></span>: T &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">if</span> (modelClass.isAssignableFrom(MyViewModel::<span class="keyword">class</span>.java)) &#123;</span><br><span class="line">            MyViewModel(initialCounter) <span class="keyword">as</span> T</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">throw</span> IllegalArgumentException(<span class="string">&quot;Unknown ViewModel class&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>在 Activity 中传递参数：</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> myViewModel: MyViewModel</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 使用自定义的 ViewModelProvider.Factory 来传递参数</span></span><br><span class="line">        <span class="keyword">val</span> factory = MyViewModelFactory(<span class="number">10</span>)  <span class="comment">// 传递初始计数器值</span></span><br><span class="line">        myViewModel = ViewModelProvider(<span class="keyword">this</span>, factory).<span class="keyword">get</span>(MyViewModel::<span class="keyword">class</span>.java)</span><br><span class="line"></span><br><span class="line">        myViewModel.counter.observe(<span class="keyword">this</span>, Observer &#123; count -&gt;</span><br><span class="line">            <span class="comment">// 更新 UI</span></span><br><span class="line">            counterText.text = count.toString()</span><br><span class="line">        &#125;)</span><br><span class="line"></span><br><span class="line">        incrementButton.setOnClickListener &#123;</span><br><span class="line">            myViewModel.incrementCounter()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>ViewModel</strong> 通过持有 UI 相关的数据并且与 Activity/Fragment 的生命周期分离，帮助开发者管理数据和 UI 更新。</li><li>使用 <code>LiveData</code> 来响应数据变化并更新 UI，是与 ViewModel 配合的常见做法。</li><li>ViewModel 能够在配置变化时保存数据，避免了因 Activity 或 Fragment 被销毁而重新加载数据的性能问题。</li></ul><h3 id="livedata"><a class="markdownIt-Anchor" href="#livedata"></a> **LiveData **</h3><p><strong>LiveData</strong> 是 Jetpack 中的一个数据持有类，它是一个生命周期感知的数据容器。LiveData 允许你将数据与生命周期相关联，只有在相关组件（如 Activity 或 Fragment）处于活跃状态时，数据才会被推送给观察者。这使得 LiveData 非常适用于更新 UI 数据，因为它能够自动处理生命周期变化（如屏幕旋转）时的 UI 更新。</p><ul><li><strong>生命周期感知</strong>：LiveData 会在与其绑定的组件（如 Activity 或 Fragment）处于活跃状态时发送数据更新。它自动管理观察者的生命周期，无需手动移除观察者。</li><li><strong>数据驱动 UI</strong>：UI 会自动根据 LiveData 中数据的变化进行更新。</li><li><strong>支持异步数据流</strong>：LiveData 非常适合处理从后台线程传来的数据，可以确保 UI 只在适当的时机更新。</li></ul><h4 id="1-添加依赖-2"><a class="markdownIt-Anchor" href="#1-添加依赖-2"></a> 1. <strong>添加依赖</strong></h4><p>首先，确保你的项目中已加入 LiveData 所需的依赖。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">implementation &quot;androidx.lifecycle:lifecycle-livedata-ktx:2.5.1&quot;</span><br></pre></td></tr></table></figure><h4 id="2-创建-livedata"><a class="markdownIt-Anchor" href="#2-创建-livedata"></a> 2. <strong>创建 LiveData</strong></h4><p>LiveData 通常作为 ViewModel 的一部分来使用，来持有 UI 相关的数据并与 Activity 或 Fragment 交互。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyViewModel</span> : <span class="type">ViewModel</span>() &#123;</span><br><span class="line">    <span class="comment">// 创建 LiveData</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> _counter = MutableLiveData&lt;<span class="built_in">Int</span>&gt;()</span><br><span class="line">    <span class="keyword">val</span> counter: LiveData&lt;<span class="built_in">Int</span>&gt; <span class="keyword">get</span>() = _counter</span><br><span class="line"></span><br><span class="line">    <span class="keyword">init</span> &#123;</span><br><span class="line">        _counter.value = <span class="number">0</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 更新 LiveData 数据</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">incrementCounter</span><span class="params">()</span></span> &#123;</span><br><span class="line">        _counter.value = (_counter.value ?: <span class="number">0</span>) + <span class="number">1</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>MutableLiveData</strong>: 可变的 LiveData 类型，允许数据更新。</li><li><strong>LiveData</strong>: 不可变的 LiveData 类型，只能观察数据，不能直接修改数据。</li></ul><h4 id="3-在-activity-或-fragment-中观察-livedata"><a class="markdownIt-Anchor" href="#3-在-activity-或-fragment-中观察-livedata"></a> 3. <strong>在 Activity 或 Fragment 中观察 LiveData</strong></h4><p>在 Activity 或 Fragment 中，你可以通过 <code>observe()</code> 方法观察 LiveData。只有当观察者处于活跃状态时，LiveData 才会发送更新。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取 ViewModel 实例</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> myViewModel: MyViewModel <span class="keyword">by</span> viewModels()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 观察 LiveData 数据</span></span><br><span class="line">        myViewModel.counter.observe(<span class="keyword">this</span>, Observer &#123; count -&gt;</span><br><span class="line">            <span class="comment">// 更新 UI</span></span><br><span class="line">            counterText.text = count.toString()</span><br><span class="line">        &#125;)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置按钮点击事件</span></span><br><span class="line">        incrementButton.setOnClickListener &#123;</span><br><span class="line">            myViewModel.incrementCounter()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中，<code>observe()</code> 会监听 <code>counter</code> 的变化，并在数据发生变化时更新 UI。<code>Observer</code> 会在 ViewModel 的 LiveData 数据更新时自动收到通知。</p><h4 id="4-livedata-的生命周期感知"><a class="markdownIt-Anchor" href="#4-livedata-的生命周期感知"></a> 4. <strong>LiveData 的生命周期感知</strong></h4><p>LiveData 是生命周期感知的，这意味着它会在组件的生命周期内自动管理观察者的注册和注销。例如，如果 Activity 被销毁，LiveData 会自动停止发送数据更新，避免内存泄漏。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">myViewModel.counter.observe(<span class="keyword">this</span>, Observer &#123; count -&gt;</span><br><span class="line">    <span class="comment">// 数据变化时更新 UI</span></span><br><span class="line">    counterText.text = count.toString()</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><ul><li><strong>当 Activity 进入前台时</strong>，LiveData 会开始推送数据。</li><li><strong>当 Activity 离开前台时</strong>，LiveData 会停止推送数据，防止不必要的资源消耗。</li></ul><h4 id="5-从后台线程更新-livedata"><a class="markdownIt-Anchor" href="#5-从后台线程更新-livedata"></a> 5. <strong>从后台线程更新 LiveData</strong></h4><p>LiveData 也支持从后台线程更新数据，但你需要确保在主线程中更新 LiveData。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyViewModel</span> : <span class="type">ViewModel</span>() &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> _counter = MutableLiveData&lt;<span class="built_in">Int</span>&gt;()</span><br><span class="line">    <span class="keyword">val</span> counter: LiveData&lt;<span class="built_in">Int</span>&gt; <span class="keyword">get</span>() = _counter</span><br><span class="line"></span><br><span class="line">    <span class="keyword">init</span> &#123;</span><br><span class="line">        _counter.value = <span class="number">0</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">incrementCounter</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="comment">// 更新 UI 数据需要运行在主线程</span></span><br><span class="line">        _counter.postValue((_counter.value ?: <span class="number">0</span>) + <span class="number">1</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><code>setValue()</code>：只能在主线程中调用，用于更新 LiveData。</li><li><code>postValue()</code>：可以在后台线程中调用，用于更新 LiveData，LiveData 会在主线程中自动处理更新。</li></ul><hr /><ul><li><strong>LiveData</strong> 是一个生命周期感知的数据持有类，可以帮助开发者在配置变化时管理 UI 数据。</li><li><strong>MutableLiveData</strong> 用于存储和修改数据，<strong>LiveData</strong> 用于观察和展示数据。</li><li>LiveData 会自动管理观察者的生命周期，无需手动移除观察者。</li><li>可以通过 <code>postValue()</code> 和 <code>setValue()</code> 方法来更新 LiveData 的数据，其中 <code>setValue()</code> 只能在主线程中调用，<code>postValue()</code> 可以在后台线程中调用。</li></ul><h3 id="map-和-switchmap-介绍及用法"><a class="markdownIt-Anchor" href="#map-和-switchmap-介绍及用法"></a> <strong>map 和 switchMap 介绍及用法</strong></h3><p>在 Jetpack 中，<code>map</code> 和 <code>switchMap</code> 是两个常用的转换操作符，通常与 <strong>LiveData</strong> 或 <strong>Flow</strong> 等响应式数据流一起使用。它们用于将一个数据流的输出转换成另一个形式的输出。理解这两个操作符的区别和应用场景对于有效地管理数据流非常重要。</p><h4 id="map-操作符"><a class="markdownIt-Anchor" href="#map-操作符"></a> <strong>map 操作符</strong></h4><p><code>map</code> 是一种转换操作符，用于将一个数据流（如 <code>LiveData</code>）中的值转换为另一个数据类型。它不会改变数据流的生命周期，它只是对数据进行映射，返回一个新的 <code>LiveData</code> 或其他数据流。</p><p><strong>特点</strong>:</p><ul><li>适用于需要对现有数据进行转换的场景。</li><li>保持原有数据流的生命周期，不会触发新的数据流。</li></ul><h5 id="使用场景"><a class="markdownIt-Anchor" href="#使用场景"></a> <strong>使用场景</strong>:</h5><ul><li>将 API 返回的数据转换为 UI 需要的格式。</li><li>将单一数据类型转换为其他类型（如从字符串转换为数字）。</li><li>储存缓存数据。</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyViewModel</span> : <span class="type">ViewModel</span>() &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> _userName = MutableLiveData&lt;String&gt;()</span><br><span class="line">    <span class="keyword">val</span> userName: LiveData&lt;String&gt; <span class="keyword">get</span>() = _userName</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用 map 将原始数据转换为需要的格式</span></span><br><span class="line">    <span class="keyword">val</span> userGreeting: LiveData&lt;String&gt; = userName.map &#123; name -&gt;</span><br><span class="line">        <span class="string">&quot;Hello, <span class="variable">$name</span>&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">updateUserName</span><span class="params">(name: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        _userName.value = name</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>说明</strong>:</p><ul><li><code>map</code> 操作符将 <code>userName</code> 的值（字符串类型）转换为一个问候语（例如 “Hello, John”）。</li><li><code>userGreeting</code> 是 <code>userName</code> 数据的转换结果，当 <code>userName</code> 改变时，<code>userGreeting</code> 会随之更新。</li></ul><h4 id="switchmap-操作符"><a class="markdownIt-Anchor" href="#switchmap-操作符"></a> <strong>switchMap 操作符</strong></h4><p><code>switchMap</code> 也是一种转换操作符，它类似于 <code>map</code>，但有一个重要的区别：<code>switchMap</code> 会根据新的数据值切换到一个新的数据流。当源数据改变时，它会取消之前的数据流，重新发起一个新的数据请求。</p><p><strong>特点</strong>:</p><ul><li>每次输入数据改变时，<code>switchMap</code> 都会取消之前的工作并发起新的工作。</li><li>适用于需要根据某个数据触发新的网络请求或数据库查询的场景。</li><li>处理异步操作时，<code>switchMap</code> 非常有用，它保证了只有最后一个请求的结果会被消费。</li></ul><h5 id="使用场景-2"><a class="markdownIt-Anchor" href="#使用场景-2"></a> <strong>使用场景</strong>:</h5><ul><li>需要根据输入数据动态发起网络请求或数据库查询，并且只关心最新的结果。</li><li>用于防止过时的数据影响 UI，例如输入框搜索时，只显示用户最终输入的结果。</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyViewModel</span> : <span class="type">ViewModel</span>() &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> _searchQuery = MutableLiveData&lt;String&gt;()</span><br><span class="line">    <span class="keyword">val</span> searchQuery: LiveData&lt;String&gt; <span class="keyword">get</span>() = _searchQuery</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用 switchMap 来根据 searchQuery 触发新的搜索请求</span></span><br><span class="line">    <span class="keyword">val</span> searchResults: LiveData&lt;List&lt;String&gt;&gt; = searchQuery.switchMap &#123; query -&gt;</span><br><span class="line">        <span class="comment">// 假设搜索是一个异步操作，这里模拟为一个简单的列表</span></span><br><span class="line">        MutableLiveData&lt;List&lt;String&gt;&gt;().apply &#123;</span><br><span class="line">            value = listOf(<span class="string">&quot;Result for <span class="variable">$query</span>&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">updateSearchQuery</span><span class="params">(query: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        _searchQuery.value = query</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>说明</strong>:</p><ul><li><code>searchQuery</code> 是一个包含搜索关键字的 LiveData。</li><li><code>switchMap</code> 将每次更新的 <code>searchQuery</code> 映射到一个新的 LiveData（这里模拟为搜索结果）。</li><li>当 <code>searchQuery</code> 更新时，<code>switchMap</code> 会取消之前的查询并触发新的查询。</li></ul><h4 id="map-vs-switchmap"><a class="markdownIt-Anchor" href="#map-vs-switchmap"></a> <strong>map vs switchMap</strong></h4><table><thead><tr><th><strong>特性</strong></th><th><strong>map</strong></th><th><strong>switchMap</strong></th></tr></thead><tbody><tr><td><strong>数据流转换</strong></td><td>对数据进行简单的转换，保持原数据流的生命周期。</td><td>根据新的数据发起新的数据流，取消先前的数据流。</td></tr><tr><td><strong>数据更新</strong></td><td>更新数据时，转换结果会随之更新。</td><td>只会关注最新的输入数据，之前的请求会被取消。</td></tr><tr><td><strong>适用场景</strong></td><td>用于简单的计算和数据转换。</td><td>用于根据数据发起新的异步操作（例如网络请求、数据库查询）。</td></tr></tbody></table><ul><li><strong>map</strong> 是用于将 LiveData 中的数据进行转换的操作符，它不会改变数据流的生命周期，只是对数据进行简单的映射。</li><li><strong>switchMap</strong> 是用于处理数据流转换的操作符，特别适用于处理异步请求或网络操作，它会根据新的输入数据切换到新的数据流，并取消先前的操作。</li></ul><h3 id="room"><a class="markdownIt-Anchor" href="#room"></a> <strong>Room</strong></h3><p><strong>Room</strong> 是 Jetpack 提供的一个数据库库，简化了 SQLite 的操作。它提供了一个抽象层，允许开发者更方便、更安全地访问本地数据库。Room 提供了注解（annotation）支持，结合 Kotlin 的数据类和架构组件，能够更高效地管理数据持久化操作。</p><p>Room 提供了一个 <strong>DAO（数据访问对象）</strong> 层来进行数据库操作，DAO 使用 SQL 查询语句来与数据库交互。Room 会在编译时生成实际的实现代码，减少了手动编写 SQL 语句的繁琐。</p><ul><li><strong>简单易用</strong>：相比直接使用 SQLite，Room 提供了更简单、类型安全的 API。</li><li><strong>SQLite 完全支持</strong>：它是基于 SQLite 的，可以执行 SQL 查询、插入、更新和删除操作。</li><li><strong>注解支持</strong>：使用注解定义数据库实体、DAO 和数据库版本。</li><li><strong>与 LiveData 兼容</strong>：Room 支持与 LiveData 结合使用，当数据改变时，Room 可以自动通知观察者。</li></ul><h4 id="1-添加依赖-3"><a class="markdownIt-Anchor" href="#1-添加依赖-3"></a> 1. <strong>添加依赖</strong></h4><p>在 <code>build.gradle</code> 文件中添加 Room 的依赖：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">plugins &#123;</span><br><span class="line">    alias(libs.plugins.android.application)</span><br><span class="line">    alias(libs.plugins.kotlin.android)</span><br><span class="line">    kotlin(&quot;kapt&quot;)  // 添加 KAPT 插件</span><br><span class="line">&#125;</span><br><span class="line">    implementation(&quot;androidx.room:room-runtime:2.6.1&quot;)  // Room 的运行时库</span><br><span class="line">    kapt(&quot;androidx.room:room-compiler:2.6.1&quot;)  // Room 的编译器，使用 KAPT 注解处理</span><br><span class="line">    implementation(&quot;androidx.room:room-ktx:2.6.1&quot;)  // Room 的扩展库，提供了一些扩展函数和属性.</span><br></pre></td></tr></table></figure><h4 id="2-定义实体类-entity"><a class="markdownIt-Anchor" href="#2-定义实体类-entity"></a> 2. <strong>定义实体类 (Entity)</strong></h4><p>Room 使用 <strong>实体类 (Entity)</strong> 来表示数据库表。实体类中的每个字段都会映射到数据库中的列。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Entity(tableName = <span class="string">&quot;users&quot;</span>)</span></span><br><span class="line"><span class="keyword">data</span> <span class="keyword">class</span> <span class="title class_">User</span>(</span><br><span class="line">    <span class="meta">@PrimaryKey(autoGenerate = true)</span> <span class="keyword">val</span> id: <span class="built_in">Int</span> = <span class="number">0</span>,</span><br><span class="line">    <span class="keyword">val</span> name: String,</span><br><span class="line">    <span class="keyword">val</span> age: <span class="built_in">Int</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure><ul><li><strong>@Entity</strong> 注解标识这个类是一个数据库表。</li><li><strong>@PrimaryKey</strong> 注解指定主键，并可以设置 <code>autoGenerate = true</code> 让 Room 自动生成主键。</li></ul><h4 id="3-定义-dao数据访问对象"><a class="markdownIt-Anchor" href="#3-定义-dao数据访问对象"></a> 3. <strong>定义 DAO（数据访问对象）</strong></h4><p><strong>DAO</strong> 是与数据库交互的接口。通过 DAO 定义数据库操作（增、查、改、删）。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Dao</span></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">UserDao</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Insert</span></span><br><span class="line">    <span class="keyword">suspend</span> <span class="function"><span class="keyword">fun</span> <span class="title">insertUser</span><span class="params">(user: <span class="type">User</span>)</span></span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Query(<span class="string">&quot;SELECT * FROM users WHERE id = :id&quot;</span>)</span></span><br><span class="line">    <span class="keyword">suspend</span> <span class="function"><span class="keyword">fun</span> <span class="title">getUserById</span><span class="params">(id: <span class="type">Int</span>)</span></span>: User?</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Query(<span class="string">&quot;SELECT * FROM users&quot;</span>)</span></span><br><span class="line">    <span class="keyword">suspend</span> <span class="function"><span class="keyword">fun</span> <span class="title">getAllUsers</span><span class="params">()</span></span>: List&lt;User&gt;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Query(<span class="string">&quot;DELETE FROM users&quot;</span>)</span></span><br><span class="line">    <span class="keyword">suspend</span> <span class="function"><span class="keyword">fun</span> <span class="title">deleteAllUsers</span><span class="params">()</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>@Dao</strong> 注解标识这个接口是 DAO。</li><li><strong>@Insert</strong> 注解用于插入数据。</li><li><strong>@Query</strong> 注解定义 SQL 查询语句。</li></ul><h4 id="4-定义数据库-database"><a class="markdownIt-Anchor" href="#4-定义数据库-database"></a> 4. <strong>定义数据库 (Database)</strong></h4><p><strong>RoomDatabase</strong> 是抽象类，用于创建数据库实例并提供对 DAO 的访问。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Database(entities = [User::class], version = 1)</span></span><br><span class="line"><span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">AppDatabase</span> : <span class="type">RoomDatabase</span>() &#123;</span><br><span class="line">    <span class="keyword">abstract</span> <span class="function"><span class="keyword">fun</span> <span class="title">userDao</span><span class="params">()</span></span>: UserDao</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>@Database</strong> 注解用于定义数据库，并指定包含的实体类和数据库版本。</li><li><code>abstract fun userDao()</code> 方法提供对 DAO 的访问。</li></ul><h4 id="5-获取数据库实例"><a class="markdownIt-Anchor" href="#5-获取数据库实例"></a> 5. <strong>获取数据库实例</strong></h4><p>可以通过 <code>Room.databaseBuilder()</code> 方法获取数据库实例。在 <code>Activity</code> 或 <code>Application</code> 中进行初始化。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> db = Room.databaseBuilder(applicationContext, AppDatabase::<span class="keyword">class</span>.java, <span class="string">&quot;app_database&quot;</span>)</span><br><span class="line">    .fallbackToDestructiveMigration() <span class="comment">// 如果数据库版本不一致时进行迁移</span></span><br><span class="line">    .build()</span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> userDao = db.userDao()</span><br></pre></td></tr></table></figure><h4 id="6-在-viewmodel-中使用-room-和-livedata"><a class="markdownIt-Anchor" href="#6-在-viewmodel-中使用-room-和-livedata"></a> 6. <strong>在 ViewModel 中使用 Room 和 LiveData</strong></h4><p>Room 可以与 LiveData 和 ViewModel 结合使用，确保数据在界面更新时保持一致性，并自动处理生命周期。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">UserViewModel</span>(application: Application) : AndroidViewModel(application) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> db = Room.databaseBuilder(application, AppDatabase::<span class="keyword">class</span>.java, <span class="string">&quot;app_database&quot;</span>).build()</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> userDao = db.userDao()</span><br><span class="line"></span><br><span class="line">    <span class="comment">// LiveData 用于观察数据库中的数据</span></span><br><span class="line">    <span class="keyword">val</span> allUsers: LiveData&lt;List&lt;User&gt;&gt; = userDao.getAllUsers()</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 插入数据</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">insertUser</span><span class="params">(user: <span class="type">User</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 使用协程执行插入操作</span></span><br><span class="line">        viewModelScope.launch &#123;</span><br><span class="line">            userDao.insertUser(user)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中，<code>allUsers</code> 是一个 LiveData 类型的数据流，当数据库中的用户数据改变时，UI 会自动更新。</p><h4 id="7-在-activity-或-fragment-中使用-viewmodel"><a class="markdownIt-Anchor" href="#7-在-activity-或-fragment-中使用-viewmodel"></a> 7. <strong>在 Activity 或 Fragment 中使用 ViewModel</strong></h4><p>在 Activity 或 Fragment 中，使用 ViewModel 来获取数据并更新 UI。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> userViewModel: UserViewModel <span class="keyword">by</span> viewModels()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 观察数据库中的用户数据</span></span><br><span class="line">        userViewModel.allUsers.observe(<span class="keyword">this</span>, Observer &#123; users -&gt;</span><br><span class="line">            <span class="comment">// 更新 UI</span></span><br><span class="line">            userListAdapter.submitList(users)</span><br><span class="line">        &#125;)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 添加新用户</span></span><br><span class="line">        addButton.setOnClickListener &#123;</span><br><span class="line">            <span class="keyword">val</span> user = User(name = <span class="string">&quot;John&quot;</span>, age = <span class="number">25</span>)</span><br><span class="line">            userViewModel.insertUser(user)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="8-数据库迁移"><a class="markdownIt-Anchor" href="#8-数据库迁移"></a> 8. <strong>数据库迁移</strong></h4><p>当你修改数据库结构时（如添加新的表或列），Room 提供了数据库迁移的机制。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Database(entities = [User::class], version = 2)</span></span><br><span class="line"><span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">AppDatabase</span> : <span class="type">RoomDatabase</span>() &#123;</span><br><span class="line">    <span class="keyword">abstract</span> <span class="function"><span class="keyword">fun</span> <span class="title">userDao</span><span class="params">()</span></span>: UserDao</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义迁移策略</span></span><br><span class="line"><span class="keyword">val</span> MIGRATION_1_2 = <span class="keyword">object</span> : Migration(<span class="number">1</span>, <span class="number">2</span>) &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">migrate</span><span class="params">(database: <span class="type">SupportSQLiteDatabase</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 在迁移时执行 SQL 操作，例如添加新的列</span></span><br><span class="line">        database.execSQL(<span class="string">&quot;ALTER TABLE users ADD COLUMN email TEXT&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用数据库迁移</span></span><br><span class="line"><span class="keyword">val</span> db = Room.databaseBuilder(applicationContext, AppDatabase::<span class="keyword">class</span>.java, <span class="string">&quot;app_database&quot;</span>)</span><br><span class="line">    .addMigrations(MIGRATION_1_2)</span><br><span class="line">    .build()</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Database(version = 3, entities = [User::class, Book::class])</span></span><br><span class="line"><span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">AppDatabase</span> : <span class="type">RoomDatabase</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * UserDao 是用于操作 User 实体的 DAO（Data Access Object）。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">abstract</span> <span class="function"><span class="keyword">fun</span> <span class="title">userDao</span><span class="params">()</span></span>: UserDao</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * BookDao 是用于操作 Book 实体的 DAO（Data Access Object）。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">abstract</span> <span class="function"><span class="keyword">fun</span> <span class="title">bookDao</span><span class="params">()</span></span>: BookDao</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Companion 对象用于存储数据库的迁移信息和实例。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">companion</span> <span class="keyword">object</span> &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * MIGRATION_1_2 是从数据库版本 1 到 2 的迁移。</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">val</span> MIGRATION_1_2 = <span class="keyword">object</span> : Migration(<span class="number">1</span>, <span class="number">2</span>) &#123;</span><br><span class="line">            <span class="comment">/**</span></span><br><span class="line"><span class="comment">             * migrate 方法用于执行从版本 1 到 2 的迁移。</span></span><br><span class="line"><span class="comment">             * 在这个例子中，我们创建了一个名为 Book 的新表。</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">            <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">migrate</span><span class="params">(database: <span class="type">SupportSQLiteDatabase</span>)</span></span> &#123;</span><br><span class="line">                database.execSQL(<span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">                    CREATE TABLE Book (</span></span><br><span class="line"><span class="string">                        id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,</span></span><br><span class="line"><span class="string">                        name TEXT NOT NULL,</span></span><br><span class="line"><span class="string">                        pages INTEGER NOT NULL,</span></span><br><span class="line"><span class="string">                        author TEXT NOT NULL</span></span><br><span class="line"><span class="string">                    )</span></span><br><span class="line"><span class="string">                &quot;&quot;&quot;</span>.trimIndent())</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * MIGRATION_2_3 是从数据库版本 2 到 3 的迁移。</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">val</span> MIGRATION_2_3 = <span class="keyword">object</span> : Migration(<span class="number">2</span>, <span class="number">3</span>) &#123;</span><br><span class="line">            <span class="comment">/**</span></span><br><span class="line"><span class="comment">             * migrate 方法用于执行从版本 2 到 3 的迁移。</span></span><br><span class="line"><span class="comment">             * 在这个例子中，我们添加了一个名为 author 的新列到 User 表中。</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">            <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">migrate</span><span class="params">(database: <span class="type">SupportSQLiteDatabase</span>)</span></span> &#123;</span><br><span class="line">                database.execSQL(<span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">                    ALTER TABLE User ADD COLUMN </span></span><br><span class="line"><span class="string">                    author TEXT NOT NULL DEFAULT &#x27;unknown&#x27;</span></span><br><span class="line"><span class="string">                    &quot;&quot;&quot;</span>)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * instance 是 AppDatabase 的实例。</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">var</span> instance: AppDatabase? = <span class="literal">null</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * getDatabase 方法用于获取 AppDatabase 的实例。</span></span><br><span class="line"><span class="comment">         * 如果实例已经存在，则直接返回实例。</span></span><br><span class="line"><span class="comment">         * 否则，则创建一个新的实例并返回。</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="meta">@Synchronized</span></span><br><span class="line">        <span class="function"><span class="keyword">fun</span> <span class="title">getDatabase</span><span class="params">(context: <span class="type">Context</span>)</span></span>: AppDatabase &#123;</span><br><span class="line">            instance?.let &#123; <span class="keyword">return</span> it &#125;</span><br><span class="line">            <span class="keyword">return</span> Room.databaseBuilder(</span><br><span class="line">                context.applicationContext,</span><br><span class="line">                AppDatabase::<span class="keyword">class</span>.java,</span><br><span class="line">                <span class="string">&quot;app_database&quot;</span></span><br><span class="line">            )</span><br><span class="line">                .addMigrations(MIGRATION_1_2, MIGRATION_2_3)</span><br><span class="line">                .build().apply &#123; instance = <span class="keyword">this</span> &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><ul><li><strong>Room</strong> 是一个简化数据库操作的库，提供了更简洁、安全的方式来访问 SQLite 数据库。</li><li><strong>Entity</strong> 用于定义数据库表，<strong>DAO</strong> 用于定义操作数据库的方法，<strong>RoomDatabase</strong> 用于创建数据库实例。</li><li>使用 <strong>LiveData</strong> 和 <strong>ViewModel</strong> 可以让 Room 的数据与 UI 组件轻松集成，确保数据在生命周期变化时的一致性。</li><li>Room 支持数据库迁移，确保在数据库结构变化时不会丢失数据。</li></ul><h3 id="workmanager"><a class="markdownIt-Anchor" href="#workmanager"></a> **WorkManager **</h3><p><strong>WorkManager</strong> 是 Android Jetpack 提供的一个库，用于管理后台任务，尤其适用于需要保证任务执行的场景，如定期任务、长时间运行的任务或设备重启后的任务。它能够自动处理任务的执行、失败重试、网络条件以及设备充电状态等复杂的任务执行策略。</p><p>WorkManager 适用于那些需要确保任务即使在应用关闭、设备重启后仍然能够执行的场景。它通过任务链的方式，提供了更高效、更灵活的后台任务管理方案。</p><ul><li><strong>可靠性</strong>：WorkManager 保证任务会在适当的时候执行，即使应用关闭、设备重启，或者处于低电量状态。</li><li><strong>条件控制</strong>：可以控制任务的执行条件，如网络连接状态、电池状态等。</li><li><strong>任务重试机制</strong>：如果任务失败，可以设置重试策略。</li><li><strong>兼容性</strong>：WorkManager 可以在 API 级别 14 及以上的设备上运行。</li></ul><hr /><h4 id="1-添加依赖-4"><a class="markdownIt-Anchor" href="#1-添加依赖-4"></a> 1. <strong>添加依赖</strong></h4><p>首先，需要在 <code>build.gradle</code> 文件中添加 WorkManager 的依赖：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">implementation &quot;androidx.work:work-runtime-ktx:2.7.1&quot;</span><br></pre></td></tr></table></figure><h4 id="2-创建-worker-类"><a class="markdownIt-Anchor" href="#2-创建-worker-类"></a> 2. <strong>创建 Worker 类</strong></h4><p><code>Worker</code> 是 WorkManager 中用来执行任务的核心类。你需要创建一个继承自 <code>Worker</code> 或 <code>CoroutineWorker</code> 的类，并重写 <code>doWork()</code> 方法来定义任务的具体操作。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyWorker</span>(appContext: Context, workerParams: WorkerParameters) : Worker(appContext, workerParams) &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">doWork</span><span class="params">()</span></span>: Result &#123;</span><br><span class="line">        <span class="comment">// 任务执行的具体操作</span></span><br><span class="line">        Log.d(<span class="string">&quot;WorkManager&quot;</span>, <span class="string">&quot;Task is running!&quot;</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 返回任务的执行结果</span></span><br><span class="line">        <span class="keyword">return</span> Result.success() <span class="comment">// 表示任务成功完成</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>doWork()</strong>：在这个方法中定义具体的任务逻辑，任务完成后返回一个 <code>Result</code> 对象，表示任务是成功还是失败。<ul><li><code>Result.success()</code>：任务成功完成</li><li><code>Result.failure()</code>：任务失败</li><li><code>Result.retry()</code>：任务需要重试</li></ul></li></ul><h4 id="3-创建-workrequest"><a class="markdownIt-Anchor" href="#3-创建-workrequest"></a> 3. <strong>创建 WorkRequest</strong></h4><p><code>WorkRequest</code> 用于定义任务的执行计划。在 WorkManager 中，可以使用 <code>OneTimeWorkRequest</code> 来执行一次性任务，或使用 <code>PeriodicWorkRequest</code> 来执行定期任务。</p><h5 id="onetimeworkrequest-示例"><a class="markdownIt-Anchor" href="#onetimeworkrequest-示例"></a> <strong>OneTimeWorkRequest</strong> 示例：</h5><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> workRequest = OneTimeWorkRequest.Builder(MyWorker::<span class="keyword">class</span>.java)</span><br><span class="line">    .build()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动任务</span></span><br><span class="line">WorkManager.getInstance(applicationContext).enqueue(workRequest)</span><br></pre></td></tr></table></figure><h5 id="periodicworkrequest-示例定期执行任务"><a class="markdownIt-Anchor" href="#periodicworkrequest-示例定期执行任务"></a> <strong>PeriodicWorkRequest</strong> 示例（定期执行任务）：</h5><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> periodicWorkRequest = PeriodicWorkRequest.Builder(MyWorker::<span class="keyword">class</span>.java, <span class="number">1</span>, TimeUnit.HOURS)</span><br><span class="line">    .build()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动定期任务</span></span><br><span class="line">WorkManager.getInstance(applicationContext).enqueue(periodicWorkRequest)</span><br></pre></td></tr></table></figure><ul><li><strong>OneTimeWorkRequest</strong>：一次性任务，适合执行单次的后台工作。</li><li><strong>PeriodicWorkRequest</strong>：定期任务，适合执行周期性重复的任务，如每隔一段时间执行某个任务。</li></ul><h4 id="4-设置任务约束constraints"><a class="markdownIt-Anchor" href="#4-设置任务约束constraints"></a> 4. <strong>设置任务约束（Constraints）</strong></h4><p>你可以为任务设置约束条件，例如要求任务只有在网络连接、设备充电等情况下执行。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> constraints = Constraints.Builder()</span><br><span class="line">    .setRequiredNetworkType(NetworkType.CONNECTED) <span class="comment">// 需要联网</span></span><br><span class="line">    .setRequiresBatteryNotLow(<span class="literal">true</span>) <span class="comment">// 设备电池需要有足够电量</span></span><br><span class="line">    .build()</span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> workRequest = OneTimeWorkRequest.Builder(MyWorker::<span class="keyword">class</span>.java)</span><br><span class="line">    .setConstraints(constraints)</span><br><span class="line">    .build()</span><br><span class="line"></span><br><span class="line"><span class="comment">// 启动任务</span></span><br><span class="line">WorkManager.getInstance(applicationContext).enqueue(workRequest)</span><br></pre></td></tr></table></figure><h4 id="5-观察任务状态"><a class="markdownIt-Anchor" href="#5-观察任务状态"></a> 5. <strong>观察任务状态</strong></h4><p>你可以使用 <code>LiveData</code> 来观察任务的状态，例如任务是否完成，或者是否失败。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">WorkManager.getInstance(applicationContext)</span><br><span class="line">    .getWorkInfoByIdLiveData(workRequest.id)</span><br><span class="line">    .observe(<span class="keyword">this</span>, Observer &#123; workInfo -&gt;</span><br><span class="line">        <span class="keyword">if</span> (workInfo != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">when</span> (workInfo.state) &#123;</span><br><span class="line">                WorkInfo.State.SUCCEEDED -&gt; &#123;</span><br><span class="line">                    <span class="comment">// 任务成功</span></span><br><span class="line">                &#125;</span><br><span class="line">                WorkInfo.State.FAILED -&gt; &#123;</span><br><span class="line">                    <span class="comment">// 任务失败</span></span><br><span class="line">                &#125;</span><br><span class="line">                WorkInfo.State.RUNNING -&gt; &#123;</span><br><span class="line">                    <span class="comment">// 任务正在运行</span></span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> -&gt; &#123;</span><br><span class="line">                    <span class="comment">// 任务还未开始或已经完成</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)</span><br></pre></td></tr></table></figure><h4 id="6-取消任务"><a class="markdownIt-Anchor" href="#6-取消任务"></a> 6. <strong>取消任务</strong></h4><p>你可以通过 <code>WorkManager</code> 来取消正在执行的任务。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">WorkManager.getInstance(applicationContext).cancelWorkById(workRequest.id)</span><br></pre></td></tr></table></figure><h4 id="7-设置任务重试策略"><a class="markdownIt-Anchor" href="#7-设置任务重试策略"></a> 7. <strong>设置任务重试策略</strong></h4><p>如果任务执行失败，你可以设置任务的重试策略。可以指定最大重试次数和重试间隔。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> workRequest = OneTimeWorkRequest.Builder(MyWorker::<span class="keyword">class</span>.java)</span><br><span class="line">    .setBackoffCriteria(BackoffPolicy.EXPONENTIAL, <span class="number">10</span>, TimeUnit.SECONDS)</span><br><span class="line">    .build()</span><br><span class="line"></span><br><span class="line">WorkManager.getInstance(applicationContext).enqueue(workRequest)</span><br></pre></td></tr></table></figure><ul><li><strong>BackoffPolicy.EXPONENTIAL</strong>：指数回退，表示任务失败后，重试的间隔会逐渐增加。</li><li><strong>BackoffPolicy.LINEAR</strong>：线性回退，表示任务失败后，重试的间隔是固定的。</li></ul><h4 id="8-链式任务"><a class="markdownIt-Anchor" href="#8-链式任务"></a> 8. <strong>链式任务</strong></h4><p>你可以将多个任务按顺序串联执行。<code>WorkManager</code> 提供了 <code>beginWith()</code> 和 <code>then()</code> 方法来执行链式任务。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> firstRequest = OneTimeWorkRequest.Builder(MyWorker::<span class="keyword">class</span>.java).build()</span><br><span class="line"><span class="keyword">val</span> secondRequest = OneTimeWorkRequest.Builder(SecondWorker::<span class="keyword">class</span>.java).build()</span><br><span class="line"></span><br><span class="line">WorkManager.getInstance(applicationContext)</span><br><span class="line">    .beginWith(firstRequest)</span><br><span class="line">    .then(secondRequest)</span><br><span class="line">    .enqueue()</span><br></pre></td></tr></table></figure><hr /><ul><li><strong>WorkManager</strong> 是用于管理后台任务的 Jetpack 库，确保任务能够在设备重启后继续执行，适用于长期任务或需要保证执行的任务。</li><li><strong>Worker</strong> 是执行任务的核心类，重写 <code>doWork()</code> 方法来定义任务的具体操作。</li><li>通过 <strong>WorkRequest</strong> 可以定义任务的执行计划，包括一次性任务和定期任务。</li><li><strong>Constraints</strong> 允许设置任务的执行条件，如网络连接、电池状态等。</li><li>WorkManager 支持任务的重试策略、任务链、任务观察等功能，能够轻松管理复杂的后台任务。</li></ul><hr /><h2 id="10全局获取context的技巧"><a class="markdownIt-Anchor" href="#10全局获取context的技巧"></a> 10.全局获取Context的技巧</h2><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyApplication</span> : <span class="type">Application</span>() &#123;</span><br><span class="line">    <span class="keyword">companion</span> <span class="keyword">object</span> &#123;</span><br><span class="line">        <span class="meta">@SuppressLint(<span class="string">&quot;StaticFieldLeak&quot;</span>)</span></span><br><span class="line">        <span class="keyword">lateinit</span> <span class="keyword">var</span> context: Context</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate()</span><br><span class="line">        context = applicationContext</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">application</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:name</span>=<span class="string">&quot;.MyApplication&quot;</span></span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Toast.makeText(MyApplication.context, <span class="string">&quot;onCreate&quot;</span>, Toast.LENGTH_SHORT).show()</span><br></pre></td></tr></table></figure><hr /><h2 id="11使用intent传递对象"><a class="markdownIt-Anchor" href="#11使用intent传递对象"></a> 11.使用Intent传递对象</h2><h3 id="1-serializable"><a class="markdownIt-Anchor" href="#1-serializable"></a> 1. <strong>Serializable</strong></h3><h4 id="什么是-serializable"><a class="markdownIt-Anchor" href="#什么是-serializable"></a> 什么是 Serializable？</h4><p><code>Serializable</code> 是 Java 提供的一个接口，它标志着一个类可以被序列化，也就是可以将对象转换成字节流（<code>byte[]</code>），从而方便地存储或传输。<code>Serializable</code> 接口没有强制要求实现任何方法，Java 在运行时会自动处理对象的序列化和反序列化。</p><h4 id="如何使用-serializable"><a class="markdownIt-Anchor" href="#如何使用-serializable"></a> 如何使用 Serializable</h4><p>要使用 <code>Serializable</code> 传递对象，首先需要让对象类实现 <code>Serializable</code> 接口。然后，你可以通过 <code>Intent.putExtra()</code> 方法将对象传递给另一个 <code>Activity</code>，在接收方通过 <code>Intent.getSerializableExtra()</code> 来获取该对象。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个可以序列化的对象</span></span><br><span class="line"><span class="keyword">data</span> <span class="keyword">class</span> <span class="title class_">User</span>(<span class="keyword">val</span> name: String, <span class="keyword">val</span> age: <span class="built_in">Int</span>) : Serializable</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在第一个Activity中传递对象</span></span><br><span class="line"><span class="keyword">val</span> user = User(<span class="string">&quot;John&quot;</span>, <span class="number">30</span>)</span><br><span class="line"><span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, SecondActivity::<span class="keyword">class</span>.java)</span><br><span class="line">intent.putExtra(<span class="string">&quot;user_data&quot;</span>, user)</span><br><span class="line">startActivity(intent)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在第二个Activity中接收对象</span></span><br><span class="line"><span class="keyword">val</span> user = intent.getSerializableExtra(<span class="string">&quot;user_data&quot;</span>) <span class="keyword">as</span>? User</span><br></pre></td></tr></table></figure><h4 id="优点与缺点"><a class="markdownIt-Anchor" href="#优点与缺点"></a> 优点与缺点</h4><p><strong>优点：</strong></p><ul><li>实现简单：只需让对象类实现 <code>Serializable</code> 接口，无需额外的操作。</li><li>适用于简单的数据传输。</li></ul><p><strong>缺点：</strong></p><ul><li>性能问题：由于 <code>Serializable</code> 依赖于 Java 的反射机制，序列化和反序列化的过程相对较慢，尤其是当对象较复杂时。</li><li>内存消耗较高：序列化通常会产生较大的字节数组，占用较多内存。</li></ul><h3 id="2-parcelable"><a class="markdownIt-Anchor" href="#2-parcelable"></a> 2. <strong>Parcelable</strong></h3><h4 id="什么是-parcelable"><a class="markdownIt-Anchor" href="#什么是-parcelable"></a> 什么是 Parcelable？</h4><p><code>Parcelable</code> 是 Android 提供的一种对象序列化机制，它与 <code>Serializable</code> 类似，目的是将对象转换为可以通过 <code>Intent</code> 或 <code>Bundle</code> 等传递的字节流，但它的实现更加高效。与 Java 标准的 <code>Serializable</code> 相比，<code>Parcelable</code> 更加高效，因为它不会依赖于反射，使用了特定的内存块和优化的序列化方式。</p><h4 id="如何使用-parcelable"><a class="markdownIt-Anchor" href="#如何使用-parcelable"></a> 如何使用 Parcelable</h4><p>要使用 <code>Parcelable</code> 传递对象，需要让对象类实现 <code>Parcelable</code> 接口，并且重写一些方法（如 <code>writeToParcel()</code> 和 <code>describeContents()</code>）。此外，还需要提供一个 <code>CREATOR</code> 静态字段，用于反序列化对象。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个实现 Parcelable 的对象</span></span><br><span class="line"><span class="keyword">data</span> <span class="keyword">class</span> <span class="title class_">User</span>(<span class="keyword">val</span> name: String, <span class="keyword">val</span> age: <span class="built_in">Int</span>) : Parcelable &#123;</span><br><span class="line">    <span class="keyword">constructor</span>(parcel: Parcel) : <span class="keyword">this</span>(</span><br><span class="line">        parcel.readString() ?: <span class="string">&quot;&quot;</span>,</span><br><span class="line">        parcel.readInt()</span><br><span class="line">    )</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">writeToParcel</span><span class="params">(parcel: <span class="type">Parcel</span>, flags: <span class="type">Int</span>)</span></span> &#123;</span><br><span class="line">        parcel.writeString(name)</span><br><span class="line">        parcel.writeInt(age)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">describeContents</span><span class="params">()</span></span>: <span class="built_in">Int</span> = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">companion</span> <span class="keyword">object</span> &#123;</span><br><span class="line">        <span class="meta">@JvmField</span></span><br><span class="line">        <span class="keyword">val</span> CREATOR: Parcelable.Creator&lt;User&gt; = <span class="keyword">object</span> : Parcelable.Creator&lt;User&gt; &#123;</span><br><span class="line">            <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">createFromParcel</span><span class="params">(parcel: <span class="type">Parcel</span>)</span></span>: User &#123;</span><br><span class="line">                <span class="keyword">return</span> User(parcel)</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">newArray</span><span class="params">(size: <span class="type">Int</span>)</span></span>: Array&lt;User?&gt; &#123;</span><br><span class="line">                <span class="keyword">return</span> arrayOfNulls(size)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在第一个Activity中传递对象</span></span><br><span class="line"><span class="keyword">val</span> user = User(<span class="string">&quot;John&quot;</span>, <span class="number">30</span>)</span><br><span class="line"><span class="keyword">val</span> intent = Intent(<span class="keyword">this</span>, SecondActivity::<span class="keyword">class</span>.java)</span><br><span class="line">intent.putExtra(<span class="string">&quot;user_data&quot;</span>, user)</span><br><span class="line">startActivity(intent)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在第二个Activity中接收对象</span></span><br><span class="line"><span class="keyword">val</span> user = intent.getParcelableExtra&lt;User&gt;(<span class="string">&quot;user_data&quot;</span>)</span><br></pre></td></tr></table></figure><h4 id="优点与缺点-2"><a class="markdownIt-Anchor" href="#优点与缺点-2"></a> 优点与缺点</h4><p><strong>优点：</strong></p><ul><li>性能更高：<code>Parcelable</code> 被设计为更高效，特别是对内存和性能的优化，使得它在 Android 中传递对象时比 <code>Serializable</code> 更有优势。</li><li>可控制性：开发者可以完全控制序列化和反序列化过程，因此可以根据需求进行优化。</li></ul><p><strong>缺点：</strong></p><ul><li>实现复杂：相比于 <code>Serializable</code>，<code>Parcelable</code> 的实现较为繁琐。需要手动写 <code>writeToParcel()</code> 和 <code>createFromParcel()</code> 方法，且必须提供 <code>CREATOR</code> 静态字段。</li><li>容易出错：手动处理的序列化和反序列化代码容易出错，特别是涉及到嵌套对象和数组时。</li></ul><h3 id="3-serializable-与-parcelable-的比较"><a class="markdownIt-Anchor" href="#3-serializable-与-parcelable-的比较"></a> 3. <strong>Serializable 与 Parcelable 的比较</strong></h3><table><thead><tr><th>特性</th><th><code>Serializable</code></th><th><code>Parcelable</code></th></tr></thead><tbody><tr><td><strong>实现复杂度</strong></td><td>简单，只需实现接口</td><td>较复杂，需要手动编写序列化和反序列化代码</td></tr><tr><td><strong>性能</strong></td><td>较低，基于 Java 反射机制，性能较差</td><td>更高效，专为 Android 优化</td></tr><tr><td><strong>内存消耗</strong></td><td>较高，序列化后生成的字节数组较大</td><td>较低，优化了内存和性能</td></tr><tr><td><strong>兼容性</strong></td><td>适用于所有 Java 类</td><td>仅适用于 Android，且需要手动实现</td></tr><tr><td><strong>适用场景</strong></td><td>适用于轻量级的传输和简单对象</td><td>适用于复杂对象、性能敏感的场景</td></tr></tbody></table><hr /><h2 id="12定制自己的日志工具"><a class="markdownIt-Anchor" href="#12定制自己的日志工具"></a> 12.定制自己的日志工具</h2><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">object</span> LogUtil &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">const</span> <span class="keyword">val</span> VERBOSE = <span class="number">1</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">const</span> <span class="keyword">val</span> DEBUG = <span class="number">2</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">const</span> <span class="keyword">val</span> INFO = <span class="number">3</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">const</span> <span class="keyword">val</span> WARN = <span class="number">4</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">const</span> <span class="keyword">val</span> ERROR = <span class="number">5</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">var</span> level = VERBOSE</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">v</span><span class="params">(tag: <span class="type">String</span>, msg: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (level &lt;= VERBOSE) &#123;</span><br><span class="line">            Log.v(tag, msg)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">d</span><span class="params">(tag: <span class="type">String</span>, msg: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (level &lt;= DEBUG) &#123;</span><br><span class="line">            Log.d(tag, msg)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">i</span><span class="params">(tag: <span class="type">String</span>, msg: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (level &lt;= INFO) &#123;</span><br><span class="line">            Log.i(tag, msg)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">w</span><span class="params">(tag: <span class="type">String</span>, msg: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (level &lt;= WARN) &#123;</span><br><span class="line">            Log.w(tag, msg)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">e</span><span class="params">(tag: <span class="type">String</span>, msg: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (level &lt;= ERROR) &#123;</span><br><span class="line">            Log.e(tag, msg)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">LogUtil.d(<span class="string">&quot;TAG&quot;</span>, <span class="string">&quot;debug log&quot;</span>)</span><br></pre></td></tr></table></figure><hr /><h2 id="13深色与浅色主题的切换"><a class="markdownIt-Anchor" href="#13深色与浅色主题的切换"></a> 13.深色与浅色主题的切换</h2><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">toggleTheme</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> currentMode = AppCompatDelegate.getDefaultNightMode()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (currentMode == AppCompatDelegate.MODE_NIGHT_YES) &#123;</span><br><span class="line">        <span class="comment">// 当前是深色主题，切换到浅色主题</span></span><br><span class="line">        AppCompatDelegate.setDefaultNightMode(AppCompatDelegate.MODE_NIGHT_NO)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 当前是浅色主题，切换到深色主题</span></span><br><span class="line">        AppCompatDelegate.setDefaultNightMode(AppCompatDelegate.MODE_NIGHT_YES)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 你可以在这里保存用户的选择，以便下次启动时保持一致</span></span><br><span class="line">    <span class="keyword">val</span> sharedPreferences = getSharedPreferences(<span class="string">&quot;settings&quot;</span>, MODE_PRIVATE)</span><br><span class="line">    <span class="keyword">val</span> editor = sharedPreferences.edit()</span><br><span class="line">    editor.putBoolean(<span class="string">&quot;dark_mode&quot;</span>, currentMode == AppCompatDelegate.MODE_NIGHT_YES)</span><br><span class="line">    editor.apply()</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;kfc疯狂星期四&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#kfc疯狂星期四&quot;&gt;&lt;/a&gt; &lt;strong&gt;KFC疯狂星期四&lt;/strong&gt;&lt;/h1&gt;
&lt;h2 id=&quot;1探究activity&quot;&gt;&lt;a class=&quot;markdownI</summary>
      
    
    
    
    
    <category term="第一行代码" scheme="https://11111-beep.github.io/tags/%E7%AC%AC%E4%B8%80%E8%A1%8C%E4%BB%A3%E7%A0%81/"/>
    
  </entry>
  
  <entry>
    <title>Kotlin语法简要</title>
    <link href="https://11111-beep.github.io/2025/09/17/Kotlin%E8%AF%AD%E6%B3%95%E7%AE%80%E8%A6%81/"/>
    <id>https://11111-beep.github.io/2025/09/17/Kotlin%E8%AF%AD%E6%B3%95%E7%AE%80%E8%A6%81/</id>
    <published>2025-09-17T09:18:30.000Z</published>
    <updated>2025-12-14T09:20:00.307Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1变量"><a class="markdownIt-Anchor" href="#1变量"></a> 1.变量</h2><p><strong>val</strong>:不可变的变量</p><p><strong>var</strong>:可变的变量</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">val</span> a=<span class="number">10</span></span><br><span class="line">    <span class="comment">// var a: Int=10</span></span><br><span class="line">    println(<span class="string">&quot;a=<span class="variable">$a</span>&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="2函数"><a class="markdownIt-Anchor" href="#2函数"></a> 2.函数</h2><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">largerNumber</span><span class="params">(num1: <span class="type">Int</span>, num2: <span class="type">Int</span>)</span></span> = max(num1, num2)</span><br><span class="line"><span class="comment">// 只有一行代码时可以不写函数体,类型推导机制让我们不必声明返回类型</span></span><br></pre></td></tr></table></figure><hr /><h2 id="3函数的执行语句"><a class="markdownIt-Anchor" href="#3函数的执行语句"></a> 3.函数的执行语句</h2><h3 id="if语句"><a class="markdownIt-Anchor" href="#if语句"></a> 🔹 if语句</h3><p>if具有返回值,返回值就是if语句中最后一行代码的返回值</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">largerNumber</span><span class="params">(num1: <span class="type">Int</span>, num2: <span class="type">Int</span>)</span></span> = <span class="keyword">if</span> (num1&gt;num2)num1 <span class="keyword">else</span> num2</span><br></pre></td></tr></table></figure><h3 id="when条件语句类似于switch"><a class="markdownIt-Anchor" href="#when条件语句类似于switch"></a> 🔹 when条件语句(类似于switch)</h3><p>和if一样也是自带返回值</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">getScore</span><span class="params">(name: <span class="type">String</span>)</span></span> = <span class="keyword">when</span> (name) &#123;</span><br><span class="line">    <span class="string">&quot;Tom&quot;</span> -&gt; <span class="number">86</span></span><br><span class="line">    <span class="string">&quot;Jim&quot;</span> -&gt; <span class="number">95</span></span><br><span class="line">    <span class="string">&quot;Lily&quot;</span> -&gt; <span class="number">100</span></span><br><span class="line">    <span class="keyword">else</span> -&gt; <span class="number">0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> num = <span class="number">10L</span> <span class="comment">// Long类型</span></span><br><span class="line">    checkNumber(num)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">checkNumber</span><span class="params">(num: <span class="type">Number</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">when</span> (num) &#123;</span><br><span class="line">        <span class="keyword">is</span> <span class="built_in">Int</span> -&gt; println(<span class="string">&quot;number is Int&quot;</span>)</span><br><span class="line">        <span class="keyword">is</span> <span class="built_in">Double</span> -&gt; println(<span class="string">&quot;number is Double&quot;</span>)</span><br><span class="line">        <span class="keyword">else</span> -&gt; println(<span class="string">&quot;number not support&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// Number类型的参数是一个抽象类</span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">getScore</span><span class="params">(name: <span class="type">String</span>)</span></span> = <span class="keyword">when</span> &#123;</span><br><span class="line">    name.startsWith(<span class="string">&quot;Tom&quot;</span>) -&gt; <span class="number">86</span> <span class="comment">// 以Tom开头的名字都是86</span></span><br><span class="line">    name == <span class="string">&quot;Jim&quot;</span> -&gt; <span class="number">77</span></span><br><span class="line">    name == <span class="string">&quot;Jack&quot;</span> -&gt; <span class="number">95</span></span><br><span class="line">    name == <span class="string">&quot;Lily&quot;</span> -&gt; <span class="number">100</span></span><br><span class="line">    <span class="keyword">else</span> -&gt; <span class="number">0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="循环语句"><a class="markdownIt-Anchor" href="#循环语句"></a> 循环语句</h3><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> range = <span class="number">0.</span><span class="number">.10</span> <span class="comment">// [0,10]的区间</span></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (i <span class="keyword">in</span> <span class="number">0.</span><span class="number">.10</span>)&#123;</span><br><span class="line">        println(i)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> range = <span class="number">0</span> until <span class="number">10</span> <span class="comment">// [0,10)的区间</span></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (i <span class="keyword">in</span> <span class="number">0</span> until <span class="number">10</span> step <span class="number">2</span>)&#123; <span class="comment">// 每次递增2</span></span><br><span class="line">        println(i)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (i <span class="keyword">in</span> <span class="number">10</span> downTo <span class="number">1</span>)&#123;</span><br><span class="line">        println(i) <span class="comment">// 降序</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="3类与对象"><a class="markdownIt-Anchor" href="#3类与对象"></a> 3.类与对象</h2><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span>(<span class="keyword">var</span> name: String, <span class="keyword">var</span> age: <span class="built_in">Int</span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> name: String = <span class="string">&quot;&quot;</span></span><br><span class="line">    <span class="keyword">var</span> age=<span class="number">0</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">eat</span><span class="params">()</span></span>&#123;</span><br><span class="line">        println(<span class="string">&quot;<span class="variable">$name</span> is eating. He is <span class="variable">$age</span> years old.&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">val</span> p = Person()</span><br><span class="line">    P.name = <span class="string">&quot;Jack&quot;</span></span><br><span class="line">    P.age = <span class="number">19</span></span><br><span class="line">    P.eat()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="4继承与构造函数"><a class="markdownIt-Anchor" href="#4继承与构造函数"></a> 4.继承与构造函数</h2><p>在Kotlin中任何一个非抽象类默认都是不可被继承的</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">open</span> <span class="keyword">class</span> <span class="title class_">Person</span>(<span class="keyword">var</span> name: String, <span class="keyword">var</span> age: <span class="built_in">Int</span>) &#123; <span class="comment">// 加上open就可以被继承</span></span><br><span class="line">    <span class="keyword">var</span> name: String = <span class="string">&quot;&quot;</span></span><br><span class="line">    <span class="keyword">var</span> age=<span class="number">0</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">eat</span><span class="params">()</span></span>&#123;</span><br><span class="line">        println(name+<span class="string">&quot; is eating. He is &quot;</span>+ age +<span class="string">&quot; years old.&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> : <span class="type">Person</span>() &#123; <span class="comment">// 继承用冒号</span></span><br><span class="line">    <span class="keyword">var</span> sno = <span class="string">&quot;&quot;</span></span><br><span class="line">    <span class="keyword">var</span> grade = <span class="number">0</span>                  sad /.</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>主构造函数</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>(name: String, age: <span class="built_in">Int</span>) : Person(name, age)&#123;</span><br><span class="line">    <span class="keyword">init</span> &#123; <span class="comment">// 创建实例后会执行该逻辑</span></span><br><span class="line">        println(<span class="string">&quot;sno is <span class="variable">$sno</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="5接口"><a class="markdownIt-Anchor" href="#5接口"></a> 5.接口</h2><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Study</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">readBooks</span><span class="params">()</span></span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">doHomeWork</span><span class="params">()</span></span>&#123;</span><br><span class="line">        println(<span class="string">&quot;do homework default implementation.&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span>(name: String, age: <span class="built_in">Int</span>) : Person(name, age), Study&#123; <span class="comment">// 调用接口用逗号</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">readBooks</span><span class="params">()</span></span> &#123;</span><br><span class="line">        println(name+<span class="string">&quot; is reading.&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">doHomeWork</span><span class="params">()</span></span> &#123;</span><br><span class="line">        println(name+<span class="string">&quot; is doing homework.&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="5数据类"><a class="markdownIt-Anchor" href="#5数据类"></a> 5.数据类</h2><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">data</span> <span class="keyword">class</span> <span class="title class_">Cellphone</span>(<span class="keyword">val</span> brand: String, <span class="keyword">var</span> price: <span class="built_in">Double</span>) <span class="comment">// 数据类用data关键字</span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">val</span> cellphone1 = Cellphone(<span class="string">&quot;Samsung&quot;</span>, <span class="number">1299.99</span>)</span><br><span class="line">    <span class="keyword">val</span> cellphone2 = Cellphone(<span class="string">&quot;Samsung&quot;</span>, <span class="number">1299.99</span>)</span><br><span class="line">    println(cellphone1)</span><br><span class="line">    println(<span class="string">&quot;&amp;&#123;cellphone1 == cellphone2&#125;&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="6单例类在全局只有一个实例"><a class="markdownIt-Anchor" href="#6单例类在全局只有一个实例"></a> 6.单例类(在全局只有一个实例)</h2><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">object</span> Singleton &#123; <span class="comment">// 单例类用odject关键字</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">singletonTest</span><span class="params">()</span></span> &#123;</span><br><span class="line">        println(<span class="string">&quot;SingletonTest is called.&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Singleton.singletonTest() <span class="comment">// 调用</span></span><br></pre></td></tr></table></figure><hr /><h2 id="7lambda编程"><a class="markdownIt-Anchor" href="#7lambda编程"></a> 7.Lambda编程</h2><h3 id="常见使用场景总览"><a class="markdownIt-Anchor" href="#常见使用场景总览"></a> 🔹常见使用场景总览</h3><table><thead><tr><th>场景</th><th>描述</th><th>示例</th></tr></thead><tbody><tr><td>1️⃣ 集合操作</td><td><code>map</code>、<code>filter</code>、<code>forEach</code> 等</td><td><code>list.filter &#123; it.isNotEmpty() &#125;</code></td></tr><tr><td>2️⃣ 事件监听</td><td>点击、输入等</td><td><code>button.setOnClickListener &#123; showToast() &#125;</code></td></tr><tr><td>3️⃣ 函数参数</td><td>高阶函数传入行为</td><td><code>repeat(5) &#123; println(&quot;Hello&quot;) &#125;</code></td></tr><tr><td>4️⃣ 线程/协程</td><td>简化异步任务</td><td><code>Thread &#123; doWork() &#125;.start()</code></td></tr><tr><td>5️⃣ 自定义回调</td><td>封装逻辑，暴露接口</td><td><code>fetchData &#123; result -&gt; println(result) &#125;</code></td></tr></tbody></table><h3 id="集合的创建与遍历"><a class="markdownIt-Anchor" href="#集合的创建与遍历"></a> 🔹 集合的创建与遍历</h3><p><strong>List</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> list = ListOf(<span class="string">&quot;Apple&quot;</span>, <span class="string">&quot;Banana&quot;</span>, <span class="string">&quot;Orange&quot;</span>, <span class="string">&quot;Pear&quot;</span>, <span class="string">&quot;Grape&quot;</span>) <span class="comment">// 不可变集合</span></span><br><span class="line"><span class="keyword">for</span> (fruit <span class="keyword">in</span> list) &#123;</span><br><span class="line">    println(fruit)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> list = mutableListOf(<span class="string">&quot;Apple&quot;</span>, <span class="string">&quot;Banana&quot;</span>, <span class="string">&quot;Orange&quot;</span>, <span class="string">&quot;Pear&quot;</span>, <span class="string">&quot;Grape&quot;</span>) <span class="comment">// 可变集合</span></span><br><span class="line">    list.add(<span class="string">&quot;Watermalon&quot;</span>)</span><br><span class="line">    <span class="keyword">val</span> anyResult=list.any&#123; it.length &lt;= <span class="number">5</span>&#125;</span><br><span class="line">    <span class="keyword">val</span> allResult=list.all&#123; it.length &lt;= <span class="number">5</span>&#125;</span><br><span class="line">    println(<span class="string">&quot;anyResult is <span class="variable">$anyResult</span>,allResult is <span class="variable">$allResult</span>&quot;</span>)</span><br><span class="line">    <span class="keyword">for</span> (fruit <span class="keyword">in</span> list) &#123;</span><br><span class="line">        println(fruit)</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><p><strong>Set</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> list = SetOf(<span class="string">&quot;Apple&quot;</span>, <span class="string">&quot;Banana&quot;</span>, <span class="string">&quot;Orange&quot;</span>, <span class="string">&quot;Pear&quot;</span>, <span class="string">&quot;Grape&quot;</span>) <span class="comment">// 不包含重复元素 </span></span><br><span class="line"><span class="keyword">for</span> (fruit <span class="keyword">in</span> list) &#123;</span><br><span class="line">    println(fruit)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>Map</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> map = mapOf(<span class="string">&quot;Apple&quot;</span> to <span class="number">1</span>, <span class="string">&quot;Banana&quot;</span> to <span class="number">2</span>, <span class="string">&quot;Orange&quot;</span> to <span class="number">3</span>,<span class="string">&quot;Pear&quot;</span> to <span class="number">4</span>, <span class="string">&quot;Grape&quot;</span> to <span class="number">5</span>)</span><br><span class="line">    <span class="keyword">for</span> ((fruit, number) <span class="keyword">in</span> map) &#123;</span><br><span class="line">        println(<span class="string">&quot;fruit is <span class="variable">$fruit</span>, number is <span class="variable">$number</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h3 id="集合的函数api"><a class="markdownIt-Anchor" href="#集合的函数api"></a> 🔹 集合的函数API</h3><p>Lambda表达式的语法结构 : {参数名1: 参数类型, 参数名2: 参数类型 -&gt; 函数体}</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 定义一个具名 lambda 变量，然后传入 maxBy 函数</span></span><br><span class="line"><span class="keyword">val</span> lambda = &#123; fruit: String -&gt; fruit.length &#125;</span><br><span class="line"><span class="comment">// 此处将 lambda 表达式赋值给变量 lambda，该表达式接收一个 String，返回该字符串的长度。</span></span><br><span class="line"><span class="keyword">val</span> maxLengthFruit1 = list.maxBy(lambda)</span><br><span class="line"><span class="comment">// 通过传入预先定义的 lambda，找出列表中字符串长度最大的元素。</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 直接在 maxBy 函数调用中传入 lambda 表达式（括号内写法）</span></span><br><span class="line"><span class="keyword">val</span> maxLengthFruit2 = list.maxBy(&#123; fruit: String -&gt; fruit.length &#125;)</span><br><span class="line"><span class="comment">// 在括号内直接定义 lambda，显示指定参数类型为 String，然后返回 fruit.length。</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 使用尾随 lambda 的写法（lambda 表达式放在圆括号外，但仍写出参数类型）</span></span><br><span class="line"><span class="keyword">val</span> maxLengthFruit3 = list.maxBy() &#123; fruit: String -&gt; fruit.length &#125;</span><br><span class="line"><span class="comment">// 当函数的最后一个参数是 lambda 时，可以把它放在括号外；这里仍然明确了参数类型。</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 4. 更简洁的尾随 lambda 写法，省略了圆括号</span></span><br><span class="line"><span class="keyword">val</span> maxLengthFruit4 = list.maxBy &#123; fruit: String -&gt; fruit.length &#125;</span><br><span class="line"><span class="comment">// 同上，使用尾随 lambda 并省略空的括号，lambda 内部仍然指定参数类型。</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 5. 省略 lambda 参数的类型，由编译器自动推断</span></span><br><span class="line"><span class="keyword">val</span> maxLengthFruit5 = list.maxBy &#123; fruit -&gt; fruit.length &#125;</span><br><span class="line"><span class="comment">// 此写法中省略了参数类型，编译器根据上下文知道 fruit 是 String，因此代码更简洁。</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 6. 当 lambda 只有一个参数时，可以使用隐式参数 it</span></span><br><span class="line"><span class="keyword">val</span> maxLengthFruit6 = list.maxBy &#123; it.length &#125;</span><br><span class="line"><span class="comment">// 当 lambda 只有一个参数时，Kotlin 允许使用 it 作为默认参数名称，使代码更简洁。</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p>map函数是最常用的一种函数API</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> list = listOf(<span class="string">&quot;Apple&quot;</span>, <span class="string">&quot;Banana&quot;</span>, <span class="string">&quot;Orange&quot;</span>, <span class="string">&quot;Pear&quot;</span>, <span class="string">&quot;Grape&quot;</span>)</span><br><span class="line">    <span class="keyword">val</span> newList = list.map &#123; it.toUpperCase() &#125; <span class="comment">// 将所有水果名变成大写模式</span></span><br><span class="line">    <span class="keyword">for</span> (fruit <span class="keyword">in</span> newlist) &#123;</span><br><span class="line">    println(fruit)</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>filter函数用于过滤集合中的数据</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> list = listOf(<span class="string">&quot;Apple&quot;</span>, <span class="string">&quot;Banana&quot;</span>, <span class="string">&quot;Orange&quot;</span>, <span class="string">&quot;Pear&quot;</span>, <span class="string">&quot;Grape&quot;</span>)</span><br><span class="line">    <span class="keyword">val</span> newList = list.map &#123; it.length &lt;= <span class="number">5</span> &#125;.map &#123; it.toUpperCase() &#125; <span class="comment">// 保留五个字母以内的水果</span></span><br><span class="line">    <span class="keyword">for</span> (fruit <span class="keyword">in</span> newlist) &#123;</span><br><span class="line">    println(fruit)</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>any和all函数</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> list = mutableListOf(<span class="string">&quot;Apple&quot;</span>, <span class="string">&quot;Banana&quot;</span>, <span class="string">&quot;Orange&quot;</span>, <span class="string">&quot;Pear&quot;</span>, <span class="string">&quot;Grape&quot;</span>)</span><br><span class="line">    list.add(<span class="string">&quot;Watermalon&quot;</span>)</span><br><span class="line">    <span class="keyword">val</span> anyResult=list.any&#123; it.length &lt;= <span class="number">5</span>&#125; <span class="comment">// 只要一个满足条件</span></span><br><span class="line">    <span class="keyword">val</span> allResult=list.all&#123; it.length &lt;= <span class="number">5</span>&#125; <span class="comment">// 所有的都要满足条件</span></span><br><span class="line">    println(<span class="string">&quot;anyResult is <span class="variable">$anyResult</span>,allResult is <span class="variable">$allResult</span>&quot;</span>)</span><br></pre></td></tr></table></figure><h3 id="java函数式api的使用"><a class="markdownIt-Anchor" href="#java函数式api的使用"></a> 🔹 Java函数式API的使用</h3><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Thread&#123;</span><br><span class="line">        println(<span class="string">&quot;Hello World&quot;</span>)</span><br><span class="line">    &#125;.start()</span><br></pre></td></tr></table></figure><hr /><h2 id="8空指针检查"><a class="markdownIt-Anchor" href="#8空指针检查"></a> 8.空指针检查</h2><p>Kotlin默认所有参数和变量不能为空</p><p>**?         ?.       ?: **</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">doStudy</span><span class="params">(study: <span class="type">Study</span>?)</span></span> &#123; <span class="comment">// 加上?可为空</span></span><br><span class="line">    <span class="comment">// 检查study是否不为null，避免空指针异常</span></span><br><span class="line">    <span class="keyword">if</span> (study != <span class="literal">null</span>) &#123;</span><br><span class="line">        study.readBooks()</span><br><span class="line">        study.doHomeWork()</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 如果study为null，函数将不会执行任何操作</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">doStudy</span><span class="params">(study: <span class="type">Study</span>?)</span></span> &#123; </span><br><span class="line">    study?.readBooks() <span class="comment">//不为空就调用</span></span><br><span class="line">    study?.doHomeWork()</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> c = a ?: b <span class="comment">// 左边为空就返回右边的结果</span></span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">fetTextLength</span><span class="params">(text: <span class="type">String</span>?)</span></span> = text?.length ?: <span class="number">0</span></span><br></pre></td></tr></table></figure><p><strong>非空断言工具 : !!</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> upperCase = content!!.toUpperCase() <span class="comment">// 不用空指针检查</span></span><br></pre></td></tr></table></figure><p><strong>let函数</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">doStudy</span><span class="params">(study: <span class="type">Study</span>?)</span></span> &#123; </span><br><span class="line">    study?.let &#123; stu -&gt;</span><br><span class="line">    stu.readBooks() <span class="comment">//不为空就调用</span></span><br><span class="line">    stu.doHomeWork()</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><hr /><h2 id="9函数的参数默认值"><a class="markdownIt-Anchor" href="#9函数的参数默认值"></a> 9.函数的参数默认值</h2><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">printParams</span><span class="params">(num: <span class="type">Int</span> = <span class="number">100</span>, str: <span class="type">String</span>)</span></span>&#123; <span class="comment">// 不传参数时返回默认值</span></span><br><span class="line">    println(<span class="string">&quot;num is <span class="variable">$num</span>,str is <span class="variable">$str</span>&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    printParams(str = <span class="string">&quot;world&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="10标准函数withrun和apply"><a class="markdownIt-Anchor" href="#10标准函数withrun和apply"></a> 10.标准函数with,run和apply</h2><h3 id="with"><a class="markdownIt-Anchor" href="#with"></a> 🔹 with</h3><ul><li><p><strong>作用</strong>：<br />用于对传入的对象执行一系列操作，省去重复引用该对象的麻烦。<br />它并不是扩展函数，而是一个顶层函数。</p></li><li><p><strong>返回值</strong>：<br />返回 lambda 表达式中最后一行代码的执行结果。</p></li><li><p><strong>语法</strong>：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">with(receiver) &#123;</span><br><span class="line">    <span class="comment">// 在这里可以直接访问 receiver 的成员（属性和方法）</span></span><br><span class="line">    <span class="comment">// 最后一行的表达式将作为返回值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>示例代码</strong>：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 假设有一个 Person 类</span></span><br><span class="line"><span class="keyword">data</span> <span class="keyword">class</span> <span class="title class_">Person</span>(<span class="keyword">var</span> name: String, <span class="keyword">var</span> age: <span class="built_in">Int</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> person = Person(<span class="string">&quot;Alice&quot;</span>, <span class="number">25</span>)</span><br><span class="line">    <span class="comment">// 使用 with 对 person 对象进行操作</span></span><br><span class="line">    <span class="keyword">val</span> info = with(person) &#123;</span><br><span class="line">        <span class="comment">// 可以直接访问 person 的属性和方法</span></span><br><span class="line">        println(<span class="string">&quot;Name: <span class="variable">$name</span>&quot;</span>)</span><br><span class="line">        println(<span class="string">&quot;Age: <span class="variable">$age</span>&quot;</span>)</span><br><span class="line">        <span class="comment">// 返回一个结果字符串</span></span><br><span class="line">        <span class="string">&quot;<span class="variable">$name</span> is <span class="variable">$age</span> years old.&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line">    println(info) <span class="comment">// 输出: &quot;Alice is 25 years old.&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="run"><a class="markdownIt-Anchor" href="#run"></a> 🔹 run</h3><ul><li><p><strong>作用</strong>：<br />run 既可以作为扩展函数，也可以作为普通函数。作为扩展函数时，它允许在对象上直接调用，从而在 lambda 表达式内使用该对象的上下文。</p></li><li><p><strong>返回值</strong>：<br />返回 lambda 表达式中最后一行代码的执行结果。</p></li><li><p><strong>语法</strong>：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">receiver.run &#123;</span><br><span class="line">    <span class="comment">// 在 lambda 中，this 指向 receiver，可以直接访问其成员</span></span><br><span class="line">    <span class="comment">// 最后一行的表达式的结果将作为返回值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>示例代码</strong>：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 继续使用 Person 类</span></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> person = Person(<span class="string">&quot;Alice&quot;</span>, <span class="number">25</span>)</span><br><span class="line">    <span class="comment">// 使用 run 在 person 对象上直接调用</span></span><br><span class="line">    <span class="keyword">val</span> result = person.run &#123;</span><br><span class="line">        <span class="comment">// 在这里可以直接引用 person 的属性和方法</span></span><br><span class="line">        println(<span class="string">&quot;Name: <span class="variable">$name</span>&quot;</span>)</span><br><span class="line">        println(<span class="string">&quot;Age: <span class="variable">$age</span>&quot;</span>)</span><br><span class="line">        <span class="comment">// 返回一个字符串作为结果</span></span><br><span class="line">        <span class="string">&quot;<span class="variable">$name</span>, age <span class="variable">$age</span>&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line">    println(result) <span class="comment">// 输出: &quot;Alice, age 25&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="apply"><a class="markdownIt-Anchor" href="#apply"></a> 🔹 apply</h3><ul><li><p><strong>作用</strong>：<br />apply 主要用于对象的初始化或配置。它以扩展函数的形式存在，可以对对象的属性进行设置，而无需多次引用对象本身。</p></li><li><p><strong>返回值</strong>：<br />返回调用对象本身，而不是 lambda 中最后一行表达式的结果。</p></li><li><p><strong>语法</strong>:</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">receiver.apply &#123;</span><br><span class="line">    <span class="comment">// 在 lambda 中，this 指向 receiver</span></span><br><span class="line">    <span class="comment">// 对对象进行一系列配置或初始化操作</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>示例代码</strong>：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 继续使用 Person 类</span></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="comment">// 使用 apply 对 Person 对象进行初始化</span></span><br><span class="line">    <span class="keyword">val</span> newPerson = Person(<span class="string">&quot;Bob&quot;</span>, <span class="number">30</span>).apply &#123;</span><br><span class="line">        <span class="comment">// 这里可以设置对象属性或者调用对象方法</span></span><br><span class="line">        println(<span class="string">&quot;Initializing person: <span class="variable">$name</span>, <span class="variable">$age</span>&quot;</span>)</span><br><span class="line">        <span class="comment">// 可以进行链式调用</span></span><br><span class="line">        age += <span class="number">1</span>  <span class="comment">// 修改属性</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// apply 返回的是修改后的 newPerson 对象</span></span><br><span class="line">    println(newPerson) <span class="comment">// 输出: Person(name=Bob, age=31)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="总结"><a class="markdownIt-Anchor" href="#总结"></a> 🔹 总结</h3><ul><li><strong>with</strong>：<ul><li>需要传入对象作为参数。</li><li>在 lambda 中操作对象，返回值为最后一行表达式的结果。</li><li>适合对对象进行一系列操作后需要返回计算结果的场景。</li></ul></li><li><strong>run</strong>：<ul><li>作为扩展函数使用，可在对象上直接调用。</li><li>同样返回 lambda 的最后一行表达式结果。</li><li>常用于执行一段代码块并返回一个计算结果。</li></ul></li><li><strong>apply</strong>：<ul><li>主要用于对象的初始化与配置。</li><li>返回调用对象本身，便于链式调用。</li><li>适合在创建对象后需要对对象属性进行设置的场景。</li></ul></li></ul><hr /><h2 id="11定义静态方法不需要实例就可以调用的方法"><a class="markdownIt-Anchor" href="#11定义静态方法不需要实例就可以调用的方法"></a> 11.定义静态方法(不需要实例就可以调用的方法)</h2><h3 id="运用注解"><a class="markdownIt-Anchor" href="#运用注解"></a> 🔹 运用注解</h3><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Util</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">doAction1</span><span class="params">()</span></span> &#123;</span><br><span class="line">        println(<span class="string">&quot;do action1&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">companion</span> <span class="keyword">object</span> &#123;</span><br><span class="line">        <span class="meta">@JvmStatic</span></span><br><span class="line">        <span class="function"><span class="keyword">fun</span> <span class="title">doAction2</span><span class="params">()</span></span> &#123; <span class="comment">// 静态方法</span></span><br><span class="line">            println(<span class="string">&quot;do action2&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Util.doAction2() <span class="comment">// 调用静态方法</span></span><br></pre></td></tr></table></figure><h3 id="运用顶层方法"><a class="markdownIt-Anchor" href="#运用顶层方法"></a> 🔹 运用顶层方法</h3><p>创建一个Kotlin文件(File),在里面写上静态方法,后面可以直接调用</p><hr /><h2 id="12变量延迟初始化"><a class="markdownIt-Anchor" href="#12变量延迟初始化"></a> 12.变量延迟初始化</h2><p>使其不用进行判空处理,但注意要记得初始化</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// private var adapter: MsgAdapter?=null</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> adapter</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(!::adapter.isInitialized)&#123;</span><br><span class="line">    <span class="comment">// 初始化操作</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="13使用密封类优化代码"><a class="markdownIt-Anchor" href="#13使用密封类优化代码"></a> 13.使用密封类优化代码</h2><p>选择时可以不加 else</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* interface Result</span></span><br><span class="line"><span class="comment">class Success(val msg: String) : Result</span></span><br><span class="line"><span class="comment">class Failure(val error: Exception) : Result */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">sealed</span> <span class="keyword">class</span> <span class="title class_">Rexult</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Success</span>(<span class="keyword">val</span> msg: String) : Result()</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Failure</span>(<span class="keyword">val</span> error: Exception) : Result() </span><br><span class="line"></span><br></pre></td></tr></table></figure><hr /><h2 id="14扩展函数"><a class="markdownIt-Anchor" href="#14扩展函数"></a> 14.扩展函数</h2><p>扩展函数是 Kotlin 中的一种语言特性，它允许你在不继承或修改原有类源码的情况下，为已有的类添加新的功能。</p><p><strong>语法结构</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> ClassName.<span class="title">methodName</span><span class="params">(param1: <span class="type">Int</span>, param2: <span class="type">Int</span>)</span></span>: <span class="built_in">Int</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>创建一个String.kt 文件</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> String.<span class="title">lettersCount</span><span class="params">()</span></span>: int &#123;</span><br><span class="line">    <span class="keyword">var</span> count = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> (char <span class="keyword">in</span> <span class="keyword">this</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (char.isLetter())&#123;</span><br><span class="line">            count++</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> count</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> count = <span class="string">&quot;AVSVGS_+&quot;</span>.lettersCount()</span><br></pre></td></tr></table></figure><hr /><h2 id="15运算符重载"><a class="markdownIt-Anchor" href="#15运算符重载"></a> 15.运算符重载</h2><p>运算符重载允许你为自定义类型定义标准运算符（如 +、-、*、/ 等）的行为，从而使得这些类型的对象可以像基本数据类型一样使用运算符。运算符重载通常通过定义特定名称的函数来实现，在 Kotlin 中需要使用 <code>operator</code> 修饰符。例如，为了重载加号运算符，你可以为一个类定义一个 <code>plus</code> 函数：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Money</span>(<span class="keyword">val</span> value: <span class="built_in">Int</span>) &#123;</span><br><span class="line">    <span class="keyword">operator</span> <span class="function"><span class="keyword">fun</span> <span class="title">plus</span><span class="params">(money: <span class="type">Money</span>)</span></span>: Money &#123;</span><br><span class="line">        <span class="keyword">val</span> sum = value + money.value</span><br><span class="line">        <span class="keyword">return</span> Money(sum)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">operator</span> <span class="function"><span class="keyword">fun</span> <span class="title">plus</span><span class="params">(newValue: <span class="type">Int</span>)</span></span>: Money &#123;</span><br><span class="line">        <span class="keyword">val</span> sum = value + newValue</span><br><span class="line">        <span class="keyword">return</span> Money(sum)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> money1 = Money(<span class="number">5</span>)</span><br><span class="line"><span class="keyword">val</span> money2 = Money(<span class="number">10</span>)</span><br><span class="line"><span class="keyword">val</span> money3 = Money1+money2</span><br><span class="line"><span class="keyword">val</span> money4 = money3 +<span class="number">20</span></span><br><span class="line">println(money4.value)</span><br></pre></td></tr></table></figure><hr /><h2 id="16高阶函数"><a class="markdownIt-Anchor" href="#16高阶函数"></a> 16.高阶函数</h2><p>在 Kotlin 中，高阶函数（Higher-Order Function）是指接受函数作为参数或者将函数作为返回值的函数。由于 Kotlin 将函数视为一等公民（first-class citizens），这使得你可以灵活地处理函数，实现更高的抽象和代码复用。下面详细介绍 Kotlin 中高阶函数的相关概念和用法。</p><h3 id="接受函数作为参数"><a class="markdownIt-Anchor" href="#接受函数作为参数"></a> 🔹 接受函数作为参数</h3><p>定义一个高阶函数，该函数接受一个函数作为参数：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个高阶函数，接受两个整数和一个函数作为参数</span></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">calculate</span><span class="params">(a: <span class="type">Int</span>, b: <span class="type">Int</span>, operation: (<span class="type">Int</span>, <span class="type">Int</span>) -&gt; <span class="type">Int</span>)</span></span>: <span class="built_in">Int</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> operation(a, b)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用 calculate，并传入一个 lambda 表达式</span></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> sum = calculate(<span class="number">3</span>, <span class="number">4</span>) &#123; x, y -&gt; x + y &#125;</span><br><span class="line">    println(<span class="string">&quot;Sum: <span class="variable">$sum</span>&quot;</span>)  <span class="comment">// 输出: Sum: 7</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中，<code>calculate</code> 函数通过参数 <code>operation</code> 接受了一个 lambda 表达式，用于定义具体的计算操作。</p><h3 id="将函数作为返回值"><a class="markdownIt-Anchor" href="#将函数作为返回值"></a> 🔹 将函数作为返回值</h3><p>一个函数可以返回另一个函数，例如：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个函数，返回一个加法函数</span></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">getAdder</span><span class="params">()</span></span>: (<span class="built_in">Int</span>, <span class="built_in">Int</span>) -&gt; <span class="built_in">Int</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123; x, y -&gt; x + y &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> adder = getAdder()</span><br><span class="line">    println(<span class="string">&quot;Adder: <span class="subst">$&#123;adder(<span class="number">5</span>, <span class="number">10</span>)&#125;</span>&quot;</span>)  <span class="comment">// 输出: Adder: 15</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里 <code>getAdder</code> 返回一个 lambda 表达式，该表达式实现了两个整数相加的功能。</p><hr /><h2 id="17内联函数"><a class="markdownIt-Anchor" href="#17内联函数"></a> 17.内联函数</h2><p>在 Kotlin 中，使用 <code>inline</code> 关键字声明的函数称为内联函数。编译器在编译过程中会将内联函数的代码直接插入到调用处，而不是生成独立的函数调用。这样做可以避免高阶函数传递 lambda 表达式时产生的对象分配和调用开销。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个内联函数，接受两个整数和一个 lambda 表达式作为操作</span></span><br><span class="line"><span class="keyword">inline</span> <span class="function"><span class="keyword">fun</span> <span class="title">operate</span><span class="params">(a: <span class="type">Int</span>, b: <span class="type">Int</span>, operation: (<span class="type">Int</span>, <span class="type">Int</span>) -&gt; <span class="type">Int</span>)</span></span>: <span class="built_in">Int</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> operation(a, b)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="comment">// 调用内联函数，并传入一个 lambda 表达式</span></span><br><span class="line">    <span class="keyword">val</span> sum = operate(<span class="number">3</span>, <span class="number">4</span>) &#123; x, y -&gt; x + y &#125;</span><br><span class="line">    println(<span class="string">&quot;Sum: <span class="variable">$sum</span>&quot;</span>)  <span class="comment">// 输出: Sum: 7</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="noinline"><a class="markdownIt-Anchor" href="#noinline"></a> 🔹 noinline</h3><p><strong>作用与场景</strong></p><ul><li><strong>默认行为</strong>：在内联函数中，所有的 lambda 参数默认都会被内联（即直接替换到调用处），从而减少额外的函数调用开销。</li><li><strong>使用场景</strong>：有时需要将某个 lambda 参数当作普通对象传递，比如存储到变量中、放入集合、或者传递给其它非内联函数等，这时就不能将它内联，否则会破坏其“对象”特性。</li><li><strong>noinline 关键字</strong>：标记在 lambda 参数前，表示该参数在调用过程中<strong>不内联</strong>，编译器不会将该 lambda 展开到调用处。</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个内联函数，其中 block1 会内联，block2 不会内联</span></span><br><span class="line"><span class="keyword">inline</span> <span class="function"><span class="keyword">fun</span> <span class="title">process</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    block1: () -&gt; <span class="type">Unit</span>,</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="keyword">noinline</span> block2: () -&gt; <span class="type">Unit</span></span></span></span><br><span class="line"><span class="params"><span class="function">)</span></span> &#123;</span><br><span class="line">    block1()  <span class="comment">// 会被内联展开</span></span><br><span class="line">    block2()  <span class="comment">// 不会被内联展开，可以作为对象使用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="crossinline"><a class="markdownIt-Anchor" href="#crossinline"></a> 🔹 crossinline</h3><p><strong>作用与场景</strong></p><ul><li><strong>非局部返回问题</strong>：在内联函数中，lambda 参数默认允许使用非局部返回（non-local return），即在 lambda 中使用 <code>return</code> 可以直接返回到外层调用函数。但当 lambda 被传递到其他执行环境中（比如异步回调或其它嵌套调用）时，非局部返回可能会导致逻辑混乱或无法预料的行为。</li><li><strong>使用场景</strong>：如果希望 lambda 参数仍然内联（从而获得性能优势），但又不允许其使用非局部返回，就可以使用 crossinline。</li><li><strong>crossinline 关键字</strong>：标记在 lambda 参数前，强制该 lambda 内的 <code>return</code> 只能用于返回 lambda 本身，不能用于返回外层函数。</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个内联函数，其中 block 参数被标记为 crossinline</span></span><br><span class="line"><span class="keyword">inline</span> <span class="function"><span class="keyword">fun</span> <span class="title">execute</span><span class="params">(<span class="keyword">crossinline</span> block: () -&gt; <span class="type">Unit</span>)</span></span> &#123;</span><br><span class="line">    <span class="comment">// 此处可能将 block 传递给另一个函数或在另一个上下文中执行</span></span><br><span class="line">    <span class="keyword">val</span> runnable = Runnable &#123; </span><br><span class="line">        <span class="comment">// 在这里使用 return 只能退出 lambda，而不能退出 execute 函数</span></span><br><span class="line">        block()  </span><br><span class="line">    &#125;</span><br><span class="line">    runnable.run()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="18高阶函数的运用"><a class="markdownIt-Anchor" href="#18高阶函数的运用"></a> 18.高阶函数的运用</h2><p><strong>简化SharedPreferences</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">getSharedPreferences(<span class="string">&quot;data&quot;</span>,Context.MODE_PRIVATE).edit &#123;</span><br><span class="line">    putString(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;Tom&quot;</span>)</span><br><span class="line">    putInt(<span class="string">&quot;age&quot;</span>, <span class="number">28</span>)</span><br><span class="line">    putBoolean(<span class="string">&quot;married&quot;</span>, <span class="literal">false</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>简化ContentValues</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> values =contentValuesOf(</span><br><span class="line">    <span class="string">&quot;name&quot;</span> to <span class="string">&quot;Game of Thrones&quot;</span>,</span><br><span class="line">    <span class="string">&quot;authpr&quot;</span> to <span class="string">&quot;George Martin&quot;</span>,</span><br><span class="line">    <span class="string">&quot;pages&quot;</span> to <span class="number">720</span>,</span><br><span class="line">    <span class="string">&quot;price&quot;</span> to <span class="number">20.85</span> </span><br><span class="line">)</span><br><span class="line">db.insert(<span class="string">&quot;Book&quot;</span>,<span class="literal">null</span>,values)</span><br></pre></td></tr></table></figure><hr /><h2 id="19泛型的基本用法"><a class="markdownIt-Anchor" href="#19泛型的基本用法"></a> 19.泛型的基本用法</h2><p>泛型允许我们在不指定具体类型的情况下进行编程</p><p><strong>定义泛型类</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span>&lt;<span class="type">T</span>&gt; &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">method</span><span class="params">(param: <span class="type">T</span>)</span></span>: T &#123;</span><br><span class="line">        <span class="keyword">return</span> param</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>调用泛型类</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> myClass = MyClass&lt;<span class="built_in">Int</span>&gt;()</span><br><span class="line"><span class="keyword">val</span> result = myClass.method(<span class="number">123</span>)</span><br></pre></td></tr></table></figure><p><strong>定义一个泛型方法</strong></p> <figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="type">&lt;T&gt;</span> <span class="title">method</span><span class="params">(param: <span class="type">T</span>)</span></span>: T &#123;</span><br><span class="line">        <span class="keyword">return</span> param</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>调用一个泛型方法</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> myClass = MyClass()</span><br><span class="line"><span class="keyword">val</span> result = myClass.method(<span class="number">123</span>)</span><br></pre></td></tr></table></figure><p><strong>对泛型进行类型限制</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="type">&lt;T : Number&gt;</span> <span class="title">method</span><span class="params">(param: <span class="type">T</span>)</span></span>: T &#123;</span><br><span class="line">        <span class="keyword">return</span> param</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="20类"><a class="markdownIt-Anchor" href="#20类"></a> 20.类</h2><p><strong>概念：</strong></p><p>类委托是 Kotlin 提供的一种设计模式，可以将接口的实现委托给另一个对象。也就是说，当一个类需要实现某个接口时，可以将接口中的部分或全部方法调用转交给另一个对象处理，从而实现代码复用和职责分离。</p><p><strong>用途：</strong></p><ul><li>简化代码实现，避免重复编写接口方法。</li><li>通过组合而非继承来扩展类的功能。</li><li>动态改变行为，增强灵活性。</li></ul><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个接口</span></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">Base</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">printMessage</span><span class="params">()</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 实现接口的类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BaseImpl</span>(<span class="keyword">val</span> message: String) : Base &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">printMessage</span><span class="params">()</span></span> &#123;</span><br><span class="line">        println(message)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用类委托的方式实现接口</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Derived</span>(b: Base) : Base <span class="keyword">by</span> b</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> baseImpl = BaseImpl(<span class="string">&quot;Hello, Kotlin Delegation!&quot;</span>)</span><br><span class="line">    <span class="keyword">val</span> derived = Derived(baseImpl)</span><br><span class="line">    derived.printMessage()  <span class="comment">// 输出：Hello, Kotlin Delegation!</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上面的例子中，<code>Derived</code> 类通过 <code>: Base by b</code> 将 <code>Base</code> 接口的实现委托给了 <code>baseImpl</code> 对象，因此调用 <code>derived.printMessage()</code> 实际上调用的是 <code>baseImpl</code> 中的方法实现。</p><hr /><h2 id="21委托属性"><a class="markdownIt-Anchor" href="#21委托属性"></a> 21.委托属性</h2><p><strong>概念：</strong><br />委托属性允许你将属性的 getter 和 setter 的逻辑交由另一个对象处理，这个对象称为“委托”。Kotlin 标准库提供了一些常用的委托，如 <code>lazy</code>、<code>observable</code>、<code>vetoable</code> 等，也可以自定义委托。</p><p><strong>用途：</strong></p><ul><li>实现延迟初始化（如 <code>lazy</code> 委托）。</li><li>属性变更监听（如 <code>observable</code> 委托）。</li><li>属性值验证、缓存等其他特殊逻辑。</li><li>简化重复代码，提高代码复用性。</li></ul><ol><li><p><strong>延迟初始化（Lazy Delegation）</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> lazyValue: String <span class="keyword">by</span> lazy &#123;</span><br><span class="line">    println(<span class="string">&quot;Computed!&quot;</span>)</span><br><span class="line">    <span class="string">&quot;Hello, Lazy Delegation!&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    println(<span class="string">&quot;Before accessing lazyValue&quot;</span>)</span><br><span class="line">    println(lazyValue)  <span class="comment">// 第一次访问时，计算并打印 &quot;Computed!&quot; 和 &quot;Hello, Lazy Delegation!&quot;</span></span><br><span class="line">    println(lazyValue)  <span class="comment">// 后续访问直接返回已计算的值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>可观察属性（Observable Delegation）</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> observedValue: <span class="built_in">Int</span> <span class="keyword">by</span> Delegates.observable(<span class="number">0</span>) &#123; property, oldValue, newValue -&gt;</span><br><span class="line">    println(<span class="string">&quot;属性 <span class="subst">$&#123;property.name&#125;</span> 从 <span class="variable">$oldValue</span> 变为 <span class="variable">$newValue</span>&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    observedValue = <span class="number">10</span>  <span class="comment">// 输出：属性 observedValue 从 0 变为 10</span></span><br><span class="line">    observedValue = <span class="number">20</span>  <span class="comment">// 输出：属性 observedValue 从 10 变为 20</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><p>在这两个例子中，委托属性分别将属性初始化逻辑和修改监听逻辑分离到专门的委托对象中，从而让属性本身的声明更加简洁。</p><h2 id="22infix函数构造更可读的语法改名字"><a class="markdownIt-Anchor" href="#22infix函数构造更可读的语法改名字"></a> 22.infix函数构造更可读的语法(改名字)</h2><p>创建一个<code>beginsWith</code>函数,使其具有<code>startsWith</code>函数的功能</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">infix</span> <span class="function"><span class="keyword">fun</span> String.<span class="title">beginsWith</span><span class="params">(prefix: <span class="type">String</span>)</span></span> = startsWith(prefix)</span><br></pre></td></tr></table></figure><p>创建一个<code>has</code>函数,使其具有<code>contains</code>函数的功能</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">infix</span> <span class="function"><span class="keyword">fun</span> <span class="type">&lt;T&gt;</span> Collection<span class="type">&lt;T&gt;</span>.<span class="title">has</span><span class="params">(element: <span class="type">T</span>)</span></span> = contains(element)</span><br></pre></td></tr></table></figure><hr /><h2 id="23对泛型进行实化"><a class="markdownIt-Anchor" href="#23对泛型进行实化"></a> 23.对泛型进行实化</h2><p>在 <strong>Kotlin 的内联函数（inline function）</strong> 中，我们可以使用 <code>reified</code> 关键字<strong>实化泛型</strong>，使得泛型参数在<strong>运行时仍然可以被访问</strong>。</p><h3 id="使用-reified-实化泛型"><a class="markdownIt-Anchor" href="#使用-reified-实化泛型"></a> <strong>🔹 使用 <code>reified</code> 实化泛型</strong></h3><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">inline</span> <span class="function"><span class="keyword">fun</span> <span class="type">&lt;<span class="keyword">reified</span> T&gt;</span> <span class="title">printType</span><span class="params">()</span></span> &#123;</span><br><span class="line">    println(<span class="string">&quot;类型是：<span class="subst">$&#123;T::class.simpleName&#125;</span>&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">printType&lt;<span class="built_in">Int</span>&gt;()   <span class="comment">// 输出: 类型是：Int</span></span><br><span class="line">printType&lt;String&gt;() <span class="comment">// 输出: 类型是：String</span></span><br></pre></td></tr></table></figure><p><strong>泛型 <code>T</code> 仍然存在于运行时</strong>，可以用 <code>T::class.simpleName</code> 获取类型名称。</p><hr /><h3 id="为什么要使用-reified"><a class="markdownIt-Anchor" href="#为什么要使用-reified"></a> <strong>🔹 为什么要使用 <code>reified</code>？</strong></h3><h4 id="1-运行时获取泛型类型"><a class="markdownIt-Anchor" href="#1-运行时获取泛型类型"></a> <strong>(1) 运行时获取泛型类型</strong></h4><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">inline</span> <span class="function"><span class="keyword">fun</span> <span class="type">&lt;<span class="keyword">reified</span> T&gt;</span> <span class="title">isType</span><span class="params">(value: <span class="type">Any</span>)</span></span>: <span class="built_in">Boolean</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> value <span class="keyword">is</span> T  <span class="comment">// 运行时可以判断类型</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">println(isType&lt;String&gt;(<span class="string">&quot;Hello&quot;</span>)) <span class="comment">// 输出: true</span></span><br><span class="line">println(isType&lt;<span class="built_in">Int</span>&gt;(<span class="string">&quot;Hello&quot;</span>))    <span class="comment">// 输出: false</span></span><br></pre></td></tr></table></figure><p><strong>普通泛型无法做到 <code>is T</code> 判断，而 <code>reified</code> 泛型可以！</strong></p><hr /><h4 id="2-在泛型函数中创建对象"><a class="markdownIt-Anchor" href="#2-在泛型函数中创建对象"></a> <strong>(2) 在泛型函数中创建对象</strong></h4><p>使用 <code>reified</code> 可以直接创建对象，而普通泛型无法做到：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">inline</span> <span class="function"><span class="keyword">fun</span> <span class="type">&lt;<span class="keyword">reified</span> T&gt;</span> <span class="title">createInstance</span><span class="params">()</span></span>: T? &#123;</span><br><span class="line">    <span class="keyword">return</span> T::<span class="keyword">class</span>.java.getDeclaredConstructor().newInstance()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">data</span> <span class="keyword">class</span> <span class="title class_">User</span>(<span class="keyword">val</span> name: String = <span class="string">&quot;Default&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> user: User? = createInstance&lt;User&gt;()</span><br><span class="line">println(user) <span class="comment">// 输出: User(name=Default)</span></span><br></pre></td></tr></table></figure><p><strong>可以直接实例化泛型类型 <code>T</code>，避免手动传递 <code>Class&lt;T&gt;</code>。</strong></p><hr /><h4 id="3-简化-class-作为参数的-api"><a class="markdownIt-Anchor" href="#3-简化-class-作为参数的-api"></a> <strong>(3) 简化 <code>Class</code> 作为参数的 API</strong></h4><p>在 Java 中，我们通常需要显式传递 <code>Class&lt;T&gt;</code>：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> &lt;T&gt; T fromJson(String json, Class&lt;T&gt; clazz) &#123;</span><br><span class="line">    <span class="keyword">return</span> new Gson().fromJson(json, clazz);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在 Kotlin 中，<code>reified</code> 让 <code>Class&lt;T&gt;</code> 变得<strong>不再必要</strong>：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">inline</span> <span class="function"><span class="keyword">fun</span> <span class="type">&lt;<span class="keyword">reified</span> T&gt;</span> Gson.<span class="title">fromJson</span><span class="params">(json: <span class="type">String</span>)</span></span>: T &#123;</span><br><span class="line">    <span class="keyword">return</span> fromJson(json, T::<span class="keyword">class</span>.java)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> json = <span class="string">&quot;&quot;&quot;&#123;&quot;name&quot;:&quot;Alice&quot;&#125;&quot;&quot;&quot;</span></span><br><span class="line"><span class="keyword">val</span> user: User = Gson().fromJson&lt;User&gt;(json)</span><br><span class="line">println(user)  <span class="comment">// 输出: User(name=Alice)</span></span><br></pre></td></tr></table></figure><p><strong>可以直接使用 <code>T::class.java</code>，无需手动传递 <code>Class&lt;T&gt;</code>！</strong></p><hr /><h4 id="4-简化-activity-的启动"><a class="markdownIt-Anchor" href="#4-简化-activity-的启动"></a> <strong>(4) 简化 <code>Activity</code> 的启动</strong></h4><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">inline</span> <span class="function"><span class="keyword">fun</span> <span class="type">&lt;<span class="keyword">reified</span> T&gt;</span> <span class="title">startActivity</span><span class="params">(context: <span class="type">Context</span>, block: <span class="type">Intent</span>.() -&gt; <span class="type">Unit</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> intent = Intent(context, T::<span class="keyword">class</span>,java)</span><br><span class="line">    intent.block()</span><br><span class="line">    context.startActivity(intent)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">startActivity&lt;TestActivity&gt;(context) &#123;</span><br><span class="line">    putExtra(<span class="string">&quot;param1&quot;</span>, <span class="string">&quot;data&quot;</span>)</span><br><span class="line">    putExtra(<span class="string">&quot;param2&quot;</span>, <span class="number">123</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="24泛型的协变与逆变"><a class="markdownIt-Anchor" href="#24泛型的协变与逆变"></a> 24.<strong>泛型的协变与逆变</strong></h2><p>在 Kotlin 中，泛型可以是 <strong>协变（covariant）</strong> 或 <strong>逆变（contravariant）</strong>，用于控制泛型类型参数在继承关系中的行为，以保证类型安全。</p><h3 id="什么是协变"><a class="markdownIt-Anchor" href="#什么是协变"></a> <strong>🔹 什么是协变？</strong></h3><p><strong>协变允许子类型泛型对象赋值给父类型泛型对象</strong>，但泛型类型参数<strong>只能作为返回值，不能作为参数</strong>。</p><p><strong>规则：<code>Producer&lt;T&gt;</code>（生产者）—— <code>T</code> 只能被返回，不可被消费</strong></p><ul><li><strong>关键字：<code>out</code></strong></li><li><strong>适用于只“生产”数据的场景</strong></li><li><strong>子类泛型对象可以赋值给父类泛型对象</strong></li></ul><h3 id="举例"><a class="markdownIt-Anchor" href="#举例"></a> <strong>🔹 举例</strong></h3><p>假设我们有一个 <code>Animal</code> 类和 <code>Dog</code> 类：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">open</span> <span class="keyword">class</span> <span class="title class_">Animal</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Dog</span> : <span class="type">Animal</span>()</span><br></pre></td></tr></table></figure><p>定义一个协变接口：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Producer</span>&lt;<span class="type">out T</span>&gt; &#123;  <span class="comment">// 使用 `out` 关键字</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">produce</span><span class="params">()</span></span>: T  <span class="comment">// 只能返回 T，不允许接收 T</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>可以将 <code>Producer&lt;Dog&gt;</code> 赋值给 <code>Producer&lt;Animal&gt;</code></strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">DogProducer</span> : <span class="type">Producer</span>&lt;<span class="type">Dog</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">produce</span><span class="params">()</span></span>: Dog &#123;</span><br><span class="line">        <span class="keyword">return</span> Dog()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> dogProducer: Producer&lt;Dog&gt; = DogProducer()</span><br><span class="line"><span class="keyword">val</span> animalProducer: Producer&lt;Animal&gt; = dogProducer  <span class="comment">// 允许赋值</span></span><br></pre></td></tr></table></figure><p><strong>为什么可以赋值？</strong></p><ul><li><code>Producer&lt;Dog&gt;</code> 只能 <strong>返回</strong> <code>Dog</code>，而 <code>Producer&lt;Animal&gt;</code> 也应该能返回 <code>Animal</code></li><li>由于 <code>Dog</code> 是 <code>Animal</code> 的子类，因此 <code>Dog</code> 的生产者也可以充当 <code>Animal</code> 的生产者</li></ul><p><strong>错误：协变泛型不能用作输入参数</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Producer</span>&lt;<span class="type">out T</span>&gt; &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">produce</span><span class="params">()</span></span>: T</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">consume</span><span class="params">(value: <span class="type">T</span>)</span></span>  <span class="comment">// 报错：out 泛型不能用于输入参数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="适用场景"><a class="markdownIt-Anchor" href="#适用场景"></a> <strong>🔹 适用场景</strong></h3><ul><li><strong>生产者模式（Producer）</strong>，例如：<ul><li><code>List&lt;T&gt;</code>（<code>List&lt;Dog&gt;</code> 可以赋值给 <code>List&lt;Animal&gt;</code>）</li><li><code>Sequence&lt;T&gt;</code></li></ul></li></ul><h3 id="什么是逆变"><a class="markdownIt-Anchor" href="#什么是逆变"></a> <strong>🔹 什么是逆变？</strong></h3><p><strong>逆变允许父类型泛型对象赋值给子类型泛型对象</strong>，但泛型类型参数<strong>只能作为输入参数，不能作为返回值</strong>。</p><p><strong>规则：<code>Consumer&lt;T&gt;</code>（消费者）—— <code>T</code> 只能被消费，不可被返回</strong></p><ul><li><strong>关键字：<code>in</code></strong></li><li><strong>适用于只“消费”数据的场景</strong></li><li><strong>父类泛型对象可以赋值给子类泛型对象</strong></li></ul><h3 id="举例-2"><a class="markdownIt-Anchor" href="#举例-2"></a> <strong>🔹 举例</strong></h3><p>定义一个逆变接口：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Consumer</span>&lt;<span class="type">in T</span>&gt; &#123;  <span class="comment">// 使用 `in` 关键字</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">consume</span><span class="params">(value: <span class="type">T</span>)</span></span>  <span class="comment">// 只能接收 T，不允许返回 T</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>可以将 <code>Consumer&lt;Animal&gt;</code> 赋值给 <code>Consumer&lt;Dog&gt;</code></strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">AnimalConsumer</span> : <span class="type">Consumer</span>&lt;<span class="type">Animal</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">consume</span><span class="params">(value: <span class="type">Animal</span>)</span></span> &#123;</span><br><span class="line">        println(<span class="string">&quot;Consuming an animal&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">val</span> animalConsumer: Consumer&lt;Animal&gt; = AnimalConsumer()</span><br><span class="line"><span class="keyword">val</span> dogConsumer: Consumer&lt;Dog&gt; = animalConsumer  <span class="comment">// 允许赋值</span></span><br></pre></td></tr></table></figure><p><strong>为什么可以赋值？</strong></p><ul><li><code>Consumer&lt;Animal&gt;</code> 需要消费 <code>Animal</code> 或其子类</li><li><code>Consumer&lt;Dog&gt;</code> 也需要消费 <code>Dog</code></li><li>由于 <code>Dog</code> 是 <code>Animal</code> 的子类，因此 <code>Consumer&lt;Animal&gt;</code> 也可以用于 <code>Consumer&lt;Dog&gt;</code></li></ul><p><strong>错误：逆变泛型不能用作返回值</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Consumer</span>&lt;<span class="type">in T</span>&gt; &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">consume</span><span class="params">(value: <span class="type">T</span>)</span></span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">produce</span><span class="params">()</span></span>: T  <span class="comment">// 报错：in 泛型不能用于返回值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="适用场景-2"><a class="markdownIt-Anchor" href="#适用场景-2"></a> <strong>🔹 适用场景</strong></h3><ul><li><strong>消费者模式（Consumer）</strong>，例如：<ul><li><code>Comparator&lt;T&gt;</code>（比较器）</li><li><code>Function&lt;T, R&gt;</code>（输入 <code>T</code>）</li></ul></li></ul><h2 id=""><a class="markdownIt-Anchor" href="#"></a> </h2><table><thead><tr><th><strong>对比项</strong></th><th><strong>协变 (<code>out</code>)</strong></th><th><strong>逆变 (<code>in</code>)</strong></th></tr></thead><tbody><tr><td><strong>关键字</strong></td><td><code>out</code></td><td><code>in</code></td></tr><tr><td><strong>可以作为返回值？</strong></td><td>可以</td><td>不可以</td></tr><tr><td><strong>可以作为输入参数？</strong></td><td>不可以</td><td>可以</td></tr><tr><td><strong>泛型类型关系</strong></td><td><code>Producer&lt;Dog&gt;</code> 可以赋值给 <code>Producer&lt;Animal&gt;</code></td><td><code>Consumer&lt;Animal&gt;</code> 可以赋值给 <code>Consumer&lt;Dog&gt;</code></td></tr><tr><td><strong>适用场景</strong></td><td>生产者（<code>Producer&lt;T&gt;</code>）</td><td>消费者（<code>Consumer&lt;T&gt;</code>）</td></tr></tbody></table><table><thead><tr><th><strong>特性</strong></th><th><strong>协变（<code>out</code>）</strong></th><th><strong>逆变（<code>in</code>）</strong></th></tr></thead><tbody><tr><td><strong>作用</strong></td><td>生产者，返回 <code>T</code></td><td>消费者，接收 <code>T</code></td></tr><tr><td><strong>关键字</strong></td><td><code>out</code></td><td><code>in</code></td></tr><tr><td><strong>可以作为返回值？</strong></td><td>可以</td><td>不可以</td></tr><tr><td><strong>可以作为输入参数？</strong></td><td>不可以</td><td>可以</td></tr><tr><td><strong>子类型赋值关系</strong></td><td><code>Producer&lt;Dog&gt;</code> 可以赋值给 <code>Producer&lt;Animal&gt;</code></td><td><code>Consumer&lt;Animal&gt;</code> 可以赋值给 <code>Consumer&lt;Dog&gt;</code></td></tr><tr><td><strong>使用场景</strong></td><td><code>List&lt;T&gt;</code>、<code>Sequence&lt;T&gt;</code></td><td><code>Comparator&lt;T&gt;</code>、<code>Function&lt;T, R&gt;</code></td></tr></tbody></table><p><strong>记住：</strong></p><ul><li><strong>“生产者用 <code>out</code>，消费者用 <code>in</code>”</strong></li><li><strong><code>List&lt;T&gt;</code> 是 <code>out</code>，<code>Comparator&lt;T&gt;</code> 是 <code>in</code></strong></li><li><strong><code>MutableList&lt;T&gt;</code> 不能用 <code>out</code> 或 <code>in</code></strong></li></ul><h2 id="25协程"><a class="markdownIt-Anchor" href="#25协程"></a> 25.协程</h2><p>Kotlin 协程（Coroutines）是 Kotlin 提供的一种用于简化异步编程和并发编程的工具。它能够高效地管理多任务，并且与线程相比，协程的开销小、运行速度快。协程让我们能够以一种顺序的方式编写异步代码，避免了回调地狱、线程阻塞等问题。</p><h3 id="什么是协程"><a class="markdownIt-Anchor" href="#什么是协程"></a> <strong>🔹 什么是协程</strong></h3><p>协程是轻量级的线程，它们能够在单一线程中并发运行，且不会像传统的线程那样消耗过多的资源。协程本质上是挂起函数，它允许在执行过程中暂停，并且能够恢复执行。</p><h3 id="协程的基本概念"><a class="markdownIt-Anchor" href="#协程的基本概念"></a> <strong>🔹协程的基本概念</strong></h3><ul><li><strong>挂起函数（Suspending Function）</strong>：是协程的核心，允许协程暂停并恢复执行。挂起函数通常以 <code>suspend</code> 关键字声明。</li><li><strong>协程作用域（Coroutine Scope）</strong>：协程在一个特定的作用域内运行，确保协程的生命周期与作用域相关联。协程作用域的任务可以并行执行，并且在作用域结束时，所有的协程都会被取消。</li><li><strong>协程调度器（Dispatcher）</strong>：负责控制协程在哪个线程上执行。例如，<code>Dispatchers.Main</code> 表示协程在主线程执行，<code>Dispatchers.IO</code> 用于 I/O 操作。</li><li><strong>挂起点（Suspension Point）</strong>：当协程执行到某个挂起函数时，它会在该点暂停，挂起函数可能会等待某些异步操作完成后恢复执行。</li></ul><h3 id="使用协程的基本步骤"><a class="markdownIt-Anchor" href="#使用协程的基本步骤"></a> <strong>🔹 使用协程的基本步骤</strong></h3><h4 id="1添加协程依赖"><a class="markdownIt-Anchor" href="#1添加协程依赖"></a> <strong>(1)添加协程依赖</strong></h4><p>首先，你需要在 <code>build.gradle</code> 文件中添加协程库依赖：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">// 协程基础库</span><br><span class="line">implementation &#x27;org.jetbrains.kotlinx:kotlinx-coroutines-core:1.5.0&#x27;</span><br><span class="line"></span><br><span class="line">// Android 特定的协程支持库</span><br><span class="line">implementation &#x27;org.jetbrains.kotlinx:kotlinx-coroutines-android:1.5.0&#x27;</span><br></pre></td></tr></table></figure><h4 id="2启动协程"><a class="markdownIt-Anchor" href="#2启动协程"></a> <strong>(2)启动协程</strong></h4><p>你可以在不同的协程作用域内启动协程。<code>GlobalScope</code> 是最简单的方式，但在 Android 中不推荐使用。推荐在 <code>ViewModel</code> 中使用 <code>viewModelScope</code>，或者在 <code>Activity</code> 和 <code>Fragment</code> 中使用 <code>lifecycleScope</code>。</p><p><strong>使用 <code>GlobalScope</code> 启动一个协程（不推荐在 UI 线程中使用）：</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="comment">// 在后台线程启动协程</span></span><br><span class="line">    GlobalScope.launch &#123;</span><br><span class="line">        println(<span class="string">&quot;Start Coroutine&quot;</span>)</span><br><span class="line">        delay(<span class="number">1000L</span>) <span class="comment">// 延迟 1 秒</span></span><br><span class="line">        println(<span class="string">&quot;End Coroutine&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong><code>launch</code></strong>：启动一个协程，返回一个 <code>Job</code> 对象，用于管理协程的生命周期。</li><li><strong><code>delay</code></strong>：挂起当前协程，但不会阻塞线程，其他协程可以继续运行。</li></ul><h4 id="3挂起函数"><a class="markdownIt-Anchor" href="#3挂起函数"></a> <strong>(3)挂起函数</strong></h4><p>协程中的挂起函数是协程的核心，可以暂停协程的执行，并在异步操作完成后恢复。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> = runBlocking &#123;  <span class="comment">// runBlocking 用于在主线程启动协程</span></span><br><span class="line">    println(<span class="string">&quot;Start Coroutine&quot;</span>)</span><br><span class="line">    <span class="keyword">val</span> result = fetchDataFromNetwork()</span><br><span class="line">    println(<span class="string">&quot;Data fetched: <span class="variable">$result</span>&quot;</span>)</span><br><span class="line">    println(<span class="string">&quot;End Coroutine&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">suspend</span> <span class="function"><span class="keyword">fun</span> <span class="title">fetchDataFromNetwork</span><span class="params">()</span></span>: String &#123;</span><br><span class="line">    delay(<span class="number">2000L</span>) <span class="comment">// 模拟网络请求延迟</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;Network Data&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong><code>runBlocking</code></strong>：用于启动一个协程并阻塞当前线程，通常在顶层函数中使用来启动协程。</li><li><strong><code>suspend</code></strong>：修饰挂起函数，将该该函数挂起使其可以在<strong>runBlocking</strong>协程作用域中调用。</li><li><strong><code>delay</code></strong>：不会阻塞线程，它只会挂起当前协程。</li></ul><h4 id="4使用不同的协程作用域"><a class="markdownIt-Anchor" href="#4使用不同的协程作用域"></a> <strong>(4)使用不同的协程作用域</strong></h4><p>在 Android 开发中，我们通常会使用 <code>lifecycleScope</code> 和 <code>viewModelScope</code> 来启动协程。</p><p><strong>在 <code>Activity</code> 或 <code>Fragment</code> 中使用 <code>lifecycleScope</code>：</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">lifecycleScope.launch &#123;</span><br><span class="line">    <span class="keyword">val</span> result = fetchDataFromNetwork()</span><br><span class="line">    <span class="comment">// 更新 UI</span></span><br><span class="line">    textView.text = result</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong><code>lifecycleScope</code></strong>：与 Activity 或 Fragment 的生命周期绑定，协程会在生命周期结束时自动取消。</li></ul><p><strong>在 <code>ViewModel</code> 中使用 <code>viewModelScope</code>：</strong></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">fetchData</span><span class="params">()</span></span> &#123;</span><br><span class="line">        viewModelScope.launch &#123;</span><br><span class="line">            <span class="keyword">val</span> result = fetchDataFromNetwork()</span><br><span class="line">            <span class="comment">// 更新 UI 或 LiveData</span></span><br><span class="line">            _data.value = result</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong><code>viewModelScope</code></strong>：与 ViewModel 的生命周期绑定，当 ViewModel 被清除时，所有协程会自动取消。</li></ul><h4 id="5使用-async-和-await-进行并行操作"><a class="markdownIt-Anchor" href="#5使用-async-和-await-进行并行操作"></a> <strong>(5)使用 <code>async</code> 和 <code>await</code> 进行并行操作</strong></h4><p><code>async</code> 和 <code>await</code> 用于同时启动多个协程，并在完成时返回结果。<code>async</code> 会返回一个 <code>Deferred</code> 对象，<code>await</code> 用来获取返回值。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> = runBlocking &#123;</span><br><span class="line">    <span class="keyword">val</span> deferred1 = async &#123; fetchDataFromNetwork() &#125;</span><br><span class="line">    <span class="keyword">val</span> deferred2 = async &#123; fetchDataFromNetwork() &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 等待两个协程都完成</span></span><br><span class="line">    <span class="keyword">val</span> result1 = deferred1.await()</span><br><span class="line">    <span class="keyword">val</span> result2 = deferred2.await()</span><br><span class="line"></span><br><span class="line">    println(<span class="string">&quot;Result 1: <span class="variable">$result1</span>&quot;</span>)</span><br><span class="line">    println(<span class="string">&quot;Result 2: <span class="variable">$result2</span>&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">suspend</span> <span class="function"><span class="keyword">fun</span> <span class="title">fetchDataFromNetwork</span><span class="params">()</span></span>: String &#123;</span><br><span class="line">    delay(<span class="number">1000L</span>)</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;Data from network&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong><code>async</code></strong>：启动一个并发协程并返回 <code>Deferred</code> 对象。</li><li><strong><code>await</code></strong>：等待协程的结果。</li></ul><h4 id="6使用-withcontext-切换协程上下文"><a class="markdownIt-Anchor" href="#6使用-withcontext-切换协程上下文"></a> <strong>(6)使用 <code>withContext</code> 切换协程上下文</strong></h4><p>有时你需要切换协程执行的线程上下文，例如在网络请求后将结果更新到 UI 线程上。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> = runBlocking &#123;</span><br><span class="line">    <span class="keyword">val</span> result = withContext(Dispatchers.IO) &#123;</span><br><span class="line">        fetchDataFromNetwork()  <span class="comment">// 在 IO 线程执行</span></span><br><span class="line">    &#125;</span><br><span class="line">    withContext(Dispatchers.Main) &#123;</span><br><span class="line">        println(<span class="string">&quot;Data fetched: <span class="variable">$result</span>&quot;</span>)  <span class="comment">// 更新 UI</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">suspend</span> <span class="function"><span class="keyword">fun</span> <span class="title">fetchDataFromNetwork</span><span class="params">()</span></span>: String &#123;</span><br><span class="line">    delay(<span class="number">2000L</span>)  <span class="comment">// 模拟网络请求延迟</span></span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;Network Data&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong><code>withContext(Dispatchers.IO)</code></strong>：切换到 I/O 线程来执行耗时操作。</li><li><strong><code>withContext(Dispatchers.Main)</code></strong>：切换到主线程（UI 线程）来更新 UI。</li></ul><h3 id="协程异常处理"><a class="markdownIt-Anchor" href="#协程异常处理"></a> <strong>🔹协程异常处理</strong></h3><p>Kotlin 协程还提供了异常处理机制。你可以使用 <code>try-catch</code> 块来捕获协程内部的异常。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> = runBlocking &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        launch &#123;</span><br><span class="line">            <span class="comment">// 这里会抛出异常</span></span><br><span class="line">            <span class="keyword">throw</span> Exception(<span class="string">&quot;Something went wrong&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (e: Exception) &#123;</span><br><span class="line">        println(<span class="string">&quot;Caught exception: <span class="subst">$&#123;e.message&#125;</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="协程的取消"><a class="markdownIt-Anchor" href="#协程的取消"></a> <strong>🔹协程的取消</strong></h3><p>协程支持取消操作。通过调用 <code>Job.cancel()</code> 方法可以取消协程，<code>cancel()</code> 会使协程挂起并且不会继续执行。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> job = GlobalScope.launch &#123;</span><br><span class="line">    repeat(<span class="number">1000</span>) &#123; i -&gt;</span><br><span class="line">        println(<span class="string">&quot;Coroutine <span class="variable">$i</span>&quot;</span>)</span><br><span class="line">        delay(<span class="number">500L</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">job.cancel()  <span class="comment">// 取消协程</span></span><br></pre></td></tr></table></figure><hr /><h2 id="26dsl构建专有的语法结构"><a class="markdownIt-Anchor" href="#26dsl构建专有的语法结构"></a> 26.DSL构建专有的语法结构</h2><p>DSL 是专为特定领域（如数据库查询、配置文件、UI布局等）设计的编程语言或语言的一个子集。Kotlin 提供了丰富的支持，帮助开发者用 Kotlin 代码构建出自己的 DSL 语言结构，以使得代码更加易读和易写。</p><ul><li><strong>扩展函数</strong>：你可以通过扩展函数给现有类添加自定义功能，从而实现新的语法结构。</li><li><strong>高阶函数</strong>：Kotlin 允许将函数作为参数传递给其他函数，这使得定义和构建 DSL 成为可能。</li><li><strong>Lambda 表达式</strong>：你可以通过 lambda 表达式使代码更加简洁，形成类似声明式的语法。</li><li><strong>智能类型推断</strong>：这使得你无需显式声明类型，语法更清晰，推导更自然。</li></ul><h3 id="lambda-with-receiver接收者-lambda"><a class="markdownIt-Anchor" href="#lambda-with-receiver接收者-lambda"></a> 🔹<strong>Lambda with Receiver（接收者 lambda）</strong></h3><p>接收者 lambda 允许在 lambda 中访问外部对象的成员，而不需要显式的引用对象。通过这种方式，你可以通过一个语法块来访问和操作对象，类似于嵌套调用的结构。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">DSL</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">sayHello</span><span class="params">()</span></span> = println(<span class="string">&quot;Hello, World!&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">dslExample</span><span class="params">(<span class="keyword">init</span>: <span class="type">DSL</span>.() -&gt; <span class="type">Unit</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> dsl = DSL()</span><br><span class="line">    dsl.<span class="keyword">init</span>()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    dslExample &#123;</span><br><span class="line">        sayHello()  <span class="comment">// 直接调用 DSL 中的方法，类似于在 &quot;this&quot; 上调用方法</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中，<code>init</code> 是一个扩展函数 <code>DSL.() -&gt; Unit</code>，其中 <code>DSL</code> 是接收者类型，而 <code>init</code> 是一个 lambda 表达式，它可以直接访问 <code>DSL</code> 类的成员。</p><h3 id="构建层次结构"><a class="markdownIt-Anchor" href="#构建层次结构"></a> 🔹<strong>构建层次结构</strong></h3><p>你可以使用 lambda 表达式来构建一个嵌套的结构，这在构建树形结构或配置文件时特别有用。例如，构建 UI 或 HTML 模板时，嵌套的 DSL 会让代码看起来更简洁。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">HtmlBuilder</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> children = mutableListOf&lt;String&gt;()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">div</span><span class="params">(<span class="keyword">init</span>: <span class="type">HtmlBuilder</span>.() -&gt; <span class="type">Unit</span>)</span></span> &#123;</span><br><span class="line">        <span class="keyword">val</span> builder = HtmlBuilder()</span><br><span class="line">        builder.<span class="keyword">init</span>()</span><br><span class="line">        children.add(<span class="string">&quot;&lt;div&gt;<span class="subst">$&#123;builder&#125;</span>&lt;/div&gt;&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">toString</span><span class="params">()</span></span>: String = children.joinToString(<span class="string">&quot;&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">html</span><span class="params">(<span class="keyword">init</span>: <span class="type">HtmlBuilder</span>.() -&gt; <span class="type">Unit</span>)</span></span>: String &#123;</span><br><span class="line">    <span class="keyword">val</span> builder = HtmlBuilder()</span><br><span class="line">    builder.<span class="keyword">init</span>()</span><br><span class="line">    <span class="keyword">return</span> builder.toString()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> htmlContent = html &#123;</span><br><span class="line">        div &#123;</span><br><span class="line">            div &#123;</span><br><span class="line">                println(<span class="string">&quot;Nested Div&quot;</span>)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    println(htmlContent)  <span class="comment">// 输出 &lt;div&gt;&lt;div&gt;&lt;div&gt;Nested Div&lt;/div&gt;&lt;/div&gt;&lt;/div&gt;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里，我们创建了一个简单的 HTML 构建器 DSL。<code>div</code> 函数会在 <code>HtmlBuilder</code> 类中创建一个新的 <code>div</code> 标签，并可以嵌套其他标签。你可以在外部 <code>html</code> 块中定义结构，从而形成一个层次化的结构。</p><h3 id="操作符重载"><a class="markdownIt-Anchor" href="#操作符重载"></a> 🔹<strong>操作符重载</strong></h3><p>在 Kotlin 中，你可以重载操作符，创建自定义的语法。这让你能够使用自然的语法来表达一些操作。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Vector</span>(<span class="keyword">val</span> x: <span class="built_in">Int</span>, <span class="keyword">val</span> y: <span class="built_in">Int</span>) &#123;</span><br><span class="line">    <span class="keyword">operator</span> <span class="function"><span class="keyword">fun</span> <span class="title">plus</span><span class="params">(v: <span class="type">Vector</span>)</span></span> = Vector(x + v.x, y + v.y)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> vector1 = Vector(<span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line">    <span class="keyword">val</span> vector2 = Vector(<span class="number">3</span>, <span class="number">4</span>)</span><br><span class="line">    <span class="keyword">val</span> result = vector1 + vector2  <span class="comment">// 使用重载的 plus 操作符</span></span><br><span class="line">    println(<span class="string">&quot;Result: (<span class="subst">$&#123;result.x&#125;</span>, <span class="subst">$&#123;result.y&#125;</span>)&quot;</span>)  <span class="comment">// 输出 Result: (4, 6)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中，通过重载 <code>plus</code> 操作符，你可以使用 <code>+</code> 来直接操作自定义的类 <code>Vector</code>，使得代码更具有直观性和简洁性。</p><h3 id="kotlin-中构建-dsl-的常见应用"><a class="markdownIt-Anchor" href="#kotlin-中构建-dsl-的常见应用"></a> 🔹 Kotlin 中构建 DSL 的常见应用</h3><ul><li><strong>HTML DSL</strong>：用于生成 HTML 文档，类似于上述的例子，可以通过嵌套的 lambda 构建 HTML 标签。</li><li><strong>UI 构建器</strong>：许多 UI 框架（如 Jetpack Compose）使用 DSL 来简化 UI 组件的声明式布局。</li><li><strong>配置文件</strong>：可以为 JSON、YAML 或 XML 等配置格式创建 DSL，使其更加自然易读。</li><li><strong>构建工具</strong>：如 Gradle 就是使用 Kotlin DSL 来定义构建脚本。</li></ul><h3 id="示例kotlin-dsl-构建数据库查询"><a class="markdownIt-Anchor" href="#示例kotlin-dsl-构建数据库查询"></a> 🔹示例：Kotlin DSL 构建数据库查询</h3><p>假设你想要构建一个用于数据库查询的 DSL，使得查询语句更加优雅和简洁。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">QueryBuilder</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> conditions = mutableListOf&lt;String&gt;()</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">where</span><span class="params">(condition: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        conditions.add(<span class="string">&quot;WHERE <span class="variable">$condition</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">and</span><span class="params">(condition: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        conditions.add(<span class="string">&quot;AND <span class="variable">$condition</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">or</span><span class="params">(condition: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        conditions.add(<span class="string">&quot;OR <span class="variable">$condition</span>&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">build</span><span class="params">()</span></span>: String = <span class="string">&quot;SELECT * FROM table <span class="subst">$&#123;conditions.joinToString(<span class="string">&quot; &quot;</span>)&#125;</span>&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">query</span><span class="params">(<span class="keyword">init</span>: <span class="type">QueryBuilder</span>.() -&gt; <span class="type">Unit</span>)</span></span>: String &#123;</span><br><span class="line">    <span class="keyword">val</span> builder = QueryBuilder()</span><br><span class="line">    builder.<span class="keyword">init</span>()</span><br><span class="line">    <span class="keyword">return</span> builder.build()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">val</span> query = query &#123;</span><br><span class="line">        <span class="keyword">where</span>(<span class="string">&quot;age &gt; 30&quot;</span>)</span><br><span class="line">        and(<span class="string">&quot;name = &#x27;John&#x27;&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    println(query)  <span class="comment">// 输出: SELECT * FROM table WHERE age &gt; 30 AND name = &#x27;John&#x27;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr />]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1变量&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#1变量&quot;&gt;&lt;/a&gt; 1.变量&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;val&lt;/strong&gt;:不可变的变量&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;var&lt;/strong&gt;:可变的变量&lt;/p&gt;
&lt;f</summary>
      
    
    
    
    
    <category term="Kotlin" scheme="https://11111-beep.github.io/tags/Kotlin/"/>
    
  </entry>
  
  <entry>
    <title>第一行代码-Java</title>
    <link href="https://11111-beep.github.io/2025/08/24/%E7%AC%AC%E4%B8%80%E8%A1%8C%E4%BB%A3%E7%A0%81-Java/"/>
    <id>https://11111-beep.github.io/2025/08/24/%E7%AC%AC%E4%B8%80%E8%A1%8C%E4%BB%A3%E7%A0%81-Java/</id>
    <published>2025-08-24T09:13:05.000Z</published>
    <updated>2025-12-14T09:17:52.873Z</updated>
    
    <content type="html"><![CDATA[<h2 id="android应用开发特色"><a class="markdownIt-Anchor" href="#android应用开发特色"></a> Android应用开发特色</h2><p>四大组件:活动(Activity),服务(Service),广播接收器(Broadcast Receiver)和内容提供器(Content Provider)。</p><hr /><h2 id="引入布局"><a class="markdownIt-Anchor" href="#引入布局"></a> 引入布局</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span>&#123;</span><br><span class="line">    <span class="built_in">super</span>.onCreate(saveInstanceState);</span><br><span class="line">    setContentView(R.layout.hello_world_layout);<span class="comment">//引入布局</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">R.string.app_name<span class="comment">//字符串引用</span></span><br><span class="line">R.drawable.app_name<span class="comment">//图片引用</span></span><br><span class="line">R.mipmap.app_name<span class="comment">//应用图标引用</span></span><br><span class="line">R.layout.app_name<span class="comment">//布局文件引用</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">@string/app_name//字符串引用</span><br><span class="line">@drawable/app_name//图片引用</span><br><span class="line">@mipmap/app_name//应用图标引用</span><br><span class="line">@layout/app_name//布局文件引用</span><br></pre></td></tr></table></figure><hr /><h2 id="log日志打印"><a class="markdownIt-Anchor" href="#log日志打印"></a> Log日志打印</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">级别由高到低</span><br><span class="line">Log.v()<span class="comment">//打印繁琐意义小的日志信息</span></span><br><span class="line">Log.d()<span class="comment">//打印调试信息</span></span><br><span class="line">Log.i()<span class="comment">//打印比较重要的数据</span></span><br><span class="line">Log.w()<span class="comment">//打印一些警告信息</span></span><br><span class="line">Log.e()<span class="comment">//打印错误信息</span></span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">TAG</span> <span class="operator">=</span> <span class="string">&quot;HelloWorldActivity&quot;</span></span><br><span class="line">Log.d(<span class="string">&quot;TAG&quot;</span>, <span class="string">&quot;hahaha&quot;</span>)</span><br><span class="line"><span class="comment">//第一个tag传入类名,第二个msg传入想打印的内容</span></span><br><span class="line">    </span><br><span class="line">快捷键:logd</span><br></pre></td></tr></table></figure><hr /><h2 id="设置主活动"><a class="markdownIt-Anchor" href="#设置主活动"></a> 设置主活动</h2><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">&quot;android.intent.action.MAIN&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">category</span> <span class="attr">android:name</span>=<span class="string">&quot;android.intent.category.LAUNCHER&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br></pre></td></tr></table></figure><hr /><h2 id="方法重写"><a class="markdownIt-Anchor" href="#方法重写"></a> 方法重写</h2><p>快捷键:Ctrl+O</p><hr /><h2 id="toast"><a class="markdownIt-Anchor" href="#toast"></a> Toast</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Toast.makeText(LoginActivity.<span class="built_in">this</span>, <span class="string">&quot;登录成功啦!&quot;</span>, Toast.LENGTH_SHORT).show();</span><br></pre></td></tr></table></figure><hr /><h2 id="菜单设置menu"><a class="markdownIt-Anchor" href="#菜单设置menu"></a> 菜单设置(menu)</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">重写onCreateOptionsMenu和onOptionsItemSelected(<span class="keyword">if</span> <span class="keyword">else</span>)方法</span><br></pre></td></tr></table></figure><p>修改themes.xml文件</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">resources</span> <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- Base application theme. --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">style</span> <span class="attr">name</span>=<span class="string">&quot;Base.Theme.NoteTest&quot;</span> <span class="attr">parent</span>=<span class="string">&quot;Theme.MaterialComponents.DayNight.DarkActionBar&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure><hr /><h2 id="intent跳转活动"><a class="markdownIt-Anchor" href="#intent跳转活动"></a> intent跳转活动</h2><h3 id="1显式intent"><a class="markdownIt-Anchor" href="#1显式intent"></a> 1.显式intent</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> Button login; </span><br><span class="line">login.setOnClickListener(<span class="keyword">new</span> <span class="title class_">View</span>.OnClickListener() &#123; <span class="comment">//登录按钮的监听器</span></span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">                    <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(LoginActivity.<span class="built_in">this</span>, MainActivity.class);</span><br><span class="line">                    startActivity(intent);  <span class="comment">// 启动主界面活动</span></span><br><span class="line">                    finish();  <span class="comment">// 结束当前活动</span></span><br><span class="line">                &#125;</span><br></pre></td></tr></table></figure><h3 id="2隐式intent"><a class="markdownIt-Anchor" href="#2隐式intent"></a> 2.隐式intent</h3><h4 id="androidmanifestxml"><a class="markdownIt-Anchor" href="#androidmanifestxml"></a> AndroidManifest.xml</h4><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">activity</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.SecondActivity&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:exported</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:launchMode</span>=<span class="string">&quot;singleInstance&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">&quot;com.example.activitytest.ACTION_START&quot;</span> /&gt;</span> //action</span><br><span class="line">                <span class="tag">&lt;<span class="name">category</span> <span class="attr">android:name</span>=<span class="string">&quot;android.intent.category.DEFAULT&quot;</span> /&gt;</span> //category默认附加信息</span><br><span class="line">                <span class="tag">&lt;<span class="name">category</span> <span class="attr">android:name</span>=<span class="string">&quot;com.example.activitytest.MY_CATEGORY&quot;</span> /&gt;</span> //category默认附加信息</span><br><span class="line">            <span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">activity</span>&gt;</span></span><br></pre></td></tr></table></figure><p>要同时匹配action和category才能响应该Intent</p><h4 id="firstactivityjava"><a class="markdownIt-Anchor" href="#firstactivityjava"></a> FirstActivity.java</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">button1.setOnClickListener(<span class="keyword">new</span> <span class="title class_">View</span>.OnClickListener()&#123;<span class="comment">//注册监听器并调用onClick()方法</span></span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span>&#123;  <span class="comment">//按钮点击的方法   </span></span><br><span class="line">                Intent intent=<span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="string">&quot;com.example.activitytest.ACTION_START&quot;</span>);</span><br><span class="line">                intent.addCategory(<span class="string">&quot;com.example.activitytest.MY_CATEGORY&quot;</span>);</span><br><span class="line"></span><br><span class="line">                Intent intent=<span class="keyword">new</span> <span class="title class_">Intent</span>(Intent.ACTION_VIEW);</span><br><span class="line">                intent.setData(Uri.parse(<span class="string">&quot;http://www.baidu.com&quot;</span>)); <span class="comment">//打开百度</span></span><br><span class="line">               </span><br><span class="line">                startActivity(intent);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 匿名 new View.OnClickListener() 可被替换为 lambda</span></span><br><span class="line">button1.setOnClickListener(v -&gt; &#123;</span><br><span class="line">            Intent intent=<span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="string">&quot;com.example.activitytest.ACTION_START&quot;</span>);</span><br><span class="line">            intent.addCategory(<span class="string">&quot;com.example.activitytest.MY_CATEGORY&quot;</span>);</span><br><span class="line"></span><br><span class="line">            Intent intent=<span class="keyword">new</span> <span class="title class_">Intent</span>(Intent.ACTION_VIEW);</span><br><span class="line">            intent.setData(Uri.parse(<span class="string">&quot;http://www.baidu.com&quot;</span>));</span><br><span class="line"></span><br><span class="line">            <span class="comment">//startActivityForResult(intent,1);</span></span><br><span class="line">            startActivity(intent);</span><br><span class="line">        &#125;);</span><br></pre></td></tr></table></figure><hr /><h2 id="传递数据"><a class="markdownIt-Anchor" href="#传递数据"></a> 传递数据</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> EditText editText;</span><br><span class="line"><span class="comment">//onCreate方法里面添加</span></span><br><span class="line">editText=(EditText)findViewById(R.id.edit_text) <span class="comment">//相对应的布局</span></span><br><span class="line"><span class="type">String</span> <span class="variable">inputText</span> <span class="operator">=</span> editText.getText().toString(); <span class="comment">//获取输入的内容</span></span><br></pre></td></tr></table></figure><h3 id="1传出数据"><a class="markdownIt-Anchor" href="#1传出数据"></a> 1.传出数据</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">holder.rlContainer.setOnClickListener(v -&gt; &#123;</span><br><span class="line">            <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(mContext, EditActivity.class);</span><br><span class="line">            intent.putExtra(<span class="string">&quot;extra_note&quot;</span>, note);  <span class="comment">// 传递数据(键,真正的数据)</span></span><br><span class="line">            mContext.startActivity(intent);</span><br><span class="line">            <span class="comment">// startActivity(intent);</span></span><br><span class="line">        &#125;);</span><br></pre></td></tr></table></figure><h3 id="2获取数据"><a class="markdownIt-Anchor" href="#2获取数据"></a> 2.获取数据</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> getIntent();  <span class="comment">// 获取启动该活动时传递的 Intent</span></span><br><span class="line">note = (Note) intent.getSerializableExtra(<span class="string">&quot;extra_note&quot;</span>);  <span class="comment">// 获取传递的笔记对象</span></span><br></pre></td></tr></table></figure><hr /><h2 id="返回数据给上一个活动"><a class="markdownIt-Anchor" href="#返回数据给上一个活动"></a> 返回数据给上一个活动</h2><p>startActivityForResult方法(FirstActivity)</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">IntentFirstActivity</span>.<span class="built_in">this</span>, SecondActivity.class);</span><br><span class="line">startActivityForResult(intent,<span class="number">1</span>); <span class="comment">//通过startActivityForResult启动活动,请求码为1</span></span><br></pre></td></tr></table></figure><p>1.点击按钮返回数据:setResult方法(SecondActivity)</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">holder.rlContainer.setOnClickListener(v -&gt; &#123;</span><br><span class="line">            <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(mContext, EditActivity.class);</span><br><span class="line">            intent.putExtra(<span class="string">&quot;note&quot;</span>, note);  <span class="comment">// 传递数据</span></span><br><span class="line">            setResult(RESULT_OK, intent);</span><br><span class="line">            finish();</span><br><span class="line">        &#125;);</span><br></pre></td></tr></table></figure><p>2.back返回数据:重写onBackPressed方法(SecondActivity)</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onBackPressed</span><span class="params">()</span>&#123;</span><br><span class="line">    Intent intent=<span class="keyword">new</span> <span class="title class_">Intent</span>();</span><br><span class="line">    intent.putExtra(<span class="string">&quot;data_return&quot;</span>,<span class="string">&quot;Hello FirstActivity&quot;</span>);</span><br><span class="line">    setResult(RESULT_OK,intent);</span><br><span class="line">    finish();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>重写onActivityResult方法(FirstActivity)</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@SuppressLint(&quot;MissingSuperCall&quot;)</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onActivityResult</span><span class="params">(<span class="type">int</span> requestCode, <span class="type">int</span> resultCode,Intent data)</span> &#123;</span><br><span class="line">        <span class="keyword">switch</span> (requestCode)&#123;</span><br><span class="line">            <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">                <span class="keyword">if</span>(resultCode==RESULT_OK)&#123;</span><br><span class="line">                    String returnedData=data.getStringExtra(<span class="string">&quot;data_return&quot;</span>);</span><br><span class="line">                    Log.d(<span class="string">&quot;FirstActivity&quot;</span>,returnedData);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">default</span>:</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="活动的生命周期"><a class="markdownIt-Anchor" href="#活动的生命周期"></a> 活动的生命周期</h2><p>注:如果活动是对话框式的,要设置AndroidManifest.xml的主题</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">activity</span> <span class="attr">android:name</span>=<span class="string">&quot;.DialogActivity&quot;</span></span></span><br><span class="line"><span class="tag">          <span class="attr">android:theme</span>=<span class="string">&quot;@style/Theme.AppCompat.Dialog.Alert&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">activity</span>&gt;</span></span><br></pre></td></tr></table></figure><h4 id="活动被回收前的历史数据保存"><a class="markdownIt-Anchor" href="#活动被回收前的历史数据保存"></a> 活动被回收前的历史数据保存</h4><p>使用onSaveInstanceState方法</p><hr /><h2 id="progressbar进度条"><a class="markdownIt-Anchor" href="#progressbar进度条"></a> ProgressBar进度条</h2><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ProgressBar</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/progressBar&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">style</span>=<span class="string">&quot;?android:attr/progressBarStyle&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:visibility</span>=<span class="string">&quot;gone&quot;</span> //<span class="attr">刚开始不显示</span></span></span><br><span class="line"><span class="tag">        /&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 显示 ProgressBar</span></span><br><span class="line">               progressBar.setVisibility(View.VISIBLE);</span><br><span class="line">               Intent intent=<span class="keyword">new</span> <span class="title class_">Intent</span>(LoginActivity.<span class="built_in">this</span>, DialogActivity.class);</span><br><span class="line">               <span class="comment">// 888毫秒后隐藏 ProgressBar</span></span><br><span class="line">               <span class="keyword">new</span> <span class="title class_">Handler</span>().postDelayed(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">                   <span class="meta">@Override</span></span><br><span class="line">                   <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                       progressBar.setVisibility(View.GONE); <span class="comment">// 隐藏 ProgressBar</span></span><br><span class="line">                       startActivity(intent);  <span class="comment">// 启动主界面活动</span></span><br><span class="line">                   &#125;</span><br><span class="line">               &#125;, <span class="number">888</span>); <span class="comment">// 延迟 888 毫秒</span></span><br></pre></td></tr></table></figure><hr /><h2 id="visibility控件属性"><a class="markdownIt-Anchor" href="#visibility控件属性"></a> Visibility(控件属性)</h2><h3 id="visibility-的使用场景"><a class="markdownIt-Anchor" href="#visibility-的使用场景"></a> <strong><code>visibility</code> 的使用场景</strong></h3><h4 id="1-visible"><a class="markdownIt-Anchor" href="#1-visible"></a> <strong>1. <code>visible</code></strong></h4><ul><li>当需要正常显示视图时。</li><li>默认状态下所有视图都是 <code>visible</code>。</li></ul><h4 id="2-invisible"><a class="markdownIt-Anchor" href="#2-invisible"></a> <strong>2. <code>invisible</code></strong></h4><ul><li>当需要临时隐藏视图，但又需要保留其空间时。</li><li>常用于占位布局或切换内容时。</li><li>示例：隐藏未加载的数据占位布局，避免布局重新排列。</li></ul><h4 id="3-gone"><a class="markdownIt-Anchor" href="#3-gone"></a> <strong>3. <code>gone</code></strong></h4><ul><li><p>当需要完全移除视图，并让其他视图调整布局时。</p></li><li><p>常用于条件显示的内容，例如动态菜单、可选功能等。</p></li><li><p>示例：表单中某些字段仅在用户选择某个选项时显示。</p></li></ul><hr /><h2 id="布局"><a class="markdownIt-Anchor" href="#布局"></a> 布局</h2><h3 id="线性布局linearlayout"><a class="markdownIt-Anchor" href="#线性布局linearlayout"></a> 线性布局(LinearLayout)</h3><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">android:orientation=&quot;vertical&quot; //垂直排列</span><br><span class="line">android:orientation=&quot;horizontal&quot; //水平排列</span><br><span class="line"></span><br><span class="line">android:gravity=center_vertical|center_horizontal//文字在控件中的对齐方式</span><br><span class="line">android:layout_gravity=&quot;top&quot; //控件在布局中的对齐方式</span><br><span class="line"></span><br><span class="line">android:layout_widtn=&quot;0dp&quot;</span><br><span class="line">android:layout_weight=&quot;1&quot;; //水平宽度占一份</span><br><span class="line"></span><br><span class="line">android:layoutLeft=&quot;5dp&quot; //在某个方向偏移的距离(左)</span><br></pre></td></tr></table></figure><h3 id="相对布局relativelayout"><a class="markdownIt-Anchor" href="#相对布局relativelayout"></a> 相对布局(RelativeLayout)</h3><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">相对于父布局</span><br><span class="line">android:layout_alignParentLeft=&quot;ture&quot;</span><br><span class="line">android:layout_alignParentRight=&quot;ture&quot;</span><br><span class="line">android:layout_alignParentBottom=&quot;ture&quot;</span><br><span class="line">android:layout_alignParentTop=&quot;ture&quot;</span><br><span class="line">android:layout_centerInParent=&quot;true&quot;</span><br><span class="line"></span><br><span class="line">位于控件的位置</span><br><span class="line">android:layout_above=&quot;@id/button3&quot;</span><br><span class="line">android:layout_below=&quot;@id/button3&quot;</span><br><span class="line">android:layout_toLeftOf=&quot;@id/button3&quot;</span><br><span class="line">android:layout_toRightOf=&quot;@id/button3&quot;</span><br><span class="line"></span><br><span class="line">对齐的位置</span><br><span class="line">android:layout_alignLeft=&quot;@id/button3&quot;</span><br><span class="line">android:layout_alignRight=&quot;@id/button3&quot;</span><br><span class="line">android:layout_alignBottom=&quot;@id/button3&quot;</span><br><span class="line">android:layout_alignTop=&quot;@id/button3&quot;</span><br></pre></td></tr></table></figure><h3 id="帧布局framelayout"><a class="markdownIt-Anchor" href="#帧布局framelayout"></a> 帧布局(Framelayout)</h3><p>默认情况下控件都位于左上角</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">android:layout_gravity=&quot;left&quot;</span><br><span class="line">android:layout_gravity=&quot;right&quot;</span><br></pre></td></tr></table></figure><h4 id="注所有控件都是直接或间接继承自view的所有的布局都是直接或间接继承自viewgroup的viewgroup是一种特殊的view"><a class="markdownIt-Anchor" href="#注所有控件都是直接或间接继承自view的所有的布局都是直接或间接继承自viewgroup的viewgroup是一种特殊的view"></a> 注:所有控件都是直接或间接继承自View的,所有的布局都是直接或间接继承自ViewGroup的,ViewGroup是一种特殊的View.</h4><h3 id="引入布局与自定义控件"><a class="markdownIt-Anchor" href="#引入布局与自定义控件"></a> 引入布局与自定义控件</h3><p>看书!!!</p><hr /><h2 id="recyclerview牛逼控件"><a class="markdownIt-Anchor" href="#recyclerview牛逼控件"></a> RecyclerView(牛逼控件)</h2><h3 id="1appbuildgradle文件的dependencies闭包添加依赖库"><a class="markdownIt-Anchor" href="#1appbuildgradle文件的dependencies闭包添加依赖库"></a> 1.app/build.gradle文件的dependencies闭包添加依赖库</h3><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">implementation(<span class="string">&quot;androidx.recyclerview:recyclerview:1.3.2&quot;</span>)</span><br></pre></td></tr></table></figure><h3 id="2在布局中添加recyclerview控件"><a class="markdownIt-Anchor" href="#2在布局中添加recyclerview控件"></a> 2.在布局中添加RecyclerView控件</h3><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">androidx.recyclerview.widget.RecyclerView</span></span></span><br><span class="line"><span class="tag">     <span class="attr">android:id</span>=<span class="string">&quot;@+id/rlv&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">     <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">     /&gt;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="3准备适配器"><a class="markdownIt-Anchor" href="#3准备适配器"></a> 3.准备适配器</h3><p>新建适配器MyAdapter并继承自RecyclerView.Adapter,泛型为RecyclerView.ViewHolder,ViewHolder是内部类</p><p>1.MyViewHolder继承RecyclerView.ViewHolder,在里面的MyViewHolder构造函数中传入参数view(布局)</p><p>2.MyAdapter构造函数赋值</p><p>3.重写onCreateViewHolder,onBindViewHolder,getItemCount()三个方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyAdapter</span> <span class="keyword">extends</span> <span class="title class_">RecyclerView</span>.Adapter&lt;RecyclerView.ViewHolder&gt; &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> List&lt;Note&gt; mBeanList;  <span class="comment">// 存储数据的列表</span></span><br><span class="line">   </span><br><span class="line">    <span class="keyword">private</span> Context mContext;  <span class="comment">// 上下文对象，用于访问应用资源和启动活动</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> NoteDbOpenHelper mNoteDbOpenHelper;  <span class="comment">// 数据库帮助类，用于与数据库交互</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//private int viewType;  // 当前视图类型</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义两种视图类型：线性布局和网格布局</span></span><br><span class="line">    <span class="comment">//public static int TYPE_LINEAR_LAYOUT = 0;</span></span><br><span class="line">    <span class="comment">//public static int TYPE_GRID_LAYOUT = 1;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构造方法，传入上下文和数据列表</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MyAdapter</span><span class="params">(Context context, List&lt;Note&gt; mBeanList)</span>&#123;</span><br><span class="line">        <span class="built_in">this</span>.mBeanList = mBeanList;</span><br><span class="line">        <span class="built_in">this</span>.mContext = context;</span><br><span class="line">        mLayoutInflater = LayoutInflater.from(mContext);  <span class="comment">// 初始化布局膨胀器</span></span><br><span class="line">        mNoteDbOpenHelper = <span class="keyword">new</span> <span class="title class_">NoteDbOpenHelper</span>(mContext);  <span class="comment">// 初始化数据库帮助类</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建 ViewHolder，根据视图类型加载不同的布局</span></span><br><span class="line">    <span class="meta">@NonNull</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> RecyclerView.ViewHolder <span class="title function_">onCreateViewHolder</span><span class="params">(<span class="meta">@NonNull</span> ViewGroup parent, <span class="type">int</span> viewType)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span>(viewType == TYPE_LINEAR_LAYOUT)&#123;</span><br><span class="line">            <span class="comment">// 加载线性布局</span></span><br><span class="line">            <span class="type">View</span> <span class="variable">view</span> <span class="operator">=</span> mLayoutInflater.inflate(R.layout.list_item_layout, parent, <span class="literal">false</span>);</span><br><span class="line">            <span class="type">MyViewHolder</span> <span class="variable">myViewHolder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MyViewHolder</span>(view);</span><br><span class="line">            <span class="keyword">return</span> myViewHolder;</span><br><span class="line">        &#125;<span class="keyword">else</span> <span class="keyword">if</span>(viewType == TYPE_GRID_LAYOUT)&#123;</span><br><span class="line">            <span class="comment">// 加载网格布局</span></span><br><span class="line">            <span class="type">View</span> <span class="variable">view</span> <span class="operator">=</span> mLayoutInflater.inflate(R.layout.list_item_grid_layout, parent, <span class="literal">false</span>);</span><br><span class="line">            <span class="type">MyGridViewHolder</span> <span class="variable">myGridViewHolder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MyGridViewHolder</span>(view);</span><br><span class="line">            <span class="keyword">return</span> myGridViewHolder;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;  <span class="comment">// 默认返回 null（应该永远不会执行到）</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 绑定数据到对应的 ViewHolder</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onBindViewHolder</span><span class="params">(<span class="meta">@NonNull</span> RecyclerView.ViewHolder holder, <span class="type">int</span> position)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (holder == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(holder <span class="keyword">instanceof</span> MyViewHolder)&#123;</span><br><span class="line">            <span class="comment">// 绑定线性布局的视图</span></span><br><span class="line">            bindMyViewHolder((MyViewHolder) holder, position);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (holder <span class="keyword">instanceof</span> MyGridViewHolder) &#123;</span><br><span class="line">            <span class="comment">// 绑定网格布局的视图</span></span><br><span class="line">            bindGridViewHolder((MyGridViewHolder) holder, position);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取列表项数量</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getItemCount</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> mBeanList.size();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 线性布局的 ViewHolder</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">MyViewHolder</span> <span class="keyword">extends</span> <span class="title class_">RecyclerView</span>.ViewHolder &#123;</span><br><span class="line">        TextView mTvTitle;</span><br><span class="line">        TextView mTvContent;</span><br><span class="line">        TextView mTvTime;</span><br><span class="line">        ViewGroup rlContainer;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="title function_">MyViewHolder</span><span class="params">(<span class="meta">@NonNull</span> View itemView)</span> &#123;</span><br><span class="line">            <span class="built_in">super</span>(itemView);</span><br><span class="line">            <span class="built_in">this</span>.mTvTitle = itemView.findViewById(R.id.tv_title);</span><br><span class="line">            <span class="built_in">this</span>.mTvContent = itemView.findViewById(R.id.tv_content);</span><br><span class="line">            <span class="built_in">this</span>.mTvTime = itemView.findViewById(R.id.tv_time);</span><br><span class="line">            <span class="built_in">this</span>.rlContainer = itemView.findViewById(R.id.rl_item_container);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 网格布局的 ViewHolder</span></span><br><span class="line">     <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">MyGridViewHolder</span> <span class="keyword">extends</span> <span class="title class_">RecyclerView</span>.ViewHolder &#123;</span><br><span class="line">        TextView mTvTitle;</span><br><span class="line">        TextView mTvContent;</span><br><span class="line">        TextView mTvTime;</span><br><span class="line">        ViewGroup rlContainer;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="title function_">MyGridViewHolder</span><span class="params">(<span class="meta">@NonNull</span> View itemView)</span> &#123;</span><br><span class="line">            <span class="built_in">super</span>(itemView);</span><br><span class="line">            <span class="built_in">this</span>.mTvTitle = itemView.findViewById(R.id.tv_title);</span><br><span class="line">            <span class="built_in">this</span>.mTvContent = itemView.findViewById(R.id.tv_content);</span><br><span class="line">            <span class="built_in">this</span>.mTvTime = itemView.findViewById(R.id.tv_time);</span><br><span class="line">            <span class="built_in">this</span>.rlContainer = itemView.findViewById(R.id.rl_item_container);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="4使用recyclerview"><a class="markdownIt-Anchor" href="#4使用recyclerview"></a> 4.使用RecyclerView</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> RecyclerView mRecyclerView;  <span class="comment">// RecyclerView 用于显示列表</span></span><br><span class="line">    <span class="keyword">private</span> List&lt;Note&gt; mNotes;  <span class="comment">// 存储笔记数据的列表</span></span><br><span class="line">    <span class="keyword">private</span> MyAdapter mMyAdapter;  <span class="comment">// RecyclerView 的适配器</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_main);  <span class="comment">// 设置布局文件</span></span><br><span class="line">        initView();  <span class="comment">// 初始化视图</span></span><br><span class="line">        initData();  <span class="comment">// 初始化数据</span></span><br><span class="line">        initEvent();  <span class="comment">// 初始化事件</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 根据存储的布局模式设置列表布局</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">setListLayout</span><span class="params">()</span> &#123;</span><br><span class="line">        currentListLayoutMode = SpfUtil.getIntWithDefault(<span class="built_in">this</span>, KEY_LAYOUT_MODE, MODE_LINEAR);</span><br><span class="line">        <span class="keyword">if</span> (currentListLayoutMode == MODE_LINEAR) &#123;</span><br><span class="line">            setToLinearList();  <span class="comment">// 如果是线性布局，设置为线性布局</span></span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            setToGridList();  <span class="comment">// 如果是网格布局，设置为网格布局</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 从数据库获取笔记数据并刷新适配器</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">refreshDataFromDb</span><span class="params">()</span> &#123;</span><br><span class="line">        mNotes = getDataFromDB();  <span class="comment">// 从数据库获取数据</span></span><br><span class="line">        mMyAdapter.refreshData(mNotes);  <span class="comment">// 刷新适配器的数据</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">initEvent</span><span class="params">()</span> &#123;</span><br><span class="line">        mMyAdapter = <span class="keyword">new</span> <span class="title class_">MyAdapter</span>(<span class="built_in">this</span>, mNotes);  <span class="comment">// 初始化适配器</span></span><br><span class="line">        mRecyclerView.setAdapter(mMyAdapter);  <span class="comment">// 设置 RecyclerView 的适配器</span></span><br><span class="line">        setListLayout();  <span class="comment">// 设置布局模式</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化数据</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">initData</span><span class="params">()</span> &#123;</span><br><span class="line">        mNotes = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();  <span class="comment">// 初始化笔记列表</span></span><br><span class="line">        mNoteDbOpenHelper = <span class="keyword">new</span> <span class="title class_">NoteDbOpenHelper</span>(<span class="built_in">this</span>);  <span class="comment">// 初始化数据库帮助类</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化视图</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">initView</span><span class="params">()</span> &#123;</span><br><span class="line">        mRecyclerView = findViewById(R.id.rlv);  <span class="comment">// 获取 RecyclerView 控件</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="5recyclerview的点击事件myadapter"><a class="markdownIt-Anchor" href="#5recyclerview的点击事件myadapter"></a> 5.RecyclerView的点击事件(MyAdapter)</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 设置点击事件，跳转到编辑页面</span></span><br><span class="line">       holder.rlContainer.setOnClickListener(v -&gt; &#123;</span><br><span class="line">           <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(mContext, EditActivity.class);</span><br><span class="line">           intent.putExtra(<span class="string">&quot;note&quot;</span>, note);  <span class="comment">// 传递数据</span></span><br><span class="line">           mContext.startActivity(intent);</span><br><span class="line">       &#125;);</span><br><span class="line"></span><br><span class="line">       <span class="comment">// 设置长按事件，弹出弹窗</span></span><br><span class="line">       holder.rlContainer.setOnLongClickListener(v -&gt; &#123;</span><br><span class="line">           <span class="type">Dialog</span> <span class="variable">dialog</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Dialog</span>(mContext, android.R.style.ThemeOverlay_Material_Dialog_Alert);</span><br><span class="line">           <span class="type">View</span> <span class="variable">dialogView</span> <span class="operator">=</span> mLayoutInflater.inflate(R.layout.list_item_dialog_layout, <span class="literal">null</span>);</span><br><span class="line">           <span class="type">TextView</span> <span class="variable">tvDelete</span> <span class="operator">=</span> dialogView.findViewById(R.id.tv_delete);</span><br><span class="line">           <span class="type">TextView</span> <span class="variable">tvEdit</span> <span class="operator">=</span> dialogView.findViewById(R.id.tv_edit);</span><br><span class="line"></span><br><span class="line">           <span class="comment">// 删除操作</span></span><br><span class="line">           tvDelete.setOnClickListener(v1 -&gt; &#123;</span><br><span class="line">               <span class="type">int</span> <span class="variable">row</span> <span class="operator">=</span> mNoteDbOpenHelper.deleteFromDbById(note.getId());  <span class="comment">// 从数据库删除</span></span><br><span class="line">               <span class="keyword">if</span> (row &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                   removeData(position);  <span class="comment">// 从列表中移除</span></span><br><span class="line">               &#125;</span><br><span class="line">               dialog.dismiss();  <span class="comment">// 关闭对话框</span></span><br><span class="line">           &#125;);</span><br><span class="line"></span><br><span class="line">           <span class="comment">// 编辑操作</span></span><br><span class="line">           tvEdit.setOnClickListener(v2 -&gt; &#123;</span><br><span class="line">               <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(mContext, EditActivity.class);</span><br><span class="line">               intent.putExtra(<span class="string">&quot;note&quot;</span>, note);  <span class="comment">// 传递数据</span></span><br><span class="line">               mContext.startActivity(intent);</span><br><span class="line">               dialog.dismiss();  <span class="comment">// 关闭对话框</span></span><br><span class="line">           &#125;);</span><br><span class="line"></span><br><span class="line">           dialog.setContentView(dialogView);  <span class="comment">// 设置对话框内容</span></span><br><span class="line">           dialog.setCanceledOnTouchOutside(<span class="literal">true</span>);  <span class="comment">// 点击外部区域关闭对话框</span></span><br><span class="line">           dialog.show();  <span class="comment">// 显示对话框</span></span><br><span class="line">           <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">       &#125;);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="碎片fragment"><a class="markdownIt-Anchor" href="#碎片fragment"></a> 碎片(Fragment)</h2><h3 id="简单用法"><a class="markdownIt-Anchor" href="#简单用法"></a> 简单用法</h3><p>新建left_fragment.xml和right_fragment_xml(和普通的一样)</p><p>新建LeftFragment和RightFragment类,继承自Fragment</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LeftFragment</span> <span class="keyword">extends</span> <span class="title class_">Fragment</span> &#123;</span><br><span class="line">    <span class="meta">@Nullable</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> View <span class="title function_">onCreateView</span><span class="params">(<span class="meta">@NonNull</span> LayoutInflater inflater, <span class="meta">@Nullable</span> ViewGroup container, <span class="meta">@Nullable</span> Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        View view=inflater.inflate(R.layout.left_fragment,container,<span class="literal">false</span>);<span class="comment">/*</span></span><br><span class="line"><span class="comment">1.整体功能(添加多层布局)</span></span><br><span class="line"><span class="comment">作用：将 R.layout.left_fragment（一个 XML 布局文件）解析为对应的 View 对象。</span></span><br><span class="line"><span class="comment">返回值：解析后的根视图对象（View 类型），可以用来操作 UI 元素或返回到调用者。</span></span><br><span class="line"><span class="comment">2. 参数解析</span></span><br><span class="line"><span class="comment">(1) R.layout.left_fragment</span></span><br><span class="line"><span class="comment">定义：指定要加载的布局文件的资源 ID，通常在 res/layout 文件夹中定义。</span></span><br><span class="line"><span class="comment">类型：一个整数常量（资源 ID），由 Android 在编译时生成。</span></span><br><span class="line"><span class="comment">作用：告诉 LayoutInflater 需要加载哪个 XML 布局文件。</span></span><br><span class="line"><span class="comment">(2) container</span></span><br><span class="line"><span class="comment">定义：ViewGroup 类型，表示布局的父容器（容器的视图层级）。</span></span><br><span class="line"><span class="comment">来源：通常是当前布局所在的父容器，常见于 Fragment 或 RecyclerView 中。</span></span><br><span class="line"><span class="comment">作用：为加载的布局提供参考信息，例如继承父容器的布局参数（如 match_parent、wrap_content 等）。</span></span><br><span class="line"><span class="comment">注意：如果不需要父容器的布局信息，可以传 null。</span></span><br><span class="line"><span class="comment">(3) false</span></span><br><span class="line"><span class="comment">定义：一个布尔值，表示是否将解析后的布局直接添加到父容器中。</span></span><br><span class="line"><span class="comment">作用：</span></span><br><span class="line"><span class="comment">true：LayoutInflater 会自动将解析的 View 添加到父容器中。</span></span><br><span class="line"><span class="comment">false：不会添加到父容器，只返回解析后的 View。</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">        <span class="keyword">return</span> view;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>修改activity_main.xml</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;horizontal&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">fragment</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/left_fragment&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:name</span>=<span class="string">&quot;com.example.fragmenttest.LeftFragment&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">fragment</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/right_fragment&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:name</span>=<span class="string">&quot;com.example.fragmenttest.RightFragment&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="动态加载碎片"><a class="markdownIt-Anchor" href="#动态加载碎片"></a> 动态加载碎片</h3><p>新建another_right_fragment.xml</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:background</span>=<span class="string">&quot;#ffff00&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_gravity</span>=<span class="string">&quot;center_horizontal&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:textSize</span>=<span class="string">&quot;20sp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;This is anothor right fragment&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><p>修改activity_main_xml</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;horizontal&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">fragment</span>  &lt;!<span class="attr">--碎片</span>  <span class="attr">--</span>&gt;</span></span><br><span class="line">        android:id=&quot;@+id/left_fragment&quot;</span><br><span class="line">        android:name=&quot;com.example.fragmenttest.LeftFragment&quot;</span><br><span class="line">        android:layout_width=&quot;match_parent&quot;</span><br><span class="line">        android:layout_height=&quot;match_parent&quot; /&gt;</span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">FrameLayout</span> &lt;!<span class="attr">--布局</span>  <span class="attr">--</span>&gt;</span></span><br><span class="line">        android:id=&quot;@+id/right_layout&quot;</span><br><span class="line">        android:layout_width=&quot;0dp&quot;</span><br><span class="line">        android:layout_height=&quot;match_parent&quot;</span><br><span class="line">        android:layout_weight=&quot;1&quot;&gt;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">FrameLayout</span>&gt;</span></span><br><span class="line">    </span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><p>修改MainActivity</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> <span class="keyword">implements</span> <span class="title class_">View</span>.OnClickListener&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line">        Button button=(Button)findViewById(R.id.button);<span class="comment">//不用管</span></span><br><span class="line">        button.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">        replaceFragment(<span class="keyword">new</span> <span class="title class_">RightFragment</span>());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">            <span class="keyword">if</span>(v.getId()==R.id.button)&#123;</span><br><span class="line">                replaceFragment(<span class="keyword">new</span> <span class="title class_">AnotherRightFragment</span>());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">replaceFragment</span><span class="params">(Fragment fragment)</span>&#123;</span><br><span class="line">        FragmentManager fragmentManager=getSupportFragmentManager();</span><br><span class="line">        FragmentTransaction transaction=fragmentManager.beginTransaction();</span><br><span class="line">        transaction.replace(R.id.right_layout,fragment);</span><br><span class="line">        transaction.addToBackStack(<span class="literal">null</span>); <span class="comment">//提供返回栈</span></span><br><span class="line">        transaction.commit();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="碎片与活动之间进行通信"><a class="markdownIt-Anchor" href="#碎片与活动之间进行通信"></a> 碎片与活动之间进行通信</h3><h4 id="在活动中调用碎片里的方法"><a class="markdownIt-Anchor" href="#在活动中调用碎片里的方法"></a> 在活动中调用碎片里的方法</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 从布局文件中获取碎片的实例</span></span><br><span class="line"><span class="type">RighrFragment</span> <span class="variable">rightFragment</span> <span class="operator">=</span> (RightFragment)getSupportFragmentManager().findFragmentById(R.id.right_fragment)</span><br></pre></td></tr></table></figure><h4 id="在碎片中调用活动里的方法"><a class="markdownIt-Anchor" href="#在碎片中调用活动里的方法"></a> 在碎片中调用活动里的方法</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">MainActivity</span> <span class="variable">activity</span> <span class="operator">=</span> (MainActivity)getActivity();</span><br></pre></td></tr></table></figure><h3 id="碎片的生命周期"><a class="markdownIt-Anchor" href="#碎片的生命周期"></a> 碎片的生命周期</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">左: <span class="comment">//不提供返回栈</span></span><br><span class="line">右:transaction.addToBackStack(<span class="literal">null</span>); <span class="comment">//提供返回栈</span></span><br></pre></td></tr></table></figure><h3 id="限定符"><a class="markdownIt-Anchor" href="#限定符"></a> 限定符</h3><p>根据设备的分辨率或屏幕的大小来决定加载哪个布局</p><hr /><h2 id="广播"><a class="markdownIt-Anchor" href="#广播"></a> 广播</h2><h3 id="注册广播动态在代码中注册程序启动后才能接受广播"><a class="markdownIt-Anchor" href="#注册广播动态在代码中注册程序启动后才能接受广播"></a> 注册广播(动态:在代码中注册):程序启动后才能接受广播</h3><p>1.新建NetworkChangeReceiver继承BroadcastReceiver,并重写onReceive</p><p>2.在onCreate中添加广播并注册</p><p>3.取消注册</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> IntentFilter intentFilter;</span><br><span class="line">    <span class="keyword">private</span> NetworkChangeReceiver networkChangeReceiver;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line">        intentFilter = <span class="keyword">new</span> <span class="title class_">IntentFilter</span>();</span><br><span class="line">        intentFilter.addAction(<span class="string">&quot;android.net.conn.CONNECTIVITY_CHANGE&quot;</span>); <span class="comment">//网络变化时系统发出该广播,我们把他添加</span></span><br><span class="line">        networkChangeReceiver = <span class="keyword">new</span> <span class="title class_">NetworkChangeReceiver</span>();</span><br><span class="line">        registerReceiver(networkChangeReceiver, intentFilter); <span class="comment">//进行注册,networkChangeReceiver能接受该广播</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123; <span class="comment">//动态注册一定要记得取消注册</span></span><br><span class="line">        <span class="built_in">super</span>.onDestroy();</span><br><span class="line">        unregisterReceiver(networkChangeReceiver);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">NetworkChangeReceiver</span> <span class="keyword">extends</span> <span class="title class_">BroadcastReceiver</span>&#123; <span class="comment">//网络发生变化onReceive得到执行</span></span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onReceive</span><span class="params">(Context context, Intent intent)</span> &#123;</span><br><span class="line">            ConnectivityManager connectionManager=(ConnectivityManager) getSystemService(Context.CONNECTIVITY_SERVICE); <span class="comment">//系统服务系</span></span><br><span class="line">            NetworkInfo networkInfo=connectionManager.getActiveNetworkInfo();</span><br><span class="line">            <span class="keyword">if</span>(networkInfo!=<span class="literal">null</span> &amp;&amp; networkInfo.isAvailable())&#123;</span><br><span class="line">                Toast.makeText(context,<span class="string">&quot;network is available&quot;</span>,Toast.LENGTH_SHORT).show();</span><br><span class="line">            &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">                Toast.makeText(context,<span class="string">&quot;network is unavailable&quot;</span>,Toast.LENGTH_SHORT).show();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>添加权限(AndroidManifest.xml)</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">manifest</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.ACCESS_NETWORK_STATE&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><h3 id="注册广播静态在androidmanifestxml中注册程序启动前可以接受广播"><a class="markdownIt-Anchor" href="#注册广播静态在androidmanifestxml中注册程序启动前可以接受广播"></a> 注册广播(静态:在AndroidManifest.xml中注册):程序启动前可以接受广播</h3><p>1.用快捷注册自动生成(包-New-Other-Broadcast Receiver-全选)</p><p>2.修改该生成的广播接收器的代码</p><p>3.在AndroidManifest.xml添加相应的action</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BootCompleteReceiver</span> <span class="keyword">extends</span> <span class="title class_">BroadcastReceiver</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onReceive</span><span class="params">(Context context, Intent intent)</span> &#123;</span><br><span class="line">        Toast.makeText(context,<span class="string">&quot;Boot Complete&quot;</span>,Toast.LENGTH_LONG).show();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.RECEIVE_BOOT_COMPLETED&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">receiver</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.BootCompleteReceiver&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:enabled</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:exported</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">&quot;android.intent.action.BOOT_COMPLETED&quot;</span> /&gt;</span> <span class="comment">&lt;!--系统开机发送的广播  --&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">receiver</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="发送标准广播"><a class="markdownIt-Anchor" href="#发送标准广播"></a> 发送标准广播</h3><p>1.新建MyBroadcastReceiver</p><p>2.在AndroidManifest.xml添加相应的action</p><p>3.在MainActivity发送该广播</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBroadcastReceiver</span> <span class="keyword">extends</span> <span class="title class_">BroadcastReceiver</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onReceive</span><span class="params">(Context context, Intent intent)</span> &#123;</span><br><span class="line">        Toast.makeText(context,<span class="string">&quot;received in MyBroadcastReceiver&quot;</span>,Toast.LENGTH_SHORT).show();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">receiver</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.MyBroadcastReceiver&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:enabled</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:exported</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">&quot;com.example.broadcasttest.MY_BROADCAST&quot;</span> /&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">receiver</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line">        Button button=(Button) findViewById(R.id.button);</span><br><span class="line">        button.setOnClickListener(<span class="keyword">new</span> <span class="title class_">View</span>.OnClickListener() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">                Intent intent=<span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="string">&quot;com.example.broadcasttest.MY_BROADCAST&quot;</span>);</span><br><span class="line">                intent.setPackage(getPackageName());</span><br><span class="line">                sendBroadcast(intent);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h3 id="发送有序广播"><a class="markdownIt-Anchor" href="#发送有序广播"></a> 发送有序广播</h3><p>1.把MainActivity中的 sendBroadcast方法改成sendOrderedBroadcast方法</p><p>2.在AndroidManifest.xml里面设置广播接收器的优先级</p><p>3.允许广播继续传递abortBroadcast();</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line">        Button button=(Button) findViewById(R.id.button);</span><br><span class="line">        button.setOnClickListener(<span class="keyword">new</span> <span class="title class_">View</span>.OnClickListener() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">                Intent intent=<span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="string">&quot;com.example.broadcasttest.MY_BROADCAST&quot;</span>);</span><br><span class="line">                sendOrderedBroadcast(intent,<span class="literal">null</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">receiver</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.MyBroadcastReceiver&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:enabled</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:exported</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">intent-filter</span> <span class="attr">android:priority</span>=<span class="string">&quot;100&quot;</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">&quot;com.example.broadcasttest.MY_BROADCAST&quot;</span> /&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">receiver</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBroadcastReceiver</span> <span class="keyword">extends</span> <span class="title class_">BroadcastReceiver</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onReceive</span><span class="params">(Context context, Intent intent)</span> &#123;</span><br><span class="line">        Toast.makeText(context,<span class="string">&quot;received in MyBroadcastReceiver&quot;</span>,Toast.LENGTH_SHORT).show();</span><br><span class="line">        abortBroadcast();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="使用本地广播在应用程序的内部传递"><a class="markdownIt-Anchor" href="#使用本地广播在应用程序的内部传递"></a> 使用本地广播(在应用程序的内部传递)</h3><p>注:不能静态注册</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义广播过滤器，用于指定接收的广播类型</span></span><br><span class="line">    <span class="keyword">private</span> IntentFilter intentFilter;</span><br><span class="line">    <span class="comment">// 自定义的本地广播接收器</span></span><br><span class="line">    <span class="keyword">private</span> LocalReceiver localReceiver;</span><br><span class="line">    <span class="comment">// 本地广播管理器，用于发送和接收广播</span></span><br><span class="line">    <span class="keyword">private</span> LocalBroadcastManager localBroadcastManager;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        <span class="comment">// 设置活动的布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取本地广播管理器实例</span></span><br><span class="line">        localBroadcastManager = LocalBroadcastManager.getInstance(<span class="built_in">this</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 查找布局中的按钮并设置点击事件监听器</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">button</span> <span class="operator">=</span> (Button) findViewById(R.id.button);</span><br><span class="line">        button.setOnClickListener(<span class="keyword">new</span> <span class="title class_">View</span>.OnClickListener() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">                <span class="comment">// 创建一个 Intent，指定广播的 action</span></span><br><span class="line">                <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="string">&quot;com.example.broadcasttest.MY_BROADCAST&quot;</span>);</span><br><span class="line">                <span class="comment">// 通过本地广播管理器发送广播</span></span><br><span class="line">                localBroadcastManager.sendBroadcast(intent);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 初始化广播过滤器并添加过滤的 action</span></span><br><span class="line">        intentFilter = <span class="keyword">new</span> <span class="title class_">IntentFilter</span>();</span><br><span class="line">        intentFilter.addAction(<span class="string">&quot;com.example.broadcasttest.MY_BROADCAST&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 初始化自定义广播接收器</span></span><br><span class="line">        localReceiver = <span class="keyword">new</span> <span class="title class_">LocalReceiver</span>();</span><br><span class="line">        <span class="comment">// 注册广播接收器到本地广播管理器，并设置过滤器</span></span><br><span class="line">        localBroadcastManager.registerReceiver(localReceiver, intentFilter);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onDestroy();</span><br><span class="line">        <span class="comment">// 取消注册广播接收器，防止内存泄漏</span></span><br><span class="line">        localBroadcastManager.unregisterReceiver(localReceiver);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 自定义本地广播接收器类，继承自 BroadcastReceiver</span></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">LocalReceiver</span> <span class="keyword">extends</span> <span class="title class_">BroadcastReceiver</span> &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onReceive</span><span class="params">(Context context, Intent intent)</span> &#123;</span><br><span class="line">            <span class="comment">// 当接收到广播时，弹出 Toast 提示用户</span></span><br><span class="line">            Toast.makeText(context, <span class="string">&quot;received local broadcast&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="数据储存-持久化技术"><a class="markdownIt-Anchor" href="#数据储存-持久化技术"></a> 数据储存-持久化技术</h2><h3 id="文件储存"><a class="markdownIt-Anchor" href="#文件储存"></a> 文件储存</h3><p>注:所有的文件都是默认储存到/data/data/<package name>/files/目录下的.</p><h4 id="储存数据"><a class="markdownIt-Anchor" href="#储存数据"></a> 储存数据</h4><p>1.新建save方法(可不记),运用openFileOutput接收(“文件名”,保存模式(强制覆盖:MODE_PRIVATE,增添追加:MODE_APPEND)),通过BufferedWriter将其写入到文件.</p><p>2.重写onDestroy(),确保活动销毁前一定会调用save方法.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 声明 EditText 控件，用于输入文本</span></span><br><span class="line">    <span class="keyword">private</span> EditText edit;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// onCreate 方法：Activity 创建时调用</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_main);  <span class="comment">// 设置布局文件</span></span><br><span class="line">        edit = findViewById(R.id.edit);  <span class="comment">// 初始化 EditText 控件</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// onDestroy 方法：Activity 销毁时调用</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onDestroy();</span><br><span class="line">        <span class="comment">// 获取 EditText 中输入的文本</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">inputText</span> <span class="operator">=</span> edit.getText().toString();</span><br><span class="line">        <span class="comment">// 调用 save 方法保存文本内容</span></span><br><span class="line">        save(inputText);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// save 方法：保存输入的文本到文件</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">save</span><span class="params">(String inputText)</span> &#123;</span><br><span class="line">        <span class="type">FileOutputStream</span> <span class="variable">out</span> <span class="operator">=</span> <span class="literal">null</span>;  <span class="comment">// 声明文件输出流</span></span><br><span class="line">        <span class="type">BufferedWriter</span> <span class="variable">writer</span> <span class="operator">=</span> <span class="literal">null</span>;  <span class="comment">// 声明缓冲写入流</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 打开文件输出流，MODE_PRIVATE 表示私有模式，文件只能被该应用访问</span></span><br><span class="line">            out = openFileOutput(<span class="string">&quot;data&quot;</span>, Context.MODE_PRIVATE);</span><br><span class="line">            <span class="comment">// 将输出流包装成缓冲写入流，使用 UTF-8 编码</span></span><br><span class="line">            writer = <span class="keyword">new</span> <span class="title class_">BufferedWriter</span>(<span class="keyword">new</span> <span class="title class_">OutputStreamWriter</span>(out));</span><br><span class="line">            <span class="comment">// 将输入文本写入文件</span></span><br><span class="line">            writer.write(inputText);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            e.printStackTrace();  <span class="comment">// 捕获并打印异常</span></span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 关闭 writer 流</span></span><br><span class="line">                <span class="keyword">if</span> (writer != <span class="literal">null</span>) &#123;</span><br><span class="line">                    writer.close();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();  <span class="comment">// 捕获并打印关闭流时的异常</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="读取数据"><a class="markdownIt-Anchor" href="#读取数据"></a> 读取数据</h4><p>新建load方法(可不记),运用openFileInput接收(“文件名”),通过BufferedReader一行一行读取对象.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> String <span class="title function_">load</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="type">FileInputStream</span> <span class="variable">in</span> <span class="operator">=</span> <span class="literal">null</span>;  <span class="comment">// 声明文件输入流</span></span><br><span class="line">    <span class="type">BufferedReader</span> <span class="variable">reader</span> <span class="operator">=</span> <span class="literal">null</span>;  <span class="comment">// 声明缓冲读取流</span></span><br><span class="line">    <span class="type">StringBuilder</span> <span class="variable">content</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StringBuilder</span>();  <span class="comment">// 使用 StringBuilder 来存储读取的内容</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 打开文件输入流，读取名为 &quot;data&quot; 的文件</span></span><br><span class="line">        in = openFileInput(<span class="string">&quot;data&quot;</span>);</span><br><span class="line">        <span class="comment">// 将文件输入流包装成缓冲读取流，使用 UTF-8 编码</span></span><br><span class="line">        reader = <span class="keyword">new</span> <span class="title class_">BufferedReader</span>(<span class="keyword">new</span> <span class="title class_">InputStreamReader</span>(in));</span><br><span class="line">        <span class="type">String</span> <span class="variable">line</span> <span class="operator">=</span> <span class="string">&quot;&quot;</span>;  <span class="comment">// 用于存储每次读取的一行内容</span></span><br><span class="line">        <span class="comment">// 逐行读取文件内容，直到文件结束</span></span><br><span class="line">        <span class="keyword">while</span> ((line = reader.readLine()) != <span class="literal">null</span>) &#123;</span><br><span class="line">            content.append(line);  <span class="comment">// 将读取的每一行内容追加到 content 中</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();  <span class="comment">// 捕获并打印异常</span></span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="comment">// 确保 reader 流被关闭</span></span><br><span class="line">        <span class="keyword">if</span> (reader != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                reader.close();  <span class="comment">// 关闭缓冲读取流</span></span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();  <span class="comment">// 捕获并打印关闭流时的异常</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> content.toString();  <span class="comment">// 返回读取到的所有内容（作为字符串）</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> EditText edit;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line">        edit=findViewById(R.id.edit);</span><br><span class="line">        String inputText=load();</span><br><span class="line">        <span class="keyword">if</span>(!TextUtils.isEmpty(inputText))&#123;</span><br><span class="line">            edit.setText(inputText);</span><br><span class="line">            edit.setSelection(inputText.length());</span><br><span class="line">            Toast.makeText(<span class="built_in">this</span>,<span class="string">&quot;Restoring succeeded&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onDestroy();</span><br><span class="line">        String inputText=edit.getText().toString();</span><br><span class="line">        save(inputText);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">save</span><span class="params">(String inputText)</span>&#123;</span><br><span class="line">        FileOutputStream out=<span class="literal">null</span>;</span><br><span class="line">        BufferedWriter writer=<span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">try</span>&#123;</span><br><span class="line">            out=openFileOutput(<span class="string">&quot;data&quot;</span>, Context.MODE_PRIVATE);</span><br><span class="line">            writer=<span class="keyword">new</span> <span class="title class_">BufferedWriter</span>(<span class="keyword">new</span> <span class="title class_">OutputStreamWriter</span>(out));</span><br><span class="line">            writer.write(inputText);</span><br><span class="line">        &#125;<span class="keyword">catch</span>(IOException e)&#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;<span class="keyword">finally</span>&#123;</span><br><span class="line">            <span class="keyword">try</span>&#123;</span><br><span class="line">                <span class="keyword">if</span>(writer!=<span class="literal">null</span>)&#123;</span><br><span class="line">                    writer.close();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;<span class="keyword">catch</span> (IOException e)&#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">load</span><span class="params">()</span>&#123;</span><br><span class="line">        FileInputStream in=<span class="literal">null</span>;</span><br><span class="line">        BufferedReader reader=<span class="literal">null</span>;</span><br><span class="line">        StringBuilder content=<span class="keyword">new</span> <span class="title class_">StringBuilder</span>();</span><br><span class="line">        <span class="keyword">try</span>&#123;</span><br><span class="line">            in=openFileInput(<span class="string">&quot;data&quot;</span>);</span><br><span class="line">            reader=<span class="keyword">new</span> <span class="title class_">BufferedReader</span>(<span class="keyword">new</span> <span class="title class_">InputStreamReader</span>(in));</span><br><span class="line">            String line=<span class="string">&quot;&quot;</span>;</span><br><span class="line">            <span class="keyword">while</span>((line=reader.readLine())!=<span class="literal">null</span>)&#123;</span><br><span class="line">                content.append(line);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;<span class="keyword">catch</span> (IOException e)&#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">if</span>(reader!=<span class="literal">null</span>)&#123;</span><br><span class="line">                <span class="keyword">try</span>&#123;</span><br><span class="line">                    reader.close();</span><br><span class="line">                &#125;<span class="keyword">catch</span>(IOException e)&#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> content.toString();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="sharedpreferences存储应用于偏好设置功能"><a class="markdownIt-Anchor" href="#sharedpreferences存储应用于偏好设置功能"></a> SharedPreferences存储(应用于偏好设置功能)</h3><p>注:所有的文件都是默认储存到/data/data/<package name>/shared_prefs/目录下的.</p><h4 id="储存数据-2"><a class="markdownIt-Anchor" href="#储存数据-2"></a> 储存数据</h4><p>1.用getSharedPreferences设置存放的文件名与模式(唯一).</p><p>2.调用edit()获取对象.</p><p>2.存入数据并调用apply()进行提交.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">    <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">    setContentView(R.layout.activity_main);  <span class="comment">// 设置布局文件</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取布局文件中的 Button 控件</span></span><br><span class="line">    <span class="type">Button</span> <span class="variable">saveData</span> <span class="operator">=</span> findViewById(R.id.save_data);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 为按钮设置点击事件监听器</span></span><br><span class="line">    saveData.setOnClickListener(v -&gt; &#123;</span><br><span class="line">        <span class="comment">// 获取 SharedPreferences 对象，存储在 &quot;data&quot; 文件中，MODE_PRIVATE 表示私有模式</span></span><br><span class="line">        SharedPreferences.<span class="type">Editor</span> <span class="variable">editor</span> <span class="operator">=</span> getSharedPreferences(<span class="string">&quot;data&quot;</span>, MODE_PRIVATE).edit();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 使用 SharedPreferences.Editor 存储数据</span></span><br><span class="line">        editor.putString(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;Tom&quot;</span>);  <span class="comment">// 存储字符串数据 &quot;name&quot; 为 &quot;Tom&quot;</span></span><br><span class="line">        editor.putInt(<span class="string">&quot;age&quot;</span>, <span class="number">28</span>);  <span class="comment">// 存储整型数据 &quot;age&quot; 为 28</span></span><br><span class="line">        editor.putBoolean(<span class="string">&quot;married&quot;</span>, <span class="literal">false</span>);  <span class="comment">// 存储布尔值数据 &quot;married&quot; 为 false</span></span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 提交数据（apply() 异步提交，不会返回结果）</span></span><br><span class="line">        editor.apply();</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="读取数据-2"><a class="markdownIt-Anchor" href="#读取数据-2"></a> 读取数据</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 获取布局文件中的 Button 控件，用于恢复数据</span></span><br><span class="line"><span class="type">Button</span> <span class="variable">restoreData</span> <span class="operator">=</span> findViewById(R.id.restore_data);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 为按钮设置点击事件监听器</span></span><br><span class="line">restoreData.setOnClickListener(v -&gt; &#123;</span><br><span class="line">    <span class="comment">// 获取 SharedPreferences 对象，用于读取 &quot;data&quot; 文件中的数据</span></span><br><span class="line">    <span class="type">SharedPreferences</span> <span class="variable">pref</span> <span class="operator">=</span> getSharedPreferences(<span class="string">&quot;data&quot;</span>, MODE_PRIVATE);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 从 SharedPreferences 中读取存储的数据，如果没有找到，则使用默认值</span></span><br><span class="line">    <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> pref.getString(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;&quot;</span>);  <span class="comment">// 获取 &quot;name&quot; 的值，默认值为空字符串</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">age</span> <span class="operator">=</span> pref.getInt(<span class="string">&quot;age&quot;</span>, <span class="number">0</span>);  <span class="comment">// 获取 &quot;age&quot; 的值，默认值为 0</span></span><br><span class="line">    <span class="type">boolean</span> <span class="variable">married</span> <span class="operator">=</span> pref.getBoolean(<span class="string">&quot;married&quot;</span>, <span class="literal">false</span>);  <span class="comment">// 获取 &quot;married&quot; 的值，默认值为 false</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 输出日志，打印恢复的数据</span></span><br><span class="line">    Log.d(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;name is &quot;</span> + name);  <span class="comment">// 打印 &quot;name&quot; 的值</span></span><br><span class="line">    Log.d(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;age is &quot;</span> + age);  <span class="comment">// 打印 &quot;age&quot; 的值</span></span><br><span class="line">    Log.d(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;married is &quot;</span> + married);  <span class="comment">// 打印 &quot;married&quot; 的值</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>注:get( 键 , 默认值 ):键找不到对应值时就会返回默认值</p><hr /><h2 id="sql数据库储存结构化查询语言"><a class="markdownIt-Anchor" href="#sql数据库储存结构化查询语言"></a> SQL数据库储存(结构化查询语言)</h2><p>注:所有的文件都是默认储存到/data/data/<package name>/databases/目录下的.</p><h3 id="sql语句"><a class="markdownIt-Anchor" href="#sql语句"></a> SQL语句</h3><h4 id="1整型integer"><a class="markdownIt-Anchor" href="#1整型integer"></a> 1.整型:integer</h4><h4 id="2浮点型real"><a class="markdownIt-Anchor" href="#2浮点型real"></a> 2.浮点型:real</h4><h4 id="3文本类型text"><a class="markdownIt-Anchor" href="#3文本类型text"></a> 3.文本类型:text</h4><h4 id="4二进制型blob"><a class="markdownIt-Anchor" href="#4二进制型blob"></a> 4.二进制型:blob</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个常量 CREATE_BOOK，表示创建 Book 表的 SQL 语句</span></span><br><span class="line"><span class="number">1.</span><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">CREATE_BOOK</span> <span class="operator">=</span> <span class="string">&quot;create table Book(&quot;</span>  <span class="comment">// 创建一个名为 Book 的表</span></span><br><span class="line">        + <span class="string">&quot;id integer primary key autoincrement,&quot;</span>  <span class="comment">// 定义 id 字段，作为主键，并设置为自增</span></span><br><span class="line">        + <span class="string">&quot;author text,&quot;</span>  <span class="comment">// 定义 author 字段，类型为文本，用于存储书籍的作者名</span></span><br><span class="line">        + <span class="string">&quot;price real,&quot;</span>  <span class="comment">// 定义 price 字段，类型为实数，用于存储书籍的价格</span></span><br><span class="line">        + <span class="string">&quot;pages integer,&quot;</span>  <span class="comment">// 定义 pages 字段，类型为整数，用于存储书籍的页数</span></span><br><span class="line">        + <span class="string">&quot;name text)&quot;</span>;  <span class="comment">// 定义 name 字段，类型为文本，用于存储书籍的名称</span></span><br><span class="line"></span><br><span class="line"><span class="number">2.</span><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">CREATE_TABLE_SQL</span> <span class="operator">=</span> <span class="string">&quot;create table &quot;</span> + TABLE_NAME_NOTE + <span class="string">&quot; (id integer primary key autoincrement, title text, content text, create_time text)&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//两种方法都行</span></span><br></pre></td></tr></table></figure><h3 id="创建数据库"><a class="markdownIt-Anchor" href="#创建数据库"></a> 创建数据库</h3><ol><li><p>新建帮助类NoteDbOpenHelper继承自SQLiteOpenHelper</p></li><li><p>重写构造方法</p></li><li><p>重写onCreate调用execSQL执行建表语句</p></li><li><p>重写onUpgrade</p></li><li><p>之后可以调用getWritableDatabase()或getReadableDatabase()获取数据库</p></li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 构造方法，初始化数据库</span></span><br><span class="line">   <span class="keyword">public</span> <span class="title function_">NoteDbOpenHelper</span><span class="params">(Context context)</span> &#123;</span><br><span class="line">       <span class="built_in">super</span>(context, DB_NAME, <span class="literal">null</span>, <span class="number">1</span>);  <span class="comment">// 数据库名称、游标工厂、版本号</span></span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// 创建数据库表</span></span><br><span class="line">   <span class="meta">@Override</span></span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(SQLiteDatabase db)</span> &#123;</span><br><span class="line">       db.execSQL(CREATE_TABLE_SQL);  <span class="comment">// 执行创建表的 SQL 语句</span></span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">// 更新数据库表，暂时未实现</span></span><br><span class="line">   <span class="meta">@Override</span></span><br><span class="line">   <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onUpgrade</span><span class="params">(SQLiteDatabase db, <span class="type">int</span> oldVersion, <span class="type">int</span> newVersion)</span> &#123;</span><br><span class="line">       <span class="comment">// 可以在此进行数据库版本升级时的操作</span></span><br><span class="line">   &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="升级数据库"><a class="markdownIt-Anchor" href="#升级数据库"></a> 升级数据库</h3><p>如果你在原先已经创建好数据库的基础上还想增添几类数据就需要升级数据库</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyDatabaseHelper</span> <span class="keyword">extends</span> <span class="title class_">SQLiteOpenHelper</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String  CREATE_BOOK=<span class="string">&quot;create table Book(&quot;</span></span><br><span class="line">            +<span class="string">&quot;id integer primary key autoincrement,&quot;</span></span><br><span class="line">            +<span class="string">&quot;author text,&quot;</span></span><br><span class="line">            +<span class="string">&quot;price real,&quot;</span></span><br><span class="line">            +<span class="string">&quot;pages integer,&quot;</span></span><br><span class="line">            +<span class="string">&quot;name text)&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String CREATE_CATEGORY=<span class="string">&quot;create table Category(&quot;</span></span><br><span class="line">            +<span class="string">&quot;id integer primary key autoincrement,&quot;</span></span><br><span class="line">            +<span class="string">&quot;category_name text,&quot;</span></span><br><span class="line">            +<span class="string">&quot;category_code integer)&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> Context mContext;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">NoteDbOpenHelper</span><span class="params">(Context context)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(context, DB_NAME, <span class="literal">null</span>, <span class="number">2</span>);  <span class="comment">// 提高版本</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(SQLiteDatabase db)</span> &#123;</span><br><span class="line">        db.execSQL(CREATE_BOOK);</span><br><span class="line">        db.execSQL(CREATE_CATEGORY);</span><br><span class="line">        Toast.makeText(mContext,<span class="string">&quot;Create succeeded&quot;</span>,Toast.LENGTH_SHORT).show();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onUpgrade</span><span class="params">(SQLiteDatabase db, <span class="type">int</span> oldVersion, <span class="type">int</span> newVersion)</span> &#123;</span><br><span class="line">        db.execSQL(<span class="string">&quot;drop table if exists Book&quot;</span>); <span class="comment">// 如果存在 Book 表，则删除</span></span><br><span class="line">        db.execSQL(<span class="string">&quot;drop table if exists Category&quot;</span>); <span class="comment">// 如果存在 Category 表，则删除</span></span><br><span class="line">        onCreate(db); <span class="comment">// 重新创建表格</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="crud-create创建-read读取-update更新-和-delete删除"><a class="markdownIt-Anchor" href="#crud-create创建-read读取-update更新-和-delete删除"></a> CRUD:  <strong>Create（创建）</strong>、<strong>Read（读取）</strong>、<strong>Update（更新）</strong> 和 <strong>Delete（删除）</strong></h3><h3 id="添加数据"><a class="markdownIt-Anchor" href="#添加数据"></a> 添加数据</h3><p><code>insert()</code> 方法通常用于向数据库表中插入新的记录。在 Android 开发中，通常是通过 <code>SQLiteDatabase</code> 类的 <code>insert()</code> 方法来完成数据库操作。这个方法用于将一条新的数据插入到表中。</p><h3 id="基本语法"><a class="markdownIt-Anchor" href="#基本语法"></a> 基本语法：</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">long</span> <span class="title function_">insert</span><span class="params">(String table, String nullColumnHack, ContentValues values)</span></span><br></pre></td></tr></table></figure><h3 id="参数说明"><a class="markdownIt-Anchor" href="#参数说明"></a> 参数说明：</h3><ul><li><strong><code>table</code></strong>：表的名称，表示你要插入数据的目标表。</li><li><strong><code>nullColumnHack</code></strong>：如果提供了该值并且 <code>values</code> 中的某个列值为 <code>null</code>，则该列将会插入 <code>null</code>。通常如果表中允许某些列为 <code>NULL</code>，可以传入 <code>null</code>。如果没有这种需求，通常传入 <code>null</code>。</li><li><strong><code>values</code></strong>：<code>ContentValues</code> 对象，它存储了要插入的数据。每个键值对代表一列和该列的值。</li></ul><p><code>insert()</code> 方法会返回一个 <code>long</code> 类型的值，表示插入数据后的行 ID。如果插入失败，则返回 <code>-1</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">long</span> <span class="title function_">insertData</span><span class="params">(Note note)</span> &#123;</span><br><span class="line">        <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> getWritableDatabase();  <span class="comment">// 获取可写的数据库</span></span><br><span class="line"></span><br><span class="line">        <span class="type">ContentValues</span> <span class="variable">values</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ContentValues</span>();  <span class="comment">// 创建 ContentValues 存储插入的数据</span></span><br><span class="line">        values.put(<span class="string">&quot;title&quot;</span>, note.getTitle());  <span class="comment">// 插入标题</span></span><br><span class="line">        values.put(<span class="string">&quot;content&quot;</span>, note.getContent());  <span class="comment">// 插入内容</span></span><br><span class="line">        values.put(<span class="string">&quot;create_time&quot;</span>, note.getCreatedTime());  <span class="comment">// 插入创建时间</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> db.insert(TABLE_NAME_NOTE, <span class="literal">null</span>, values);  <span class="comment">// 插入数据，返回插入的行号</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h3 id="更新数据"><a class="markdownIt-Anchor" href="#更新数据"></a> 更新数据</h3><p>在 Android 中，使用 <code>SQLiteDatabase</code> 类的 <code>update()</code> 方法来更新数据库中的记录。这个方法允许你根据一定的条件（通过 <code>WHERE</code> 子句）更新指定表格中的一条或多条记录。</p><h3 id="基本语法-2"><a class="markdownIt-Anchor" href="#基本语法-2"></a> 基本语法：</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">update</span><span class="params">(String table, ContentValues values, String whereClause, String[] whereArgs)</span></span><br></pre></td></tr></table></figure><h3 id="参数说明-2"><a class="markdownIt-Anchor" href="#参数说明-2"></a> 参数说明：</h3><ul><li><strong><code>table</code></strong>：要更新的表格的名称。</li><li><strong><code>values</code></strong>：一个 <code>ContentValues</code> 对象，包含了要更新的列和值。每个键值对代表一列和对应的值。</li><li><strong><code>whereClause</code></strong>：指定更新的条件，类似于 SQL 中的 <code>WHERE</code> 子句。如果不指定该参数，所有记录都将被更新。</li><li><strong><code>whereArgs</code></strong>：<code>whereClause</code> 中的占位符（<code>?</code>）的实际值。是一个字符串数组，用来替代 <code>whereClause</code> 中的参数。</li></ul><p><code>update()</code> 方法返回一个 <code>int</code> 值，表示受影响的行数。如果没有满足条件的记录，则返回 0。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">updateData</span><span class="params">(Note note)</span> &#123;</span><br><span class="line">        <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> getWritableDatabase();  <span class="comment">// 获取可写的数据库</span></span><br><span class="line"></span><br><span class="line">        <span class="type">ContentValues</span> <span class="variable">values</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ContentValues</span>();  <span class="comment">// 创建 ContentValues 存储更新的数据</span></span><br><span class="line">        values.put(<span class="string">&quot;title&quot;</span>, note.getTitle());  <span class="comment">// 更新标题</span></span><br><span class="line">        values.put(<span class="string">&quot;content&quot;</span>, note.getContent());  <span class="comment">// 更新内容</span></span><br><span class="line">        values.put(<span class="string">&quot;create_time&quot;</span>, note.getCreatedTime());  <span class="comment">// 更新创建时间</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> db.update(TABLE_NAME_NOTE, values, <span class="string">&quot;id like ?&quot;</span>, <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;note.getId()&#125;);  <span class="comment">// 执行更新操作</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">/* like也可以改成=</span></span><br><span class="line"><span class="comment">id = ?：用于 精确匹配，适用于已知具体值时。</span></span><br><span class="line"><span class="comment">id like ?：用于 模糊匹配，适用于需要匹配部分值、前缀或后缀时。</span></span><br><span class="line"><span class="comment">如果你只希望更新 id 为某个特定值的记录，建议使用 id = ?。如果你希望根据某个模式进行匹配，则可以使用 id LIKE ?。*/</span></span><br></pre></td></tr></table></figure><h3 id="删除数据"><a class="markdownIt-Anchor" href="#删除数据"></a> 删除数据</h3><p>在 Android 中，使用 <code>SQLiteDatabase</code> 类的 <code>delete()</code> 方法可以删除数据库表中的一条或多条记录。这个方法允许你根据条件删除特定的记录，或者删除整个表中的所有记录。</p><h3 id="基本语法-3"><a class="markdownIt-Anchor" href="#基本语法-3"></a> 基本语法</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">public int delete(String table, String whereClause, String[] whereArgs)</span><br></pre></td></tr></table></figure><h3 id="参数说明-3"><a class="markdownIt-Anchor" href="#参数说明-3"></a> 参数说明：</h3><ul><li><strong><code>table</code></strong>：要删除数据的表名。</li><li><strong><code>whereClause</code></strong>：删除的条件（类似于 SQL 中的 <code>WHERE</code> 子句），只有符合条件的记录才会被删除。如果为 <code>null</code>，将删除表中所有记录。</li><li><strong><code>whereArgs</code></strong>：<code>whereClause</code> 中占位符（<code>?</code>）的实际值。它是一个字符串数组，用来替代 <code>whereClause</code> 中的参数。如果没有条件需要传入，通常可以传入 <code>null</code>。</li></ul><p><code>delete()</code> 方法返回一个 <code>int</code> 值，表示实际删除的行数。如果没有符合条件的记录被删除，返回 <code>0</code>。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">deleteFromDbById</span><span class="params">(String id)</span> &#123;</span><br><span class="line">        <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> getWritableDatabase();  <span class="comment">// 获取可写的数据库</span></span><br><span class="line">        <span class="keyword">return</span> db.delete(TABLE_NAME_NOTE, <span class="string">&quot;id like ?&quot;</span>, <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;id&#125;);  <span class="comment">// 执行删除操作</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h3 id="查询数据"><a class="markdownIt-Anchor" href="#查询数据"></a> 查询数据</h3><p>在 Android 中，<code>SQLiteDatabase</code> 类的 <code>query()</code> 方法用于从数据库表中检索数据。通过 <code>query()</code> 方法，我们可以指定查询的表、要查询的列、查询的条件、排序方式等参数，以便灵活地从数据库中获取所需的数据。</p><h3 id="基本语法-4"><a class="markdownIt-Anchor" href="#基本语法-4"></a> 基本语法：</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">public Cursor query(String table, String[] columns, String selection,</span><br><span class="line">                    String[] selectionArgs, String groupBy, String having, String orderBy)</span><br></pre></td></tr></table></figure><h3 id="参数说明-4"><a class="markdownIt-Anchor" href="#参数说明-4"></a> 参数说明：</h3><ul><li><strong><code>table</code></strong>：要查询的表名。</li><li><strong><code>columns</code></strong>：要返回的列名数组。可以指定要查询的具体列，若为 <code>null</code>，表示查询所有列。</li><li><strong><code>selection</code></strong>：查询条件，相当于 SQL 中的 <code>WHERE</code> 子句。可以为 <code>null</code>，表示没有条件（即返回所有记录）。</li><li><strong><code>selectionArgs</code></strong>：<code>selection</code> 中占位符（<code>?</code>）的实际值，必须与 <code>selection</code> 中的 <code>?</code> 占位符一一对应。如果没有占位符，传入 <code>null</code>。</li><li><strong><code>groupBy</code></strong>：对查询结果进行分组的列名数组，通常与 <code>GROUP BY</code> 子句结合使用。如果不需要分组，可以传入 <code>null</code>。</li><li><strong><code>having</code></strong>：分组后筛选的条件，相当于 SQL 中的 <code>HAVING</code> 子句。如果不需要筛选分组后的数据，可以传入 <code>null</code>。</li><li><strong><code>orderBy</code></strong>：排序方式，相当于 SQL 中的 <code>ORDER BY</code> 子句。如果为 <code>null</code>，则不进行排序。</li></ul><h3 id="返回值"><a class="markdownIt-Anchor" href="#返回值"></a> 返回值：</h3><p><code>query()</code> 方法返回一个 <code>Cursor</code> 对象，它包含了查询的结果。通过 <code>Cursor</code>，你可以逐行遍历查询结果并获取每一行的数据。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 根据标题模糊查询笔记</span></span><br><span class="line"><span class="keyword">public</span> List&lt;Note&gt; <span class="title function_">queryFromDbByTitle</span><span class="params">(String title)</span> &#123;</span><br><span class="line">    <span class="comment">// 如果传入的 title 为 null 或空字符串，返回所有笔记</span></span><br><span class="line">    <span class="keyword">if</span> (TextUtils.isEmpty(title)) &#123;</span><br><span class="line">        <span class="keyword">return</span> queryAllFromDb();  <span class="comment">// 调用 queryAllFromDb() 方法返回所有笔记</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取可写的数据库对象，用于执行查询操作</span></span><br><span class="line">    <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> getWritableDatabase();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建一个空的笔记列表，用于存放查询结果</span></span><br><span class="line">    List&lt;Note&gt; noteList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用 query() 方法执行 SQL 查询，查询 title 字段包含传入 title 的笔记记录</span></span><br><span class="line">    <span class="comment">// &quot;%&quot; + title + &quot;%&quot; 用于模糊匹配，即查询所有标题中包含传入 title 字符串的记录</span></span><br><span class="line">    <span class="type">Cursor</span> <span class="variable">cursor</span> <span class="operator">=</span> db.query(TABLE_NAME_NOTE, <span class="literal">null</span>, <span class="string">&quot;title like ?&quot;</span>, <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;<span class="string">&quot;%&quot;</span> + title + <span class="string">&quot;%&quot;</span>&#125;, <span class="literal">null</span>, <span class="literal">null</span>, <span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 如果 cursor 不为空，说明查询到了一些结果</span></span><br><span class="line">    <span class="keyword">if</span> (cursor != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="comment">// 遍历查询结果，每次移动到下一行</span></span><br><span class="line">        <span class="keyword">while</span> (cursor.moveToNext()) &#123;</span><br><span class="line">            <span class="comment">// 使用 getColumnIndex() 获取列索引，使用索引来获取对应列的值</span></span><br><span class="line">            <span class="meta">@SuppressLint(&quot;Range&quot;)</span> </span><br><span class="line">            <span class="type">String</span> <span class="variable">id</span> <span class="operator">=</span> cursor.getString(cursor.getColumnIndex(<span class="string">&quot;id&quot;</span>));  <span class="comment">// 获取 id 字段的值</span></span><br><span class="line">            <span class="meta">@SuppressLint(&quot;Range&quot;)</span> </span><br><span class="line">            <span class="type">String</span> <span class="variable">title2</span> <span class="operator">=</span> cursor.getString(cursor.getColumnIndex(<span class="string">&quot;title&quot;</span>));  <span class="comment">// 获取 title 字段的值</span></span><br><span class="line">            <span class="meta">@SuppressLint(&quot;Range&quot;)</span> </span><br><span class="line">            <span class="type">String</span> <span class="variable">content</span> <span class="operator">=</span> cursor.getString(cursor.getColumnIndex(<span class="string">&quot;content&quot;</span>));  <span class="comment">// 获取 content 字段的值</span></span><br><span class="line">            <span class="meta">@SuppressLint(&quot;Range&quot;)</span> </span><br><span class="line">            <span class="type">String</span> <span class="variable">createTime</span> <span class="operator">=</span> cursor.getString(cursor.getColumnIndex(<span class="string">&quot;create_time&quot;</span>));  <span class="comment">// 获取 create_time 字段的值</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">// 创建一个新的 Note 对象，并将查询结果设置为 Note 对象的属性</span></span><br><span class="line">            <span class="type">Note</span> <span class="variable">note</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Note</span>();</span><br><span class="line">            note.setId(id);</span><br><span class="line">            note.setTitle(title2);</span><br><span class="line">            note.setContent(content);</span><br><span class="line">            note.setCreatedTime(createTime);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 将 Note 对象添加到 noteList 列表中</span></span><br><span class="line">            noteList.add(note);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 查询完成后关闭 cursor 以释放资源</span></span><br><span class="line">        cursor.close();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 返回包含所有匹配标题的笔记对象的列表</span></span><br><span class="line">    <span class="keyword">return</span> noteList;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="sql语法"><a class="markdownIt-Anchor" href="#sql语法"></a> SQL语法</h3><p><a href="https://blog.csdn.net/Innocence_0/article/details/132438303?fromshare=blogdetail&amp;sharetype=blogdetail&amp;sharerId=132438303&amp;sharerefer=PC&amp;sharesource=hqw11111&amp;sharefrom=from_link" title="一个垃圾的搜索引擎">SQL</a></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> <span class="built_in">this</span>.getWritableDatabase();</span><br><span class="line"><span class="type">String</span> <span class="variable">sql</span> <span class="operator">=</span> <span class="string">&quot;INSERT INTO Book (name, author) VALUES (&#x27;Book Name&#x27;, &#x27;Author Name&#x27;)&quot;</span>;</span><br><span class="line">db.execSQL(sql);</span><br><span class="line">db.close();</span><br></pre></td></tr></table></figure><hr /><h2 id="内容提供器"><a class="markdownIt-Anchor" href="#内容提供器"></a> 内容提供器</h2><p>Android的内容提供器（Content Provider）是四大组件之一，用于在不同应用间共享数据。它提供统一的接口，允许应用访问或修改其他应用的数据，同时保护数据安全。</p><h4 id="主要功能"><a class="markdownIt-Anchor" href="#主要功能"></a> 主要功能</h4><ol><li><strong>数据共享</strong>：支持应用间共享数据，如联系人、短信等。</li><li><strong>数据访问</strong>：通过URI标识数据，使用ContentResolver进行查询、插入、更新和删除操作。</li><li><strong>数据安全</strong>：通过权限控制保护数据访问。</li></ol><h4 id="核心类"><a class="markdownIt-Anchor" href="#核心类"></a> 核心类</h4><ul><li><strong>ContentProvider</strong>：基类，需实现增删改查等方法。</li><li><strong>ContentResolver</strong>：用于与ContentProvider交互。</li><li><strong>Uri</strong>：标识数据，格式为<code>content://authority/path/id</code>。</li></ul><h4 id="使用步骤"><a class="markdownIt-Anchor" href="#使用步骤"></a> 使用步骤</h4><ol><li><strong>定义ContentProvider</strong>：继承并实现必要方法。</li><li><strong>声明Provider</strong>：在<code>AndroidManifest.xml</code>中注册。</li><li><strong>访问数据</strong>：通过ContentResolver操作数据。</li></ol><h3 id="在程序运行时申请权限危险权限"><a class="markdownIt-Anchor" href="#在程序运行时申请权限危险权限"></a> 在程序运行时申请权限(危险权限)</h3><p>1.在activity_main.xml设置一个按钮</p><p>2.在MainActivity运用隐式intent(导入android.Manifest)</p><p>3.在AndroidManifest.xml添加权限</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> android.Manifest;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        <span class="comment">// 设置布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line">        <span class="comment">// 获取布局中的按钮</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">makeCall</span> <span class="operator">=</span> findViewById(R.id.make_call);</span><br><span class="line">        <span class="comment">// 设置按钮的点击事件</span></span><br><span class="line">        makeCall.setOnClickListener(v -&gt; &#123;</span><br><span class="line">            <span class="comment">// 检查是否已经授予拨打电话的权限</span></span><br><span class="line">            <span class="keyword">if</span> (ContextCompat.checkSelfPermission(MainActivity.<span class="built_in">this</span>, Manifest.permission.CALL_PHONE) != PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                <span class="comment">// 如果没有权限，请求权限 ContextCompat.checkSelfPermission(Context,权限名)</span></span><br><span class="line">                ActivityCompat.requestPermissions(MainActivity.<span class="built_in">this</span>, <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;Manifest.permission.CALL_PHONE&#125;, <span class="number">1</span>);</span><br><span class="line"><span class="comment">/*方法签名</span></span><br><span class="line"><span class="comment">public static void requestPermissions(Activity activity, String[] permissions, int requestCode);</span></span><br><span class="line"><span class="comment">参数说明：</span></span><br><span class="line"><span class="comment">activity:当前活动的 Activity 实例。</span></span><br><span class="line"><span class="comment">通常是 this 或 MainActivity.this。</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">permissions:需要请求的权限数组。</span></span><br><span class="line"><span class="comment">例如：new String[]&#123;Manifest.permission.CALL_PHONE&#125;。</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">requestCode:请求的唯一标识码（自定义整数）。</span></span><br><span class="line"><span class="comment">用于在 onRequestPermissionsResult 中区分不同的权限请求。*/</span></span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 如果已经有权限，直接拨打电话</span></span><br><span class="line">                call();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 拨打电话的方法</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">call</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 创建一个拨打电话的Intent</span></span><br><span class="line">            <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(Intent.ACTION_CALL);</span><br><span class="line">            <span class="comment">// 设置拨打的电话号码</span></span><br><span class="line">            intent.setData(Uri.parse(<span class="string">&quot;tel:10086&quot;</span>));</span><br><span class="line">            <span class="comment">// 启动拨打电话的Activity</span></span><br><span class="line">            startActivity(intent);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (SecurityException e) &#123;</span><br><span class="line">            <span class="comment">// 捕获权限异常并打印堆栈信息</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 处理权限请求结果的回调方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onRequestPermissionsResult</span><span class="params">(<span class="type">int</span> requestCode, <span class="meta">@NonNull</span> String[] permissions, <span class="meta">@NonNull</span> <span class="type">int</span>[] grantResults, <span class="type">int</span> deviceId)</span> &#123;</span><br><span class="line">        <span class="keyword">switch</span> (requestCode) &#123;</span><br><span class="line">            <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">                <span class="comment">// 检查是否授予了拨打电话的权限</span></span><br><span class="line">                <span class="keyword">if</span> (grantResults.length &gt; <span class="number">0</span> &amp;&amp; grantResults[<span class="number">0</span>] == PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                    <span class="comment">// 如果授予了权限，拨打电话</span></span><br><span class="line">                    call();</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 如果拒绝了权限，显示提示信息</span></span><br><span class="line">                    Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;You denied the permission&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">default</span>:</span><br><span class="line">                <span class="comment">// 其他情况不做处理</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">uses-feature</span></span></span><br><span class="line"><span class="tag">       <span class="attr">android:name</span>=<span class="string">&quot;android.hardware.telephony&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">android:required</span>=<span class="string">&quot;false&quot;</span> /&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.CALL_PHONE&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure><h3 id="访问其他程序中的数据"><a class="markdownIt-Anchor" href="#访问其他程序中的数据"></a> 访问其他程序中的数据</h3><h3 id="contentresolver用法"><a class="markdownIt-Anchor" href="#contentresolver用法"></a> ContentResolver用法</h3><p><code>ContentResolver</code> 是 Android 中用于访问内容提供器（Content Provider）数据的核心类。它充当应用程序与内容提供器之间的桥梁，允许应用程序通过 URI 查询、插入、更新和删除其他应用程序提供的数据。</p><hr /><h3 id="核心功能"><a class="markdownIt-Anchor" href="#核心功能"></a> 核心功能</h3><ol><li><strong>查询数据</strong>：通过 <code>query()</code> 方法从内容提供器中获取数据。</li><li><strong>插入数据</strong>：通过 <code>insert()</code> 方法向内容提供器中添加数据。</li><li><strong>更新数据</strong>：通过 <code>update()</code> 方法修改内容提供器中的数据。</li><li><strong>删除数据</strong>：通过 <code>delete()</code> 方法从内容提供器中删除数据。</li></ol><hr /><h3 id="使用步骤-2"><a class="markdownIt-Anchor" href="#使用步骤-2"></a> 使用步骤</h3><ol><li><strong>获取 ContentResolver 实例</strong><br />通过 <code>getContentResolver()</code> 方法获取 <code>ContentResolver</code> 实例。</li><li><strong>定义 URI</strong><br />URI 是访问内容提供器中数据的唯一标识符。格式通常为：<br /><code>content://&lt;authority&gt;/&lt;path&gt;/&lt;id&gt;</code><br />例如：<ul><li>访问联系人数据：<code>content://com.android.contacts/contacts</code></li><li>访问媒体文件：<code>content://media/external/images/media</code></li></ul></li><li><strong>操作数据</strong><br />使用 <code>ContentResolver</code> 的方法（如 <code>query()</code>、<code>insert()</code>、<code>update()</code>、<code>delete()</code>）操作数据。</li></ol><hr /><h3 id="示例代码"><a class="markdownIt-Anchor" href="#示例代码"></a> 示例代码</h3><h4 id="1-查询数据"><a class="markdownIt-Anchor" href="#1-查询数据"></a> 1. 查询数据</h4><p>以下示例演示如何查询联系人数据：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> android.database.Cursor;</span><br><span class="line"><span class="keyword">import</span> android.net.Uri;</span><br><span class="line"><span class="keyword">import</span> android.os.Bundle;</span><br><span class="line"><span class="keyword">import</span> android.provider.ContactsContract;</span><br><span class="line"><span class="keyword">import</span> androidx.appcompat.app.AppCompatActivity;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 1. 定义 URI，访问联系人数据</span></span><br><span class="line">        <span class="type">Uri</span> <span class="variable">uri</span> <span class="operator">=</span> ContactsContract.Contacts.CONTENT_URI;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 定义要查询的列</span></span><br><span class="line">        String[] projection = <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;</span><br><span class="line">                ContactsContract.Contacts._ID,           <span class="comment">// 联系人 ID</span></span><br><span class="line">                ContactsContract.Contacts.DISPLAY_NAME,  <span class="comment">// 联系人名称</span></span><br><span class="line">                ContactsContract.Contacts.HAS_PHONE_NUMBER <span class="comment">// 是否有电话号码</span></span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 定义查询条件（可选）</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">selection</span> <span class="operator">=</span> ContactsContract.Contacts.HAS_PHONE_NUMBER + <span class="string">&quot; = ?&quot;</span>; <span class="comment">// 查询有电话号码的联系人</span></span><br><span class="line">        String[] selectionArgs = <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;<span class="string">&quot;1&quot;</span>&#125;; <span class="comment">// 查询条件参数</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 定义排序方式（可选）</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">sortOrder</span> <span class="operator">=</span> ContactsContract.Contacts.DISPLAY_NAME + <span class="string">&quot; ASC&quot;</span>; <span class="comment">// 按名称升序排序</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 5. 获取 ContentResolver 实例并执行查询</span></span><br><span class="line">        <span class="type">Cursor</span> <span class="variable">cursor</span> <span class="operator">=</span> getContentResolver().query(uri, projection, selection, selectionArgs, sortOrder);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 6. 处理查询结果</span></span><br><span class="line">        <span class="keyword">if</span> (cursor != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">while</span> (cursor.moveToNext()) &#123;</span><br><span class="line">                <span class="comment">// 获取联系人 ID、名称和是否有电话号码</span></span><br><span class="line">                <span class="type">String</span> <span class="variable">id</span> <span class="operator">=</span> cursor.getString(cursor.getColumnIndex(ContactsContract.Contacts._ID));</span><br><span class="line">                <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> cursor.getString(cursor.getColumnIndex(ContactsContract.Contacts.DISPLAY_NAME));</span><br><span class="line">                <span class="type">int</span> <span class="variable">hasPhoneNumber</span> <span class="operator">=</span> cursor.getInt(cursor.getColumnIndex(ContactsContract.Contacts.HAS_PHONE_NUMBER));</span><br><span class="line"></span><br><span class="line">                System.out.println(<span class="string">&quot;ID: &quot;</span> + id + <span class="string">&quot;, Name: &quot;</span> + name + <span class="string">&quot;, Has Phone: &quot;</span> + (hasPhoneNumber == <span class="number">1</span> ? <span class="string">&quot;Yes&quot;</span> : <span class="string">&quot;No&quot;</span>));</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 7. 关闭游标</span></span><br><span class="line">            cursor.close();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h4 id="2-插入数据"><a class="markdownIt-Anchor" href="#2-插入数据"></a> 2. 插入数据</h4><p>以下示例演示如何向媒体库插入一张图片：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> android.content.ContentValues;</span><br><span class="line"><span class="keyword">import</span> android.net.Uri;</span><br><span class="line"><span class="keyword">import</span> android.os.Bundle;</span><br><span class="line"><span class="keyword">import</span> android.provider.MediaStore;</span><br><span class="line"><span class="keyword">import</span> androidx.appcompat.app.AppCompatActivity;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 定义 URI，访问媒体库</span></span><br><span class="line">        <span class="type">Uri</span> <span class="variable">uri</span> <span class="operator">=</span> MediaStore.Images.Media.EXTERNAL_CONTENT_URI;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建 ContentValues 对象，存储要插入的数据</span></span><br><span class="line">        <span class="type">ContentValues</span> <span class="variable">values</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ContentValues</span>();</span><br><span class="line">        values.put(MediaStore.Images.Media.DISPLAY_NAME, <span class="string">&quot;my_image.jpg&quot;</span>);</span><br><span class="line">        values.put(MediaStore.Images.Media.MIME_TYPE, <span class="string">&quot;image/jpeg&quot;</span>);</span><br><span class="line">        values.put(MediaStore.Images.Media.RELATIVE_PATH, <span class="string">&quot;Pictures/MyApp&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 插入数据</span></span><br><span class="line">        <span class="type">Uri</span> <span class="variable">insertedUri</span> <span class="operator">=</span> getContentResolver().insert(uri, values);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (insertedUri != <span class="literal">null</span>) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;Image inserted at: &quot;</span> + insertedUri.toString());</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;Failed to insert image.&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h4 id="3-更新数据"><a class="markdownIt-Anchor" href="#3-更新数据"></a> 3. 更新数据</h4><p>以下示例演示如何更新联系人名称：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> android.content.ContentValues;</span><br><span class="line"><span class="keyword">import</span> android.net.Uri;</span><br><span class="line"><span class="keyword">import</span> android.os.Bundle;</span><br><span class="line"><span class="keyword">import</span> android.provider.ContactsContract;</span><br><span class="line"><span class="keyword">import</span> androidx.appcompat.app.AppCompatActivity;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 定义 URI，指定要更新的联系人</span></span><br><span class="line">        <span class="type">Uri</span> <span class="variable">uri</span> <span class="operator">=</span> Uri.withAppendedPath(ContactsContract.Contacts.CONTENT_URI, <span class="string">&quot;1&quot;</span>); <span class="comment">// 更新 ID 为 1 的联系人</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建 ContentValues 对象，存储要更新的数据</span></span><br><span class="line">        <span class="type">ContentValues</span> <span class="variable">values</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ContentValues</span>();</span><br><span class="line">        values.put(ContactsContract.Contacts.DISPLAY_NAME, <span class="string">&quot;New Name&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 更新数据</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">rowsUpdated</span> <span class="operator">=</span> getContentResolver().update(uri, values, <span class="literal">null</span>, <span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (rowsUpdated &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;Contact updated successfully.&quot;</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;Failed to update contact.&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h4 id="4-删除数据"><a class="markdownIt-Anchor" href="#4-删除数据"></a> 4. 删除数据</h4><p>以下示例演示如何删除联系人：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> android.net.Uri;</span><br><span class="line"><span class="keyword">import</span> android.os.Bundle;</span><br><span class="line"><span class="keyword">import</span> android.provider.ContactsContract;</span><br><span class="line"><span class="keyword">import</span> androidx.appcompat.app.AppCompatActivity;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 定义 URI，指定要删除的联系人</span></span><br><span class="line">        <span class="type">Uri</span> <span class="variable">uri</span> <span class="operator">=</span> Uri.withAppendedPath(ContactsContract.Contacts.CONTENT_URI, <span class="string">&quot;1&quot;</span>); <span class="comment">// 删除 ID 为 1 的联系人</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 删除数据</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">rowsDeleted</span> <span class="operator">=</span> getContentResolver().delete(uri, <span class="literal">null</span>, <span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (rowsDeleted &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;Contact deleted successfully.&quot;</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;Failed to delete contact.&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h3 id="关键点"><a class="markdownIt-Anchor" href="#关键点"></a> 关键点</h3><ol><li><p><strong>URI</strong><br />URI 是访问内容提供器中数据的唯一标识符。不同的内容提供器有不同的 URI。</p></li><li><p><strong>权限</strong><br />访问某些内容提供器（如联系人、媒体库）需要在 <code>AndroidManifest.xml</code> 中声明权限。例如：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.READ_CONTACTS&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.WRITE_CONTACTS&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.READ_EXTERNAL_STORAGE&quot;</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.WRITE_EXTERNAL_STORAGE&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><p>运行 HTML</p></li><li><p><strong>游标管理</strong><br />使用 <code>Cursor</code> 查询数据后，务必调用 <code>cursor.close()</code> 关闭游标，避免内存泄漏。</p></li><li><p><strong>线程安全</strong><br /><code>ContentResolver</code> 的操作通常是同步的，建议在子线程中执行耗时操作（如查询大量数据）。</p></li></ol><h3 id="创建内容提供器"><a class="markdownIt-Anchor" href="#创建内容提供器"></a> 创建内容提供器</h3><p>1.新建Myprovider继承自ContentProvider,并重写6个方法</p><p>2.借助UriMatcher匹配内容内容URI</p><p>3.AndroidManifest.xml会自动注册</p><p>以下是 <code>ContentProvider</code> 的 6 个核心方法及其作用：</p><h3 id="1-oncreate"><a class="markdownIt-Anchor" href="#1-oncreate"></a> 1. <strong>onCreate()</strong></h3><ul><li><p><strong>作用</strong>：在 <code>ContentProvider</code> 创建时调用，用于执行初始化操作（如打开数据库连接）。</p></li><li><p><strong>调用时机</strong>：当 <code>ContentProvider</code> 第一次被访问时调用。</p></li><li><p><strong>返回值</strong>：<code>boolean</code>，表示 <code>ContentProvider</code> 是否初始化成功。</p></li><li><p><strong>示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">onCreate</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 初始化数据库连接</span></span><br><span class="line">    dbHelper = <span class="keyword">new</span> <span class="title class_">MyDatabaseHelper</span>(getContext());</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">// 返回 true 表示 ContentProvider 初始化成功</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><hr /><h3 id="2-query"><a class="markdownIt-Anchor" href="#2-query"></a> 2. <strong>query()</strong></h3><ul><li><p><strong>作用</strong>：查询数据，返回一个 <code>Cursor</code> 对象。</p></li><li><p><strong>参数</strong>：</p><ul><li><code>uri</code>：数据的 URI，用于指定要查询的数据源。</li><li><code>projection</code>：要查询的列（字段）数组。</li><li><code>selection</code>：查询条件，类似于 SQL 中的 <code>WHERE</code> 子句。</li><li><code>selectionArgs</code>：查询条件的参数值。</li><li><code>sortOrder</code>：排序方式，类似于 SQL 中的 <code>ORDER BY</code> 子句。</li></ul></li><li><p><strong>返回值</strong>：<code>Cursor</code>，包含查询结果。</p></li><li><p><strong>示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> Cursor <span class="title function_">query</span><span class="params">(Uri uri, String[] projection, String selection, String[] selectionArgs, String sortOrder)</span> &#123;</span><br><span class="line">    <span class="comment">// 获取一个可读的SQLiteDatabase实例</span></span><br><span class="line">    <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> dbHelper.getReadableDatabase();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 执行查询操作，查询&quot;my_table&quot;表，并根据传入的参数筛选、排序结果</span></span><br><span class="line">    <span class="type">Cursor</span> <span class="variable">cursor</span> <span class="operator">=</span> db.query(<span class="string">&quot;my_table&quot;</span>, projection, selection, selectionArgs, <span class="literal">null</span>, <span class="literal">null</span>, sortOrder);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 设置内容通知URI，允许内容观察者在数据发生变化时收到通知</span></span><br><span class="line">    cursor.setNotificationUri(getContext().getContentResolver(), uri);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 返回查询结果的Cursor对象</span></span><br><span class="line">    <span class="keyword">return</span> cursor;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></li></ul><hr /><h3 id="3-insert"><a class="markdownIt-Anchor" href="#3-insert"></a> 3. <strong>insert()</strong></h3><ul><li><p><strong>作用</strong>：插入新数据，返回新插入数据的 URI。</p></li><li><p><strong>参数</strong>：</p><ul><li><code>uri</code>：数据的 URI，用于指定要插入的数据源。</li><li><code>values</code>：要插入的数据，封装在 <code>ContentValues</code> 对象中。</li></ul></li><li><p><strong>返回值</strong>：<code>Uri</code>，表示新插入数据的 URI。</p></li><li><p><strong>示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> Uri <span class="title function_">insert</span><span class="params">(Uri uri, ContentValues values)</span> &#123;</span><br><span class="line">    <span class="comment">// 获取可写的SQLiteDatabase实例</span></span><br><span class="line">    <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> dbHelper.getWritableDatabase();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 插入数据到数据库表&quot;my_table&quot;中，并获取插入的行ID</span></span><br><span class="line">    <span class="type">long</span> <span class="variable">id</span> <span class="operator">=</span> db.insert(<span class="string">&quot;my_table&quot;</span>, <span class="literal">null</span>, values);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 如果插入成功（id大于0），则构造新插入数据的URI</span></span><br><span class="line">    <span class="keyword">if</span> (id &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 使用ContentUris.withAppendedId()为插入的记录生成一个唯一的URI</span></span><br><span class="line">        <span class="type">Uri</span> <span class="variable">newUri</span> <span class="operator">=</span> ContentUris.withAppendedId(uri, id);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 通知内容观察者该URI的数据已经发生变化</span></span><br><span class="line">        getContext().getContentResolver().notifyChange(newUri, <span class="literal">null</span>);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 返回新插入数据的URI</span></span><br><span class="line">        <span class="keyword">return</span> newUri;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 如果插入失败，抛出异常</span></span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">SQLException</span>(<span class="string">&quot;Failed to insert row into &quot;</span> + uri);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></li></ul><hr /><h3 id="4-update"><a class="markdownIt-Anchor" href="#4-update"></a> 4. <strong>update()</strong></h3><ul><li><p><strong>作用</strong>：更新数据，返回受影响的行数。</p></li><li><p><strong>参数</strong>：</p><ul><li><code>uri</code>：数据的 URI，用于指定要更新的数据源。</li><li><code>values</code>：要更新的数据，封装在 <code>ContentValues</code> 对象中。</li><li><code>selection</code>：更新条件，类似于 SQL 中的 <code>WHERE</code> 子句。</li><li><code>selectionArgs</code>：更新条件的参数值。</li></ul></li><li><p><strong>返回值</strong>：<code>int</code>，表示受影响的行数。</p></li><li><p><strong>示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">update</span><span class="params">(Uri uri, ContentValues values, String selection, String[] selectionArgs)</span> &#123;</span><br><span class="line">    <span class="comment">// 获取可写的SQLiteDatabase实例，以便对数据库进行更新操作</span></span><br><span class="line">    <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> dbHelper.getWritableDatabase();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 执行更新操作，将数据表&quot;my_table&quot;中符合条件的行更新为新的值</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">rowsUpdated</span> <span class="operator">=</span> db.update(<span class="string">&quot;my_table&quot;</span>, values, selection, selectionArgs);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 如果更新操作影响了至少一行数据，通知内容观察者数据已变化</span></span><br><span class="line">    <span class="keyword">if</span> (rowsUpdated &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        getContext().getContentResolver().notifyChange(uri, <span class="literal">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 返回更新的行数</span></span><br><span class="line">    <span class="keyword">return</span> rowsUpdated;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></li></ul><hr /><h3 id="5-delete"><a class="markdownIt-Anchor" href="#5-delete"></a> 5. <strong>delete()</strong></h3><ul><li><p><strong>作用</strong>：删除数据，返回被删除的行数。</p></li><li><p><strong>参数</strong>：</p><ul><li><code>uri</code>：数据的 URI，用于指定要删除的数据源。</li><li><code>selection</code>：删除条件，类似于 SQL 中的 <code>WHERE</code> 子句。</li><li><code>selectionArgs</code>：删除条件的参数值。</li></ul></li><li><p><strong>返回值</strong>：<code>int</code>，表示被删除的行数。</p></li><li><p><strong>示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">delete</span><span class="params">(Uri uri, String selection, String[] selectionArgs)</span> &#123;</span><br><span class="line">    <span class="comment">// 获取可写的SQLiteDatabase实例，以便对数据库进行删除操作</span></span><br><span class="line">    <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> dbHelper.getWritableDatabase();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 执行删除操作，删除表&quot;my_table&quot;中符合条件的行</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">rowsDeleted</span> <span class="operator">=</span> db.delete(<span class="string">&quot;my_table&quot;</span>, selection, selectionArgs);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 如果删除操作影响了至少一行数据，通知内容观察者数据已变化</span></span><br><span class="line">    <span class="keyword">if</span> (rowsDeleted &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        getContext().getContentResolver().notifyChange(uri, <span class="literal">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 返回删除的行数</span></span><br><span class="line">    <span class="keyword">return</span> rowsDeleted;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></li></ul><hr /><h3 id="6-gettype"><a class="markdownIt-Anchor" href="#6-gettype"></a> 6. <strong>getType()</strong></h3><ul><li><p><strong>作用</strong>：返回指定 URI 对应的 MIME 类型。</p></li><li><p><strong>参数</strong>：</p><ul><li><code>uri</code>：数据的 URI。</li></ul></li><li><p><strong>返回值</strong>：<code>String</code>，表示 MIME 类型。</p></li><li><p><strong>MIME 类型格式</strong>：</p><ul><li>单条数据：<code>vnd.android.cursor.item/vnd.&lt;authority&gt;.&lt;path&gt;</code></li><li>多条数据：<code>vnd.android.cursor.dir/vnd.&lt;authority&gt;.&lt;path&gt;</code></li></ul></li><li><p><strong>示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> String <span class="title function_">getType</span><span class="params">(Uri uri)</span> &#123;</span><br><span class="line">    <span class="comment">// 如果 URI 的最后一个路径段为空，返回表示多条数据的 MIME 类型</span></span><br><span class="line">    <span class="keyword">if</span> (uri.getLastPathSegment() == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;vnd.android.cursor.dir/vnd.com.example.provider.my_table&quot;</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 否则，返回表示单条数据的 MIME 类型</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;vnd.android.cursor.item/vnd.com.example.provider.my_table&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></li></ul><hr /><h3 id="完整示例"><a class="markdownIt-Anchor" href="#完整示例"></a> 完整示例</h3><p>以下是一个简单的 <code>ContentProvider</code> 实现示例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> android.content.ContentProvider;</span><br><span class="line"><span class="keyword">import</span> android.content.ContentUris;</span><br><span class="line"><span class="keyword">import</span> android.content.ContentValues;</span><br><span class="line"><span class="keyword">import</span> android.content.UriMatcher;</span><br><span class="line"><span class="keyword">import</span> android.database.Cursor;</span><br><span class="line"><span class="keyword">import</span> android.database.sqlite.SQLiteDatabase;</span><br><span class="line"><span class="keyword">import</span> android.net.Uri;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyContentProvider</span> <span class="keyword">extends</span> <span class="title class_">ContentProvider</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义 ContentProvider 的授权标识（authority），通常是应用的包名</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">AUTHORITY</span> <span class="operator">=</span> <span class="string">&quot;com.example.provider&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义数据表的路径</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">PATH</span> <span class="operator">=</span> <span class="string">&quot;my_table&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义 ContentProvider 的 URI，格式为：content://&lt;authority&gt;/&lt;path&gt;</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Uri</span> <span class="variable">CONTENT_URI</span> <span class="operator">=</span> Uri.parse(<span class="string">&quot;content://&quot;</span> + AUTHORITY + <span class="string">&quot;/&quot;</span> + PATH);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义 URI 匹配码</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">ITEMS</span> <span class="operator">=</span> <span class="number">1</span>;      <span class="comment">// 匹配多条数据</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">ITEM_ID</span> <span class="operator">=</span> <span class="number">2</span>;    <span class="comment">// 匹配单条数据</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建 UriMatcher 对象，用于匹配 URI</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">UriMatcher</span> <span class="variable">uriMatcher</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UriMatcher</span>(UriMatcher.NO_MATCH);</span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="comment">// 添加 URI 匹配规则</span></span><br><span class="line">        uriMatcher.addURI(AUTHORITY, PATH, ITEMS); <span class="comment">// 匹配 content://com.example.provider/my_table</span></span><br><span class="line">        uriMatcher.addURI(AUTHORITY, PATH + <span class="string">&quot;/#&quot;</span>, ITEM_ID); <span class="comment">// 匹配content://com.example.provider/my_table/1</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 数据库帮助类对象，用于管理数据库的创建和版本控制</span></span><br><span class="line">    <span class="keyword">private</span> MyDatabaseHelper dbHelper;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// ContentProvider 初始化方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">onCreate</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 初始化数据库帮助类</span></span><br><span class="line">        dbHelper = <span class="keyword">new</span> <span class="title class_">MyDatabaseHelper</span>(getContext());</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">// 返回 true 表示初始化成功</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 查询数据</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Cursor <span class="title function_">query</span><span class="params">(Uri uri, String[] projection, String selection, String[] selectionArgs, String sortOrder)</span> &#123;</span><br><span class="line">        <span class="comment">// 获取可读数据库</span></span><br><span class="line">        <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> dbHelper.getReadableDatabase();</span><br><span class="line">        Cursor cursor;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 根据 URI 匹配码执行不同的查询逻辑</span></span><br><span class="line">        <span class="keyword">switch</span> (uriMatcher.match(uri)) &#123;</span><br><span class="line">            <span class="keyword">case</span> ITEMS:</span><br><span class="line">                <span class="comment">// 查询整个表</span></span><br><span class="line">                cursor = db.query(<span class="string">&quot;my_table&quot;</span>, projection, selection, selectionArgs, <span class="literal">null</span>, <span class="literal">null</span>, sortOrder);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> ITEM_ID:</span><br><span class="line">                <span class="comment">// 查询单条数据，根据 ID 查询</span></span><br><span class="line">                cursor = db.query(<span class="string">&quot;my_table&quot;</span>, projection, <span class="string">&quot;_id=?&quot;</span>, <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;uri.getLastPathSegment()&#125;, <span class="literal">null</span>, <span class="literal">null</span>, sortOrder);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">default</span>:</span><br><span class="line">                <span class="comment">// 如果 URI 不匹配，抛出异常</span></span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Unknown URI: &quot;</span> + uri);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置通知 URI，当数据变化时通知监听器</span></span><br><span class="line">        cursor.setNotificationUri(getContext().getContentResolver(), uri);</span><br><span class="line">        <span class="keyword">return</span> cursor; <span class="comment">// 返回查询结果</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 插入数据</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Uri <span class="title function_">insert</span><span class="params">(Uri uri, ContentValues values)</span> &#123;</span><br><span class="line">        <span class="comment">// 获取可写数据库</span></span><br><span class="line">        <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> dbHelper.getWritableDatabase();</span><br><span class="line">        <span class="comment">// 插入数据并返回新行的 ID</span></span><br><span class="line">        <span class="type">long</span> <span class="variable">id</span> <span class="operator">=</span> db.insert(<span class="string">&quot;my_table&quot;</span>, <span class="literal">null</span>, values);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (id &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">// 构造新插入数据的 URI</span></span><br><span class="line">            <span class="type">Uri</span> <span class="variable">newUri</span> <span class="operator">=</span> ContentUris.withAppendedId(CONTENT_URI, id);</span><br><span class="line">            <span class="comment">// 通知数据变化</span></span><br><span class="line">            getContext().getContentResolver().notifyChange(newUri, <span class="literal">null</span>);</span><br><span class="line">            <span class="keyword">return</span> newUri; <span class="comment">// 返回新插入数据的 URI</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 插入失败，抛出异常</span></span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Failed to insert row into &quot;</span> + uri);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 更新数据</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">update</span><span class="params">(Uri uri, ContentValues values, String selection, String[] selectionArgs)</span> &#123;</span><br><span class="line">        <span class="comment">// 获取可写数据库</span></span><br><span class="line">        <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> dbHelper.getWritableDatabase();</span><br><span class="line">        <span class="comment">// 更新数据并返回受影响的行数</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">rowsUpdated</span> <span class="operator">=</span> db.update(<span class="string">&quot;my_table&quot;</span>, values, selection, selectionArgs);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (rowsUpdated &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">// 通知数据变化</span></span><br><span class="line">            getContext().getContentResolver().notifyChange(uri, <span class="literal">null</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> rowsUpdated; <span class="comment">// 返回受影响的行数</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 删除数据</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">delete</span><span class="params">(Uri uri, String selection, String[] selectionArgs)</span> &#123;</span><br><span class="line">        <span class="comment">// 获取可写数据库</span></span><br><span class="line">        <span class="type">SQLiteDatabase</span> <span class="variable">db</span> <span class="operator">=</span> dbHelper.getWritableDatabase();</span><br><span class="line">        <span class="comment">// 删除数据并返回被删除的行数</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">rowsDeleted</span> <span class="operator">=</span> db.delete(<span class="string">&quot;my_table&quot;</span>, selection, selectionArgs);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (rowsDeleted &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">// 通知数据变化</span></span><br><span class="line">            getContext().getContentResolver().notifyChange(uri, <span class="literal">null</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> rowsDeleted; <span class="comment">// 返回被删除的行数</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 返回指定 URI 对应的 MIME 类型</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getType</span><span class="params">(Uri uri)</span> &#123;</span><br><span class="line">        <span class="comment">// 根据 URI 匹配码返回不同的 MIME 类型</span></span><br><span class="line">        <span class="keyword">switch</span> (uriMatcher.match(uri)) &#123;</span><br><span class="line">            <span class="keyword">case</span> ITEMS:</span><br><span class="line">                <span class="comment">// 多条数据的 MIME 类型</span></span><br><span class="line">                <span class="keyword">return</span> <span class="string">&quot;vnd.android.cursor.dir/vnd.com.example.provider.my_table&quot;</span>;</span><br><span class="line">            <span class="keyword">case</span> ITEM_ID:</span><br><span class="line">                <span class="comment">// 单条数据的 MIME 类型</span></span><br><span class="line">                <span class="keyword">return</span> <span class="string">&quot;vnd.android.cursor.item/vnd.com.example.provider.my_table&quot;</span>;</span><br><span class="line">            <span class="keyword">default</span>:</span><br><span class="line">                <span class="comment">// 如果 URI 不匹配，抛出异常</span></span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Unknown URI: &quot;</span> + uri);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h3 id="总结"><a class="markdownIt-Anchor" href="#总结"></a> 总结</h3><p><code>ContentProvider</code> 的 6 个核心方法分别是：</p><ol><li><code>onCreate()</code>：初始化。</li><li><code>query()</code>：查询数据。</li><li><code>insert()</code>：插入数据。</li><li><code>update()</code>：更新数据。</li><li><code>delete()</code>：删除数据。</li><li><code>getType()</code>：返回 MIME 类型。</li></ol><p>通过实现这些方法，可以创建一个功能完整的 <code>ContentProvider</code>，用于在 Android 应用中共享数据。</p><hr /><h2 id="手机多媒体"><a class="markdownIt-Anchor" href="#手机多媒体"></a> 手机多媒体</h2><h3 id="通知进阶"><a class="markdownIt-Anchor" href="#通知进阶"></a> 通知(进阶)</h3><p>1.创建通知通道并请求通知权限</p><p>2.用getSystemService()获取一个NotificationManager对通知进行管理</p><p>3.创建Notification对象并进行一些基础设置</p><p>4.用notify()显示通知</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 演示通知功能的主活动类</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> <span class="keyword">implements</span> <span class="title class_">View</span>.OnClickListener &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        <span class="comment">// 加载布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建通知通道（仅在 Android 8.0 及以上版本需要）</span></span><br><span class="line">        createNotificationChannel();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取按钮控件并绑定点击事件</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">sendNotice</span> <span class="operator">=</span> findViewById(R.id.send_notice);</span><br><span class="line">        sendNotice.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">        <span class="comment">// 判断是否是发送通知按钮被点击</span></span><br><span class="line">        <span class="keyword">if</span> (v.getId() == R.id.send_notice) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 创建一个Intent，用于在通知被点击时启动NotificationActivity</span></span><br><span class="line">                <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="built_in">this</span>, NotificationActivity.class);</span><br><span class="line">                <span class="comment">// 创建一个PendingIntent，作为通知的点击动作</span></span><br><span class="line">                <span class="type">PendingIntent</span> <span class="variable">pi</span> <span class="operator">=</span> PendingIntent.getActivity(<span class="built_in">this</span>, <span class="number">0</span>, intent, PendingIntent.FLAG_UPDATE_CURRENT);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 获取通知管理器实例</span></span><br><span class="line">                <span class="type">NotificationManager</span> <span class="variable">manager</span> <span class="operator">=</span> (NotificationManager) getSystemService(NOTIFICATION_SERVICE);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 构建通知对象</span></span><br><span class="line">                <span class="type">Notification</span> <span class="variable">notification</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NotificationCompat</span>.Builder(<span class="built_in">this</span>, <span class="string">&quot;default_channel_id&quot;</span>)</span><br><span class="line">                        <span class="comment">// 设置通知标题</span></span><br><span class="line">                        .setContentTitle(<span class="string">&quot;This is content title&quot;</span>)</span><br><span class="line">                        <span class="comment">// 设置通知内容</span></span><br><span class="line">                        .setContentText(<span class="string">&quot;Learn how to build notifications, send and sync data, and use voice actions. Get the official Android IDE and developer tools to build apps for Android.&quot;</span>)</span><br><span class="line">                        <span class="comment">// 设置通知时间</span></span><br><span class="line">                        .setWhen(System.currentTimeMillis())</span><br><span class="line">                        <span class="comment">// 设置小图标</span></span><br><span class="line">                        .setSmallIcon(R.mipmap.ic_launcher)</span><br><span class="line">                        <span class="comment">// 设置大图标</span></span><br><span class="line">                        .setLargeIcon(BitmapFactory.decodeResource(getResources(), R.mipmap.ic_launcher))</span><br><span class="line">                        <span class="comment">// 设置通知点击的Intent</span></span><br><span class="line">                        .setContentIntent(pi)</span><br><span class="line">                        <span class="comment">// 设置自动取消通知</span></span><br><span class="line">                        .setAutoCancel(<span class="literal">true</span>)</span><br><span class="line">                        <span class="comment">// 设置默认通知声音、震动、灯光</span></span><br><span class="line">                        .setDefaults(NotificationCompat.DEFAULT_ALL)</span><br><span class="line">                        <span class="comment">// 使用大文本样式</span></span><br><span class="line">                        .setStyle(<span class="keyword">new</span> <span class="title class_">NotificationCompat</span>.BigTextStyle().bigText(<span class="string">&quot;Learn how to build notifications, send and&quot;</span> +</span><br><span class="line">                                <span class="string">&quot;sync data, and use voice actions,Get the official Android IDE and developer tools to build apps for Android.&quot;</span>))</span><br><span class="line">                        <span class="comment">// 设置通知优先级</span></span><br><span class="line">                        .setPriority(NotificationCompat.PRIORITY_MAX)</span><br><span class="line">                        <span class="comment">// 构建通知对象</span></span><br><span class="line">                        .build();</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 处理 Android 13 及以上版本的 POST_NOTIFICATIONS 权限</span></span><br><span class="line">                <span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.TIRAMISU) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (ContextCompat.checkSelfPermission(MainActivity.<span class="built_in">this</span>, Manifest.permission.POST_NOTIFICATIONS) != PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                        <span class="comment">// 如果没有权限，请求权限</span></span><br><span class="line">                        ActivityCompat.requestPermissions(MainActivity.<span class="built_in">this</span>, <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;Manifest.permission.POST_NOTIFICATIONS&#125;, <span class="number">1</span>);</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="comment">// 如果有权限，发送通知</span></span><br><span class="line">                        manager.notify(<span class="number">1</span>, notification);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// Android 13 以下版本直接发送通知</span></span><br><span class="line">                    manager.notify(<span class="number">1</span>, notification);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                <span class="comment">// 捕获并处理异常</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">                Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;发送通知失败： &quot;</span> + e.getMessage(), Toast.LENGTH_LONG).show();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onRequestPermissionsResult</span><span class="params">(<span class="type">int</span> requestCode, <span class="meta">@NonNull</span> String[] permissions, <span class="meta">@NonNull</span> <span class="type">int</span>[] grantResults)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onRequestPermissionsResult(requestCode, permissions, grantResults);</span><br><span class="line">        <span class="comment">// 获取通知管理器实例</span></span><br><span class="line">        <span class="type">NotificationManager</span> <span class="variable">manager</span> <span class="operator">=</span> (NotificationManager) getSystemService(NOTIFICATION_SERVICE);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 重新构建通知对象（与 onClick 中相同）</span></span><br><span class="line">            <span class="type">Notification</span> <span class="variable">notification</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NotificationCompat</span>.Builder(<span class="built_in">this</span>, <span class="string">&quot;default_channel_id&quot;</span>)</span><br><span class="line">                    .setContentTitle(<span class="string">&quot;This is content title&quot;</span>)</span><br><span class="line">                    .setContentText(<span class="string">&quot;Learn how to build notifications, send and sync data, and use voice actions. Get the official Android IDE and developer tools to build apps for Android.&quot;</span>)</span><br><span class="line">                    .setWhen(System.currentTimeMillis())</span><br><span class="line">                    .setSmallIcon(R.mipmap.ic_launcher)</span><br><span class="line">                    .setLargeIcon(BitmapFactory.decodeResource(getResources(), R.mipmap.ic_launcher))</span><br><span class="line">                    .setAutoCancel(<span class="literal">true</span>)</span><br><span class="line">                    .setDefaults(NotificationCompat.DEFAULT_ALL)</span><br><span class="line">                    .setStyle(<span class="keyword">new</span> <span class="title class_">NotificationCompat</span>.BigTextStyle().bigText(<span class="string">&quot;Learn how to build notifications, send and&quot;</span> +</span><br><span class="line">                            <span class="string">&quot;sync data, and use voice actions,Get the official Android IDE and developer tools to build apps for Android.&quot;</span>))</span><br><span class="line">                    .setPriority(NotificationCompat.PRIORITY_MAX)</span><br><span class="line">                    .build();</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 根据请求码处理权限请求结果</span></span><br><span class="line">            <span class="keyword">switch</span> (requestCode) &#123;</span><br><span class="line">                <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">                    <span class="comment">// 处理 POST_NOTIFICATIONS 权限请求结果</span></span><br><span class="line">                    <span class="keyword">if</span> (grantResults.length &gt; <span class="number">0</span> &amp;&amp; grantResults[<span class="number">0</span>] == PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                        <span class="comment">// 权限授予，发送通知</span></span><br><span class="line">                        manager.notify(<span class="number">1</span>, notification);</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="comment">// 权限被拒绝，显示提示信息</span></span><br><span class="line">                        Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;You denied the permission&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">default</span>:</span><br><span class="line">                    <span class="comment">// 处理其他请求码</span></span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="comment">// 捕获并处理异常</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">            Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;发送通知失败： &quot;</span> + e.getMessage(), Toast.LENGTH_LONG).show();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 创建通知通道（仅在 Android 8.0 及以上版本需要）</span></span><br><span class="line"><span class="comment">     * 通知通道用于分组和管理不同的通知</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">createNotificationChannel</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.O) &#123;</span><br><span class="line">            <span class="comment">// 创建一个 NotificationChannel 对象</span></span><br><span class="line">            <span class="type">NotificationChannel</span> <span class="variable">channel</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NotificationChannel</span>(</span><br><span class="line">                    <span class="string">&quot;default_channel_id&quot;</span>, <span class="comment">// 通道 ID</span></span><br><span class="line">                    <span class="string">&quot;Default Channel&quot;</span>,    <span class="comment">// 通道名称</span></span><br><span class="line">                    NotificationManager.IMPORTANCE_DEFAULT <span class="comment">// 重要性级别</span></span><br><span class="line">            );</span><br><span class="line">            <span class="comment">// 获取通知管理器实例</span></span><br><span class="line">            <span class="type">NotificationManager</span> <span class="variable">manager</span> <span class="operator">=</span> (NotificationManager) getSystemService(NOTIFICATION_SERVICE);</span><br><span class="line">            <span class="comment">// 创建通知通道</span></span><br><span class="line">            manager.createNotificationChannel(channel);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">NotificationActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.notification_layout);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.POST_NOTIFICATIONS&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.VIBRATE&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure><h3 id="调用摄像头拍照"><a class="markdownIt-Anchor" href="#调用摄像头拍照"></a> 调用摄像头拍照</h3><p>1.设置Button和ImageView控件</p><p>2.申请相机权限,创建File对象存放在SD卡的应用关联缓存目录下</p><p>3.把File转换为Uri,被内容提供器FileProvider通过getUriForFile()接收</p><p>4.构建intent启动相机</p><p>5.重写onActivityResult处理结果</p><p>6.在AndroidManifest.xml注册内容提供器,声明权限</p><p>7.在xml目录下创建file_paths.xml共享路径</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/main&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">tools:context</span>=<span class="string">&quot;.MainActivity&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">Button</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/take_photo&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;Take Photo&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">ImageView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/picture&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_gravity</span>=<span class="string">&quot;center_horizontal&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 主活动类，实现了拍照和从相册选择图片的功能</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 日志标签，用于日志输出</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">TAG</span> <span class="operator">=</span> <span class="string">&quot;MainActivity&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 请求拍照的常量</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">TAKE_PHOTO</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 权限请求码</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">REQUEST_PERMISSION</span> <span class="operator">=</span> <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用于显示图片的ImageView</span></span><br><span class="line">    <span class="keyword">private</span> ImageView picture;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用于保存图片的Uri</span></span><br><span class="line">    <span class="keyword">private</span> Uri imageUri;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        <span class="comment">// 加载布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取控件实例</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">takePhoto</span> <span class="operator">=</span> findViewById(R.id.take_photo);</span><br><span class="line">        <span class="type">Button</span> <span class="variable">chooseFromAlbum</span> <span class="operator">=</span> findViewById(R.id.choose_from_album);</span><br><span class="line">        picture = findViewById(R.id.picture);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 检查相机权限</span></span><br><span class="line">        <span class="keyword">if</span> (ContextCompat.checkSelfPermission(<span class="built_in">this</span>, Manifest.permission.CAMERA) != PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">            <span class="comment">// 如果没有权限，请求相机权限</span></span><br><span class="line">            ActivityCompat.requestPermissions(<span class="built_in">this</span>, <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;Manifest.permission.CAMERA&#125;, REQUEST_PERMISSION);</span><br><span class="line">            Log.d(TAG, <span class="string">&quot;onCreate: 请求相机权限&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 检查设备是否具备相机功能</span></span><br><span class="line">        <span class="keyword">if</span> (!getPackageManager().hasSystemFeature(PackageManager.FEATURE_CAMERA_ANY)) &#123;</span><br><span class="line">            <span class="comment">// 如果设备没有相机，显示提示信息并返回</span></span><br><span class="line">            Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;设备没有相机功能&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置拍照按钮的点击事件监听器</span></span><br><span class="line">        takePhoto.setOnClickListener(v -&gt; &#123;</span><br><span class="line">            <span class="comment">// 创建一个文件用于保存拍摄的图片</span></span><br><span class="line">            <span class="type">File</span> <span class="variable">outputImage</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">File</span>(getExternalCacheDir(), <span class="string">&quot;output_image.jpg&quot;</span>);</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (outputImage.exists()) &#123;</span><br><span class="line">                    <span class="comment">// 如果文件已存在，删除旧文件</span></span><br><span class="line">                    outputImage.delete();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 创建新文件</span></span><br><span class="line">                outputImage.createNewFile();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                <span class="comment">// 捕获并打印异常</span></span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 根据Android版本适配Uri的获取方式</span></span><br><span class="line">            <span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= <span class="number">24</span>) &#123;</span><br><span class="line">                <span class="comment">// Android 7.0及以上版本，使用FileProvider获取Uri</span></span><br><span class="line">                Log.d(TAG, <span class="string">&quot;onCreate: 3&quot;</span>);</span><br><span class="line">                imageUri = FileProvider.getUriForFile(MainActivity.<span class="built_in">this</span>, <span class="string">&quot;com.example.cameraalbumtest.fileprovider&quot;</span>, outputImage);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 旧版本直接使用Uri.fromFile()</span></span><br><span class="line">                imageUri = Uri.fromFile(outputImage);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 创建Intent启动相机应用</span></span><br><span class="line">            <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="string">&quot;android.media.action.IMAGE_CAPTURE&quot;</span>);</span><br><span class="line">            <span class="comment">// 将拍照保存路径传递给相机应用</span></span><br><span class="line">            intent.putExtra(MediaStore.EXTRA_OUTPUT, imageUri);</span><br><span class="line">            <span class="comment">// 启动相机并等待结果</span></span><br><span class="line">            startActivityForResult(intent, TAKE_PHOTO);</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理拍照和选择图片的结果</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> requestCode 请求码，用于区分不同的操作</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> resultCode 结果码，表示操作的结果</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> data 返回的数据，包含图片信息</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@SuppressLint(&quot;MissingSuperCall&quot;)</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onActivityResult</span><span class="params">(<span class="type">int</span> requestCode, <span class="type">int</span> resultCode, Intent data)</span> &#123;</span><br><span class="line">        <span class="comment">// 确保返回的是拍照结果</span></span><br><span class="line">        <span class="keyword">if</span> (requestCode == TAKE_PHOTO) &#123;</span><br><span class="line">            <span class="comment">// 如果拍照成功</span></span><br><span class="line">            <span class="keyword">if</span> (resultCode == RESULT_OK) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="comment">// 使用 BitmapFactory.Options 缩放图片，避免内存溢出</span></span><br><span class="line">                    BitmapFactory.<span class="type">Options</span> <span class="variable">options</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BitmapFactory</span>.Options();</span><br><span class="line">                    options.inSampleSize = <span class="number">4</span>; <span class="comment">// 设置缩放比例，减少内存消耗</span></span><br><span class="line">                    <span class="comment">// 从Uri中获取图片并转换为Bitmap</span></span><br><span class="line">                    <span class="type">Bitmap</span> <span class="variable">bitmap</span> <span class="operator">=</span> BitmapFactory.decodeStream(getContentResolver().openInputStream(imageUri), <span class="literal">null</span>, options);</span><br><span class="line">                    <span class="comment">// 设置Bitmap到ImageView中显示</span></span><br><span class="line">                    picture.setImageBitmap(bitmap);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (FileNotFoundException e) &#123;</span><br><span class="line">                    <span class="comment">// 捕获文件未找到异常</span></span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; </span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理权限请求的结果</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> requestCode 请求码</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> permissions 请求的权限数组</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> grantResults 权限授予结果数组</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onRequestPermissionsResult</span><span class="params">(<span class="type">int</span> requestCode, String[] permissions, <span class="type">int</span>[] grantResults)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onRequestPermissionsResult(requestCode, permissions, grantResults);</span><br><span class="line">        <span class="comment">// 如果请求的是相机权限</span></span><br><span class="line">        <span class="keyword">if</span> (requestCode == REQUEST_PERMISSION) &#123;</span><br><span class="line">            <span class="comment">// 判断权限是否被授予</span></span><br><span class="line">            <span class="keyword">if</span> (grantResults.length &gt; <span class="number">0</span> &amp;&amp; grantResults[<span class="number">0</span>] == PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                <span class="comment">// 权限授予，提示用户</span></span><br><span class="line">                Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;相机权限已授予&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 权限被拒绝，提示用户</span></span><br><span class="line">                Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;相机权限被拒绝&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (requestCode == <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="comment">// 如果请求的是存储权限</span></span><br><span class="line">            <span class="keyword">if</span> (grantResults.length &gt; <span class="number">0</span> &amp;&amp; grantResults[<span class="number">0</span>] == PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                <span class="comment">// 权限授予，打开相册</span></span><br><span class="line">                openAlbum();</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 权限被拒绝，提示用户</span></span><br><span class="line">                Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;存储权限被拒绝，无法打开相册&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.CAMERA&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">uses-feature</span> <span class="attr">android:name</span>=<span class="string">&quot;android.hardware.camera&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">uses-feature</span> <span class="attr">android:name</span>=<span class="string">&quot;android.hardware.camera.autofocus&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.WRITE_EXTERNAL_STORAGE&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.READ_EXTERNAL_STORAGE&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 声明一个内容提供者，用于在应用程序之间安全地共享文件 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">provider</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:name</span>=<span class="string">&quot;androidx.core.content.FileProvider&quot;</span></span></span><br><span class="line"><span class="tag">    &lt;!<span class="attr">--</span> <span class="attr">指定内容提供者的类名</span>，<span class="attr">这里使用的是FileProvider</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">    android:authorities=&quot;com.example.cameraalbumtest.fileprovider&quot;</span><br><span class="line">    <span class="comment">&lt;!-- 定义内容提供者的authority，用于唯一标识这个provider，通常使用应用包名加上自定义后缀 --&gt;</span></span><br><span class="line">    android:exported=&quot;false&quot;</span><br><span class="line">    <span class="comment">&lt;!-- 设置provider是否可以被其他应用访问，这里设置为false，表示只有当前应用可以访问 --&gt;</span></span><br><span class="line">    android:grantUriPermissions=&quot;true&quot;&gt;</span><br><span class="line">    <span class="comment">&lt;!-- 设置是否允许共享URI权限，true表示允许其他应用访问指定的文件 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta-data</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:name</span>=<span class="string">&quot;android.support.FILE_PROVIDER_PATHS&quot;</span></span></span><br><span class="line"><span class="tag">        &lt;!<span class="attr">--</span> <span class="attr">指定meta-data的名称</span>，<span class="attr">这里使用FILE_PROVIDER_PATHS</span>，<span class="attr">用于FileProvider的路径配置</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">        android:resource=&quot;@xml/file_paths&quot; /&gt;</span><br><span class="line">    <span class="comment">&lt;!-- 指定文件路径的配置文件，@xml/file_paths表示文件路径在res/xml/file_paths.xml中定义 --&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">provider</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 定义文件路径的根元素 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">paths</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 定义一个外部存储路径 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">external-path</span></span></span><br><span class="line"><span class="tag">        <span class="attr">name</span>=<span class="string">&quot;my_images&quot;</span> &lt;!<span class="attr">--</span> <span class="attr">路径的名称</span>，<span class="attr">用于在应用中引用该路径</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">        path=&quot;.&quot; /&gt; <span class="comment">&lt;!-- 路径的位置，此处表示外部存储的根目录 --&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">paths</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="从相册中选择图片"><a class="markdownIt-Anchor" href="#从相册中选择图片"></a> 从相册中选择图片</h3><p>1.申请权限</p><p>2.创建并调用openAlum()方法</p><p>3.重写onActivityResult处理结果</p><p>4.针对不同图片格式进行不同操作</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br><span class="line">200</span><br><span class="line">201</span><br><span class="line">202</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 主活动类，实现了拍照和从相册选择图片的功能</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 日志标签，用于日志输出</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">TAG</span> <span class="operator">=</span> <span class="string">&quot;MainActivity&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 请求拍照的常量</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">TAKE_PHOTO</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 权限请求码</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">REQUEST_PERMISSION</span> <span class="operator">=</span> <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 从相册选择图片的请求码</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">CHOOSE_PHOTO</span> <span class="operator">=</span> <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用于显示图片的ImageView</span></span><br><span class="line">    <span class="keyword">private</span> ImageView picture;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用于保存图片的Uri</span></span><br><span class="line">    <span class="keyword">private</span> Uri imageUri;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        <span class="comment">// 加载布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取控件实例</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">takePhoto</span> <span class="operator">=</span> findViewById(R.id.take_photo);</span><br><span class="line">        <span class="type">Button</span> <span class="variable">chooseFromAlbum</span> <span class="operator">=</span> findViewById(R.id.choose_from_album);</span><br><span class="line">        picture = findViewById(R.id.picture);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置从相册选择图片按钮的点击事件监听器</span></span><br><span class="line">        chooseFromAlbum.setOnClickListener(v -&gt; &#123;</span><br><span class="line">            <span class="comment">// 检查存储读取权限</span></span><br><span class="line">            <span class="keyword">if</span> (ContextCompat.checkSelfPermission(MainActivity.<span class="built_in">this</span>, Manifest.permission.WRITE_EXTERNAL_STORAGE) != PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                <span class="comment">// 如果没有权限，请求权限</span></span><br><span class="line">                ActivityCompat.requestPermissions(MainActivity.<span class="built_in">this</span>, <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;Manifest.permission.WRITE_EXTERNAL_STORAGE&#125;, <span class="number">1</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 如果有权限，打开相册</span></span><br><span class="line">                openAlbum();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 打开相册，选择图片</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">openAlbum</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="string">&quot;android.intent.action.GET_CONTENT&quot;</span>);</span><br><span class="line">        <span class="comment">// 设置类型为图片</span></span><br><span class="line">        intent.setType(<span class="string">&quot;image/*&quot;</span>);</span><br><span class="line">        <span class="comment">// 启动相册并等待结果</span></span><br><span class="line">        startActivityForResult(intent, CHOOSE_PHOTO);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理拍照和选择图片的结果</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> requestCode 请求码，用于区分不同的操作</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> resultCode 结果码，表示操作的结果</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> data 返回的数据，包含图片信息</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@SuppressLint(&quot;MissingSuperCall&quot;)</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onActivityResult</span><span class="params">(<span class="type">int</span> requestCode, <span class="type">int</span> resultCode, Intent data)</span> &#123;</span><br><span class="line">        <span class="comment">// 确保返回的是拍照结果</span></span><br><span class="line">        <span class="keyword">if</span> (requestCode == TAKE_PHOTO) &#123;</span><br><span class="line">            <span class="comment">// 如果拍照成功</span></span><br><span class="line">            <span class="keyword">if</span> (resultCode == RESULT_OK) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="comment">// 使用 BitmapFactory.Options 缩放图片，避免内存溢出</span></span><br><span class="line">                    BitmapFactory.<span class="type">Options</span> <span class="variable">options</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BitmapFactory</span>.Options();</span><br><span class="line">                    options.inSampleSize = <span class="number">4</span>; <span class="comment">// 设置缩放比例，减少内存消耗</span></span><br><span class="line">                    <span class="comment">// 从Uri中获取图片并转换为Bitmap</span></span><br><span class="line">                    <span class="type">Bitmap</span> <span class="variable">bitmap</span> <span class="operator">=</span> BitmapFactory.decodeStream(getContentResolver().openInputStream(imageUri), <span class="literal">null</span>, options);</span><br><span class="line">                    <span class="comment">// 设置Bitmap到ImageView中显示</span></span><br><span class="line">                    picture.setImageBitmap(bitmap);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (FileNotFoundException e) &#123;</span><br><span class="line">                    <span class="comment">// 捕获文件未找到异常</span></span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (requestCode == CHOOSE_PHOTO) &#123;</span><br><span class="line">            <span class="comment">// 如果是从相册选择图片的结果</span></span><br><span class="line">            <span class="keyword">if</span> (resultCode == RESULT_OK) &#123;</span><br><span class="line">                <span class="comment">// 根据Android版本处理图片</span></span><br><span class="line">                <span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= <span class="number">19</span>) &#123;</span><br><span class="line">                    handleImageOnKitKat(data);</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    handleImageBeforeKitKat(data);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理权限请求的结果</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> requestCode 请求码</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> permissions 请求的权限数组</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> grantResults 权限授予结果数组</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onRequestPermissionsResult</span><span class="params">(<span class="type">int</span> requestCode, String[] permissions, <span class="type">int</span>[] grantResults)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onRequestPermissionsResult(requestCode, permissions, grantResults);</span><br><span class="line">        <span class="comment">// 如果请求的是相机权限</span></span><br><span class="line">        <span class="keyword">if</span> (requestCode == REQUEST_PERMISSION) &#123;</span><br><span class="line">            <span class="comment">// 判断权限是否被授予</span></span><br><span class="line">            <span class="keyword">if</span> (grantResults.length &gt; <span class="number">0</span> &amp;&amp; grantResults[<span class="number">0</span>] == PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                <span class="comment">// 权限授予，提示用户</span></span><br><span class="line">                Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;相机权限已授予&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 权限被拒绝，提示用户</span></span><br><span class="line">                Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;相机权限被拒绝&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (requestCode == <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="comment">// 如果请求的是存储权限</span></span><br><span class="line">            <span class="keyword">if</span> (grantResults.length &gt; <span class="number">0</span> &amp;&amp; grantResults[<span class="number">0</span>] == PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                <span class="comment">// 权限授予，打开相册</span></span><br><span class="line">                openAlbum();</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 权限被拒绝，提示用户</span></span><br><span class="line">                Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;存储权限被拒绝，无法打开相册&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理Android 4.4及以上版本的图片选择结果</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> data 包含图片信息的Intent</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@TargetApi(19)</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">handleImageOnKitKat</span><span class="params">(Intent data)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">imagePath</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="type">Uri</span> <span class="variable">uri</span> <span class="operator">=</span> data.getData();</span><br><span class="line">        <span class="keyword">if</span> (DocumentsContract.isDocumentUri(<span class="built_in">this</span>, uri)) &#123;</span><br><span class="line">            <span class="type">String</span> <span class="variable">docId</span> <span class="operator">=</span> DocumentsContract.getDocumentId(uri);</span><br><span class="line">            <span class="keyword">if</span> (<span class="string">&quot;com.android.providers.media.documents&quot;</span>.equals(uri.getAuthority())) &#123;</span><br><span class="line">                <span class="comment">// 处理媒体库中的图片</span></span><br><span class="line">                <span class="type">String</span> <span class="variable">id</span> <span class="operator">=</span> docId.split(<span class="string">&quot;:&quot;</span>)[<span class="number">1</span>];</span><br><span class="line">                <span class="type">String</span> <span class="variable">selection</span> <span class="operator">=</span> MediaStore.Images.Media._ID + <span class="string">&quot;=&quot;</span> + id;</span><br><span class="line">                imagePath = getImagePath(MediaStore.Images.Media.EXTERNAL_CONTENT_URI, selection);</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;com.android.providers.downloads.documents&quot;</span>.equals(uri.getAuthority())) &#123;</span><br><span class="line">                <span class="comment">// 处理下载目录中的图片</span></span><br><span class="line">                <span class="type">Uri</span> <span class="variable">contentUri</span> <span class="operator">=</span> ContentUris.withAppendedId(Uri.parse(<span class="string">&quot;content://downloads/public_downloads&quot;</span>), Long.valueOf(docId));</span><br><span class="line">                imagePath = getImagePath(contentUri, <span class="literal">null</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;content&quot;</span>.equalsIgnoreCase(uri.getScheme())) &#123;</span><br><span class="line">            <span class="comment">// 处理内容提供者中的图片</span></span><br><span class="line">            imagePath = getImagePath(uri, <span class="literal">null</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="string">&quot;file&quot;</span>.equalsIgnoreCase(uri.getScheme())) &#123;</span><br><span class="line">            <span class="comment">// 处理文件路径中的图片</span></span><br><span class="line">            imagePath = uri.getPath();</span><br><span class="line">        &#125;</span><br><span class="line">        displayImage(imagePath);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理Android 4.4以下版本的图片选择结果</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> data 包含图片信息的Intent</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">handleImageBeforeKitKat</span><span class="params">(Intent data)</span> &#123;</span><br><span class="line">        <span class="type">Uri</span> <span class="variable">uri</span> <span class="operator">=</span> data.getData();</span><br><span class="line">        <span class="type">String</span> <span class="variable">imagePath</span> <span class="operator">=</span> getImagePath(uri, <span class="literal">null</span>);</span><br><span class="line">        displayImage(imagePath);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 获取图片路径</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> uri 图片的Uri</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> selection 查询条件</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 图片路径</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@SuppressLint(&quot;Range&quot;)</span></span><br><span class="line">    <span class="keyword">private</span> String <span class="title function_">getImagePath</span><span class="params">(Uri uri, String selection)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">path</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="type">Cursor</span> <span class="variable">cursor</span> <span class="operator">=</span> getContentResolver().query(uri, <span class="literal">null</span>, selection, <span class="literal">null</span>, <span class="literal">null</span>);</span><br><span class="line">        <span class="keyword">if</span> (cursor != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (cursor.moveToFirst()) &#123;</span><br><span class="line">                path = cursor.getString(cursor.getColumnIndex(MediaStore.Images.Media.DATA));</span><br><span class="line">            &#125;</span><br><span class="line">            cursor.close();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> path;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 显示图片</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> imagePath 图片路径</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">displayImage</span><span class="params">(String imagePath)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (imagePath != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 将图片文件解码为Bitmap</span></span><br><span class="line">            <span class="type">Bitmap</span> <span class="variable">bitmap</span> <span class="operator">=</span> BitmapFactory.decodeFile(imagePath);</span><br><span class="line">            <span class="comment">// 显示Bitmap到ImageView中</span></span><br><span class="line">            picture.setImageBitmap(bitmap);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;failed to get image&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="播放音频"><a class="markdownIt-Anchor" href="#播放音频"></a> 播放音频</h3><p>1.创造一个MediaPlayer对象</p><p>2.动态申请权限</p><p>3.调用initMediaPlayer()进行初始化</p><p><img src="E:/%E6%A1%8C%E9%9D%A2/Android%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/images/MediaPlayer%E6%93%8D%E4%BD%9C.jpg" alt="MediaPlayer操作" /></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/main&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">tools:context</span>=<span class="string">&quot;.MainActivity&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">Button</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/play&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;Play&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">Button</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/pause&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;Pause&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">Button</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/stop&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;Stop&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * MainActivity类，负责控制音频播放。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> <span class="keyword">implements</span> <span class="title class_">View</span>.OnClickListener &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// MediaPlayer对象，用于播放音频。</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">MediaPlayer</span> <span class="variable">mediaPlayer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MediaPlayer</span>();</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取控件。</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">play</span> <span class="operator">=</span> (Button) findViewById(R.id.play);</span><br><span class="line">        <span class="type">Button</span> <span class="variable">pause</span> <span class="operator">=</span> (Button) findViewById(R.id.pause);</span><br><span class="line">        <span class="type">Button</span> <span class="variable">stop</span> <span class="operator">=</span> (Button) findViewById(R.id.stop);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 为控件设置点击监听器。</span></span><br><span class="line">        play.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">        pause.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">        stop.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 检查是否有写外部存储权限，如果没有，则请求权限。</span></span><br><span class="line">        <span class="keyword">if</span> (ContextCompat.checkSelfPermission(MainActivity.<span class="built_in">this</span>, Manifest.permission.WRITE_EXTERNAL_STORAGE) != PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">            ActivityCompat.requestPermissions(MainActivity.<span class="built_in">this</span>, <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;Manifest.permission.WRITE_EXTERNAL_STORAGE&#125;, <span class="number">1</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 如果有权限，则初始化MediaPlayer。</span></span><br><span class="line">            initMediaPlayer();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 初始化MediaPlayer，设置音频文件路径和准备播放。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">initMediaPlayer</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 获取外部存储目录下的音频文件。</span></span><br><span class="line">            <span class="type">File</span> <span class="variable">file</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">File</span>(Environment.getExternalStorageDirectory(), <span class="string">&quot;music.mp3&quot;</span>);</span><br><span class="line">            <span class="comment">// 设置音频文件路径。</span></span><br><span class="line">            mediaPlayer.setDataSource(file.getPath());</span><br><span class="line">            <span class="comment">// 准备播放。</span></span><br><span class="line">            mediaPlayer.prepare();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="comment">// 如果发生异常，则打印异常信息。</span></span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onRequestPermissionsResult</span><span class="params">(<span class="type">int</span> requestCode, <span class="meta">@NonNull</span> String[] permissions, <span class="meta">@NonNull</span> <span class="type">int</span>[] grantResults)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onRequestPermissionsResult(requestCode, permissions, grantResults);</span><br><span class="line">        <span class="keyword">switch</span> (requestCode) &#123;</span><br><span class="line">            <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">                <span class="comment">// 如果用户同意了权限请求，则初始化MediaPlayer。</span></span><br><span class="line">                <span class="keyword">if</span> (grantResults.length &gt; <span class="number">0</span> &amp;&amp; grantResults[<span class="number">0</span>] == PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                    initMediaPlayer();</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 如果用户拒绝了权限请求，则提示用户无法使用程序。</span></span><br><span class="line">                    Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;拒绝权限将无法使用程序&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">                    finish();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">default</span>:</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">        <span class="comment">// 根据点击的控件ID执行相应的操作。</span></span><br><span class="line">        <span class="keyword">if</span> (v.getId() == R.id.play) &#123;</span><br><span class="line">            <span class="comment">// 如果点击的是播放按钮，则开始播放音频。</span></span><br><span class="line">            <span class="keyword">if</span> (!mediaPlayer.isPlaying()) &#123;</span><br><span class="line">                mediaPlayer.start();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (v.getId() == R.id.pause) &#123;</span><br><span class="line">            <span class="comment">// 如果点击的是暂停按钮，则暂停播放音频。</span></span><br><span class="line">            <span class="keyword">if</span> (mediaPlayer.isPlaying()) &#123;</span><br><span class="line">                mediaPlayer.pause();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (v.getId() == R.id.stop) &#123;</span><br><span class="line">            <span class="comment">// 如果点击的是停止按钮，则停止播放音频并重新初始化MediaPlayer。</span></span><br><span class="line">            <span class="keyword">if</span> (mediaPlayer.isPlaying()) &#123;</span><br><span class="line">                mediaPlayer.reset();</span><br><span class="line">                initMediaPlayer();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onDestroy();</span><br><span class="line">        <span class="comment">// 在程序销毁时释放MediaPlayer资源。</span></span><br><span class="line">        <span class="keyword">if</span> (mediaPlayer != <span class="literal">null</span>) &#123;</span><br><span class="line">            mediaPlayer.release();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="播放视频"><a class="markdownIt-Anchor" href="#播放视频"></a> 播放视频</h3><p>1.创造一个VideoView对象</p><p>2.动态申请权限</p><p>3.调用initVideoPath()进行初始化</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/main&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">tools:context</span>=<span class="string">&quot;.MainActivity&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">LinearLayout</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">Button</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/play&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;0dp&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_weight</span>=<span class="string">&quot;1&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:text</span>=<span class="string">&quot;Play&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">Button</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/pause&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;0dp&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_weight</span>=<span class="string">&quot;1&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:text</span>=<span class="string">&quot;Pause&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">Button</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/replay&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;0dp&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_weight</span>=<span class="string">&quot;1&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:text</span>=<span class="string">&quot;Replay&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">VideoView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/video_view&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 主活动类，继承自AppCompatActivity，实现OnClickListener接口</span></span><br><span class="line"><span class="comment"> * 用于播放视频的基本操作：播放、暂停、重新播放</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> <span class="keyword">implements</span> <span class="title class_">View</span>.OnClickListener &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 声明VideoView控件，用于播放视频</span></span><br><span class="line">    <span class="keyword">private</span> VideoView videoView;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        <span class="comment">// 加载布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 初始化VideoView控件</span></span><br><span class="line">        videoView = (VideoView) findViewById(R.id.video_view);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 初始化播放按钮</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">play</span> <span class="operator">=</span> (Button) findViewById(R.id.play);</span><br><span class="line">        <span class="comment">// 初始化暂停按钮</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">pause</span> <span class="operator">=</span> (Button) findViewById(R.id.pause);</span><br><span class="line">        <span class="comment">// 初始化重新播放按钮</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">replay</span> <span class="operator">=</span> (Button) findViewById(R.id.replay);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 为按钮设置点击事件监听器</span></span><br><span class="line">        play.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">        pause.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">        replay.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 检查写入外部存储的权限</span></span><br><span class="line">        <span class="keyword">if</span> (ContextCompat.checkSelfPermission(MainActivity.<span class="built_in">this</span>, Manifest.permission.WRITE_EXTERNAL_STORAGE) != PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">            <span class="comment">// 如果没有权限，请求权限</span></span><br><span class="line">            ActivityCompat.requestPermissions(MainActivity.<span class="built_in">this</span>, <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;Manifest.permission.WRITE_EXTERNAL_STORAGE&#125;, <span class="number">1</span>);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 如果有权限，初始化视频路径</span></span><br><span class="line">            initVideoPath();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 初始化视频文件路径</span></span><br><span class="line"><span class="comment">     * 从外部存储目录中查找名为movie.mp4的视频文件，并设置给VideoView</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">initVideoPath</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 获取外部存储目录中的movie.mp4文件</span></span><br><span class="line">        <span class="type">File</span> <span class="variable">file</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">File</span>(Environment.getExternalStorageDirectory(), <span class="string">&quot;movie.mp4&quot;</span>);</span><br><span class="line">        <span class="comment">// 设置视频路径到VideoView</span></span><br><span class="line">        videoView.setVideoPath(file.getPath());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理权限请求的回调</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> requestCode 请求码</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> permissions 请求的权限数组</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> grantResults 权限授予结果数组</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> deviceId 设备ID（这里未使用）</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onRequestPermissionsResult</span><span class="params">(<span class="type">int</span> requestCode, <span class="meta">@NonNull</span> String[] permissions, <span class="meta">@NonNull</span> <span class="type">int</span>[] grantResults, <span class="type">int</span> deviceId)</span> &#123;</span><br><span class="line">        <span class="keyword">switch</span> (requestCode) &#123;</span><br><span class="line">            <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">                <span class="comment">// 处理写入外部存储权限的结果</span></span><br><span class="line">                <span class="keyword">if</span> (grantResults.length &gt; <span class="number">0</span> &amp;&amp; grantResults[<span class="number">0</span>] == PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                    <span class="comment">// 如果权限授予，初始化视频路径</span></span><br><span class="line">                    initVideoPath();</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 如果权限拒绝，显示提示并结束活动</span></span><br><span class="line">                    Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;拒绝权限将无法使用程序&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">                    finish();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">default</span>:</span><br><span class="line">                <span class="comment">// 处理其他请求码</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理按钮点击事件</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> v 被点击的视图</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (v.getId() == R.id.play) &#123;</span><br><span class="line">            <span class="comment">// 播放按钮点击事件</span></span><br><span class="line">            <span class="keyword">if</span> (!videoView.isPlaying()) &#123;</span><br><span class="line">                <span class="comment">// 如果没有在播放，开始播放</span></span><br><span class="line">                videoView.start();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (v.getId() == R.id.pause) &#123;</span><br><span class="line">            <span class="comment">// 暂停按钮点击事件</span></span><br><span class="line">            <span class="keyword">if</span> (videoView.isPlaying()) &#123;</span><br><span class="line">                <span class="comment">// 如果正在播放，暂停播放</span></span><br><span class="line">                videoView.pause();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (v.getId() == R.id.replay) &#123;</span><br><span class="line">            <span class="comment">// 重新播放按钮点击事件</span></span><br><span class="line">            <span class="keyword">if</span> (videoView.isPlaying()) &#123;</span><br><span class="line">                <span class="comment">// 如果正在播放，恢复播放</span></span><br><span class="line">                videoView.resume();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 活动销毁时的回调</span></span><br><span class="line"><span class="comment">     * 释放资源，避免内存泄漏</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onDestroy();</span><br><span class="line">        <span class="keyword">if</span> (videoView != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 停止视频播放并释放资源</span></span><br><span class="line">            videoView.suspend();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="webview的用法"><a class="markdownIt-Anchor" href="#webview的用法"></a> WebView的用法</h2><p>可以用WebView嵌入一个游览器</p><p>1.在activity_main.xml导入WebView控件</p><p>2.在MainActivity获取实例,并用getSettings设置属性</p><p>3.用setWebViewClient使目标网页在WebView显示</p><p>4.加入权限并加开http的读取(也可以直接使用更稳定的https连接)</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/main&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">tools:context</span>=<span class="string">&quot;.MainActivity&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">WebView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/web_view&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line">        <span class="type">WebView</span> <span class="variable">webView</span> <span class="operator">=</span>(WebView) findViewById(R.id.web_view);</span><br><span class="line">        webView.getSettings().setJavaScriptEnabled(<span class="literal">true</span>); <span class="comment">// 让WebView支持JavaScript脚本</span></span><br><span class="line">        webView.setWebViewClient(<span class="keyword">new</span> <span class="title class_">WebViewClient</span>()); <span class="comment">// 使目标网页在WebView显示</span></span><br><span class="line">        webView.loadUrl(<span class="string">&quot;https://www.baidu.com/&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">manifest</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.INTERNET&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">application</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:usesCleartextTraffic</span>=<span class="string">&quot;true&quot;</span></span></span><br></pre></td></tr></table></figure><h2 id="运用http协议访问网络"><a class="markdownIt-Anchor" href="#运用http协议访问网络"></a> 运用HTTP协议访问网络</h2><h3 id="使用httpurlconnection"><a class="markdownIt-Anchor" href="#使用httpurlconnection"></a> 使用HttpURLConnection</h3><p>1.调用sendRequestWithHttpURLConnection()方法开启子线程,并使用HttpURLConnection发出HTTP请求</p><p>2.利用BufferedReader对服务器返回的流进行读取,并将结果传到showResponse方法中</p><p>3.在showResponse里通过runOnUiThread将线程切换到主线程,然后更新UI元素</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 主活动类，展示如何通过HttpURLConnection发送网络请求</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> <span class="keyword">implements</span> <span class="title class_">View</span>.OnClickListener &#123;</span><br><span class="line"></span><br><span class="line">    TextView responseText; <span class="comment">// 用于显示服务器响应的文本视图</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        <span class="comment">// 设置内容视图</span></span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 初始化按钮和文本视图</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">sendRequest</span> <span class="operator">=</span> findViewById(R.id.send_request);</span><br><span class="line">        responseText = findViewById(R.id.response_text);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置按钮的点击监听器</span></span><br><span class="line">        sendRequest.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">        <span class="comment">// 判断是否是发送请求按钮的点击事件</span></span><br><span class="line">        <span class="keyword">if</span> (v.getId() == R.id.send_request) &#123;</span><br><span class="line">            <span class="comment">// 调用发送请求的方法</span></span><br><span class="line">            sendRequestWithHttpURLConnection();    </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 使用HttpURLConnection发送网络请求</span></span><br><span class="line"><span class="comment">     * 在子线程中执行网络操作，避免阻塞主线程</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">sendRequestWithHttpURLConnection</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                <span class="type">HttpURLConnection</span> <span class="variable">connection</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">                <span class="type">BufferedReader</span> <span class="variable">reader</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">                <span class="type">InputStream</span> <span class="variable">in</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="comment">// 创建URL对象</span></span><br><span class="line">                    <span class="type">URL</span> <span class="variable">url</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">URL</span>(<span class="string">&quot;https://www.baidu.com&quot;</span>);</span><br><span class="line">                    <span class="comment">// 打开HTTP连接</span></span><br><span class="line">                    connection = (HttpURLConnection) url.openConnection();</span><br><span class="line">                    <span class="comment">// 设置请求方法为GET</span></span><br><span class="line">                    </span><br><span class="line">                    <span class="comment">/*</span></span><br><span class="line"><span class="comment">                    GET:从服务器获取数据</span></span><br><span class="line"><span class="comment">                    POST:提交数据给服务器</span></span><br><span class="line"><span class="comment">                    */</span></span><br><span class="line">                    </span><br><span class="line">                    connection.setRequestMethod(<span class="string">&quot;GET&quot;</span>);</span><br><span class="line">                    <span class="comment">// 设置连接超时时间（单位：毫秒）</span></span><br><span class="line">                    connection.setConnectTimeout(<span class="number">8000</span>);</span><br><span class="line">                    <span class="comment">// 设置读取超时时间（单位：毫秒）</span></span><br><span class="line">                    connection.setReadTimeout(<span class="number">8000</span>);</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 获取输入流，读取服务器响应内容</span></span><br><span class="line">                    in = connection.getInputStream();</span><br><span class="line">                    reader = <span class="keyword">new</span> <span class="title class_">BufferedReader</span>(<span class="keyword">new</span> <span class="title class_">InputStreamReader</span>(in));</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 读取响应内容</span></span><br><span class="line">                    <span class="type">StringBuilder</span> <span class="variable">response</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StringBuilder</span>();</span><br><span class="line">                    String line;</span><br><span class="line">                    <span class="keyword">while</span> ((line = reader.readLine()) != <span class="literal">null</span>) &#123;</span><br><span class="line">                        response.append(line);</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 更新UI显示响应内容</span></span><br><span class="line">                    showResponse(response.toString());</span><br><span class="line"></span><br><span class="line">                &#125; <span class="keyword">catch</span> (MalformedURLException e) &#123;</span><br><span class="line">                    <span class="comment">// URL格式错误异常</span></span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                    <span class="comment">// 网络IO异常</span></span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    <span class="comment">// 确保释放资源</span></span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        <span class="keyword">if</span> (reader != <span class="literal">null</span>) &#123;</span><br><span class="line">                            reader.close();</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="keyword">if</span> (in != <span class="literal">null</span>) &#123;</span><br><span class="line">                            in.close();</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">if</span> (connection != <span class="literal">null</span>) &#123;</span><br><span class="line">                        connection.disconnect();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 在主线程中更新UI，显示服务器响应内容</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> response 服务器响应的内容</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">showResponse</span><span class="params">(<span class="keyword">final</span> String response)</span> &#123;</span><br><span class="line">        runOnUiThread(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                <span class="comment">// 更新文本视图显示响应内容</span></span><br><span class="line">                responseText.setText(response);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="服务service"><a class="markdownIt-Anchor" href="#服务service"></a> 服务(Service)</h2><h3 id="android多线程编程"><a class="markdownIt-Anchor" href="#android多线程编程"></a> Android多线程编程</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>()&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="comment">// 处理具体的逻辑</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;).start();</span><br></pre></td></tr></table></figure><h3 id="在子线程中更新ui"><a class="markdownIt-Anchor" href="#在子线程中更新ui"></a> 在子线程中更新UI</h3><p>如果想要更新程序里的UI元素必须在主线程中进行(UI线程不安全)</p><p>1.在主线程进行操作</p><p>2.在子线程进行回调</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span><br><span class="line">&lt;RelativeLayout xmlns:android=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span><br><span class="line">    android:fitsSystemWindows=<span class="string">&quot;true&quot;</span></span><br><span class="line">    android:layout_width=<span class="string">&quot;match_parent&quot;</span></span><br><span class="line">    android:layout_height=<span class="string">&quot;match_parent&quot;</span>&gt;</span><br><span class="line"></span><br><span class="line">    &lt;Button</span><br><span class="line">        android:id=<span class="string">&quot;@+id/change_text&quot;</span></span><br><span class="line">        android:layout_width=<span class="string">&quot;match_parent&quot;</span></span><br><span class="line">        android:layout_height=<span class="string">&quot;wrap_content&quot;</span></span><br><span class="line">        android:text=<span class="string">&quot;Change Text&quot;</span> /&gt;</span><br><span class="line"></span><br><span class="line">    &lt;TextView</span><br><span class="line">        android:id=<span class="string">&quot;@+id/text&quot;</span></span><br><span class="line">        android:layout_width=<span class="string">&quot;wrap_content&quot;</span></span><br><span class="line">        android:layout_height=<span class="string">&quot;wrap_content&quot;</span></span><br><span class="line">        android:layout_centerInParent=<span class="string">&quot;true&quot;</span></span><br><span class="line">        android:text=<span class="string">&quot;Hello world&quot;</span></span><br><span class="line">        android:textSize=<span class="string">&quot;20sp&quot;</span>/&gt;</span><br><span class="line"></span><br><span class="line">&lt;/RelativeLayout&gt;</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> <span class="keyword">implements</span> <span class="title class_">View</span>.OnClickListener &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义一个常量，用于标识更新文本的消息类型</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">UPDATE_TEXT</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 声明一个TextView对象，用于显示文本</span></span><br><span class="line">    <span class="keyword">private</span> TextView text;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 声明一个Handler对象，用于处理线程之间的消息通信</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">Handler</span> <span class="variable">handler</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Handler</span>() &#123;</span><br><span class="line">        <span class="comment">// 重写处理消息的方法</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">handleMessage</span><span class="params">(Message msg)</span> &#123;</span><br><span class="line">            <span class="comment">// 判断消息类型，如果是UPDATE_TEXT类型，则更新文本</span></span><br><span class="line">            <span class="keyword">if</span> (msg.what == UPDATE_TEXT) &#123;</span><br><span class="line">                text.setText(<span class="string">&quot;Nice to meet you&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// activity创建时的回调方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="comment">// 调用父类的 onCreate 方法</span></span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        <span class="comment">// 加载布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line">        <span class="comment">// 通过ID找到TextView对象，并赋值给text变量</span></span><br><span class="line">        text = (TextView) findViewById(R.id.text);</span><br><span class="line">        <span class="comment">// 通过ID找到Button对象</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">changeText</span> <span class="operator">=</span> (Button) findViewById(R.id.change_text);</span><br><span class="line">        <span class="comment">// 为Button设置点击监听器</span></span><br><span class="line">        changeText.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// OnClickListener接口的回调方法，处理点击事件</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">        <span class="comment">// 判断点击的View是否是change_text按钮</span></span><br><span class="line">        <span class="keyword">if</span> (v.getId() == R.id.change_text) &#123;</span><br><span class="line">            <span class="comment">// 启动一个新的线程</span></span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">                <span class="comment">// 线程的run方法</span></span><br><span class="line">                <span class="meta">@Override</span></span><br><span class="line">                <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                    <span class="comment">// 创建一个消息对象</span></span><br><span class="line">                    <span class="type">Message</span> <span class="variable">message</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Message</span>();</span><br><span class="line">                    <span class="comment">// 设置消息类型为UPDATE_TEXT</span></span><br><span class="line">                    message.what = UPDATE_TEXT;</span><br><span class="line">                    <span class="comment">// 通过Handler发送消息到主线程</span></span><br><span class="line">                    handler.sendMessage(message);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).start();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="异步消息处理机制"><a class="markdownIt-Anchor" href="#异步消息处理机制"></a> 异步消息处理机制</h3><ol><li><h4 id="handler"><a class="markdownIt-Anchor" href="#handler"></a> <strong>Handler</strong></h4><ul><li><strong>定义</strong>：Handler是一个接口，用于在不同线程之间发送和处理消息。</li><li><strong>功能</strong>：通过Handler，可以将消息或Runnable对象发送到与之绑定的Looper所在的线程，处理消息或执行任务。</li><li><strong>使用场景</strong>：常用于更新UI，因为Android不允许在子线程直接修改UI，需要通过Handler将UI操作发送到主线程执行。</li></ul></li><li><h4 id="message"><a class="markdownIt-Anchor" href="#message"></a> <strong>Message</strong></h4><ul><li><strong>定义</strong>：Message是Handler处理的消息对象，用于封装需要传递的数据。</li><li><strong>功能</strong>：Message可以包含数据和标识信息，通过Handler发送到目标线程，供相应的处理逻辑使用。</li><li><strong>使用场景</strong>：当需要在不同线程之间传递数据时，使用Message来封装数据，并通过Handler发送。</li></ul></li><li><h4 id="runnable"><a class="markdownIt-Anchor" href="#runnable"></a> <strong>Runnable</strong></h4><ul><li><strong>定义</strong>：Runnable是一个接口，表示可以被执行的任务。</li><li><strong>功能</strong>：通过实现Runnable的run方法，可以定义需要在不同线程执行的任务。Handler可以通过post方法执行Runnable。</li><li><strong>使用场景</strong>：当需要在子线程执行某些任务，并通过Handler在主线程更新UI时，使用Runnable定义任务。</li></ul></li><li><h4 id="looper"><a class="markdownIt-Anchor" href="#looper"></a> <strong>Looper</strong></h4><ul><li><strong>定义</strong>：Looper是消息队列的管理者，负责不断地从消息队列中取出消息，并分发给相应的Handler处理。</li><li><strong>功能</strong>：每个线程如果需要处理消息，必须有一个Looper，它会运行一个无限循环，处理消息队列中的消息。</li><li><strong>使用场景</strong>：主线程默认有一个Looper，子线程如果需要使用Handler处理消息，必须手动创建Looper并启动消息循环。</li></ul></li></ol><h3 id="服务的基本用法"><a class="markdownIt-Anchor" href="#服务的基本用法"></a> 服务的基本用法</h3><p>1.启动服务(onCreate()在第一次执行,onStartCommand()每次启动都执行)</p><p>2.停止服务(在服务里的任何位置调用stopSelf()也可以停止服务)</p><p>3.绑定服务与解绑服务 :</p><p>在服务里创建一个新的Binder对象(新建一个内部类继承自Binder).</p><p>在活动中创建一个ServiceConnection的匿名类,并重写onServiceConnected(绑定调用)与onServiceDisconnected(解绑调用)方法</p><p>调用bindService和unbindService方法</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * MyService是一个Android服务类，用于演示服务的基本使用和生命周期管理。</span></span><br><span class="line"><span class="comment"> * 该服务提供了绑定接口，允许活动与服务进行交互。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyService</span> <span class="keyword">extends</span> <span class="title class_">Service</span> &#123;</span><br><span class="line">    <span class="comment">// 用于与活动交互的Binder实例</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">DownloadBinder</span> <span class="variable">mBinder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">DownloadBinder</span>();</span><br><span class="line">    <span class="comment">// 日志标签，用于输出日志信息</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">TAG</span> <span class="operator">=</span> <span class="string">&quot;Myservice&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * DownloadBinder是一个Binder内部类，提供了服务的接口方法。</span></span><br><span class="line"><span class="comment">     * 通过该Binder，活动可以调用服务的方法。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">DownloadBinder</span> <span class="keyword">extends</span> <span class="title class_">Binder</span> &#123;</span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 开始下载的方法。</span></span><br><span class="line"><span class="comment">         * 当前仅输出日志信息，具体下载逻辑需在此实现。</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">startDownload</span><span class="params">()</span> &#123;</span><br><span class="line">            Log.d(TAG, <span class="string">&quot;startDownload: executed&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 获取下载进度的方法。</span></span><br><span class="line"><span class="comment">         * 当前返回0，具体实现需根据实际下载进度来返回。</span></span><br><span class="line"><span class="comment">         *</span></span><br><span class="line"><span class="comment">         * <span class="doctag">@return</span> 下载进度，范围0-100</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getProgress</span><span class="params">()</span> &#123;</span><br><span class="line">            Log.d(TAG, <span class="string">&quot;getProgress: executed&quot;</span>);</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 服务构造方法。</span></span><br><span class="line"><span class="comment">     * Android服务的构造方法，不建议在此进行耗时操作。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MyService</span><span class="params">()</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * onBind方法，返回一个IBinder接口，供活动与服务交互。</span></span><br><span class="line"><span class="comment">     * 当活动调用bindService()时，系统会调用此方法。</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> intent 绑定服务的意图</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 提供给活动的IBinder接口</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> IBinder <span class="title function_">onBind</span><span class="params">(Intent intent)</span> &#123;</span><br><span class="line">        <span class="comment">// <span class="doctag">TODO:</span> Return the communication channel to the service.</span></span><br><span class="line">        <span class="keyword">return</span> mBinder;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * onCreate方法，服务被创建时调用。</span></span><br><span class="line"><span class="comment">     * 适用于一些一次性初始化的操作。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@SuppressLint(&quot;ForegroundServiceType&quot;)</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate();</span><br><span class="line">        Log.d(TAG, <span class="string">&quot;onCreate: onCreate executed&quot;</span>);</span><br><span class="line">        <span class="comment">// 创建一个intent，用于从通知中启动MainActivity</span></span><br><span class="line">        <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="built_in">this</span>, MainActivity.class);</span><br><span class="line">        <span class="comment">// 创建一个PendingIntent，作为通知的点击处理</span></span><br><span class="line">        <span class="type">PendingIntent</span> <span class="variable">pi</span> <span class="operator">=</span> PendingIntent.getActivity(<span class="built_in">this</span>, <span class="number">0</span>, intent, PendingIntent.FLAG_IMMUTABLE);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * onStartCommand方法，服务被启动时调用。</span></span><br><span class="line"><span class="comment">     * 适用于 IntentService 或者不需要绑定的服务。</span></span><br><span class="line"><span class="comment">     * 当服务通过startService()启动时，系统会调用此方法。</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> intent 启动服务的意图</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> flags  服务启动的标志</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> startId 服务的启动ID</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 服务的启动行为，常用返回值：</span></span><br><span class="line"><span class="comment">     *         START_STICKY：服务被杀后，会重新创建</span></span><br><span class="line"><span class="comment">     *         START_NOT_STICKY：服务被杀后，不会重新创建</span></span><br><span class="line"><span class="comment">     *         START_REDELIVER_INTENT：服务被杀后，会重新调佣onStartCommand()，并传递最后的intent</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">onStartCommand</span><span class="params">(Intent intent, <span class="type">int</span> flags, <span class="type">int</span> startId)</span> &#123;</span><br><span class="line">        <span class="comment">// 启动一个新线程，执行一些异步任务</span></span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                <span class="comment">// 在新线程中调用stopSelf()，停止服务</span></span><br><span class="line">                stopSelf();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>.onStartCommand(intent, flags, startId);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * onDestroy方法，服务被销毁时调用。</span></span><br><span class="line"><span class="comment">     * 可以在此进行资源的释放和清理操作。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onDestroy();</span><br><span class="line">        Log.d(TAG, <span class="string">&quot;onDestroy: onDestory executed&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> <span class="keyword">implements</span> <span class="title class_">View</span>.OnClickListener &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 声明一个DownloadBinder类型的变量，用于与服务通信</span></span><br><span class="line">    <span class="keyword">private</span> MyService.DownloadBinder downloadBinder;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义一个ServiceConnection的匿名类，处理服务连接和断开的情况</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">ServiceConnection</span> <span class="variable">connection</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ServiceConnection</span>() &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 当服务连接成功时回调此方法</span></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onServiceConnected</span><span class="params">(ComponentName name, IBinder service)</span> &#123;</span><br><span class="line">            <span class="comment">// 将IBinder对象转换为DownloadBinder类型</span></span><br><span class="line">            downloadBinder = (MyService.DownloadBinder) service;</span><br><span class="line">            <span class="comment">// 调用DownloadBinder的方法开始下载</span></span><br><span class="line">            downloadBinder.startDownload();</span><br><span class="line">            <span class="comment">// 调用DownloadBinder的方法获取下载进度</span></span><br><span class="line">            downloadBinder.getProgress();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 当服务断开连接时回调此方法</span></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onServiceDisconnected</span><span class="params">(ComponentName name)</span> &#123;</span><br><span class="line">            <span class="comment">// 此处可以添加断开连接后的处理逻辑</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// activity创建时的回调方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="comment">// 调用父类的 onCreate 方法</span></span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        <span class="comment">// 设置activity的布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 检查是否有POST_NOTIFICATIONS权限（适用于Android 13及以上版本）</span></span><br><span class="line">        <span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.TIRAMISU) &#123;</span><br><span class="line">            <span class="keyword">if</span> (checkSelfPermission(Manifest.permission.POST_NOTIFICATIONS) != PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                <span class="comment">// 如果没有权限，请求权限</span></span><br><span class="line">                requestPermissions(<span class="keyword">new</span> <span class="title class_">String</span>[]&#123;Manifest.permission.POST_NOTIFICATIONS&#125;, <span class="number">1</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 找到布局文件中的按钮，并为它们设置点击监听器</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">startService</span> <span class="operator">=</span> (Button) findViewById(R.id.start_service);</span><br><span class="line">        <span class="type">Button</span> <span class="variable">stopService</span> <span class="operator">=</span> (Button) findViewById(R.id.stop_service);</span><br><span class="line">        <span class="type">Button</span> <span class="variable">startIntentService</span> <span class="operator">=</span> (Button) findViewById(R.id.start_intent_service);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 为按钮设置点击事件监听器</span></span><br><span class="line">        startService.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">        stopService.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">        startIntentService.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// OnClickListener接口的回调方法，处理按钮点击事件</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">        <span class="comment">// 根据点击的按钮ID执行不同的操作</span></span><br><span class="line">        <span class="keyword">if</span> (v.getId() == R.id.start_service) &#123;</span><br><span class="line">            <span class="comment">// 创建Intent，用于启动MyService服务</span></span><br><span class="line">            <span class="type">Intent</span> <span class="variable">startIntent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="built_in">this</span>, MyService.class);</span><br><span class="line">            startService(startIntent); <span class="comment">// 启动服务</span></span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (v.getId() == R.id.stop_service) &#123;</span><br><span class="line">            <span class="comment">// 创建Intent，用于停止MyService服务</span></span><br><span class="line">            <span class="type">Intent</span> <span class="variable">stopIntent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="built_in">this</span>, MyService.class);</span><br><span class="line">            stopService(stopIntent); <span class="comment">// 停止服务</span></span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (v.getId() == R.id.bind_service) &#123;</span><br><span class="line">            <span class="comment">// 创建Intent，用于绑定到MyService服务</span></span><br><span class="line">            <span class="type">Intent</span> <span class="variable">bindIntent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="built_in">this</span>, MyService.class);</span><br><span class="line">            <span class="comment">// 绑定服务，并传入ServiceConnection实例</span></span><br><span class="line">            bindService(bindIntent, connection, BIND_AUTO_CREATE);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (v.getId() == R.id.unbind_service) &#123;</span><br><span class="line">            <span class="comment">// 解绑服务</span></span><br><span class="line">            unbindService(connection);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (v.getId() == R.id.start_intent_service) &#123;</span><br><span class="line">            <span class="comment">// 在日志中打印当前线程的ID</span></span><br><span class="line">            Log.d(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;Thread id is &quot;</span> + Thread.currentThread().getId());</span><br><span class="line">            <span class="comment">// 创建Intent，用于启动MyIntentService服务</span></span><br><span class="line">            <span class="type">Intent</span> <span class="variable">intentService</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="built_in">this</span>, MyIntentService.class);</span><br><span class="line">            startService(intentService); <span class="comment">// 启动服务</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="intentservice简单创建异步的会自动停止的服务"><a class="markdownIt-Anchor" href="#intentservice简单创建异步的会自动停止的服务"></a> IntentService(简单创建异步的,会自动停止的服务)</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyIntentService</span> <span class="keyword">extends</span> <span class="title class_">IntentService</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">TAG</span> <span class="operator">=</span> <span class="string">&quot;MyIntentService&quot;</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MyIntentService</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(<span class="string">&quot;MyIntentService&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onHandleIntent</span><span class="params">(<span class="meta">@Nullable</span> Intent intent)</span> &#123;</span><br><span class="line">        Log.d(TAG, <span class="string">&quot;Thread id is &quot;</span>+Thread.currentThread().getId());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onDestroy();</span><br><span class="line">        Log.d(TAG, <span class="string">&quot;onDestroy executed&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (v.getId() == R.id.start_intent_service) &#123;</span><br><span class="line">            <span class="comment">// 在日志中打印当前线程的ID</span></span><br><span class="line">            Log.d(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;Thread id is &quot;</span> + Thread.currentThread().getId());</span><br><span class="line">            <span class="comment">// 创建Intent，用于启动MyIntentService服务</span></span><br><span class="line">            <span class="type">Intent</span> <span class="variable">intentService</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="built_in">this</span>, MyIntentService.class);</span><br><span class="line">            startService(intentService); <span class="comment">// 启动服务</span></span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">service</span> <span class="attr">android:name</span>=<span class="string">&quot;.MyIntentService&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><h2 id="服务的最佳实践完整版的下载实例"><a class="markdownIt-Anchor" href="#服务的最佳实践完整版的下载实例"></a> 服务的最佳实践—完整版的下载实例</h2><p>1.添加一个OKHttp的依赖</p><p>2.定义一个DownloadListener回调接口</p><p>3.新建一个DownloadTask继承自AsyncTask</p><p>doInBackground()用于后台执行具体的下载逻辑</p><p>onProgressUpdate()用于在界面上更新当前的下载进度</p><p>onPostExecute()用于通知最终的下载结果</p><p>4.创建DownloadService服务(记得创建通道,用DownloadBinder实现服务与活动之间的通信)</p><p>5.修改activity_main.xml</p><p>6.修改MainActivity(活动销毁了,一定要记得对服务进行解绑)</p><p>7.注册权限</p><figure class="highlight kts"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">implementation(<span class="string">&quot;com.squareup.okhttp3:okhttp:4.9.0&quot;</span>)</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">DownloadListener</span> &#123;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">onProgress</span><span class="params">(<span class="type">int</span> progress)</span>;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">onSuccess</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">onFailed</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">onPaused</span><span class="params">()</span>;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">onCanceled</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br><span class="line">160</span><br><span class="line">161</span><br><span class="line">162</span><br><span class="line">163</span><br><span class="line">164</span><br><span class="line">165</span><br><span class="line">166</span><br><span class="line">167</span><br><span class="line">168</span><br><span class="line">169</span><br><span class="line">170</span><br><span class="line">171</span><br><span class="line">172</span><br><span class="line">173</span><br><span class="line">174</span><br><span class="line">175</span><br><span class="line">176</span><br><span class="line">177</span><br><span class="line">178</span><br><span class="line">179</span><br><span class="line">180</span><br><span class="line">181</span><br><span class="line">182</span><br><span class="line">183</span><br><span class="line">184</span><br><span class="line">185</span><br><span class="line">186</span><br><span class="line">187</span><br><span class="line">188</span><br><span class="line">189</span><br><span class="line">190</span><br><span class="line">191</span><br><span class="line">192</span><br><span class="line">193</span><br><span class="line">194</span><br><span class="line">195</span><br><span class="line">196</span><br><span class="line">197</span><br><span class="line">198</span><br><span class="line">199</span><br><span class="line">200</span><br><span class="line">201</span><br><span class="line">202</span><br><span class="line">203</span><br><span class="line">204</span><br><span class="line">205</span><br><span class="line">206</span><br><span class="line">207</span><br><span class="line">208</span><br><span class="line">209</span><br><span class="line">210</span><br><span class="line">211</span><br><span class="line">212</span><br><span class="line">213</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DownloadTask</span> <span class="keyword">extends</span> <span class="title class_">AsyncTask</span>&lt;String, Integer, Integer&gt; &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 下载状态：成功</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">TYPE_SUCCESS</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 下载状态：失败</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">TYPE_FAILED</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 下载状态：暂停</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">TYPE_PAUSED</span> <span class="operator">=</span> <span class="number">2</span>;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 下载状态：取消</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">TYPE_CANCELED</span> <span class="operator">=</span> <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 下载事件监听器，用于接收下载状态和进度更新</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> DownloadListener listener;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 标记下载是否被取消</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">boolean</span> <span class="variable">isCanceled</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 标记下载是否被暂停</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">boolean</span> <span class="variable">isPaused</span> <span class="operator">=</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 记录最后一次上传的进度值，避免重复更新</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> lastProgress;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 构造函数，初始化下载监听器</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> listener 下载事件的回调接口</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">DownloadTask</span><span class="params">(DownloadListener listener)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.listener = listener;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 后台执行下载任务的主方法</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> params 参数数组，第一个元素为下载 URL</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 下载状态，取值为 TYPE_SUCCESS、TYPE_FAILED、TYPE_PAUSED、TYPE_CANCELED</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> Integer <span class="title function_">doInBackground</span><span class="params">(String... params)</span> &#123;</span><br><span class="line">        <span class="type">InputStream</span> <span class="variable">is</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="type">RandomAccessFile</span> <span class="variable">savedFile</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="type">File</span> <span class="variable">file</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="type">long</span> <span class="variable">downloadedLength</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 获取下载 URL 和文件名</span></span><br><span class="line">            <span class="type">String</span> <span class="variable">downloadUrl</span> <span class="operator">=</span> params[<span class="number">0</span>];</span><br><span class="line">            <span class="type">String</span> <span class="variable">fileName</span> <span class="operator">=</span> downloadUrl.substring(downloadUrl.lastIndexOf(<span class="string">&quot;/&quot;</span>));</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 设置下载目录为公共下载目录</span></span><br><span class="line">            <span class="type">String</span> <span class="variable">directory</span> <span class="operator">=</span> Environment.getExternalStoragePublicDirectory(Environment.DIRECTORY_DOWNLOADS).getPath();</span><br><span class="line">            file = <span class="keyword">new</span> <span class="title class_">File</span>(directory + fileName);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 如果文件已存在，记录已下载的长度</span></span><br><span class="line">            <span class="keyword">if</span> (file.exists()) &#123;</span><br><span class="line">                downloadedLength = file.length();</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 获取文件总长度</span></span><br><span class="line">            <span class="type">long</span> <span class="variable">contentLength</span> <span class="operator">=</span> getContentLength(downloadUrl);</span><br><span class="line">            <span class="keyword">if</span> (contentLength == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">return</span> TYPE_FAILED;</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (contentLength == downloadedLength) &#123;</span><br><span class="line">                <span class="keyword">return</span> TYPE_SUCCESS;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 发送带有 Range 头的请求，继续断点下载</span></span><br><span class="line">            <span class="type">OkHttpClient</span> <span class="variable">client</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">OkHttpClient</span>();</span><br><span class="line">            <span class="type">Request</span> <span class="variable">request</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Request</span>.Builder()</span><br><span class="line">                    .addHeader(<span class="string">&quot;RANGE&quot;</span>, <span class="string">&quot;bytes=&quot;</span> + downloadedLength + <span class="string">&quot;-&quot;</span>)</span><br><span class="line">                    .url(downloadUrl)</span><br><span class="line">                    .build();</span><br><span class="line">            <span class="type">Response</span> <span class="variable">response</span> <span class="operator">=</span> client.newCall(request).execute();</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (response != <span class="literal">null</span>) &#123;</span><br><span class="line">                is = response.body().byteStream();</span><br><span class="line">                savedFile = <span class="keyword">new</span> <span class="title class_">RandomAccessFile</span>(file, <span class="string">&quot;rw&quot;</span>);</span><br><span class="line">                savedFile.seek(downloadedLength);</span><br><span class="line"></span><br><span class="line">                <span class="type">byte</span>[] b = <span class="keyword">new</span> <span class="title class_">byte</span>[<span class="number">1024</span>];</span><br><span class="line">                <span class="type">int</span> <span class="variable">total</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">                <span class="type">int</span> len;</span><br><span class="line">                <span class="keyword">while</span> ((len = is.read(b)) != -<span class="number">1</span>) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (isCanceled) &#123;</span><br><span class="line">                        <span class="keyword">return</span> TYPE_CANCELED;</span><br><span class="line">                    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (isPaused) &#123;</span><br><span class="line">                        <span class="keyword">return</span> TYPE_PAUSED;</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        total += len;</span><br><span class="line">                        savedFile.write(b, <span class="number">0</span>, len);</span><br><span class="line"></span><br><span class="line">                        <span class="comment">// 计算并发布进度</span></span><br><span class="line">                        <span class="type">int</span> <span class="variable">progress</span> <span class="operator">=</span> (<span class="type">int</span>) ((total + downloadedLength) * <span class="number">100</span> / contentLength);</span><br><span class="line">                        publishProgress(progress);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                response.body().close();</span><br><span class="line">                <span class="keyword">return</span> TYPE_SUCCESS;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 释放资源</span></span><br><span class="line">                <span class="keyword">if</span> (is != <span class="literal">null</span>) &#123;</span><br><span class="line">                    is.close();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (savedFile != <span class="literal">null</span>) &#123;</span><br><span class="line">                    savedFile.close();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 如果下载被取消，删除已下载的文件</span></span><br><span class="line">                <span class="keyword">if</span> (isCanceled &amp;&amp; file != <span class="literal">null</span>) &#123;</span><br><span class="line">                    file.delete();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> TYPE_FAILED;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 在主线程更新下载进度</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> values 进度数组，第一个元素为当前进度</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onProgressUpdate</span><span class="params">(Integer... values)</span> &#123;</span><br><span class="line">        <span class="type">int</span> <span class="variable">progress</span> <span class="operator">=</span> values[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">if</span> (progress &gt; lastProgress) &#123;</span><br><span class="line">            listener.onProgress(progress);</span><br><span class="line">            lastProgress = progress;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 在主线程执行下载完成后的处理</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> status 下载状态，取值为 TYPE_SUCCESS、TYPE_FAILED、TYPE_PAUSED、TYPE_CANCELED</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onPostExecute</span><span class="params">(Integer status)</span> &#123;</span><br><span class="line">        <span class="keyword">switch</span> (status) &#123;</span><br><span class="line">            <span class="keyword">case</span> TYPE_SUCCESS:</span><br><span class="line">                listener.onSuccess();</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> TYPE_FAILED:</span><br><span class="line">                listener.onFailed();</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> TYPE_PAUSED:</span><br><span class="line">                listener.onPaused();</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> TYPE_CANCELED:</span><br><span class="line">                listener.onCanceled();</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">default</span>:</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 暂停下载</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">pauseDownload</span><span class="params">()</span> &#123;</span><br><span class="line">        isPaused = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 取消下载</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">cancelDownload</span><span class="params">()</span> &#123;</span><br><span class="line">        isCanceled = <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 获取下载文件的总长度</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> downloadUrl 下载 URL</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 文件总长度</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> IOException 网络请求异常</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">long</span> <span class="title function_">getContentLength</span><span class="params">(String downloadUrl)</span> <span class="keyword">throws</span> IOException &#123;</span><br><span class="line">        <span class="type">OkHttpClient</span> <span class="variable">client</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">OkHttpClient</span>();</span><br><span class="line">        <span class="type">Request</span> <span class="variable">request</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Request</span>.Builder()</span><br><span class="line">                .url(downloadUrl)</span><br><span class="line">                .build();</span><br><span class="line">        <span class="type">Response</span> <span class="variable">response</span> <span class="operator">=</span> client.newCall(request).execute();</span><br><span class="line">        <span class="keyword">if</span> (response != <span class="literal">null</span> &amp;&amp; response.isSuccessful()) &#123;</span><br><span class="line">            <span class="type">long</span> <span class="variable">contentLength</span> <span class="operator">=</span> response.body().contentLength();</span><br><span class="line">            response.body().close();</span><br><span class="line">            <span class="keyword">return</span> contentLength;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DownloadService</span> <span class="keyword">extends</span> <span class="title class_">Service</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> DownloadTask downloadTask; <span class="comment">// 声明下载任务对象，用于执行下载操作</span></span><br><span class="line">    <span class="keyword">private</span> String downloadUrl; <span class="comment">// 用于存储下载的URL</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建一个下载监听器，监听下载过程中的不同状态（进度、成功、失败、暂停、取消）</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">DownloadListener</span> <span class="variable">listener</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">DownloadListener</span>() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onProgress</span><span class="params">(<span class="type">int</span> progress)</span> &#123;</span><br><span class="line">            <span class="comment">// 在下载过程中更新通知，显示当前的下载进度</span></span><br><span class="line">            getNotificationManager().notify(<span class="number">1</span>, getNotification(<span class="string">&quot;Downloading...&quot;</span>, progress));</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onSuccess</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="comment">// 下载成功，停止前台服务并更新通知显示“下载成功”</span></span><br><span class="line">            downloadTask = <span class="literal">null</span>; <span class="comment">// 清空下载任务</span></span><br><span class="line">            stopForeground(<span class="literal">true</span>); <span class="comment">// 停止前台服务</span></span><br><span class="line">            getNotificationManager().notify(<span class="number">1</span>, getNotification(<span class="string">&quot;Download Success&quot;</span>, -<span class="number">1</span>)); <span class="comment">// 更新通知，显示“下载成功”</span></span><br><span class="line">            Toast.makeText(DownloadService.<span class="built_in">this</span>, <span class="string">&quot;Download Success&quot;</span>, Toast.LENGTH_SHORT).show(); <span class="comment">// 弹出Toast提示</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onFailed</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="comment">// 下载失败，停止前台服务并更新通知显示“下载失败”</span></span><br><span class="line">            downloadTask = <span class="literal">null</span>; <span class="comment">// 清空下载任务</span></span><br><span class="line">            stopForeground(<span class="literal">true</span>); <span class="comment">// 停止前台服务</span></span><br><span class="line">            getNotificationManager().notify(<span class="number">1</span>, getNotification(<span class="string">&quot;Download Failed&quot;</span>, -<span class="number">1</span>)); <span class="comment">// 更新通知，显示“下载失败”</span></span><br><span class="line">            Toast.makeText(DownloadService.<span class="built_in">this</span>, <span class="string">&quot;Download Failed&quot;</span>, Toast.LENGTH_SHORT).show(); <span class="comment">// 弹出Toast提示</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onPaused</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="comment">// 下载暂停，清空下载任务并显示暂停的Toast提示</span></span><br><span class="line">            downloadTask = <span class="literal">null</span>;</span><br><span class="line">            Toast.makeText(DownloadService.<span class="built_in">this</span>, <span class="string">&quot;Paused&quot;</span>, Toast.LENGTH_SHORT).show(); <span class="comment">// 弹出Toast提示</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onCanceled</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="comment">// 下载被取消，停止前台服务并清空下载任务</span></span><br><span class="line">            downloadTask = <span class="literal">null</span>; <span class="comment">// 清空下载任务</span></span><br><span class="line">            stopForeground(<span class="literal">true</span>); <span class="comment">// 停止前台服务</span></span><br><span class="line">            Toast.makeText(DownloadService.<span class="built_in">this</span>, <span class="string">&quot;Canceled&quot;</span>, Toast.LENGTH_SHORT).show(); <span class="comment">// 弹出Toast提示</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 自定义Binder类，用于Activity与Service之间的通信</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">DownloadBinder</span> <span class="variable">mBinder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">DownloadBinder</span>();</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> IBinder <span class="title function_">onBind</span><span class="params">(Intent intent)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> mBinder; <span class="comment">// 返回DownloadBinder对象，允许Activity与Service之间进行通信</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 自定义Binder类，提供下载控制方法</span></span><br><span class="line">    <span class="keyword">class</span> <span class="title class_">DownloadBinder</span> <span class="keyword">extends</span> <span class="title class_">Binder</span> &#123;</span><br><span class="line">        <span class="meta">@SuppressLint(&quot;ForegroundServiceType&quot;)</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">startDownload</span><span class="params">(String url)</span> &#123;</span><br><span class="line">            <span class="comment">// 开始下载</span></span><br><span class="line">            <span class="keyword">if</span> (downloadTask == <span class="literal">null</span>) &#123; <span class="comment">// 如果当前没有正在进行的下载任务</span></span><br><span class="line">                downloadUrl = url; <span class="comment">// 保存下载URL</span></span><br><span class="line">                downloadTask = <span class="keyword">new</span> <span class="title class_">DownloadTask</span>(listener); <span class="comment">// 创建新的下载任务，并传入监听器</span></span><br><span class="line">                downloadTask.execute(downloadUrl); <span class="comment">// 执行下载任务</span></span><br><span class="line">                startForeground(<span class="number">1</span>, getNotification(<span class="string">&quot;Downloading...&quot;</span>, <span class="number">0</span>)); <span class="comment">// 开始前台服务，显示通知</span></span><br><span class="line">                Toast.makeText(DownloadService.<span class="built_in">this</span>, <span class="string">&quot;Downloading...&quot;</span>, Toast.LENGTH_SHORT).show(); <span class="comment">// 弹出Toast提示</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">pauseDownload</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="comment">// 暂停下载</span></span><br><span class="line">            <span class="keyword">if</span> (downloadTask != <span class="literal">null</span>) &#123;</span><br><span class="line">                downloadTask.pauseDownload(); <span class="comment">// 调用下载任务的暂停方法</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">cancelDownload</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="comment">// 取消下载</span></span><br><span class="line">            <span class="keyword">if</span> (downloadTask != <span class="literal">null</span>) &#123;</span><br><span class="line">                downloadTask.cancelDownload(); <span class="comment">// 调用下载任务的取消方法</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (downloadUrl != <span class="literal">null</span>) &#123;</span><br><span class="line">                <span class="comment">// 删除已下载的文件</span></span><br><span class="line">                <span class="type">String</span> <span class="variable">fileName</span> <span class="operator">=</span> downloadUrl.substring(downloadUrl.lastIndexOf(<span class="string">&quot;/&quot;</span>)); <span class="comment">// 获取文件名</span></span><br><span class="line">                <span class="type">String</span> <span class="variable">directory</span> <span class="operator">=</span> Environment.getExternalStoragePublicDirectory(Environment.DIRECTORY_DOWNLOADS).getPath(); <span class="comment">// 获取下载目录路径</span></span><br><span class="line">                <span class="type">File</span> <span class="variable">file</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">File</span>(directory + fileName); <span class="comment">// 创建文件对象</span></span><br><span class="line">                <span class="keyword">if</span> (file.exists()) &#123;</span><br><span class="line">                    file.delete(); <span class="comment">// 如果文件存在，删除文件</span></span><br><span class="line">                &#125;</span><br><span class="line">                getNotificationManager().cancel(<span class="number">1</span>); <span class="comment">// 取消当前通知</span></span><br><span class="line">                stopForeground(<span class="literal">true</span>); <span class="comment">// 停止前台服务</span></span><br><span class="line">                Toast.makeText(DownloadService.<span class="built_in">this</span>, <span class="string">&quot;Canceled&quot;</span>, Toast.LENGTH_SHORT).show(); <span class="comment">// 弹出Toast提示</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取NotificationManager对象</span></span><br><span class="line">    <span class="keyword">private</span> NotificationManager <span class="title function_">getNotificationManager</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> (NotificationManager) getSystemService(NOTIFICATION_SERVICE); <span class="comment">// 返回系统的通知管理器</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建通知</span></span><br><span class="line">    <span class="keyword">private</span> Notification <span class="title function_">getNotification</span><span class="params">(String title, <span class="type">int</span> progress)</span> &#123;</span><br><span class="line">        <span class="comment">// 创建Intent，当点击通知时打开MainActivity</span></span><br><span class="line">        <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="built_in">this</span>, MainActivity.class);</span><br><span class="line">        <span class="type">PendingIntent</span> <span class="variable">pi</span> <span class="operator">=</span> PendingIntent.getActivity(<span class="built_in">this</span>, <span class="number">0</span>, intent, PendingIntent.FLAG_UPDATE_CURRENT);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果系统版本是 Android 8.0 或更高，创建通知通道</span></span><br><span class="line">        <span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.O) &#123;</span><br><span class="line">            <span class="type">NotificationChannel</span> <span class="variable">channel</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NotificationChannel</span>(</span><br><span class="line">                    <span class="string">&quot;default_channel_id&quot;</span>,  <span class="comment">// 通道 ID</span></span><br><span class="line">                    <span class="string">&quot;Default Channel&quot;</span>,     <span class="comment">// 通道名称</span></span><br><span class="line">                    NotificationManager.IMPORTANCE_DEFAULT</span><br><span class="line">            );</span><br><span class="line">            <span class="type">NotificationManager</span> <span class="variable">manager</span> <span class="operator">=</span> (NotificationManager) getSystemService(Context.NOTIFICATION_SERVICE);</span><br><span class="line">            manager.createNotificationChannel(channel);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 构建通知</span></span><br><span class="line">        NotificationCompat.<span class="type">Builder</span> <span class="variable">builder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">NotificationCompat</span>.Builder(<span class="built_in">this</span>, <span class="string">&quot;default_channel_id&quot;</span>); <span class="comment">// 使用通知通道</span></span><br><span class="line">        builder.setSmallIcon(R.mipmap.ic_launcher); <span class="comment">// 设置小图标</span></span><br><span class="line">        builder.setLargeIcon(BitmapFactory.decodeResource(getResources(), R.mipmap.ic_launcher)); <span class="comment">// 设置大图标</span></span><br><span class="line">        builder.setContentIntent(pi); <span class="comment">// 设置点击通知时的行为，打开MainActivity</span></span><br><span class="line">        builder.setContentTitle(title); <span class="comment">// 设置通知标题</span></span><br><span class="line">        <span class="keyword">if</span> (progress &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">            builder.setContentText(progress + <span class="string">&quot;%&quot;</span>); <span class="comment">// 设置通知文本，显示下载进度</span></span><br><span class="line">            builder.setProgress(<span class="number">100</span>, progress, <span class="literal">false</span>); <span class="comment">// 设置进度条，最大值100，当前进度为progress</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> builder.build(); <span class="comment">// 返回构建好的通知</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 主活动类，继承自AppCompatActivity，并实现了View.OnClickListener接口</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> <span class="keyword">implements</span> <span class="title class_">View</span>.OnClickListener &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 日志标签，用于输出日志信息</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">TAG</span> <span class="operator">=</span> <span class="string">&quot;MainActivity&quot;</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 权限请求代码</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">PERMISSION_REQUEST_CODE</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 下载服务的绑定器，用于与下载服务进行交互</span></span><br><span class="line">    <span class="keyword">private</span> DownloadService.DownloadBinder downloadBinder;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 服务连接对象，用于绑定服务的回调</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">ServiceConnection</span> <span class="variable">connection</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ServiceConnection</span>() &#123;</span><br><span class="line">        <span class="comment">// 当服务连接成功时回调</span></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onServiceConnected</span><span class="params">(ComponentName name, IBinder service)</span> &#123;</span><br><span class="line">            <span class="comment">// 将服务绑定器强制转换为DownloadBinder类型</span></span><br><span class="line">            downloadBinder = (DownloadService.DownloadBinder) service;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 当服务断开连接时回调</span></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onServiceDisconnected</span><span class="params">(ComponentName name)</span> &#123;</span><br><span class="line">            <span class="comment">// 此处可添加断开连接后的处理逻辑</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 活动创建时的回调方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        <span class="comment">// 设置活动的布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取开始下载按钮，并设置点击监听器</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">startDownload</span> <span class="operator">=</span> (Button) findViewById(R.id.start_download);</span><br><span class="line">        <span class="comment">// 获取暂停下载按钮，并设置点击监听器</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">pauseDownload</span> <span class="operator">=</span> (Button) findViewById(R.id.pause_download);</span><br><span class="line">        <span class="comment">// 获取取消下载按钮，并设置点击监听器</span></span><br><span class="line">        <span class="type">Button</span> <span class="variable">cancelDownload</span> <span class="operator">=</span> (Button) findViewById(R.id.cancel_download);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 为按钮设置点击事件监听器</span></span><br><span class="line">        startDownload.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">        pauseDownload.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line">        cancelDownload.setOnClickListener(<span class="built_in">this</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建启动下载服务的意图</span></span><br><span class="line">        <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="built_in">this</span>, DownloadService.class);</span><br><span class="line">        <span class="comment">// 启动下载服务</span></span><br><span class="line">        startService(intent);</span><br><span class="line">        <span class="comment">// 绑定服务，确保活动与服务能够交互</span></span><br><span class="line">        bindService(intent, connection, BIND_AUTO_CREATE);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 检查应用是否具有写入外部存储的权限</span></span><br><span class="line">        <span class="keyword">if</span> (ContextCompat.checkSelfPermission(MainActivity.<span class="built_in">this</span>, Manifest.permission.WRITE_EXTERNAL_STORAGE) != PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">            <span class="comment">// 如果没有权限，请求权限</span></span><br><span class="line">            ActivityCompat.requestPermissions(MainActivity.<span class="built_in">this</span>, <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;Manifest.permission.WRITE_EXTERNAL_STORAGE&#125;, <span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 点击事件处理方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">        <span class="comment">// 如果downloadBinder为空，说明服务尚未绑定成功，直接返回</span></span><br><span class="line">        <span class="keyword">if</span> (downloadBinder == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 根据点击的按钮执行相应的操作</span></span><br><span class="line">        <span class="keyword">if</span> (v.getId() == R.id.start_download) &#123;</span><br><span class="line">            <span class="comment">// 开始下载按钮被点击</span></span><br><span class="line">            <span class="type">String</span> <span class="variable">url</span> <span class="operator">=</span> <span class="string">&quot;https://raw.githubusercontent.com/guolindev/eclipse/master/eclipse-inst-win64.exe&quot;</span>;</span><br><span class="line">            <span class="comment">// 调用下载服务开始下载</span></span><br><span class="line">            downloadBinder.startDownload(url);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (v.getId() == R.id.pause_download) &#123;</span><br><span class="line">            <span class="comment">// 暂停下载按钮被点击</span></span><br><span class="line">            downloadBinder.pauseDownload();</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (v.getId() == R.id.cancel_download) &#123;</span><br><span class="line">            <span class="comment">// 取消下载按钮被点击</span></span><br><span class="line">            downloadBinder.cancelDownload();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 权限请求结果的回调方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onRequestPermissionsResult</span><span class="params">(<span class="type">int</span> requestCode, <span class="meta">@NonNull</span> String[] permissions, <span class="meta">@NonNull</span> <span class="type">int</span>[] grantResults, <span class="type">int</span> deviceId)</span> &#123;</span><br><span class="line">        <span class="comment">// 根据请求码处理权限请求结果</span></span><br><span class="line">        <span class="keyword">switch</span> (requestCode) &#123;</span><br><span class="line">            <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">                <span class="comment">// 如果用户拒绝了权限申请</span></span><br><span class="line">                <span class="keyword">if</span> (grantResults.length &gt; <span class="number">0</span> &amp;&amp; grantResults[<span class="number">0</span>] != PackageManager.PERMISSION_GRANTED) &#123;</span><br><span class="line">                    <span class="comment">// 提示用户权限被拒绝</span></span><br><span class="line">                    Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;拒绝权限将无法使用程序&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">                    <span class="comment">// 退出活动</span></span><br><span class="line">                    finish();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">default</span>:</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 活动销毁时的回调方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onDestroy();</span><br><span class="line">        <span class="comment">// 解除服务绑定，释放资源</span></span><br><span class="line">        unbindService(connection);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">&lt;uses-permission android:name=<span class="string">&quot;android.permission.POST_NOTIFICATIONS&quot;</span> /&gt;</span><br><span class="line">    &lt;uses-permission android:name=<span class="string">&quot;android.permission.INTERNET&quot;</span>/&gt;</span><br><span class="line">    &lt;uses-permission android:name=<span class="string">&quot;android.permission.WRITE_EXTERNAL_STORAGE&quot;</span>/&gt;</span><br><span class="line">    &lt;uses-permission android:name=<span class="string">&quot;android.permission.FOREGROUND_SERVICE&quot;</span> /&gt;</span><br></pre></td></tr></table></figure><hr /><h2 id="material-design设计原则"><a class="markdownIt-Anchor" href="#material-design设计原则"></a> Material Design设计原则</h2><h3 id="toolbar标题栏"><a class="markdownIt-Anchor" href="#toolbar标题栏"></a> Toolbar(标题栏)</h3><p>1.设置主题为Theme.AppCompat.Light.NoActionBar(淡色主题)</p><p>2.在activity_main.xml用Toolbar替代ActionBar</p><p>3.在menu下创建一个toolbar.xml文件,并设计相应item</p><p>4.在Mainactivity获取toolbar实例并设置点击事件</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">resources</span> <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- Base application theme. --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">style</span> <span class="attr">name</span>=<span class="string">&quot;Base.Theme.MaterialTest&quot;</span> <span class="attr">parent</span>=<span class="string">&quot;Theme.AppCompat.Light.NoActionBar&quot;</span>&gt;</span><span class="language-xml"></span></span><br><span class="line"><span class="language-xml">        <span class="comment">&lt;!-- Customize your light theme here. --&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;colorPrimary&quot;</span>&gt;</span>#F2B3C8<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;colorAccent&quot;</span>&gt;</span>#F2B4C9<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;colorPrimaryVariant&quot;</span>&gt;</span>#F492B4<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;colorOnPrimary&quot;</span>&gt;</span>#C77E99<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="comment">&lt;!-- Secondary brand color. --&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;colorSecondary&quot;</span>&gt;</span>#EFA2BC<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;colorSecondaryVariant&quot;</span>&gt;</span>#E7155B<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;colorOnSecondary&quot;</span>&gt;</span>#CCCE7F7F<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="comment">&lt;!-- Status bar color. --&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;android:statusBarColor&quot;</span> <span class="attr">tools:targetApi</span>=<span class="string">&quot;l&quot;</span>&gt;</span>#F6D3D3<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    </span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">style</span> <span class="attr">name</span>=<span class="string">&quot;Theme.MaterialTest&quot;</span> <span class="attr">parent</span>=<span class="string">&quot;Base.Theme.MaterialTest&quot;</span> /&gt;</span><span class="language-xml"></span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;/<span class="name">resources</span>&gt;</span></span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">androidx.appcompat.widget.Toolbar</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/toolbar&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_height</span>=<span class="string">&quot;?attr/actionBarSize&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:background</span>=<span class="string">&quot;?attr/colorPrimary&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:theme</span>=<span class="string">&quot;@style/ThemeOverlay.AppCompat.Dark.ActionBar&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">app:popupTheme</span>=<span class="string">&quot;@style/ThemeOverlay.AppCompat.Light&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">app:layout_scrollFlags</span>=<span class="string">&quot;scroll|enterAlways|snap&quot;</span>/&gt;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"> <span class="type">Toolbar</span> <span class="variable">toolbar</span> <span class="operator">=</span> (Toolbar) findViewById(R.id.toolbar);</span><br><span class="line">        setSupportActionBar(toolbar);</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">onOptionsItemSelected</span><span class="params">(<span class="meta">@NonNull</span> MenuItem item)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (item.getItemId()==R.id.backup)&#123;</span><br><span class="line">            Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;You clicked Backup&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (item.getItemId()==R.id.delete) &#123;</span><br><span class="line">            Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;You clicked Delete&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (item.getItemId()==R.id.settings) &#123;</span><br><span class="line">            Toast.makeText(<span class="built_in">this</span>, <span class="string">&quot;You clicked Settings&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (item.getItemId()==android.R.id.home) &#123;</span><br><span class="line">            mDrawerLayout.openDrawer(GravityCompat.START);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line">&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot;?&gt;</span><br><span class="line">&lt;menu xmlns:tools=&quot;http://schemas.android.com/tools&quot;</span><br><span class="line">    xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;</span><br><span class="line">    xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot;&gt;</span><br><span class="line"></span><br><span class="line">    &lt;item</span><br><span class="line">        android:id=&quot;@+id/menu1&quot;</span><br><span class="line">        android:icon=&quot;@drawable/nav_friends&quot;</span><br><span class="line">        android:title=&quot;Mune&quot;</span><br><span class="line">        app:showAsAction=&quot;always&quot; /&gt;</span><br><span class="line"></span><br><span class="line">    &lt;item</span><br><span class="line">        android:id=&quot;@+id/backup&quot;</span><br><span class="line">        android:icon=&quot;@drawable/ic_backup&quot;</span><br><span class="line">        android:title=&quot;Backup&quot;</span><br><span class="line">        app:showAsAction=&quot;always&quot; /&gt;</span><br><span class="line">    &lt;item</span><br><span class="line">        android:id=&quot;@+id/delete&quot;</span><br><span class="line">        android:icon=&quot;@drawable/ic_delete&quot;</span><br><span class="line">        android:title=&quot;Delete&quot;</span><br><span class="line">        app:showAsAction=&quot;ifRoom&quot; /&gt;</span><br><span class="line">    &lt;item</span><br><span class="line">        android:id=&quot;@+id/settings&quot;</span><br><span class="line">        android:icon=&quot;@drawable/ic_settings&quot;</span><br><span class="line">        android:title=&quot;Settings&quot;</span><br><span class="line">        app:showAsAction=&quot;never&quot; /&gt;</span><br><span class="line">&lt;/menu&gt;</span><br></pre></td></tr></table></figure><h3 id="滑动菜单"><a class="markdownIt-Anchor" href="#滑动菜单"></a> 滑动菜单</h3><h4 id="drawerlayout控件"><a class="markdownIt-Anchor" href="#drawerlayout控件"></a> DrawerLayout控件</h4><p>1.在activity_main.xml添加DrawerLayout</p><p>2.在MainActivity添加一个导航按钮</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">androidx.drawerlayout.widget.DrawerLayout</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:id</span>=<span class="string">&quot;@+id/drawer_layout&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// DrawerLayout对象</span></span><br><span class="line"><span class="keyword">private</span> DrawerLayout mDrawerLayout;</span><br><span class="line"></span><br><span class="line"> mDrawerLayout = (DrawerLayout) findViewById(R.id.drawer_layout);</span><br><span class="line">           <span class="comment">// 获取ActionBar对象</span></span><br><span class="line">        <span class="type">ActionBar</span> <span class="variable">actionBar</span> <span class="operator">=</span> getSupportActionBar();</span><br><span class="line">        <span class="keyword">if</span> (actionBar != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 显示HomeAsUp按钮</span></span><br><span class="line">            actionBar.setDisplayHomeAsUpEnabled(<span class="literal">true</span>);</span><br><span class="line">            <span class="comment">// 设置HomeAsUp按钮的图标</span></span><br><span class="line">            <span class="comment">// actionBar.setHomeAsUpIndicator(R.drawable.ic_menu);</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span> (item.getItemId() == android.R.id.home) &#123;</span><br><span class="line">            <span class="comment">// 打开DrawerLayout</span></span><br><span class="line">            mDrawerLayout.openDrawer(GravityCompat.START);</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure><h4 id="navigationview滑动菜单页面"><a class="markdownIt-Anchor" href="#navigationview滑动菜单页面"></a> NavigationView(滑动菜单页面)</h4><p>1.导入依赖库</p><p>2.在menu下创建nav_menu.xml文件,并设计相应item</p><p>3.在layout下创建一个nav_header.xml文件(头像区)</p><p>4.在activity_main.xml使用NavigationView</p><p>5.创建点击事件</p><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">implementation(<span class="string">&quot;de.hdodenhof:circleimageview:2.1.0&quot;</span>)</span><br><span class="line">    implementation(<span class="string">&quot;androidx.core:core:1.13.0&quot;</span>)</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">menu</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">group</span> <span class="attr">android:checkableBehavior</span>=<span class="string">&quot;single&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">item</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/nav_call&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:icon</span>=<span class="string">&quot;@drawable/nav_call&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:title</span>=<span class="string">&quot;Call&quot;</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">item</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/nav_friends&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:icon</span>=<span class="string">&quot;@drawable/nav_friends&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:title</span>=<span class="string">&quot;Friends&quot;</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">item</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/location&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:icon</span>=<span class="string">&quot;@drawable/nav_location&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:title</span>=<span class="string">&quot;Location&quot;</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">item</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/nav_task&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:icon</span>=<span class="string">&quot;@drawable/nav_task&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:title</span>=<span class="string">&quot;Task&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;/<span class="name">group</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">menu</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">RelativeLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;180dp&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:padding</span>=<span class="string">&quot;10dp&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:background</span>=<span class="string">&quot;?attr/colorPrimary&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">tools:ignore</span>=<span class="string">&quot;ExtraText&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">de.hdodenhof.circleimageview.CircleImageView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/icon_image&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;70dp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;70dp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:src</span>=<span class="string">&quot;@drawable/nav_icon&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_centerInParent</span>=<span class="string">&quot;true&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/mail&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_alignParentBottom</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;240236946@qq.com&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:textColor</span>=<span class="string">&quot;#FFF&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:textSize</span>=<span class="string">&quot;14sp&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/username&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_above</span>=<span class="string">&quot;@+id/mail&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;左红柊&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:textColor</span>=<span class="string">&quot;#FFF&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:textSize</span>=<span class="string">&quot;14sp&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">RelativeLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">com.google.android.material.navigation.NavigationView</span></span></span><br><span class="line"><span class="tag">       <span class="attr">android:id</span>=<span class="string">&quot;@+id/nav_view&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">android:layout_gravity</span>=<span class="string">&quot;start&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">app:menu</span>=<span class="string">&quot;@menu/nav_menu&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">app:headerLayout</span>=<span class="string">&quot;@layout/nav_header&quot;</span></span></span><br><span class="line"><span class="tag">       <span class="attr">tools:ignore</span>=<span class="string">&quot;MissingClass&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 获取NavigationView对象</span></span><br><span class="line">       <span class="type">NavigationView</span> <span class="variable">navView</span> <span class="operator">=</span> (NavigationView) findViewById(R.id.nav_view);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置NavigationView的选中项</span></span><br><span class="line">       navView.setCheckedItem(R.id.nav_call);</span><br><span class="line"></span><br><span class="line">       <span class="comment">// 设置NavigationView的点击事件</span></span><br><span class="line">       navView.setNavigationItemSelectedListener(<span class="keyword">new</span> <span class="title class_">NavigationView</span>.OnNavigationItemSelectedListener() &#123;</span><br><span class="line">           <span class="meta">@Override</span></span><br><span class="line">           <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">onNavigationItemSelected</span><span class="params">(MenuItem menuItem)</span> &#123;</span><br><span class="line">               <span class="comment">// 关闭DrawerLayout</span></span><br><span class="line">               mDrawerLayout.closeDrawers();</span><br><span class="line">               <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;);</span><br></pre></td></tr></table></figure><h3 id="悬浮按钮和可交互显示"><a class="markdownIt-Anchor" href="#悬浮按钮和可交互显示"></a> 悬浮按钮和可交互显示</h3><h4 id="floatingactionbutton"><a class="markdownIt-Anchor" href="#floatingactionbutton"></a> FloatingActionButton</h4><p>1.在activity_main.xml</p><p>2.设置点击事件</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">com.google.android.material.floatingactionbutton.FloatingActionButton</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/fab&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_gravity</span>=<span class="string">&quot;bottom|end&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_margin</span>=<span class="string">&quot;16dp&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:src</span>=<span class="string">&quot;@android:drawable/ic_input_add&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:contentDescription</span>=<span class="string">&quot;添加&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">app:tint</span>=<span class="string">&quot;#E27298&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">app:elevation</span>=<span class="string">&quot;8dp&quot;</span> /&gt;</span> //投影高度</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 获取FloatingActionButton对象</span></span><br><span class="line">      <span class="type">FloatingActionButton</span> <span class="variable">fab</span> <span class="operator">=</span> (FloatingActionButton) findViewById(R.id.fab);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 设置FloatingActionButton的点击事件</span></span><br><span class="line">      fab.setOnClickListener(v -&gt; &#123;</span><br><span class="line">          <span class="comment">// 显示Snackbar</span></span><br><span class="line">          Snackbar.make(v, <span class="string">&quot;Data deleted&quot;</span>, Snackbar.LENGTH_SHORT).setAction(<span class="string">&quot;Undo&quot;</span>, <span class="keyword">new</span> <span class="title class_">View</span>.OnClickListener() &#123;</span><br><span class="line">              <span class="meta">@Override</span></span><br><span class="line">              <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">                  <span class="comment">// 显示Toast</span></span><br><span class="line">                  Toast.makeText(MainActivity.<span class="built_in">this</span>, <span class="string">&quot;Data restored&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">              &#125;</span><br><span class="line">          &#125;).show();</span><br><span class="line">      &#125;);</span><br></pre></td></tr></table></figure><h4 id="snackbar可交互的toast"><a class="markdownIt-Anchor" href="#snackbar可交互的toast"></a> Snackbar(可交互的Toast)</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 设置FloatingActionButton的点击事件</span></span><br><span class="line">        fab.setOnClickListener(v -&gt; &#123;</span><br><span class="line">            <span class="comment">// 显示Snackbar</span></span><br><span class="line">            Snackbar.make(v, <span class="string">&quot;Data deleted&quot;</span>, Snackbar.LENGTH_SHORT).setAction(<span class="string">&quot;Undo&quot;</span>, <span class="keyword">new</span> <span class="title class_">View</span>.OnClickListener() &#123;</span><br><span class="line">                <span class="meta">@Override</span></span><br><span class="line">                <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">                    <span class="comment">// 显示Toast</span></span><br><span class="line">                    Toast.makeText(MainActivity.<span class="built_in">this</span>, <span class="string">&quot;Data restored&quot;</span>, Toast.LENGTH_SHORT).show();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).show();</span><br><span class="line">        &#125;);</span><br></pre></td></tr></table></figure><h4 id="coordinatorlayout加强版framelayout"><a class="markdownIt-Anchor" href="#coordinatorlayout加强版framelayout"></a> CoordinatorLayout(加强版FrameLayout)</h4><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">androidx.coordinatorlayout.widget.CoordinatorLayout</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">com.google.android.material.appbar.AppBarLayout</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="tag">&lt;<span class="name">androidx.appcompat.widget.Toolbar</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/toolbar&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_height</span>=<span class="string">&quot;?attr/actionBarSize&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:background</span>=<span class="string">&quot;?attr/colorPrimary&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:theme</span>=<span class="string">&quot;@style/ThemeOverlay.AppCompat.Dark.ActionBar&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">app:popupTheme</span>=<span class="string">&quot;@style/ThemeOverlay.AppCompat.Light&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">app:layout_scrollFlags</span>=<span class="string">&quot;scroll|enterAlways|snap&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="tag">&lt;/<span class="name">com.google.android.material.appbar.AppBarLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">androidx.swiperefreshlayout.widget.SwipeRefreshLayout</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/swipe_refresh&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">app:layout_behavior</span>=<span class="string">&quot;@string/appbar_scrolling_view_behavior&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">tools:ignore</span>=<span class="string">&quot;MissingClass&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="tag">&lt;<span class="name">androidx.recyclerview.widget.RecyclerView</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/recycler_view&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;/<span class="name">androidx.swiperefreshlayout.widget.SwipeRefreshLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">com.google.android.material.floatingactionbutton.FloatingActionButton</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/fab&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_gravity</span>=<span class="string">&quot;bottom|end&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_margin</span>=<span class="string">&quot;16dp&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:src</span>=<span class="string">&quot;@android:drawable/ic_input_add&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:contentDescription</span>=<span class="string">&quot;添加&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">app:tint</span>=<span class="string">&quot;#E27298&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">app:elevation</span>=<span class="string">&quot;8dp&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;/<span class="name">androidx.coordinatorlayout.widget.CoordinatorLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="卡片式布局"><a class="markdownIt-Anchor" href="#卡片式布局"></a> 卡片式布局</h3><h4 id="cardview提供圆角与阴影等效果"><a class="markdownIt-Anchor" href="#cardview提供圆角与阴影等效果"></a> CardView(提供圆角与阴影等效果)</h4><p>1.添加依赖库(CardView与RecyclerView)</p><p>2.在activity_main.xml调用RecyclerView</p><p>3.定义一个实体类(构造方法Fruit)与一个自定义布局(Fruit_item.xml)</p><p>4.创建RecyclerView适配器</p><p>5.在MainActivity展示卡片</p><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">implementation(<span class="string">&quot;androidx.recyclerview:recyclerview:1.3.2&quot;</span>)</span><br><span class="line">    implementation(<span class="string">&quot;androidx.cardview:cardview:1.0.0&quot;</span>)</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">androidx.recyclerview.widget.RecyclerView</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/recycler_view&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.materialtest;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Fruit</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span>  <span class="type">int</span> imageId;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Fruit</span><span class="params">(String name,<span class="type">int</span> imageId)</span>&#123;</span><br><span class="line">        <span class="built_in">this</span>.name=name;</span><br><span class="line">        <span class="built_in">this</span>.imageId=imageId;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getName</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getImageId</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> imageId;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">androidx.cardview.widget.CardView</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_margin</span>=<span class="string">&quot;5dp&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">app:cardCornerRadius</span>=<span class="string">&quot;4dp&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">LinearLayout</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span> &gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">ImageView</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/fruit_image&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;120dp&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:scaleType</span>=<span class="string">&quot;centerCrop&quot;</span></span></span><br><span class="line"><span class="tag">            /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/fruit_name&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_gravity</span>=<span class="string">&quot;center_horizontal&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_margin</span>=<span class="string">&quot;5dp&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:textSize</span>=<span class="string">&quot;16sp&quot;</span></span></span><br><span class="line"><span class="tag">            /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">androidx.cardview.widget.CardView</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">FruitAdapter</span> <span class="keyword">extends</span> <span class="title class_">RecyclerView</span>.Adapter&lt;FruitAdapter.ViewHolder&gt; &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 上下文对象</span></span><br><span class="line">    <span class="keyword">private</span> Context mContext;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 水果列表</span></span><br><span class="line">    <span class="keyword">private</span> List&lt;Fruit&gt; mFruitList;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * ViewHolder类，负责显示单个水果的信息。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">ViewHolder</span> <span class="keyword">extends</span> <span class="title class_">RecyclerView</span>.ViewHolder &#123;</span><br><span class="line">        <span class="comment">// 卡片视图</span></span><br><span class="line">        CardView cardView;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 水果图片</span></span><br><span class="line">        ImageView fruitImage;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 水果名称</span></span><br><span class="line">        TextView fruitName;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 构造函数，初始化视图。</span></span><br><span class="line"><span class="comment">         *</span></span><br><span class="line"><span class="comment">         * <span class="doctag">@param</span> view 视图对象</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">public</span> <span class="title function_">ViewHolder</span><span class="params">(View view)</span> &#123;</span><br><span class="line">            <span class="built_in">super</span>(view);</span><br><span class="line">            <span class="comment">// 获取卡片视图</span></span><br><span class="line">            cardView = (CardView) view;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 获取水果图片</span></span><br><span class="line">            fruitImage = (ImageView) view.findViewById(R.id.fruit_image);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 获取水果名称</span></span><br><span class="line">            fruitName = (TextView) view.findViewById(R.id.fruit_name);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 构造函数，初始化水果列表。</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> fruitList 水果列表</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">FruitAdapter</span><span class="params">(List&lt;Fruit&gt; fruitList)</span> &#123;</span><br><span class="line">        <span class="comment">// 初始化水果列表</span></span><br><span class="line">        mFruitList = fruitList;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 创建ViewHolder对象。</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> parent  父视图</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> viewType 视图类型</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> ViewHolder对象</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@NonNull</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> ViewHolder <span class="title function_">onCreateViewHolder</span><span class="params">(<span class="meta">@NonNull</span> ViewGroup parent, <span class="type">int</span> viewType)</span> &#123;</span><br><span class="line">        <span class="comment">// 如果上下文对象为空，则获取父视图的上下文</span></span><br><span class="line">        <span class="keyword">if</span> (mContext == <span class="literal">null</span>) &#123;</span><br><span class="line">            mContext = parent.getContext();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取视图</span></span><br><span class="line">        <span class="type">View</span> <span class="variable">view</span> <span class="operator">=</span> LayoutInflater.from(mContext).inflate(R.layout.fruit_item, parent, <span class="literal">false</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建ViewHolder对象</span></span><br><span class="line">        <span class="keyword">final</span> <span class="type">ViewHolder</span> <span class="variable">holder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ViewHolder</span>(view);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置卡片视图的点击事件</span></span><br><span class="line">        holder.cardView.setOnClickListener(<span class="keyword">new</span> <span class="title class_">View</span>.OnClickListener() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onClick</span><span class="params">(View v)</span> &#123;</span><br><span class="line">                <span class="comment">// 获取当前位置</span></span><br><span class="line">                <span class="type">int</span> <span class="variable">position</span> <span class="operator">=</span> holder.getAdapterPosition();</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 获取当前水果</span></span><br><span class="line">                <span class="type">Fruit</span> <span class="variable">fruit</span> <span class="operator">=</span> mFruitList.get(position);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 创建意图</span></span><br><span class="line">                <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(mContext, FruitActivity.class);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 设置意图的参数</span></span><br><span class="line">                intent.putExtra(FruitActivity.FRUIT_NAME, fruit.getName());</span><br><span class="line">                intent.putExtra(FruitActivity.FRUIT_IMAGE_ID, fruit.getImageId());</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 启动活动</span></span><br><span class="line">                mContext.startActivity(intent);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 返回ViewHolder对象</span></span><br><span class="line">        <span class="keyword">return</span> holder;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 绑定数据到ViewHolder对象。</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> holder ViewHolder对象</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> position 位置</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onBindViewHolder</span><span class="params">(<span class="meta">@NonNull</span> FruitAdapter.ViewHolder holder, <span class="type">int</span> position)</span> &#123;</span><br><span class="line">        <span class="comment">// 获取当前水果</span></span><br><span class="line">        <span class="type">Fruit</span> <span class="variable">fruit</span> <span class="operator">=</span> mFruitList.get(position);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置水果名称</span></span><br><span class="line">        holder.fruitName.setText(fruit.getName());</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 加载水果图片</span></span><br><span class="line">        Glide.with(mContext).load(fruit.getImageId()).into(holder.fruitImage);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 获取数据项数量。</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 数据项数量</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getItemCount</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 返回水果列表的大小</span></span><br><span class="line">        <span class="keyword">return</span> mFruitList.size();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> Fruit[] fruits = <span class="keyword">new</span> <span class="title class_">Fruit</span>[]&#123;</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Fruit</span>(<span class="string">&quot;劳左福利套餐_黑暗系劳左&quot;</span>, R.drawable.nav_icon),</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Fruit</span>(<span class="string">&quot;劳左福利套餐_白月光劳左&quot;</span>, R.drawable.banana),</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Fruit</span>(<span class="string">&quot;劳左福利套餐_小仙女劳左&quot;</span>, R.drawable.orange)</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 水果列表</span></span><br><span class="line">    <span class="keyword">private</span> List&lt;Fruit&gt; fruitList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 适配器</span></span><br><span class="line">    <span class="keyword">private</span> FruitAdapter adapter;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">//OnCreate()</span></span><br><span class="line"><span class="comment">// 初始化水果数据</span></span><br><span class="line">        initFruits();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取RecyclerView对象</span></span><br><span class="line">        <span class="type">RecyclerView</span> <span class="variable">recyclerView</span> <span class="operator">=</span> (RecyclerView) findViewById(R.id.recycler_view);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置RecyclerView的布局管理器</span></span><br><span class="line">        <span class="type">GridLayoutManager</span> <span class="variable">layoutManager</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">GridLayoutManager</span>(<span class="built_in">this</span>, <span class="number">2</span>);</span><br><span class="line">        recyclerView.setLayoutManager(layoutManager);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置RecyclerView的适配器</span></span><br><span class="line">        adapter = <span class="keyword">new</span> <span class="title class_">FruitAdapter</span>(fruitList);</span><br><span class="line">        recyclerView.setAdapter(adapter);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"> <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 初始化水果数据。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">initFruits</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 清空水果列表</span></span><br><span class="line">        fruitList.clear();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 生成50个随机水果</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">50</span>; i++) &#123;</span><br><span class="line">            <span class="type">Random</span> <span class="variable">random</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Random</span>();</span><br><span class="line">            <span class="type">int</span> <span class="variable">index</span> <span class="operator">=</span> random.nextInt(fruits.length);</span><br><span class="line">            fruitList.add(fruits[index]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="appbarlayout垂直方向上的linearlayout滚动事件的封装"><a class="markdownIt-Anchor" href="#appbarlayout垂直方向上的linearlayout滚动事件的封装"></a> AppBarLayout(垂直方向上的Linearlayout,滚动事件的封装)</h4><p>1.将Toolbar嵌套到AppBarLayout</p><p>2.给RecyclerView指定一个布局行为</p><p>3.在activity_main.xml添加app:layout_scrollFlags=“scroll|enterAlways|snap”(滑动时隐藏Toolbar)</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">com.google.android.material.appbar.AppBarLayout</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="tag">&lt;<span class="name">androidx.appcompat.widget.Toolbar</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/toolbar&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_height</span>=<span class="string">&quot;?attr/actionBarSize&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:background</span>=<span class="string">&quot;?attr/colorPrimary&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:theme</span>=<span class="string">&quot;@style/ThemeOverlay.AppCompat.Dark.ActionBar&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">app:popupTheme</span>=<span class="string">&quot;@style/ThemeOverlay.AppCompat.Light&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">app:layout_scrollFlags</span>=<span class="string">&quot;scroll|enterAlways|snap&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="tag">&lt;/<span class="name">com.google.android.material.appbar.AppBarLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"> // androidx.swiperefreshlayout.widget.SwipeRefreshLayout</span><br><span class="line"></span><br><span class="line">app:layout_behavior=&quot;@string/appbar_scrolling_view_behavior&quot;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">app:layout_scrollFlags=&quot;scroll|enterAlways|snap&quot;</span><br></pre></td></tr></table></figure><h3 id="下拉刷新"><a class="markdownIt-Anchor" href="#下拉刷新"></a> 下拉刷新</h3><p>1.在activity_main.xml中实现SwipeRefreshLayout核心类</p><p>2.在MainActivity处理刷新逻辑</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">androidx.swiperefreshlayout.widget.SwipeRefreshLayout</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/swipe_refresh&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">app:layout_behavior</span>=<span class="string">&quot;@string/appbar_scrolling_view_behavior&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">tools:ignore</span>=<span class="string">&quot;MissingClass&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="tag">&lt;<span class="name">androidx.recyclerview.widget.RecyclerView</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/recycler_view&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;/<span class="name">androidx.swiperefreshlayout.widget.SwipeRefreshLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> SwipeRefreshLayout swipeRefresh;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 获取SwipeRefreshLayout对象</span></span><br><span class="line">        swipeRefresh = (SwipeRefreshLayout) findViewById(R.id.swipe_refresh);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置SwipeRefreshLayout的颜色</span></span><br><span class="line">        swipeRefresh.setColorSchemeResources(R.color.pink);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置SwipeRefreshLayout的刷新监听器</span></span><br><span class="line">        swipeRefresh.setOnRefreshListener(() -&gt; refreshFruits());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 刷新水果数据。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">refreshFruits</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 创建线程</span></span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="comment">// 等待1秒</span></span><br><span class="line">                    Thread.sleep(<span class="number">999</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 刷新UI</span></span><br><span class="line">                runOnUiThread(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">                    <span class="meta">@Override</span></span><br><span class="line">                    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                        <span class="comment">// 初始化水果数据</span></span><br><span class="line">                        initFruits();</span><br><span class="line">                        <span class="comment">// 通知适配器更新数据</span></span><br><span class="line">                        adapter.notifyDataSetChanged();</span><br><span class="line">                        <span class="comment">// 停止SwipeRefreshLayout的刷新</span></span><br><span class="line">                        swipeRefresh.setRefreshing(<span class="literal">false</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="可折叠式标题栏"><a class="markdownIt-Anchor" href="#可折叠式标题栏"></a> 可折叠式标题栏</h3><h4 id="collapsingtoolbarlayout作用于toolbar之上的布局"><a class="markdownIt-Anchor" href="#collapsingtoolbarlayout作用于toolbar之上的布局"></a> CollapsingToolbarLayout(作用于Toolbar之上的布局)</h4><p>1.设置FruitActivity作为详情展示界面</p><p>2.修改activity_fruit.xml</p><p>3.在AppBarLayout嵌套一个CollapsingToolbarLayout</p><p>4.在FruitActivity编写功能逻辑</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">androidx.coordinatorlayout.widget.CoordinatorLayout</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:app</span>=<span class="string">&quot;http://schemas.android.com/apk/res-auto&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:fitsSystemWindows</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">com.google.android.material.appbar.AppBarLayout</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/appBar&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;250dp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:fitsSystemWindows</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">com.google.android.material.appbar.CollapsingToolbarLayout</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:id</span>=<span class="string">&quot;@+id/collapsing_toolbar&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:theme</span>=<span class="string">&quot;@style/ThemeOverlay.AppCompat.Dark.ActionBar&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:fitsSystemWindows</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">app:contentScrim</span>=<span class="string">&quot;?attr/colorPrimary&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">app:layout_scrollFlags</span>=<span class="string">&quot;scroll|exitUntilCollapsed&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="tag">&lt;<span class="name">ImageView</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/fruit_image_view&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:scaleType</span>=<span class="string">&quot;centerCrop&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:fitsSystemWindows</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">app:layout_collapseMode</span>=<span class="string">&quot;parallax&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="tag">&lt;<span class="name">androidx.appcompat.widget.Toolbar</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:id</span>=<span class="string">&quot;@+id/toolbar&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_height</span>=<span class="string">&quot;?attr/actionBarSize&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">app:layout_collapseMode</span>=<span class="string">&quot;pin&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;/<span class="name">com.google.android.material.appbar.CollapsingToolbarLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;/<span class="name">com.google.android.material.appbar.AppBarLayout</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">androidx.core.widget.NestedScrollView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">app:layout_behavior</span>=<span class="string">&quot;@string/appbar_scrolling_view_behavior&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">LinearLayout</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="tag">&lt;<span class="name">androidx.cardview.widget.CardView</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_marginBottom</span>=<span class="string">&quot;15dp&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_marginLeft</span>=<span class="string">&quot;15dp&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_marginRight</span>=<span class="string">&quot;15dp&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">android:layout_marginTop</span>=<span class="string">&quot;35dp&quot;</span></span></span><br><span class="line"><span class="tag">                <span class="attr">app:cardCornerRadius</span>=<span class="string">&quot;4dp&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">                <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">                    <span class="attr">android:id</span>=<span class="string">&quot;@+id/fruit_content_text&quot;</span></span></span><br><span class="line"><span class="tag">                    <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">                    <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">                    <span class="attr">android:layout_margin</span>=<span class="string">&quot;10dp&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">            <span class="tag">&lt;/<span class="name">androidx.cardview.widget.CardView</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">androidx.core.widget.NestedScrollView</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">com.google.android.material.floatingactionbutton.FloatingActionButton</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_margin</span>=<span class="string">&quot;16dp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:src</span>=<span class="string">&quot;@drawable/ic_backup&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">app:layout_anchor</span>=<span class="string">&quot;@id/appBar&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">app:layout_anchorGravity</span>=<span class="string">&quot;bottom|end&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">androidx.coordinatorlayout.widget.CoordinatorLayout</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">FruitActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 水果名称的意图键值</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">FRUIT_NAME</span> <span class="operator">=</span> <span class="string">&quot;fruit_name&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 水果图片ID的意图键值</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">FRUIT_IMAGE_ID</span> <span class="operator">=</span> <span class="string">&quot;fruit_image_id&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="comment">// 调用父类的onCreate方法</span></span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置布局文件</span></span><br><span class="line">        setContentView(R.layout.activity_fruit);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取意图</span></span><br><span class="line">        <span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> getIntent();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取水果名称和图片ID</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">fruitName</span> <span class="operator">=</span> intent.getStringExtra(FRUIT_NAME);</span><br><span class="line">        <span class="type">int</span> <span class="variable">fruitImageId</span> <span class="operator">=</span> intent.getIntExtra(FRUIT_IMAGE_ID, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取Toolbar和CollapsingToolbarLayout</span></span><br><span class="line">        <span class="type">Toolbar</span> <span class="variable">toolbar</span> <span class="operator">=</span> (Toolbar) findViewById(R.id.toolbar);</span><br><span class="line">        <span class="type">CollapsingToolbarLayout</span> <span class="variable">collapsingToolbar</span> <span class="operator">=</span> (CollapsingToolbarLayout) findViewById(R.id.collapsing_toolbar);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取水果图片和内容文本</span></span><br><span class="line">        <span class="type">ImageView</span> <span class="variable">fruitImageView</span> <span class="operator">=</span> (ImageView) findViewById(R.id.fruit_image_view);</span><br><span class="line">        <span class="type">TextView</span> <span class="variable">fruitContentText</span> <span class="operator">=</span> (TextView) findViewById(R.id.fruit_content_text);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置Toolbar为ActionBar</span></span><br><span class="line">        setSupportActionBar(toolbar);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取ActionBar</span></span><br><span class="line">        <span class="type">ActionBar</span> <span class="variable">actionBar</span> <span class="operator">=</span> getSupportActionBar();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果ActionBar不为空，则显示返回按钮</span></span><br><span class="line">        <span class="keyword">if</span> (actionBar != <span class="literal">null</span>) &#123;</span><br><span class="line">            actionBar.setDisplayHomeAsUpEnabled(<span class="literal">true</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置CollapsingToolbarLayout的标题</span></span><br><span class="line">        collapsingToolbar.setTitle(fruitName);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 加载水果图片</span></span><br><span class="line">        Glide.with(<span class="built_in">this</span>).load(fruitImageId).into(fruitImageView);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 生成水果内容文本</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">fruitContent</span> <span class="operator">=</span> generateFruitContent(fruitName);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 设置水果内容文本</span></span><br><span class="line">        fruitContentText.setText(fruitContent);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 生成水果内容文本。</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> fruitName 水果名称</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 水果内容文本</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> String <span class="title function_">generateFruitContent</span><span class="params">(String fruitName)</span> &#123;</span><br><span class="line">        <span class="comment">// 创建StringBuilder对象</span></span><br><span class="line">        <span class="type">StringBuilder</span> <span class="variable">fruitContent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StringBuilder</span>();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 生成500个水果名称</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">500</span>; i++) &#123;</span><br><span class="line">            fruitContent.append(fruitName);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 返回水果内容文本</span></span><br><span class="line">        <span class="keyword">return</span> fruitContent.toString();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">onOptionsItemSelected</span><span class="params">(<span class="meta">@NonNull</span> MenuItem item)</span> &#123;</span><br><span class="line">        <span class="comment">// 判断是否是返回按钮</span></span><br><span class="line">        <span class="keyword">switch</span> (item.getItemId()) &#123;</span><br><span class="line">            <span class="keyword">case</span> android.R.id.home:</span><br><span class="line">                <span class="comment">// 如果是返回按钮，则finish当前活动</span></span><br><span class="line">                finish();</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果不是返回按钮，则调用父类的onOptionsItemSelected方法</span></span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">super</span>.onOptionsItemSelected(item);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="充分利用系统空间状态栏"><a class="markdownIt-Anchor" href="#充分利用系统空间状态栏"></a> 充分利用系统空间状态栏</h4><p>1.使用android:fitsSystemWindows=“true”</p><p>2.在values-v21创建一个styles.xml</p><p>3.创建values的styles.xml</p><p>4.在AndroidManifest.xml应用FruitActivityTheme主题</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">android:fitsSystemWindows=&quot;true&quot;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">resources</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">style</span> <span class="attr">name</span>=<span class="string">&quot;AppTheme&quot;</span> <span class="attr">parent</span>=<span class="string">&quot;Theme.AppCompat.Light.NoActionBar&quot;</span>&gt;</span><span class="language-xml"></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;colorPrimary&quot;</span>&gt;</span>@color/pink<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;colorPrimaryDark&quot;</span>&gt;</span>@color/pink<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;colorAccent&quot;</span>&gt;</span>@color/pink<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    </span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">style</span> <span class="attr">name</span>=<span class="string">&quot;FruitActivityTheme&quot;</span> <span class="attr">parent</span>=<span class="string">&quot;AppTheme&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resources</span>&gt;</span></span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">resources</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">style</span> <span class="attr">name</span>=<span class="string">&quot;FruitActivityTheme&quot;</span> <span class="attr">parent</span>=<span class="string">&quot;AppTheme&quot;</span>&gt;</span><span class="language-xml"></span></span><br><span class="line"><span class="language-xml">        <span class="tag">&lt;<span class="name">item</span> <span class="attr">name</span>=<span class="string">&quot;android:statusBarColor&quot;</span>&gt;</span>@android:color/transparent<span class="tag">&lt;/<span class="name">item</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    </span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resources</span>&gt;</span></span><br></pre></td></tr></table></figure><hr /><h2 id="使用intent传递对象"><a class="markdownIt-Anchor" href="#使用intent传递对象"></a> 使用Intent传递对象</h2><h3 id="一-serializablejava-原生接口"><a class="markdownIt-Anchor" href="#一-serializablejava-原生接口"></a> 一、Serializable（Java 原生接口）</h3><h4 id="1-核心特点"><a class="markdownIt-Anchor" href="#1-核心特点"></a> 1. 核心特点</h4><ul><li><strong>简单易用</strong>：仅需实现接口，无需额外代码。</li><li><strong>性能一般</strong>：依赖反射机制，序列化和反序列化速度较慢。</li><li><strong>适用场景</strong>：数据存储到文件、网络传输等对性能要求不高的场景。</li></ul><h4 id="2-实现步骤"><a class="markdownIt-Anchor" href="#2-实现步骤"></a> 2. 实现步骤</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.Serializable;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> &#123;</span><br><span class="line">    <span class="comment">// 建议手动指定 serialVersionUID（避免类结构修改时反序列化失败）</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> <span class="number">1L</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构造方法、Getter/Setter...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="3-序列化与反序列化"><a class="markdownIt-Anchor" href="#3-序列化与反序列化"></a> 3. 序列化与反序列化</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 发送方</span></span><br><span class="line"><span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="built_in">this</span>, TargetActivity.class);</span><br><span class="line">intent.putExtra(<span class="string">&quot;user_data&quot;</span>, user);</span><br><span class="line">startActivity(intent);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 接收方</span></span><br><span class="line"><span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> getIntent().getSerializableExtra(<span class="string">&quot;user_data&quot;</span>);</span><br></pre></td></tr></table></figure><hr /><h3 id="二-parcelableandroid-专用接口"><a class="markdownIt-Anchor" href="#二-parcelableandroid-专用接口"></a> 二、Parcelable（Android 专用接口）</h3><h4 id="1-核心特点-2"><a class="markdownIt-Anchor" href="#1-核心特点-2"></a> 1. 核心特点</h4><ul><li><strong>高效</strong>：基于内存的二进制流操作，速度极快。</li><li><strong>复杂实现</strong>：需要手动编写序列化/反序列化逻辑。</li><li><strong>适用场景</strong>：内存中数据传输（如 Activity 间传递对象）。</li></ul><h4 id="2-实现步骤-2"><a class="markdownIt-Anchor" href="#2-实现步骤-2"></a> 2. 实现步骤</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> android.os.Parcel;</span><br><span class="line"><span class="keyword">import</span> android.os.Parcelable;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> <span class="keyword">implements</span> <span class="title class_">Parcelable</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构造方法、Getter/Setter...</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 反序列化逻辑（从 Parcel 构造对象）</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="title function_">User</span><span class="params">(Parcel in)</span> &#123;</span><br><span class="line">        name = in.readString();</span><br><span class="line">        age = in.readInt();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 序列化逻辑（将对象写入 Parcel）</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">writeToParcel</span><span class="params">(Parcel dest, <span class="type">int</span> flags)</span> &#123;</span><br><span class="line">        dest.writeString(name);</span><br><span class="line">        dest.writeInt(age);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 描述内容（通常返回 0）</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">describeContents</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建 Creator 静态字段（必须）</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> Creator&lt;User&gt; CREATOR = <span class="keyword">new</span> <span class="title class_">Creator</span>&lt;User&gt;() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> User <span class="title function_">createFromParcel</span><span class="params">(Parcel in)</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">User</span>(in);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> User[] newArray(<span class="type">int</span> size) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">User</span>[size];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="3-在-activity-间传递对象"><a class="markdownIt-Anchor" href="#3-在-activity-间传递对象"></a> 3. 在 Activity 间传递对象</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 发送方</span></span><br><span class="line"><span class="type">Intent</span> <span class="variable">intent</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Intent</span>(<span class="built_in">this</span>, TargetActivity.class);</span><br><span class="line">intent.putExtra(<span class="string">&quot;user_data&quot;</span>, user);</span><br><span class="line">startActivity(intent);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 接收方</span></span><br><span class="line"><span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> getIntent().getParcelableExtra(<span class="string">&quot;user_data&quot;</span>);</span><br></pre></td></tr></table></figure><hr /><h3 id="三-对比与选型建议"><a class="markdownIt-Anchor" href="#三-对比与选型建议"></a> 三、对比与选型建议</h3><table><thead><tr><th style="text-align:left"><strong>特性</strong></th><th style="text-align:left">Serializable</th><th style="text-align:left">Parcelable</th></tr></thead><tbody><tr><td style="text-align:left"><strong>实现复杂度</strong></td><td style="text-align:left">简单（自动）</td><td style="text-align:left">复杂（手动编写代码）</td></tr><tr><td style="text-align:left"><strong>性能</strong></td><td style="text-align:left">低（反射机制）</td><td style="text-align:left">高（直接内存操作）</td></tr><tr><td style="text-align:left"><strong>适用场景</strong></td><td style="text-align:left">存储/网络传输</td><td style="text-align:left">内存中数据传输</td></tr><tr><td style="text-align:left"><strong>Android 系统支持</strong></td><td style="text-align:left">支持</td><td style="text-align:left">仅限 Android</td></tr></tbody></table><ul><li><strong>选型原则</strong>：<ul><li>需要跨平台或持久化存储 → <strong>Serializable</strong>。</li><li>Android 内存高效传输（如 Intent/Bundle） → <strong>Parcelable</strong>。</li></ul></li><li><strong>优化技巧</strong>：<ul><li>使用 Kotlin 的 <code>@Parcelize</code> 注解可自动生成 Parcelable 代码。</li><li>对复杂对象考虑使用 <code>Bundle</code> 或第三方库（如 Gson + Serializable）。</li></ul></li></ul><hr /><h2 id="全局获取context"><a class="markdownIt-Anchor" href="#全局获取context"></a> 全局获取Context</h2><p>1.定制一个Application类MyApplication</p><p>2.在AndroidManifest.xml对其进行指定</p><p>3.在需要的地方调用</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyApplication</span> <span class="keyword">extends</span> <span class="title class_">Application</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Context context;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate();</span><br><span class="line">        context = getApplicationContext();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Context <span class="title function_">getContext</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> context;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">application</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:name</span>=<span class="string">&quot;com.example.materialtest.MyApplication&quot;</span></span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Toast.makeText(MyApplication.getContext(), <span class="string">&quot;You clicked Backup&quot;</span>, Toast.LENGTH_SHORT).show();</span><br></pre></td></tr></table></figure><hr /><h2 id="定制日志工具"><a class="markdownIt-Anchor" href="#定制日志工具"></a> 定制日志工具</h2><p>1.新建一个LogUtil类</p><p>2.用LogUtil打印日志</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LogUtil</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">VERBOSE</span> <span class="operator">=</span> <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">DEBUG</span> <span class="operator">=</span> <span class="number">2</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">INFO</span> <span class="operator">=</span> <span class="number">3</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">WARN</span> <span class="operator">=</span> <span class="number">4</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">ERROR</span> <span class="operator">=</span> <span class="number">5</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">NOTHING</span> <span class="operator">=</span> <span class="number">6</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">LEVEL</span> <span class="operator">=</span> VERBOSE; <span class="comment">// 设定日志显示级别</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">v</span><span class="params">(String tag, String msg)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (LEVEL &lt;= VERBOSE) &#123;</span><br><span class="line">            Log.v(tag, msg);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">d</span><span class="params">(String tag, String msg)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (LEVEL &lt;= DEBUG) &#123;</span><br><span class="line">            Log.d(tag, msg);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">i</span><span class="params">(String tag, String msg)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (LEVEL &lt;= INFO) &#123;</span><br><span class="line">            Log.i(tag, msg);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">w</span><span class="params">(String tag, String msg)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (LEVEL &lt;= WARN) &#123;</span><br><span class="line">            Log.w(tag, msg);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">e</span><span class="params">(String tag, String msg)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (LEVEL &lt;= ERROR) &#123;</span><br><span class="line">            Log.e(tag, msg);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">LogUtil.d(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;You clicked Delete&quot;</span>);</span><br></pre></td></tr></table></figure><hr /><h2 id="多窗口模式"><a class="markdownIt-Anchor" href="#多窗口模式"></a> 多窗口模式</h2><h4 id="1取消进入多窗口时活动的重新创建"><a class="markdownIt-Anchor" href="#1取消进入多窗口时活动的重新创建"></a> 1.取消进入多窗口时活动的重新创建</h4><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">android:configChanges=&quot;orientation|keyboardHidden|screenSize|screenLayout&quot;&gt;</span><br></pre></td></tr></table></figure><h4 id="2禁用多窗口模式"><a class="markdownIt-Anchor" href="#2禁用多窗口模式"></a> 2.禁用多窗口模式</h4><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">android:resizeableActivity=&quot;false&quot;&gt;</span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;android应用开发特色&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#android应用开发特色&quot;&gt;&lt;/a&gt; Android应用开发特色&lt;/h2&gt;
&lt;p&gt;四大组件:活动(Activity),服务(Service),广播接收器(Bro</summary>
      
    
    
    
    
    <category term="第一行代码" scheme="https://11111-beep.github.io/tags/%E7%AC%AC%E4%B8%80%E8%A1%8C%E4%BB%A3%E7%A0%81/"/>
    
  </entry>
  
  <entry>
    <title>Java笔记总结</title>
    <link href="https://11111-beep.github.io/2025/08/10/Java%E7%AC%94%E8%AE%B0%E6%80%BB%E7%BB%93/"/>
    <id>https://11111-beep.github.io/2025/08/10/Java%E7%AC%94%E8%AE%B0%E6%80%BB%E7%BB%93/</id>
    <published>2025-08-10T09:08:04.000Z</published>
    <updated>2025-12-14T09:11:32.699Z</updated>
    
    <content type="html"><![CDATA[<h1 id="注释"><a class="markdownIt-Anchor" href="#注释"></a> 注释</h1><h2 id="单行注释"><a class="markdownIt-Anchor" href="#单行注释"></a> 单行注释</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 这是一个单行注释</span></span><br></pre></td></tr></table></figure><h2 id="多行注释"><a class="markdownIt-Anchor" href="#多行注释"></a> 多行注释</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">  这是一个多行注释</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><h2 id="文档注释"><a class="markdownIt-Anchor" href="#文档注释"></a> 文档注释</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 这是一个文档注释</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><p><strong>注意</strong>: 文档注释一般是写在方法和类的上面.</p><hr /><h1 id="基本数据类型"><a class="markdownIt-Anchor" href="#基本数据类型"></a> 基本数据类型</h1><table><thead><tr><th>数据类型 (Data Type)</th><th>字节 (Bytes)</th><th>位数 (Bits)</th><th>默认值</th><th>取值范围 / 说明</th></tr></thead><tbody><tr><td><strong>byte</strong></td><td>1</td><td>8</td><td>0</td><td><code>-128</code> 到 <code>127</code></td></tr><tr><td><strong>short</strong></td><td>2</td><td>16</td><td>0</td><td><code>-32768</code> 到 <code>32767</code></td></tr><tr><td><strong>int</strong></td><td>4</td><td>32</td><td>0</td><td><code>-2^31</code> 到 <code>2^31 - 1</code></td></tr><tr><td><strong>long</strong></td><td>8</td><td>64</td><td>0L</td><td><code>-2^63</code> 到 <code>2^63 - 1</code></td></tr><tr><td><strong>float</strong></td><td>4</td><td>32</td><td>0.0f</td><td>单精度浮点数</td></tr><tr><td><strong>double</strong></td><td>8</td><td>64</td><td>0.0d</td><td>双精度浮点数</td></tr><tr><td><strong>char</strong></td><td>2</td><td>16</td><td><code>'\u0000'</code></td><td>存储单个 Unicode 字符</td></tr><tr><td><strong>boolean</strong></td><td><strong>不确定</strong></td><td><strong>逻辑上 1</strong></td><td><code>false</code></td><td>只有 <code>true</code> 和 <code>false</code> 两个值</td></tr></tbody></table><hr /><h1 id="关键字与标识符"><a class="markdownIt-Anchor" href="#关键字与标识符"></a> 关键字与标识符</h1><h2 id="关键字"><a class="markdownIt-Anchor" href="#关键字"></a> 关键字</h2><p>关键字是预先定义好的赋予了特殊含义的单词.</p><p><em>主要特点：</em></p><ul><li>全部小写.</li><li>被 Java 语言保留,不能用作标识符（即不能用它们来给变量、方法、类等命名）.</li><li>在常见的代码编辑器中,关键字通常会以高亮颜色显示.</li></ul><h2 id="标识符"><a class="markdownIt-Anchor" href="#标识符"></a> 标识符</h2><p>标识符就是程序员在编程时为<strong>变量、方法、类、接口、包</strong>等程序元素自定义的名称。</p><p><em>主要特点：</em></p><ul><li>可以由字母（A-Z, a-z）、美元符号（<code>$</code>）、下划线（<code>_</code>）和数字（0-9）组成.</li><li>不能以数字开头.</li><li>不能是 Java 的关键字或保留字（如 <code>int</code>, <code>class</code>, <code>goto</code> 等）.</li><li>严格区分大小写.</li></ul><h2 id="命名规范"><a class="markdownIt-Anchor" href="#命名规范"></a> 命名规范</h2><p>这是业界的最佳实践,虽然不遵守也能编译通过,但强烈建议遵循.</p><ol><li><strong>类名和接口名</strong>：<ul><li>采用<strong>大驼峰命名法 (Upper Camel Case)</strong>.</li><li>每个单词的首字母都大写.</li><li>示例：<code>public class HelloWorld</code>, <code>interface Runnable</code></li></ul></li><li><strong>变量名和方法名</strong>：<ul><li>采用<strong>小驼峰命名法 (lower Camel Case)</strong>.</li><li>第一个单词首字母小写,从第二个单词开始,每个单词的首字母大写.</li><li>示例：<code>int studentAge;</code>, <code>String userName;</code>, <code>void calculateSum()</code></li></ul></li><li><strong>常量名</strong>：<ul><li>所有字母都<strong>大写</strong>，单词之间用**下划线（<code>_</code>）**连接.</li><li>通常由 <code>public static final</code> 修饰.</li><li>示例：<code>public static final int MAX_USERS = 100;</code>, <code>final double PI = 3.14;</code></li></ul></li><li><strong>包名</strong>：<ul><li>所有字母都<strong>小写</strong>,通常是公司或组织的域名的反向形式.</li><li>示例：<code>package com.google.common;</code>, <code>package java.util;</code></li></ul></li></ol><hr /><h1 id="方法重载"><a class="markdownIt-Anchor" href="#方法重载"></a> 方法重载</h1><p>方法重载是面向对象的一种概念,方法重载就是指在同一个类里,允许多个方法拥有相同的名字,但它们的参数列表必须不同(类型不同,个数不同,顺序不同).</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Printer</span> &#123;</span><br><span class="line">    <span class="comment">// 方法名叫 print</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">print</span><span class="params">(String text)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;打印文本: &quot;</span> + text);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 方法名也叫 print，但参数类型不同 (int)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">print</span><span class="params">(<span class="type">int</span> number)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;打印数字: &quot;</span> + number);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 方法名也叫 print，但参数数量不同 (两个)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">print</span><span class="params">(String text, <span class="type">int</span> copies)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;打印文本 &#x27;&quot;</span> + text + <span class="string">&quot;&#x27;，共 &quot;</span> + copies + <span class="string">&quot; 份&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如何使用：</span></span><br><span class="line"><span class="type">Printer</span> <span class="variable">myPrinter</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Printer</span>();</span><br><span class="line">myPrinter.print(<span class="string">&quot;Hello World&quot;</span>); <span class="comment">// 调用第一个 print 方法</span></span><br><span class="line">myPrinter.print(<span class="number">1024</span>);         <span class="comment">// 调用第二个 print 方法</span></span><br><span class="line">myPrinter.print(<span class="string">&quot;报告&quot;</span>, <span class="number">3</span>);      <span class="comment">// 调用第三个 print 方法</span></span><br></pre></td></tr></table></figure><hr /><h1 id="逻辑运算符"><a class="markdownIt-Anchor" href="#逻辑运算符"></a> 逻辑运算符</h1><table><thead><tr><th>运算符</th><th>名称</th><th>运算类型</th><th>功能说明</th><th>特点</th></tr></thead><tbody><tr><td><code>&amp;</code></td><td>按位与 / 逻辑与（非短路）</td><td>位运算 / 布尔运算</td><td><strong>位运算</strong>：两个二进制位都为 <code>1</code> 才为 <code>1</code>；<strong>布尔运算</strong>：两个条件都为 <code>true</code> 才为 <code>true</code></td><td>不会短路，两个表达式都会执行</td></tr><tr><td>`</td><td>`</td><td>按位或 / 逻辑或（非短路）</td><td>位运算 / 布尔运算</td><td><strong>位运算</strong>：只要有一个二进制位为 <code>1</code> 就为 <code>1</code>；<strong>布尔运算</strong>：只要有一个条件为 <code>true</code> 就为 <code>true</code></td></tr><tr><td><code>!</code></td><td>逻辑非</td><td>布尔运算</td><td>取反运算，<code>true → false</code>，<code>false → true</code></td><td>只能用于布尔类型</td></tr><tr><td><code>^</code></td><td>按位异或 / 逻辑异或</td><td>位运算 / 布尔运算</td><td><strong>位运算</strong>：两个二进制位不同为 <code>1</code>；<strong>布尔运算</strong>：两个条件不同为 <code>true</code></td><td>常用于判断差异或变量交换</td></tr><tr><td><code>&amp;&amp;</code></td><td>逻辑与（短路）</td><td>布尔运算</td><td>两个条件都为 <code>true</code>，结果才为 <code>true</code></td><td><strong>短路</strong>：左边为 <code>false</code> 时，右边不执行</td></tr><tr><td>`</td><td></td><td>`</td><td>逻辑或（短路）</td><td>布尔运算</td></tr></tbody></table><hr /><h1 id="后缀补全"><a class="markdownIt-Anchor" href="#后缀补全"></a> 后缀补全</h1><h2 id="java-后缀补全用法大全"><a class="markdownIt-Anchor" href="#java-后缀补全用法大全"></a> <strong>Java 后缀补全用法大全</strong></h2><p>这份表格适用于在 <code>.java</code> 文件中进行 Android 或其他 Java 项目开发.</p><table><thead><tr><th style="text-align:center">分类</th><th style="text-align:center">模板</th><th style="text-align:center">说明</th><th style="text-align:center">输入示例</th><th style="text-align:center">生成代码</th></tr></thead><tbody><tr><td style="text-align:center"><strong>变量声明</strong></td><td style="text-align:center"><code>.var</code></td><td style="text-align:center">为表达式的结果创建一个局部变量</td><td style="text-align:center"><code>new User(&quot;admin&quot;)</code><strong>.var</strong></td><td style="text-align:center"><code>User user = new User(&quot;admin&quot;);</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.field</code></td><td style="text-align:center">为表达式的结果创建一个类的成员变量（Field）</td><td style="text-align:center"><code>new Button(this)</code><strong>.field</strong></td><td style="text-align:center"><code>private final Button button;</code> <br/>(在类的顶部声明)</td></tr><tr><td style="text-align:center"><strong>循环</strong></td><td style="text-align:center"><code>.for</code></td><td style="text-align:center">遍历一个 <code>Iterable</code> 对象（增强 for 循环）</td><td style="text-align:center"><code>getUsers()</code><strong>.for</strong></td><td style="text-align:center"><code>for (User user : getUsers()) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.fori</code></td><td style="text-align:center">使用索引遍历数组或 <code>List</code>（传统 for 循环）</td><td style="text-align:center"><code>userList</code><strong>.fori</strong></td><td style="text-align:center"><code>for (int i = 0; i &lt; userList.size(); i++) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.forr</code></td><td style="text-align:center">使用索引<strong>反向</strong>遍历数组或 <code>List</code></td><td style="text-align:center"><code>userList</code><strong>.forr</strong></td><td style="text-align:center"><code>for (int i = userList.size() - 1; i &gt;= 0; i--) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"><strong>条件与分支</strong></td><td style="text-align:center"><code>.if</code></td><td style="text-align:center">基于布尔表达式生成 <code>if</code> 语句</td><td style="text-align:center"><code>user.isActive()</code><strong>.if</strong></td><td style="text-align:center"><code>if (user.isActive()) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.else</code></td><td style="text-align:center">基于布尔表达式生成 <code>if (!expr)</code> 语句</td><td style="text-align:center"><code>list.isEmpty()</code><strong>.else</strong></td><td style="text-align:center"><code>if (!list.isEmpty()) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.switch</code></td><td style="text-align:center">为表达式生成 <code>switch</code> 语句</td><td style="text-align:center"><code>user.getRole()</code><strong>.switch</strong></td><td style="text-align:center"><code>switch (user.getRole()) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"><strong>空值检查</strong></td><td style="text-align:center"><code>.null</code></td><td style="text-align:center">生成 <code>if (expr == null)</code> 检查</td><td style="text-align:center"><code>currentUser</code><strong>.null</strong></td><td style="text-align:center"><code>if (currentUser == null) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.notnull</code> / <code>.nn</code></td><td style="text-align:center">生成 <code>if (expr != null)</code> 检查</td><td style="text-align:center"><code>currentUser</code><strong>.notnull</strong></td><td style="text-align:center"><code>if (currentUser != null) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"><strong>类型操作</strong></td><td style="text-align:center"><code>.cast</code></td><td style="text-align:center">将对象强制类型转换</td><td style="text-align:center"><code>view</code><strong>.cast</strong></td><td style="text-align:center"><code>((Type) view)</code> <br/>(光标在 Type 位置)</td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.instanceof</code></td><td style="text-align:center">生成 <code>instanceof</code> 类型检查</td><td style="text-align:center"><code>obj</code><strong>.instanceof</strong></td><td style="text-align:center"><code>if (obj instanceof Type) &#123; ... &#125;</code> <br/>(光标在 Type 位置)</td></tr><tr><td style="text-align:center"><strong>代码包裹</strong></td><td style="text-align:center"><code>.try</code></td><td style="text-align:center">将语句包裹在 <code>try-catch</code> 块中</td><td style="text-align:center"><code>someRiskyOperation()</code><strong>.try</strong></td><td style="text-align:center"><code>try &#123; someRiskyOperation(); &#125; catch (Exception e) &#123; e.printStackTrace(); &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.synchronized</code></td><td style="text-align:center">将语句包裹在 <code>synchronized</code> 块中</td><td style="text-align:center"><code>this</code><strong>.synchronized</strong></td><td style="text-align:center"><code>synchronized (this) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.sout</code></td><td style="text-align:center">用 <code>System.out.println()</code> 打印表达式</td><td style="text-align:center"><code>&quot;Hello&quot;</code><strong>.sout</strong></td><td style="text-align:center"><code>System.out.println(&quot;Hello&quot;);</code></td></tr><tr><td style="text-align:center"><strong>返回与抛出</strong></td><td style="text-align:center"><code>.return</code></td><td style="text-align:center">从方法返回表达式的值</td><td style="text-align:center"><code>buildUser()</code><strong>.return</strong></td><td style="text-align:center"><code>return buildUser();</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.throw</code></td><td style="text-align:center">抛出一个 <code>Throwable</code> 异常</td><td style="text-align:center"><code>new Exception()</code><strong>.throw</strong></td><td style="text-align:center"><code>throw new Exception();</code></td></tr><tr><td style="text-align:center"><strong>Android 专用</strong></td><td style="text-align:center"><code>.toast</code></td><td style="text-align:center">显示一个 Toast 提示</td><td style="text-align:center"><code>&quot;Login failed&quot;</code><strong>.toast</strong></td><td style="text-align:center"><code>Toast.makeText(context, &quot;Login failed&quot;, Toast.LENGTH_SHORT).show();</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.logd</code></td><td style="text-align:center">用 <code>Log.d</code> 打印 Debug 级别日志</td><td style="text-align:center"><code>message</code><strong>.logd</strong></td><td style="text-align:center"><code>Log.d(TAG, message);</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.loge</code></td><td style="text-align:center">用 <code>Log.e</code> 打印 Error 级别日志</td><td style="text-align:center"><code>errorMessage</code><strong>.loge</strong></td><td style="text-align:center"><code>Log.e(TAG, &quot;errorMessage: &quot;, errorMessage);</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.logi</code> / <code>.logw</code></td><td style="text-align:center">用 <code>Log.i</code> / <code>Log.w</code> 打印日志</td><td style="text-align:center"><code>info</code><strong>.logi</strong></td><td style="text-align:center"><code>Log.i(TAG, info);</code></td></tr></tbody></table><h2 id="kotlin-后缀补全用法大全"><a class="markdownIt-Anchor" href="#kotlin-后缀补全用法大全"></a> <strong>Kotlin 后缀补全用法大全</strong></h2><p>这份表格适用于在 <code>.kt</code> 文件中进行现代 Android 开发,充分利用了 Kotlin 的语言特性.</p><table><thead><tr><th style="text-align:center">分类</th><th style="text-align:center">模板</th><th style="text-align:center">说明</th><th style="text-align:center">输入示例</th><th style="text-align:center">生成代码</th></tr></thead><tbody><tr><td style="text-align:center"><strong>变量声明</strong></td><td style="text-align:center"><code>.val</code></td><td style="text-align:center">创建一个不可变引用 (<code>val</code>)</td><td style="text-align:center"><code>User(&quot;guest&quot;)</code><strong>.val</strong></td><td style="text-align:center"><code>val user = User(&quot;guest&quot;)</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.var</code></td><td style="text-align:center">创建一个可变引用 (<code>var</code>)</td><td style="text-align:center"><code>0</code><strong>.var</strong></td><td style="text-align:center"><code>var i = 0</code></td></tr><tr><td style="text-align:center"><strong>空安全与<br>作用域函数</strong></td><td style="text-align:center"><code>.null</code></td><td style="text-align:center">检查表达式是否为 <code>null</code></td><td style="text-align:center"><code>user</code><strong>.null</strong></td><td style="text-align:center"><code>if (user == null) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.notnull</code></td><td style="text-align:center">检查表达式是否不为 <code>null</code></td><td style="text-align:center"><code>user</code><strong>.notnull</strong></td><td style="text-align:center"><code>if (user != null) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><strong><code>.let</code></strong></td><td style="text-align:center">安全调用,将非空对象作为 <code>it</code> 传入 lambda</td><td style="text-align:center"><code>user?.name</code><strong>.let</strong></td><td style="text-align:center"><code>user?.name?.let &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><strong><code>.apply</code></strong></td><td style="text-align:center">安全调用,将非空对象作为 <code>this</code> 配置.返回对象本身</td><td style="text-align:center"><code>TextView(this)</code><strong>.apply</strong></td><td style="text-align:center"><code>TextView(this).apply &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><strong><code>.also</code></strong></td><td style="text-align:center">安全调用,将非空对象作为 <code>it</code> 执行附加操作.返回对象本身</td><td style="text-align:center"><code>user</code><strong>.also</strong></td><td style="text-align:center"><code>user.also &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><strong><code>.run</code></strong></td><td style="text-align:center">安全调用，将非空对象作为 <code>this</code> 执行 lambda。返回 lambda 结果</td><td style="text-align:center"><code>user?.address</code><strong>.run</strong></td><td style="text-align:center"><code>user?.address?.run &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.with</code></td><td style="text-align:center">使用 <code>with(expr) &#123; ... &#125;</code> 包裹代码块</td><td style="text-align:center"><code>user</code><strong>.with</strong></td><td style="text-align:center"><code>with(user) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"><strong>循环</strong></td><td style="text-align:center"><code>.for</code> / <code>.iter</code></td><td style="text-align:center">遍历一个 <code>Iterable</code> 对象</td><td style="text-align:center"><code>userList</code><strong>.for</strong></td><td style="text-align:center"><code>for (user in userList) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.fori</code></td><td style="text-align:center">带索引遍历</td><td style="text-align:center"><code>list</code><strong>.fori</strong></td><td style="text-align:center"><code>for (i in list.indices) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.forr</code></td><td style="text-align:center">带索引反向遍历</td><td style="text-align:center"><code>list</code><strong>.forr</strong></td><td style="text-align:center"><code>for (i in list.indices.reversed()) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"><strong>条件与分支</strong></td><td style="text-align:center"><code>.if</code></td><td style="text-align:center">基于布尔表达式生成 <code>if</code> 语句</td><td style="text-align:center"><code>user.age &gt; 18</code><strong>.if</strong></td><td style="text-align:center"><code>if (user.age &gt; 18) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.else</code></td><td style="text-align:center">基于布尔表达式生成 <code>if (!expr)</code> 语句</td><td style="text-align:center"><code>list.isEmpty()</code><strong>.else</strong></td><td style="text-align:center"><code>if (!list.isEmpty()) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.when</code></td><td style="text-align:center">为表达式生成 <code>when</code> 分支语句</td><td style="text-align:center"><code>response.code</code><strong>.when</strong></td><td style="text-align:center"><code>when (response.code) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"><strong>类型操作</strong></td><td style="text-align:center"><code>.cast</code></td><td style="text-align:center">智能类型转换并赋值给新变量</td><td style="text-align:center"><code>view</code><strong>.cast</strong></td><td style="text-align:center"><code>val type = view as Type</code> <br/>(光标在 Type 位置)</td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.is</code></td><td style="text-align:center">生成 <code>if (expr is Type)</code> 类型检查</td><td style="text-align:center"><code>animal</code><strong>.is</strong></td><td style="text-align:center"><code>if (animal is Dog) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.notis</code></td><td style="text-align:center">生成 <code>if (expr !is Type)</code> 类型检查</td><td style="text-align:center"><code>animal</code><strong>.notis</strong></td><td style="text-align:center"><code>if (animal !is Cat) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"><strong>代码包裹</strong></td><td style="text-align:center"><code>.try</code></td><td style="text-align:center">将语句包裹在 <code>try-catch</code> 块中</td><td style="text-align:center"><code>readFile()</code><strong>.try</strong></td><td style="text-align:center"><code>try &#123; readFile() &#125; catch (e: Exception) &#123; ... &#125;</code></td></tr><tr><td style="text-align:center"></td><td style="text-align:center"><code>.sout</code></td><td style="text-align:center">用 <code>println()</code> 打印表达式</td><td style="text-align:center"><code>&quot;Hello Kotlin&quot;</code><strong>.sout</strong></td><td style="text-align:center"><code>println(&quot;Hello Kotlin&quot;)</code></td></tr><tr><td style="text-align:center"><strong>返回与抛出</strong></td><td style="text-align:center"><code>.return</code></td><td style="text-align:center">从函数返回表达式的值</td><td style="text-align:center"><code>buildResult()</code><strong>.return</strong></td><td style="text-align:center"><code>return buildResult()</code></td></tr></tbody></table><hr /><h1 id="数组"><a class="markdownIt-Anchor" href="#数组"></a> 数组</h1><h2 id="一维数组"><a class="markdownIt-Anchor" href="#一维数组"></a> 一维数组</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 快捷输入</span></span><br><span class="line"><span class="keyword">new</span> <span class="title class_">int</span>[<span class="number">7</span>].<span class="keyword">var</span></span><br><span class="line">    </span><br><span class="line"><span class="type">int</span>[] a = <span class="keyword">new</span> <span class="title class_">int</span>[<span class="number">7</span>];</span><br></pre></td></tr></table></figure><h2 id="二维数组"><a class="markdownIt-Anchor" href="#二维数组"></a> 二维数组</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 快捷输入</span></span><br><span class="line"><span class="keyword">new</span> <span class="title class_">String</span>[][].<span class="keyword">var</span></span><br><span class="line">    </span><br><span class="line">String[][] str = <span class="keyword">new</span> <span class="title class_">String</span>[][]&#123;&#123;<span class="string">&quot;你好&quot;</span>,<span class="string">&quot;hello&quot;</span>&#125;,&#123;<span class="string">&quot;世界&quot;</span>,<span class="string">&quot;world&quot;</span>&#125;&#125;;</span><br></pre></td></tr></table></figure><hr /><h1 id="面向对象"><a class="markdownIt-Anchor" href="#面向对象"></a> 面向对象</h1><h2 id="对象"><a class="markdownIt-Anchor" href="#对象"></a> 对象</h2><p>对象是一种<strong>存储数据</strong>的特殊的数据结构,里面也可以包含对应的数据代码,new对象前你需要创建一个<code>class</code>类,它作为模版来创建具体的对象.</p><p>1.创建类</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 这是一个“猫”的蓝图 (Class)</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Cat</span> &#123;</span><br><span class="line">    <span class="comment">// 状态 (属性)</span></span><br><span class="line">    String name; <span class="comment">// 名字</span></span><br><span class="line">    <span class="type">int</span> age;     <span class="comment">// 年龄</span></span><br><span class="line">    String color; <span class="comment">// 颜色</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 行为 (方法)</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">meow</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot; says: 喵喵~&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">eat</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot; 正在吃东西...&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 对象将自己的数据（状态）和操作数据的代码（行为）打包在一起，形成一个独立的单元,也体现了封装的思想</span></span><br></pre></td></tr></table></figure><ol start="2"><li>new对象</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 创建第一个猫对象，名叫“小白”</span></span><br><span class="line">        <span class="type">Cat</span> <span class="variable">cat1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Cat</span>(); <span class="comment">// new Cat() 就是在创建对象</span></span><br><span class="line">        cat1.name = <span class="string">&quot;小白&quot;</span>;</span><br><span class="line">        cat1.age = <span class="number">2</span>;</span><br><span class="line">        cat1.color = <span class="string">&quot;白色&quot;</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建第二个猫对象，名叫“阿橘”</span></span><br><span class="line">        <span class="type">Cat</span> <span class="variable">cat2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Cat</span>();</span><br><span class="line">        cat2.name = <span class="string">&quot;阿橘&quot;</span>;</span><br><span class="line">        cat2.age = <span class="number">3</span>;</span><br><span class="line">        cat2.color = <span class="string">&quot;橘色&quot;</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// cat1 和 cat2 是两个完全独立的对象，尽管它们都来自同一个Cat类</span></span><br><span class="line">        <span class="comment">// 它们有各自的状态（名字、年龄等）</span></span><br><span class="line">        System.out.println(cat1.name); <span class="comment">// 输出: 小白</span></span><br><span class="line">        System.out.println(cat2.name); <span class="comment">// 输出: 阿橘</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 我们可以让它们执行自己的行为</span></span><br><span class="line">        cat1.meow(); <span class="comment">// 输出: 小白 says: 喵喵~</span></span><br><span class="line">        cat2.eat();  <span class="comment">// 输出: 阿橘 正在吃东西...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="构造器"><a class="markdownIt-Anchor" href="#构造器"></a> 构造器</h2><p>构造器（也常被称为构造方法、构造函数）是类中的一个特殊方法.它的<strong>唯一目的</strong>就是在创建一个类的对象时,对这个新创建的对象进行<strong>初始化</strong>.</p><p>我们可以把它想象成一个“生产车间”里的“初始化工序”.当使用 <code>new</code> 关键字生产一个新对象时,这个初始化工具立刻被调用,为对象的属性（成员变量）赋予初始值.</p><p><em>特点：</em></p><ol><li><strong>名称必须与类名完全相同</strong>：这是识别构造器的硬性标准.</li><li><strong>没有返回类型</strong>：它连 <code>void</code> 都没有,因为它“返回”的其实是那个被初始化好的对象实例的引用,这个过程是隐式的.</li><li><strong>不能被直接调用</strong>：你不能像调用普通方法那样（例如 <code>myCat.constructor()</code>) 来调用构造器.它只能在创建对象时,<em>由 <code>new</code> 关键字自动调用.</em></li></ol><p><em>应用场景：</em></p><p>完成对象的成员变量的初始化赋值.</p><h3 id="无参构造器"><a class="markdownIt-Anchor" href="#无参构造器"></a> 无参构造器</h3><p>这是最简单的构造器,它不接受任何参数.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Dog</span> &#123;</span><br><span class="line">    String name;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 这是一个无参构造器</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Dog</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;一只小狗被创建了！&quot;</span>);</span><br><span class="line">        <span class="built_in">this</span>.name = <span class="string">&quot;旺财&quot;</span>; <span class="comment">// 可以在这里设置默认值</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 当执行 new Dog() 时，上面的无参构造器会被自动调用</span></span><br><span class="line">        <span class="type">Dog</span> <span class="variable">dog1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Dog</span>(); <span class="comment">// 控制台会输出 &quot;一只小狗被创建了！&quot;</span></span><br><span class="line">        System.out.println(dog1.name); <span class="comment">// 输出 &quot;旺财&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="有参构造器"><a class="markdownIt-Anchor" href="#有参构造器"></a> 有参构造器</h3><p>这种构造器接受一个或多个参数，使得我们可以在创建对象时就传递初始数据，实现更灵活的初始化。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Cat</span> &#123;</span><br><span class="line">    String name; <span class="comment">// 成员变量</span></span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 这是一个有参构造器，接受名字和年龄</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Cat</span><span class="params">(String name, <span class="type">int</span> age)</span> &#123; <span class="comment">// 局部变量</span></span><br><span class="line">        System.out.println(<span class="string">&quot;正在创建一只名叫 &quot;</span> + name + <span class="string">&quot; 的猫...&quot;</span>);</span><br><span class="line">        <span class="built_in">this</span>.name = name; <span class="comment">// 使用 this 关键字区分成员变量和参数</span></span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 创建对象时，必须提供匹配的参数</span></span><br><span class="line">        <span class="type">Cat</span> <span class="variable">cat1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Cat</span>(<span class="string">&quot;咪咪&quot;</span>, <span class="number">2</span>); <span class="comment">// &quot;咪咪&quot;和 2 会被传递给构造器</span></span><br><span class="line">        System.out.println(<span class="string">&quot;猫的名字是: &quot;</span> + cat1.name); <span class="comment">// 输出 &quot;猫的名字是: 咪咪&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><em>注意:</em></p><ol><li>类默认带了一个无参构造器.</li><li>如果定义了个有参构造器,默认的无参构造器会消失,需要手动定义一个.</li></ol><h3 id="通过构造器传递对象"><a class="markdownIt-Anchor" href="#通过构造器传递对象"></a> 通过构造器传递对象</h3><p>将通过构造函数参数传递进来的 <code>Movie</code> 对象,赋值给当前 <code>MovieOpearator</code> 实例的成员变量 <code>movie</code>,再调用构造方法输出数据.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Movie 类：定义了电影的基本属性。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Movie</span> &#123;</span><br><span class="line">    String title; <span class="comment">// 电影标题</span></span><br><span class="line">    String genre; <span class="comment">// 电影类型</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构造器：用于创建并初始化一个 Movie 对象</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Movie</span><span class="params">(String title, String genre)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.title = title;</span><br><span class="line">        <span class="built_in">this</span>.genre = genre;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getTitle</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.title;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getGenre</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.genre;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * MovieOperator 类：用于对一个 Movie 对象进行操作。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MovieOperator</span> &#123;</span><br><span class="line">    <span class="comment">// 持有一个私有的 Movie 实例</span></span><br><span class="line">    <span class="keyword">private</span> Movie movie;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构造器：通过构造器注入所依赖的 Movie 对象</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MovieOperator</span><span class="params">(Movie movie)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.movie = movie;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setMovie</span><span class="params">(Movie movie)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.movie = movie;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 显示当前电影的详细信息</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">displayMovieDetails</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;--- 电影详情 ---&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;片名: &quot;</span> + movie.getTitle());</span><br><span class="line">        System.out.println(<span class="string">&quot;类型: &quot;</span> + movie.getGenre());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Main 类：程序的执行入口。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 创建一个 Movie 对象</span></span><br><span class="line">        <span class="type">Movie</span> <span class="variable">inception</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Movie</span>(<span class="string">&quot;盗梦空间&quot;</span>, <span class="string">&quot;科幻/悬疑&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 创建 Operator 并注入 Movie 对象</span></span><br><span class="line">        <span class="type">MovieOperator</span> <span class="variable">operator</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MovieOperator</span>(inception);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 调用方法，执行操作</span></span><br><span class="line">        operator.displayMovieDetails();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="this关键字"><a class="markdownIt-Anchor" href="#this关键字"></a> this关键字</h2><p><code>this</code> 是一个关键字,也是一个引用,在任何方法或构造器内部,它都<strong>指向调用该方法或构造器的当前对象</strong>.</p><p><em>特点：</em></p><p>哪个对象调用带有<code>this</code>的方法,<code>this</code>就拿到哪个对象.</p><p><em>应用场景:</em></p><p><strong>1. 区分成员变量和局部变量（最常用的功能）</strong></p><p>当方法的参数名或局部变量名与类的成员变量名相同时,<code>this</code> 可以用来明确地指代<strong>成员变量</strong>.</p><p><strong>场景示例：</strong> 在一个构造器或 Setter 方法中,我们通常希望参数名能清晰地描述其含义,所以会把它起得和成员变量名一样.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> &#123;</span><br><span class="line">    <span class="comment">// 成员变量 (Instance Variable)</span></span><br><span class="line">    String name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构造器</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Student</span><span class="params">(String name, <span class="type">int</span> age)</span> &#123;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 正确的写法：使用 this 关键字</span></span><br><span class="line">        <span class="comment">// this.name 指的是当前Student对象的成员变量name</span></span><br><span class="line">        <span class="comment">// = 右边的 name 指的是传入的参数name</span></span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setName</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="comment">// 同理，在setter方法中也常用</span></span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">introduce</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 在没有歧义的情况下，this可以省略</span></span><br><span class="line">        <span class="comment">// 下面这行代码等价于 System.out.println(&quot;我是&quot; + this.name);</span></span><br><span class="line">        System.out.println(<span class="string">&quot;我是&quot;</span> + name);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Student</span> <span class="variable">zhangsan</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Student</span>(<span class="string">&quot;张三&quot;</span>, <span class="number">20</span>);</span><br><span class="line">        System.out.println(zhangsan.name); <span class="comment">// 输出: 张三</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>小结：<code>this.成员变量 = 参数;</code> 是 <code>this</code> 最核心、最频繁的用法.</p><hr /><p><strong>2.在构造器中调用本类的其他构造器</strong></p><p>为了避免在重载的构造器中编写重复的初始化代码,可以使用 <code>this(...)</code> 来调用本类中的另一个构造器.</p><p><em>规则：</em></p><ul><li><code>this(...)</code> 必须是构造器中的<strong>第一条可执行语句</strong>.</li><li>只能在构造器中使用.</li></ul><p><strong>场景示例：</strong> 假设我们有一个 <code>Rectangle</code> 类,希望提供多种创建方式.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Rectangle</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> width;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> height;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. &quot;全功能&quot;构造器，负责实际的初始化工作</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Rectangle</span><span class="params">(<span class="type">int</span> width, <span class="type">int</span> height)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.width = width;</span><br><span class="line">        <span class="built_in">this</span>.height = height;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 创建一个正方形的构造器</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Rectangle</span><span class="params">(<span class="type">int</span> sideLength)</span> &#123;</span><br><span class="line">        <span class="comment">// 调用上面的双参数构造器，传入相同的边长</span></span><br><span class="line">        <span class="built_in">this</span>(sideLength, sideLength);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 无参构造器，创建一个默认大小的矩形</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Rectangle</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 调用上面的双参数构造器，传入默认值</span></span><br><span class="line">        <span class="built_in">this</span>(<span class="number">10</span>, <span class="number">5</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">printInfo</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;宽度: &quot;</span> + width + <span class="string">&quot;, 高度: &quot;</span> + height);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Rectangle</span> <span class="variable">r1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Rectangle</span>(); <span class="comment">// 调用无参构造器</span></span><br><span class="line">        r1.printInfo(); <span class="comment">// 输出: 宽度: 10, 高度: 5</span></span><br><span class="line"></span><br><span class="line">        <span class="type">Rectangle</span> <span class="variable">r2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Rectangle</span>(<span class="number">20</span>); <span class="comment">// 调用单参数构造器</span></span><br><span class="line">        r2.printInfo(); <span class="comment">// 输出: 宽度: 20, 高度: 20</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>小结：通过 <code>this(...)</code> 实现构造器链，可以有效减少代码冗余。</p><hr /><p><strong>3.返回当前对象的引用</strong></p><p>在方法中可以使用 <code>return this;</code> 来返回调用该方法的对象本身,这种用法常用于实现<strong>链式调用（Method Chaining）</strong>,让代码写起来更流畅,常见于构建器模式.</p><p>设计一个简单的计算器,可以连续进行操作.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Calculator</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Calculator <span class="title function_">add</span><span class="params">(<span class="type">int</span> num)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.result += num;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>; <span class="comment">// 返回计算器对象本身</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> Calculator <span class="title function_">subtract</span><span class="params">(<span class="type">int</span> num)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.result -= num;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>; <span class="comment">// 返回计算器对象本身</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">printResult</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;最终结果是: &quot;</span> + <span class="built_in">this</span>.result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Calculator</span> <span class="variable">calc</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Calculator</span>();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 因为每个方法都返回了 calc 对象自身，所以可以连续调用</span></span><br><span class="line">        calc.add(<span class="number">10</span>).add(<span class="number">5</span>).subtract(<span class="number">3</span>);</span><br><span class="line"></span><br><span class="line">        calc.printResult(); <span class="comment">// 输出: 最终结果是: 12</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>小结：<code>return this;</code> 是实现链式编程的关键.</p><hr /><p><strong>4. 将当前对象作为参数传递</strong></p><p>如果一个方法需要接收一个当前类的对象作为参数,可以在调用时直接传递 <code>this</code>.</p><p><strong>场景示例：</strong> 一个事件监听器需要把自己注册到事件源中.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 事件源</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">EventSource</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">registerListener</span><span class="params">(EventListener listener)</span> &#123;</span><br><span class="line">        listener.onEventHappened();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 事件监听器</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">EventListener</span> &#123;</span><br><span class="line">    <span class="comment">// 这个方法把自己注册到事件源</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">startListening</span><span class="params">(EventSource source)</span> &#123;</span><br><span class="line">        <span class="comment">// 把“我”这个EventListener对象传给source</span></span><br><span class="line">        source.registerListener(<span class="built_in">this</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onEventHappened</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;事件发生了，我（监听器）收到了！&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">EventSource</span> <span class="variable">source</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">EventSource</span>();</span><br><span class="line">        <span class="type">EventListener</span> <span class="variable">listener</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">EventListener</span>();</span><br><span class="line">        listener.startListening(source); <span class="comment">// 监听器把自己注册进去</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><table><thead><tr><th>用法</th><th>语法</th><th>目的</th></tr></thead><tbody><tr><td><strong>区分变量</strong></td><td><code>this.variable</code></td><td>明确指出要访问的是类的成员变量,而非同名局部变量.</td></tr><tr><td><strong>调用构造器</strong></td><td><code>this(...)</code></td><td>在一个构造器中调用本类的另一个构造器,减少代码重复.</td></tr><tr><td><strong>返回当前对象</strong></td><td><code>return this;</code></td><td>从方法中返回当前对象实例,以支持链式调用.</td></tr><tr><td><strong>传递当前对象</strong></td><td><code>someMethod(this)</code></td><td>在方法调用中,将当前对象作为参数传递给另一个方法.</td></tr></tbody></table><hr /><h2 id="封装"><a class="markdownIt-Anchor" href="#封装"></a> 封装</h2><p>封装是指将对象的<strong>数据（属性）和操作数据的代码（方法）捆绑成一个独立的单元（即类）,并对对象的内部细节进行信息隐藏</strong>,外部世界只能通过该对象允许的公开接口来访问它,而不能直接访问其内部的私有数据.</p><p><strong>设计要求:合理隐藏,合理暴露</strong></p><p>在 Java 中，实现封装通常遵循以下三个步骤：</p><ol><li><strong>将属性设为私有（private）</strong>：使用 <code>private</code> 关键字修饰类的成员变量,这样它们就不能在类的外部被直接访问.</li><li><strong>提供公有的 Getter 方法</strong>：创建一个 <code>public</code> 的方法,用于读取某个私有属性的值（例如 <code>getName()</code>）.</li><li><strong>提供公有的 Setter 方法</strong>：创建一个 <code>public</code> 的方法,用于设置某个私有属性的值（例如 <code>setName(String newName)</code>）,最关键的是,我们可以在这个方法中加入控制和验证逻辑.</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="comment">// 1. 将属性设为私有</span></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 提供公有的 Getter 方法，用于读取 name</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getName</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 提供公有的 Setter 方法，用于设置 name</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setName</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 提供公有的 Getter 方法，用于读取 age</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getAge</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">this</span>.age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 提供公有的 Setter 方法，用于设置 age</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setAge</span><span class="params">(<span class="type">int</span> age)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (age &gt; <span class="number">0</span> &amp;&amp; age &lt; <span class="number">130</span>) &#123; <span class="comment">// 只接受合法的年龄</span></span><br><span class="line">            <span class="built_in">this</span>.age = age;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;错误：年龄值不合法！&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Person</span> <span class="variable">p</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Person</span>();</span><br><span class="line">        p.setName(<span class="string">&quot;李四&quot;</span>);</span><br><span class="line">        p.setAge(<span class="number">25</span>); <span class="comment">// 合法值，设置成功</span></span><br><span class="line">        System.out.println(p.getName() + <span class="string">&quot;的年龄是 &quot;</span> + p.getAge()); <span class="comment">// 输出: 李四的年龄是 25</span></span><br><span class="line"></span><br><span class="line">        p.setAge(-<span class="number">30</span>); <span class="comment">// 非法值，设置失败</span></span><br><span class="line">        System.out.println(p.getName() + <span class="string">&quot;的年龄是 &quot;</span> + p.getAge()); <span class="comment">// 输出: 错误：年龄值不合法！                       </span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><em>注意: <code>get</code>和<code>set</code>方法可以通过右键里面的<code>generate</code>生成.</em></p><hr /><h2 id="实体类javabean"><a class="markdownIt-Anchor" href="#实体类javabean"></a> 实体类/Javabean</h2><p>它是一种特殊的技术规范或设计模式,可以把它想象成一个标准化的,自包含的数据封装容器,它的严格规范使得各种工具和框架都能自动地发现,使用和管理它的属性.</p><p><em>特点:</em></p><ol><li><strong>公有的无参构造器</strong>：必须提供一个 <code>public</code> 的、不带任何参数的构造函数.这是为了让很多框架（如 Spring, JSP）能够轻松地通过反射来创建它的实例.</li><li><strong>私有的成员变量</strong>：所有属性（fields）都应该是 <code>private</code> 的,这体现了<strong>封装</strong>的原则.</li><li><strong>公有的 Getter 和 Setter 方法</strong>：对每一个私有属性,都提供 <code>public</code> 的 <code>getXxx()</code> 和 <code>setXxx()</code> 方法用于读取和写入.对于布尔类型的属性,getter 方法可以是 <code>isXxx()</code>.</li><li><strong>可序列化（可选但推荐）</strong>：实现 <code>java.io.Serializable</code> 接口,这使得 JavaBean 对象可以在网络上传输或持久化到文件中.</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 这是一个标准的 JavaBean</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserBean</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 私有属性</span></span><br><span class="line">    <span class="keyword">private</span> String userName;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 公有的无参构造器</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">UserBean</span><span class="params">()</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 公有的 Getter 和 Setter 方法</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getUserName</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> userName;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setUserName</span><span class="params">(String userName)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.userName = userName;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getAge</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setAge</span><span class="params">(<span class="type">int</span> age)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="static-静态变量方法"><a class="markdownIt-Anchor" href="#static-静态变量方法"></a> static-静态变量/方法</h2><p><code>static</code> 关键字的核心思想是：<strong>被 <code>static</code> 修饰的成员（变量或方法）属于整个类,而不是属于某个具体的对象实例.</strong></p><h3 id="static修饰成员变量"><a class="markdownIt-Anchor" href="#static修饰成员变量"></a> static修饰成员变量</h3><p>被<code>static</code>修饰的成员变量我们称为静态变量或者类变量,在计算机中只有一份,会被类的全部对象共享.</p><p><em>特点:</em></p><p>当一个变量被 <code>static</code> 修饰后，它就不再是对象的属性,而是类的属性.</p><ul><li><strong>内存分配</strong>：<code>static</code> 变量在类被加载到内存时就分配了空间,并且只分配一次,无论创建了多少个对象,静态变量在内存中都只有一个副本.</li><li><strong>调用方式</strong>：推荐直接使用 <strong><code>类名.静态变量</code></strong> 的方式调用,虽然也可以用 <code>对象名.静态变量</code> 调用,但这会引起混淆,不推荐.</li></ul><p><em>应用场景</em></p><p>在开发中,如果某个数据只需要一份,且希望能够被共享(访问,修改),则该数据可以定义成类变量来记住.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span> &#123;</span><br><span class="line">    <span class="comment">// 实例变量：每个学生对象都有一份</span></span><br><span class="line">    String name;</span><br><span class="line">    <span class="type">int</span> studentId;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 静态变量：所有Student对象共享</span></span><br><span class="line">    <span class="keyword">static</span> <span class="type">String</span> <span class="variable">university</span> <span class="operator">=</span> <span class="string">&quot;北京大学&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 直接通过类名访问静态变量</span></span><br><span class="line">        System.out.println(<span class="string">&quot;学校是：&quot;</span> + Student.university);</span><br><span class="line"></span><br><span class="line">        <span class="type">Student</span> <span class="variable">s1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Student</span>();</span><br><span class="line">        s1.name = <span class="string">&quot;张三&quot;</span>;</span><br><span class="line"></span><br><span class="line">        <span class="type">Student</span> <span class="variable">s2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Student</span>();</span><br><span class="line">        s2.name = <span class="string">&quot;李四&quot;</span>;</span><br><span class="line"></span><br><span class="line">        System.out.println(s1.name); <span class="comment">// 输出: 张三</span></span><br><span class="line">        System.out.println(s2.name); <span class="comment">// 输出: 李四</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 所有对象共享同一个静态变量</span></span><br><span class="line">        System.out.println(s1.university); <span class="comment">// 输出: 北京大学</span></span><br><span class="line">        </span><br><span class="line">        System.out.println(s2.university); <span class="comment">// 输出: 北京大学</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 修改静态变量会影响所有地方</span></span><br><span class="line">        Student.university = <span class="string">&quot;清华大学&quot;</span>;</span><br><span class="line">        System.out.println(<span class="string">&quot;s1的学校变为：&quot;</span> + s1.university); <span class="comment">// 输出: s1的学校变为：清华大学</span></span><br><span class="line">        System.out.println(<span class="string">&quot;s2的学校变为：&quot;</span> + s2.university); <span class="comment">// 输出: s2的学校变为：清华大学</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h3 id="static修饰成员方法"><a class="markdownIt-Anchor" href="#static修饰成员方法"></a> static修饰成员方法</h3><p>与静态变量类似,静态方法是属于整个类的方法,而不是属于某个具体对象实例的方法.可以提高代码复用性与运行效率,不用创建对象,调用方便.</p><p><em>规范:</em></p><p>如果这个方法只是为了做一个功能且不需要直接访问,这个方法直接定义成静态方法.</p><p><em>特点:</em></p><ul><li>静态方法内部不能直接调用非静态方法或访问非静态变量.</li><li>不能使用 <code>this</code> 或 <code>super</code> 关键字.</li></ul><p><em>应用场景</em></p><ul><li>自定义工具类**(记得私有化构造器)**</li><li>工厂方法</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 字符串处理工具类</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">StringUtils</span> &#123;</span><br><span class="line">    <span class="comment">// 私有化构造器，防止被实例化</span></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">StringUtils</span><span class="params">()</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 判断字符串是否为空或null</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="type">boolean</span> <span class="title function_">isBlank</span><span class="params">(String str)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> str == <span class="literal">null</span> || str.trim().isEmpty();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将字符串首字母大写</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> String <span class="title function_">capitalize</span><span class="params">(String str)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (isBlank(str)) &#123;</span><br><span class="line">            <span class="keyword">return</span> str;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> Character.toUpperCase(str.charAt(<span class="number">0</span>)) + str.substring(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line"><span class="keyword">if</span> (StringUtils.isBlank(userInput)) &#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;输入不能为空！&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 产品接口</span></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">draw</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 具体产品类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Circle</span> <span class="keyword">implements</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">draw</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;画了一个圆形 ○&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Rectangle</span> <span class="keyword">implements</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">draw</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;画了一个矩形 □&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 工厂类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">ShapeFactory</span> &#123;</span><br><span class="line">    <span class="comment">// 提供一个公开的静态方法来获取Shape对象</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Shape <span class="title function_">getShape</span><span class="params">(String shapeType)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (shapeType == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (shapeType.equalsIgnoreCase(<span class="string">&quot;CIRCLE&quot;</span>)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Circle</span>();</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (shapeType.equalsIgnoreCase(<span class="string">&quot;RECTANGLE&quot;</span>)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Rectangle</span>();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 客户端不需要知道 Circle 或 Rectangle 的存在，只和工厂打交道</span></span><br><span class="line">        <span class="type">Shape</span> <span class="variable">shape1</span> <span class="operator">=</span> ShapeFactory.getShape(<span class="string">&quot;CIRCLE&quot;</span>);</span><br><span class="line">        shape1.draw(); <span class="comment">// 输出: 画了一个圆形 ○</span></span><br><span class="line"></span><br><span class="line">        <span class="type">Shape</span> <span class="variable">shape2</span> <span class="operator">=</span> ShapeFactory.getShape(<span class="string">&quot;RECTANGLE&quot;</span>);</span><br><span class="line">        shape2.draw(); <span class="comment">// 输出: 画了一个矩形 □</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><em>注意:</em></p><ul><li>静态方法中可以直接访问静态成员,不可以直接访问实例成员(但可以在里面创建对象实现间接访问).</li><li>实例方法中既可以直接访问静态成员,也可以直接访问实例成员.</li><li>实例方法中可以出现<code>this</code>和<code>super</code>关键字,静态方法中不能出现(<code>this</code>代表的是对象,<code>super</code>代表的是父类).</li></ul><hr /><h2 id="权限修饰符"><a class="markdownIt-Anchor" href="#权限修饰符"></a> 权限修饰符</h2><p>这张表格可以清晰地展示四个修饰符的访问权限范围：</p><table><thead><tr><th>修饰符</th><th>同一个类</th><th>同一个包</th><th>不同包的子类</th><th>任何地方</th></tr></thead><tbody><tr><td><strong><code>private</code></strong></td><td>✅</td><td>❌</td><td>❌</td><td>❌</td></tr><tr><td><strong><code>default</code></strong></td><td>✅</td><td>✅</td><td>❌</td><td>❌</td></tr><tr><td><strong><code>protected</code></strong></td><td>✅</td><td>✅</td><td>✅</td><td>❌</td></tr><tr><td><strong><code>public</code></strong></td><td>✅</td><td>✅</td><td>✅</td><td>✅</td></tr></tbody></table><p><em>注意:</em></p><p>一般成员变量使用<code>private</code>,方法和函数使用<code>public</code>就可以了.</p><hr /><h2 id="继承"><a class="markdownIt-Anchor" href="#继承"></a> 继承</h2><p>继承的概念与现实世界中的生物遗传非常相似.</p><p>子女会从父母那里继承一些特征,比如眼睛的颜色、身高.同时,子女也会有自己独特的特征,比如一项新的爱好或技能.</p><p>在编程中：</p><ul><li><strong>子类可以从父类（基类）那里“继承”来属性（成员变量）和行为（方法）</strong>.</li><li>子类不仅拥有父类的所有非私有成员,还可以添加自己独有的新属性和新方法,或者**重写（Override）**从父类继承来的方法,使其表现出不同的行为.</li></ul><p><em>特点</em></p><ul><li><code>Java</code>是单继承模式,不支持多继承.</li><li><code>Java</code>中所有的类都是<code>Object</code>的子类.</li><li>就近原则:会优先访问自己类中的局部变量,其次用<code>this</code>访问成员变量,访问父类的变量要用<code>super</code>.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 父类 Animal</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    String name;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Animal</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        System.out.println(<span class="string">&quot;Animal 的构造器被调用了！&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">eat</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot; 正在吃东西...&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sleep</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot; 正在睡觉...&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 子类 Dog 继承自 Animal</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Dog</span> <span class="keyword">extends</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Dog 类特有的方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">bark</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(name + <span class="string">&quot; 汪汪叫！&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="访问成员-就进原则"><a class="markdownIt-Anchor" href="#访问成员-就进原则"></a> 访问成员-就进原则</h3><p>“就近原则”指的是,当子类的方法中需要访问一个变量或调用一个方法时,程序会按照一个由近及远的顺序来查找这个成员,一旦找到了就会立刻停止查找并使用它.</p><p>这个查找顺序可以概括为：</p><ol><li><strong>先在局部范围找</strong>：首先在当前方法的局部变量中查找.</li><li><strong>再到当前对象找</strong>：如果在方法内没找到,就在当前子类对象的成员变量中查找.</li><li><strong>最后去父类对象找</strong>：如果在子类中也没找到,就沿着继承关系向上,到直接父类的成员变量中查找.如果父类还有父类,会继续向上,直到 <code>Object</code> 类为止.</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Father</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> <span class="string">&quot;张伟&quot;</span>; <span class="comment">// 成员变量</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sayHello</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;我是父亲，我的名字是 &quot;</span> + name);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Son</span> <span class="keyword">extends</span> <span class="title class_">Father</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> <span class="string">&quot;张小宝&quot;</span>; <span class="comment">// 与父类同名的成员变量</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">showName</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> <span class="string">&quot;临时名&quot;</span>; <span class="comment">// 局部变量</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 场景一：直接访问 name</span></span><br><span class="line">        <span class="comment">// 根据就近原则，首先找到的是方法内的局部变量 &quot;临时名&quot;</span></span><br><span class="line">        System.out.println(name); </span><br><span class="line"></span><br><span class="line">        <span class="comment">/** </span></span><br><span class="line"><span class="comment">         * 场景二：使用 this 访问 name</span></span><br><span class="line"><span class="comment">         * this 关键字代表当前对象（Son对象），所以它会跳过局部变量，直接在当前类的成员变量中查找。</span></span><br><span class="line"><span class="comment">         * 找到了 Son 类中的 &quot;张小宝&quot;</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        System.out.println(<span class="built_in">this</span>.name); </span><br><span class="line"></span><br><span class="line">        <span class="comment">/** </span></span><br><span class="line"><span class="comment">         * 场景三：使用 super 访问 name</span></span><br><span class="line"><span class="comment">         * super 关键字代表父类对象，所以它会跳过局部变量和当前子类成员，直接去父类中查找。</span></span><br><span class="line"><span class="comment">         * 找到了 Father 类中的 &quot;张伟&quot;</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        System.out.println(<span class="built_in">super</span>.name); </span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 重写（Override）父类的 sayHello 方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sayHello</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;我是儿子，我的名字是 &quot;</span> + <span class="built_in">this</span>.name);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>从上面的例子可以看出,<code>this</code> 和 <code>super</code> 是打破默认“就近”查找顺序、进行精确访问的关键工具.</p><ul><li><strong><code>this</code> 关键字</strong><ul><li><strong>含义</strong>：代表当前对象的引用.</li><li><strong>作用</strong>：用来明确访问当前类的成员变量或成员方法,当成员变量和局部变量同名时,必须使用 <code>this</code> 来区分.</li></ul></li><li><strong><code>super</code> 关键字</strong><ul><li><strong>含义</strong>：代表对父类对象的引用.</li><li><strong>作用</strong>：用来明确访问父类的成员变量或成员方法,当子类隐藏了父类的变量或重写了父类的方法后,可以用 <code>super</code> 来访问父类中的版本.</li></ul></li></ul><hr /><h3 id="方法重写"><a class="markdownIt-Anchor" href="#方法重写"></a> 方法重写</h3><p>也称为方法覆盖,指的是<strong>在子类中创建一个与父类中某个方法具有相同方法签名（即方法名和参数列表完全相同）的新方法,从而覆盖掉父类的版本</strong>.</p><p>简单来说,就是父类有一个通用的功能,但子类觉得这个功能的实现方式不适合自己,需要一个“定制版”或“特殊版”,于是子类就重新写了一遍这个功能.</p><p><strong>一个形象的比喻：</strong></p><ul><li><strong>父类 <code>Animal</code></strong> 有一个 <code>move()</code> 方法,它的实现可能是“移动身体”.</li><li><strong>子类 <code>Fish</code></strong> 继承了 <code>Animal</code>,但鱼的移动方式很特殊，是“摇动尾巴游泳”.</li><li><strong>子类 <code>Bird</code></strong> 也继承了 <code>Animal</code>,但鸟的移动方式是“扇动翅膀飞翔”.</li></ul><p>在这里,<code>Fish</code> 和 <code>Bird</code> 都重写了父类 <code>Animal</code> 的 <code>move()</code> 方法,为其提供了更具体的实现.</p><p><em>作用和好处</em></p><p>方法重写是实现<strong>多态性</strong>的基,这是面向对象编程的三大特性（封装、继承、多态）之一.</p><p>其主要好处是：</p><ol><li><strong>功能扩展和定制化</strong>：子类可以在不修改父类代码的前提下,改变或扩展从父类继承来的方法的行为,使其更符合子类的特性.</li><li><strong>实现多态</strong>：允许我们使用父类型的引用来指向子类型的对象,并在调用方法时,程序能够自动选择并执行子类重写后的方法.这使得代码更加灵活、可扩展和易于维护.</li></ol><p><em>规则</em></p><p>要想正确地重写一个方法,必须遵循以下严格的规则.一个流行的记忆法是**“三同一小一大”**：</p><ul><li><p><strong><code>三同</code> - 三个地方必须相同</strong></p><ol><li><strong>方法名必须相同</strong></li><li><strong>参数列表必须相同</strong>（参数的类型、数量、顺序都必须一致）</li><li><em>（方法名 + 参数列表 = 方法签名，所以核心是方法签名必须相同）</em></li></ol></li><li><p><strong><code>一小</code> - 返回值类型要更小或相等</strong></p><ol><li><strong>返回值类型</strong>必须与父类中被重写方法的返回值类型<strong>相同</strong>,或者是其<strong>子类型</strong>(父类方法返回 <code>Animal</code>,子类重写后可以返回 <code>Animal</code> 或者 <code>Dog</code>（假设<code>Dog</code>是<code>Animal</code>的子类）).</li></ol></li><li><p><strong><code>一大</code> - 访问权限要更大或相等</strong></p><ol><li><p>子类重写方法的<strong>访问修饰符</strong>权限<strong>不能低于</strong>父类被重写方法的权限.</p></li><li><p>权限从大到小排序为：<code>public</code> &gt; <code>protected</code> &gt; <code>default</code> (包访问权限) &gt; <code>private</code>(如果父类方法是 <code>protected</code>，子类重写时可以是 <code>protected</code> 或 <code>public</code>，但不能是 <code>default</code> 或 <code>private</code>).</p></li></ol></li></ul><p><em>注意：</em></p><ul><li><strong><code>private</code> 方法不能被重写</strong>：因为 <code>private</code> 方法对子类不可见.</li><li><strong><code>final</code> 方法不能被重写</strong>：<code>final</code> 关键字修饰的方法表示这是最终版本，不希望被任何子类修改.</li><li><strong><code>static</code> 方法不能被重写</strong>：静态方法属于类而不是对象.类可以定义同名的静态方法,但这被称为<strong>隐藏（Hiding）</strong>,而不是重写.</li></ul><hr /><h3 id="构造器的调用"><a class="markdownIt-Anchor" href="#构造器的调用"></a> 构造器的调用</h3><h4 id="this调用同一个类中的其他构造器"><a class="markdownIt-Anchor" href="#this调用同一个类中的其他构造器"></a> this()：调用同一个类中的其他构造器</h4><p><code>this()</code> 用于在一个构造器中调用同一个类中的另一个重载的构造器.这在当你希望重用构造逻辑,避免代码重复时非常有用,<strong>通过调用兄弟构造器,来对其初始化,类似于继承.</strong></p><p><em>核心规则：</em></p><ul><li><strong>必须是构造器的第一行语句</strong>：<code>this()</code> 调用必须是构造器中的第一个可执行语句.</li><li><strong>只能在构造器中使用</strong>：你不能在普通方法中使用 <code>this()</code> 来调用构造器.</li><li><strong>不能形成递归调用</strong>：两个或多个构造器之间不能通过 <code>this()</code> 相互调用,否则会造成无限循环,导致编译错误.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 无参构造器</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Person</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 调用下面的有两个参数的构造器，并提供默认值</span></span><br><span class="line">        <span class="built_in">this</span>(<span class="string">&quot;Unknown&quot;</span>, <span class="number">0</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;无参构造器被调用&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 只带一个 name 参数的构造器</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Person</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="comment">// 调用下面的有两个参数的构造器，年龄使用默认值</span></span><br><span class="line">        <span class="built_in">this</span>(name, <span class="number">0</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;一个参数的构造器被调用&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 带有 name 和 age 两个参数的构造器 (主要的初始化逻辑)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Person</span><span class="params">(String name, <span class="type">int</span> age)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">        System.out.println(<span class="string">&quot;两个参数的构造器被调用 (核心初始化)&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">display</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Name: &quot;</span> + <span class="built_in">this</span>.name + <span class="string">&quot;, Age: &quot;</span> + <span class="built_in">this</span>.age);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;--- 创建 p1 ---&quot;</span>);</span><br><span class="line">        <span class="type">Person</span> <span class="variable">p1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Person</span>(); <span class="comment">// 调用无参构造器</span></span><br><span class="line">        p1.display();</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;\n--- 创建 p2 ---&quot;</span>);</span><br><span class="line">        <span class="type">Person</span> <span class="variable">p2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Alice&quot;</span>); <span class="comment">// 调用一个参数的构造器</span></span><br><span class="line">        p2.display();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">--- 创建 p1 ---</span></span><br><span class="line"><span class="comment">两个参数的构造器被调用 (核心初始化)</span></span><br><span class="line"><span class="comment">无参构造器被调用</span></span><br><span class="line"><span class="comment">Name: Unknown, Age: 0</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">        --- 创建 p2 ---</span></span><br><span class="line"><span class="comment">两个参数的构造器被调用 (核心初始化)</span></span><br><span class="line"><span class="comment">一个参数的构造器被调用</span></span><br><span class="line"><span class="comment">Name: Alice, Age: 0*/</span></span><br></pre></td></tr></table></figure><ul><li>创建 <code>p1</code> 时，<code>new Person()</code> 调用了无参构造器。该构造器第一行 <code>this(&quot;Unknown&quot;, 0)</code> 将调用权转交给了 <code>Person(String, int)</code> 构造器。</li><li><code>Person(String, int)</code> 执行完核心的赋值操作后，控制权返回到无参构造器，继续执行后面的打印语句。</li><li>这种模式被称为<strong>构造器链 (Constructor Chaining)</strong>，它将初始化逻辑集中在一个构造器中，使得代码更易于维护。</li><li>相当于构造器不用重写</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// this.name = name;</span></span><br><span class="line"><span class="comment">// this.age = age;</span></span><br><span class="line"><span class="comment">// System.out.println(&quot;两个参数的构造器被调用 (核心初始化)&quot;);</span></span><br></pre></td></tr></table></figure><hr /><h4 id="super调用父类的构造器"><a class="markdownIt-Anchor" href="#super调用父类的构造器"></a> super()：调用父类的构造器</h4><p><code>super()</code> 用于在子类的构造器中显式地调用其直接父类的构造器,<strong>子类的所有构造器都会先调用父类的构造器再执行自己,确保父对象的部分得到正确的初始化</strong>.</p><p><em>核心规则</em></p><ul><li><strong>隐式调用</strong>：如果你在子类的构造器中没有显式地使用 <code>super()</code> 或 <code>this()</code>，那么Java编译器会自动在构造器的第一行插入一个无参的 <code>super()</code> 调用，即 <code>super();</code>。</li><li><strong>父类必须有可访问的无参构造器</strong>：如果父类没有提供无参构造器（例如，父类只定义了有参构造器），那么在子类的构造器中<strong>必须</strong>显式地使用 <code>super()</code> 来调用父类某个存在的有参构造器，否则会产生编译错误。</li><li><strong>必须是构造器的第一行语句</strong>：和 <code>this()</code> 一样，<code>super()</code> 调用也必须是子类构造器中的第一个可执行语句。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 父类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    String name;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 父类的构造器</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Animal</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        System.out.println(<span class="string">&quot;Animal 的构造器被调用&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 子类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Dog</span> <span class="keyword">extends</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    String breed;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 子类的构造器</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Dog</span><span class="params">(String name, String breed)</span> &#123;</span><br><span class="line">        <span class="comment">// 显式调用父类带有一个 String 参数的构造器</span></span><br><span class="line">        <span class="comment">// 必须是第一行</span></span><br><span class="line">        <span class="built_in">super</span>(name);</span><br><span class="line"></span><br><span class="line">        <span class="built_in">this</span>.breed = breed;</span><br><span class="line">        System.out.println(<span class="string">&quot;Dog 的构造器被调用&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">display</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Name: &quot;</span> + <span class="built_in">this</span>.name + <span class="string">&quot;, Breed: &quot;</span> + <span class="built_in">this</span>.breed);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Dog</span> <span class="variable">myDog</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Dog</span>(<span class="string">&quot;Buddy&quot;</span>, <span class="string">&quot;Golden Retriever&quot;</span>);</span><br><span class="line">        myDog.display();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">Animal 的构造器被调用</span></span><br><span class="line"><span class="comment">Dog 的构造器被调用</span></span><br><span class="line"><span class="comment">Name: Buddy, Breed: Golden Retriever*/</span></span><br></pre></td></tr></table></figure><ol><li>当我们创建 <code>Dog</code> 对象时 <code>new Dog(...)</code>,<code>Dog</code> 的构造器被调用.</li><li><code>Dog</code> 构造器的第一行是 <code>super(name)</code>/这会立即调用父类 <code>Animal</code> 中匹配的构造器（即 <code>Animal(String name)</code>）,并把 “Buddy” 传递过去.</li><li><code>Animal</code> 的构造器执行,初始化 <code>name</code> 属性,并打印消息.</li><li>父类构造器执行完毕后,控制权返回到 <code>Dog</code> 的构造器,继续执行 <code>this.breed = breed;</code> 和后面的打印语句.</li></ol><p>这个过程确保了在初始化子类特有属性（如 <code>breed</code>）之前,从父类继承来的属性（如 <code>name</code>）已经被正确地初始化了.</p><table><thead><tr><th style="text-align:center">特性</th><th><code>this()</code></th><th><code>super()</code></th></tr></thead><tbody><tr><td style="text-align:center"><strong>目的</strong></td><td>调用<strong>同一个类中</strong>的另一个重载构造器</td><td>调用<strong>直接父类</strong>的构造器.</td></tr><tr><td style="text-align:center"><strong>位置</strong></td><td>必须是构造器的<strong>第一行语句</strong></td><td>必须是构造器的<strong>第一行语句</strong>.</td></tr><tr><td style="text-align:center"><strong>共存</strong></td><td><code>this()</code> 和 <code>super()</code> <strong>不能</strong>在同一个构造器中同时出现,因为它们都要求自己是第一行语句.</td><td></td></tr><tr><td style="text-align:center"><strong>隐式调用</strong></td><td>不会自动调用.</td><td>如果没有显式调用 <code>this()</code> 或 <code>super()</code>,编译器会自动在第一行插入一个无参的 <code>super()</code>.</td></tr><tr><td style="text-align:center"><strong>使用场景</strong></td><td>代码复用，将多个构造器的初始化逻辑引导到一个主构造器中.</td><td>初始化子对象时,确保父对象的部分得到正确的初始化,这是继承中构造过程的必要环节.</td></tr></tbody></table><hr /><h2 id="多态"><a class="markdownIt-Anchor" href="#多态"></a> 多态</h2><p>多态是继承/实现情况下的一种现象,表现为: 对象多态和行为多态.<strong>它的核心思想是：同一操作作用于不同的对象,可以有不同的解释,产生不同的执行结果.</strong></p><h3 id="多态的基本介绍"><a class="markdownIt-Anchor" href="#多态的基本介绍"></a> 多态的基本介绍</h3><p><strong>实现多态的三个必要条件</strong></p><p>在大多数主流编程语言中,要实现多态,通常需要满足以下三个条件：</p><ol><li><strong>要有继承.：</strong> 必须存在父类和子类的关系.</li><li><strong>要有方法重写：</strong> 子类必须重写父类的某个方法.</li><li><strong>要有父类引用指向子类对象.：</strong> 在使用时,通过父类的引用变量来引用子类的实例.</li></ol><p>例如，在Java中：<code>Animal animal = new Dog();</code></p><ul><li><code>Animal</code> 是父类（引用变量的声明类型）</li><li><code>Dog</code> 是子类（对象的实际类型）</li><li><code>animal</code> 是一个父类引用，但它指向了一个 <code>Dog</code> 对象</li></ul><p>多态主要分为两种：</p><ol><li><strong>编译时多态/ 静态多态/ 对象多态(只接受父类方法,编译看左边)：</strong><ul><li>通过<strong>方法重载</strong>实现。</li><li>方法重载指的是在同一个类中，可以有多个同名的方法，但它们的参数列表（参数的个数、类型或顺序）不同。</li><li>编译器在编译阶段，就会根据你传入的参数来决定具体调用哪个方法。</li></ul></li><li><strong>运行时多态/ 动态多态/ 行为多态(接受不同子类的方法,运行看右边)：</strong><ul><li>这是我们通常所说的多态，通过<strong>方法重写</strong>实现。</li><li>在程序运行时，系统根据对象的实际类型来决定调用哪个方法。</li><li>上面的代码示例就是典型的运行时多态。</li></ul></li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 父类：形状</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">draw</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;绘制一个形状&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 子类：圆形</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Circle</span> <span class="keyword">extends</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="comment">// 重写父类的 draw 方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">draw</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;绘制一个圆形 ○&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Circle类特有的方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">roll</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;圆形正在滚动...&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 子类：矩形</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Rectangle</span> <span class="keyword">extends</span> <span class="title class_">Shape</span> &#123;</span><br><span class="line">    <span class="comment">// 重写父类的 draw 方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">draw</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;绘制一个矩形 □&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Rectangle类特有的方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">calculateArea</span><span class="params">(<span class="type">double</span> width, <span class="type">double</span> height)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> width * height;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>对象多态</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ObjectPolymorphismDemo</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;--- 对象多态示例 ---&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 1. 创建一个 Circle 对象，并用 Circle 类型的引用指向它</span></span><br><span class="line">        <span class="comment">// 这是最常规的用法，不涉及多态</span></span><br><span class="line">        <span class="type">Circle</span> <span class="variable">circleObject</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Circle</span>();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 2. 创建一个 Circle 对象，并用 Shape 类型的引用指向它</span></span><br><span class="line">        <span class="comment">// 这就是“对象多态”的核心体现</span></span><br><span class="line">        <span class="comment">// shapeRef 的“声明类型”是 Shape，“实际类型”是 Circle</span></span><br><span class="line">        <span class="type">Shape</span> <span class="variable">shapeRef</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Circle</span>();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 通过父类引用，我们可以调用父类中定义的方法</span></span><br><span class="line">        <span class="comment">// 尽管 shapeRef 实际指向 Circle，但编译器认为它是一个 Shape</span></span><br><span class="line">        shapeRef.draw(); <span class="comment">// 合法调用</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 【关键点】通过父类引用，我们“不能”调用子类特有的方法</span></span><br><span class="line">        <span class="comment">// 下面这行代码会导致“编译错误”！</span></span><br><span class="line">        <span class="comment">// 因为编译器检查 shapeRef 的类型是 Shape，而 Shape 类没有 roll() 方法。</span></span><br><span class="line">        <span class="comment">// 这就展示了对象多態在编译阶段的类型限制。</span></span><br><span class="line">        <span class="comment">// shapeRef.roll(); // -&gt; 编译错误：方法 roll() 在类型 Shape 中未定义</span></span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;shapeRef 的引用类型是 Shape, 但它实际指向了一个 Circle 对象。&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;这种身份上的多重性，就是对象多态。&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>行为多态</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BehaviorPolymorphismDemo</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 定义一个静态方法，参数是父类 Shape 类型</span></span><br><span class="line">    <span class="comment">// 这个方法可以接受任何 Shape 的子类对象，体现了多态的扩展性</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">executeDraw</span><span class="params">(Shape s)</span> &#123;</span><br><span class="line">        System.out.print(<span class="string">&quot;准备执行绘制动作... &quot;</span>);</span><br><span class="line">        s.draw(); <span class="comment">// 核心：同样的一行代码，行为却不同</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;--- 行为多态示例 ---&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建两个不同的子类对象</span></span><br><span class="line">        <span class="type">Shape</span> <span class="variable">myCircle</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Circle</span>();       <span class="comment">// 对象多态：用父类引用指向子类对象</span></span><br><span class="line">        <span class="type">Shape</span> <span class="variable">myRectangle</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Rectangle</span>(); <span class="comment">// 对象多态：用父类引用指向子类对象</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 调用同一个 executeDraw 方法，传入不同的对象</span></span><br><span class="line">        executeDraw(myCircle);</span><br><span class="line">        executeDraw(myRectangle);</span><br><span class="line">        </span><br><span class="line">        System.out.println(<span class="string">&quot;\n--- 在数组中演示 ---&quot;</span>);</span><br><span class="line">        <span class="comment">// 将不同子类对象放入一个父类类型的数组中</span></span><br><span class="line">        Shape[] shapes = &#123;<span class="keyword">new</span> <span class="title class_">Circle</span>(), <span class="keyword">new</span> <span class="title class_">Rectangle</span>(), <span class="keyword">new</span> <span class="title class_">Shape</span>()&#125;;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 循环遍历数组，对每个元素执行 draw()</span></span><br><span class="line">        <span class="keyword">for</span> (Shape currentShape : shapes) &#123;</span><br><span class="line">            currentShape.draw(); <span class="comment">// 运行时，JVM会判断 currentShape 的真实身份并调用对应的方法</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>我们可以用一个简单的因果关系来理解它们：</p><p><strong>因为</strong> 有了 <strong>对象多态</strong>（一个 <code>Dog</code> 对象可以被看作是 <code>Animal</code>）， <strong>所以</strong> 才能实现 <strong>行为多态</strong>（当这个被看作 <code>Animal</code> 的 <code>Dog</code> 对象执行 <code>makeSound</code> 动作时，它表现出 <code>Dog</code> 的行为）。</p><table><thead><tr><th style="text-align:center">特性</th><th>对象多态</th><th>行为多态</th></tr></thead><tbody><tr><td style="text-align:center"><strong>核心概念</strong></td><td>对象身份的多样性 (一个对象, 多种类型)</td><td>方法行为的多样性 (一个接口, 多种实现)</td></tr><tr><td style="text-align:center"><strong>关键机制</strong></td><td>向上转型</td><td>方法重写  和 动态绑定</td></tr><tr><td style="text-align:center"><strong>作用阶段</strong></td><td>编译期进行类型检查，运行期进行对象实例化</td><td>运行期决定调用哪个方法</td></tr><tr><td style="text-align:center"><strong>关注点</strong></td><td>对象<strong>是什么</strong> (Is-A relationship)</td><td>对象<strong>做什么</strong> (How it behaves)</td></tr><tr><td style="text-align:center"><strong>关系</strong></td><td><strong>前提 / 基础</strong></td><td><strong>结果 / 体现</strong></td></tr></tbody></table><p><em>注意</em></p><p>多态是对象的知识,和成员变量无关.</p><hr /><h3 id="多态的好处及其存在的问题"><a class="markdownIt-Anchor" href="#多态的好处及其存在的问题"></a> 多态的好处及其存在的问题</h3><h4 id="好处"><a class="markdownIt-Anchor" href="#好处"></a> 好处</h4><ul><li>在多态形式下,右边对象是解耦合的(可以拆分与组装),更便于扩展和维护.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">People</span> <span class="variable">p1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Student</span>();</span><br><span class="line"><span class="type">People</span> <span class="variable">p1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Teacher</span>();</span><br><span class="line"></span><br><span class="line">p1.run();</span><br></pre></td></tr></table></figure><ul><li>定义方法时,使用父类类型的形参,可以接受一切子类对象,扩展性更强,更便利.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">Wolf</span> <span class="variable">w</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Wolf</span>();</span><br><span class="line">go(w);</span><br><span class="line"></span><br><span class="line"><span class="type">Tortoise</span> <span class="variable">t</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Tortoise</span>();</span><br><span class="line">go(t);</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">go</span><span class="params">(Animal a)</span>&#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;开始&quot;</span>);</span><br><span class="line">    a.run();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="问题"><a class="markdownIt-Anchor" href="#问题"></a> 问题</h4><ul><li>多态下不能使用子类的独有功能(父类没有重写的).</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">eat</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;动物在吃东西&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Dog</span> <span class="keyword">extends</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">eat</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;狗在啃骨头&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Dog的独有方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">bark</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;汪汪汪！&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Cat</span> <span class="keyword">extends</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">eat</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;猫在吃鱼&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Cat的独有方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">meow</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;喵喵喵~&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Animal</span> <span class="variable">pet</span> <span class="operator">=</span> getPet(<span class="string">&quot;dog&quot;</span>); <span class="comment">// getPet方法返回一个Animal引用，实际可能是Dog或Cat</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 我们可以安全地调用eat()，因为Animal类里有定义</span></span><br><span class="line">        pet.eat(); <span class="comment">// 运行时会根据pet的实际类型决定调用Dog还是Cat的eat()方法</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 尝试调用Dog的独有方法</span></span><br><span class="line">        <span class="comment">// pet.bark(); // ！！！这里会产生编译错误 ！！！</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> Animal <span class="title function_">getPet</span><span class="params">(String petType)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="string">&quot;dog&quot;</span>.equals(petType)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Dog</span>();</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Cat</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译器在检查 <code>pet.bark()</code> 时,它只知道 <code>pet</code> 是一个 <code>Animal</code> 类型的引用,它无法在编译阶段预知 <code>getPet()</code> 方法在运行时到底会返回一个 <code>Dog</code> 对象还是一个 <code>Cat</code> 对象.</p><ul><li>如果返回的是 <code>Dog</code>,调用 <code>bark()</code> 没问题.</li><li>但如果返回的是 <code>Cat</code>,<code>Cat</code> 对象根本没有 <code>bark()</code> 方法,运行时就会出错（<code>MethodNotFound</code> 之类的错误）.</li></ul><p><strong>解决方法</strong></p><p>既然编译器是因为不确定引用的真实类型才阻止我们,那么我们只需要向编译器“证明”这个引用的真实类型是安全的,就可以调用了,这个“证明”的过程就是 <strong>强制类型转换（向下转型）</strong>.</p><p>但是,在转换之前,做一个负责任的程序员,我们应该先用 <code>instanceof</code> 来检查一下,确保转换是安全的,避免 <code>ClassCastException</code>.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">    <span class="type">Animal</span> <span class="variable">pet</span> <span class="operator">=</span> getPet(<span class="string">&quot;dog&quot;</span>);</span><br><span class="line">    pet.eat(); <span class="comment">// 输出: 狗在啃骨头</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 想要调用bark()，需要先判断再强转</span></span><br><span class="line">    <span class="keyword">if</span> (pet <span class="keyword">instanceof</span> Dog) &#123;</span><br><span class="line">        <span class="type">Dog</span> <span class="variable">realDog</span> <span class="operator">=</span> (Dog) pet; <span class="comment">// 向下转型</span></span><br><span class="line">        realDog.bark(); <span class="comment">// 现在可以安全地调用了。输出: 汪汪汪！</span></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pet <span class="keyword">instanceof</span> Cat) &#123;</span><br><span class="line">        <span class="type">Cat</span> <span class="variable">realCat</span> <span class="operator">=</span> (Cat) pet;</span><br><span class="line">        realCat.meow();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过 <code>if (pet instanceof Dog)</code>,我们向编译器和JVM都明确了一件事：在这个代码块内部,<code>pet</code> 确实指向一个 <code>Dog</code> 对象,因此可以安全地将其转换为 <code>Dog</code> 类型,并调用其独有的 <code>bark()</code> 方法.</p><hr /><h1 id="final关键字"><a class="markdownIt-Anchor" href="#final关键字"></a> final关键字</h1><p>final关键字是最终的意思,可以修饰:类,方法,变量.</p><ul><li>修饰类: 该类被称为最终类,特点是不能被继承.(工具类)</li><li>修饰方法: 该方法被称为最终方法,特点是不能被重写了.</li><li>修饰变量: 该变量有且仅能被赋值一次.(定义常量)</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 应用全局常量类</span></span><br><span class="line"><span class="comment"> * final 关键字确保这个类不能被继承。</span></span><br><span class="line"><span class="comment"> * 私有的构造函数防止外部通过 new AppConstants() 来创建实例。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">class</span> <span class="title class_">AppConstants</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 私有构造函数，防止实例化</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">AppConstants</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 这个构造函数永远不会被调用</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// --- 应用基本信息 ---</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">APP_NAME</span> <span class="operator">=</span> <span class="string">&quot;My Awesome App&quot;</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">APP_VERSION</span> <span class="operator">=</span> <span class="string">&quot;1.0.2&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// --- 网络配置 ---</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">API_BASE_URL</span> <span class="operator">=</span> <span class="string">&quot;https://api.example.com/v1/&quot;</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">DEFAULT_TIMEOUT_SECONDS</span> <span class="operator">=</span> <span class="number">30</span>; <span class="comment">// 单位：秒</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">MAX_RETRIES</span> <span class="operator">=</span> <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// --- 数据库配置 ---</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DB_NAME</span> <span class="operator">=</span> <span class="string">&quot;user_database&quot;</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">DB_VERSION</span> <span class="operator">=</span> <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// --- 默认配置 ---</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">DEFAULT_LANGUAGE</span> <span class="operator">=</span> <span class="string">&quot;zh-CN&quot;</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">DEFAULT_PAGE_SIZE</span> <span class="operator">=</span> <span class="number">20</span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><table><thead><tr><th>修饰目标</th><th>效果</th><th>主要目的</th></tr></thead><tbody><tr><td><strong>变量</strong></td><td><strong>一次赋值,终身不变</strong>。<br>- 基本类型：值不变.<br>- 引用类型：指向的地址不变,但对象内容可变.</td><td>创建常量,保证线程安全,实现不可变对象.</td></tr><tr><td><strong>方法</strong></td><td><strong>不能被子类重写 (Override)</strong>.</td><td>锁定核心逻辑,防止子类破坏原有设计,保证程序稳定性.</td></tr><tr><td><strong>类</strong></td><td><strong>不能被继承 (Inherit)</strong>.</td><td>保证类的实现不会被修改,安全性,设计完整性.</td></tr></tbody></table><hr /><h1 id="单例类"><a class="markdownIt-Anchor" href="#单例类"></a> 单例类</h1><p>单例模式是一种创建型设计模式,它确保一个类只有一个实例,并提供一个全局访问点来获取这个唯一的实例.</p><p>这种模式在很多场景下都非常有用,比如：</p><ul><li><strong>应用上下文管理</strong>：提供一个全局、安全的 Context,避免内存泄漏.</li><li><strong>数据库/Repository</strong>：保证全局使用唯一的数据库连接,避免资源浪费和性能开销.</li><li><strong>网络请求客户端</strong>：复用底层的线程池、连接池和缓存,提高网络请求效率.</li><li><strong><code>SharedPreferences </code>工具类</strong>：统一管理数据读写,方便在应用各处调用.</li></ul><p><em>特点：</em></p><ol><li>**私有的构造函数 **：为了防止外部通过 <code>new</code> 关键字直接创建类的实例,需要将构造函数声明为私有的.</li><li>**私有的静态实例变量 **：在类的内部创建一个该类自身的静态实例.</li><li>**公有的静态工厂方法 **：提供一个全局的、静态的方法,用于返回这个唯一的实例,这个方法通常被命名为 <code>getInstance()</code>.</li></ol><h2 id="饿汉式"><a class="markdownIt-Anchor" href="#饿汉式"></a> 饿汉式</h2><p>饿汉式在类加载的时候就直接创建实例,不管你是否需要它.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">EagerSingleton</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 1. 私有的静态实例，在类加载时就进行初始化</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">EagerSingleton</span> <span class="variable">instance</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">EagerSingleton</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 私有的构造函数</span></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">EagerSingleton</span><span class="params">()</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 公有的静态方法，返回已经创建好的实例</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> EagerSingleton <span class="title function_">getInstance</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>优点</strong>：<ul><li>实现简单.</li><li>线程安全.因为 JVM 在加载类时,静态变量的初始化过程是线程安全的.</li></ul></li><li><strong>缺点</strong>：<ul><li>资源浪费.如果这个实例从始至终都没有被使用,那么它所占用的内存就浪费了.</li></ul></li></ul><h2 id="懒汉式"><a class="markdownIt-Anchor" href="#懒汉式"></a> 懒汉式</h2><p>懒汉式在第一次调用 <code>getInstance()</code> 方法时才创建实例.</p><h3 id="基础懒汉式线程不安全"><a class="markdownIt-Anchor" href="#基础懒汉式线程不安全"></a> 基础懒汉式（线程不安全）</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LazySingleton</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> LazySingleton instance;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">LazySingleton</span><span class="params">()</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> LazySingleton <span class="title function_">getInstance</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 在第一次使用时才创建实例</span></span><br><span class="line">        <span class="keyword">if</span> (instance == <span class="literal">null</span>) &#123;</span><br><span class="line">            instance = <span class="keyword">new</span> <span class="title class_">LazySingleton</span>();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>优点</strong>：<ul><li>实现了懒加载,避免了资源浪费.</li></ul></li><li><strong>缺点</strong>：<ul><li><strong>线程不安全</strong>.在多线程环境下,可能会有多个线程同时进入 <code>if (instance == null)</code> 判断,从而创建出多个实例,违背了单例的初衷.<strong>因此,这种方式不推荐在多线程环境中使用.</strong></li></ul></li></ul><h3 id="同步方法懒汉式线程安全"><a class="markdownIt-Anchor" href="#同步方法懒汉式线程安全"></a> 同步方法懒汉式（线程安全）</h3><p>为了解决线程不安全问题,最直接的方法就是给 <code>getInstance()</code> 方法加上 <code>synchronized</code> 关键字.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SynchronizedLazySingleton</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> SynchronizedLazySingleton instance;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">SynchronizedLazySingleton</span><span class="params">()</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用 synchronized 关键字保证线程安全</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">synchronized</span> SynchronizedLazySingleton <span class="title function_">getInstance</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (instance == <span class="literal">null</span>) &#123;</span><br><span class="line">            instance = <span class="keyword">new</span> <span class="title class_">SynchronizedLazySingleton</span>();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>优点</strong>：<ul><li>线程安全.</li><li>实现了懒加载.</li></ul></li><li><strong>缺点</strong>：<ul><li><strong>性能低下</strong>.<code>synchronized</code> 会给整个方法加锁,每次调用 <code>getInstance()</code> 都会进行同步,但实际上只有在第一次创建实例时才需要同步.一旦实例创建完毕,后续的同步就成了不必要的开销,会严重影响性能.</li></ul></li></ul><h3 id="双重检查锁定"><a class="markdownIt-Anchor" href="#双重检查锁定"></a> 双重检查锁定</h3><p>这是对同步方法懒汉式的一种优化,在保证线程安全的同时,也兼顾了性能.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DoubleCheckedLockingSingleton</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用 volatile 关键字确保多线程下的可见性和禁止指令重排</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> DoubleCheckedLockingSingleton instance;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">DoubleCheckedLockingSingleton</span><span class="params">()</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> DoubleCheckedLockingSingleton <span class="title function_">getInstance</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 第一次检查，避免不必要的同步</span></span><br><span class="line">        <span class="keyword">if</span> (instance == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 同步块，只在实例未创建时进行同步</span></span><br><span class="line">            <span class="keyword">synchronized</span> (DoubleCheckedLockingSingleton.class) &#123;</span><br><span class="line">                <span class="comment">// 第二次检查，确保只有一个线程创建实例</span></span><br><span class="line">                <span class="keyword">if</span> (instance == <span class="literal">null</span>) &#123;</span><br><span class="line">                    instance = <span class="keyword">new</span> <span class="title class_">DoubleCheckedLockingSingleton</span>();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>注意</strong>：<code>instance</code> 变量必须用 <code>volatile</code> 关键字修饰.这是因为 <code>instance = new DoubleCheckedLockingSingleton();</code> 这行代码在 JVM 中并非原子操作,它大致可以分为三步：<ol><li>为 <code>instance</code> 分配内存空间.</li><li>初始化 <code>instance</code> 对象.</li><li>将 <code>instance</code> 变量指向分配的内存地址. JVM 可能会进行指令重排序,导致步骤 3 在步骤 2 之前执行.如果一个线程执行了 1 和 3,但还没执行 2,另一个线程在第一次检查 <code>if (instance == null)</code> 时会发现 <code>instance</code> 不为 <code>null</code>,然后直接返回一个未完全初始化的对象,从而导致问题.<code>volatile</code> 可以禁止这种指令重排.</li></ol></li><li><strong>优点</strong>：<ul><li>线程安全.</li><li>性能较高,只有在第一次创建时才会同步.</li><li>实现了懒加载.</li></ul></li><li><strong>缺点</strong>：<ul><li>实现相对复杂.</li></ul></li></ul><h2 id="静态内部类"><a class="markdownIt-Anchor" href="#静态内部类"></a> 静态内部类</h2><p>这是一种被广泛推荐的实现方式,它结合了懒汉式和饿汉式的优点.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StaticInnerClassSingleton</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">StaticInnerClassSingleton</span><span class="params">()</span> &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 静态内部类</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">SingletonHolder</span> &#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">StaticInnerClassSingleton</span> <span class="variable">INSTANCE</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StaticInnerClassSingleton</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> StaticInnerClassSingleton <span class="title function_">getInstance</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> SingletonHolder.INSTANCE;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>原理</strong>：<ul><li>当 <code>StaticInnerClassSingleton</code> 类被加载时,它的静态内部类 <code>SingletonHolder</code> 并不会被加载.</li><li>只有当第一次调用 <code>getInstance()</code> 方法时,JVM 才会加载 <code>SingletonHolder</code> 类,并初始化 <code>INSTANCE</code> 静态变量.</li><li>JVM 在类加载时保证了初始化的线程安全性.</li></ul></li><li><strong>优点</strong>：<ul><li>线程安全.</li><li>实现了懒加载.</li><li>实现简单，代码清晰.</li></ul></li></ul><h2 id="枚举"><a class="markdownIt-Anchor" href="#枚举"></a> 枚举</h2><p>这是《Effective Java》作者 Joshua Bloch 极力推荐的方式,也是最简单、最安全的实现方式.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">EnumSingleton</span> &#123;</span><br><span class="line">    INSTANCE;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">doSomething</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Doing something...&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 直接通过 EnumSingleton.INSTANCE 获取唯一的实例</span></span><br><span class="line">        <span class="type">EnumSingleton</span> <span class="variable">instance1</span> <span class="operator">=</span> EnumSingleton.INSTANCE;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 调用实例的方法</span></span><br><span class="line">        instance1.doSomething(); <span class="comment">// 输出: Doing something...</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 再次获取实例，验证是否为同一个对象</span></span><br><span class="line">        <span class="type">EnumSingleton</span> <span class="variable">instance2</span> <span class="operator">=</span> EnumSingleton.INSTANCE;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 比较 instance1 和 instance2 的内存地址</span></span><br><span class="line">        <span class="comment">// 结果会是 true，证明了它们是同一个实例</span></span><br><span class="line">        System.out.println(instance1 == instance2); <span class="comment">// 输出: true</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用时,直接通过 <code>EnumSingleton.INSTANCE</code> 来访问.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> <span class="title class_">TrafficLight</span> &#123;</span><br><span class="line">    RED, YELLOW, GREEN</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TrafficController</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">handleLight</span><span class="params">(TrafficLight currentLight)</span> &#123;</span><br><span class="line">        <span class="keyword">switch</span> (currentLight) &#123;</span><br><span class="line">            <span class="keyword">case</span> RED:</span><br><span class="line">                System.out.println(<span class="string">&quot;停止！&quot;</span>);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> YELLOW:</span><br><span class="line">                System.out.println(<span class="string">&quot;减速，准备停车。&quot;</span>);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> GREEN:</span><br><span class="line">                System.out.println(<span class="string">&quot;通行！&quot;</span>);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="comment">// 不需要 default，因为所有枚举情况都已覆盖。</span></span><br><span class="line">            <span class="comment">// 如果未来给 TrafficLight 增加了新的常量，编译器会警告 switch 语句不完整。</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">TrafficController</span> <span class="variable">controller</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">TrafficController</span>();</span><br><span class="line">        controller.handleLight(TrafficLight.RED); <span class="comment">// 输出: 停止！</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>做信息分类和标志.</p><ul><li><strong>优点</strong>：<ul><li><strong>实现极其简单</strong>.</li><li><strong>线程安全</strong>.由 JVM 从根本上保证了线程安全.</li><li><strong>能防止反序列化重新创建新的对象</strong>.Java 的序列化机制允许通过反序列化创建一个新的对象,但对于枚举类,JVM 会保证即使反序列化也不会创建新的实例.而其他方式实现的单例类需要特殊处理才能防止反序列化破坏单例.</li><li><strong>能防止反射攻击</strong>。其他方式可以通过反射调用私有构造函数来创建新实例,而枚举类则可以防止这种情况.</li></ul></li><li><strong>缺点</strong>：<ul><li>非懒加载.</li><li>可读性可能稍差,对于不熟悉枚举单例的开发者可能需要时间理解.</li></ul></li></ul><table><thead><tr><th>实现方式</th><th>线程安全</th><th>懒加载</th><th>推荐程度</th><th>备注</th></tr></thead><tbody><tr><td><strong>饿汉式</strong></td><td>是</td><td>否</td><td>⭐⭐⭐</td><td>简单，但可能浪费资源</td></tr><tr><td><strong>懒汉式（基础）</strong></td><td>否</td><td>是</td><td>⭐</td><td><strong>不推荐使用</strong></td></tr><tr><td><strong>懒汉式（同步方法）</strong></td><td>是</td><td>是</td><td>⭐⭐</td><td>性能差，不推荐使用</td></tr><tr><td><strong>双重检查锁定</strong></td><td>是</td><td>是</td><td>⭐⭐⭐⭐</td><td>推荐，但实现略复杂，需注意 <code>volatile</code></td></tr><tr><td><strong>静态内部类</strong></td><td>是</td><td>是</td><td>⭐⭐⭐⭐⭐</td><td><strong>强烈推荐</strong>，兼顾性能和简洁</td></tr><tr><td><strong>枚举</strong></td><td>是</td><td>否</td><td>⭐⭐⭐⭐⭐</td><td><strong>强烈推荐</strong>，最简单、最安全的方式</td></tr></tbody></table><hr /><h1 id="抽象类方法"><a class="markdownIt-Anchor" href="#抽象类方法"></a> 抽象类/方法</h1><h2 id="抽象方法"><a class="markdownIt-Anchor" href="#抽象方法"></a> 抽象方法</h2><p>抽象方法是指一个<strong>只有方法声明,没有具体方法体</strong>（即没有 <code>&#123;&#125;</code> 代码块）的方法.它存在的意义是定义一个规范或契约,告诉子类“你必须实现这个功能”,但具体怎么实现,由子类自己决定.</p><p><strong>语法特征</strong>：</p><ol><li>使用 <code>abstract</code> 关键字进行修饰.</li><li>没有方法体,直接以分号 <code>;</code> 结束.</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">class</span> <span class="title class_">Vehicle</span> &#123;</span><br><span class="line">    <span class="comment">// 这是一个抽象方法，它没有具体的实现</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title function_">startEngine</span><span class="params">()</span>; </span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 这不是抽象方法，它有自己的实现</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">turnOff</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;引擎已关闭。&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中，<code>startEngine()</code> 就是一个抽象方法.它规定了所有 <code>Vehicle</code>（交通工具）都必须有“启动引擎”这个功能,但汽车（Car）和摩托车（Motorcycle）启动引擎的方式可能不同,所以具体实现留给它们各自的子类.</p><hr /><h2 id="抽象类"><a class="markdownIt-Anchor" href="#抽象类"></a> 抽象类</h2><p>抽象类是指一个<strong>包含抽象方法的类</strong>.只要一个类里哪怕只有一个抽象方法,这个类就必须被声明为抽象类.抽象类就像一个“半成品”或“模板”,它不能被直接实例化（即不能用 <code>new</code> 关键字创建对象）,因为它包含未实现的功能.它的唯一用途就是被其他类继承.</p><p><strong>核心特点</strong>：</p><ol><li><strong>不能被实例化</strong>：你不能写 <code>new Vehicle()</code> 这样的代码,因为 <code>Vehicle</code> 是一个抽象类,它的 <code>startEngine()</code> 方法还没有实现,直接创建对象没有意义.</li><li><strong>必须被继承</strong>：抽象类存在的目的就是为了让子类来继承它,并实现它所有的抽象方法.</li><li><strong>可以包含非抽象方法</strong>：抽象类不仅可以有抽象方法,也可以有已经实现了的具体方法（如上面的 <code>turnOff()</code> 方法）.这使得子类可以复用这些通用功能**(用<code>final </code>关键字修饰)**.</li><li><strong>可以包含成员变量、构造函数等</strong>：抽象类和普通类一样,可以拥有成员变量和构造函数.其构造函数主要是为了方便子类在构造时调用 <code>super()</code> 来初始化父类的成员.</li><li><strong>子类的责任</strong>：<ul><li>一个类如果继承了抽象类,那么它<strong>必须实现父类中所有（未被实现的）抽象方法</strong>.</li><li>如果子类不想实现父类的所有抽象方法,那么这个<strong>子类必须被声明为抽象类</strong>.</li></ul></li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Car 是一个具体的类，它继承了抽象类 Vehicle</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Car</span> <span class="keyword">extends</span> <span class="title class_">Vehicle</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 必须实现父类中所有的抽象方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">startEngine</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;汽车：插入钥匙，转动点火。&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">    </span><br><span class="line"><span class="comment">// Motorcycle 也是一个具体的类</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Motorcycle</span> <span class="keyword">extends</span> <span class="title class_">Vehicle</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">startEngine</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;摩托车：脚踩启动杆。&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// Vehicle vehicle = new Vehicle(); // 错误！不能实例化抽象类</span></span><br><span class="line"></span><br><span class="line">        <span class="type">Vehicle</span> <span class="variable">myCar</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Car</span>();         <span class="comment">// 正确，使用子类实例化</span></span><br><span class="line">        <span class="type">Vehicle</span> <span class="variable">myMotorcycle</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Motorcycle</span>(); <span class="comment">// 正确</span></span><br><span class="line"></span><br><span class="line">        myCar.startEngine();        <span class="comment">// 调用 Car 类自己实现的方法</span></span><br><span class="line">        myCar.turnOff();            <span class="comment">// 调用从 Vehicle 类继承来的通用方法</span></span><br><span class="line"></span><br><span class="line">        myMotorcycle.startEngine(); <span class="comment">// 调用 Motorcycle 类自己实现的方法</span></span><br><span class="line">        myMotorcycle.turnOff();     <span class="comment">// 调用从 Vehicle 类继承来的通用方法</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="抽象类方法的作用"><a class="markdownIt-Anchor" href="#抽象类方法的作用"></a> 抽象类/方法的作用</h2><p>抽象类的主要目的是为了<strong>代码复用</strong>和<strong>强制规范</strong>.</p><ol><li><strong>代码复用 (Code Reusability)</strong>： 当多个子类有一些共同的功能时,可以将这些功能实现在抽象父类中（作为具体方法）,子类只需要继承就可以直接使用,避免了在每个子类中重复编写相同的代码.例如上面例子中的 <code>turnOff()</code> 方法.</li><li><strong>强制规范 (Enforce a Contract)</strong>： 通过抽象方法,父类可以为所有子类定义一个统一的接口或模板.它强制要求所有子类都必须提供某个特定功能的实现,从而保证了体系内所有对象都具有某些基本行为,这使得多态（Polymorphism）的应用更加安全和方便.例如,我们能确保任何 <code>Vehicle</code> 类型的对象,都可以调用 <code>startEngine()</code> 方法,而不用担心这个方法不存在.</li></ol><hr /><h1 id="接口"><a class="markdownIt-Anchor" href="#接口"></a> 接口</h1><p>接口(Interface)是一种行为规范或契约,它一般只<strong>定义一种方法签名(方法名,参数,返回值类型),但不提供任何具体的实现.<strong>如果任何类实现了这个接口,那么它必须要为方法提供</strong>具体的实现代码.</strong></p><p>你可以把接口想象成一份合同或一个设备的插座标准：</p><ul><li><strong>合同</strong>：合同规定了甲方和乙方需要履行的义务（必须做什么），但没有规定具体怎么做。任何签署合同的人都必须履行这些义务。</li><li><strong>USB 插座</strong>：USB 接口标准规定了插座的形状、引脚定义和功能。任何设备（U盘、鼠标、键盘）只要遵循这个标准，就能插入USB端口并正常工作。电脑不关心你插的是什么设备，只关心它是否符合USB规范。</li></ul><p><em>特点与优势</em></p><ol><li><strong>纯粹的抽象</strong>：接口是完全抽象的,它只包含方法的声明,不包含方法的实现.<em>注：现代编程语言如 Java 8+ 允许接口包含 <code>private</code>,<code>default</code>和 <code>static</code> 方法,但这属于特例,其核心思想仍是定义规范.</em></li><li><strong>不能被实例化</strong>：你不能直接用 <code>new</code> 关键字创建一个接口的对象,因为它没有具体的功能实现.</li><li><strong>必须被类实现</strong>：接口的价值在于被类（Class）来实现,一个类可以实现一个或多个接口.</li><li><strong>强制实现</strong>：实现接口的类必须为接口中的所有方法提供具体的实现,否则,这个类必须被声明为抽象类.</li><li><strong>多实现</strong>：一个类只能继承一个父类（单继承）,但可以<strong>实现多个接口</strong>.这解决了单继承的局限性,使得一个类可以同时拥有多种“能力”.</li><li><strong>多继承</strong>: 一个接口可以继承多个接口.</li><li><strong>解耦合</strong>: 让程序可以面向接口编程,程序员可以切换各种任务实现.</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义一个“可飞行的”接口</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Flyable</span> &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 接口中的方法默认是 public abstract 的，所以可以省略这两个关键字</span></span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">takeOff</span><span class="params">()</span>; <span class="comment">// 起飞</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">fly</span><span class="params">()</span>;     <span class="comment">// 飞行</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">land</span><span class="params">()</span>;    <span class="comment">// 降落</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Bird</span> <span class="keyword">implements</span> <span class="title class_">Flyable</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">takeOff</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;鸟：扇动翅膀，蹬腿起飞。&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">fly</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;鸟：在空中翱翔。&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">land</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;鸟：收拢翅膀，平稳着陆。&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明为接口类型 List，实例化为具体类 CopyOnWriteArrayList</span></span><br><span class="line">List&lt;String&gt; myList = <span class="keyword">new</span> <span class="title class_">CopyOnWriteArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 我们可以像操作任何普通 List 一样操作它</span></span><br><span class="line">myList.add(<span class="string">&quot;A&quot;</span>);</span><br><span class="line">myList.add(<span class="string">&quot;B&quot;</span>);</span><br><span class="line">System.out.println(myList.get(<span class="number">0</span>)); <span class="comment">// 输出 &quot;A&quot;</span></span><br></pre></td></tr></table></figure><h2 id="接口的主要用途"><a class="markdownIt-Anchor" href="#接口的主要用途"></a> 接口的主要用途</h2><ol><li><p><strong>实现多态接口使得我们可以编写更通用、更灵活的代码,我们可以面向接口编程,而不是面向具体的实现类.</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Airport</span> &#123;</span><br><span class="line">    <span class="comment">// 这个方法可以接受任何实现了 Flyable 接口的对象</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">performFlight</span><span class="params">(Flyable flyingObject)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;--- 飞行表演开始 ---&quot;</span>);</span><br><span class="line">        flyingObject.takeOff();</span><br><span class="line">        flyingObject.fly();</span><br><span class="line">        flyingObject.land();</span><br><span class="line">        System.out.println(<span class="string">&quot;--- 飞行表演结束 ---\n&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Airport</span> <span class="variable">airport</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Airport</span>();</span><br><span class="line"></span><br><span class="line">        <span class="type">Flyable</span> <span class="variable">bird</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Bird</span>();</span><br><span class="line">        <span class="type">Flyable</span> <span class="variable">airplane</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Airplane</span>();</span><br><span class="line">        <span class="type">Flyable</span> <span class="variable">kite</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Kite</span>();</span><br><span class="line"></span><br><span class="line">        airport.performFlight(bird);</span><br><span class="line"></span><br><span class="line">        airport.performFlight(airplane);</span><br><span class="line"></span><br><span class="line">        airport.performFlight(kite);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>performFlight</code> 方法不关心传来的是鸟、飞机还是风筝,它只认 <code>Flyable</code> 这个接口.这大大提高了代码的扩展性.如果未来我们新增一个 <code>Drone</code>（无人机）类,只要它也实现 <code>Flyable</code> 接口,就可以无缝地被 <code>performFlight</code> 方法调用,无需修改任何现有代码.</p></li><li><p><strong>定义程序的契约和规范 (API Contract)</strong> 在大型项目中,不同模块或团队之间可以通过接口来约定功能.例如,A 团队负责定义接口（需要什么功能）,B 团队负责实现这些接口（如何实现这些功能）,这实现了开发的解耦,双方可以并行工作.</p></li><li><p><strong>降低耦合度 (Decoupling)</strong> 通过面向接口编程,模块之间的依赖关系从“依赖具体的类”转变为“依赖抽象的接口”.这样,只要接口不变,具体的实现类可以随意更换,而不会影响到调用方.这在<strong>依赖注入 (Dependency Injection)</strong> 和各种设计模式中是核心思想.</p></li></ol><table><thead><tr><th>应用场景</th><th>核心目的</th><th>典型例子</th></tr></thead><tbody><tr><td><strong>事件监听/回调</strong></td><td><strong>解耦</strong> 和 <strong>异步通知</strong>。让一个对象能在特定事件发生时，通知另一个关心该事件的对象</td><td><code>View.OnClickListener</code>, <code>TextWatcher</code></td></tr><tr><td><strong>网络/数据库API定义</strong></td><td><strong>定义契约</strong>。只声明需要做什么（如获取数据），而不关心具体怎么做（由库生成实现）</td><td>Retrofit 的<code>Service</code>接口, Room 的 <code>DAO</code> 接口</td></tr><tr><td><strong>Fragment-Activity通信</strong></td><td><strong>解耦</strong> 和 <strong>标准化交互</strong>。让 Fragment 可复用，不依赖于任何特定的 Activity</td><td>自定义的回调接口</td></tr><tr><td><strong>依赖注入(DI)</strong></td><td><strong>面向接口编程</strong>。降低模块间依赖，方便替换实现和进行单元测试</td><td>在 Hilt/Dagger 中为<code>Repository</code>等定义接口</td></tr></tbody></table><h2 id="接口与抽象类的区别"><a class="markdownIt-Anchor" href="#接口与抽象类的区别"></a> 接口与抽象类的区别</h2><table><thead><tr><th>特性</th><th>接口 (Interface)</th><th>抽象类 (Abstract Class)</th></tr></thead><tbody><tr><td><strong>设计意图</strong></td><td>定义一种<strong>能力</strong>或<strong>行为规范</strong> (can-do)</td><td>定义一个<strong>族系</strong>或<strong>模板</strong> (is-a)</td></tr><tr><td><strong>关系</strong></td><td>类与接口是<strong>实现</strong>关系 (<code>implements</code>。</td><td>类与抽象类是<strong>继承</strong>关系 (<code>extends</code>)</td></tr><tr><td><strong>继承限制</strong></td><td>一个类可以<strong>实现多个</strong>接口</td><td>一个类<strong>只能继承一个</strong>父类（包括抽象类）</td></tr><tr><td><strong>主要用途</strong></td><td>做功能的解耦合</td><td>实现代码复用性</td></tr></tbody></table><p><strong>总结一下</strong>：</p><ul><li>当你想要定义一组不相关的类应该共同遵守的行为规范时，使用<strong>接口</strong>.例如，<code>Flyable</code>、<code>Runnable</code>（可运行的）、<code>Serializable</code>（可序列化的）.</li><li>当你想要为一组高度相关的类创建一个共享代码和通用特性的基类时,使用<strong>抽象类</strong>.例如，<code>Shape</code>（图形）是 <code>Circle</code>（圆形）和 <code>Square</code>（方形）的抽象父类.</li></ul><h2 id="拓展通过接口声明变量来创建对象"><a class="markdownIt-Anchor" href="#拓展通过接口声明变量来创建对象"></a> 拓展(通过接口声明变量来创建对象)</h2><p><strong><code>CopyOnWriteArrayList&lt;&gt;()</code>已经实现了<code>List</code>接口并重写了<code>List</code>里面的方法,那<code>List&lt;String&gt; myList = new CopyOnWriteArrayList&lt;&gt;();</code>还有什么意义</strong></p><p>这是一个非常非常好的问题,它直接触及了面向对象设计中一个极其重要的核心原则,也是区分初级和中高级程序员的一个关键点.</p><p>这个原则就是：<strong>面向接口编程</strong></p><p><code>List&lt;String&gt; myList = new ArrayList&lt;&gt;();</code> 这行代码的两个部分各司其职,缺一不可：</p><ul><li><strong><code>List&lt;String&gt; myList</code></strong> （左边部分 - 变量类型）<ul><li><strong>作用</strong>：定义了**“契约”和“规范”**.它向你的程序承诺,“<code>myList</code> 这个变量只能执行 <code>List</code> 里面含有的方法”.</li><li><strong>目的</strong>：为了代码的<strong>灵活性</strong>和<strong>可维护性</strong>（面向接口编程）.</li></ul></li><li><strong><code>new ArrayList&lt;&gt;()</code></strong> （右边部分 - 对象实例化）<ul><li><strong>作用</strong>：提供了**“具体的实现”和“性能的保障”**.它创建了一个真实存在的、按照 <code>ArrayList</code> 方式工作的对象,对 <code>List</code> 里面含有的方法进行重写.</li><li><strong>目的</strong>：为了让程序能够<strong>实际运行</strong>,并选择符合你需求的<strong>性能特征</strong>（比如快速随机访问）.</li></ul></li></ul><p>简单来说,<code>ArrayList</code> 的用处就是<strong>提供一个具体的、可运行的、具有特定性能优势的工人</strong>,来完成 <code>List</code> 这份合同里规定的所有任务.而你的 <code>myList</code> 变量,就是那个只看合同不关心工人是谁的“项目经理”.</p><h3 id="为了未来的灵活性和可维护性"><a class="markdownIt-Anchor" href="#为了未来的灵活性和可维护性"></a> 为了未来的灵活性和可维护性</h3><p>通过使用接口类型 <code>List</code> 来声明变量,你的代码就不再依赖于某个<strong>具体的实现</strong>（如 <code>CopyOnWriteArrayList</code>）,而仅仅依赖于<strong>抽象的规范</strong>（<code>List</code> 接口）.</p><p><strong>我们来看一个场景：</strong></p><p>假设你正在开发一个用户列表展示功能.一开始,你预估这个列表的读操作会非常频繁,而写操作很少,并且可能存在多线程读取的情况.<code>CopyOnWriteArrayList</code> 是一个绝佳的选择。</p><p>你的代码是这样写的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// V1 版本：使用 CopyOnWriteArrayList</span></span><br><span class="line">List&lt;String&gt; userList = <span class="keyword">new</span> <span class="title class_">CopyOnWriteArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line"><span class="comment">// ... 添加一些用户 ...</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 你有一个方法专门处理这个列表</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">displayUsers</span><span class="params">(List&lt;String&gt; users)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (String user : users) &#123;</span><br><span class="line">        System.out.println(user);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">displayUsers(userList);</span><br></pre></td></tr></table></figure><p>这段代码运行得很好.但是,几个月后,产品需求变更了,现在用户可以频繁地添加和删除好友,写操作变得非常多.<code>CopyOnWriteArrayList</code> 每次写入都复制整个数组,性能急剧下降.此时,一个普通的 <code>ArrayList</code>（在单线程环境下）或 <code>Collections.synchronizedList(new ArrayList&lt;&gt;())</code> 会是更好的选择.</p><p><strong>现在，神奇的事情发生了：</strong></p><p>因为你的变量 <code>userList</code> 和方法 <code>displayUsers</code> 的参数类型都是 <code>List</code>,所以要更换实现,你<strong>只需要修改一行代码</strong>！</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// V2 版本：只需要修改这一行！</span></span><br><span class="line">List&lt;String&gt; userList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(); <span class="comment">// 或者 new LinkedList&lt;&gt;();</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// ... 添加一些用户 ...</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// displayUsers 方法完全不需要做任何改动！</span></span><br><span class="line">displayUsers(userList);</span><br></pre></td></tr></table></figure><p><strong>反过来想，如果你当初写的是：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 耦合于具体实现的“坏”代码</span></span><br><span class="line">CopyOnWriteArrayList&lt;String&gt; userList = <span class="keyword">new</span> <span class="title class_">CopyOnWriteArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">displayUsers</span><span class="params">(CopyOnWriteArrayList&lt;String&gt; users)</span> &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那么当你要更换成 <code>ArrayList</code> 时,你需要修改变量声明、方法参数、以及所有其他使用了 <code>CopyOnWriteArrayList</code> 类型的地方.在一个大型项目中,这会是一场灾难.</p><h3 id="解耦合"><a class="markdownIt-Anchor" href="#解耦合"></a> 解耦合</h3><p>使用 <code>List&lt;String&gt; list = ...</code> 的写法,你的代码被分成了两部分：</p><ul><li><strong>使用者 (Client)</strong>：<code>displayUsers</code> 方法就是使用者.它不关心列表具体是哪种实现,它只关心“我拿到的是一个 <code>List</code>,它有 <code>.get()</code>、<code>.size()</code> 等方法就够了”.</li><li><strong>提供者 (Provider)</strong>：<code>new CopyOnWriteArrayList&lt;&gt;()</code> 是提供者.它提供了 <code>List</code> 功能的一种具体实现.</li></ul><p><code>List</code> 接口就像它们之间的一份<strong>合同</strong>.只要提供者遵守这份合同,使用者就可以放心地工作,完全不用在乎提供者是谁,或者将来会换成谁.</p><h3 id="清晰地表达意图"><a class="markdownIt-Anchor" href="#清晰地表达意图"></a> 清晰地表达意图</h3><p>当你把变量或参数声明为 <code>List</code> 时,你其实在告诉其他阅读你代码的程序员：“我在这里只需要这个对象基本的 <code>List</code> 功能,我不在乎它的底层是数组还是链表,也不在乎它是否线程安全.只要它是个列表就行.” 这使得代码的意图更加清晰.</p><h3 id="什么时候应该使用具体类来声明"><a class="markdownIt-Anchor" href="#什么时候应该使用具体类来声明"></a> 什么时候应该使用具体类来声明？</h3><p>当然,也有例外.当你需要使用<strong>那个具体实现类特有的、而接口中没有定义的方法</strong>时,你就必须使用具体类来声明.</p><p>例如,<code>LinkedList</code> 类有 <code>addFirst()</code> 和 <code>getLast()</code> 方法,但 <code>List</code> 接口没有.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 错误的代码，因为 List 接口没有 addFirst 方法</span></span><br><span class="line">List&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line"><span class="comment">// list.addFirst(&quot;hello&quot;); // 编译错误！</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 正确的代码</span></span><br><span class="line">LinkedList&lt;String&gt; linkedList = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line">linkedList.addFirst(<span class="string">&quot;hello&quot;</span>); <span class="comment">// 正确！</span></span><br></pre></td></tr></table></figure><p>在这种情况下,你就做出了一个权衡：为了使用特殊功能,你牺牲了未来更换实现的灵活性.</p><p><code>List&lt;String&gt; myList = new CopyOnWriteArrayList&lt;&gt;();</code> 的意义在于：</p><p>它遵循了“<strong>面向接口编程</strong>”的黄金法则,为你带来了巨大的<strong>灵活性、可维护性和代码解耦</strong>.这使得你的代码像是由标准的乐高积木搭建而成,可以轻松替换零件；而不是用胶水粘死的、牵一发而动全身的脆弱结构.</p><hr /><h1 id="代码块"><a class="markdownIt-Anchor" href="#代码块"></a> 代码块</h1><h2 id="实例代码块"><a class="markdownIt-Anchor" href="#实例代码块"></a> 实例代码块</h2><p>实例代码块,或称构造代码块,是直接定义在类中的、没有 <code>static</code> 关键字修饰的代码块.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyClass</span> &#123;</span><br><span class="line">    <span class="comment">// 这是一个实例代码块</span></span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;实例代码块执行了&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MyClass</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;构造方法执行了&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><em>核心特性</em></p><ul><li><strong>执行时机</strong>: <strong>每次创建类的实例（对象）时</strong>执行.它的执行顺序在<strong>父类构造方法之后,当前类构造方法之前</strong>.</li><li><strong>主要用途</strong>: 用于初始化实例变量（非静态成员变量）,或者执行所有对象在创建时都需要运行的通用逻辑.它可以被看作是所有构造方法的“公共前缀”,有助于减少构造方法之间的代码重复.</li><li><strong>访问权限</strong>: 可以访问类的<strong>任何成员</strong>,包括实例成员（变量和方法）和静态成员（变量和方法）.因为它在对象创建时运行,所以此时实例成员已经分配了内存.</li></ul><h2 id="静态代码块"><a class="markdownIt-Anchor" href="#静态代码块"></a> 静态代码块</h2><p>静态代码块是使用 <code>static</code> 关键字修饰的、直接定义在类中的代码块.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyClass</span> &#123;</span><br><span class="line">    <span class="comment">// 这是一个静态代码块</span></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;静态代码块执行了&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><em>核心特性</em></p><ul><li><strong>执行时机</strong>: 在 <strong>类第一次被加载到 JVM（Java 虚拟机）时</strong>执行.这通常发生在第一次创建该类的对象、第一次访问该类的静态成员、或通过反射加载该类时.</li><li><strong>执行频率</strong>: <strong>整个程序的生命周期中只执行一次</strong>.</li><li><strong>主要用途</strong>: 用于对类级别的静态成员（<code>static</code> 变量）进行复杂的初始化,或者执行一些类级别的、只需要进行一次的设置操作（如加载数据库驱动、初始化配置文件等）.</li><li><strong>访问权限</strong>: <strong>只能访问类的静态成员（静态变量和静态方法）</strong>.它<strong>不能</strong>直接访问实例成员（非静态变量和方法）,因为在静态代码块执行时,类的任何实例（对象）都还没有被创建,实例成员尚未分配内存.</li></ul><hr /><table><thead><tr><th>特性</th><th>静态代码块 (Static Block)</th><th>实例代码块 (Instance Block)</th></tr></thead><tbody><tr><td><strong>关键字</strong></td><td>使用 <code>static &#123;&#125;</code> 定义</td><td>直接使用 <code>&#123;&#125;</code> 定义</td></tr><tr><td><strong>执行时机</strong></td><td>类第一次加载到 JVM 时执行</td><td>每次创建类的实例（<code>new</code>一个对象）时执行</td></tr><tr><td><strong>执行频率</strong></td><td><strong>仅执行一次</strong></td><td>每创建一个对象就<strong>执行一次</strong></td></tr><tr><td><strong>执行顺序</strong></td><td><strong>最先执行</strong>，在任何对象创建之前</td><td>在构造方法之前执行</td></tr><tr><td><strong>访问能力</strong></td><td><strong>只能</strong>访问静态成员（变量和方法）</td><td><strong>可以</strong>访问静态成员和实例成员</td></tr><tr><td><strong>主要用途</strong></td><td>对静态变量进行初始化，或执行类级别的、一次性的准备工作（如加载驱动）</td><td>对实例变量进行初始化，提取多个构造方法中的公共代码，执行对象级别的通用逻辑</td></tr></tbody></table><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CodeBlockExample</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 静态变量</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">String</span> <span class="variable">staticField</span> <span class="operator">=</span> <span class="string">&quot;初始静态字段&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 实例变量</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">String</span> <span class="variable">instanceField</span> <span class="operator">=</span> <span class="string">&quot;初始实例字段&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 静态代码块</span></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;1. 静态代码块执行&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;   - 静态字段值: &quot;</span> + staticField);</span><br><span class="line">        <span class="comment">// System.out.println(instanceField); // 编译错误！不能访问实例字段</span></span><br><span class="line">        staticField = <span class="string">&quot;静态代码块修改后的静态字段&quot;</span>;</span><br><span class="line">        System.out.println(<span class="string">&quot;   - 修改后静态字段值: &quot;</span> + staticField);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 实例代码块</span></span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;2. 实例代码块执行&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;   - 静态字段值: &quot;</span> + staticField);</span><br><span class="line">        System.out.println(<span class="string">&quot;   - 实例字段值: &quot;</span> + instanceField);</span><br><span class="line">        <span class="built_in">this</span>.instanceField = <span class="string">&quot;实例代码块修改后的实例字段&quot;</span>;</span><br><span class="line">        System.out.println(<span class="string">&quot;   - 修改后实例字段值: &quot;</span> + <span class="built_in">this</span>.instanceField);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构造方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">CodeBlockExample</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;3. 构造方法执行&quot;</span>);</span><br><span class="line">        System.out.println(<span class="string">&quot;   - 实例字段值: &quot;</span> + <span class="built_in">this</span>.instanceField);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;--- main 方法开始 ---&quot;</span>);</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;\n--- 准备创建第一个实例 ---&quot;</span>);</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">CodeBlockExample</span>();</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;\n--- 准备创建第二个实例 ---&quot;</span>);</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">CodeBlockExample</span>();</span><br><span class="line">        </span><br><span class="line">        System.out.println(<span class="string">&quot;\n--- main 方法结束 ---&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">1. 静态代码块执行</span><br><span class="line">   - 静态字段值: 初始静态字段</span><br><span class="line">   - 修改后静态字段值: 静态代码块修改后的静态字段</span><br><span class="line">--- main 方法开始 ---</span><br><span class="line"></span><br><span class="line">--- 准备创建第一个实例 ---</span><br><span class="line">2. 实例代码块执行</span><br><span class="line">   - 静态字段值: 静态代码块修改后的静态字段</span><br><span class="line">   - 实例字段值: 初始实例字段</span><br><span class="line">   - 修改后实例字段值: 实例代码块修改后的实例字段</span><br><span class="line">3. 构造方法执行</span><br><span class="line">   - 实例字段值: 实例代码块修改后的实例字段</span><br><span class="line"></span><br><span class="line">--- 准备创建第二个实例 ---</span><br><span class="line">2. 实例代码块执行</span><br><span class="line">   - 静态字段值: 静态代码块修改后的静态字段</span><br><span class="line">   - 实例字段值: 初始实例字段</span><br><span class="line">   - 修改后实例字段值: 实例代码块修改后的实例字段</span><br><span class="line">3. 构造方法执行</span><br><span class="line">   - 实例字段值: 实例代码块修改后的实例字段</span><br><span class="line"></span><br><span class="line">--- main 方法结束 ---</span><br></pre></td></tr></table></figure><ol><li><strong>静态代码块只执行了一次</strong>：在 <code>main</code> 方法开始执行,<code>CodeBlockExample</code> 类被加载时,静态代码块就立刻执行了,并且之后再也没有执行过.</li><li><strong>实例代码块和构造方法执行了两次</strong>：每次调用 <code>new CodeBlockExample()</code> 创建新对象时,实例代码块和构造方法都会按顺序执行一次.</li><li><strong>执行顺序</strong>：对于单个对象的创建过程,顺序是 <strong>实例代码块 -&gt; 构造方法</strong>.</li><li><strong>访问权限</strong>：静态代码块中无法访问 <code>instanceField</code>,而实例代码块中可以自由访问 <code>staticField</code> 和 <code>instanceField</code>.</li></ol><hr /><h1 id="内部类"><a class="markdownIt-Anchor" href="#内部类"></a> 内部类</h1><p><em>优点：</em></p><ol><li><strong>逻辑分组与封装</strong>：如果一个类只被某一个其他类使用，那么将它定义为内部类可以从逻辑上将它们组织在一起。这隐藏了实现细节，使得代码结构更清晰。</li><li><strong>增强封装性</strong>：内部类可以访问其外部类的所有成员，包括私有成员（private fields and methods）。这提供了一种特殊的访问权限，可以实现更紧密的协作，而无需将外部类成员声明为 <code>public</code> 或 <code>protected</code>。</li><li><strong>代码更简洁可读</strong>：尤其是在事件处理（如 GUI 编程）或实现某些设计模式（如迭代器模式）时，使用内部类（特别是匿名内部类）可以让代码更紧凑、更贴近使用场景。</li></ol><h2 id="成员内部类"><a class="markdownIt-Anchor" href="#成员内部类"></a> 成员内部类</h2><p>这是最常见的内部类形式，它像外部类的成员变量一样，定义在类的内部、方法的外部。</p><p><em>特点：</em></p><ul><li><strong>依赖外部类实例</strong>：成员内部类的实例必须依附于一个外部类的实例,你不能在没有外部类对象的情况下创建成员内部类对象.</li><li><strong>访问权限</strong>：它可以无条件地访问外部类的所有成员（包括静态和非静态，私有和公有）.</li><li><strong>不能有静态成员</strong>：成员内部类中不能定义任何静态（<code>static</code>）方法或变量,除非是 <code>static final</code> 的常量.</li><li><strong><code>this</code> 关键字</strong>：在内部类中,<code>this</code> 指向内部类自身的实例,要引用外部类的实例需要使用 <code>OuterClassName.this</code>.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Outer</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">String</span> <span class="variable">outerField</span> <span class="operator">=</span> <span class="string">&quot;Outer Field&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">String</span> <span class="variable">staticOuterField</span> <span class="operator">=</span> <span class="string">&quot;Static Outer Field&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 成员内部类</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Inner</span> &#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="type">String</span> <span class="variable">innerField</span> <span class="operator">=</span> <span class="string">&quot;Inner Field&quot;</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">display</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="comment">// 直接访问外部类的私有成员</span></span><br><span class="line">            System.out.println(<span class="string">&quot;Accessing outer field: &quot;</span> + outerField);</span><br><span class="line">            <span class="comment">// 直接访问外部类的静态成员</span></span><br><span class="line">            System.out.println(<span class="string">&quot;Accessing static outer field: &quot;</span> + staticOuterField);</span><br><span class="line">            <span class="comment">// 访问内部类自己的成员</span></span><br><span class="line">            System.out.println(<span class="string">&quot;Accessing inner field: &quot;</span> + <span class="built_in">this</span>.innerField);</span><br><span class="line">            <span class="comment">// 访问外部类的实例</span></span><br><span class="line">            System.out.println(<span class="string">&quot;Accessing outer instance field using &#x27;this&#x27;: &quot;</span> + Outer.<span class="built_in">this</span>.outerField);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 创建外部类实例</span></span><br><span class="line">        <span class="type">Outer</span> <span class="variable">outer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Outer</span>();</span><br><span class="line">        <span class="comment">// 2. 通过外部类实例创建内部类实例</span></span><br><span class="line">        Outer.<span class="type">Inner</span> <span class="variable">inner</span> <span class="operator">=</span> outer.<span class="keyword">new</span> <span class="title class_">Inner</span>();</span><br><span class="line">        </span><br><span class="line">        inner.display();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>如何实例化：</strong> <code>OuterClass.InnerClass innerObject = outerObject.new InnerClass();</code></p><h2 id="静态内部类-2"><a class="markdownIt-Anchor" href="#静态内部类-2"></a> 静态内部类</h2><p>静态内部类使用 <code>static</code> 关键字修饰.它与外部类的关系更像是“命名空间”的归属关系,而不是成员关系.</p><p><em>特点：</em></p><ul><li><strong>不依赖外部类实例</strong>：创建静态内部类的实例不需要外部类的实例.</li><li><strong>访问权限</strong>：它只能访问外部类的静态（<code>static</code>）成员,不能直接访问外部类的非静态（实例）成员.</li><li><strong>可以有静态成员</strong>：静态内部类可以拥有自己的静态和非静态成员.</li><li><strong>行为类似普通类</strong>：除了定义的位置和对外部类静态成员的访问权限外,它几乎和一个普通的顶级类一样.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Outer</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">String</span> <span class="variable">outerField</span> <span class="operator">=</span> <span class="string">&quot;Outer Field&quot;</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="type">String</span> <span class="variable">staticOuterField</span> <span class="operator">=</span> <span class="string">&quot;Static Outer Field&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 静态内部类</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title class_">StaticInner</span> &#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="type">String</span> <span class="variable">innerField</span> <span class="operator">=</span> <span class="string">&quot;Static Inner Field&quot;</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">display</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="comment">// 错误！不能直接访问外部类的非静态成员</span></span><br><span class="line">            <span class="comment">// System.out.println(outerField); </span></span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 可以访问外部类的静态成员</span></span><br><span class="line">            System.out.println(<span class="string">&quot;Accessing static outer field: &quot;</span> + staticOuterField);</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 访问自己的成员</span></span><br><span class="line">            System.out.println(<span class="string">&quot;Accessing inner field: &quot;</span> + innerField);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 直接创建静态内部类的实例，无需外部类实例</span></span><br><span class="line">        Outer.<span class="type">StaticInner</span> <span class="variable">staticInner</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Outer</span>.StaticInner();</span><br><span class="line">        </span><br><span class="line">        staticInner.display();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>如何实例化：</strong> <code>OuterClass.StaticInnerClass nestedObject = new OuterClass.StaticInnerClass();</code></p><h2 id="局部内部类"><a class="markdownIt-Anchor" href="#局部内部类"></a> 局部内部类</h2><p>局部内部类是定义在一个方法或者一个作用域（如 <code>if</code> 语句块）内部的类.</p><p><em>特点：</em></p><ul><li><strong>作用域极小</strong>：它的生命周期和可见性仅限于定义它的方法或块中.</li><li><strong>不能有访问修饰符</strong>：不能使用 <code>public</code>,<code>private</code> 等修饰符,也不能使用 <code>static</code> 修饰.</li><li><strong>访问外部成员</strong>：和成员内部类一样,它可以访问外部类的所有成员.</li><li><strong>访问局部变量</strong>：它可以访问所在方法中的局部变量,但这些变量必须是 <code>final</code> 或“事实上的 final”（effectively final,即变量在初始化后没有被再次赋值）.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Outer</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">String</span> <span class="variable">outerField</span> <span class="operator">=</span> <span class="string">&quot;Outer Field&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">someMethod</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">final</span> <span class="type">String</span> <span class="variable">localVariable</span> <span class="operator">=</span> <span class="string">&quot;Local Variable&quot;</span>; <span class="comment">// 必须是 final 或 effectively final</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 局部内部类</span></span><br><span class="line">        <span class="keyword">class</span> <span class="title class_">LocalInner</span> &#123;</span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">display</span><span class="params">()</span> &#123;</span><br><span class="line">                <span class="comment">// 访问外部类成员</span></span><br><span class="line">                System.out.println(<span class="string">&quot;Accessing outer field: &quot;</span> + outerField);</span><br><span class="line">                <span class="comment">// 访问方法内的局部变量</span></span><br><span class="line">                System.out.println(<span class="string">&quot;Accessing local variable: &quot;</span> + localVariable);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 在方法内创建并使用局部内部类</span></span><br><span class="line">        <span class="type">LocalInner</span> <span class="variable">localInner</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">LocalInner</span>();</span><br><span class="line">        localInner.display();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Outer</span> <span class="variable">outer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Outer</span>();</span><br><span class="line">        outer.someMethod();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="匿名内部类"><a class="markdownIt-Anchor" href="#匿名内部类"></a> 匿名内部类</h2><p>匿名内部类是没有名字的局部内部类.它通常用于快速创建某个类或接口的子类实例,非常适合“一次性使用”的场景.</p><p><em>特点：</em></p><ul><li><strong>没有名字</strong>：它在定义的同时就创建了实例.</li><li><strong>语法</strong>：通常是 <code>new SuperType() &#123; ... &#125;</code> 或 <code>new Interface() &#123; ... &#125;</code> 的形式.</li><li><strong>没有构造函数</strong>：因为它没有名字,所以不能定义构造函数,如果需要初始化,可以使用实例初始化块.</li><li><strong>适用场景</strong>：最常用于实现接口（如 <code>Runnable</code>, <code>Comparator</code>）或作为事件监听器.</li><li><strong>访问规则</strong>：和局部内部类一样,可以访问外部类的所有成员,以及所在方法中 <code>final</code> 或“事实上的 final”的局部变量.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 使用接口</span></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">Greeting</span> &#123;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">sayHello</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Outer</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">startThread</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 使用匿名内部类实现 Runnable 接口</span></span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">Thread</span>(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;A new thread is running...&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">greetSomeone</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 使用匿名内部类实现 Greeting 接口</span></span><br><span class="line">        <span class="type">Greeting</span> <span class="variable">englishGreeting</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Greeting</span>() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sayHello</span><span class="params">()</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;Hello!&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        englishGreeting.sayHello();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Outer</span> <span class="variable">outer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Outer</span>();</span><br><span class="line">        outer.startThread();</span><br><span class="line">        outer.greetSomeone();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><table><thead><tr><th>特性</th><th>成员内部类</th><th>静态内部类</th><th>局部内部类</th><th>匿名内部类</th></tr></thead><tbody><tr><td><strong>定义位置</strong></td><td>类的成员位置</td><td>类的成员位置</td><td>方法或代码块内</td><td>表达式中（new之后）</td></tr><tr><td><strong><code>static</code> 关键字</strong></td><td>不能使用</td><td><strong>必须使用</strong></td><td>不能使用</td><td>不能使用</td></tr><tr><td><strong>访问外部类成员</strong></td><td>所有成员</td><td><strong>仅静态成员</strong></td><td>所有成员</td><td>所有成员</td></tr><tr><td><strong>访问局部变量</strong></td><td>N/A</td><td>N/A</td><td>必须是<code>final</code>或<code>effectively final</code></td><td>必须是<code>final</code>或<code>effectively final</code></td></tr><tr><td><strong>创建实例方式</strong></td><td><code>outer.new Inner()</code></td><td><code>new Outer.Inner()</code></td><td>在方法内 <code>new Inner()</code></td><td><code>new Interface()&#123;...&#125;</code></td></tr><tr><td><strong>依赖外部实例</strong></td><td><strong>是</strong></td><td><strong>否</strong></td><td>是</td><td>是</td></tr><tr><td><strong>拥有静态成员</strong></td><td>否 (除了<code>static final</code>)</td><td><strong>是</strong></td><td>否</td><td>否</td></tr><tr><td><strong>应用场景</strong></td><td>创建与外部类实例状态紧密相关的辅助类,如实现迭代器（Iterator）.</td><td>仅为逻辑上组织,与外部类实例无关,如 <code>Map.Entry</code> 或建造者模式（Builder Pattern）.</td><td>在方法内需要一个临时的、具名的类,且实现比匿名类复杂时使用（较少用）.</td><td>用于实现事件监听器、<code>Runnable</code>、<code>Comparator</code> 等只需一次性使用的简单接口或类。</td></tr></tbody></table><hr /><h1 id="函数式编程"><a class="markdownIt-Anchor" href="#函数式编程"></a> 函数式编程</h1><p><strong>函数式编程</strong>是一种编程范式,就像面向对象编程一样,它将计算机运算视为数学函数的求值,并避免使用<strong>可变状态和副作用</strong>.</p><p>简单来说,它的核心思想是：<strong>像对待数学函数一样对待程序中的函数</strong>.</p><p>我们来分解一下这个概念的关键特征：</p><p><em>a. 函数是“一等公民”（First-Class Citizens）</em></p><p>这是函数式编程最核心的特点。它意味着函数和其他数据类型（如 <code>int</code>、<code>String</code>、对象）处于同等地位.具体表现为：</p><ul><li><strong>可以作为变量赋值</strong>：<code>Function&lt;Integer, Integer&gt; square = x -&gt; x * x;</code></li><li><strong>可以作为参数传递</strong>：将一个函数传递给另一个方法.</li><li><strong>可以作为返回值返回</strong>：一个方法可以返回一个函数.</li></ul><p>Java 通过<strong>函数式接口</strong>和 <strong>Lambda 表达式</strong>实现了这一点.</p><p><em>b. 纯函数（Pure Functions）</em></p><p>纯函数是指满足以下两个条件的函数：</p><ol><li><strong>无副作用</strong>：函数不会修改其作用域之外的任何状态.例如,它不会修改全局变量、类的成员变量,也不会进行 I/O 操作（如打印到控制台、读写文件）.</li><li>**引用透明性：**对于相同的输入,永远返回相同的输出.函数的结果只依赖于其输入参数,而不依赖于任何外部状态.</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 纯函数：结果只依赖于输入 a 和 b</span></span><br><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">sum</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 非纯函数：因为它修改了外部状态 (total)</span></span><br><span class="line"><span class="keyword">private</span> <span class="type">int</span> <span class="variable">total</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"><span class="keyword">public</span> <span class="type">int</span> <span class="title function_">addToTotal</span><span class="params">(<span class="type">int</span> a)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.total += a; <span class="comment">// 副作用：修改了成员变量</span></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">this</span>.total;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>纯函数的好处是巨大的：</p><ul><li><strong>可预测性</strong>：给定输入,输出总是确定的,非常容易理解和推理.</li><li><strong>易于测试</strong>：不需要复杂的 Mock 或环境设置,只需提供输入并验证输出即可.</li><li><strong>并行/并发安全</strong>：因为纯函数不共享和修改状态,所以它们天然是线程安全的,可以安全地并行执行.</li></ul><p><em>c. 不可变性（Immutability）</em></p><p>函数式编程推崇创建不可变的数据结构.一旦创建了数据,就不再修改它,如果需要修改,应该是创建一个包含新值的新对象,而不是在原地修改旧对象.</p><p>这避免了因状态变化而引起的复杂性和错误,尤其是在多线程环境中.Java 中的 <code>String</code> 类就是一个典型的不可变对象.</p><p><em>d. 声明式编程（Declarative） vs. 命令式编程（Imperative）</em></p><ul><li><strong>命令式编程</strong>：关注**“如何做”（How）**.你需要一步步地告诉计算机执行的具体指令,传统的 <code>for</code> 循环就是典型的命令式编程.</li><li><strong>声明式编程</strong>：关注**“做什么”（What）**.你只描述你想要的结果,而不关心具体的实现步骤,函数式编程就是一种声明式编程.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; numbers = Arrays.asList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 命令式编程 (Imperative)</span></span><br><span class="line">List&lt;Integer&gt; evenDoubled = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span> (Integer number : numbers) &#123;</span><br><span class="line">    <span class="keyword">if</span> (number % <span class="number">2</span> == <span class="number">0</span>) &#123;</span><br><span class="line">        evenDoubled.add(number * <span class="number">2</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明式编程 (Declarative / Functional)</span></span><br><span class="line">List&lt;Integer&gt; evenDoubledFunctional = numbers.stream()</span><br><span class="line">                                             .filter(n -&gt; n % <span class="number">2</span> == <span class="number">0</span>) <span class="comment">// 做什么：筛选偶数</span></span><br><span class="line">                                             .map(n -&gt; n * <span class="number">2</span>)         <span class="comment">// 做什么：乘以 2</span></span><br><span class="line">                                             .collect(Collectors.toList());</span><br></pre></td></tr></table></figure><hr /><h2 id="lambda-表达式"><a class="markdownIt-Anchor" href="#lambda-表达式"></a> Lambda 表达式</h2><p>Lambda 表达式(闭包)是<code>java8</code>的新特性,Lambda运行将函数作为一个方法的参数,也就是函数作为参数传递到方法中,使用lambda表达式可以让代码更加简洁.</p><p>Lambda表达式的使用场景: <strong>用以简化匿名内部类和接口实现.</strong></p><p>关于接口实现,可以有很多种方式来实现.例如: 设计接口的实现类,使用匿名内部类.但是lambda表达式,比这两种方式都简单.</p><h3 id="函数式接口"><a class="markdownIt-Anchor" href="#函数式接口"></a> 函数式接口</h3><p>虽然说,lambda表达式可以在⼀定程度上简化接口的实现.但是,并不是所有的接口都可以使用lambda表达式来简洁实现的.</p><p>lambda表达式毕竟只是⼀个匿名方法,当实现的接口中的方法过多或者多少的时候,lambda表达式都是不适用的.</p><p><strong>lambda表达式只能实现函数式接口。</strong></p><p>如果说,⼀个接口中，要求实现类必须实现的抽象方法,有且只有⼀个！这样的接口,就是函数式接口.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//有且只有一个实现类必须要实现的抽象方法，所以是函数式接口</span></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">Test</span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">test</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="functionalinterface"><a class="markdownIt-Anchor" href="#functionalinterface"></a> <code>@FunctionalInterface</code></h3><p>是⼀个注解,用在接口之前,判断这个接口是否是⼀个函数式接口.如果是函数式接口,没有任何问题.如果不是函数式接口,则会报错.功能类似于 <code>@Override</code>.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">Test</span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">test</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="lambda-表达式的核心语法"><a class="markdownIt-Anchor" href="#lambda-表达式的核心语法"></a> Lambda 表达式的核心语法</h3><p>Lambda 表达式的语法非常紧凑,其基本结构如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(parameters) -&gt; expression</span><br></pre></td></tr></table></figure><p>或者</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">(parameters) -&gt; &#123; statements; &#125;</span><br></pre></td></tr></table></figure><p>它由三个部分组成：</p><ol><li><strong>参数列表 (parameters)</strong>：方法的参数.<ul><li>如果没有参数,必须使用一对空括号 <code>()</code>.</li><li>如果只有一个参数,可以省略括号.</li><li>参数的类型通常可以由编译器根据上下文推断出来,所以大部分情况下可以省略.</li></ul></li><li><strong>箭头操作符 (-&gt;)</strong>：也称为 Lambda 操作符,它将参数列表和 Lambda 主体分开.</li><li><strong>Lambda 主体 (body)</strong>：<ul><li>如果主体只有<strong>一条语句</strong>,可以省略大括号 <code>&#123;&#125;</code>和<code>return</code>.表达式的结果会自动作为返回值.</li><li>如果主体包含<strong>多条语句</strong>,则必须使用大括号 <code>&#123;&#125;</code> 将它们包裹起来,并且如果方法需要返回值,必须显式使用 <code>return</code> 语句.</li></ul></li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 未使用 Lambda (通过匿名内部类)</span></span><br><span class="line"><span class="comment">// 整个结构非常冗长,核心逻辑只有一行 s.startsWith(&quot;B&quot;),Predicate是函数式接口</span></span><br><span class="line"></span><br><span class="line">names.removeIf(<span class="keyword">new</span> <span class="title class_">Predicate</span>&lt;String&gt;() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">boolean</span> <span class="title function_">test</span><span class="params">(String s)</span> &#123;</span><br><span class="line">        <span class="comment">// 如果字符串以 &quot;B&quot; 开头，返回 true，该元素将被移除</span></span><br><span class="line">        <span class="keyword">return</span> s.startsWith(<span class="string">&quot;B&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">System.out.println(<span class="string">&quot;未使用 Lambda 过滤后: &quot;</span> + names);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 Lambda 表达式</span></span><br><span class="line"><span class="comment">// s -&gt; s.startsWith(&quot;B&quot;) 就是对 Predicate 接口中 test 方法的实现。</span></span><br><span class="line"><span class="comment">// s 是参数，-&gt; 右边是方法体。</span></span><br><span class="line"></span><br><span class="line">names.removeIf(s -&gt; s.startsWith(<span class="string">&quot;B&quot;</span>));</span><br><span class="line"></span><br><span class="line">System.out.println(<span class="string">&quot;使用 Lambda 过滤后: &quot;</span> + names);</span><br><span class="line"></span><br></pre></td></tr></table></figure><hr /><h2 id="方法引用"><a class="markdownIt-Anchor" href="#方法引用"></a> 方法引用</h2><p>方法引用是 Java 8 中与 Lambda 表达式一同引入的一项重要特性,你可以把它看作是 Lambda 表达式的一种<strong>语法糖</strong>.当你想要使用的 Lambda 表达式的实现恰好是调用一个<strong>已经存在的方法</strong>时,可以用方法引用来进一步简化代码,使其更加简洁、清晰和可读.</p><p>方法引用使用 <code>::</code>（双冒号）操作符.<code>::</code> 的<strong>左边是类名或对象名,右边是方法名或 <code>new</code> 关键字.</strong></p><p><strong>使用 Lambda 表达式：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; names = Arrays.asList(<span class="string">&quot;Alice&quot;</span>, <span class="string">&quot;Bob&quot;</span>, <span class="string">&quot;Charlie&quot;</span>);</span><br><span class="line">names.forEach(s -&gt; System.out.println(s));</span><br></pre></td></tr></table></figure><p>观察这个 Lambda 表达式 <code>s -&gt; System.out.println(s)</code>.它所做的唯一一件事就是：接收一个参数 <code>s</code>,然后立刻将这个 <code>s</code> 作为参数传递给 <code>System.out.println()</code> 方法.</p><p>在这种“<strong>参数只是简单地传递给另一个方法</strong>”的情况下,我们就可以使用方法引用来让代码变得更干净.</p><p><strong>使用方法引用：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; names = Arrays.asList(<span class="string">&quot;Alice&quot;</span>, <span class="string">&quot;Bob&quot;</span>, <span class="string">&quot;Charlie&quot;</span>);</span><br><span class="line">names.forEach(System.out::println);</span><br></pre></td></tr></table></figure><p><code>System.out::println</code> 就是一个方法引用,它清晰地表达了“对列表中的每个元素,都调用 <code>System.out</code> 对象的 <code>println</code> 方法”这个意图.</p><h3 id="静态方法引用"><a class="markdownIt-Anchor" href="#静态方法引用"></a> 静态方法引用</h3><ul><li><strong>语法</strong>：<code>ClassName::staticMethodName</code></li><li><strong>说明</strong>：如果 Lambda 表达式的主体只是调用一个静态方法,并且 Lambda 的参数列表与该静态方法的参数列表相匹配.</li></ul><p><strong>示例：</strong> 将一个字符串列表转换为整数列表,</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; numbersAsStrings = Arrays.asList(<span class="string">&quot;1&quot;</span>, <span class="string">&quot;2&quot;</span>, <span class="string">&quot;3&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 Lambda 表达式</span></span><br><span class="line">List&lt;Integer&gt; numbers = numbersAsStrings.stream()</span><br><span class="line">                                        .map(s -&gt; Integer.parseInt(s))</span><br><span class="line">                                        .collect(Collectors.toList());</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用静态方法引用</span></span><br><span class="line">List&lt;Integer&gt; numbersRef = numbersAsStrings.stream()</span><br><span class="line">                                           .map(Integer::parseInt)</span><br><span class="line">                                           .collect(Collectors.toList());</span><br><span class="line"></span><br><span class="line">System.out.println(numbersRef); <span class="comment">// 输出: [1, 2, 3]</span></span><br></pre></td></tr></table></figure><p>这里的 <code>Integer::parseInt</code> 就等同于 <code>s -&gt; Integer.parseInt(s)</code>。</p><hr /><h3 id="特定对象的实例方法引用"><a class="markdownIt-Anchor" href="#特定对象的实例方法引用"></a> 特定对象的实例方法引用</h3><ul><li><strong>语法</strong>：<code>instance::instanceMethodName</code></li><li><strong>说明</strong>：如果 Lambda 表达式的主体只是调用一个<strong>已经存在</strong>的对象的实例方法,并且 Lambda 的参数与该实例方法的参数相匹配.</li></ul><p><strong>示例：</strong> 我们回到开头的打印例子.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; names = Arrays.asList(<span class="string">&quot;Alice&quot;</span>, <span class="string">&quot;Bob&quot;</span>, <span class="string">&quot;Charlie&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// `System.out` 是 PrintStream 类的一个已经存在的实例对象</span></span><br><span class="line"><span class="type">PrintStream</span> <span class="variable">out</span> <span class="operator">=</span> System.out;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 Lambda 表达式</span></span><br><span class="line">names.forEach(s -&gt; out.println(s));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用实例方法引用</span></span><br><span class="line">names.forEach(out::println);</span><br></pre></td></tr></table></figure><p>这里的 <code>System.out::println</code> 就是 <code>out::println</code>，因为 <code>System.out</code> 是一个已经存在的对象。</p><hr /><h3 id="特定类型的任意对象的实例方法引用"><a class="markdownIt-Anchor" href="#特定类型的任意对象的实例方法引用"></a> 特定类型的任意对象的实例方法引用</h3><ul><li><strong>语法</strong>：<code>ClassName::instanceMethodName</code></li><li><strong>说明</strong>：这是最特殊的一种.当 Lambda 表达式的<strong>第一个参数</strong>是实例方法的调用者,并且<strong>后续参数</strong>（如果有的话）被传递给该实例方法时,可以使用这种引用.</li></ul><p><strong>示例：</strong> 将一个字符串列表全部转为大写.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; names = Arrays.asList(<span class="string">&quot;alice&quot;</span>, <span class="string">&quot;bob&quot;</span>, <span class="string">&quot;charlie&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 Lambda 表达式</span></span><br><span class="line">List&lt;String&gt; upperNames = names.stream()</span><br><span class="line">                               .map(s -&gt; s.toUpperCase())</span><br><span class="line">                               .collect(Collectors.toList());</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用方法引用</span></span><br><span class="line">List&lt;String&gt; upperNamesRef = names.stream()</span><br><span class="line">                                  .map(String::toUpperCase)</span><br><span class="line">                                  .collect(Collectors.toList());</span><br><span class="line"></span><br><span class="line">System.out.println(upperNamesRef); <span class="comment">// 输出: [ALICE, BOB, CHARLIE]</span></span><br></pre></td></tr></table></figure><p>这里,<code>map</code> 方法接收的 Lambda <code>s -&gt; s.toUpperCase()</code> 中,第一个参数 <code>s</code> 正好是 <code>toUpperCase()</code> 方法的调用者.因此,它可以被简化为 <code>String::toUpperCase</code>.</p><p><strong>另一个例子：</strong> 排序</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">String[] names = &#123;<span class="string">&quot;Charlie&quot;</span>, <span class="string">&quot;Alice&quot;</span>, <span class="string">&quot;Bob&quot;</span>&#125;;</span><br><span class="line"><span class="comment">// Lambda: (s1, s2) -&gt; s1.compareToIgnoreCase(s2)</span></span><br><span class="line">Arrays.sort(names, String::compareToIgnoreCase);</span><br></pre></td></tr></table></figure><p>这里,第一个参数 <code>s1</code> 是方法的调用者,第二个参数 <code>s2</code> 是方法的参数,完美匹配.</p><hr /><h3 id="构造方法引用"><a class="markdownIt-Anchor" href="#构造方法引用"></a> 构造方法引用</h3><ul><li><strong>语法</strong>：<code>ClassName::new</code></li><li><strong>说明</strong>：如果 Lambda 表达式的主体只是创建一个新对象,那么可以使用构造方法引用.</li></ul><p><strong>示例：</strong> 将一个字符串列表的内容复制到一个新的 <code>ArrayList</code> 中.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; names = Arrays.asList(<span class="string">&quot;Alice&quot;</span>, <span class="string">&quot;Bob&quot;</span>, <span class="string">&quot;Charlie&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 Lambda 表达式</span></span><br><span class="line">Supplier&lt;List&lt;String&gt;&gt; listSupplier = () -&gt; <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">List&lt;String&gt; newNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(names);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用构造方法引用 (常用于 Stream 的 collect 操作)</span></span><br><span class="line">List&lt;String&gt; copiedNames = names.stream()</span><br><span class="line">                                .collect(Collectors.toCollection(ArrayList::<span class="keyword">new</span>));</span><br><span class="line"></span><br><span class="line">System.out.println(copiedNames);</span><br></pre></td></tr></table></figure><p>这里的 <code>ArrayList::new</code> 就等同于 <code>() -&gt; new ArrayList&lt;&gt;()</code>，它提供了一个创建 <code>ArrayList</code> 实例的功能。</p><ul><li>方法引用是 Lambda 表达式的简化写法，让代码更具可读性。</li><li>当你发现你的 Lambda 表达式 <code>(...) -&gt; ClassName.someMethod(...)</code> 或 <code>(...) -&gt; object.someMethod(...)</code> 只是在简单地调用一个已存在的方法时，就应该考虑使用方法引用。</li><li>它和 Lambda 表达式一样，都必须在需要<strong>函数式接口</strong>的上下文中使用。编译器会根据函数式接口的抽象方法签名来推断并匹配对应的方法引用。</li></ul><hr /><h1 id="string类"><a class="markdownIt-Anchor" href="#string类"></a> String类</h1><p><code>String</code>代表字符串,它的对象可以封装字符串数据,并提供了很多方法对字符串进行处理.</p><p><strong>String创建字符串的方式</strong></p><ul><li>方式一: Java程序中的所有字符串文字(例如<code>&quot;abc&quot;</code>)都为此类的对象.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> <span class="string">&quot;小何&quot;</span>;</span><br><span class="line"><span class="type">String</span> <span class="variable">schoolName</span> <span class="operator">=</span> <span class="string">&quot;程序员&quot;</span>;</span><br></pre></td></tr></table></figure><p>只要是以&quot;…&quot;方式写出的字符串对象,字符串会存储到字符串常量池,且相同内容的字符串只存储一份.</p><ul><li>方式二: 调用<code>String</code>类的构造器初始化字符串对象.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span>[] chars = &#123;<span class="string">&#x27;h&#x27;</span>,<span class="string">&#x27;e&#x27;</span>,<span class="string">&#x27;b&#x27;</span>&#125;;</span><br><span class="line"><span class="type">String</span> <span class="variable">s4</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">String</span>(chars);</span><br></pre></td></tr></table></figure><p>通过new方式创建字符串对象,每new一次都会产生一个新的对象放在堆内存中,他们在栈内存的地址不相同.</p><p><strong>字符串对象的内容比较</strong></p><p>千万不要用<code>==</code>,<code>==</code>默认比较地址,字符串对象内容一样时地址不一定一样(方式二).判断字符串内容,建议大家用<code>String</code>提供的<code>equals</code>方法,只关心内容一样,就返回<code>true</code>,不关心地址.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span>(okLoginName.equals(loginName))&#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;恭喜你,登录成功!&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h1 id="异常"><a class="markdownIt-Anchor" href="#异常"></a> 异常</h1><p>在 Java 中,<strong>异常</strong>是在程序执行期间发生的、中断了程序正常指令流的事件.当一个方法中发生错误时,该方法会创建一个<strong>异常对象</strong>并将其<strong>抛出</strong> (throw).这个异常对象包含了关于错误的详细信息,例如错误的类型和程序当时的状态.然后,Java 运行时系统会负责寻找能够处理这个异常的代码块,这个过程被称为<strong>捕获异常</strong> (catch an exception)…</p><h2 id="java-异常的层次结构"><a class="markdownIt-Anchor" href="#java-异常的层次结构"></a> Java 异常的层次结构</h2><p>要理解 Java 的异常,首先需要了解其层次结构.所有异常类型都是从 <code>java.lang.Throwable</code> 类派生出来的.<code>Throwable</code> 类有两个主要的子类：<code>Error</code> 和 <code>Exception</code>.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">        java.lang.Throwable</span><br><span class="line">          /              \</span><br><span class="line">         /                \</span><br><span class="line">java.lang.Error        java.lang.Exception</span><br><span class="line">                         /              \ </span><br><span class="line">                        /                \</span><br><span class="line">       Checked Exceptions               RuntimeException</span><br><span class="line">          (编译时异常)                      (运行时异常)</span><br><span class="line">       /              \                 /              \</span><br><span class="line"> IOException    SQLException    NullPointerException  ArrayIndexOutOfBoundsException</span><br><span class="line">           ...etc                            ...etc</span><br></pre></td></tr></table></figure><ul><li><strong><code>Error</code> (错误)</strong>：表示 Java 运行时系统内部的错误和资源耗尽等严重问题.这些问题通常是应用程序无法处理和恢复的,例如 <code>OutOfMemoryError</code> (内存溢出)、<code>StackOverflowError</code> (栈溢出).程序通常对此无能为力，我们一般不编写代码来捕获 <code>Error</code>.</li><li><strong><code>Exception</code> (异常)</strong>：是应用程序本身可以处理的异常情况.这是我们编程时主要关注和处理的部分,它分为两大类：<strong>编译时异常</strong> 和 <strong>运行时异常</strong>.</li></ul><hr /><h2 id="编译时异常和运行时异常"><a class="markdownIt-Anchor" href="#编译时异常和运行时异常"></a> 编译时异常和运行时异常</h2><h3 id="编译时异常"><a class="markdownIt-Anchor" href="#编译时异常"></a> 编译时异常</h3><p>编译时异常,也称为受检异常,是那些 Java 编译器 <strong>强制</strong> 要求程序员必须处理的异常.如果一个方法可能会抛出编译时异常,那么该方法必须在方法签名中使用 <code>throws</code> 关键字声明它可能抛出的异常类型,或者在方法内部使用 <code>try-catch</code> 块来捕获并处理这个异常.否则,程序将无法通过编译.</p><p>这种机制的设计目的是提醒开发者,调用这个方法时可能会出现一些可预见的、需要处理的问题,从而强制编写更健壮的代码.</p><ul><li><strong>特点</strong>：<ul><li><strong>强制处理</strong>：编译器会检查代码是否对这类异常进行了处理.</li><li><strong>可预见性</strong>：通常是程序外部的、可预见的问题,例如 I/O 操作失败、数据库连接中断等.</li><li><strong>继承关系</strong>：继承自 <code>java.lang.Exception</code> 类,但<strong>不</strong>继承自 <code>java.lang.RuntimeException</code>.</li></ul></li><li><strong>常见例子</strong>：<ul><li><strong><code>IOException</code></strong>: 处理文件、网络流等 I/O 操作时最常见的异常.</li><li><strong><code>SQLException</code></strong>: 与数据库交互时可能发生的异常.</li><li><strong><code>ClassNotFoundException</code></strong>: 试图加载不存在的类时抛出.</li></ul></li></ul><p><em>注意</em></p><p>外层当你写了<code>return异常(Exception)</code>时,他会强制你使用<code>try-catch</code>或<code>throw</code>给方法(使用当前方法的时候用<code>try-catch</code>).</p><p>快捷键: <code>ctrl+alt+t</code>.</p><h3 id="运行时异常"><a class="markdownIt-Anchor" href="#运行时异常"></a> 运行时异常</h3><p>运行时异常，也称为非受检异常 (Unchecked Exception),是那些 Java 编译器 <strong>不强制</strong> 要求程序员必须处理的异常.这类异常通常是由程序自身的逻辑错误（Bugs）引起的,是可以在编码阶段避免的.</p><p>Java 的设计者认为,如果强制要求处理所有运行时异常,代码会变得非常冗长.因此,选择不强制检查它们,而是留给程序员自己决定是否需要处理.</p><ul><li><strong>特点</strong>：<ul><li><strong>非强制处理</strong>：编译器不会检查代码是否处理了这类异常.</li><li><strong>逻辑错误</strong>：通常是由程序逻辑缺陷导致的,例如访问了空对象、数组越界等.</li><li><strong>可避免性</strong>：在大多数情况下,可以通过编写更严谨的代码（如添加 <code>if</code> 判断）来避免.</li><li><strong>继承关系</strong>：继承自 <code>java.lang.RuntimeException</code> 类.</li></ul></li><li><strong>常见例子</strong>：<ul><li><strong><code>NullPointerException</code></strong>: 空指针异常，最常见的运行时异常.</li><li><strong><code>ArrayIndexOutOfBoundsException</code></strong>: 数组索引越界异常.</li><li><strong><code>IllegalArgumentException</code></strong>: 非法参数异常.</li><li><strong><code>ClassCastException</code></strong>: 类型转换异常.</li></ul></li></ul><hr /><h2 id="推荐使用运行时异常"><a class="markdownIt-Anchor" href="#推荐使用运行时异常"></a> 推荐使用运行时异常</h2><p>尽管编译时异常的初衷是好的,但在长期的软件工程实践中,开发者们发现它往往会带来一些弊端,而运行时异常则能提供更大的灵活性.因此,现代 Java 开发（尤其是大型后端应用和框架设计）的趋势是<strong>优先和广泛地使用运行时异常</strong>.</p><h3 id="推荐运行时异常的原因"><a class="markdownIt-Anchor" href="#推荐运行时异常的原因"></a> 推荐运行时异常的原因</h3><ol><li><p><strong>避免代码污染和降低耦合度</strong>编译时异常具有“传染性”.如果一个底层方法声明 <code>throws SomeCheckedException</code>,那么所有直接或间接调用它的方法都必须处理这个异常（要么 <code>catch</code>，要么继续 <code>throws</code>）.这会导致：</p><ul><li><strong><code>throws</code> 子句冗长</strong>：方法签名上会挂上一长串的 <code>throws</code> 声明,非常混乱.</li><li><strong>破坏封装</strong>：高层业务逻辑被迫关心底层的实现细节.例如,一个 <code>UserService</code> 的 <code>register()</code> 方法,本应只关心业务逻辑,但如果底层使用了文件操作，它可能就不得不 <code>throws IOException</code>.如果未来实现从文件改成数据库,<code>throws</code> 声明又要改成 <code>SQLException</code>,所有调用链上的代码都要修改.这严重违反了高内聚、低耦合的设计原则.</li><li><strong>运行时异常解决了这个问题</strong>：它不需要在方法签名上声明,从而将异常处理的责任从“强制的层层传递”变成了“在合适的地方统一处理”.</li></ul></li><li><p>**避免无意义的 <code>catch</code> 块 **为了通过编译,开发者常常被迫编写毫无意义的 <code>catch</code> 块,这被称为“异常吞噬”,是一种非常危险的反模式.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 糟糕的实践：为了通过编译而吞噬异常</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    someMethodThatThrowsCheckedException();</span><br><span class="line">&#125; <span class="keyword">catch</span> (SomeCheckedException e) &#123;</span><br><span class="line">    <span class="comment">// 只是打印一下，然后假装无事发生</span></span><br><span class="line">    e.printStackTrace(); </span><br><span class="line">    <span class="comment">// 更糟糕的是，catch块为空，完全忽略错误！</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样做会隐藏真正的问题,让程序在一个错误的状态下继续运行,最终可能在别处引发一个更难追踪的错误.而运行时异常<strong>不会强制你 <code>try-catch</code></strong>,从而鼓励你只在真正有能力处理异常的地方才去捕获它.</p></li><li><p><strong>更符合现代应用的处理逻辑</strong> 在大多数服务器端应用中,绝大多数异常对于当前执行的方法来说是<strong>不可恢复</strong>的.例如：</p><ul><li>数据库连接池耗尽.</li><li>配置文件加载失败.</li><li>必要的远程服务不可用.</li></ul><p>在这些情况下,当前方法几乎不可能自己修复问题.最好的策略是<strong>快速失败 (Fail-Fast)</strong>：中断当前操作,让异常自然地冒泡到上层的全局异常处理器。全局处理器可以统一记录日志、向监控系统报警,并向客户端返回一个标准化的错误响应（例如 HTTP 500）.运行时异常完美契合这种模型.</p></li></ol><h3 id="使用运行时异常"><a class="markdownIt-Anchor" href="#使用运行时异常"></a> 使用运行时异常</h3><ol><li><p><strong>使用标准运行时异常进行断言和前置条件检查</strong> 在你的方法入口处,检查传入的参数是否合法,如果不合法,立即抛出运行时异常来中断执行.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setAge</span><span class="params">(<span class="type">int</span> age)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (age &lt; <span class="number">0</span> || age &gt; <span class="number">150</span>) &#123;</span><br><span class="line">        <span class="comment">// 参数不合法，这是一个编程错误，应使用运行时异常</span></span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Age must be between 0 and 150.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">this</span>.age = age;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">processOrder</span><span class="params">(OrderData order)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (order == <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">NullPointerException</span>(<span class="string">&quot;OrderData cannot be null.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>异常包装 (Exception Wrapping)</strong> 这是现代框架（如 Spring）中广泛使用的一种强大技术.当底层的代码（如 JDBC、JPA）抛出编译时异常时,在你的数据访问层捕获它,然后将其<strong>包装</strong>成一个自定义的、更能体现业务含义的运行时异常再抛出.</p><ul><li><strong>优点</strong>：<ul><li><strong>隐藏实现细节</strong>：上层调用者无需关心底层用的是 JDBC 还是 Hibernate,它只需要处理 <code>DataAccessException</code>.</li><li><strong>提供更多上下文</strong>：在包装时，可以将原始异常作为 cause 传入,并添加更多业务相关的描述信息.</li><li><strong>保持代码干净</strong>：避免了编译时异常的“污染”.</li></ul></li></ul><p><strong>示例代码：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 自定义一个运行时异常</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">DataAccessException</span> <span class="keyword">extends</span> <span class="title class_">RuntimeException</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">DataAccessException</span><span class="params">(String message, Throwable cause)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(message, cause);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在数据访问层进行异常包装</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserRepository</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> User <span class="title function_">findById</span><span class="params">(String id)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// ... 执行JDBC查询，可能会抛出 SQLException (编译时异常)</span></span><br><span class="line">            <span class="keyword">return</span> someJdbcOperation(id); </span><br><span class="line">        &#125; <span class="keyword">catch</span> (SQLException e) &#123;</span><br><span class="line">            <span class="comment">// 捕获编译时异常，包装成自定义的运行时异常再抛出</span></span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">DataAccessException</span>(<span class="string">&quot;Failed to find user with id: &quot;</span> + id, e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>分析这段代码的行为：</strong></p><ol><li><p><strong>异常传播链没有中断</strong>：<code>catch</code> 块的最后一行是 <code>throw</code>.这意味着 <code>findById</code> 方法的执行会<strong>立即终止</strong>,并将一个新的 <code>DataAccessException</code> 异常抛给上层调用者.<strong>错误信息并没有被隐藏,而是以另一种形式继续向上传播.</strong></p></li><li><p><strong>原始异常信息被保留</strong>：通过 <code>new DataAccessException(..., e)</code>,原始的 <code>SQLException</code> 对象 <code>e</code> 被设置为了新异常的 <code>cause</code>.当这个新异常被打印堆栈时,你会清晰地看到 “Caused by: java.sql.SQLException: …” 的信息.所有细节都没有丢失.</p></li><li><p><strong>提升了代码的抽象层次</strong>：上层的服务层（Service Layer）调用 <code>UserRepository</code> 时,它不需要关心底层用的是什么数据库技术,因此它不应该处理 <code>SQLException</code>.通过包装,<code>UserRepository</code> 将底层的技术细节（<code>SQLException</code>）转换成了更高层次的、与业务更相关的抽象（<code>DataAccessException</code>）,这是一种非常重要的解耦方式.</p></li></ol></li><li><p><strong>创建自定义的运行时异常来表达业务问题</strong> 对于特定的业务失败场景,可以创建继承自 <code>RuntimeException</code> 的自定义异常类,使其名称更具可读性和业务含义.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 用户未找到异常</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserNotFoundException</span> <span class="keyword">extends</span> <span class="title class_">RuntimeException</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">UserNotFoundException</span><span class="params">(String userId)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(<span class="string">&quot;User with ID &#x27;&quot;</span> + userId + <span class="string">&quot;&#x27; was not found.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 库存不足异常</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">InsufficientStockException</span> <span class="keyword">extends</span> <span class="title class_">RuntimeException</span> &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">InsufficientStockException</span><span class="params">(String productId)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>(<span class="string">&quot;Insufficient stock for product: &quot;</span> + productId);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><table><thead><tr><th>场景</th><th>推荐做法</th></tr></thead><tbody><tr><td><strong>编程错误</strong> (如非法参数, null 值)</td><td><strong>必须</strong> 使用标准的运行时异常 (<code>IllegalArgumentException</code>, <code>NullPointerException</code>).</td></tr><tr><td><strong>不可恢复的系统/环境问题</strong> (如数据库访问失败)</td><td><strong>强烈推荐</strong> 使用“异常包装”技术,将底层的编译时异常转换为自定义的运行时异常.</td></tr><tr><td><strong>可预测且调用方应处理的 API 问题</strong></td><td><strong>可以考虑</strong> 使用编译时异常.这是它唯一推荐的用武之地.例如,一个库的 <code>login(user, pass)</code> 方法可能会 <code>throws InvalidCredentialsException</code>,强制调用者处理登录失败的逻辑,但即便如此,很多人也倾向于用运行时异常来设计.</td></tr><tr><td><strong>业务逻辑失败</strong> (如用户不存在, 库存不足)</td><td><strong>推荐</strong> 使用自定义的运行时异常.这样可以在全局异常处理器中根据异常类型返回不同的错误码和信息给前端.</td></tr></tbody></table><hr /><h1 id="泛型"><a class="markdownIt-Anchor" href="#泛型"></a> 泛型</h1><p>Java 泛型是 JDK 5 中引入的一个重要新特性,它允许在定义类、接口和方法时使用“类型参数”（type parameter）.通过这种方式,可以编写出更通用、更安全、更清晰的代码.</p><p>想象一下,在没有泛型之前,如果你想创建一个可以持有任何类型对象的列表,你可能会使用 <code>ArrayList</code>,它内部存储的是 <code>Object</code> 类型的对象.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 没有泛型的代码</span></span><br><span class="line"><span class="type">List</span> <span class="variable">list</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ArrayList</span>();</span><br><span class="line">list.add(<span class="string">&quot;Hello&quot;</span>); <span class="comment">// 存入字符串</span></span><br><span class="line">list.add(<span class="number">123</span>);     <span class="comment">// 存入整数</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 取出时需要强制类型转换</span></span><br><span class="line"><span class="type">String</span> <span class="variable">firstItem</span> <span class="operator">=</span> (String) list.get(<span class="number">0</span>);</span><br><span class="line"><span class="comment">// 如果不小心转换错了类型，运行时会抛出 ClassCastException</span></span><br><span class="line"><span class="comment">// Integer secondItem = (String) list.get(1); // 运行时错误！</span></span><br></pre></td></tr></table></figure><p>这里有两个主要问题：</p><ol><li><strong>类型不安全</strong>：你可以往列表里添加任何类型的对象,但在取出时,你必须“记住”你当初存的是什么类型,并进行强制类型转换.如果记错了,就会在运行时抛出 <code>ClassCastException</code> 异常.</li><li><strong>代码可读性差</strong>：任何人看到 <code>List list</code> 都无法直接知道这个列表到底打算存储什么类型的元素.</li></ol><p><strong>泛型就是为了解决这些问题而生的。</strong></p><p>通过使用泛型,你可以在编译时就指定容器中能够存放的对象类型.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 使用泛型的代码</span></span><br><span class="line">List&lt;String&gt; stringList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">stringList.add(<span class="string">&quot;Hello&quot;</span>);</span><br><span class="line"><span class="comment">// stringList.add(123); // 编译错误！编译器直接阻止你添加非 String 类型的对象</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 取出时不再需要强制类型转换</span></span><br><span class="line"><span class="type">String</span> <span class="variable">firstItem</span> <span class="operator">=</span> stringList.get(<span class="number">0</span>);</span><br></pre></td></tr></table></figure><p>使用泛型后：</p><ol><li><strong>类型安全</strong>：编译器会检查你向集合中添加的元素类型是否正确.如果类型不匹配,代码将无法通过编译.这等于将运行时错误提前到了编译时,大大提高了程序的健壮性.</li><li><strong>代码更清晰</strong>：<code>List&lt;String&gt;</code> 清晰地表明了这个列表只能包含 <code>String</code> 类型的元素,提高了代码的可读性和可维护性.</li><li><strong>消除强制类型转换</strong>：从泛型集合中获取元素时,不再需要手动进行强制类型转换,代码更简洁.</li></ol><hr /><h2 id="泛型类"><a class="markdownIt-Anchor" href="#泛型类"></a> 泛型类</h2><p>通过在类名后面加上 <code>&lt;T&gt;</code> 来定义一个泛型类,其中 <code>T</code> 是一个类型参数的占位符,可以换成任何合法的标识符,通常使用单个大写字母（如 <code>T</code> for Type, <code>E</code> for Element, <code>K</code> for Key, <code>V</code> for Value）.</p><p><strong>示例：</strong> 创建一个通用的容器类 <code>Box</code>.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Box</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> T item;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">set</span><span class="params">(T item)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.item = item;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> T <span class="title function_">get</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> item;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 创建一个只能存放 Integer 的 Box</span></span><br><span class="line">        Box&lt;Integer&gt; integerBox = <span class="keyword">new</span> <span class="title class_">Box</span>&lt;&gt;();</span><br><span class="line">        integerBox.set(<span class="number">10</span>);</span><br><span class="line">        <span class="comment">// integerBox.set(&quot;hello&quot;); // 编译错误</span></span><br><span class="line">        <span class="type">Integer</span> <span class="variable">value</span> <span class="operator">=</span> integerBox.get();</span><br><span class="line">        System.out.println(value); <span class="comment">// 输出 10</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建一个只能存放 String 的 Box</span></span><br><span class="line">        Box&lt;String&gt; stringBox = <span class="keyword">new</span> <span class="title class_">Box</span>&lt;&gt;();</span><br><span class="line">        stringBox.set(<span class="string">&quot;Java Generics&quot;</span>);</span><br><span class="line">        <span class="type">String</span> <span class="variable">text</span> <span class="operator">=</span> stringBox.get();</span><br><span class="line">        System.out.println(text); <span class="comment">// 输出 Java Generics</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="泛型接口"><a class="markdownIt-Anchor" href="#泛型接口"></a> 泛型接口</h2><p>泛型接口的定义与泛型类类似.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Pair</span>&lt;K, V&gt; &#123;</span><br><span class="line">    K <span class="title function_">getKey</span><span class="params">()</span>;</span><br><span class="line">    V <span class="title function_">getValue</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 实现泛型接口</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">OrderedPair</span>&lt;K, V&gt; <span class="keyword">implements</span> <span class="title class_">Pair</span>&lt;K, V&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> K key;</span><br><span class="line">    <span class="keyword">private</span> V value;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">OrderedPair</span><span class="params">(K key, V value)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.key = key;</span><br><span class="line">        <span class="built_in">this</span>.value = value;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> K <span class="title function_">getKey</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> key;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> V <span class="title function_">getValue</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> value;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="泛型方法"><a class="markdownIt-Anchor" href="#泛型方法"></a> 泛型方法</h2><p>泛型方法允许方法的类型参数独立于类的类型参数.你可以在任何方法（静态或非静态）中定义类型参数.类型参数声明位于修饰符和返回类型之间.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Util</span> &#123;</span><br><span class="line">    <span class="comment">// 定义一个泛型方法</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="keyword">void</span> <span class="title function_">printArray</span><span class="params">(T[] inputArray)</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (T element : inputArray) &#123;</span><br><span class="line">            System.out.printf(<span class="string">&quot;%s &quot;</span>, element);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        Integer[] intArray = &#123; <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span> &#125;;</span><br><span class="line">        String[] stringArray = &#123; <span class="string">&quot;Hello&quot;</span>, <span class="string">&quot;World&quot;</span> &#125;;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;Integer Array contains:&quot;</span>);</span><br><span class="line">        printArray(intArray); <span class="comment">// 编译器会自动推断 T 为 Integer</span></span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;String Array contains:&quot;</span>);</span><br><span class="line">        printArray(stringArray); <span class="comment">// 编译器会自动推断 T 为 String</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="有界类型参数"><a class="markdownIt-Anchor" href="#有界类型参数"></a> 有界类型参数</h2><p>有时候,你可能希望限制泛型参数的类型范围.例如,一个方法可能只接受 <code>Number</code> 类或其子类（如 <code>Integer</code>, <code>Double</code>）.这时就可以使用 <code>extends</code> 关键字来设置上界.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Stats</span>&lt;T <span class="keyword">extends</span> <span class="title class_">Number</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> T[] nums;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Stats</span><span class="params">(T[] nums)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.nums = nums;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">average</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">double</span> <span class="variable">sum</span> <span class="operator">=</span> <span class="number">0.0</span>;</span><br><span class="line">        <span class="keyword">for</span> (T num : nums) &#123;</span><br><span class="line">            sum += num.doubleValue(); <span class="comment">// 可以安全调用 Number 类的方法</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> sum / nums.length;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line"><span class="comment">// Stats&lt;Integer&gt; intStats = new Stats&lt;&gt;(new Integer[]&#123;1, 2, 3&#125;);</span></span><br><span class="line"><span class="comment">// Stats&lt;String&gt; stringStats = new Stats&lt;&gt;(new String[]&#123;&quot;a&quot;, &quot;b&quot;&#125;); // 编译错误！String 不是 Number 的子类</span></span><br></pre></td></tr></table></figure><h2 id="通配符"><a class="markdownIt-Anchor" href="#通配符"></a> 通配符</h2><p>通配符 <code>?</code> 表示未知的类型,它主要用于提高代码的灵活性,尤其是在处理泛型方法的参数时.</p><h3 id="上界通配符-extends-type"><a class="markdownIt-Anchor" href="#上界通配符-extends-type"></a> 上界通配符: <code>&lt;? extends Type&gt;</code></h3><p><code>? extends Type</code> 表示这个未知的类型是 <code>Type</code> 本身或者其某个子类。这种集合<strong>只能从中读取数据 (get)，不能写入数据 (add)</strong>（除了<code>null</code>），因为编译器无法确定 <code>?</code> 到底代表哪个具体的子类型。</p><p><strong>示例：</strong> 一个可以计算任何 <code>Number</code> 列表总和的方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="type">double</span> <span class="title function_">sumOfList</span><span class="params">(List&lt;? extends Number&gt; list)</span> &#123;</span><br><span class="line">    <span class="type">double</span> <span class="variable">sum</span> <span class="operator">=</span> <span class="number">0.0</span>;</span><br><span class="line">    <span class="keyword">for</span> (Number n : list) &#123;</span><br><span class="line">        sum += n.doubleValue();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// list.add(1); // 编译错误！不能往里面添加元素</span></span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个方法可以接受 <code>List&lt;Integer&gt;</code>, <code>List&lt;Double&gt;</code> 等。</p><h3 id="下界通配符-super-type"><a class="markdownIt-Anchor" href="#下界通配符-super-type"></a> 下界通配符: <code>&lt;? super Type&gt;</code></h3><p><code>? super Type</code> 表示这个未知的类型是 <code>Type</code> 本身或者其某个父类.这种集合<strong>只能向其中写入 <code>Type</code> 或其子类型的数据 (add),但读取出来的对象只能被当作 <code>Object</code> 对待</strong>.</p><p><strong>示例：</strong> 一个可以将整数添加到列表的方法。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">addIntegers</span><span class="params">(List&lt;? <span class="built_in">super</span> Integer&gt; list)</span> &#123;</span><br><span class="line">    list.add(<span class="number">1</span>);</span><br><span class="line">    list.add(<span class="number">2</span>);</span><br><span class="line">    <span class="comment">// Object obj = list.get(0); // 读取出来是 Object 类型</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个方法可以接受 <code>List&lt;Integer&gt;</code>, <code>List&lt;Number&gt;</code>, <code>List&lt;Object&gt;</code>。</p><blockquote><p>**<code>PECS</code> 法则 **</p><ul><li>如果你需要从一个数据结构中<strong>读取</strong>数据（生产者），使用 <code>? extends T</code>。</li><li>如果你需要向一个数据结构中<strong>写入</strong>数据（消费者），使用 <code>? super T</code>。</li></ul></blockquote><h2 id="包装类"><a class="markdownIt-Anchor" href="#包装类"></a> 包装类</h2><p>将基本数据类型定义为对象,避免强转,多用于泛型中(<strong>泛型不支持基本数据类型,只支持引用数据类型</strong>).它会帮助你自动装箱和自动拆箱.</p><table><thead><tr><th>基本类型</th><th>包装类</th><th>主要用途和特点</th><th>常用方法/常量示例</th></tr></thead><tbody><tr><td><strong><code>byte</code></strong></td><td><strong><code>Byte</code></strong></td><td>8位有符号整数的对象表示。</td><td><code>Byte.parseByte(String s)</code>, <code>byteValue()</code></td></tr><tr><td><strong><code>short</code></strong></td><td><strong><code>Short</code></strong></td><td>16位有符号整数的对象表示。</td><td><code>Short.parseShort(String s)</code>, <code>shortValue()</code></td></tr><tr><td><strong><code>int</code></strong></td><td><strong><code>Integer</code></strong></td><td>32位有符号整数的对象表示。<strong>最常用</strong>。</td><td><code>Integer.parseInt(String s)</code>, <code>Integer.valueOf(int i)</code>, <code>intValue()</code>, <code>toString()</code></td></tr><tr><td><strong><code>long</code></strong></td><td><strong><code>Long</code></strong></td><td>64位有符号整数的对象表示。</td><td><code>Long.parseLong(String s)</code>, <code>longValue()</code></td></tr><tr><td><strong><code>float</code></strong></td><td><strong><code>Float</code></strong></td><td>32位单精度浮点数的对象表示。</td><td><code>Float.parseFloat(String s)</code>, <code>floatValue()</code>, <code>isNaN()</code></td></tr><tr><td><strong><code>double</code></strong></td><td><strong><code>Double</code></strong></td><td>64位双精度浮点数的对象表示。</td><td><code>Double.parseDouble(String s)</code>, <code>doubleValue()</code></td></tr><tr><td><strong><code>char</code></strong></td><td><strong><code>Character</code></strong></td><td>16位 Unicode 字符的对象表示。</td><td><code>Character.isDigit(char ch)</code>, <code>Character.isLetter(char ch)</code>, <code>toLowerCase(char ch)</code></td></tr><tr><td><strong><code>boolean</code></strong></td><td><strong><code>Boolean</code></strong></td><td>布尔值的对象表示 (<code>true</code>/<code>false</code>)。</td><td><code>Boolean.parseBoolean(String s)</code>, <code>booleanValue()</code>, <code>Boolean.TRUE</code>, <code>Boolean.FALSE</code></td></tr></tbody></table><h2 id="类型擦除"><a class="markdownIt-Anchor" href="#类型擦除"></a> 类型擦除</h2><p>Java 泛型的一个核心概念是<strong>类型擦除</strong>,这是为了兼容 JDK 5 之前的代码而做出的设计.</p><p>简单来说,泛型信息只存在于代码的编译阶段,在编译后的字节码 (<code>.class</code> 文件) 中,所有的泛型类型参数都会被替换为它们的上界（如果没有指定上界,则替换为 <code>Object</code>）.</p><p>例如,<code>List&lt;String&gt;</code> 在编译后会变成 <code>List</code>,<code>Box&lt;T extends Number&gt;</code> 会变成 <code>Box&lt;Number&gt;</code>.</p><p><strong>这意味着：</strong></p><ul><li>在运行时，<code>ArrayList&lt;String&gt;</code> 和 <code>ArrayList&lt;Integer&gt;</code> 的类对象是同一个,即 <code>ArrayList.class</code>.</li><li>你不能创建泛型数组，如 <code>new T[]</code>,因为在运行时 <code>T</code> 的类型信息已经被擦除了.</li></ul><p>编译器会在必要的地方自动插入强制类型转换代码,来保证我们前面提到的类型安全.</p><table><thead><tr><th>特性</th><th>描述</th><th>优点</th><th>应用场景 (Android 开发中)</th></tr></thead><tbody><tr><td><strong>核心思想</strong></td><td>在编译时进行类型检查, 而不是在运行时。</td><td>提高程序的类型安全性和健壮性。</td><td>定义 <code>List&lt;User&gt;</code> 来接收用户数据列表,而不是使用原始的 <code>List</code>,可以防止在运行时因添加了错误类型的数据（如 <code>Product</code> 对象）而导致 <code>ClassCastException</code> 崩溃.</td></tr><tr><td><strong>泛型类/接口</strong></td><td><code>class MyClass&lt;T&gt; &#123;&#125;</code>, <code>interface MyInterface&lt;T&gt; &#123;&#125;</code></td><td>定义通用的数据结构，实现代码复用。</td><td><strong>网络请求封装</strong>: 创建一个通用的 <code>ApiResponse&lt;T&gt;</code> 类来处理所有网络请求的返回数据.<code>T</code> 可以是 <code>User</code>、<code>Order</code> 或任何其他数据模型,避免为每种数据类型都写一个重复的包装类.</td></tr><tr><td><strong>泛型方法</strong></td><td><code>&lt;T&gt; T myMethod(T arg)</code></td><td>允许方法处理多种类型的数据，独立于类的泛型。</td><td><strong>JSON 解析工具</strong>: 编写一个通用的 <code>JsonUtils.fromJson(String json, Class&lt;T&gt; clazz)</code> 方法.调用时可以传入不同的 Class 对象（如 <code>User.class</code>）,方法会返回相应类型的实例,非常灵活.</td></tr><tr><td><strong>有界类型</strong></td><td><code>&lt;T extends Number&gt;</code></td><td>限制类型参数的范围，可以使用上界类型的方法。</td><td><strong>RecyclerView Adapter</strong>: 定义 <code>Adapter</code> 时，其 <code>ViewHolder</code> 必须继承自 <code>RecyclerView.ViewHolder</code>.<code>public class MyAdapter extends RecyclerView.Adapter&lt;MyAdapter.MyViewHolder&gt; &#123; ... &#125;</code> 就是一个典型的有界类型应用.</td></tr><tr><td><strong>通配符</strong></td><td><code>? extends T</code>, <code>? super T</code></td><td>增加 API 的灵活性，特别是在处理集合参数时。</td><td><strong>数据库操作</strong>: 在 Room 或其他 ORM 框架中,一个批量保存方法可以定义为 <code>void saveItems(List&lt;? extends BaseEntity&gt; items)</code>.这样它就能接受任何继承自 <code>BaseEntity</code> 的实体列表（如 <code>List&lt;User&gt;</code>, <code>List&lt;Product&gt;</code>）,增强了方法的通用性.</td></tr><tr><td><strong>类型擦除</strong></td><td>泛型信息在编译后被移除。</td><td>保证了对旧版本 Java 的向后兼容性。</td><td><strong>理解其限制以避免错误</strong>: 运行时无法直接判断 <code>list instanceof List&lt;String&gt;</code>.因此,在需要运行时类型信息的场景（如反序列化）,必须显式传递 <code>Class&lt;T&gt;</code> 对象或使用 <code>TypeToken</code> (如 Gson 库中) 来捕获和传递完整的泛型类型信息.</td></tr></tbody></table><hr /><h1 id="collection集合"><a class="markdownIt-Anchor" href="#collection集合"></a> Collection集合</h1><p>在 Java 中,<strong>集合框架 (Collection Framework)</strong> 是一个用来存储和操作一组对象的统一架构,它包含了一系列的接口和类,位于 <code>java.util</code> 包中.这个框架的核心是 <code>Collection</code> 接口和 <code>Map</code> 接口.</p><p>这里我们主要关注 <code>Collection</code> 接口,它是所有单列集合的“根”接口.也就是说,它处理的是一组独立的对象元素,而不是键值对.</p><p><code>Collection</code> 接口下面主要派生出三个核心子接口：<code>List</code>、<code>Set</code> 和 <code>Queue</code>,它们各自代表了一种不同的集合类型.</p><h2 id="list-接口-列表"><a class="markdownIt-Anchor" href="#list-接口-列表"></a> List 接口 (列表)</h2><p><code>List</code> 是一个<strong>有序</strong>的集合,允许存储<strong>重复</strong>的元素.因为它有序,所以可以通过索引（整数位置）来访问、添加或删除元素.</p><ul><li><strong>主要实现类</strong>:<ul><li><code>ArrayList</code>: 基于动态数组实现.<strong>查询和随机访问速度快</strong>（通过索引 <code>get(i)</code>）,但在中间插入或删除元素较慢.</li><li><code>LinkedList</code>: 基于双向链表实现.<strong>插入和删除速度快</strong>,但随机访问速度慢.它也实现了 <code>Queue</code> 接口,可以作为队列或栈使用.</li><li><code>Vector</code>: 古老的实现,与 <code>ArrayList</code> 类似但<strong>线程安全</strong>,性能较差,现在已不推荐使用.</li></ul></li></ul><h2 id="set-接口-集"><a class="markdownIt-Anchor" href="#set-接口-集"></a> Set 接口 (集)</h2><p><code>Set</code> 是一个<strong>无序</strong>（通常情况下）的集合,<strong>不允许</strong>存储<strong>重复</strong>的元素.它主要用来确保集合中元素的唯一性.</p><ul><li><strong>主要实现类</strong>:<ul><li><code>HashSet</code>: 基于哈希表 (<code>HashMap</code>) 实现.提供最快的性能（添加、删除、查找）,但不保证元素的任何顺序.</li><li><code>LinkedHashSet</code>: <code>HashSet</code> 的子类,使用链表维护了元素<strong>插入的顺序</strong>.在需要保持插入顺序且保证元素唯一的场景下使用.</li><li><code>TreeSet</code>: 基于红黑树实现.元素会自动进行<strong>自然排序</strong>（或者根据构造时提供的 <code>Comparator</code> 排序）,性能略低于 <code>HashSet</code>.</li></ul></li></ul><h2 id="queue-接口-队列"><a class="markdownIt-Anchor" href="#queue-接口-队列"></a> Queue 接口 (队列)</h2><p><code>Queue</code> 是一种遵循 <strong>先进先出 (FIFO - First-In, First-Out)</strong> 原则的集合.元素在队尾添加,在队头被移除.</p><ul><li><strong>主要实现类</strong>:<ul><li><code>LinkedList</code>: 如前所述,它也实现了 <code>Queue</code> 接口,可以作为标准的 FIFO 队列使.</li><li><code>PriorityQueue</code>: 优先队列.元素并非按照插入顺序排序,而是根据其自然顺序或者指定的比较器进行排序,每次取出的都是队列中“最小”的元素.</li><li><code>ArrayDeque</code>: 基于数组实现的双端队列,可以高效地在队列的头部和尾部进行添加和删除操作.</li></ul></li></ul><blockquote><p><strong>注意</strong>: <code>Map</code> 接口（如 <code>HashMap</code>, <code>TreeMap</code>）是集合框架的另一大分支,它存储的是<strong>键值对 (Key-Value)</strong>,不继承自 <code>Collection</code> 接口,因此这里不详细展开,但它的遍历方式与 <code>Set</code> 类似.</p></blockquote><hr /><h2 id="集合的遍历方法"><a class="markdownIt-Anchor" href="#集合的遍历方法"></a> 集合的遍历方法</h2><p>遍历是操作集合最常见的需求.Java 提供了多种遍历 <code>Collection</code> 的方法,主要有以下三种：</p><h4 id="迭代器-iterator"><a class="markdownIt-Anchor" href="#迭代器-iterator"></a> 迭代器 (Iterator)</h4><p>这是<strong>最通用、最标准</strong>的遍历方式,适用于所有 <code>Collection</code> 类型.<code>Iterator</code> 是一个接口,它提供了一种安全地访问集合元素并按需移除元素的方式.</p><p><strong>核心方法</strong>:</p><ul><li><code>hasNext()</code>: 检查序列中是否还有下一个元素.</li><li><code>next()</code>: 返回序列中的下一个元素.</li><li><code>remove()</code>: 从集合中移除 <code>next()</code> 方法最后返回的那个元素.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">list.add(<span class="string">&quot;Apple&quot;</span>);</span><br><span class="line">list.add(<span class="string">&quot;Banana&quot;</span>);</span><br><span class="line">list.add(<span class="string">&quot;Cherry&quot;</span>);</span><br><span class="line"></span><br><span class="line">Iterator&lt;String&gt; iterator = list.iterator();</span><br><span class="line"><span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">fruit</span> <span class="operator">=</span> iterator.next();</span><br><span class="line">    System.out.println(fruit);</span><br><span class="line">    <span class="keyword">if</span> (fruit.equals(<span class="string">&quot;Banana&quot;</span>)) &#123;</span><br><span class="line">        iterator.remove(); <span class="comment">// 安全地在遍历时删除元素</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">System.out.println(<span class="string">&quot;删除后: &quot;</span> + list); <span class="comment">// 输出: 删除后: [Apple, Cherry]</span></span><br></pre></td></tr></table></figure><p><strong>优点</strong>:</p><ul><li><strong>通用性</strong>: 所有 <code>Collection</code> 都支持.</li><li><strong>安全删除</strong>: 它是唯一一种可以在遍历过程中安全地修改集合（删除元素）的方式.</li></ul><h4 id="for-each-循环-增强型-for-循环"><a class="markdownIt-Anchor" href="#for-each-循环-增强型-for-循环"></a> For-Each 循环 (增强型 For 循环)</h4><p>这是从 JDK 5 开始引入的语法糖,其底层实现原理仍然是 <code>Iterator</code>,它使得遍历代码更加<strong>简洁易读</strong>.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">Set&lt;String&gt; set = <span class="keyword">new</span> <span class="title class_">HashSet</span>&lt;&gt;();</span><br><span class="line">set.add(<span class="string">&quot;Java&quot;</span>);</span><br><span class="line">set.add(<span class="string">&quot;Python&quot;</span>);</span><br><span class="line">set.add(<span class="string">&quot;Go&quot;</span>);</span><br><span class="line"></span><br><span class="line">System.out.println(<span class="string">&quot;使用 For-Each 循环遍历 Set:&quot;</span>);</span><br><span class="line"><span class="keyword">for</span> (String language : set) &#123;</span><br><span class="line">    System.out.println(language);</span><br><span class="line">    <span class="comment">// if (language.equals(&quot;Python&quot;)) &#123;</span></span><br><span class="line">    <span class="comment">//     set.remove(language); // 错误！会导致 ConcurrentModificationException</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>优点</strong>:</p><ul><li><strong>代码简洁</strong>: 写法非常简单,可读性强.</li><li><strong>不易出错</strong>: 无需手动管理迭代器,避免了 <code>hasNext()</code>/<code>next()</code> 的调用错误.</li></ul><p><strong>缺点</strong>:</p><ul><li><strong>无法在遍历时修改集合</strong>: 如果在 for-each 循环中直接调用集合的 <code>remove()</code> 或 <code>add()</code> 方法,会抛出 <code>ConcurrentModificationException</code> 异常.</li><li><strong>无法访问索引</strong>: 对于 <code>List</code>,无法在循环中直接获取当前元素的索引.</li></ul><h4 id="foreach方法-stream-api-或-lambda"><a class="markdownIt-Anchor" href="#foreach方法-stream-api-或-lambda"></a> <code>forEach()</code>方法 (Stream API 或 Lambda)</h4><p>从 Java 8 开始,<code>Collection</code> 接口（继承自 <code>Iterable</code>）有了 <code>forEach()</code> 方法,它结合 Lambda 表达式,可以写出非常紧凑的函数式风格的代码.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">Queue&lt;Integer&gt; queue = <span class="keyword">new</span> <span class="title class_">LinkedList</span>&lt;&gt;();</span><br><span class="line">queue.add(<span class="number">10</span>);</span><br><span class="line">queue.add(<span class="number">20</span>);</span><br><span class="line">queue.add(<span class="number">30</span>);</span><br><span class="line"></span><br><span class="line">System.out.println(<span class="string">&quot;使用 forEach 和 Lambda 表达式遍历 Queue:&quot;</span>);</span><br><span class="line">queue.forEach(number -&gt; System.out.println(number));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 也可以使用方法引用，代码更简洁</span></span><br><span class="line"><span class="comment">// queue.forEach(System.out::println);</span></span><br></pre></td></tr></table></figure><p><strong>优点</strong>:</p><ul><li><strong>代码最简洁</strong>: 特别适合简单的遍历打印或对每个元素执行单一操作.</li><li><strong>函数式编程</strong>: 可以与强大的 Stream API 无缝集成,进行 <code>filter</code>, <code>map</code>, <code>reduce</code> 等链式操作.</li></ul><p><strong>缺点</strong>:</p><ul><li>和 for-each 循环一样,不能在其中安全地修改集合.</li><li>无法使用 <code>break</code> 或 <code>return</code> 来提前终止循环（但可以通过一些变通方法实现）.</li></ul><table><thead><tr><th>遍历方式</th><th>优点</th><th>缺点</th><th>适用场景</th></tr></thead><tbody><tr><td><strong>Iterator</strong></td><td>通用,可在遍历时安全删除元素</td><td>写法相对繁琐</td><td>需要在遍历过程中修改集合（仅限删除）的场景.</td></tr><tr><td><strong>For-Each 循环</strong></td><td>代码简洁,可读性高,不易出错</td><td>无法在遍历时修改集合,无法访问索引</td><td>最常用、最推荐的通用遍历场景.</td></tr><tr><td><strong><code>forEach()</code> + Lambda</strong></td><td>代码最简洁,可与 Stream API 结合</td><td>无法在遍历时修改集合,无法 <code>break</code></td><td>简单的元素处理,或进行复杂的函数式编程（过滤、转换等）.</td></tr></tbody></table><hr /><h1 id="map集合"><a class="markdownIt-Anchor" href="#map集合"></a> Map集合</h1><p>与 <code>Collection</code> 存储单个独立的元素不同,<code>Map</code> 接口存储的是<strong>键值对 (Key-Value Pair)</strong>.它是一个将唯一的“键 (Key)”映射到“值 (Value)”的对象.你可以把它想象成一本现实生活中的字典,通过单词（Key）可以查到它的释义（Value）.</p><p><code>Map</code> 接口本身不继承自 <code>Collection</code> 接口,是 Java 集合框架中独立的一大分支.</p><p><em>核心特点</em></p><ol><li><strong>键值对存储</strong>：每个元素都由一个键和一个值组成,即一个 <code>Entry</code>.</li><li><strong>键的唯一性</strong>：在一个 <code>Map</code> 中,键 (Key) 必须是唯一的,不允许重复.如果用一个已存在的键去添加新的值,那么旧的值将会被覆盖.</li><li><strong>值的可重复性</strong>：值 (Value) 可以重复,多个不同的键可以映射到同一个值.</li><li><strong>无序性（通常）</strong>：大多数 <code>Map</code> 的实现类不保证元素的存入顺序，例如 <code>HashMap</code>.</li></ol><h2 id="map的实现类"><a class="markdownIt-Anchor" href="#map的实现类"></a> Map的实现类</h2><p><code>Map</code> 接口有多个实现类,各自具有不同的特性,适用于不同的场景.</p><ul><li><strong><code>HashMap</code></strong>:<ul><li><strong>内部结构</strong>：基于<strong>哈希表</strong>（数组 + 链表/红黑树）实现.</li><li><strong>特点</strong>：<strong>性能最高</strong>（增删改查的平均时间复杂度为 O(1)）,<strong>不保证</strong>元素的任何顺序（既不是插入顺序也不是排序顺序）,允许一个 <code>null</code> 键和多个 <code>null</code> 值,<strong>是 <code>Map</code>中最常用的实现类</strong>.</li><li><strong>适用场景</strong>：绝大多数需要键值对存储且不关心顺序的场景**(设置缓存)**.</li></ul></li><li><strong><code>LinkedHashMap</code></strong>:<ul><li><strong>内部结构</strong>：继承自 <code>HashMap</code>,额外使用一个<strong>双向链表</strong>来维护元素的顺序.</li><li><strong>特点</strong>：保留了元素的<strong>插入顺序</strong>,性能略低于 <code>HashMap</code>,因为需要维护链表.</li><li><strong>适用场景</strong>：需要保持元素插入顺序的场景,例如实现 LRU 缓存.</li></ul></li><li><strong><code>TreeMap</code></strong>:<ul><li><strong>内部结构</strong>：基于<strong>红黑树</strong>实现.</li><li><strong>特点</strong>：元素会根据键 (Key) 进行<strong>自然排序</strong>（要求 Key 实现 <code>Comparable</code> 接口）或根据构造时传入的 <code>Comparator</code> 进行定制排序.性能通常低于 <code>HashMap</code>.</li><li><strong>适用场景</strong>：需要对键进行排序的场景.</li></ul></li><li><strong><code>Hashtable</code></strong>:<ul><li><strong>内部结构</strong>：与 <code>HashMap</code> 类似,也是基于哈希表.</li><li><strong>特点</strong>：是古老的<strong>线程安全</strong>版本,方法都是同步的 (<code>synchronized</code>),因此性能很差,不允许 <code>null</code> 键和 <code>null</code> 值.现在已不推荐使用,线程安全的场景应使用 <code>ConcurrentHashMap</code>.</li></ul></li></ul><hr /><h2 id="集合的遍历方法-2"><a class="markdownIt-Anchor" href="#集合的遍历方法-2"></a> 集合的遍历方法</h2><p>由于 <code>Map</code> 本身不是 <code>Collection</code>,它没有迭代器 <code>iterator()</code>。因此,不能直接用 for-each 循环来遍历 <code>Map</code>.但我们可以通过 <code>Map</code> 提供的三个“视图”方法,将其转换为 <code>Collection</code> 类型,然后再进行遍历.这三种视图分别是：</p><ol><li><code>keySet()</code>: 获取 <code>Map</code> 中所有键 (Key) 的 <code>Set</code> 集合.</li><li><code>values()</code>: 获取 <code>Map</code> 中所有值 (Value) 的 <code>Collection</code> 集合.</li><li><code>entrySet()</code>: 获取 <code>Map</code> 中所有键值对 (<code>Map.Entry</code>) 的 <code>Set</code> 集合.</li></ol><p>下面是基于这三种视图的四种核心遍历方法.</p><h4 id="遍历-entryset-键值对集合-最推荐"><a class="markdownIt-Anchor" href="#遍历-entryset-键值对集合-最推荐"></a> 遍历 <code>entrySet()</code> (键值对集合) - <strong>最推荐</strong></h4><p>这是<strong>最高效、最常用</strong>的遍历方式,因为它一次性就可以获取到键和值.无需二次查询.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;String, Integer&gt; map = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">map.put(<span class="string">&quot;Apple&quot;</span>, <span class="number">10</span>);</span><br><span class="line">map.put(<span class="string">&quot;Banana&quot;</span>, <span class="number">20</span>);</span><br><span class="line">map.put(<span class="string">&quot;Cherry&quot;</span>, <span class="number">30</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用 for-each 循环遍历 entrySet</span></span><br><span class="line">System.out.println(<span class="string">&quot;--- 遍历 entrySet ---&quot;</span>);</span><br><span class="line"><span class="keyword">for</span> (Map.Entry&lt;String, Integer&gt; entry : map.entrySet()) &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> entry.getKey();</span><br><span class="line">    <span class="type">Integer</span> <span class="variable">value</span> <span class="operator">=</span> entry.getValue();</span><br><span class="line">    System.out.println(key + <span class="string">&quot; -&gt; &quot;</span> + value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>特点</strong>:</p><ul><li><strong>效率高</strong>: 同时获取 Key 和 Value,无需像遍历 <code>keySet</code> 那样再去 <code>map.get(key)</code>.</li><li><strong>功能全</strong>: 可以同时操作键和值.</li></ul><h4 id="遍历-keyset-键集合"><a class="markdownIt-Anchor" href="#遍历-keyset-键集合"></a> 遍历 <code>keySet()</code> (键集合)</h4><p>先获取所有键的集合,然后根据每个键去 <code>Map</code> 中获取对应的值.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 使用 for-each 循环遍历 keySet</span></span><br><span class="line">System.out.println(<span class="string">&quot;\n--- 遍历 keySet ---&quot;</span>);</span><br><span class="line"><span class="keyword">for</span> (String key : map.keySet()) &#123;</span><br><span class="line">    <span class="type">Integer</span> <span class="variable">value</span> <span class="operator">=</span> map.get(key); <span class="comment">// 需要根据 key 再次查找 value</span></span><br><span class="line">    System.out.println(key + <span class="string">&quot; -&gt; &quot;</span> + value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>特点</strong>:</p><ul><li>可以直接操作所有的键.</li><li>效率略低于遍历 <code>entrySet</code>,因为每次循环都需要通过 <code>map.get(key)</code> 进行一次额外的查找操作.</li></ul><h4 id="遍历-values-值集合"><a class="markdownIt-Anchor" href="#遍历-values-值集合"></a> 遍历 <code>values()</code> (值集合)</h4><p>如果你只关心 <code>Map</code> 中的值,不关心键,可以使用这种方式。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">System.out.println(<span class="string">&quot;\n--- 遍历 values ---&quot;</span>);</span><br><span class="line"><span class="keyword">for</span> (Integer value : map.values()) &#123;</span><br><span class="line">    System.out.println(<span class="string">&quot;Value: &quot;</span> + value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>特点</strong>:</p><ul><li>写法简单,适用于只关心值的场景.</li><li>无法在循环中获取到与值对应的键.</li></ul><h4 id="使用-foreach-方法和-lambda"><a class="markdownIt-Anchor" href="#使用-foreach-方法和-lambda"></a> 使用 <code>forEach()</code> 方法和 Lambda</h4><p>从 Java 8 开始,<code>Map</code> 接口也提供了 <code>forEach</code> 方法,它接受一个 <code>BiConsumer</code>（同时消费 Key 和 Value）,使得遍历代码更加简洁.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">System.out.println(<span class="string">&quot;\n--- 使用 forEach 和 Lambda ---&quot;</span>);</span><br><span class="line">map.forEach((key, value) -&gt; &#123;</span><br><span class="line">    System.out.println(key + <span class="string">&quot; -&gt; &quot;</span> + value);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p><strong>优点</strong>:</p><ul><li><strong>代码最简洁</strong>,可读性强.</li><li>函数式编程风格,方便进行链式操作.</li></ul><table><thead><tr><th>遍历方式</th><th>优点</th><th>缺点/注意事项</th><th>适用场景</th></tr></thead><tbody><tr><td><strong><code>entrySet()</code></strong></td><td><strong>效率最高</strong>,一次性获取键和值</td><td>无明显缺点</td><td><strong>强烈推荐</strong>的通用遍历方式.</td></tr><tr><td><strong><code>keySet()</code></strong></td><td>直观,可以直接操作键</td><td>效率稍低（需要额外 <code>get()</code> 操作）</td><td>当你主要想对键进行操作,偶尔需要值时.</td></tr><tr><td><strong><code>values()</code></strong></td><td>简单直接</td><td>无法获取键</td><td>只关心值,完全不需要键的场景.</td></tr><tr><td><strong><code>forEach()</code> + Lambda</strong></td><td><strong>代码最简洁</strong>,语法优雅</td><td>属于内部迭代，无法 <code>break</code> 终止循环</td><td>适用于简单的遍历操作,代码风格现代化.</td></tr></tbody></table><hr /><h1 id="stream流"><a class="markdownIt-Anchor" href="#stream流"></a> Stream流</h1><p>你可以把 Stream 想象成一个来自数据源（如集合、数组）的元素队列,并支持聚合操作.它不是一个数据结构,本身也不存储数据.它更像是一个高级的迭代器（Iterator）,可以让你对数据进行一系列的流水线操作.</p><p><em>核心理念：</em></p><ul><li><strong>声明式处理：</strong> 你只需要描述“做什么”,而不需要关心“怎么做”.例如,你想“从一个整数列表中筛选出所有偶数,然后将它们乘以2,最后求和”,使用 Stream 可以非常直观地表达这个过程.</li><li><strong>流水线操作：</strong> 多个操作可以链接在一起,形成一个流水线.数据在流水线中流动,并被逐个操作处理.</li><li><strong>内部迭代：</strong> 与传统的 <code>for</code> 循环（外部迭代）不同,Stream 使用内部迭代,它会自动处理元素的遍历过程,你只需要关注每个元素需要执行的操作.</li></ul><p><em>关键特性:</em></p><ol><li><strong>非存储性  :</strong> Stream 本身不存储任何元素,它只是在源数据上进行计算的视图.</li><li><strong>函数式接口 :</strong> Stream 的操作（如 <code>filter</code>, <code>map</code>, <code>reduce</code>）通常接受 Lambda 表达式或方法引用作为参数,这使得代码非常简洁.</li><li><strong>懒加载/延迟执行 :</strong> 很多 Stream 操作（中间操作）是延迟执行的,这意味着它们不会立即执行,只有在遇到一个触发计算的“终端操作”时,整个流水线才会开始工作,这使得 Stream 可以进行很多优化.</li><li><strong>一次性消费 :</strong> 一个 Stream 只能被消费（即执行终端操作）一次,一旦消费完毕,就不能再被使用.如果需要再次遍历,你需要从数据源重新创建一个新的 Stream.</li><li><strong>可以并行处理 :</strong> Stream API 天然支持并行处理,你可以通过调用 <code>.parallel()</code> 方法轻松地将一个顺序流转换为并行流,从而在多核处理器上利用并行计算来提高性能.</li></ol><p><em>生命周期:</em></p><p>一个典型的 Stream 操作包含三个阶段：</p><ol><li><strong>创建:</strong> 从一个数据源（如 <code>Collection</code>, <code>Array</code>, I/O channel）创建一个 Stream.</li><li><strong>中间操作 :</strong> 对 Stream 进行一系列的转换操作,每个中间操作都会返回一个新的 Stream,这样就可以形成一个链式调用,这些操作是懒执行的.</li><li><strong>终端操作 :</strong> 触发整个 Stream 流水线的执行,并产生最终结果或副作用,终端操作执行后,该 Stream 就被消费掉了.</li></ol><h2 id="创建-stream"><a class="markdownIt-Anchor" href="#创建-stream"></a> 创建 Stream</h2><p>你可以从多种数据源创建 Stream：</p><ul><li><p><strong>从集合创建:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; list = Arrays.asList(<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>);</span><br><span class="line">Stream&lt;String&gt; stream = list.stream(); <span class="comment">// 创建顺序流</span></span><br><span class="line">Stream&lt;String&gt; parallelStream = list.parallelStream(); <span class="comment">// 创建并行流</span></span><br></pre></td></tr></table></figure></li><li><p><strong>从数组创建:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">String[] array = <span class="keyword">new</span> <span class="title class_">String</span>[]&#123;<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>&#125;;</span><br><span class="line">Stream&lt;String&gt; stream = Arrays.stream(array);</span><br></pre></td></tr></table></figure></li><li><p><strong>使用 <code>Stream.of()</code>:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Stream&lt;String&gt; stream = Stream.of(<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>);</span><br></pre></td></tr></table></figure></li><li><p><strong>使用 <code>Stream.generate()</code> 或 <code>Stream.iterate()</code> 创建无限流:</strong></p></li></ul><p>// 创建一个无限流，每个元素都是 “hello” Stream<String> generatedStream = Stream.generate(() -&gt; “hello”);</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 创建一个从0开始，每次加2的无限流 (0, 2, 4, 6, ...)</span></span><br><span class="line">Stream&lt;Integer&gt; iteratedStream = Stream.iterate(<span class="number">0</span>, n -&gt; n + <span class="number">2</span>);</span><br><span class="line">```</span><br><span class="line">对于无限流，通常需要配合 `limit()` 中间操作来限制其大小。</span><br></pre></td></tr></table></figure><h2 id="常用的中间操作"><a class="markdownIt-Anchor" href="#常用的中间操作"></a> 常用的中间操作</h2><p>这些操作会返回一个新的 Stream,并且是懒加载的.</p><ul><li><p><code>filter(Predicate&lt;T&gt; predicate)</code>: 过滤元素,只保留满足条件的元素.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; numbers = Arrays.asList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>);</span><br><span class="line">numbers.stream()</span><br><span class="line">       .filter(n -&gt; n % <span class="number">2</span> == <span class="number">0</span>) <span class="comment">// 只保留偶数</span></span><br><span class="line">       .forEach(System.out::println); <span class="comment">// 输出 2, 4</span></span><br></pre></td></tr></table></figure></li><li><p><code>map(Function&lt;T, R&gt; mapper)</code>: 对每个元素进行转换,映射成一个新的元素.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; words = Arrays.asList(<span class="string">&quot;hello&quot;</span>, <span class="string">&quot;world&quot;</span>);</span><br><span class="line">words.stream()</span><br><span class="line">     .map(String::toUpperCase) <span class="comment">// 将每个单词转为大写</span></span><br><span class="line">     .forEach(System.out::println); <span class="comment">// 输出 HELLO, WORLD</span></span><br></pre></td></tr></table></figure></li><li><p><code>flatMap(Function&lt;T, Stream&lt;R&gt;&gt; mapper)</code>: 将每个元素映射成一个 Stream,然后将所有生成的 Stream 连接成一个单一的 Stream（扁平化处理）.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; lines = Arrays.asList(<span class="string">&quot;Hello World&quot;</span>, <span class="string">&quot;Java Stream&quot;</span>);</span><br><span class="line">lines.stream()</span><br><span class="line">     .flatMap(line -&gt; Arrays.stream(line.split(<span class="string">&quot; &quot;</span>))) <span class="comment">// 将每行拆分成单词流，然后合并</span></span><br><span class="line">     .forEach(System.out::println); <span class="comment">// 输出 Hello, World, Java, Stream</span></span><br></pre></td></tr></table></figure></li><li><p><code>distinct()</code>: 去除重复的元素.</p></li><li><p><code>sorted()</code>: 对元素进行自然排序.</p></li><li><p><code>sorted(Comparator&lt;T&gt; comparator)</code>: 使用自定义比较器进行排序.</p></li><li><p><code>peek(Consumer&lt;T&gt; action)</code>: 对每个元素执行一个操作,主要用于调试.</p></li><li><p><code>limit(long maxSize)</code>: 截断流,使其元素不超过给定数量.</p></li><li><p><code>skip(long n)</code>: 跳过前 n 个元素.</p></li></ul><h2 id="常用的终端操作"><a class="markdownIt-Anchor" href="#常用的终端操作"></a> 常用的终端操作</h2><p>这些操作会触发 Stream 的计算并产生一个最终结果.</p><ul><li><p><strong>遍历:</strong></p><ul><li><code>forEach(Consumer&lt;T&gt; action)</code>: 对每个元素执行一个操作.</li></ul></li><li><p><strong>收集:</strong></p><ul><li><p><code>collect(Collector&lt;T, A, R&gt; collector)</code>: 将 Stream 中的元素收集到一个容器中,如 <code>List</code>, <code>Set</code>, <code>Map</code>.这是最常用的终端操作之一.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; words = Arrays.asList(<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>, <span class="string">&quot;c&quot;</span>);</span><br><span class="line">List&lt;String&gt; upperCaseWords = words.stream()</span><br><span class="line">                                     .map(String::toUpperCase)</span><br><span class="line">                                     .collect(Collectors.toList()); <span class="comment">// 收集到 List</span></span><br></pre></td></tr></table></figure></li><li><p><code>toArray()</code>: 将 Stream 转换为数组.</p></li></ul></li><li><p><strong>归约 (Reduce):</strong></p><ul><li><p><code>reduce(T identity, BinaryOperator&lt;T&gt; accumulator)</code>: 将所有元素聚合成一个结果.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; numbers = Arrays.asList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>);</span><br><span class="line"><span class="type">int</span> <span class="variable">sum</span> <span class="operator">=</span> numbers.stream()</span><br><span class="line">                 .reduce(<span class="number">0</span>, (a, b) -&gt; a + b); <span class="comment">// 计算总和，结果为 15</span></span><br></pre></td></tr></table></figure></li><li><p><code>count()</code>: 返回流中元素的总数.</p></li><li><p><code>min(Comparator&lt;T&gt; comparator)</code>: 返回流中的最小元素.</p></li><li><p><code>max(Comparator&lt;T&gt; comparator)</code>: 返回流中的最大元素.</p></li></ul></li><li><p><strong>匹配:</strong></p><ul><li><code>anyMatch(Predicate&lt;T&gt; predicate)</code>: 是否有任何一个元素匹配给定条件.</li><li><code>allMatch(Predicate&lt;T&gt; predicate)</code>: 是否所有元素都匹配给定条件.</li><li><code>noneMatch(Predicate&lt;T&gt; predicate)</code>: 是否没有元素匹配给定条件.</li></ul></li><li><p><strong>查找:</strong></p><ul><li><code>findFirst()</code>: 返回第一个元素（通常用于串行流）.</li><li><code>findAny()</code>: 返回任意一个元素（通常用于并行流，性能更好）.</li></ul></li></ul><h2 id="使用-stream-的好处"><a class="markdownIt-Anchor" href="#使用-stream-的好处"></a> 使用 Stream 的好处</h2><ol><li><strong>代码简洁优雅:</strong> 链式调用和 Lambda 表达式使得代码更短、更易读,能够清晰地表达业务逻辑.</li><li><strong>高效:</strong> 通过懒加载和内部优化（如短路操作）,Stream 在某些情况下比传统循环更高效.</li><li><strong>易于并行化:</strong> 无需编写复杂的并发代码,只需调用 <code>.parallelStream()</code> 或 <code>.parallel()</code> 即可利用多核CPU的优势,处理大规模数据时效果显著.</li></ol><h2 id="注意事项"><a class="markdownIt-Anchor" href="#注意事项"></a> 注意事项</h2><ul><li><strong>无状态的 Lambda:</strong> 传递给 Stream 操作的 Lambda 表达式最好是无状态的（即不依赖于任何外部可变状态）,这样才能保证在并行计算时结果的正确性.</li><li><strong>性能考量:</strong> 并非所有场景下 Stream 都比传统循环快.对于简单、数据量小的操作,传统 <code>for</code> 循环的开销可能更小.并行流在数据量大、单个元素处理耗时较长的情况下才能发挥优势,否则线程切换的开销可能得不偿失.</li><li><strong>调试困难:</strong> 由于懒加载和内部迭代的特性,调试 Stream 流水线比调试传统循环要困难一些,可以使用 <code>peek</code> 操作来帮助观察流中元素的状态.</li></ul><hr /><h1 id="collections工具类"><a class="markdownIt-Anchor" href="#collections工具类"></a> Collections工具类</h1><p><code>java.util.Collections</code> 是一个针对集合（Collection）操作的工具类,提供了大量静态方法,用于操作或返回集合.你可以把它看作是处理 <code>List</code>、<code>Set</code>、<code>Map</code> 等集合类型的“瑞士军刀”.它的所有方法都是静态的,因此你无需创建 <code>Collections</code> 类的实例,直接通过类名调用即可.</p><p><code>Collections</code> 工具类的核心功能可以分为以下几类：</p><h2 id="排序"><a class="markdownIt-Anchor" href="#排序"></a> 排序</h2><p>这是最常用的功能之一,用于对 <code>List</code> 集合进行排序.</p><ul><li><code>sort(List&lt;T&gt; list)</code>: 根据元素的自然顺序对列表进行升序排序,要求列表中的元素必须实现 <code>Comparable</code> 接口.</li><li><code>sort(List&lt;T&gt; list, Comparator&lt;? super T&gt; c)</code>: 根据指定的 <code>Comparator</code> 对列表进行排序.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; numbers = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(Arrays.asList(<span class="number">5</span>, <span class="number">2</span>, <span class="number">8</span>, <span class="number">1</span>, <span class="number">9</span>));</span><br><span class="line">Collections.sort(numbers);</span><br><span class="line">System.out.println(numbers); <span class="comment">// 输出: [1, 2, 5, 8, 9]</span></span><br><span class="line"></span><br><span class="line">List&lt;String&gt; names = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(Arrays.asList(<span class="string">&quot;Alice&quot;</span>, <span class="string">&quot;Bob&quot;</span>, <span class="string">&quot;Charlie&quot;</span>));</span><br><span class="line"><span class="comment">// 按字符串长度降序排序</span></span><br><span class="line">Collections.sort(names, (s1, s2) -&gt; s2.length() - s1.length());</span><br><span class="line">System.out.println(names); <span class="comment">// 输出: [Charlie, Alice, Bob]</span></span><br></pre></td></tr></table></figure><h2 id="查找与替换"><a class="markdownIt-Anchor" href="#查找与替换"></a> 查找与替换</h2><ul><li><code>binarySearch(List&lt;? extends Comparable&lt;? super T&gt;&gt; list, T key)</code>: 在一个<strong>已排序</strong>的列表中使用二分查找法查找指定元素.如果列表未排序,结果是未定义的.</li><li><code>replaceAll(List&lt;T&gt; list, T oldVal, T newVal)</code>: 替换列表中所有指定的旧值为新值.</li><li><code>max(Collection&lt;? extends T&gt; coll)</code> / <code>min(Collection&lt;? extends T&gt; coll)</code>: 查找集合中的最大/最小元素（根据自然顺序）.</li><li><code>frequency(Collection&lt;?&gt; c, Object o)</code>: 统计指定元素在集合中出现的次数.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; sortedNumbers = Arrays.asList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">5</span>, <span class="number">8</span>, <span class="number">9</span>);</span><br><span class="line"><span class="type">int</span> <span class="variable">index</span> <span class="operator">=</span> Collections.binarySearch(sortedNumbers, <span class="number">8</span>);</span><br><span class="line">System.out.println(<span class="string">&quot;8的位置: &quot;</span> + index); <span class="comment">// 输出: 3</span></span><br><span class="line"></span><br><span class="line">Collections.replaceAll(numbers, <span class="number">2</span>, <span class="number">200</span>);</span><br><span class="line">System.out.println(numbers); <span class="comment">// 输出: [1, 200, 5, 8, 9]</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="variable">max</span> <span class="operator">=</span> Collections.max(numbers);</span><br><span class="line">System.out.println(<span class="string">&quot;最大值: &quot;</span> + max); <span class="comment">// 输出: 200</span></span><br></pre></td></tr></table></figure><h2 id="同步控制"><a class="markdownIt-Anchor" href="#同步控制"></a> 同步控制</h2><p>在 Java 早期,<code>Vector</code> 和 <code>Hashtable</code> 是线程安全的集合,但性能较差.<code>ArrayList</code> 和 <code>HashMap</code> 等是线程不安全的,<code>Collections</code> 工具类提供了一系列包装方法,可以将线程不安全的集合包装成线程安全的版本.</p><ul><li><code>synchronizedList(List&lt;T&gt; list)</code></li><li><code>synchronizedSet(Set&lt;T&gt; s)</code></li><li><code>synchronizedMap(Map&lt;K,V&gt; m)</code></li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; unsafeList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">List&lt;String&gt; safeList = Collections.synchronizedList(unsafeList);</span><br><span class="line"><span class="comment">// safeList 的所有操作都是线程安全的</span></span><br></pre></td></tr></table></figure><p><strong>注意:</strong> 这种方式是通过在每个方法上加锁（<code>synchronized</code>）来实现的,性能开销较大.在现代 Java 并发编程中,更推荐使用 <code>java.util.concurrent</code> 包下的并发集合,如 <code>ConcurrentHashMap</code>、<code>CopyOnWriteArrayList</code> 等.</p><h2 id="创建不可变集合"><a class="markdownIt-Anchor" href="#创建不可变集合"></a> 创建不可变集合</h2><p>用于创建一个只读的集合视图.任何尝试修改这个视图的操作（如 <code>add</code>, <code>remove</code>, <code>set</code>）都会抛出 <code>UnsupportedOperationException</code>.这对于保护数据不被意外修改非常有用.</p><ul><li><code>unmodifiableList(List&lt;? extends T&gt; list)</code></li><li><code>unmodifiableSet(Set&lt;? extends T&gt; s)</code></li><li><code>unmodifiableMap(Map&lt;? extends K,? extends V&gt; m)</code></li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; mutableList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">mutableList.add(<span class="string">&quot;a&quot;</span>);</span><br><span class="line">List&lt;String&gt; unmodifiableList = Collections.unmodifiableList(mutableList);</span><br><span class="line"><span class="comment">// unmodifiableList.add(&quot;b&quot;); // 这行代码会抛出 UnsupportedOperationException</span></span><br></pre></td></tr></table></figure><h2 id="其他实用方法"><a class="markdownIt-Anchor" href="#其他实用方法"></a> 其他实用方法</h2><ul><li><code>shuffle(List&lt;?&gt; list)</code>: 随机打乱列表中的元素顺序.</li><li><code>reverse(List&lt;?&gt; list)</code>: 反转列表中的元素顺序.</li><li><code>fill(List&lt;? super T&gt; list, T obj)</code>: 用指定对象填充整个列表.</li><li><code>emptyList()</code>, <code>emptySet()</code>, <code>emptyMap()</code>: 返回一个空的、不可变的集合.</li></ul><h2 id="collections-工具类-vs-stream-api-对比"><a class="markdownIt-Anchor" href="#collections-工具类-vs-stream-api-对比"></a> Collections 工具类 vs. Stream API 对比</h2><p>现在,我们将 <code>Collections</code> 和 <code>Stream</code> 进行一个详细的对比.它们是解决不同问题的工具,但有时在功能上会有重叠（例如排序）.</p><table><thead><tr><th>特性</th><th>java.util.Collections</th><th>java.util.Stream</th></tr></thead><tbody><tr><td><strong>核心目的</strong></td><td>提供一系列<strong>静态方法</strong>来直接操作或包装现有的集合.</td><td>提供一种<strong>声明式</strong>的数据处理流水线,用于对数据源进行复杂的查询和转换.</td></tr><tr><td><strong>操作范式</strong></td><td><strong>命令式 (Imperative)</strong>：直接告诉程序“如何做”，比如 <code>Collections.sort(list)</code>.</td><td><strong>声明式 (Declarative)</strong>：描述“做什么”,而不是“如何做”,比如 <code>list.stream().sorted()</code>.</td></tr><tr><td><strong>数据源</strong></td><td>只能是 <code>Collection</code> 框架下的具体实现，如 <code>List</code>,<code>Set</code>.</td><td>可以是 <code>Collection</code>、数组、I/O Channel、生成函数等任何可以转换为流的数据源.</td></tr><tr><td><strong>修改原始数据</strong></td><td><strong>会修改</strong>原始集合.例如 <code>sort</code>, <code>shuffle</code>, <code>reverse</code> 都会直接改变传入的 <code>List</code>.</td><td><strong>不会修改</strong>原始数据源,它通过流水线操作生成一个新的结果或集合视图.</td></tr><tr><td><strong>执行模型</strong></td><td><strong>及早求值 (Eager Evaluation)</strong>：调用方法后立即执行,并完成操作.</td><td><strong>延迟执行 (Lazy Evaluation)</strong>：中间操作（如 <code>filter</code>, <code>map</code>）不立即执行,只有当终端操作被调用时才开始整个处理过程.</td></tr><tr><td><strong>链式调用</strong></td><td>不支持.每个操作都是独立的静态方法调用.</td><td>核心特性.多个中间操作可以链接在一起,形成一个优雅的处理流水线.</td></tr><tr><td><strong>并行处理</strong></td><td>不直接支持.需要手动管理多线程.</td><td>内建支持.通过 <code>.parallel()</code> 或 <code>.parallelStream()</code> 可以轻松实现并行处理.</td></tr><tr><td><strong>典型用例</strong></td><td>- 对列表进行<strong>原地</strong>排序、反转、打乱.<br>- 将集合包装为线程安全或不可修改的版本.<br>- 在集合中查找最大/最小值.</td><td>- 复杂的数据处理和转换，如筛选、映射、分组、归约.<br>- 对大数据集合进行并行计算.<br>- 需要组合多个操作形成流水线的场景.</td></tr></tbody></table><h2 id="核心差异详解"><a class="markdownIt-Anchor" href="#核心差异详解"></a> 核心差异详解</h2><ol><li><strong>修改性:</strong> 这是最根本的区别.<ul><li><code>Collections.sort(myList);</code> 会<strong>直接改变</strong> <code>myList</code> 内部元素的顺序.这是一种<strong>破坏性</strong>操作.</li><li><code>myList.stream().sorted().collect(Collectors.toList());</code> <strong>不会改变</strong> <code>myList</code>.它会创建一个新的、排好序的 <code>List</code> 作为结果返回。这是一种<strong>非破坏性</strong>操作,更符合函数式编程的理念.</li></ul></li><li><strong>迭代方式 :</strong><ul><li><code>Collections</code> 的操作通常是<strong>外部迭代</strong>.即使你看不到循环,它的实现内部也是通过循环来遍历和修改集合的.</li><li><code>Stream</code> 使用的是<strong>内部迭代</strong>.你将行为（Lambda 表达式）传递给 Stream,由 Stream API 自身来控制迭代过程,这也为并行化和性能优化提供了可能.</li></ul></li><li><strong>表达力:</strong><ul><li><code>Collections</code> 的方法是单一、离散的.如果你想先过滤再排序,需要分多步操作,可能会创建中间集合.</li><li><code>Stream</code> 通过链式调用,可以将多个操作（<code>filter</code>, <code>map</code>, <code>sorted</code> 等）组合成一个连贯、易读的语句,无需创建中间变量或集合.</li></ul></li></ol><p>假设有一个 <code>Person</code> 对象的 <code>List</code>，我们想找出所有成年人,按年龄排序,并获取他们的名字列表.</p><p><strong>使用传统 <code>Collections</code> 的方式（命令式）：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 筛选出成年人，存入新列表</span></span><br><span class="line">List&lt;Person&gt; adults = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span> (Person p : people) &#123;</span><br><span class="line">    <span class="keyword">if</span> (p.getAge() &gt;= <span class="number">18</span>) &#123;</span><br><span class="line">        adults.add(p);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 对新列表进行排序</span></span><br><span class="line">Collections.sort(adults, <span class="keyword">new</span> <span class="title class_">Comparator</span>&lt;Person&gt;() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">compare</span><span class="params">(Person p1, Person p2)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> Integer.compare(p1.getAge(), p2.getAge());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 提取名字</span></span><br><span class="line">List&lt;String&gt; adultNames = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span> (Person p : adults) &#123;</span><br><span class="line">    adultNames.add(p.getName());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个过程繁琐,需要创建中间集合 <code>adults</code>,并且代码逻辑分散.</p><p><strong>使用 <code>Stream</code> API 的方式（声明式）：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; adultNames = people.stream()                     <span class="comment">// 1. 创建流</span></span><br><span class="line">                         .filter(p -&gt; p.getAge() &gt;= <span class="number">18</span>)  <span class="comment">// 2. 筛选</span></span><br><span class="line">                         .sorted(Comparator.comparingInt(Person::getAge)) <span class="comment">// 3. 排序</span></span><br><span class="line">                         .map(Person::getName)           <span class="comment">// 4. 提取名字</span></span><br><span class="line">                         .collect(Collectors.toList());    <span class="comment">// 5. 收集结果</span></span><br></pre></td></tr></table></figure><p>这个过程一气呵成,代码简洁、易读，并且逻辑非常清晰.所有操作在一个流水线中完成,没有显式的中间集合.</p><ul><li>当你需要对一个已有的 <code>List</code> <strong>进行原地修改</strong>（如排序、打乱）,或者需要一个快速的方法来获取集合的<strong>同步/不可变包装</strong>时,<code>Collections</code> 工具类是你的首选.它的方法简单直接,易于理解.</li><li>当你需要处理复杂的数据查询和转换逻辑，特别是当这些逻辑涉及多个步骤（<strong>过滤、映射、排序、分组</strong>等）时，<code>Stream</code> API 是更强大、更优雅的选择。它通过<strong>非破坏性</strong>的链式操作和<strong>内部迭代</strong>,让代码更具可读性和可维护性，并且能轻松地利用多核处理器进行并行计算.</li></ul><p>在现代 Java 开发中,**<code>Stream</code> API 已经成为处理集合数据的首选方式,**而 <code>Collections</code> 工具类则继续在它擅长的特定领域（如原地排序、集合包装）发挥作用.两者相辅相成,共同构成了 Java 强大的集合处理工具集.</p><hr /><h1 id="字符集"><a class="markdownIt-Anchor" href="#字符集"></a> 字符集</h1><ol><li>标准<code>ASCLL</code>字符集( 美国信息交换标准代码)<ul><li>使用一个字节存储一个字符</li></ul></li><li><code>GBK</code>(汉字内码扩展规范,国标)<ul><li>一个中文编码成两个字节的形式存储</li><li><code>GBK</code>兼容了<code>ASCLL</code>字符集</li><li>第一个字节的第一位必须是1</li></ul></li><li><code>UTF-8</code>(Unicode字符集的一种编码方案)<ul><li>采取可变长编码方案</li><li>英文字符和数字只占一个字节,汉字字符占用三个字节</li></ul></li></ol><p><em>注意:</em></p><p>字符编码时使用的字符集和解码时使用的字符集必须一致,否则会乱码.</p><hr /><h1 id="io-流"><a class="markdownIt-Anchor" href="#io-流"></a> IO 流</h1><p>你可以把 IO 流想象成一个连接数据源和程序的“管道”.</p><ul><li>**输入流 **：从数据源（如文件、网络连接、内存数组）读取数据到程序中,就像从水库抽水到你家.</li><li><strong>输出流</strong>：将程序中的数据写入到目标位置（如文件、网络连接、内存数组）,就像把你家的水排到下水道.</li></ul><p>这个“管道”是单向的,要么是输入,要么是输出,数据在管道中像水流一样,按照顺序一个接一个地流动,因此被称为“流”.</p><h2 id="按数据流向划分"><a class="markdownIt-Anchor" href="#按数据流向划分"></a> 按数据流向划分</h2><ul><li>**输入流 **：用于读取数据,所有输入流的基类是 <code>java.io.InputStream</code> (字节输入流) 和 <code>java.io.Reader</code> (字符输入流).</li><li>**输出流 **：用于写入数据,所有输出流的基类是 <code>java.io.OutputStream</code> (字节输出流) 和 <code>java.io.Writer</code> (字符输出流).</li></ul><h2 id="按处理的数据单元划分"><a class="markdownIt-Anchor" href="#按处理的数据单元划分"></a> 按处理的数据单元划分</h2><p>这是最重要的分类方式,直接决定了你如何处理数据.</p><ul><li>**字节流 **：<ul><li>处理最基本的数据单位：字节 (byte),即 8 位二进制.</li><li>可以处理任何类型的文件,包括文本、图片、音频、视频等二进制文件.</li><li>基类是 <code>InputStream</code> 和 <code>OutputStream</code>.</li><li>常见的子类有 <code>FileInputStream</code>, <code>FileOutputStream</code>,<code>BufferedInputStream</code>, <code>BufferedOutputStream</code>.</li></ul></li><li>**字符流 **：<ul><li>处理的数据单位是字符 (char),Java 内部使用 Unicode 编码（一个字符占 2 个字节）.</li><li>专门用于处理纯文本数据（<code>.txt</code>, <code>.java</code>, <code>.xml</code> 等）,它能自动处理字符编码的转换问题.</li><li>基类是 <code>Reader</code> 和 <code>Writer</code>.</li><li>常见的子类有 <code>FileReader</code>,<code>FileWriter</code>,<code>BufferedReader</code>,<code>BufferedWriter</code>.</li></ul></li></ul><p><strong>关键区别</strong>：字节流是万能的,但处理文本时需要自己处理编码问题,字符流专门为文本而生,更方便、高效,且能避免乱码问题.</p><h2 id="按功能角色划分"><a class="markdownIt-Anchor" href="#按功能角色划分"></a> 按功能角色划分</h2><ul><li>**节点流 **：<ul><li>直接与数据源或目标设备相连接的“管道”,负责实际的读写操作.</li><li>例如：<code>FileInputStream</code> 直接连接到文件,<code>ByteArrayInputStream</code> 直接连接到内存中的字节数组.</li><li>你可以把它看作是“基础管道”.</li></ul></li><li>**处理流  / 包装流 **：<ul><li>不直接连接数据源,而是“包装”在已有的节点流之上,为其增加额外的功能.</li><li>例如：<code>BufferedInputStream</code> 包装在 <code>FileInputStream</code> 之上,为其增加缓冲功能以提高读写效率,<code>ObjectInputStream</code> 包装在其他输入流之上,使其能够直接读取 Java 对象（反序列化）.</li><li>你可以把它看作是“给基础管道加装的各种功能阀门或过滤器”,比如加速器、转换器等.</li></ul></li></ul><h2 id="文件流-字节流"><a class="markdownIt-Anchor" href="#文件流-字节流"></a> 文件流 - 字节流</h2><p>这是最基础的读写文件的方式.</p><p><strong>场景</strong>：读写图片、音频或任何二进制文件.</p><p><strong>示例：将一张图片从 <code>res/raw</code> 复制到应用的内部存储</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 从 raw 文件夹获取输入流</span></span><br><span class="line"><span class="type">InputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> getResources().openRawResource(R.raw.my_image);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建内部存储的文件输出流</span></span><br><span class="line"><span class="comment">// getFilesDir() 获取应用的内部存储路径，如 /data/data/com.example.app/files</span></span><br><span class="line"><span class="type">File</span> <span class="variable">outputFile</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">File</span>(getFilesDir(), <span class="string">&quot;copied_image.png&quot;</span>);</span><br><span class="line"><span class="type">FileOutputStream</span> <span class="variable">fileOutputStream</span> <span class="operator">=</span> <span class="literal">null</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    fileOutputStream = <span class="keyword">new</span> <span class="title class_">FileOutputStream</span>(outputFile);</span><br><span class="line">    <span class="type">byte</span>[] buffer = <span class="keyword">new</span> <span class="title class_">byte</span>[<span class="number">1024</span>]; <span class="comment">// 创建一个 1KB 的缓冲区</span></span><br><span class="line">    <span class="type">int</span> length;</span><br><span class="line">    <span class="comment">// 循环读取输入流的数据到缓冲区，然后写入输出流</span></span><br><span class="line">    <span class="keyword">while</span> ((length = inputStream.read(buffer)) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        fileOutputStream.write(buffer, <span class="number">0</span>, length);</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (inputStream != <span class="literal">null</span>) &#123;</span><br><span class="line">            inputStream.close();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (fileOutputStream != <span class="literal">null</span>) &#123;</span><br><span class="line">            fileOutputStream.close();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="文件字符流-字符流"><a class="markdownIt-Anchor" href="#文件字符流-字符流"></a> 文件字符流 - 字符流</h2><p><strong>场景</strong>：读写纯文本文件,如 JSON 字符串、日志文件.</p><p><strong>示例：向内部存储写入和读取一个文本文件</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// --- 写入文件 ---</span></span><br><span class="line"><span class="type">File</span> <span class="variable">file</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">File</span>(getFilesDir(), <span class="string">&quot;my_text.txt&quot;</span>);</span><br><span class="line"><span class="keyword">try</span> (<span class="type">FileWriter</span> <span class="variable">writer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">FileWriter</span>(file)) &#123; <span class="comment">// try-with-resources 语法，自动关闭流</span></span><br><span class="line">    writer.write(<span class="string">&quot;Hello, Android IO Stream!&quot;</span>);</span><br><span class="line">    writer.append(<span class="string">&quot;\nThis is a new line.&quot;</span>);</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// --- 读取文件 ---</span></span><br><span class="line"><span class="keyword">try</span> (<span class="type">FileReader</span> <span class="variable">reader</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">FileReader</span>(file)) &#123;</span><br><span class="line">    <span class="type">char</span>[] buffer = <span class="keyword">new</span> <span class="title class_">char</span>[<span class="number">128</span>];</span><br><span class="line">    <span class="type">int</span> length;</span><br><span class="line">    <span class="type">StringBuilder</span> <span class="variable">stringBuilder</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StringBuilder</span>();</span><br><span class="line">    <span class="keyword">while</span> ((length = reader.read(buffer)) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        stringBuilder.append(buffer, <span class="number">0</span>, length);</span><br><span class="line">    &#125;</span><br><span class="line">    Log.d(<span class="string">&quot;IO_TEST&quot;</span>, <span class="string">&quot;Read from file: &quot;</span> + stringBuilder.toString());</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="缓冲流-处理流"><a class="markdownIt-Anchor" href="#缓冲流-处理流"></a> 缓冲流 - 处理流</h2><p>直接对文件进行频繁的读写操作,性能会比较低.缓冲流内部维护一个缓冲区（一个内存数组）,它会一次性从文件中读取大量数据到缓冲区,或将程序中的大量数据先写入缓冲区,然后一次性写入文件.这大大减少了与物理磁盘的交互次数,从而显著提高性能.</p><p><strong>强烈建议在所有文件 IO 操作中都使用缓冲流进行包装！</strong></p><p><strong>示例：使用缓冲流高效地读写文本文件</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// --- 使用 BufferedWriter 高效写入 ---</span></span><br><span class="line"><span class="type">File</span> <span class="variable">file</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">File</span>(getFilesDir(), <span class="string">&quot;buffered_log.txt&quot;</span>);</span><br><span class="line"><span class="keyword">try</span> (<span class="type">BufferedWriter</span> <span class="variable">writer</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BufferedWriter</span>(<span class="keyword">new</span> <span class="title class_">FileWriter</span>(file))) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">        writer.write(<span class="string">&quot;Log entry number &quot;</span> + i);</span><br><span class="line">        writer.newLine(); <span class="comment">// 方便地写入一个换行符</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// --- 使用 BufferedReader 高效读取 ---</span></span><br><span class="line"><span class="keyword">try</span> (<span class="type">BufferedReader</span> <span class="variable">reader</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">BufferedReader</span>(<span class="keyword">new</span> <span class="title class_">FileReader</span>(file))) &#123;</span><br><span class="line">    String line;</span><br><span class="line">    <span class="keyword">while</span> ((line = reader.readLine()) != <span class="literal">null</span>) &#123; <span class="comment">// readLine() 一次读取一行，非常方便</span></span><br><span class="line">        Log.d(<span class="string">&quot;IO_TEST&quot;</span>, line);</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="对象流-处理流"><a class="markdownIt-Anchor" href="#对象流-处理流"></a> 对象流 - 处理流</h2><p><strong>场景</strong>：当需要直接将 Java 对象完整地保存到文件或通过网络传输时,可以使用对象流.这个过程称为<strong>序列化</strong>（写入对象）和<strong>反序列化</strong>（读取对象）.</p><p><strong>注意</strong>：要被序列化的对象所属的类必须实现 <code>java.io.Serializable</code> 接口.</p><p><strong>示例：保存和读取一个 User 对象</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 定义一个可序列化的 User 类</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> <span class="keyword">implements</span> <span class="title class_">java</span>.io.Serializable &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> <span class="number">1L</span>; <span class="comment">// 建议显式声明</span></span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> age;</span><br><span class="line">    <span class="comment">// ... 构造函数等</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// --- 序列化：将 User 对象写入文件 ---</span></span><br><span class="line"><span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">User</span>(<span class="string">&quot;Alice&quot;</span>, <span class="number">25</span>);</span><br><span class="line"><span class="type">File</span> <span class="variable">file</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">File</span>(getFilesDir(), <span class="string">&quot;user.dat&quot;</span>);</span><br><span class="line"><span class="keyword">try</span> (<span class="type">ObjectOutputStream</span> <span class="variable">oos</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectOutputStream</span>(<span class="keyword">new</span> <span class="title class_">FileOutputStream</span>(file))) &#123;</span><br><span class="line">    oos.writeObject(user);</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// --- 反序列化：从文件中读取 User 对象 ---</span></span><br><span class="line"><span class="keyword">try</span> (<span class="type">ObjectInputStream</span> <span class="variable">ois</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ObjectInputStream</span>(<span class="keyword">new</span> <span class="title class_">FileInputStream</span>(file))) &#123;</span><br><span class="line">    <span class="type">User</span> <span class="variable">savedUser</span> <span class="operator">=</span> (User) ois.readObject();</span><br><span class="line">    Log.d(<span class="string">&quot;IO_TEST&quot;</span>, <span class="string">&quot;User: &quot;</span> + savedUser.name + <span class="string">&quot;, Age: &quot;</span> + savedUser.age);</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException | ClassNotFoundException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="最佳实践"><a class="markdownIt-Anchor" href="#最佳实践"></a> 最佳实践</h2><ol><li><strong>明确数据类型</strong>：首先确定你要处理的是文本数据还是二进制数据.<ul><li><strong>文本</strong> -&gt; 优先使用 <strong>字符流</strong> (<code>Reader</code>/<code>Writer</code>).</li><li><strong>非文本 (图片、音频等)</strong> -&gt; 必须使用 <strong>字节流</strong> (<code>InputStream</code>/<code>OutputStream</code>).</li></ul></li><li><strong>性能优先</strong>：无论使用哪种流,都强烈建议使用<strong>缓冲流</strong> (<code>Buffered...</code>) 进行包装,以提高 IO 性能.</li><li><strong>代码简洁与安全</strong>：尽量使用 <strong>try-with-resources</strong> 语句来管理流,它能确保在代码块执行完毕后,无论是否发生异常,流都会被自动关闭,避免了资源泄漏.</li><li><strong>关闭流</strong>：如果未使用 <code>try-with-resources</code>,必须在 <code>finally</code> 块中手动调用 <code>close()</code> 方法关闭流,并处理可能抛出的 <code>IOException</code>.</li><li><strong>字符编码</strong>：在使用字符流时,特别是 <code>InputStreamReader</code> 和 <code>OutputStreamWriter</code>（它们是字节流和字符流之间的桥梁）,可以指定字符编码（如 “UTF-8”）,这在处理网络数据或特定编码的文本文件时非常重要,可以有效防止乱码.</li></ol><hr /><h1 id="多线程"><a class="markdownIt-Anchor" href="#多线程"></a> 多线程</h1><h2 id="继承-thread-类"><a class="markdownIt-Anchor" href="#继承-thread-类"></a> 继承 Thread 类</h2><p>这是最直观的一种创建线程的方式,你需要创建一个类,让它直接继承 <code>java.lang.Thread</code> 类,然后重写 <code>run()</code> 方法,<code>run()</code> 方法中包含了该线程需要执行的任务代码.</p><p><em>实现步骤：</em></p><ol><li>定义一个类继承 <code>Thread</code>.</li><li>重写父类的 <code>run()</code> 方法,将线程要执行的逻辑写在其中.</li><li>创建该子类的实例.</li><li>调用实例的 <code>start()</code> 方法来启动线程,JVM 会在新的线程中调用 <code>run()</code> 方法.</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyThread</span> <span class="keyword">extends</span> <span class="title class_">Thread</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 线程需要执行的任务</span></span><br><span class="line">        System.out.println(<span class="string">&quot;MyThread is running. Thread ID: &quot;</span> + Thread.currentThread().getId());</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 模拟执行任务</span></span><br><span class="line">            Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;MyThread finished.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 创建线程实例</span></span><br><span class="line">        <span class="type">MyThread</span> <span class="variable">thread1</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MyThread</span>();</span><br><span class="line">        <span class="comment">// 启动线程</span></span><br><span class="line">        thread1.start(); <span class="comment">// 注意是调用 start() 而不是 run()</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><em>优点：</em></p><ul><li>实现简单,代码直观,易于理解.</li><li>可以直接在类内部通过 <code>this</code> 关键字获取当前线程的引用,方便进行操作.</li></ul><p><em>缺点：</em></p><ul><li><strong>Java 是单继承的</strong>.如果你的类已经继承了另一个类,就无法再继承 <code>Thread</code> 类了,这大大限制了其使用场景.</li><li><strong>任务与线程耦合度高</strong>.线程的创建（<code>Thread</code>）和线程要执行的任务（<code>run</code> 方法的逻辑）绑定在同一个类中,不符合面向对象的“高内聚,低耦合”原则.</li></ul><h2 id="实现-runnable-接口"><a class="markdownIt-Anchor" href="#实现-runnable-接口"></a> 实现 Runnable 接口</h2><p>这是更常用、更推荐的一种方式.你需要创建一个类实现 <code>java.lang.Runnable</code> 接口,并实现其唯一的 <code>run()</code> 方法,然后将这个 <code>Runnable</code> 的实例作为参数传递给一个 <code>Thread</code> 类的构造函数来创建线程.</p><p><em>实现步骤：</em></p><ol><li>定义一个类实现 <code>Runnable</code> 接口.</li><li>实现接口中的 <code>run()</code> 方法.</li><li>创建该实现类的实例.</li><li>创建一个 <code>Thread</code> 对象,并将 <code>Runnable</code> 实例作为构造函数的参数传入.</li><li>调用 <code>Thread</code> 对象的 <code>start()</code> 方法.</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyRunnable</span> <span class="keyword">implements</span> <span class="title class_">Runnable</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 线程需要执行的任务</span></span><br><span class="line">        System.out.println(<span class="string">&quot;MyRunnable is running. Thread ID: &quot;</span> + Thread.currentThread().getId());</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;MyRunnable finished.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 创建任务实例</span></span><br><span class="line">        <span class="type">MyRunnable</span> <span class="variable">myRunnable</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MyRunnable</span>();</span><br><span class="line">        <span class="comment">// 2. 创建线程实例，并将任务传递进去</span></span><br><span class="line">        <span class="type">Thread</span> <span class="variable">thread2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Thread</span>(myRunnable);</span><br><span class="line">        <span class="comment">// 3. 启动线程</span></span><br><span class="line">        thread2.start();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 使用 Lambda 表达式可以更简洁 (Java 8+)</span></span><br><span class="line">        <span class="type">Thread</span> <span class="variable">thread3</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;Lambda Runnable is running. Thread ID: &quot;</span> + Thread.currentThread().getId());</span><br><span class="line">        &#125;);</span><br><span class="line">        thread3.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><em>优点：</em></p><ul><li><strong>解耦合</strong>.将线程的创建和要执行的任务分离开来,<code>Runnable</code> 对象只关心任务本身,而 <code>Thread</code> 对象只负责线程的创建和管理.</li><li><strong>避免单继承的局限性</strong>.你的任务类可以继承其他任何类,因为它只是实现了一个接口.</li><li><strong>资源共享</strong>.多个线程可以共享同一个 <code>Runnable</code> 实例,这使得在处理共享资源时非常方便.</li></ul><p><em>缺点：</em></p><ul><li><code>run()</code> 方法没有返回值.</li><li><code>run()</code> 方法不能抛出受检异常,只能在内部 <code>try-catch</code>.</li></ul><h2 id="实现-callable-接口"><a class="markdownIt-Anchor" href="#实现-callable-接口"></a> 实现 Callable 接口</h2><p><code>Callable</code> 接口是在 Java 1.5 中引入的,是对 <code>Runnable</code> 的一个重要补充,它解决了 <code>Runnable</code> 不能有返回值和不能抛出异常的痛点.</p><p><code>Callable</code> 通常与 <code>FutureTask</code> 或线程池（<code>ExecutorService</code>）结合使用.</p><p><em>实现步骤：</em></p><ol><li>定义一个类实现 <code>Callable&lt;V&gt;</code> 接口,其中 <code>V</code> 是你希望返回值的类型.</li><li>实现接口中的 <code>call()</code> 方法,该方法可以有返回值,也可以抛出异常.</li><li>创建一个 <code>FutureTask&lt;V&gt;</code> 对象,用 <code>Callable</code> 实例作为其构造函数的参数,<code>FutureTask</code> 既是一个 <code>Runnable</code>,又可以持有 <code>Callable</code> 的返回值.</li><li>将 <code>FutureTask</code> 对象作为参数传递给 <code>Thread</code> 构造函数并启动线程.</li><li>通过 <code>FutureTask</code> 对象的 <code>get()</code> 方法获取线程执行完毕后的返回值.</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyCallable</span> <span class="keyword">implements</span> <span class="title class_">Callable</span>&lt;String&gt; &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">call</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123; <span class="comment">// 可以抛出异常</span></span><br><span class="line">        System.out.println(<span class="string">&quot;MyCallable is running. Thread ID: &quot;</span> + Thread.currentThread().getId());</span><br><span class="line">        Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">        <span class="comment">// 可以有返回值</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;Task completed successfully!&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> ExecutionException, InterruptedException &#123;</span><br><span class="line">        <span class="comment">// 1. 创建 Callable 任务实例</span></span><br><span class="line">        <span class="type">MyCallable</span> <span class="variable">myCallable</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MyCallable</span>();</span><br><span class="line">        <span class="comment">// 2. 使用 FutureTask 包装 Callable</span></span><br><span class="line">        FutureTask&lt;String&gt; futureTask = <span class="keyword">new</span> <span class="title class_">FutureTask</span>&lt;&gt;(myCallable);</span><br><span class="line">        <span class="comment">// 3. 创建线程，并将 FutureTask 作为任务</span></span><br><span class="line">        <span class="type">Thread</span> <span class="variable">thread4</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Thread</span>(futureTask);</span><br><span class="line">        <span class="comment">// 4. 启动线程</span></span><br><span class="line">        thread4.start();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 5. 获取线程执行结果</span></span><br><span class="line">        System.out.println(<span class="string">&quot;Main thread is waiting for result...&quot;</span>);</span><br><span class="line">        <span class="comment">// get() 方法会阻塞主线程，直到子线程执行完毕并返回结果</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> futureTask.get();</span><br><span class="line">        System.out.println(<span class="string">&quot;Result from MyCallable: &quot;</span> + result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><em>优点：</em></p><ul><li><strong>可以有返回值</strong>.<code>call()</code> 方法可以返回一个结果,这在需要获取异步任务执行结果的场景中非常有用.</li><li><strong>可以抛出异常</strong>.<code>call()</code> 方法的签名允许向外抛出受检异常,使得异常处理更加灵活.</li><li>通常与功能更强大的 <code>ExecutorService</code>（线程池）结合使用,能更好地管理线程.</li></ul><p><em>缺点：</em></p><ul><li>实现比 <code>Runnable</code> 稍复杂,需要借助 <code>FutureTask</code> 等辅助类.</li></ul><h3 id="三种方式的最终比较"><a class="markdownIt-Anchor" href="#三种方式的最终比较"></a> 三种方式的最终比较</h3><p>为了让你更清晰地理解它们的区别和适用场景,这里提供一个总结表格：</p><table><thead><tr><th>特性</th><th>继承 <code>Thread</code> 类</th><th>实现 <code>Runnable</code> 接口</th><th>实现 <code>Callable</code> 接口</th></tr></thead><tbody><tr><td><strong>本质</strong></td><td>是一个线程 (<code>is-a</code> 关系)</td><td>是一个任务 (<code>has-a</code> 关系)</td><td>是一个可以返回结果的任务 (<code>has-a</code> 关系)</td></tr><tr><td><strong>继承限制</strong></td><td><strong>有</strong> (Java 单继承)</td><td><strong>无</strong> (可以实现多个接口)</td><td><strong>无</strong> (可以实现多个接口)</td></tr><tr><td><strong>耦合性</strong></td><td><strong>高</strong> (任务和线程绑定)</td><td><strong>低</strong> (任务与线程分离)</td><td><strong>低</strong> (任务与线程分离)</td></tr><tr><td><strong>返回值</strong></td><td><strong>无</strong> (<code>run()</code> 方法是 <code>void</code>)</td><td><strong>无</strong> (<code>run()</code> 方法是 <code>void</code>)</td><td><strong>有</strong> (<code>call()</code> 方法可以返回泛型结果)</td></tr><tr><td><strong>抛出异常</strong></td><td><strong>不能</strong> (只能内部 <code>try-catch</code>)</td><td><strong>不能</strong> (只能内部 <code>try-catch</code>)</td><td><strong>可以</strong> (<code>call()</code> 方法签名允许 <code>throws Exception</code>)</td></tr><tr><td><strong>启动方式</strong></td><td><code>new MyThread().start()</code></td><td><code>new Thread(new MyRunnable()).start()</code></td><td><code>FutureTask</code> + <code>Thread</code> 或通过线程池的 <code>submit</code> 方法</td></tr><tr><td><strong>获取结果</strong></td><td>无法直接获取</td><td>无法直接获取</td><td>通过 <code>FutureTask.get()</code> 或 <code>Future.get()</code> 获取 (会阻塞)</td></tr><tr><td><strong>推荐度</strong></td><td><strong>不推荐</strong></td><td><strong>推荐</strong></td><td><strong>推荐</strong> (尤其在需要返回值或处理复杂异步任务时)</td></tr></tbody></table><ol><li><strong>首选 <code>Runnable</code> 和 <code>Callable</code></strong>：在绝大多数情况下,你应该优先选择实现接口的方式 (<code>Runnable</code> 或 <code>Callable</code>).这使得你的代码更加灵活,耦合度更低.</li><li><strong>何时选择 <code>Runnable</code></strong>：当你只需要执行一个异步任务,不关心其执行结果时,<code>Runnable</code> 是最简单、最直接的选择.</li><li><strong>何时选择 <code>Callable</code></strong>：当你需要一个任务在执行完毕后返回一个结果,或者任务在执行过程中可能会抛出需要上层调用者处理的异常时,<code>Callable</code> 是不二之选.它与 <code>ExecutorService</code> 线程池的结合使用是现代 Java 并发编程的基石.</li><li><strong>何时选择继承 <code>Thread</code></strong>：只有在一个非常简单的场景,或者你需要重写 <code>Thread</code> 类的其他方法（而不仅仅是 <code>run()</code>）时,才考虑继承 <code>Thread</code>.在实际项目开发中,这种情况非常罕见.</li></ol><hr /><h2 id="线程同步锁"><a class="markdownIt-Anchor" href="#线程同步锁"></a> 线程同步(锁)</h2><p><strong>线程同步</strong>是一种机制,用于控制多个线程对共享资源的访问.当多个线程需要同时读取或修改同一个数据（例如一个变量、一个文件或一个数据库记录）时,线程同步可以确保在任何时刻,只有一个线程能够访问该资源,从而避免数据混乱和程序错误.</p><h3 id="synchronized"><a class="markdownIt-Anchor" href="#synchronized"></a> synchronized</h3><p><code>synchronized</code> 是 Java 中最基本、最常用的同步机制.它是一种“隐式锁”或“内置锁”（也称为监视器锁）,使用起来非常直观.当一个线程进入 <code>synchronized</code> 代码块或方法时,它会自动获取锁；当它退出时,会自动释放锁.</p><p><code>synchronized</code> 可以用在两个地方：</p><p><strong>a. 同步方法 (Synchronized Methods)</strong></p><p>直接在方法的声明中使用 <code>synchronized</code> 关键字.</p><ul><li><strong>对于普通实例方法</strong>：锁是当前类的实例对象（<code>this</code>）.</li><li><strong>对于静态方法</strong>：锁是当前类的 Class 对象.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Counter</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">count</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 同步实例方法，锁是 Counter 类的实例对象</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title function_">increment</span><span class="params">()</span> &#123;</span><br><span class="line">        count++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getCount</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上面的例子中,任何时候只有一个线程可以调用 <code>increment()</code> 方法,因为它们需要获取同一个 <code>Counter</code> 实例的锁.</p><p><strong>b. 同步代码块 (Synchronized Blocks)</strong></p><p>使用 <code>synchronized(lockObject)</code> 来包裹需要同步的代码片段.这种方式更加灵活,因为你可以明确指定用作锁的对象,并且可以减小锁的粒度,只对必要的代码进行同步,从而提高性能.</p><ul><li><code>lockObject</code> 可以是任何对象,但通常推荐使用一个专门为此目的创建的 <code>private final Object</code>.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Counter</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">count</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">Object</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Object</span>(); <span class="comment">// 专门用于锁定的对象</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">increment</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 同步代码块，锁是 lock 对象</span></span><br><span class="line">        <span class="keyword">synchronized</span> (lock) &#123;</span><br><span class="line">            count++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getCount</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>特点总结：</strong></p><ul><li><strong>优点</strong>：使用简单,是 JVM 内置的特性,不易出错.</li><li><strong>缺点</strong>：不够灵活.如果一个线程获得了锁,其他尝试获取该锁的线程会无限期地阻塞,无法中断,也无法设置超时.在竞争激烈的情况下,性能可能不如 <code>Lock</code>.</li></ul><h3 id="lock"><a class="markdownIt-Anchor" href="#lock"></a> Lock</h3><p><code>java.util.concurrent.locks</code> 包提供了更强大和灵活的锁机制.<code>Lock</code> 是一个接口,其最常见的实现是 <code>ReentrantLock</code>（可重入锁）.</p><p>与 <code>synchronized</code> 自动获取和释放锁不同,<code>Lock</code> 需要手动获取锁 (<code>lock()</code>) 和释放锁 (<code>unlock()</code>).为了确保锁一定会被释放（即使在代码块中发生异常）,<code>unlock()</code> 操作通常放在 <code>finally</code> 块中.</p><p><strong>主要方法：</strong></p><ul><li><code>lock()</code>: 获取锁,如果锁不可用,则当前线程将被阻塞,直到获取锁.</li><li><code>unlock()</code>: 释放锁.</li><li><code>tryLock()</code>: 尝试非阻塞地获取锁,调用后立即返回 <code>true</code>（获取成功）或 <code>false</code>（获取失败）.</li><li><code>tryLock(long time, TimeUnit unit)</code>: 在指定时间内尝试获取锁,超时则返回 <code>false</code>.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Counter</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> <span class="variable">count</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">Lock</span> <span class="variable">lock</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ReentrantLock</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">increment</span><span class="params">()</span> &#123;</span><br><span class="line">        lock.lock(); <span class="comment">// 获取锁</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            count++;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.unlock(); <span class="comment">// 在 finally 块中释放锁，确保一定执行</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">getCount</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>与 <code>synchronized</code> 的比较和优势：</strong></p><ul><li><strong>灵活性</strong>：可以尝试获取锁（<code>tryLock</code>）,可以设置超时,也可以被中断.</li><li><strong>公平性</strong>：<code>ReentrantLock</code> 构造函数可以接受一个布尔值,用于创建公平锁.公平锁会按照线程请求的顺序来分配锁,但通常会带来性能开销.<code>synchronized</code> 是非公平的.</li><li><strong>高级功能</strong>：可以实现更复杂的锁定逻辑,例如一个 <code>Lock</code> 对象可以关联多个 <code>Condition</code> 对象,实现分组唤醒（<code>signal()</code> vs <code>notify()</code>,<code>signalAll()</code> vs <code>notifyAll()</code>）.</li></ul><h3 id="volatile变量线程间统一"><a class="markdownIt-Anchor" href="#volatile变量线程间统一"></a> volatile(变量线程间统一)</h3><p><code>volatile</code> 是一种轻量级的同步机制,但它与 <code>synchronized</code> 和 <code>Lock</code> 有本质区别.它<strong>不保证原子性</strong>,但它保证了两个关键特性：</p><p><strong>a. 可见性</strong></p><p>当一个线程修改了被 <code>volatile</code> 修饰的变量的值,这个新值对其他线程是立即可见的.JVM 会确保每次读取 <code>volatile</code> 变量时,都直接从主内存中读取,而不是使用线程本地缓存.</p><p>**b. 禁止指令重排序 **</p><p>编译器和处理器为了优化性能,可能会对指令进行重排序.<code>volatile</code> 关键字可以防止对其修饰的变量的读写操作进行重排序,从而避免在某些并发场景下出现问题（例如双重检查锁定模式中的懒汉式单例）.</p><p><strong>适用场景：</strong> <code>volatile</code> 通常用于一个线程写,多个线程读的场景,或者当变量的更新不依赖于其当前值时.例如,一个布尔标志位,用于控制线程的终止.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">TaskRunner</span> <span class="keyword">implements</span> <span class="title class_">Runnable</span> &#123;</span><br><span class="line">    <span class="comment">// 使用 volatile 修饰布尔标志位</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="type">boolean</span> <span class="variable">running</span> <span class="operator">=</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">stop</span><span class="params">()</span> &#123;</span><br><span class="line">        running = <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">while</span> (running) &#123;</span><br><span class="line">            <span class="comment">// ... 执行任务 ...</span></span><br><span class="line">            System.out.println(<span class="string">&quot;Task is running...&quot;</span>);</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                Thread.currentThread().interrupt();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;Task stopped.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当其他线程调用 <code>stop()</code> 方法将 <code>running</code> 设置为 <code>false</code> 时,<code>volatile</code> 保证了正在执行 <code>run()</code> 方法的线程能够立即看到这个变化,从而跳出循环.</p><p><strong>重要提醒：</strong> <code>volatile</code> 不能替代 <code>synchronized</code> 来保证复合操作的原子性.例如,<code>count++</code> 这个操作实际上包含了三个步骤：读取 <code>count</code> 值、将值加一、写回新值.<code>volatile</code> 只能保证每次读取的 <code>count</code> 是最新的,但不能保证这三个步骤作为一个原子操作执行.因此,对于 <code>count++</code> 这样的操作,还是需要使用 <code>synchronized</code> 或 <code>Lock</code>.</p><table><thead><tr><th>特性</th><th><code>synchronized</code> 关键字</th><th><code>java.util.concurrent.locks.Lock</code></th><th><code>volatile</code> 关键字</th></tr></thead><tbody><tr><td><strong>锁机制</strong></td><td>悲观锁,隐式锁</td><td>悲观锁,显式锁</td><td>无锁</td></tr><tr><td><strong>原子性</strong></td><td><strong>保证</strong></td><td><strong>保证</strong></td><td><strong>不保证</strong>（仅保证单次读/写）</td></tr><tr><td><strong>可见性</strong></td><td><strong>保证</strong></td><td><strong>保证</strong></td><td><strong>保证</strong></td></tr><tr><td><strong>使用方式</strong></td><td>自动加锁和解锁</td><td>手动加锁和解锁</td><td>修饰变量</td></tr><tr><td><strong>灵活性</strong></td><td>较低,阻塞不可中断</td><td>高,可中断、可超时、可尝试</td><td>不适用（不是锁）</td></tr><tr><td><strong>性能</strong></td><td>简单场景下性能不错,JVM 持续优化</td><td>竞争激烈时通常性能更好</td><td>轻量级,性能开销最小</td></tr><tr><td><strong>适用场景</strong></td><td>大多数需要同步的场景</td><td>需要高级功能或高竞争的场景</td><td>保证变量的可见性,一写多读</td></tr></tbody></table><hr /><h2 id="线程池"><a class="markdownIt-Anchor" href="#线程池"></a> 线程池</h2><p>线程池是Java并发编程中不可或缺的核心工具.通过<code>ThreadPoolExecutor</code>,我们可以精细地控制线程的创建、销毁和任务的调度,从而在提高系统性能和响应速度的同时,保证系统的稳定性.在实际开发中,<strong>强烈建议使用<code>ThreadPoolExecutor</code>的构造函数来创建线程池</strong>,并根据任务类型和系统资源进行合理的参数配置.</p><p>想象一下你开了一家餐厅,如果没有线程池,就相当于每来一位顾客（一个任务）,你就雇佣一位新的服务员（一个线程）去服务他.服务结束后,这位服务员就直接解雇了.如果同时来很多顾客,你就需要瞬间雇佣大量的服务员,这会导致招聘和解雇（创建和销毁线程）的成本非常高,而且服务员数量（线程数）过多,他们之间可能会互相干扰,导致餐厅（系统）效率低下,甚至混乱崩溃.</p><p><strong>线程池（Thread Pool）</strong> 就是为了解决这个问题而生的.它就像是你提前雇佣了一批固定数量的核心服务员,并让他们随时待命.</p><ul><li>当有顾客（任务）来时,你从这群服务员中找一个空闲的去服务.</li><li>如果所有服务员都在忙,新来的顾客就在等候区（任务队列）排队等候.</li><li>当一位服务员服务完一个顾客后,他不会被解雇,而是回到待命状态,准备服务下一位等候的顾客.</li></ul><p>通过这种方式,线程池在程序启动时就创建了一定数量的线程,并将它们放入一个池中进行统一管理,当有任务需要执行时,直接从池中获取一个空闲线程来执行,任务执行完毕后,线程并不会立即销毁,而是返回池中等待下一个任务.</p><p><em>优点：</em></p><ol><li><strong>降低资源消耗</strong>：通过重复利用已创建的线程,减少了线程创建和销毁带来的开销.</li><li><strong>提高响应速度</strong>：当任务到达时,无需等待新线程的创建,可以直接从池中获取线程立即执行,从而缩短了响应时间.</li><li><strong>提高线程的可管理性</strong>：线程是稀缺资源,如果无限制地创建,不仅会消耗系统资源,还会降低系统的稳定性.线程池可以对线程进行统一分配、调优和监控,避免了野线程（unmanaged threads）的出现.</li></ol><h3 id="核心参数"><a class="markdownIt-Anchor" href="#核心参数"></a> 核心参数</h3><p>当你创建一个 <code>ThreadPoolExecutor</code> 实例时,通常需要指定以下几个核心参数：</p><ol><li><code>corePoolSize</code> (核心线程数):<ul><li>线程池中保持存活的核心线程数量.</li><li>即使这些线程处于空闲状态,它们也不会被回收（除非设置了 <code>allowCoreThreadTimeOut</code>）.</li></ul></li><li><code>maximumPoolSize</code> (最大线程数):<ul><li>线程池能够容纳同时执行的最大线程数.</li><li>这个值必须大于或等于 <code>corePoolSize</code>.</li></ul></li><li><code>keepAliveTime</code> (线程空闲存活时间):<ul><li>当线程池中的线程数量超过 <code>corePoolSize</code> 时,如果一个非核心线程空闲的时间超过 <code>keepAliveTime</code>,它就会被销毁.</li><li>这样可以释放掉多余的资源.</li></ul></li><li><code>unit</code> (时间单位):<ul><li><code>keepAliveTime</code> 的时间单位,例如 <code>TimeUnit.SECONDS</code> (秒)、<code>TimeUnit.MILLISECONDS</code> (毫秒)等.</li></ul></li><li><code>workQueue</code> (任务队列):<ul><li>一个阻塞队列（<code>BlockingQueue</code>）,用于存放等待执行的任务.</li><li>当核心线程都在忙时,新提交的任务会进入这个队列排队.</li><li>常用的队列类型有：<code>ArrayBlockingQueue</code> (有界队列)、<code>LinkedBlockingQueue</code> (无界或有界队列)、<code>SynchronousQueue</code> (不存储元素的队列)等.</li></ul></li><li><code>threadFactory</code> (线程工厂):<ul><li>用于创建新线程的工厂.</li><li>可以通过它来自定义线程的名称、是否为守护线程、优先级等.</li></ul></li><li><code>rejectedExecutionHandler</code> (拒绝策略):<ul><li>当任务队列已满,并且线程池中的线程数也达到了 <code>maximumPoolSize</code> 时,新提交的任务会根据这个策略来处理.</li><li>Java内置了四种拒绝策略：<ul><li><code>AbortPolicy</code> (默认): 直接抛出 <code>RejectedExecutionException</code> 异常.</li><li><code>CallerRunsPolicy</code>: 由提交任务的线程自己来执行这个任务.</li><li><code>DiscardPolicy</code>: 直接丢弃这个任务,什么也不做.</li><li><code>DiscardOldestPolicy</code>: 丢弃任务队列中最旧的一个任务,然后尝试重新提交当前任务.</li></ul></li></ul></li></ol><h3 id="工作流程"><a class="markdownIt-Anchor" href="#工作流程"></a> 工作流程</h3><p>当一个新任务通过 <code>execute()</code> 方法提交给线程池时,处理流程如下：</p><ol><li><strong>判断核心线程数</strong>：判断当前运行的线程数是否小于 <code>corePoolSize</code>.如果是,则立即创建一个新的核心线程来执行该任务,即使其他核心线程是空闲的.</li><li><strong>尝试加入任务队列</strong>：如果当前运行的线程数已经等于或大于 <code>corePoolSize</code>,则尝试将任务放入 <code>workQueue</code> 任务队列中.</li><li><strong>尝试创建非核心线程</strong>：如果任务队列已满,无法再放入新任务,则判断当前运行的线程数是否小于 <code>maximumPoolSize</code>,如果是,则创建一个新的非核心线程来执行该任务.</li><li><strong>执行拒绝策略</strong>：如果当前运行的线程数已经达到 <code>maximumPoolSize</code>,并且任务队列也满了,那么就无法再处理这个任务了.此时会启动指定的 <code>rejectedExecutionHandler</code> 拒绝策略来处理该任务.</li></ol><h3 id="创建线程池的方法"><a class="markdownIt-Anchor" href="#创建线程池的方法"></a> 创建线程池的方法</h3><h4 id="使用-executors工厂类-不推荐"><a class="markdownIt-Anchor" href="#使用-executors工厂类-不推荐"></a> 使用 <code>Executors</code>工厂类 (不推荐)</h4><p><code>Executors</code> 提供了一些静态工厂方法,可以方便地创建几种预设配置的线程池.</p><ol><li><p><strong><code>newFixedThreadPool(int nThreads)</code></strong></p><ul><li><strong>特点</strong>: 创建一个固定大小的线程池.<code>corePoolSize</code> 和 <code>maximumPoolSize</code> 相等,<code>keepAliveTime</code> 为0.</li><li><strong>工作队列</strong>: <code>LinkedBlockingQueue</code> (理论上是无界的).</li><li><strong>适用场景</strong>: 适用于负载比较重的服务器,需要执行长期任务,可以稳定地控制并发线程数.</li><li><strong>风险</strong>: 任务队列是无界的,如果任务提交速度远大于处理速度,可能导致大量任务堆积,引发内存溢出.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ExecutorService</span> <span class="variable">fixedThreadPool</span> <span class="operator">=</span> Executors.newFixedThreadPool(<span class="number">5</span>);</span><br><span class="line">fixedThreadPool.execute(() -&gt; &#123;</span><br><span class="line">    System.out.println(Thread.currentThread().getName() + <span class="string">&quot; is running...&quot;</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li><li><p><strong><code>newSingleThreadExecutor()</code></strong></p><ul><li><strong>特点</strong>: 创建一个只有一个线程的线程池.</li><li><strong>工作队列</strong>: <code>LinkedBlockingQueue</code> (理论上是无界的).</li><li><strong>适用场景</strong>: 适用于需要保证所有任务按照指定顺序（FIFO）执行的场景.</li><li><strong>风险</strong>: 与 <code>newFixedThreadPool</code> 类似,也存在任务堆积导致内存溢出的风险.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ExecutorService</span> <span class="variable">singleThreadExecutor</span> <span class="operator">=</span> Executors.newSingleThreadExecutor();</span><br></pre></td></tr></table></figure></li><li><p><strong><code>newCachedThreadPool()</code></strong></p><ul><li><strong>特点</strong>: 创建一个可缓存的线程池.<code>corePoolSize</code> 为0,<code>maximumPoolSize</code> 为 <code>Integer.MAX_VALUE</code>,<code>keepAliveTime</code> 为60秒.</li><li><strong>工作队列</strong>: <code>SynchronousQueue</code> (不存储元素的队列).</li><li><strong>工作机制</strong>: 来一个任务,如果没有空闲线程,就创建一个新线程；如果一个线程空闲60秒,就会被回收.</li><li><strong>适用场景</strong>: 适用于执行大量、耗时较短的异步任务.</li><li><strong>风险</strong>: <code>maximumPoolSize</code> 是无限大的,如果并发任务量巨大,可能会瞬间创建大量线程,导致系统资源耗尽,引发内存溢出.</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ExecutorService</span> <span class="variable">cachedThreadPool</span> <span class="operator">=</span> Executors.newCachedThreadPool();</span><br></pre></td></tr></table></figure></li></ol><blockquote><p><strong>注意</strong>: 阿里巴巴的《Java开发手册》中强制规定：<strong>不允许使用 <code>Executors</code> 去创建线程池,而是通过 <code>ThreadPoolExecutor</code> 的方式</strong>,这样的处理方式让写的同学更加明确线程池的运行规则,规避资源耗尽的风险.</p></blockquote><h4 id="使用-threadpoolexecutor-构造函数-推荐"><a class="markdownIt-Anchor" href="#使用-threadpoolexecutor-构造函数-推荐"></a> 使用 <code>ThreadPoolExecutor</code> 构造函数 (推荐)</h4><p>这是最灵活、最推荐的创建方式,因为它让你对线程池的每一个细节都有完全的控制.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ThreadPoolDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// CPU核心数</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">coreCount</span> <span class="operator">=</span> Runtime.getRuntime().availableProcessors();</span><br><span class="line"></span><br><span class="line">        <span class="type">ThreadPoolExecutor</span> <span class="variable">threadPool</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>(</span><br><span class="line">                coreCount,                           <span class="comment">// 核心线程数</span></span><br><span class="line">                coreCount * <span class="number">2</span>,                       <span class="comment">// 最大线程数</span></span><br><span class="line">                <span class="number">60L</span>,                                 <span class="comment">// 线程空闲存活时间</span></span><br><span class="line">                TimeUnit.SECONDS,                    <span class="comment">// 时间单位</span></span><br><span class="line">                <span class="keyword">new</span> <span class="title class_">ArrayBlockingQueue</span>&lt;&gt;(<span class="number">100</span>),       <span class="comment">// 任务队列 (有界队列)</span></span><br><span class="line">                Executors.defaultThreadFactory(),    <span class="comment">// 线程工厂 (默认)</span></span><br><span class="line">                <span class="keyword">new</span> <span class="title class_">ThreadPoolExecutor</span>.AbortPolicy() <span class="comment">// 拒绝策略 (默认)</span></span><br><span class="line">        );</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 提交任务</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">final</span> <span class="type">int</span> <span class="variable">taskNum</span> <span class="operator">=</span> i;</span><br><span class="line">            threadPool.execute(() -&gt; &#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;Executing task &quot;</span> + taskNum + <span class="string">&quot; with thread &quot;</span> + Thread.currentThread().getName());</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">500</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 关闭线程池</span></span><br><span class="line">        threadPool.shutdown();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="合理配置线程池参数"><a class="markdownIt-Anchor" href="#合理配置线程池参数"></a> 合理配置线程池参数</h3><p>合理配置线程池的大小非常关键,通常需要根据任务的类型来决定.</p><ul><li><strong>CPU密集型任务 (CPU-bound)</strong>:<ul><li>这种任务需要大量的CPU计算,例如加密、计算哈希值等.</li><li>理论上,线程数不应超过CPU核心数,因为过多的线程会导致频繁的上下文切换,反而降低性能.</li><li><strong>配置建议</strong>: <code>corePoolSize</code> = CPU核心数 + 1 (这个“+1”是为了防止某个线程因意外原因,如缺页中断,而暂停时,CPU能有另一个线程可以调度).</li></ul></li><li><strong>I/O密集型任务 (I/O-bound)</strong>:<ul><li>这种任务大部分时间都在等待I/O操作（如读写文件、网络请求等）,CPU空闲时间较多.</li><li>可以配置更多的线程,以便在一个线程等待I/O时,其他线程可以继续使用CPU.</li><li><strong>配置建议</strong>:<ul><li>一个常见的经验公式是：<code>corePoolSize</code> = CPU核心数 * (1 + 平均等待时间 / 平均CPU计算时间).</li><li>在实践中,一个更简单的估算方法是 <code>corePoolSize</code> = CPU核心数 * 2.</li><li>当然,最佳的线程数需要通过性能压测来确定.</li></ul></li></ul></li></ul><hr /><h1 id="网络编程三要素"><a class="markdownIt-Anchor" href="#网络编程三要素"></a> 网络编程三要素</h1><ul><li><strong>IP地址 (IP Address)</strong>：相当于收件人的<strong>家庭住址</strong>.它唯一地标识了网络上的一台设备（比如电脑、服务器或手机）.</li><li><strong>端口 (Port)</strong>：相当于你家里的<strong>某个具体的房间或者人</strong>.数据到达了这栋房子（IP地址）,还需要知道具体要交给谁（哪个应用程序）.</li><li><strong>协议 (Protocol)</strong>：相当于快递公司使用的<strong>打包、运输和签收规则</strong>,它规定了数据应该如何被格式化、传输和解析.</li></ul><h2 id="ip-地址-ip-address"><a class="markdownIt-Anchor" href="#ip-地址-ip-address"></a> IP 地址 (IP Address)</h2><p>IP地址是“Internet Protocol Address”的缩写,即互联网协议地址.它是在网络中分配给每台设备的一个唯一的数字标识.</p><ul><li><strong>作用</strong>：用来在复杂的网络环境中<strong>定位和识别</strong>一台特定的计算机或网络设备.就像邮递员需要通过地址找到你的家一样,网络数据包也需要通过IP地址找到目标主机.</li><li><strong>主要版本</strong>：<ul><li><strong>IPv4 (Internet Protocol version 4)</strong>: 这是目前最广泛使用的版本.它由4个字节（32位）组成,通常用“点分十进制”表示,例如 <code>192.168.1.1</code>.IPv4地址资源已接近枯竭.</li><li><strong>IPv6 (Internet Protocol version 6)</strong>: 为了解决IPv4地址耗尽的问题而推出,它由16个字节（128位）组成,地址空间极大,表示形式更复杂,例如 <code>2001:0db8:85a3:0000:0000:8a2e:0370:7334</code>.</li></ul></li><li><strong>特殊IP地址</strong>：<ul><li><code>127.0.0.1</code>：这是一个特殊的<strong>回环地址 (Loopback Address)</strong>,代表<strong>本机</strong>.当你访问这个地址时,数据包不会发送到网络上,而是直接在本机内部循环.它通常与域名 <code>localhost</code> 绑定.</li></ul></li></ul><h2 id="端口-port"><a class="markdownIt-Anchor" href="#端口-port"></a> 端口 (Port)</h2><p>如果说IP地址帮助我们将数据送到了正确的计算机,那么端口就是用来告诉这台计算机,数据应该交给<strong>哪一个应用程序</strong>来处理.</p><ul><li><strong>作用</strong>：<strong>区分和定位</strong>一台主机上正在运行的不同网络应用程序.例如,你的电脑可能同时开着浏览器（通常使用80端口）、邮件客户端（如SMTP用25端口）和FTP客户端（21端口）.当数据包到达时,操作系统通过端口号就能知道这个数据是给浏览器的,还是给邮件客户端的.</li><li><strong>本质</strong>：端口是一个<strong>逻辑概念</strong>,而不是物理设备.它是一个16位的整数,范围从 <code>0</code> 到 <code>65535</code>.</li><li><strong>端口分类</strong>：<ul><li><strong>公认端口 (Well-Known Ports)</strong>: <code>0</code> ~ <code>1023</code>.这些端口被预留给一些众所周知的服务,例如：<ul><li><code>80</code>: HTTP (Web服务)</li><li><code>443</code>: HTTPS (加密的Web服务)</li><li><code>21</code>: FTP (文件传输服务)</li><li><code>22</code>: SSH (安全远程登录)</li></ul></li><li><strong>注册端口 (Registered Ports)</strong>: <code>1024</code> ~ <code>49151</code>.分配给用户进程或应用程序.</li><li><strong>动态/私有端口 (Dynamic/Private Ports)</strong>: <code>49152</code> ~ <code>65535</code>.客户端程序通常会使用这个范围内的端口与服务器进行通信.</li></ul></li></ul><h2 id="协议-protocol"><a class="markdownIt-Anchor" href="#协议-protocol"></a> 协议 (Protocol)</h2><p>协议是“Protocol”的音译,指的是<strong>网络通信的规则和约定</strong>.通信的双方（例如你的浏览器和Web服务器）必须遵守相同的协议,才能正确地解释对方发送的数据.</p><ul><li><strong>作用</strong>：<strong>规范数据的格式、传输时序、错误处理等</strong>.没有协议,数据交换就会变得混乱无序,就像两个说着不同语言且不懂对方规则的人无法交流一样.</li><li><strong>分层模型</strong>：网络协议通常被组织成一个分层结构,最著名的是 <strong>TCP/IP协议簇 (TCP/IP Protocol Suite)</strong>.</li><li><strong>常见的关键协议</strong>：<ul><li><strong>TCP (Transmission Control Protocol, 传输控制协议)</strong>:<ul><li><strong>特点</strong>：面向连接、可靠、基于字节流的传输.在通信前需要先建立连接（“三次握手”）,通信结束后断开连接（“四次挥手”）.它能保证数据完整、有序地到达目的地.</li><li><strong>场景</strong>：适用于对数据可靠性要求高的场景,如文件传输、发送邮件、浏览网页.</li></ul></li><li><strong>UDP (User Datagram Protocol, 用户数据报协议)</strong>:<ul><li><strong>特点</strong>：无连接、不可靠、面向报文的传输.它只是尽最大努力交付数据,不保证数据是否到达、是否按顺序到达.但它的优点是开销小、传输速度快.</li><li><strong>场景</strong>：适用于对实时性要求高、能容忍少量数据丢失的场景,如在线视频、网络直播、语音通话、DNS查询.</li></ul></li><li><strong>HTTP (HyperText Transfer Protocol, 超文本传输协议)</strong>: 建立在TCP协议之上的应用层协议,是万维网数据通信的基础.</li><li><strong>IP (Internet Protocol, 网际协议)</strong>: 负责IP地址的分配和数据包的路由.</li></ul></li></ul><hr /><h2 id="完整过程"><a class="markdownIt-Anchor" href="#完整过程"></a> 完整过程</h2><blockquote><p>当你在浏览器中输入一个网址（例如 <code>www.google.com</code>）并按下回车时,你的电脑（客户端）需要向谷歌的服务器发送一个请求,这个过程就完美地体现了三要素：</p><ol><li><strong>协议</strong>：浏览器会使用 <strong>HTTP/HTTPS</strong> 协议来构建请求数据.</li><li><strong>IP地址</strong>：通过DNS服务,你的电脑将域名 <code>www.google.com</code> 解析成一个具体的 <strong>IP地址</strong>（比如 <code>172.217.164.100</code>）,从而找到了网络中谷歌服务器这台“房子”.</li><li><strong>端口</strong>：因为是Web请求,所以数据会发往服务器的 <strong>80端口（HTTP）或443端口（HTTPS）</strong>,告诉服务器这个数据请交给你的Web服务程序来处理.</li></ol><p>最终，通过 <strong>协议</strong> 规定好格式,通过 <strong>IP地址</strong> 找到服务器,再通过 <strong>端口</strong> 找到对应的服务程序,一次完整的网络通信就得以实现.</p></blockquote><hr /><h2 id="socket"><a class="markdownIt-Anchor" href="#socket"></a> Socket</h2><p><strong>Socket（套接字）</strong> 可以被理解为网络上两个程序之间进行双向通信的<strong>端点</strong>.它封装了底层的网络细节（如IP地址和端口号）,让我们能够像读写本地文件一样方便地进行网络数据交换.</p><p>一个很经典的类比是<strong>电话</strong>：</p><ul><li><strong>IP地址</strong> 就像是城市的区号,标识了一台独一无二的计算机.</li><li><strong>端口号 (Port)</strong> 就像是电话的分机号,标识了计算机上的一个特定应用程序（比如80端口通常是Web服务器,3306是MySQL数据库）.</li><li><strong>Socket</strong> 就是你手里的那部电话机,你需要知道对方的区号（IP）和分机号（Port）才能拨号建立连接.一旦连接建立,你们就可以通过各自的电话机（Socket）进行通话（数据交换）.</li></ul><p>在Java中，Socket编程主要指的是基于TCP协议的编程,因为UDP是无连接的,它使用不同的类（<code>DatagramSocket</code> 和 <code>DatagramPacket</code>）.因此,当我们通常谈论<code>java.net.Socket</code>时,我们指的就是<strong>TCP Socket</strong>.</p><h3 id="java-socket编程模型客户端服务器-cs"><a class="markdownIt-Anchor" href="#java-socket编程模型客户端服务器-cs"></a> Java Socket编程模型：客户端/服务器 (C/S)</h3><p>Java的Socket编程通常遵循客户端/服务器模型.</p><ol><li><strong>服务器 (Server)</strong>：<ul><li>创建一个 <code>ServerSocket</code> 对象,并将其“绑定”到一个特定的端口上.</li><li><code>ServerSocket</code> 就像是一个总机,它负责在一个端口上持续<strong>监听</strong>,等待客户端的连接请求.</li><li>当一个客户端请求连接时,<code>ServerSocket</code> 会调用 <code>accept()</code> 方法,这个方法会<strong>阻塞</strong>（暂停等待）,直到一个连接建立.</li><li>一旦连接成功,<code>accept()</code> 方法会返回一个全新的 <code>Socket</code> 对象,这个新的<code>Socket</code>对象才是真正用于与<strong>这个特定客户端</strong>进行通信的通道.服务器可以继续监听,以接受更多客户端的连接.</li></ul></li><li><strong>客户端 (Client)</strong>：<ul><li>创建一个 <code>Socket</code> 对象.</li><li>在创建时,需要指定服务器的IP地址和端口号,以表明“我想连接哪台机器上的哪个程序”.</li><li>一旦 <code>Socket</code> 对象成功创建,就意味着客户端和服务器之间的连接已经建立成功.</li><li>之后,客户端就可以使用这个 <code>Socket</code> 对象与服务器进行通信了.</li></ul></li></ol><h3 id="核心类与工作流程"><a class="markdownIt-Anchor" href="#核心类与工作流程"></a> 核心类与工作流程</h3><h4 id="javanetserversocket服务器端"><a class="markdownIt-Anchor" href="#javanetserversocket服务器端"></a> <code>java.net.ServerSocket</code>(服务器端)</h4><p>这是服务器的入口点.</p><ul><li><strong><code>ServerSocket(int port)</code></strong>: 构造函数,创建一个监听指定端口的服务器Socket.</li><li><strong><code>Socket accept()</code></strong>: 监听并接受客户端的连接请求,这是一个阻塞方法,程序会在这里等待,直到有客户端连接进来,它返回一个用于与该客户端通信的<code>Socket</code>实例.</li><li><strong><code>void close()</code></strong>: 关闭服务器Socket,停止监听.</li></ul><h4 id="javanetsocket-客户端和服务器与客户端的连接"><a class="markdownIt-Anchor" href="#javanetsocket-客户端和服务器与客户端的连接"></a> <code>java.net.Socket</code> (客户端和服务器与客户端的连接)</h4><p>这是通信的端点.</p><ul><li><strong><code>Socket(String host, int port)</code></strong>: 客户端使用的构造函数,用于连接到指定主机和端口的服务器.</li><li><strong><code>InputStream getInputStream()</code></strong>: 获取输入流,用于从对方读取数据.</li><li><strong><code>OutputStream getOutputStream()</code></strong>: 获取输出流,用于向对方发送数据.</li><li><strong><code>void close()</code></strong>: 关闭这个Socket连接,这会释放所有资源,并断开与对方的连接.</li></ul><h4 id="数据交换流程"><a class="markdownIt-Anchor" href="#数据交换流程"></a> 数据交换流程</h4><p>一旦连接建立（服务器的<code>accept()</code>返回<code>Socket</code>,客户端的<code>new Socket(...)</code>执行完毕）,双方就可以通过输入/输出流（<code>InputStream</code>/<code>OutputStream</code>）来读写数据,就像操作文件一样.</p><ol><li>A想给B发消息：A从自己的<code>Socket</code>获取<code>OutputStream</code>,然后写入数据.</li><li>B想接收A的消息：B从自己的<code>Socket</code>获取<code>InputStream</code>,然后读取数据.</li></ol><p>这个过程是全双工的,意味着双方可以同时进行读写.</p><hr /><h3 id="java-socket代码示例"><a class="markdownIt-Anchor" href="#java-socket代码示例"></a> Java Socket代码示例</h3><p>下面是一个非常基础的例子，客户端发送一条消息，服务器接收后回复一条消息。</p><h4 id="服务器端代码-serverjava"><a class="markdownIt-Anchor" href="#服务器端代码-serverjava"></a> 服务器端代码 (Server.java)</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Server</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 1. 创建一个ServerSocket,监听8080端口</span></span><br><span class="line">            <span class="type">ServerSocket</span> <span class="variable">serverSocket</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ServerSocket</span>(<span class="number">8080</span>);</span><br><span class="line">            System.out.println(<span class="string">&quot;服务器启动，等待客户端连接...&quot;</span>);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 2. 调用accept()方法,等待客户端连接,这是一个阻塞方法</span></span><br><span class="line">            <span class="type">Socket</span> <span class="variable">clientSocket</span> <span class="operator">=</span> serverSocket.accept();</span><br><span class="line">            System.out.println(<span class="string">&quot;客户端已连接: &quot;</span> + clientSocket.getInetAddress().getHostAddress());</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 3. 获取输入流,用来读取客户端发送的数据</span></span><br><span class="line">            <span class="type">InputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> clientSocket.getInputStream();</span><br><span class="line">            <span class="type">byte</span>[] buffer = <span class="keyword">new</span> <span class="title class_">byte</span>[<span class="number">1024</span>];</span><br><span class="line">            <span class="type">int</span> <span class="variable">len</span> <span class="operator">=</span> inputStream.read(buffer);</span><br><span class="line">            <span class="type">String</span> <span class="variable">clientMessage</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">String</span>(buffer, <span class="number">0</span>, len, StandardCharsets.UTF_8);</span><br><span class="line">            System.out.println(<span class="string">&quot;收到客户端消息: &quot;</span> + clientMessage);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 4. 获取输出流,向客户端发送响应</span></span><br><span class="line">            <span class="type">OutputStream</span> <span class="variable">outputStream</span> <span class="operator">=</span> clientSocket.getOutputStream();</span><br><span class="line">            <span class="type">String</span> <span class="variable">serverResponse</span> <span class="operator">=</span> <span class="string">&quot;你好，客户端！我已收到你的消息。&quot;</span>;</span><br><span class="line">            outputStream.write(serverResponse.getBytes(StandardCharsets.UTF_8));</span><br><span class="line">            outputStream.flush(); <span class="comment">// 确保数据被发送出去</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">// 5. 关闭资源</span></span><br><span class="line">            clientSocket.close();</span><br><span class="line">            serverSocket.close();</span><br><span class="line">            System.out.println(<span class="string">&quot;服务器已关闭。&quot;</span>);</span><br><span class="line"></span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="客户端代码-clientjava"><a class="markdownIt-Anchor" href="#客户端代码-clientjava"></a> 客户端代码 (Client.java)</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Client</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 1. 创建Socket,连接到本地（localhost）的8080端口</span></span><br><span class="line">            <span class="type">Socket</span> <span class="variable">socket</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Socket</span>(<span class="string">&quot;localhost&quot;</span>, <span class="number">8080</span>);</span><br><span class="line">            System.out.println(<span class="string">&quot;已成功连接到服务器！&quot;</span>);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 2. 获取输出流,向服务器发送消息</span></span><br><span class="line">            <span class="type">OutputStream</span> <span class="variable">outputStream</span> <span class="operator">=</span> socket.getOutputStream();</span><br><span class="line">            <span class="type">String</span> <span class="variable">message</span> <span class="operator">=</span> <span class="string">&quot;你好，服务器！&quot;</span>;</span><br><span class="line">            outputStream.write(message.getBytes(StandardCharsets.UTF_8));</span><br><span class="line">            outputStream.flush();</span><br><span class="line">            socket.shutdownOutput(); <span class="comment">// 可选：告诉服务器我这边已经发完了</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">// 3. 获取输入流,读取服务器的响应</span></span><br><span class="line">            <span class="type">InputStream</span> <span class="variable">inputStream</span> <span class="operator">=</span> socket.getInputStream();</span><br><span class="line">            <span class="type">byte</span>[] buffer = <span class="keyword">new</span> <span class="title class_">byte</span>[<span class="number">1024</span>];</span><br><span class="line">            <span class="type">int</span> <span class="variable">len</span> <span class="operator">=</span> inputStream.read(buffer);</span><br><span class="line">            <span class="type">String</span> <span class="variable">serverResponse</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">String</span>(buffer, <span class="number">0</span>, len, StandardCharsets.UTF_8);</span><br><span class="line">            System.out.println(<span class="string">&quot;收到服务器响应: &quot;</span> + serverResponse);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 4. 关闭资源</span></span><br><span class="line">            socket.close();</span><br><span class="line">            System.out.println(<span class="string">&quot;客户端已关闭。&quot;</span>);</span><br><span class="line"></span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h1 id="单元测试"><a class="markdownIt-Anchor" href="#单元测试"></a> 单元测试</h1><p>在 Android 中,测试主要分为两类,了解它们的区别非常重要：</p><ol><li><strong>本地单元测试</strong><ul><li><strong>位置</strong>: <code>app/src/test/java/</code> 目录下.</li><li><strong>运行环境</strong>: 在你电脑的 Java 虚拟机 (JVM) 上直接运行,不依赖 Android 设备或模拟器.</li><li><strong>优点</strong>: 速度极快,几秒钟就能跑完成百上千个测试.</li><li><strong>用途</strong>: 主要用于测试应用的业务逻辑,例如 ViewModel、Repository、Presenter、工具类等不直接依赖 Android 框架（如 <code>Context</code>、<code>Activity</code>）的代码.</li><li><strong>常用框架</strong>: <strong>JUnit</strong>、<strong>Mockito</strong>、<strong>Robolectric</strong>.</li></ul></li><li><strong>仪器化测试</strong><ul><li><strong>位置</strong>: <code>app/src/androidTest/java/</code> 目录下.</li><li><strong>运行环境</strong>: 必须在真实的 Android 设备或模拟器上运行.</li><li><strong>优点</strong>: 可以测试与 Android 框架紧密耦合的代码,例如 UI 交互、数据库、Service 等.</li><li><strong>缺点</strong>: 速度慢,启动模拟器和安装 APK 都需要时间.</li><li><strong>常用框架</strong>: <strong>Espresso</strong> (UI 测试), <strong>UI Automator</strong>, <strong>AndroidX Test</strong>.</li></ul></li></ol><p><strong>重点讲解第一种：本地单元测试 (Local Unit Tests)。</strong></p><h2 id="配置-buildgradle"><a class="markdownIt-Anchor" href="#配置-buildgradle"></a> 配置 <code>build.gradle</code></h2><p>通常,当你创建一个新的 Android Studio 项目时,相关的测试依赖已经自动添加好了.</p><figure class="highlight groovy"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">dependencies &#123;</span><br><span class="line">    <span class="comment">// ... 其他依赖</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 单元测试核心库</span></span><br><span class="line">    testImplementation <span class="string">&#x27;junit:junit:4.13.2&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// (可选，但强烈推荐) Mockito 用于 &quot;模拟&quot; 依赖对象</span></span><br><span class="line">    testImplementation <span class="string">&#x27;org.mockito:mockito-core:4.5.1&#x27;</span></span><br><span class="line">    testImplementation <span class="string">&#x27;org.mockito:mockito-inline:4.5.1&#x27;</span> <span class="comment">// 如果需要 mock final 类或方法</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// (可选) AndroidX Test 核心库，为测试提供一些帮助</span></span><br><span class="line">    testImplementation <span class="string">&#x27;androidx.test:core:1.5.0&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// (可选) Robolectric，让你在 JVM 上测试需要 Android 框架的代码</span></span><br><span class="line">    testImplementation <span class="string">&#x27;org.robolectric:robolectric:4.9.2&#x27;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="编写你单元测试"><a class="markdownIt-Anchor" href="#编写你单元测试"></a> 编写你单元测试</h2><p>我们从一个不依赖任何 Android 框架的简单计算器类开始.</p><h3 id="创建被测试的类"><a class="markdownIt-Anchor" href="#创建被测试的类"></a> 创建被测试的类</h3><p>在 <code>app/src/main/java/com/yourpackage/</code> 目录下创建一个简单的类,例如 <code>Calculator.java</code>.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Calculator</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> a + b;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">subtract</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> a - b;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="创建测试类"><a class="markdownIt-Anchor" href="#创建测试类"></a> 创建测试类</h3><ol><li>在 <code>Calculator.java</code> 类名上右键,选择 <strong>Go To &gt; Test</strong>.</li><li>在弹出的窗口中,点击 <strong>Create New Test…</strong>.</li><li>Android Studio 会自动为你配置好测试类的信息.<ul><li><strong>Testing library</strong>: 选择 <strong>JUnit4</strong>.</li><li><strong>Class name</strong>: 默认是 <code>CalculatorTest</code> (这是标准的命名规范).</li><li><strong>Destination package</strong>: 确保是 <code>com.yourpackage</code> (在 <strong>test</strong> 目录下).</li></ul></li><li>点击 <strong>OK</strong>,Android Studio 会在 <code>app/src/test/java/com/yourpackage/</code> 目录下生成一个 <code>CalculatorTest.java</code> 文件.</li></ol><p>生成的代码框架如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CalculatorTest</span> &#123;</span><br><span class="line">    <span class="comment">// 测试方法将写在这里</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="编写测试方法"><a class="markdownIt-Anchor" href="#编写测试方法"></a> 编写测试方法</h3><p>为 <code>add</code> 方法编写一个测试,一个标准的测试方法遵循 <strong>Arrange-Act-Assert</strong> (准备-执行-断言) 模式.</p><ul><li><strong>@Test</strong> 注解：告诉 JUnit 这是一个测试方法.</li><li><strong>方法命名</strong>: 最好能清晰地描述测试的内容,例如 <code>subject_action_expectedResult</code> (被测试对象行为期望结果).</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CalculatorTest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">add_twoNumbers_returnsCorrectSum</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 1. Arrange (准备)</span></span><br><span class="line">        <span class="type">Calculator</span> <span class="variable">calculator</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Calculator</span>();</span><br><span class="line">        <span class="type">int</span> <span class="variable">a</span> <span class="operator">=</span> <span class="number">5</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">b</span> <span class="operator">=</span> <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. Act (执行)</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> calculator.add(a, b);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. Assert (断言)</span></span><br><span class="line">        <span class="comment">// 检查结果是否和预期的一样 (8)</span></span><br><span class="line">        assertEquals(<span class="number">8</span>, result);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">subtract_twoNumbers_returnsCorrectDifference</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// Arrange</span></span><br><span class="line">        <span class="type">Calculator</span> <span class="variable">calculator</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Calculator</span>();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Act</span></span><br><span class="line">        <span class="type">int</span> <span class="variable">result</span> <span class="operator">=</span> calculator.subtract(<span class="number">10</span>, <span class="number">4</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Assert</span></span><br><span class="line">        assertEquals(<span class="number">6</span>, result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>assertEquals(expected, actual)</code> 是 JUnit 提供的一个断言方法,用于判断期望值和实际值是否相等,如果不想等,测试就会失败.</p><h3 id="运行测试"><a class="markdownIt-Anchor" href="#运行测试"></a> 运行测试</h3><p>你有多种方式可以运行测试：</p><ol><li><strong>运行单个测试方法</strong>: 点击方法名左侧的绿色三角形图标,选择 “Run ‘add_twoNumbers_returnsCorrectSum()’”.</li><li><strong>运行整个测试类</strong>: 点击类名左侧的绿色三角形图标,选择 “Run ‘CalculatorTest’”.</li><li><strong>运行所有测试</strong>: 在项目视图中,右键点击 <code>test</code> 目录,选择 “Run ‘Tests in com.yourpackage’”.</li></ol><h3 id="查看结果"><a class="markdownIt-Anchor" href="#查看结果"></a> 查看结果</h3><p>运行后,Android Studio 底部会弹出测试结果窗口.</p><ul><li><strong>绿色</strong> 对勾表示测试通过.</li><li><strong>红色</strong> 感叹号表示测试失败,失败时会明确指出哪一行断言失败了,以及期望值和实际值的差异.</li></ul><hr /><h2 id="测试依赖-android-框架的代码"><a class="markdownIt-Anchor" href="#测试依赖-android-框架的代码"></a> 测试依赖 Android 框架的代码</h2><p>麻烦的地方来了：如果你的代码依赖了 Android 的 API (比如 <code>Context</code>, <code>SharedPreferences</code>, <code>Log</code> 等),直接在 JVM 上运行测试会抛出 <code>RuntimeException: Method ... not mocked</code> 异常.</p><p>这时我们有两种主流的解决方案：</p><h3 id="mockito-模拟"><a class="markdownIt-Anchor" href="#mockito-模拟"></a> Mockito (模拟)</h3><p>当你的类依赖于其他对象时 (例如,一个 ViewModel 依赖一个 Repository),你可以使用 Mockito 来创建一个 “假” 的依赖对象,从而隔离被测试的类.</p><p><strong>示例</strong>：假设有一个类,它需要从 <code>Context</code> 中获取一个字符串资源.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ResourceManager</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Context context;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">ResourceManager</span><span class="params">(Context context)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.context = context;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getHelloWorldString</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> context.getString(R.string.hello_world);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>测试类 <code>ResourceManagerTest.java</code></strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@RunWith(MockitoJUnitRunner.class)</span> <span class="comment">// 告诉 JUnit 使用 Mockito 的运行器</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">ResourceManagerTest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Mock</span></span><br><span class="line">    <span class="keyword">private</span> Context mockContext; <span class="comment">// 创建一个 Context 的模拟对象</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">getHelloWorldString_returnsCorrectString</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// Arrange: &quot;打桩&quot; 或 &quot;规定行为&quot;</span></span><br><span class="line">        <span class="comment">// 当 mockContext.getString 方法被调用,并且传入的参数是 R.string.hello_world 时,</span></span><br><span class="line">        <span class="comment">// 我们让它返回一个预设的字符串 &quot;Hello World&quot;。</span></span><br><span class="line">        when(mockContext.getString(R.string.hello_world)).thenReturn(<span class="string">&quot;Hello World&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Act</span></span><br><span class="line">        <span class="type">ResourceManager</span> <span class="variable">resourceManager</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ResourceManager</span>(mockContext);</span><br><span class="line">        <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> resourceManager.getHelloWorldString();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Assert</span></span><br><span class="line">        assertEquals(<span class="string">&quot;Hello World&quot;</span>, result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>关键点</strong>:</p><ul><li><code>@RunWith(MockitoJUnitRunner.class)</code>: 启用 Mockito 注解.</li><li><code>@Mock</code>: 创建一个模拟对象.</li><li><code>when(...).thenReturn(...)</code>: 定义当模拟对象的某个方法被调用时,应该返回什么值,这样就避免了真实调用 Android 框架的代码.</li></ul><h3 id="robolectric"><a class="markdownIt-Anchor" href="#robolectric"></a> Robolectric</h3><p>Robolectric 是一个更强大的框架,它在 JVM 内部创建了一个模拟的 Android 环境.使用它,你可以像在真实设备上一样调用 Android API,而无需 mock 它们.</p><p><strong>示例</strong>：我们测试一个使用了 Android 工具类 <code>Patterns</code> 的邮箱验证器.</p><p><strong>被测试的类 <code>EmailValidator.java</code></strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">EmailValidator</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="type">boolean</span> <span class="title function_">isValid</span><span class="params">(String email)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (email == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> Patterns.EMAIL_ADDRESS.matcher(email).matches();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>Patterns.EMAIL_ADDRESS</code> 是一个 Android 框架内的静态变量,直接测试会失败.</p><p><strong>测试类 <code>EmailValidatorTest.java</code> (使用 Robolectric)</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@RunWith(RobolectricTestRunner.class)</span> <span class="comment">// 使用 Robolectric 运行器</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">EmailValidatorTest</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">isValid_correctEmail_returnsTrue</span><span class="params">()</span> &#123;</span><br><span class="line">        assertTrue(EmailValidator.isValid(<span class="string">&quot;name@example.com&quot;</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">isValid_invalidEmail_returnsFalse</span><span class="params">()</span> &#123;</span><br><span class="line">        assertFalse(EmailValidator.isValid(<span class="string">&quot;name@example&quot;</span>)); <span class="comment">// 缺少顶级域名</span></span><br><span class="line">        assertFalse(EmailValidator.isValid(<span class="string">&quot;&quot;</span>)); <span class="comment">// 空字符串</span></span><br><span class="line">        assertFalse(EmailValidator.isValid(<span class="literal">null</span>)); <span class="comment">// null</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>关键点</strong>:</p><ul><li><code>@RunWith(RobolectricTestRunner.class)</code>: 只要加上这个注解,Robolectric 就会在后台施展魔法,让 <code>Patterns.EMAIL_ADDRESS</code> 等 Android API 能够正常工作.</li><li>Robolectric 比较重,会拖慢测试速度,所以只在必要时使用.</li></ul><h2 id="最佳实践-2"><a class="markdownIt-Anchor" href="#最佳实践-2"></a> 最佳实践</h2><ol><li><strong>优先选择本地单元测试</strong>: 因为它速度快,能提供快速反馈.</li><li><strong>让代码易于测试</strong>: 遵循单一职责原则,使用依赖注入（例如通过构造函数传入依赖项）,这会让 mock 变得非常容易,像 MVVM 这样的架构模式本身就是为了让业务逻辑（ViewModel）和 UI（View）分离,从而方便对 ViewModel 进行单元测试.</li><li><strong>测试边界情况</strong>: 不仅要测试正常情况（“happy path”）,还要测试各种边界条件,比如 <code>null</code> 输入、空字符串、无效值等.</li><li><strong>保持测试独立</strong>: 每个测试方法都应该是独立的,不应该依赖于其他测试方法的执行顺序或结果,使用 <code>@Before</code> 注解来设置每个测试运行前的通用环境,使用 <code>@After</code> 来清理.</li></ol><hr /><h1 id="反射"><a class="markdownIt-Anchor" href="#反射"></a> 反射</h1><p>Java 反射机制是在<strong>程序运行时</strong>,对于任意一个类,都能够知道这个类的所有属性和方法；对于任意一个对象,都能够调用它的任意一个方法和属性.这种<strong>动态获取信息以及动态调用对象方法</strong>的功能被称为 Java 语言的反射机制.</p><p>简单来说,正常情况下,我们要在编译时就确定要使用的类和方法.而反射<strong>允许我们在运行时获取和使用类的所有数据</strong>,这让代码变得更加灵活和强大,Java 反射的核心是 <code>java.lang.Class</code> 类和 <code>java.lang.reflect</code> 包中的一系列类.</p><h2 id="反射的核心组成"><a class="markdownIt-Anchor" href="#反射的核心组成"></a> 反射的核心组成</h2><p><code>java.lang.reflect</code> 包提供了实现反射功能的主要工具类,其中最重要的几个是：</p><ol><li><strong><code>Class</code> 类</strong>: 这是反射的入口.每个类在 JVM 中都有一个对应的 <code>Class</code> 对象,这个对象包含了类的完整信息（如类名、父类、接口、字段、方法、构造函数等）,获取 <code>Class</code> 对象是使用反射的第一步.</li><li><strong><code>Constructor</code> 类</strong>: 代表类的构造函数.通过它可以动态地创建类的实例.</li><li><strong><code>Method</code> 类</strong>: 代表类的方法.通过它可以动态地调用对象的方法.</li><li><strong><code>Field</code> 类</strong>: 代表类的成员变量（属性）.通过它可以动态地读取或设置对象的属性值,即使是私有（private）属性.</li></ol><p><em>优点</em></p><ol><li><strong>灵活性和动态性</strong>: 反射是 Java 动态性的核心,它允许程序在运行时创建和控制任何类的对象,而无需在编译时进行硬编码,这对于编写通用性强的框架和库至关重要.</li><li><strong>扩展性</strong>: 程序可以动态加载和使用在编译时完全未知的外部类.</li></ol><p><em>缺点</em></p><ol><li><strong>性能开销大</strong>: 反射调用涉及到一系列的动态解析和类型检查,其性能远不及直接代码调用.因此,不应在性能敏感的热点代码中滥用.</li><li><strong>破坏封装性</strong>: 反射可以<strong>访问和修改类的私有成员</strong>,这违背了面向对象的封装原则,可能导致代码混乱和安全问题.</li><li><strong>代码可读性差</strong>: 反射相关的代码通常比直接调用更复杂、更冗长，降低了代码的可读性和可维护性.</li><li><strong>编译期类型检查失效</strong>: 反射操作的参数和返回值都是 <code>Object</code> 类型,编译器无法进行类型检查,容易在运行时抛出 <code>ClassCastException</code> 等异常.</li></ol><p><em>应用场景</em></p><p>尽管有缺点,但在许多场景下,反射是不可或缺的：</p><ol><li><strong>依赖注入框架 (DI)</strong>: 像 Dagger、Hilt、Koin（部分功能）等框架,会通过反射扫描带有 <code>@Inject</code>、<code>@Provides</code> 等注解的类、字段和方法,然后在运行时动态地创建实例并注入到需要它们的地方,这免去了大量手动创建和传递对象的样板代码.</li><li><strong>网络请求库 (Networking)</strong>: 以 Retrofit 为例,它通过反射解析你在接口中定义的注解（如 <code>@GET</code>、<code>@POST</code>、<code>@Path</code>）和方法签名,然后在运行时动态地创建一个实现了该接口的代理对象,这个代理对象会负责构建和发起实际的 HTTP 请求.</li><li><strong>JSON 解析库 (Serialization/Deserialization)</strong>: GSON 和 Jackson 这类库,通过反射读取一个类的字段信息（字段名、类型）,然后将 JSON 字符串中的数据自动填充到这个类的实例中,或者反过来将对象序列化为 JSON,这样就不用手动一个个解析字段了.</li><li><strong>视图绑定库 (View Binding - 早期)</strong>: 在官方推出 ViewBinding 之前,像 ButterKnife 这样的库非常流行.它通过反射查找带有 <code>@BindView</code> 等注解的字段,并将它们与 XML 布局文件中的视图进行关联,从而省去了大量的 <code>findViewById()</code> 调用. (注：现代的 ViewBinding 和 DataBinding 采用编译时代码生成技术，避免了反射的性能损耗)</li></ol><h2 id="获取-class-对象"><a class="markdownIt-Anchor" href="#获取-class-对象"></a> 获取 Class 对象</h2><p>主要有三种方式：</p><ol><li><p><strong>通过对象实例获取</strong>: <code>person.getClass()</code></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">str</span> <span class="operator">=</span> <span class="string">&quot;Hello&quot;</span>;</span><br><span class="line">Class&lt;?&gt; clazz = str.getClass();</span><br></pre></td></tr></table></figure></li><li><p><strong>通过类名获取</strong>: <code>类名.class</code> (最安全、性能最好)</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Class&lt;?&gt; clazz = String.class;</span><br></pre></td></tr></table></figure></li><li><p><strong>通过类的全限定名获取</strong>: <code>Class.forName(&quot;类的全限定名&quot;)</code> (常用于配置文件加载驱动等场景)</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    Class&lt;?&gt; clazz = Class.forName(<span class="string">&quot;java.lang.String&quot;</span>);</span><br><span class="line">&#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><h2 id="核心功能与示例"><a class="markdownIt-Anchor" href="#核心功能与示例"></a> 核心功能与示例</h2><p>我们以一个简单的 <code>Person</code> 类为例,来演示反射的强大功能.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Person</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = <span class="string">&quot;Default&quot;</span>;</span><br><span class="line">        <span class="built_in">this</span>.age = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="title function_">Person</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        <span class="built_in">this</span>.age = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Person</span><span class="params">(String name, <span class="type">int</span> age)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.name = name;</span><br><span class="line">        <span class="built_in">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sayHello</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Hello, I&#x27;m &quot;</span> + name);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String <span class="title function_">getSecret</span><span class="params">(String secretMessage)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;My secret is: &quot;</span> + name + <span class="string">&quot;, &quot;</span> + secretMessage;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">toString</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;Person&#123;&quot;</span> + <span class="string">&quot;name=&#x27;&quot;</span> + name + <span class="string">&#x27;\&#x27;&#x27;</span> + <span class="string">&quot;, age=&quot;</span> + age + <span class="string">&#x27;&#125;&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="创建对象实例"><a class="markdownIt-Anchor" href="#创建对象实例"></a> 创建对象实例</h3><p>反射可以调用任意构造函数（包括私有的）来创建对象.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">Class&lt;?&gt; personClass = Person.class;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. 调用公共无参构造函数 (已废弃，推荐使用 getDeclaredConstructor().newInstance())</span></span><br><span class="line"><span class="comment">// Person p1 = (Person) personClass.newInstance();</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 调用公共的有参构造函数</span></span><br><span class="line">Constructor&lt;?&gt; constructor = personClass.getConstructor(String.class, <span class="type">int</span>.class);</span><br><span class="line"><span class="type">Person</span> <span class="variable">p2</span> <span class="operator">=</span> (Person) constructor.newInstance(<span class="string">&quot;Alice&quot;</span>, <span class="number">25</span>);</span><br><span class="line">System.out.println(p2); </span><br><span class="line"><span class="comment">// 输出: Person&#123;name=&#x27;Alice&#x27;, age=25&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 调用私有的有参构造函数</span></span><br><span class="line">Constructor&lt;?&gt; privateConstructor = personClass.getDeclaredConstructor(String.class);</span><br><span class="line">privateConstructor.setAccessible(<span class="literal">true</span>); <span class="comment">// 必须设置为可访问，才能调用私有构造</span></span><br><span class="line"><span class="type">Person</span> <span class="variable">p3</span> <span class="operator">=</span> (Person) privateConstructor.newInstance(<span class="string">&quot;Bob&quot;</span>);</span><br><span class="line">System.out.println(p3); </span><br><span class="line"><span class="comment">// 输出: Person&#123;name=&#x27;Bob&#x27;, age=0&#125;</span></span><br></pre></td></tr></table></figure><h3 id="获取和操作成员变量-field"><a class="markdownIt-Anchor" href="#获取和操作成员变量-field"></a> 获取和操作成员变量 (Field)</h3><p>反射可以读取和修改对象的任意字段（包括私有的）.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">Class&lt;?&gt; personClass = Person.class;</span><br><span class="line"><span class="type">Person</span> <span class="variable">person</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Charlie&quot;</span>, <span class="number">30</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. 获取并操作公共字段 &#x27;age&#x27;</span></span><br><span class="line"><span class="type">Field</span> <span class="variable">ageField</span> <span class="operator">=</span> personClass.getField(<span class="string">&quot;age&quot;</span>);</span><br><span class="line">ageField.set(person, <span class="number">35</span>); <span class="comment">// 设置 person 对象的 age 字段为 35</span></span><br><span class="line">System.out.println(person.age); <span class="comment">// 输出: 35</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 获取并操作私有字段 &#x27;name&#x27;</span></span><br><span class="line"><span class="type">Field</span> <span class="variable">nameField</span> <span class="operator">=</span> personClass.getDeclaredField(<span class="string">&quot;name&quot;</span>);</span><br><span class="line">nameField.setAccessible(<span class="literal">true</span>); <span class="comment">// 必须设置为可访问</span></span><br><span class="line">nameField.set(person, <span class="string">&quot;David&quot;</span>);</span><br><span class="line"><span class="type">String</span> <span class="variable">name</span> <span class="operator">=</span> (String) nameField.get(person); <span class="comment">// 读取 person 对象的 name 字段</span></span><br><span class="line">System.out.println(name); <span class="comment">// 输出: David</span></span><br></pre></td></tr></table></figure><h3 id="获取和调用方法-method"><a class="markdownIt-Anchor" href="#获取和调用方法-method"></a> 获取和调用方法 (Method)</h3><p>反射可以调用对象的任意方法（包括私有的）.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">Class&lt;?&gt; personClass = Person.class;</span><br><span class="line"><span class="type">Person</span> <span class="variable">person</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Eve&quot;</span>, <span class="number">40</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. 获取并调用公共方法 &#x27;sayHello&#x27;</span></span><br><span class="line"><span class="type">Method</span> <span class="variable">sayHelloMethod</span> <span class="operator">=</span> personClass.getMethod(<span class="string">&quot;sayHello&quot;</span>);</span><br><span class="line">sayHelloMethod.invoke(person); <span class="comment">// 调用 person 对象的 sayHello 方法</span></span><br><span class="line"><span class="comment">// 输出: Hello, I&#x27;m Eve</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 获取并调用私有方法 &#x27;getSecret&#x27;</span></span><br><span class="line"><span class="type">Method</span> <span class="variable">getSecretMethod</span> <span class="operator">=</span> personClass.getDeclaredMethod(<span class="string">&quot;getSecret&quot;</span>, String.class);</span><br><span class="line">getSecretMethod.setAccessible(<span class="literal">true</span>); <span class="comment">// 必须设置为可访问</span></span><br><span class="line"><span class="comment">// 调用 person 对象的 getSecret 方法，并传入参数 &quot;be happy&quot;</span></span><br><span class="line"><span class="type">Object</span> <span class="variable">result</span> <span class="operator">=</span> getSecretMethod.invoke(person, <span class="string">&quot;be happy&quot;</span>);</span><br><span class="line">System.out.println(result); <span class="comment">// 输出: My secret is: Eve, be happy</span></span><br></pre></td></tr></table></figure><blockquote><p><strong><code>get...()</code> 和 <code>getDeclared...()</code> 的区别:</strong></p><ul><li><code>get...()</code> 系列方法（如 <code>getField</code>,<code>getMethod</code>）只能获取 <code>public</code> 的成员.</li><li><code>getDeclared...()</code> 系列方法（如 <code>getDeclaredField</code>,<code>getDeclaredMethod</code>）可以获取该类<strong>自己声明</strong>的所有成员（public, protected, private, default）,但不能获取父类的成员.</li></ul></blockquote><hr /><h1 id="注解"><a class="markdownIt-Anchor" href="#注解"></a> 注解</h1><p><strong>注解（Annotation）</strong> 本质上是一种<strong>元数据（Metadata）</strong>,可以把它理解成一个“标签”.</p><p>它本身不会直接改变代码的执行逻辑,而是为代码提供额外的信息.这些信息可以被编译器、注解处理器或者在运行时通过反射来读取和使用,从而实现特定的功能.</p><p>Android SDK (通过 <code>androidx.annotation</code> 包) 提供了很多非常有用的内置注解,它们主要用于<strong>静态代码分析</strong>,帮助你在编译阶段就发现潜在的错误.</p><ul><li><strong>注解</strong>是代码的“标签”,提供元数据.</li><li><strong>Android 内置注解</strong>主要用于编译期静态检查,提升代码健壮性.</li><li><strong>自定义注解</strong>通过 <code>@interface</code> 定义,并通过<strong>元注解</strong> (<code>@Retention</code>,<code>@Target</code>) 控制其行为.</li><li>注解的处理方式主要有<strong>运行时反射</strong>（灵活但性能稍差,如 Retrofit）和<strong>编译时代码生成</strong>（高效但复杂,如 Room、Hilt）.</li><li>它是现代 Android 开发中解耦、减少模板代码、提升开发效率的核心技术之一.</li></ul><h2 id="常见的-android-内置注解"><a class="markdownIt-Anchor" href="#常见的-android-内置注解"></a> 常见的 Android 内置注解</h2><ul><li><p><strong>Nullness 注解 (<code>@Nullable</code>, <code>@NonNull</code>)</strong></p><ul><li><p><strong>作用</strong>：标记一个变量、参数或返回值是否可以为 null,这是最有用的注解之一.</p></li><li><p><strong>好处</strong>：Android Studio 会根据这个注解进行检查,如果你试图对一个标记为 <code>@Nullable</code> 的对象直接调用方法,或者给一个标记为 <code>@NonNull</code> 的参数传递 null,IDE 会立刻给出警告,帮你提前发现潜在的 <code>NullPointerException</code>.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Javapublic <span class="keyword">void</span> <span class="title function_">setUserName</span><span class="params">(<span class="meta">@NonNull</span> String name)</span> &#123;      <span class="comment">// ...  &#125;   </span></span><br><span class="line">    <span class="meta">@Nullable</span>  <span class="keyword">public</span> String <span class="title function_">getUserName</span><span class="params">()</span> &#123;      <span class="comment">// ...  &#125;   </span></span><br><span class="line">        <span class="comment">// 调用时 </span></span><br><span class="line">        setUserName(<span class="literal">null</span>); <span class="comment">// IDE 会立即警告！ </span></span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>资源类型注解 (<code>@StringRes</code>, <code>@DrawableRes</code>, <code>@ColorRes</code>,<code>@LayoutRes</code> 等)</strong></p><ul><li><p><strong>作用</strong>：标记一个整型参数或变量应该是一个特定类型的资源 ID (例如 <code>R.string.app_name</code>),而不是普通的整数.</p></li><li><p><strong>好处</strong>：如果你传递了一个错误的类型（比如把一个 <code>R.drawable.icon</code> 传给了要求 <code>@StringRes</code> 的方法）,编译器会报错.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setTitle</span><span class="params">(<span class="meta">@StringRes</span> <span class="type">int</span> titleId)</span> &#123;      </span><br><span class="line">    textView.setText(titleId);  </span><br><span class="line">&#125;   </span><br><span class="line"><span class="comment">// 调用时  </span></span><br><span class="line">setTitle(R.string.welcome_message); <span class="comment">// 正确  </span></span><br><span class="line">setTitle(<span class="number">12345</span>); <span class="comment">// 错误，IDE 会警告 `</span></span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>线程注解 (<code>@UiThread</code>, <code>@WorkerThread</code>, <code>@MainThread</code>)</strong></p><ul><li><p><strong>作用</strong>：标记一个方法应该在哪种类型的线程上被调用。</p></li><li><p><strong>好处</strong>：如果你在一个工作线程（WorkerThread）中调用了标记为 <code>@UiThread</code> 的方法（比如更新 UI）,IDE 会给出警告,帮你避免主线程阻塞或子线程UI操作的错误.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@UiThread</span>  </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">updateUI</span><span class="params">()</span> &#123;      <span class="comment">// ...  &#125;  </span></span><br><span class="line"><span class="meta">@WorkerThread</span>  </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">doHeavyTask</span><span class="params">()</span> &#123;      <span class="comment">// ...  &#125;</span></span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>值约束注解 (<code>@IntRange</code>, <code>@FloatRange</code>, <code>@Size</code>)</strong></p><ul><li><p><strong>作用</strong>：限制数字的范围或集合/数组的大小.</p></li><li><p><strong>好处</strong>：静态检查,确保传入的值在有效范围内.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setAlpha</span><span class="params">(<span class="meta">@IntRange(from = 0, to = 255)</span> <span class="type">int</span> alpha)</span> &#123;      <span class="comment">// ...  &#125;</span></span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>类型定义注解 (<code>@IntDef</code>, <code>@StringDef</code>)</strong></p><ul><li><p><strong>作用</strong>：替代 Java 的枚举,创建一组类型安全的常量.</p></li><li><p><strong>好处</strong>：枚举的性能开销（内存占用）比常量要大,使用 <code>@IntDef</code> 可以在保证类型安全的同时,获得和使用普通常量一样的性能.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 定义常量  </span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">STYLE_NORMAL</span> <span class="operator">=</span> <span class="number">0</span>;  </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">STYLE_BOLD</span> <span class="operator">=</span> <span class="number">1</span>;  </span><br><span class="line"><span class="comment">// 2. 使用 @IntDef 将它们“捆绑”成一个新的注解类型  </span></span><br><span class="line"><span class="meta">@IntDef(&#123;STYLE_NORMAL, STYLE_BOLD&#125;)</span> </span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.SOURCE)</span> <span class="comment">// 保留策略,后面会讲  </span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> TextStyle &#123;&#125;   </span><br><span class="line"><span class="comment">// 3. 在方法参数中使用这个新注解 </span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setTextStyle</span><span class="params">(<span class="meta">@TextStyle</span> <span class="type">int</span> style)</span> &#123;      <span class="comment">// ...  &#125;   </span></span><br><span class="line">    <span class="comment">// 调用时 </span></span><br><span class="line">    setTextStyle(STYLE_NORMAL); <span class="comment">// 正确  </span></span><br><span class="line">    setTextStyle(<span class="number">2</span>); <span class="comment">// 错误，IDE 会警告</span></span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong><code>@Keep</code></strong></p><ul><li><strong>作用</strong>：告诉 Proguard/R8,在代码混淆和优化时,不要移除或重命名被这个注解标记的类、方法或字段.</li><li><strong>好处</strong>：当你需要通过反射访问某些代码时（比如 JNI 调用或者某些序列化库）,这个注解非常有用,可以防止它们在打包时被优化掉.</li></ul></li></ul><hr /><h2 id="创建自定义注解"><a class="markdownIt-Anchor" href="#创建自定义注解"></a> 创建自定义注解</h2><p>创建自定义注解主要涉及两个方面：<strong>定义注解</strong>和<strong>处理注解</strong>.</p><h3 id="定义注解"><a class="markdownIt-Anchor" href="#定义注解"></a> 定义注解</h3><p>使用 <code>@interface</code> 关键字来定义一个注解,在定义时,需要使用**元注解（Meta-Annotation）**来修饰你的注解,告诉系统它应该如何工作.</p><ul><li><code>@Retention</code>: 定义注解的<strong>保留策略</strong>,即注解的生命周期.<ul><li><code>RetentionPolicy.SOURCE</code>: 注解只保留在源代码中,编译后会被丢弃.主要用于静态代码检查,比如 <code>@Override</code> 和 <code>@IntDef</code>.</li><li><code>RetentionPolicy.CLASS</code>: 注解会保留在编译后的 <code>.class</code> 文件中,但在运行时（JVM）不可见,这是默认策略.</li><li><code>RetentionPolicy.RUNTIME</code>: 注解会保留到运行时,可以通过**反射（Reflection）**来获取和使用,<strong>大多数自定义注解（尤其是框架类）都使用这个策略.</strong></li></ul></li><li><code>@Target</code>: 定义注解可以被应用在哪些代码元素上.<ul><li><code>ElementType.TYPE</code>: 类、接口、枚举.</li><li><code>ElementType.FIELD</code>: 字段（成员变量）.</li><li><code>ElementType.METHOD</code>: 方法.</li><li><code>ElementType.PARAMETER</code>: 方法参数.</li><li><code>ElementType.CONSTRUCTOR</code>: 构造函数.</li><li>等等…</li></ul></li><li><code>@Documented</code>: 表示这个注解应该被包含在 Javadoc 中.</li><li><code>@Inherited</code>: 表示子类可以继承父类中的这个注解.</li></ul><p><strong>示例：创建一个用于事件追踪的注解</strong></p><p>假设我们想标记某些方法，当它们被调用时，自动上报一个追踪事件。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 使用元注解定义注解的行为</span></span><br><span class="line"><span class="meta">@Retention(RetentionPolicy.RUNTIME)</span> <span class="comment">// 我们需要在运行时通过反射读取,所以用 RUNTIME</span></span><br><span class="line"><span class="meta">@Target(ElementType.METHOD)</span>       <span class="comment">// 这个注解只能用在方法上</span></span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> EventTracking &#123;</span><br><span class="line">    <span class="comment">// 2. 定义注解的成员（参数）</span></span><br><span class="line">    String <span class="title function_">eventName</span><span class="params">()</span>; <span class="comment">// 事件名称,这是一个必须的参数</span></span><br><span class="line">    <span class="type">int</span> <span class="title function_">eventId</span><span class="params">()</span> <span class="keyword">default</span> <span class="number">0</span>; <span class="comment">// 事件ID,这是一个可选参数,给了默认值0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>如何使用我们定义的注解：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@EventTracking(eventName = &quot;user_click_login_button&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onLoginButtonClick</span><span class="params">(View view)</span> &#123;</span><br><span class="line">        <span class="comment">// 用户登录逻辑...</span></span><br><span class="line">        System.out.println(<span class="string">&quot;用户点击了登录按钮&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@EventTracking(eventName = &quot;share_content&quot;, eventId = 1001)</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onShare</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 分享逻辑...</span></span><br><span class="line">        System.out.println(<span class="string">&quot;用户分享了内容&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="处理注解"><a class="markdownIt-Anchor" href="#处理注解"></a> 处理注解</h3><p>定义好注解后,它自己不会做任何事,你需要编写代码来处理它,处理方式主要有两种：</p><ol><li><p><strong>运行时处理（通过反射）</strong> 这是最直接的方式.代码在运行时,通过 Java 的反射 API 来检查某个类、方法或字段上是否有特定的注解,并获取注解的参数值,然后执行相应逻辑.</p><p><strong>示例：处理上面定义的 <code>@EventTracking</code> 注解（通常用 AOP 思想实现,这里用一个简单的代理作为演示）</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 这是一个简化的处理逻辑示例</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">EventTracker</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">track</span><span class="params">(Object target, String methodName, Class&lt;?&gt;... parameterTypes)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 1. 获取方法对象</span></span><br><span class="line">            <span class="type">Method</span> <span class="variable">method</span> <span class="operator">=</span> target.getClass().getMethod(methodName, parameterTypes);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 2. 检查方法上是否存在 @EventTracking 注解</span></span><br><span class="line">            <span class="keyword">if</span> (method.isAnnotationPresent(EventTracking.class)) &#123;</span><br><span class="line">                <span class="comment">// 3. 获取注解实例</span></span><br><span class="line">                <span class="type">EventTracking</span> <span class="variable">annotation</span> <span class="operator">=</span> method.getAnnotation(EventTracking.class);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 4. 获取注解的参数值并上报</span></span><br><span class="line">                <span class="type">String</span> <span class="variable">eventName</span> <span class="operator">=</span> annotation.eventName();</span><br><span class="line">                <span class="type">int</span> <span class="variable">eventId</span> <span class="operator">=</span> annotation.eventId();</span><br><span class="line"></span><br><span class="line">                System.out.println(String.format(<span class="string">&quot;上报事件：name=%s, id=%d&quot;</span>, eventName, eventId));</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 实际场景中,这里会调用方法的原始逻辑</span></span><br><span class="line">            <span class="comment">// method.invoke(target, args);</span></span><br><span class="line"></span><br><span class="line">        &#125; <span class="keyword">catch</span> (NoSuchMethodException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 模拟调用</span></span><br><span class="line"><span class="type">MyActivity</span> <span class="variable">activity</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">MyActivity</span>();</span><br><span class="line"><span class="comment">// 模拟登录按钮点击</span></span><br><span class="line">EventTracker.track(activity, <span class="string">&quot;onLoginButtonClick&quot;</span>, View.class); <span class="comment">// 输出:上报事件 name=user_click_login_button,id=0</span></span><br><span class="line"><span class="comment">// 模拟分享</span></span><br><span class="line">EventTracker.track(activity, <span class="string">&quot;onShare&quot;</span>); <span class="comment">// 输出:上报事件 name=share_content, id=1001</span></span><br></pre></td></tr></table></figure><ul><li><strong>优点</strong>：实现简单直观.</li><li><strong>缺点</strong>：<strong>性能开销大</strong>,反射操作在运行时比较耗时,不适合用在频繁调用的代码中.</li></ul></li><li><p><strong>编译时处理（通过注解处理器 APT）</strong> 这是一种更高效、更现代的方式,你需要创建一个注解处理器（Annotation Processor Tool,它会在<strong>编译代码期间</strong>运行,处理器会扫描源码,找到你定义的注解,然后<strong>自动生成 Java 代码</strong>.</p><ul><li><strong>工作流程</strong>：<ol><li>你写了带注解的 Java 代码.</li><li><code>javac</code> 编译器开始编译.</li><li>编译器发现有注解处理器注册了,就调用它.</li><li>你的注解处理器开始工作,扫描源码,找到 <code>@EventTracking</code> 注解.</li><li>处理器根据注解信息,自动生成一些新的 <code>.java</code> 文件（比如一个 <code>EventTrackerHelper.java</code>）.</li><li>编译器继续编译,把这些新生成的代码也一起编译成 <code>.class</code> 文件.</li></ol></li><li><strong>优点</strong>：没有运行时的性能损耗,所有工作都在编译期完成,代码是静态生成的,类型安全.</li><li><strong>缺点</strong>：实现起来比反射复杂,需要单独创建一个 Java 模块来编写处理器.</li></ul></li></ol><hr /><h2 id="使用场景"><a class="markdownIt-Anchor" href="#使用场景"></a> 使用场景</h2><ol><li><p><strong>依赖注入</strong></p><ul><li><p><strong>框架</strong>：Dagger, Hilt, ButterKnife (已废弃但原理相同).</p></li><li><p><strong>原理</strong>：通过 <code>@Inject</code>,<code>@Component</code>,<code>@Module</code> 等注解标记依赖关系.注解处理器在编译时生成所有对象的创建和注入逻辑,避免了手动 <code>new</code> 对象和管理依赖的麻烦.</p></li></ul></li><li><p>**网络请求库 **</p><ul><li><p><strong>框架</strong>：Retrofit.</p></li><li><p><strong>原理</strong>：使用 <code>@GET</code>, <code>@POST</code>, <code>@Path</code>, <code>@Query</code> 等注解来描述一个 HTTP 请求,Retrofit 在运行时通过<strong>反射</strong>解析这些注解,然后动态地创建一个实现了 API 接口的代理对象,这个对象会负责发起真实的 HTTP 请求.</p></li></ul></li><li><p><strong>数据库</strong></p><ul><li><p><strong>框架</strong>：Room, GreenDAO.</p></li><li><p><strong>原理</strong>：使用 <code>@Entity</code>, <code>@PrimaryKey</code>, <code>@ColumnInfo</code>, <code>@Dao</code> 等注解来定义数据表结构和数据库操作,注解处理器在编译时解析这些注解,生成所有用于操作 SQLite 的模板代码,避免了手写复杂的 <code>SQL</code> 语句.</p></li></ul></li><li><p>**序列化/反序列化 **</p><ul><li><strong>框架</strong>：Gson, Moshi, Jackson.</li><li><strong>原理</strong>：使用 <code>@SerializedName</code>,<code>@Json</code> 等注解来指定一个 Java 字段和 JSON key 之间的映射关系,库在运行时通过反射读取这些注解,实现对象和 JSON 字符串之间的自动转换.</li></ul></li><li><p><strong>路由</strong></p><ul><li><strong>框架</strong>：ARouter.</li><li><strong>原理</strong>：使用 <code>@Route</code> 注解标记一个 Activity 或 Fragment,并给它一个路径.注解处理器会收集所有路径信息并生成一个映射表.在需要跳转时,你只需要提供路径,路由框架就能根据映射表找到对应的组件并启动它,实现模块间的解耦.</li></ul></li></ol><hr /><h1 id="动态代理"><a class="markdownIt-Anchor" href="#动态代理"></a> 动态代理</h1><p>动态代理是 Java 反射机制的一个强大应用,它允许我们在运行时创建“替身”对象,从而在不侵入原有代码的基础上,为程序增加统一的、额外的功能.理解了它的原理,你就能更深刻地理解许多优秀开源框架的设计思想.</p><p><em>优点</em></p><ul><li><strong>代码简洁</strong>：避免了静态代理的大量重复代码和类.</li><li><strong>高扩展性</strong>：可以为任意接口的实现类提供代理,易于维护和扩展.</li><li><strong>业务解耦</strong>：实现了非业务逻辑（如日志、监控）与核心业务逻辑的完全分离.</li></ul><p><em>缺点</em></p><ul><li><strong>只能代理接口</strong>：JDK 的动态代理是基于接口的,如果一个类没有实现任何接口,那么它就不能被这种方式代理. (可以通过 CGLIB 等第三方库实现对类的代理,但原理更复杂).</li><li><strong>性能开销</strong>：由于使用了反射机制,调用速度会比直接调用慢一些.但在绝大多数场景下（尤其是涉及 IO 操作如网络、数据库）,这点性能差异可以忽略不计.</li></ul><h2 id="代理模式"><a class="markdownIt-Anchor" href="#代理模式"></a> 代理模式</h2><p>在聊动态代理之前,我们先要理解<strong>代理模式 (Proxy Pattern)</strong>.</p><p>代理模式的核心思想是：<strong>为一个对象提供一个代理（替身）,以来控制对这个对象的访问.</strong> 代理对象和真实对象通常会实现同一个接口,这样调用者就可以像使用真实对象一样使用代理对象,而察觉不到差异.</p><p><strong>生活中的例子：</strong> 你想租房子（访问“房源”这个真实对象）,但你不直接找房东,而是通过房产中介（代理对象）.中介除了帮你完成租房的核心业务外，还可能做一些额外的事情,比如：</p><ul><li><strong>看房前</strong>：帮你筛选房源、核实信息.</li><li><strong>租房时</strong>：带你看房、协商价格.</li><li><strong>签约后</strong>：帮你办理合同、收取中介费.</li></ul><p>在这里,中介（代理）控制了你对房东（真实对象）的访问,并在此过程中加入了自己的逻辑.</p><h2 id="动态代理-2"><a class="markdownIt-Anchor" href="#动态代理-2"></a> 动态代理</h2><p>代理模式可以分为<strong>静态代理</strong>和<strong>动态代理</strong>.</p><h3 id="静态代理"><a class="markdownIt-Anchor" href="#静态代理"></a> 静态代理</h3><p><strong>静态代理</strong>是指在<strong>编译期间</strong>就已经创建好了代理类（<code>.java</code> 文件存在）.你需要手动为每一个需要被代理的接口或类创建一个代理类.</p><ul><li><strong>缺点</strong>：<ol><li><strong>类爆炸</strong>：如果需要代理的接口很多,你就得为每个接口都写一个代理类,导致项目中类的数量剧增.</li><li><strong>代码重复</strong>：所有代理类中的附加逻辑（比如打印日志）可能是相似的,这会导致大量重复代码.</li><li><strong>不易维护</strong>：如果接口发生变化（比如增加一个方法）,真实对象和代理对象都需要同步修改.</li></ol></li></ul><h3 id="动态代理-3"><a class="markdownIt-Anchor" href="#动态代理-3"></a> 动态代理</h3><p><strong>动态代理</strong>则是在<strong>程序运行时</strong>,通过反射机制动态地创建一个代理类及其对象,你不需要手动编写任何代理类的 <code>.java</code> 文件.</p><ul><li><strong>优点</strong>：<ol><li><strong>通用性强</strong>：只需要一个“处理器”就可以代理实现了任意接口的任意对象,解决了静态代理的类爆炸和代码重复问题.</li><li><strong>灵活性高</strong>：可以在运行时决定代理的逻辑,非常灵活.</li><li><strong>解耦</strong>：将代理的附加逻辑（如日志、事务、权限检查）与业务逻辑完全分离.</li></ol></li></ul><h2 id="实现动态代理"><a class="markdownIt-Anchor" href="#实现动态代理"></a> 实现动态代理</h2><p>在 Java/Android 中实现动态代理,主要依赖两个核心的 API：</p><ol><li><code>java.lang.reflect.Proxy</code>: 这是创建动态代理对象的主类,它最重要的方法是 <code>newProxyInstance()</code>.</li><li><code>java.lang.reflect.InvocationHandler</code>: 这是一个接口,你需要创建一个类来实现它,所有对代理对象的<strong>任何方法调用</strong>,最终都会被转发到这个接口的 <code>invoke()</code> 方法中.</li></ol><h3 id="实现步骤"><a class="markdownIt-Anchor" href="#实现步骤"></a> 实现步骤</h3><p><strong>第一步：定义一个接口和它的实现类（真实对象）</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 定义一个售卖电脑的接口</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">IComputerSeller</span> &#123;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">sellComputer</span><span class="params">(<span class="type">int</span> money)</span>;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">repairComputer</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 联想电脑厂商，作为真实对象（被代理的对象）</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LenovoComputer</span> <span class="keyword">implements</span> <span class="title class_">IComputerSeller</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sellComputer</span><span class="params">(<span class="type">int</span> money)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;联想厂商以 &quot;</span> + money + <span class="string">&quot; 元的价格卖出了一台电脑。&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">repairComputer</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;联想厂商维修了一台电脑。&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>第二步：创建一个 <code>InvocationHandler</code> 的实现类</strong></p><p>这是动态代理的核心,所有代理逻辑都在这里编写.</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 代理处理器，可以把它想象成“房产中介”或“电脑销售代理商”</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SellerProxyHandler</span> <span class="keyword">implements</span> <span class="title class_">InvocationHandler</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 持有真实对象的引用</span></span><br><span class="line">    <span class="keyword">private</span> Object realObject;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">SellerProxyHandler</span><span class="params">(Object realObject)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.realObject = realObject;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当你调用代理对象的任何方法时，都会自动回调这个 invoke 方法。</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> proxy     动态生成的代理对象本身 (一般很少使用)</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> method    你当前调用的方法 (例如 sellComputer() 这个方法的 Method 对象)</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> args      你调用方法时传入的参数 (例如 sellComputer(5000) 中的 5000)</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span>          你调用的方法的返回值</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> Throwable</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">invoke</span><span class="params">(Object proxy, Method method, Object[] args)</span> <span class="keyword">throws</span> Throwable &#123;</span><br><span class="line">        <span class="comment">// === 在调用真实方法前，执行预处理逻辑 ===</span></span><br><span class="line">        System.out.println(<span class="string">&quot;【代理商】开始接待客户，了解需求...&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 从参数中获取钱</span></span><br><span class="line">        <span class="keyword">if</span> (args != <span class="literal">null</span> &amp;&amp; args.length &gt; <span class="number">0</span> &amp;&amp; args[<span class="number">0</span>] <span class="keyword">instanceof</span> Integer) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">money</span> <span class="operator">=</span> (<span class="type">int</span>) args[<span class="number">0</span>];</span><br><span class="line">            <span class="keyword">if</span> (money &lt; <span class="number">6000</span>) &#123;</span><br><span class="line">                 System.out.println(<span class="string">&quot;【代理商】钱太少了，厂商不卖！&quot;</span>);</span><br><span class="line">                 <span class="keyword">return</span> <span class="literal">null</span>; <span class="comment">// 钱不够，直接拦截，不调用真实方法</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// === 调用真实对象的方法 ===</span></span><br><span class="line">        <span class="comment">// 这行代码相当于调用 lenovo.sellComputer(5000)</span></span><br><span class="line">        <span class="type">Object</span> <span class="variable">result</span> <span class="operator">=</span> method.invoke(realObject, args);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// === 在调用真实方法后，执行后处理逻辑 ===</span></span><br><span class="line">        System.out.println(<span class="string">&quot;【代理商】交易完成，赠送一个鼠标垫。&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>第三步：使用 <code>Proxy.newProxyInstance()</code> 创建代理对象并调用</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 创建真实对象</span></span><br><span class="line">        <span class="type">IComputerSeller</span> <span class="variable">lenovo</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">LenovoComputer</span>();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 创建 InvocationHandler</span></span><br><span class="line">        <span class="type">InvocationHandler</span> <span class="variable">handler</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">SellerProxyHandler</span>(lenovo);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 使用 Proxy.newProxyInstance() 创建代理对象</span></span><br><span class="line">        <span class="type">IComputerSeller</span> <span class="variable">proxySeller</span> <span class="operator">=</span> (IComputerSeller) Proxy.newProxyInstance(</span><br><span class="line">                lenovo.getClass().getClassLoader(),  <span class="comment">// 参数1: 真实对象的类加载器</span></span><br><span class="line">                lenovo.getClass().getInterfaces(),   <span class="comment">// 参数2: 真实对象所实现的所有接口</span></span><br><span class="line">                handler                              <span class="comment">// 参数3: 我们编写的 InvocationHandler</span></span><br><span class="line">        );</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 通过代理对象调用方法 (注意：此时调用的是代理对象！)</span></span><br><span class="line">        System.out.println(<span class="string">&quot;------- 场景1: 出价 5000 元 -------&quot;</span>);</span><br><span class="line">        proxySeller.sellComputer(<span class="number">5000</span>); <span class="comment">// 这个调用会被 handler 的 invoke 方法拦截</span></span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="string">&quot;\n------- 场景2: 出价 8000 元 -------&quot;</span>);</span><br><span class="line">        proxySeller.sellComputer(<span class="number">8000</span>);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 调用另一个方法</span></span><br><span class="line">        <span class="comment">// proxySeller.repairComputer(); // 同样会被 invoke 方法代理</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>运行结果：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">------- 场景1: 出价 5000 元 -------</span><br><span class="line">【代理商】开始接待客户，了解需求...</span><br><span class="line">【代理商】钱太少了，厂商不卖！</span><br><span class="line"></span><br><span class="line">------- 场景2: 出价 8000 元 -------</span><br><span class="line">【代理商】开始接待客户，了解需求...</span><br><span class="line">联想厂商以 8000 元的价格卖出了一台电脑。</span><br><span class="line">【代理商】交易完成，赠送一个鼠标垫。</span><br></pre></td></tr></table></figure><h2 id="应用场景"><a class="markdownIt-Anchor" href="#应用场景"></a> 应用场景</h2><p>动态代理的思想——<strong>面向切面编程 (AOP)</strong>,在不修改原有业务代码的情况下,增加通用功能.</p><ol><li><p><strong>Retrofit 网络请求框架</strong></p><p><strong>这是最经典的应用！</strong> 你只需要定义一个 Java 接口,并用注解 (<code>@GET</code>, <code>@POST</code>等) 描述网络请求.</p><ul><li>当你调用 <code>retrofit.create(ApiService.class)</code> 时,Retrofit 内部就使用了<strong>动态代理</strong>.它动态地创建了一个 <code>ApiService</code> 接口的实现类.</li><li>当你调用 <code>getUser(&quot;123&quot;)</code> 方法时,<code>InvocationHandler</code> 的 <code>invoke</code> 方法被触发.在这个方法里,Retrofit 解析了方法上的注解 (<code>@GET</code>) 和参数 (<code>@Path</code>),然后利用 OkHttp 组装并发出一个真实的 HTTP 请求.</li></ul></li><li><p><strong>通用日志和性能监控</strong></p><ul><li>你可以创建一个动态代理,在 <code>invoke</code> 方法的开头和结尾记录方法名、参数、执行耗时等信息,而无需在每个业务方法里手动添加日志代码.</li></ul></li><li><p><strong>权限检查和身份验证</strong></p><ul><li>在调用需要特定权限的业务方法前,可以在 <code>invoke</code> 方法里统一检查用户是否登录、是否有操作权限.如果没有,就直接拦截，不再执行真实方法.</li></ul></li><li><p><strong>RPC 框架 (远程过程调用)</strong></p><ul><li>像 Retrofit 一样,RPC 框架允许你像调用本地方法一样调用远程服务器上的方法.其底层也是通过动态代理,将本地方法调用转换为网络请求发送到远端.</li></ul></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;注释&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#注释&quot;&gt;&lt;/a&gt; 注释&lt;/h1&gt;
&lt;h2 id=&quot;单行注释&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#单行注释&quot;&gt;&lt;/a&gt; 单行注释&lt;/h2&gt;
&lt;fig</summary>
      
    
    
    
    
    <category term="Java" scheme="https://11111-beep.github.io/tags/Java/"/>
    
  </entry>
  
  <entry>
    <title>客户端与服务端详解</title>
    <link href="https://11111-beep.github.io/2025/08/04/%E5%AE%A2%E6%88%B7%E7%AB%AF%E4%B8%8E%E6%9C%8D%E5%8A%A1%E7%AB%AF%E8%AF%A6%E8%A7%A3/"/>
    <id>https://11111-beep.github.io/2025/08/04/%E5%AE%A2%E6%88%B7%E7%AB%AF%E4%B8%8E%E6%9C%8D%E5%8A%A1%E7%AB%AF%E8%AF%A6%E8%A7%A3/</id>
    <published>2025-08-04T08:44:19.000Z</published>
    <updated>2025-12-14T08:45:46.507Z</updated>
    
    <content type="html"><![CDATA[<p>在Android应用开发中,<strong>客户端</strong>(Client)与<strong>服务端</strong>(Server)是时常被提及的概念,通常来说&quot;客户端指的是用户直接交互的那个<strong>Android应用程序本身</strong>,而服务端则是为客户端提供数据,计算或存储服务的<strong>远程计算机系统&quot;</strong>.但我本人认为其实这种表述<strong>并不是特别准确</strong>,以下是我的一些分析与见解.</p><hr /><img src="/2025/08/04/%E5%AE%A2%E6%88%B7%E7%AB%AF%E4%B8%8E%E6%9C%8D%E5%8A%A1%E7%AB%AF%E8%AF%A6%E8%A7%A3/4204780b9d414fffbd98dea1a4d21096.png" class=""><h2 id="客户端client"><a class="markdownIt-Anchor" href="#客户端client"></a> 客户端(Client)</h2><p>1.定义 :<br />在Android开发中,客户端其实是你安装在手机平板上的那个由<strong>apk文件运行的应用程序</strong>,通俗点说你手机上进行<strong>UI交互的App</strong>.它是用户界面的提供者,负责显示信息,接收用户输入并根据需要向远程服务端或应用内其他组件发起请求.</p><p>2.功能 ：</p><ul><li><strong>用户界面 (UI)：</strong> 展示数据、图片、按钮等，并响应用户的触摸、滑动等操作。</li><li><strong>数据展示与处理：</strong> 从服务端获取数据后，进行格式化、展示，或进行一些本地的计算和处理。</li><li><strong>网络请求：</strong> 通过 HTTP/HTTPS 等协议向<strong>远程服务端</strong>发送请求（例如，获取用户信息、上传图片、提交订单等）。</li><li><strong>本地存储：</strong> 在设备上存储一些数据（如用户偏好设置、缓存数据）。</li><li><strong>设备功能调用：</strong> 使用摄像头、GPS、传感器等设备硬件功能。</li><li><strong>应用内通信：</strong> 向<strong>应用内服务端</strong>发送消息、绑定服务或查询数据，以利用其提供的本地服务。</li></ul><p>3.例子 :</p><ul><li><strong>微信 App：</strong> 你手机上打开的微信就是客户端。它负责显示聊天界面、朋友圈、联系人列表等。当你发送消息、刷新朋友圈时，它会向微信的<strong>远程服务器</strong>发送请求。</li><li><strong>淘宝 App：</strong> 你浏览商品、下单支付时，淘宝 App 就是客户端。它展示商品图片和描述，并向淘宝的<strong>远程服务器</strong>发送购买请求。</li><li><strong>一个音乐播放 App 中的 Activity：</strong> 它作为<strong>应用内客户端</strong>，向同应用内的<strong>音乐播放 Service</strong>（应用内服务端）发送播放、暂停等指令，以控制音乐播放。</li></ul><hr /><h2 id="服务端"><a class="markdownIt-Anchor" href="#服务端"></a> 服务端</h2><p>根据部署功能和位置,服务端其实可以分为以下两类:</p><h3 id="一远程服务端"><a class="markdownIt-Anchor" href="#一远程服务端"></a> 一.远程服务端</h3><p>1.定义 :<br />远程服务端是一台或一组运行在远程数据中心(例如阿里云,Google Cloud等)的计算机系统.它并不直接与用户交互,而是接收来自Android客户端是网络请求,处理这些请求,并返回相应数据或执行相应的操作.</p><p>2.功能 :</p><ul><li><strong>数据存储与管理：</strong> 存储和管理大量的用户数据、商品信息、图片、视频等（通常使用数据库，如 <code>MySQL</code>、<code>PostgreSQL</code>、<code>MongoDB</code>等）。</li><li><strong>业务逻辑处理：</strong> 执行复杂的业务规则和计算，例如用户认证、权限管理、订单处理、商品推荐算法、数据分析等。</li><li><strong>API 提供：</strong> 提供应用程序接口（<code>API</code>，Application Programming Interface），Android 客户端通过这些接口与远程服务端进行通信。API 定义了客户端可以如何请求数据以及服务端会如何响应。</li><li><strong>安全性：</strong> 处理用户认证、数据加密、防攻击等安全相关任务。</li><li><strong>负载均衡与伸缩性：</strong> 确保在高并发访问下系统依然稳定运行。</li></ul><p>3.例子 :</p><ul><li><strong>微信服务器：</strong> 存储所有用户的聊天记录、朋友圈动态、联系人信息。当你发送消息时，消息会先发送到微信服务器，服务器再转发给目标用户。</li><li><strong>淘宝服务器：</strong> 存储所有商品信息、用户订单、支付记录。当你搜索商品、下单时，淘宝服务器会处理这些请求，并从数据库中检索或写入相应数据。</li><li><strong>天气 API 服务：</strong> 存储全球各地的天气数据。当天气 App 客户端请求某个城市的天气时，服务器会查询数据并返回给客户端。</li></ul><h3 id="二android应用内服务端ipc服务端"><a class="markdownIt-Anchor" href="#二android应用内服务端ipc服务端"></a> 二.Android应用内服务端(IPC服务端)</h3><p>1.定义 :<br />在Android设备内部,某些应用程序组件(例如<code>Service</code>或<code>ContentProvider</code>)可以充当&quot;服务端&quot;,为同一应用或不同应用中的其他组件(充当客户端)提供特定功能或数据.这种&quot;服务端&quot;通常<strong>运行在独立的进程当中</strong>,并通过Android的进程间通信(IPC)机制进行交互.</p><p>2.功能 :</p><ul><li><strong>提供后台操作：</strong> <code>Service</code> 可以在没有用户界面的情况下执行长时间运行的操作（如音乐播放、下载文件），作为其他组件的后台服务提供者。</li><li><strong>数据共享：</strong> <code>ContentProvider</code> 允许应用程序以结构化的方式共享数据给其他应用程序。</li><li><strong>跨进程方法调用/消息传递：</strong> 通过 AIDL 或 Messenger，允许不同进程的组件互相调用方法或传递消息。</li></ul><p>2.例子 :</p><ul><li>一个音乐播放 App 中的<strong>音乐播放 Service</strong>：它作为“服务端”，负责实际的音乐播放逻辑。其他组件（客户端）可以向它发送播放、暂停等指令，即使界面不可见，Service 仍能继续播放。</li><li>系统相册的<strong>ContentProvider</strong>：作为“服务端”，它管理设备上的所有图片和视频数据。其他 App（客户端）可以通过 <code>ContentResolver</code> 查询、访问这些数据，而无需直接访问文件系统。</li><li>一个提供复杂计算的<strong>辅助 Service</strong>：如果一个应用的某个计算非常耗时且需要独立进程，它可以通过 <code>Service</code> 作为“服务端”暴露接口，供应用的 UI 进程（客户端）调用。</li></ul><hr /><h2 id="android客户端与远程服务端交互的常见方式"><a class="markdownIt-Anchor" href="#android客户端与远程服务端交互的常见方式"></a> Android客户端与远程服务端交互的常见方式</h2><p>Android客户端与远程服务端交互主要依赖于网络通信,以下是几种常见的通信方式:</p><ul><li><strong>HTTP/HTTPS (RESTful API)</strong>：这是最常用的一种方式。客户端通过HTTP/HTTPS协议发送请求（GET, POST, PUT, DELETE等），服务端接收请求并返回响应（通常是JSON或XML格式）。这种方式简单、灵活，并且有成熟的工具库支持。</li><li><strong>WebSocket</strong>：与HTTP的请求-响应模式不同，WebSocket提供全双工通信，允许客户端和服务器之间建立持久连接，双方可以随时发送消息。这对于实时应用（如聊天、游戏）非常有用。</li><li><strong>Socket (TCP/UDP)</strong>：这是更底层的网络通信方式，允许开发者直接控制数据包的发送和接收。适用于对性能和实时性要求极高，或需要自定义协议的场景。</li><li><strong>MQTT</strong>：一种轻量级的消息发布/订阅协议，适用于物联网（IoT）设备和网络带宽有限的场景。</li></ul><hr /><h2 id="进程间通信ipc"><a class="markdownIt-Anchor" href="#进程间通信ipc"></a> 进程间通信IPC</h2><p>在Android应用内部,不同组件之间(如<code>Activity</code>、<code>Service</code>、<code>BroadcastReceiver</code>、<code>ContentProvider</code>)也存在客户端与服务端的概念,这通常涉及到<strong>进程间通信(IPC)</strong>.当一个组件需要调用另一个组件的功能,而这两个组件运行在不同的进程时,就需要IPC.</p><ul><li><strong>Binder</strong>：Android中最核心的IPC机制。它基于Linux Binder驱动，提供了高效的进程间通信能力。Parcelable是Binder通信中常用的数据序列化方式。</li><li><strong>AIDL (Android Interface Definition Language)</strong>：如果需要跨进程通信，并且客户端和服务端定义了一个公共接口，AIDL可以帮助你自动生成接口代码，简化IPC的实现。它在底层也是基于Binder。</li><li><strong>Messenger</strong>：基于Handler和Message机制，可以在不同进程间发送Message对象，实现简单的IPC。相对AIDL更简单，但功能不如AIDL强大，一次只能处理一个请求。</li><li><strong>ContentProvider</strong>：主要用于进程间共享数据。它提供了一套标准接口（insert, query, update, delete），其他应用可以通过URI访问ContentProvider暴露的数据。</li><li><strong>BroadcastReceiver</strong>：通过发送和接收广播消息，实现轻量级的进程间通信。不适合大量数据传输或复杂交互。</li></ul><hr /><h2 id="样例示范"><a class="markdownIt-Anchor" href="#样例示范"></a> 样例示范</h2><p>这里我给出最常见的两个代码样例来帮助我们更深入的理解客户端与服务端的机制:</p><p><strong>1.Android客户端与远程Web服务器(HTTP/RESTful API)交互:使用Retrofit</strong><br /><strong>2. Android应用内客户端与服务端(Service)交互:使用Messenger</strong></p><hr /><h3 id="android客户端与远程web服务器httprestful-api交互使用retrofit"><a class="markdownIt-Anchor" href="#android客户端与远程web服务器httprestful-api交互使用retrofit"></a> Android客户端与远程Web服务器(HTTP/RESTful API)交互:使用Retrofit</h3><p>Retrofit是一个安全的HTTP客户端,它简化了Android应用中HTTP请求的缩写.他将REST API转化为Java接口.<br />场景:Android应用从一个公共API获取用户列表.</p><p>服务端假设:  我们假设有一个简单的REST API,例如<code>https://jsonplaceholder.typicode.com/users</code>，它返回一个JSON格式的用户列表。</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">[</span></span><br><span class="line">  <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">1</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Leanne Graham&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;username&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Bret&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;email&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Sincere@april.biz&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="comment">// ... other fields</span></span><br><span class="line">  <span class="punctuation">&#125;</span><span class="punctuation">,</span></span><br><span class="line">  <span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;id&quot;</span><span class="punctuation">:</span> <span class="number">2</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;name&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Ervin Howell&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;username&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Antonette&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;email&quot;</span><span class="punctuation">:</span> <span class="string">&quot;Shanna@melissa.tv&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="comment">// ... other fields</span></span><br><span class="line">  <span class="punctuation">&#125;</span></span><br><span class="line"><span class="punctuation">]</span></span><br></pre></td></tr></table></figure><p><strong>步骤:</strong><br />1.添加依赖</p><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">plugins &#123;</span><br><span class="line">    id <span class="string">&#x27;com.android.application&#x27;</span></span><br><span class="line">    id <span class="string">&#x27;org.jetbrains.kotlin.android&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">android &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">dependencies</span> &#123;</span><br><span class="line">    <span class="comment">// Retrofit</span></span><br><span class="line">    implementation <span class="string">&#x27;com.squareup.retrofit2:retrofit:2.9.0&#x27;</span></span><br><span class="line">    <span class="comment">// Gson Converter (将JSON转换为Java对象)</span></span><br><span class="line">    implementation <span class="string">&#x27;com.squareup.retrofit2:converter-gson:2.9.0&#x27;</span></span><br><span class="line">    <span class="comment">// OkHttp (Retrofit底层使用的HTTP客户端)</span></span><br><span class="line">    implementation <span class="string">&#x27;com.squareup.okhttp3:okhttp:4.9.0&#x27;</span></span><br><span class="line">    implementation <span class="string">&#x27;androidx.appcompat:appcompat:1.6.1&#x27;</span></span><br><span class="line">    implementation <span class="string">&#x27;com.google.android.material:material:1.12.0&#x27;</span></span><br><span class="line">    implementation <span class="string">&#x27;androidx.constraintlayout:constraintlayout:2.1.4&#x27;</span></span><br><span class="line">    testImplementation <span class="string">&#x27;junit:junit:4.13.2&#x27;</span></span><br><span class="line">    androidTestImplementation <span class="string">&#x27;androidx.test.ext:junit:1.1.5&#x27;</span></span><br><span class="line">    androidTestImplementation <span class="string">&#x27;androidx.test.espresso:espresso-core:3.5.1&#x27;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><p>2.添加网络权限</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">manifest</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.INTERNET&quot;</span> /&gt;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><hr /><p>3.定义数据类</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">data</span> <span class="keyword">class</span> <span class="title class_">User</span>(</span><br><span class="line">    <span class="keyword">val</span> id: <span class="built_in">Int</span>,</span><br><span class="line">    <span class="keyword">val</span> name: String,</span><br><span class="line">    <span class="keyword">val</span> username: String,</span><br><span class="line">    <span class="keyword">val</span> email: String</span><br><span class="line">    <span class="comment">// ... </span></span><br><span class="line">)</span><br></pre></td></tr></table></figure><hr /><p>4.定义API接口</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">ApiService</span> &#123;</span><br><span class="line">    <span class="meta">@GET(<span class="string">&quot;users&quot;</span>)</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">getUsers</span><span class="params">()</span></span>: Call&lt;List&lt;User&gt;&gt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><p>5.创建<code>Retrofit</code>实例并执行请求</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> resultTextView: TextView</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        resultTextView = findViewById(R.id.resultTextView)</span><br><span class="line">        <span class="keyword">val</span> fetchButton: Button = findViewById(R.id.fetchButton)</span><br><span class="line"></span><br><span class="line">        fetchButton.setOnClickListener &#123;</span><br><span class="line">            fetchUsers()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">fetchUsers</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="comment">// 1. 创建 Retrofit 实例</span></span><br><span class="line">        <span class="keyword">val</span> retrofit = Retrofit.Builder()</span><br><span class="line">            .baseUrl(<span class="string">&quot;https://jsonplaceholder.typicode.com/&quot;</span>) <span class="comment">// 替换为你的服务器基础URL</span></span><br><span class="line">            .addConverterFactory(GsonConverterFactory.create()) <span class="comment">// 添加Gson转换器</span></span><br><span class="line">            .build()</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 创建 ApiService 实例</span></span><br><span class="line">        <span class="keyword">val</span> apiService = retrofit.create(ApiService::<span class="keyword">class</span>.java)</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 执行网络请求 (在非UI线程)</span></span><br><span class="line">        apiService.getUsers().enqueue(<span class="keyword">object</span> : Callback&lt;List&lt;User&gt;&gt; &#123;</span><br><span class="line">            <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onResponse</span><span class="params">(call: <span class="type">Call</span>&lt;<span class="type">List</span>&lt;<span class="type">User</span>&gt;&gt;, response: <span class="type">Response</span>&lt;<span class="type">List</span>&lt;<span class="type">User</span>&gt;&gt;)</span></span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (response.isSuccessful) &#123;</span><br><span class="line">                    <span class="keyword">val</span> users = response.body()</span><br><span class="line">                    users?.let &#123;</span><br><span class="line">                        <span class="keyword">val</span> userNames = it.joinToString(separator = <span class="string">&quot;\n&quot;</span>) &#123; user -&gt; user.name &#125;</span><br><span class="line">                        resultTextView.text = <span class="string">&quot;Fetched Users:\n<span class="variable">$userNames</span>&quot;</span></span><br><span class="line">                        Log.d(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;Users fetched successfully: <span class="variable">$userNames</span>&quot;</span>)</span><br><span class="line">                    &#125; ?: run &#123;</span><br><span class="line">                        resultTextView.text = <span class="string">&quot;No users found.&quot;</span></span><br><span class="line">                        Log.e(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;Response body is null.&quot;</span>)</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">val</span> errorBody = response.errorBody()?.string()</span><br><span class="line">                    resultTextView.text = <span class="string">&quot;Error: <span class="subst">$&#123;response.code()&#125;</span> - <span class="variable">$errorBody</span>&quot;</span></span><br><span class="line">                    Log.e(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;Error response: <span class="subst">$&#123;response.code()&#125;</span> - <span class="variable">$errorBody</span>&quot;</span>)</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onFailure</span><span class="params">(call: <span class="type">Call</span>&lt;<span class="type">List</span>&lt;<span class="type">User</span>&gt;&gt;, t: <span class="type">Throwable</span>)</span></span> &#123;</span><br><span class="line">                resultTextView.text = <span class="string">&quot;Network Error: <span class="subst">$&#123;t.message&#125;</span>&quot;</span></span><br><span class="line">                Log.e(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;Network error: <span class="subst">$&#123;t.message&#125;</span>&quot;</span>, t)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="解析"><a class="markdownIt-Anchor" href="#解析"></a> 解析:</h4><ul><li><strong>客户端</strong>：<code>MainActivity</code> 负责UI交互，并通过Retrofit库发送HTTP请求。</li><li><strong>服务端</strong>：<code>https://jsonplaceholder.typicode.com/</code> 是一个远程的Web服务，它提供了RESTful API来响应客户端的请求。</li><li><strong>交互过程</strong>：<ol><li>用户点击“Fetch Users”按钮。</li><li><code>MainActivity</code> 使用Retrofit创建一个请求，该请求对应于 <code>ApiService</code> 接口中定义的 <code>getUsers()</code> 方法。</li><li>Retrofit和OkHttp在后台执行HTTP GET请求到 <code>https://jsonplaceholder.typicode.com/users</code>。</li><li>远程服务器处理请求并返回JSON数据。</li><li>Retrofit的Gson转换器将JSON数据自动解析为 <code>List&lt;User&gt;</code> 对象。</li><li><code>onResponse</code> 回调被触发，<code>MainActivity</code> 更新UI显示获取到的用户名称。</li><li>如果发生网络错误或其他异常，<code>onFailure</code> 回调被触发。</li></ol></li></ul><hr /><h3 id="android应用内客户端与服务端交互使用messenger-ipc"><a class="markdownIt-Anchor" href="#android应用内客户端与服务端交互使用messenger-ipc"></a> Android应用内客户端与服务端交互：使用Messenger (IPC)</h3><p>Messenger提供了一种在不同进程中通过通信的方式,它基于<code>Handler</code>和<code>Message</code>.一个Service可以充当&quot;服务端&quot;,而Activity或其他组件可以充当&quot;客户端&quot;.</p><p>场景:  Activity(客户端)向Service(服务端)发送消息,Service处理消息并可选择地回复消息.</p><p><strong>步骤:</strong><br />1.定义服务端<code>Service</code></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 用于客户端发送给服务端的Message的What值</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">val</span> MSG_SAY_HELLO = <span class="number">1</span></span><br><span class="line"><span class="comment">// 用于服务端回复给客户端的Message的What值</span></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">val</span> MSG_REPLY_HELLO = <span class="number">2</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyMessengerService</span> : <span class="type">Service</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理来自客户端的消息的Handler</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">internal</span> <span class="keyword">class</span> <span class="title class_">IncomingHandler</span>(</span><br><span class="line">        context: MyMessengerService,</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">val</span> applicationContext: MyMessengerService</span><br><span class="line">    ) : Handler(Looper.getMainLooper()) &#123;</span><br><span class="line">        <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">handleMessage</span><span class="params">(msg: <span class="type">Message</span>)</span></span> &#123;</span><br><span class="line">            <span class="keyword">when</span> (msg.what) &#123;</span><br><span class="line">                MSG_SAY_HELLO -&gt; &#123;</span><br><span class="line">                    <span class="keyword">val</span> messageContent = msg.<span class="keyword">data</span>.getString(<span class="string">&quot;message&quot;</span>)</span><br><span class="line">                    <span class="keyword">val</span> replyToMessenger = msg.replyTo <span class="comment">// 获取客户端的Messenger，用于回复</span></span><br><span class="line"></span><br><span class="line">                    Log.d(<span class="string">&quot;MyMessengerService&quot;</span>, <span class="string">&quot;Received message from client: <span class="variable">$messageContent</span>&quot;</span>)</span><br><span class="line">                    Toast.makeText(applicationContext, <span class="string">&quot;Service received: <span class="variable">$messageContent</span>&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 如果客户端提供了replyTo Messenger，则回复客户端</span></span><br><span class="line">                    replyToMessenger?.let &#123;</span><br><span class="line">                        <span class="keyword">val</span> replyMessage = Message.obtain(<span class="literal">null</span>, MSG_REPLY_HELLO)</span><br><span class="line">                        <span class="keyword">val</span> bundle = Bundle()</span><br><span class="line">                        bundle.putString(<span class="string">&quot;reply&quot;</span>, <span class="string">&quot;Hello from Service! Received: <span class="variable">$messageContent</span>&quot;</span>)</span><br><span class="line">                        replyMessage.<span class="keyword">data</span> = bundle</span><br><span class="line">                        <span class="keyword">try</span> &#123;</span><br><span class="line">                            it.send(replyMessage)</span><br><span class="line">                            Log.d(<span class="string">&quot;MyMessengerService&quot;</span>, <span class="string">&quot;Sent reply to client.&quot;</span>)</span><br><span class="line">                        &#125; <span class="keyword">catch</span> (e: RemoteException) &#123;</span><br><span class="line">                            Log.e(<span class="string">&quot;MyMessengerService&quot;</span>, <span class="string">&quot;Error sending reply: <span class="subst">$&#123;e.message&#125;</span>&quot;</span>, e)</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> -&gt; <span class="keyword">super</span>.handleMessage(msg)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 服务的Messenger，用于接收来自客户端的消息</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> mMessenger: Messenger</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate()</span><br><span class="line">        <span class="comment">// 创建Handler并将它与服务的Messenger关联</span></span><br><span class="line">        mMessenger = Messenger(IncomingHandler(<span class="keyword">this</span>, <span class="keyword">this</span>))</span><br><span class="line">        Log.d(<span class="string">&quot;MyMessengerService&quot;</span>, <span class="string">&quot;Service created.&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 当客户端绑定到服务时，返回服务的IBinder对象，Messenger会包装一个Binder对象</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onBind</span><span class="params">(intent: <span class="type">Intent</span>?)</span></span>: IBinder? &#123;</span><br><span class="line">        Log.d(<span class="string">&quot;MyMessengerService&quot;</span>, <span class="string">&quot;Service bound.&quot;</span>)</span><br><span class="line">        Toast.makeText(applicationContext, <span class="string">&quot;Service bound&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">        <span class="keyword">return</span> mMessenger.binder</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onUnbind</span><span class="params">(intent: <span class="type">Intent</span>?)</span></span>: <span class="built_in">Boolean</span> &#123;</span><br><span class="line">        Log.d(<span class="string">&quot;MyMessengerService&quot;</span>, <span class="string">&quot;Service unbound.&quot;</span>)</span><br><span class="line">        Toast.makeText(applicationContext, <span class="string">&quot;Service unbound&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">super</span>.onUnbind(intent)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onDestroy()</span><br><span class="line">        Log.d(<span class="string">&quot;MyMessengerService&quot;</span>, <span class="string">&quot;Service destroyed.&quot;</span>)</span><br><span class="line">        Toast.makeText(applicationContext, <span class="string">&quot;Service destroyed&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><p>2.定义客户端<code>Activity</code></p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">var</span> mService: Messenger? = <span class="literal">null</span> <span class="comment">// 服务端的Messenger</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">var</span> mBound: <span class="built_in">Boolean</span> = <span class="literal">false</span> <span class="comment">// 是否已绑定服务</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> messageEditText: EditText</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> sendButton: Button</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> statusTextView: TextView</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> replyTextView: TextView</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 处理来自服务端的消息的Handler</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">internal</span> <span class="keyword">class</span> <span class="title class_">ReplyHandler</span>(</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">val</span> activity: MainActivity</span><br><span class="line">    ) : Handler(Looper.getMainLooper()) &#123;</span><br><span class="line">        <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">handleMessage</span><span class="params">(msg: <span class="type">Message</span>)</span></span> &#123;</span><br><span class="line">            <span class="keyword">when</span> (msg.what) &#123;</span><br><span class="line">                MSG_REPLY_HELLO -&gt; &#123;</span><br><span class="line">                    <span class="keyword">val</span> replyContent = msg.<span class="keyword">data</span>.getString(<span class="string">&quot;reply&quot;</span>)</span><br><span class="line">                    activity.replyTextView.text = <span class="string">&quot;Reply from Service: <span class="variable">$replyContent</span>&quot;</span></span><br><span class="line">                    Log.d(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;Received reply from service: <span class="variable">$replyContent</span>&quot;</span>)</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> -&gt; <span class="keyword">super</span>.handleMessage(msg)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> mClientMessenger = Messenger(ReplyHandler(<span class="keyword">this</span>)) <span class="comment">// 客户端的Messenger，用于接收服务端的回复</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 定义ServiceConnection，用于监听服务连接状态</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> mConnection = <span class="keyword">object</span> : ServiceConnection &#123;</span><br><span class="line">        <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onServiceConnected</span><span class="params">(className: <span class="type">ComponentName</span>, service: <span class="type">IBinder</span>)</span></span> &#123;</span><br><span class="line">            <span class="comment">// 当服务连接成功时调用</span></span><br><span class="line">            mService = Messenger(service) <span class="comment">// 获取服务端的Messenger</span></span><br><span class="line">            mBound = <span class="literal">true</span></span><br><span class="line">            statusTextView.text = <span class="string">&quot;Service Status: Connected&quot;</span></span><br><span class="line">            Log.d(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;Service Connected.&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onServiceDisconnected</span><span class="params">(className: <span class="type">ComponentName</span>)</span></span> &#123;</span><br><span class="line">            <span class="comment">// 当与服务的连接意外断开时调用</span></span><br><span class="line">            mService = <span class="literal">null</span></span><br><span class="line">            mBound = <span class="literal">false</span></span><br><span class="line">            statusTextView.text = <span class="string">&quot;Service Status: Disconnected&quot;</span></span><br><span class="line">            Log.d(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;Service Disconnected.&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_main)</span><br><span class="line"></span><br><span class="line">        messageEditText = findViewById(R.id.messageEditText)</span><br><span class="line">        sendButton = findViewById(R.id.sendButton)</span><br><span class="line">        statusTextView = findViewById(R.id.statusTextView)</span><br><span class="line">        replyTextView = findViewById(R.id.replyTextView)</span><br><span class="line"></span><br><span class="line">        sendButton.setOnClickListener &#123;</span><br><span class="line">            sendMessageToService()</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onStart</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onStart()</span><br><span class="line">        <span class="comment">// 绑定服务</span></span><br><span class="line">        Intent(<span class="keyword">this</span>, MyMessengerService::<span class="keyword">class</span>.java).also &#123; intent -&gt;</span><br><span class="line">            bindService(intent, mConnection, Context.BIND_AUTO_CREATE)</span><br><span class="line">        &#125;</span><br><span class="line">        statusTextView.text = <span class="string">&quot;Service Status: Binding...&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onStop</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onStop()</span><br><span class="line">        <span class="comment">// 解除绑定</span></span><br><span class="line">        <span class="keyword">if</span> (mBound) &#123;</span><br><span class="line">            unbindService(mConnection)</span><br><span class="line">            mBound = <span class="literal">false</span></span><br><span class="line">            statusTextView.text = <span class="string">&quot;Service Status: Unbound&quot;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">sendMessageToService</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (!mBound) &#123;</span><br><span class="line">            statusTextView.text = <span class="string">&quot;Service Status: Not Bound yet.&quot;</span></span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">val</span> messageContent = messageEditText.text.toString()</span><br><span class="line">        <span class="keyword">if</span> (messageContent.isBlank()) &#123;</span><br><span class="line">            statusTextView.text = <span class="string">&quot;Please enter a message.&quot;</span></span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 创建要发送给Service的Message</span></span><br><span class="line">        <span class="keyword">val</span> msg: Message = Message.obtain(<span class="literal">null</span>, MSG_SAY_HELLO).apply &#123;</span><br><span class="line">            <span class="comment">// 将客户端的Messenger添加到replyTo字段，以便Service可以回复</span></span><br><span class="line">            replyTo = mClientMessenger</span><br><span class="line">            <span class="comment">// 将数据放入Bundle</span></span><br><span class="line">            <span class="keyword">data</span> = Bundle().apply &#123;</span><br><span class="line">                putString(<span class="string">&quot;message&quot;</span>, messageContent)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            mService?.send(msg) <span class="comment">// 发送消息给Service</span></span><br><span class="line">            statusTextView.text = <span class="string">&quot;Message sent: &#x27;<span class="variable">$messageContent</span>&#x27;&quot;</span></span><br><span class="line">            Log.d(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;Message sent to service: <span class="variable">$messageContent</span>&quot;</span>)</span><br><span class="line">            messageEditText.text.clear()</span><br><span class="line">        &#125; <span class="keyword">catch</span> (e: RemoteException) &#123;</span><br><span class="line">            statusTextView.text = <span class="string">&quot;Error sending message: <span class="subst">$&#123;e.message&#125;</span>&quot;</span></span><br><span class="line">            Log.e(<span class="string">&quot;MainActivity&quot;</span>, <span class="string">&quot;Error sending message: <span class="subst">$&#123;e.message&#125;</span>&quot;</span>, e)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><p>3.在AndroidManifest.xml中声明<code>Service</code></p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">manifest</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">xmlns:tools</span>=<span class="string">&quot;http://schemas.android.com/tools&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">uses-permission</span> <span class="attr">android:name</span>=<span class="string">&quot;android.permission.INTERNET&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">application</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:allowBackup</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:dataExtractionRules</span>=<span class="string">&quot;@xml/data_extraction_rules&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:fullBackupContent</span>=<span class="string">&quot;@xml/full_backup_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:icon</span>=<span class="string">&quot;@mipmap/ic_launcher&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:label</span>=<span class="string">&quot;@string/app_name&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:roundIcon</span>=<span class="string">&quot;@mipmap/ic_launcher_round&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:supportsRtl</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:theme</span>=<span class="string">&quot;@style/Theme.MyApp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">tools:targetApi</span>=<span class="string">&quot;31&quot;</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">activity</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.MainActivity&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:exported</span>=<span class="string">&quot;true&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">action</span> <span class="attr">android:name</span>=<span class="string">&quot;android.intent.action.MAIN&quot;</span> /&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">category</span> <span class="attr">android:name</span>=<span class="string">&quot;android.intent.category.LAUNCHER&quot;</span> /&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">intent-filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">activity</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">service</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:name</span>=<span class="string">&quot;.MyMessengerService&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:enabled</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:exported</span>=<span class="string">&quot;true&quot;</span></span></span><br><span class="line"><span class="tag">            <span class="attr">android:process</span>=<span class="string">&quot;:remote&quot;</span>/&gt;</span> <span class="tag">&lt;/<span class="name">application</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">manifest</span>&gt;</span></span><br></pre></td></tr></table></figure><p><strong>注意</strong>：<code>android:process=&quot;:remote&quot;</code> 属性将 <code>MyMessengerService</code> 运行在一个独立的进程中。这使得它成为一个真正的IPC场景。如果移除这个属性，Service和Activity会运行在同一个进程，Messenger仍然可以使用，但它更多是用于线程间通信，而不是进程间通信。</p><hr /><p><strong>解析</strong>：</p><ul><li><p><strong>服务端</strong>：<code>MyMessengerService</code> 作为一个后台运行的组件，通过 <code>onBind()</code> 返回一个 <code>IBinder</code>，这个 <code>IBinder</code> 被 <code>Messenger</code> 包装，并持有一个 <code>Handler</code> (<code>IncomingHandler</code>) 来处理来自客户端的消息。</p></li><li><p><strong>客户端</strong>：<code>MainActivity</code> 绑定到 <code>MyMessengerService</code>。通过 <code>ServiceConnection</code> 的 <code>onServiceConnected()</code> 方法获取到服务端的 <code>Messenger</code> 对象 (<code>mService</code>)。它也持有一个自己的 <code>Messenger</code> (<code>mClientMessenger</code>) 来接收服务端的回复。</p></li><li><p><strong>交互过程</strong>：</p><ol><li><code>MainActivity</code> (客户端) 在 <code>onStart()</code> 中调用 <code>bindService()</code> 绑定到 <code>MyMessengerService</code>。</li><li>一旦绑定成功，<code>ServiceConnection</code> 的 <code>onServiceConnected()</code> 回调被调用，客户端获取到服务端的 <code>Messenger</code>。</li><li>用户在 <code>MainActivity</code> 中输入消息并点击发送。</li><li><code>MainActivity</code> 创建一个 <code>Message</code> 对象，将用户输入的消息放入 <code>Bundle</code>，并将客户端的 <code>Messenger</code> (用于回复) 放入 <code>msg.replyTo</code> 字段。</li><li>客户端通过 <code>mService?.send(msg)</code> 将 <code>Message</code> 发送给服务端。</li><li><code>MyMessengerService</code> (服务端) 中的 <code>IncomingHandler</code> 接收到 <code>Message</code>。</li><li><code>IncomingHandler</code> 从 <code>Message</code> 中取出数据，并使用 <code>msg.replyTo</code> (即客户端的 <code>Messenger</code>) 回复消息给客户端。</li><li><code>MainActivity</code> 中的 <code>ReplyHandler</code> 接收到服务端的回复 <code>Message</code>，并更新UI。</li><li>当 <code>MainActivity</code> 不再需要服务时 (例如 <code>onStop()</code> 中)，它调用 <code>unbindService()</code> 解除绑定，服务在没有客户端绑定时可以被销毁。</li></ol><hr /></li></ul><h2 id="总结"><a class="markdownIt-Anchor" href="#总结"></a> 总结</h2><p>Android中的“服务端与客户端”是一个<strong>广义</strong>的概念。它既可以指<strong>Android应用与远程服务器</strong>，也可以指<strong>Android应用内部不同进程组件</strong>。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;在Android应用开发中,&lt;strong&gt;客户端&lt;/strong&gt;(Client)与&lt;strong&gt;服务端&lt;/strong&gt;(Server)是时常被提及的概念,通常来说&amp;quot;客户端指的是用户直接交互的那个&lt;strong&gt;Android应用程序本身&lt;/strong&gt;,</summary>
      
    
    
    
    
    <category term="Android" scheme="https://11111-beep.github.io/tags/Android/"/>
    
  </entry>
  
  <entry>
    <title>ExecutorService介绍及其使用方法</title>
    <link href="https://11111-beep.github.io/2025/06/28/ExecutorService%E4%BB%8B%E7%BB%8D%E5%8F%8A%E5%85%B6%E4%BD%BF%E7%94%A8%E6%96%B9%E6%B3%95/"/>
    <id>https://11111-beep.github.io/2025/06/28/ExecutorService%E4%BB%8B%E7%BB%8D%E5%8F%8A%E5%85%B6%E4%BD%BF%E7%94%A8%E6%96%B9%E6%B3%95/</id>
    <published>2025-06-28T08:38:06.000Z</published>
    <updated>2025-12-14T08:54:09.993Z</updated>
    
    <content type="html"><![CDATA[<p>在 Android 开发中，我们经常需要在后台执行耗时操作，以避免阻塞主线程（UI 线程），从而导致应用无响应（ANR）。传统的做法是使用 <code>Thread</code> 类，但 <code>Thread</code> 的管理开销较大，包括线程的创建、销毁和调度。当并发任务较多时，频繁地创建和销毁线程会消耗大量的系统资源，甚至可能导致 OOM（Out Of Memory）错误。</p><p><code>ExecutorService</code> 接口（及其父接口 <code>Executor</code>）是 Java 并发包 <code>java.util.concurrent</code> 中的核心组件，它提供了一种更高级、更灵活的方式来管理和执行并发任务。它将任务的提交和任务的执行解耦，允许我们使用线程池来管理线程，从而提高应用程序的性能和稳定性。</p><h3 id="executorservice-概述"><a class="markdownIt-Anchor" href="#executorservice-概述"></a> ExecutorService 概述</h3><p><code>ExecutorService</code> 是 <code>Executor</code> 的子接口，它提供了更丰富的生命周期管理方法，例如关闭线程池、判断线程池是否已关闭等。</p><p><strong>核心思想：</strong></p><ul><li><strong>线程池（Thread Pool）</strong>：<code>ExecutorService</code> 的实现通常基于线程池。线程池预先创建了一定数量的线程，当有任务提交时，线程池中的空闲线程会去执行任务。任务执行完毕后，线程不会被销毁，而是返回线程池等待下一个任务。这样就避免了频繁创建和销毁线程的开销。</li><li><strong>任务提交与执行分离</strong>：开发者只需将任务提交给 <code>ExecutorService</code>，而无需关心任务具体由哪个线程执行、线程如何管理等底层细节。</li><li><strong>任务队列（Task Queue）</strong>：当线程池中的所有线程都在忙碌时，新提交的任务会被放入一个任务队列中等待。</li></ul><img src="/2025/06/28/ExecutorService%E4%BB%8B%E7%BB%8D%E5%8F%8A%E5%85%B6%E4%BD%BF%E7%94%A8%E6%96%B9%E6%B3%95/f1f0f438f43f4991ac0550a9b7d04e9a.png" class="" title="在这里插入图片描述"><h3 id="executorservice-的主要优点"><a class="markdownIt-Anchor" href="#executorservice-的主要优点"></a> ExecutorService 的主要优点</h3><ol><li><strong>资源管理</strong>：避免了线程频繁创建和销毁的开销，降低了系统资源消耗。</li><li><strong>提高响应速度</strong>：当任务到达时，可以直接从线程池中获取线程执行，无需等待线程创建，从而提高了响应速度。</li><li><strong>提高稳定性</strong>：限制了并发线程的数量，避免了过多的线程导致系统资源耗尽。</li><li><strong>提供丰富的功能</strong>：例如定时任务、任务取消、任务结果获取等。</li></ol><table><thead><tr><th>特性 / 工具</th><th><code>Thread</code></th><th><code>ExecutorService</code></th><th><code>HandlerThread</code></th><th><code>Coroutine</code>（协程）</th><th><code>RxJava</code></th><th><code>WorkManager</code></th></tr></thead><tbody><tr><td><strong>线程复用</strong></td><td>❌ 每次新建</td><td>✅ 线程池复用</td><td>✅ 单线程长驻</td><td>✅ 调度器复用</td><td>✅ 调度器复用</td><td>✅ 系统级复用</td></tr><tr><td><strong>是否并发处理任务</strong></td><td>❌（需手动管理）</td><td>✅ 高并发处理</td><td>❌ 顺序执行（消息队列）</td><td>✅ 支持并发/顺序</td><td>✅ 支持高并发</td><td>✅（系统调度并发）</td></tr><tr><td><strong>任务调度能力</strong></td><td>❌ 无队列</td><td>✅ 可配置队列/线程策略</td><td>✅ 消息队列</td><td>✅ <code>Dispatcher</code> 灵活切换</td><td>✅ <code>.observeOn()</code> 等灵活切换</td><td>✅ 系统控制、基于约束</td></tr><tr><td><strong>主线程交互支持</strong></td><td>❌ 需手动</td><td>❌ 需结合 Handler</td><td>✅ 内置 Handler</td><td>✅ <code>withContext(Main)</code></td><td>✅ <code>.observeOn(Main)</code></td><td>✅ 自动在主线程回调</td></tr><tr><td><strong>生命周期感知</strong></td><td>❌</td><td>❌</td><td>❌</td><td>✅ 可集成 <code>ViewModelScope</code></td><td>⚠️ 需手动处理</td><td>✅ 自动管理，生命周期无关</td></tr><tr><td><strong>适合复杂流处理</strong></td><td>❌</td><td>⚠️ 需手动拆分逻辑</td><td>❌</td><td>✅ 易组合、结构化优雅</td><td>✅ 响应式流天生适合</td><td>❌（偏系统任务，不适用于复杂流）</td></tr><tr><td><strong>取消任务机制</strong></td><td>✅ <code>interrupt()</code></td><td>✅ <code>Future.cancel()</code></td><td>⚠️ 手动标记退出</td><td>✅ <code>Job.cancel()</code></td><td>✅ <code>dispose()</code></td><td>✅ 根据约束自动取消</td></tr><tr><td><strong>适合后台持久任务</strong></td><td>❌</td><td>✅</td><td>✅</td><td>✅（结合 <code>IO Dispatcher</code>）</td><td>✅</td><td>✅ 持久运行，系统可靠调度</td></tr><tr><td><strong>Jetpack 兼容性</strong></td><td>❌</td><td>⚠️ 一般</td><td>❌</td><td>✅ 完美集成</td><td>⚠️ 可与 LiveData 配合</td><td>✅ Jetpack 官方支持</td></tr><tr><td><strong>推荐使用场景</strong></td><td>简单线程任务</td><td>高并发异步任务</td><td>轮询/消息处理类后台任务</td><td>✅ 推荐主力工具，用于几乎所有异步任务</td><td>响应式编程、复杂事件流</td><td>后台约束型任务（上传/同步等）</td></tr></tbody></table><h3 id="executorservice-的创建方法"><a class="markdownIt-Anchor" href="#executorservice-的创建方法"></a> ExecutorService 的创建方法</h3><p><code>Executors</code> 工具类提供了多种静态工厂方法来创建不同类型的 <code>ExecutorService</code>：</p><ol><li><p><strong><code>Executors.newFixedThreadPool(int nThreads)</code></strong>：</p><ul><li>创建一个固定大小的线程池。</li><li>线程池中的线程数量是固定的，如果所有线程都在忙碌，新任务会进入一个无界队列等待。</li><li>适用于已知并发任务数量，并且希望控制并发线程数量的场景。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ExecutorService</span> <span class="variable">fixedThreadPool</span> <span class="operator">=</span> Executors.newFixedThreadPool(<span class="number">5</span>); <span class="comment">// 创建一个包含5个线程的线程池</span></span><br></pre></td></tr></table></figure></li><li><p><strong><code>Executors.newCachedThreadPool()</code></strong>：</p><ul><li>创建一个可缓存的线程池。</li><li>线程池的线程数量是不固定的，当有任务提交时，如果线程池中有空闲线程，则直接使用；如果没有空闲线程，则创建一个新线程来执行任务。</li><li>空闲的线程在一段时间（通常是 60 秒）后会被回收。</li><li>适用于执行大量短期异步任务的场景，可以根据任务量自动调整线程数量。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ExecutorService</span> <span class="variable">cachedThreadPool</span> <span class="operator">=</span> Executors.newCachedThreadPool();</span><br></pre></td></tr></table></figure></li><li><p><strong><code>Executors.newSingleThreadExecutor()</code></strong>：</p><ul><li>创建一个单线程的 <code>ExecutorService</code>。</li><li>它只有一个工作线程，所有的任务都按照提交的顺序串行执行。</li><li>适用于需要保证所有任务严格顺序执行的场景。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ExecutorService</span> <span class="variable">singleThreadPool</span> <span class="operator">=</span> Executors.newSingleThreadExecutor();</span><br></pre></td></tr></table></figure></li><li><p><strong><code>Executors.newScheduledThreadPool(int corePoolSize)</code></strong>：</p><ul><li>创建一个可定时执行任务的线程池。</li><li>它支持延迟执行任务和周期性执行任务。</li><li><code>corePoolSize</code> 表示核心线程数量。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ScheduledExecutorService</span> <span class="variable">scheduledThreadPool</span> <span class="operator">=</span> Executors.newScheduledThreadPool(<span class="number">3</span>);</span><br></pre></td></tr></table></figure></li></ol><h3 id="executorservice-的使用方法"><a class="markdownIt-Anchor" href="#executorservice-的使用方法"></a> ExecutorService 的使用方法</h3><p><code>ExecutorService</code> 主要通过以下方法来提交任务：</p><ol><li><p><strong><code>execute(Runnable command)</code></strong>：</p><ul><li>提交一个不需要返回结果的任务。</li><li><code>Runnable</code> 接口的 <code>run()</code> 方法不返回任何值。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">fixedThreadPool.execute(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 后台执行耗时操作</span></span><br><span class="line">        System.out.println(<span class="string">&quot;Executing task in fixedThreadPool...&quot;</span>);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            Thread.currentThread().interrupt();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">&quot;Task in fixedThreadPool finished.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li><li><p><strong><code>submit(Callable&lt;T&gt; task)</code></strong>：</p><ul><li>提交一个需要返回结果的任务。</li><li><code>Callable</code> 接口的 <code>call()</code> 方法可以返回一个值，并且可以抛出异常。</li><li><code>submit()</code> 方法会返回一个 <code>Future</code> 对象，可以通过 <code>Future</code> 对象获取任务执行结果。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;String&gt; future = cachedThreadPool.submit(<span class="keyword">new</span> <span class="title class_">Callable</span>&lt;String&gt;() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">call</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Executing task in cachedThreadPool...&quot;</span>);</span><br><span class="line">        Thread.sleep(<span class="number">3000</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;Task result from cachedThreadPool&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在需要结果的时候获取</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">result</span> <span class="operator">=</span> future.get(); <span class="comment">// 阻塞直到任务完成并返回结果</span></span><br><span class="line">    System.out.println(result);</span><br><span class="line">&#125; <span class="keyword">catch</span> (InterruptedException | ExecutionException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong><code>submit(Runnable task, T result)</code></strong>：</p><ul><li>提交一个 <code>Runnable</code> 任务，并指定一个结果对象，当任务完成时，该结果对象会作为 <code>Future</code> 的结果返回。</li><li>这个方法通常用于 <code>Runnable</code> 任务本身没有返回值，但你仍然希望通过 <code>Future</code> 获取某个特定对象的情况。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">myResult</span> <span class="operator">=</span> <span class="string">&quot;Default Result&quot;</span>;</span><br><span class="line">Future&lt;String&gt; futureRunnable = singleThreadPool.submit(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Executing Runnable with result in singleThreadPool...&quot;</span>);</span><br><span class="line">        <span class="comment">// do something</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;, myResult);</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">finalResult</span> <span class="operator">=</span> futureRunnable.get();</span><br><span class="line">    System.out.println(<span class="string">&quot;Runnable with result finished: &quot;</span> + finalResult);</span><br><span class="line">&#125; <span class="keyword">catch</span> (InterruptedException | ExecutionException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong><code>submit(Runnable task)</code></strong>：</p><ul><li>提交一个 <code>Runnable</code> 任务。</li><li>虽然 <code>Runnable</code> 没有返回值，但 <code>submit()</code> 方法仍然会返回一个 <code>Future&lt;?&gt;</code> 对象，可以用于检查任务是否完成或取消任务。当任务成功完成时，调用 <code>Future.get()</code> 会返回 <code>null</code>。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">Future&lt;?&gt; futureVoid = fixedThreadPool.submit(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Executing void task in fixedThreadPool...&quot;</span>);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            Thread.currentThread().interrupt();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    futureVoid.get(); <span class="comment">// 阻塞直到任务完成，返回null</span></span><br><span class="line">    System.out.println(<span class="string">&quot;Void task finished.&quot;</span>);</span><br><span class="line">&#125; <span class="keyword">catch</span> (InterruptedException | ExecutionException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><h3 id="scheduledexecutorservice-的使用方法"><a class="markdownIt-Anchor" href="#scheduledexecutorservice-的使用方法"></a> ScheduledExecutorService 的使用方法</h3><p><code>ScheduledExecutorService</code> 提供了定时和周期性任务执行的能力：</p><ol><li><p><strong><code>schedule(Callable&lt;V&gt; callable, long delay, TimeUnit unit)</code></strong>：</p><ul><li>在指定的延迟时间后执行一次任务。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">scheduledThreadPool.schedule(<span class="keyword">new</span> <span class="title class_">Callable</span>&lt;String&gt;() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">call</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Scheduled task executed after 5 seconds.&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;Scheduled Task Done&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;, <span class="number">5</span>, TimeUnit.SECONDS);</span><br></pre></td></tr></table></figure></li><li><p><strong><code>schedule(Runnable command, long delay, TimeUnit unit)</code></strong>：</p><ul><li>在指定的延迟时间后执行一次任务（无返回值）。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">scheduledThreadPool.schedule(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;Another scheduled runnable executed after 3 seconds.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;, <span class="number">3</span>, TimeUnit.SECONDS);</span><br></pre></td></tr></table></figure></li><li><p><strong><code>scheduleAtFixedRate(Runnable command, long initialDelay, long period, TimeUnit unit)</code></strong>：</p><ul><li>在指定的初始延迟后开始执行任务，然后以固定的频率（<code>period</code>）周期性地执行任务。</li><li><code>period</code> 是指任务开始执行的间隔时间，<strong>不考虑任务本身的执行时间</strong>。如果任务执行时间超过 <code>period</code>，则下一个任务会立即开始执行（或者在当前任务完成后立即执行）。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">scheduledThreadPool.scheduleAtFixedRate(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">long</span> <span class="variable">now</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;Fixed rate task executed at: &quot;</span> + now);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">1500</span>); <span class="comment">// 假设任务执行1.5秒</span></span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            Thread.currentThread().interrupt();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;, <span class="number">1</span>, <span class="number">2</span>, TimeUnit.SECONDS); <span class="comment">// 首次延迟1秒，之后每2秒执行一次</span></span><br></pre></td></tr></table></figure></li><li><p><strong><code>scheduleWithFixedDelay(Runnable command, long initialDelay, long delay, TimeUnit unit)</code></strong>：</p><ul><li>在指定的初始延迟后开始执行任务，然后在上一个任务<strong>执行完成后</strong>再等待指定的 <code>delay</code> 时间后执行下一个任务。</li><li>这种方式可以保证任务之间至少有 <code>delay</code> 的间隔。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">scheduledThreadPool.scheduleWithFixedDelay(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">long</span> <span class="variable">now</span> <span class="operator">=</span> System.currentTimeMillis();</span><br><span class="line">        System.out.println(<span class="string">&quot;Fixed delay task executed at: &quot;</span> + now);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">1500</span>); <span class="comment">// 假设任务执行1.5秒</span></span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            Thread.currentThread().interrupt();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;, <span class="number">1</span>, <span class="number">2</span>, TimeUnit.SECONDS); <span class="comment">// 首次延迟1秒，之后在上一个任务完成后再等待2秒执行下一个</span></span><br></pre></td></tr></table></figure></li></ol><h3 id="executorservice-的关闭"><a class="markdownIt-Anchor" href="#executorservice-的关闭"></a> ExecutorService 的关闭</h3><p>在使用完 <code>ExecutorService</code> 后，务必将其关闭，否则线程池中的线程会一直保持活跃状态，阻止应用程序正常退出，造成资源泄露。</p><ol><li><p><strong><code>shutdown()</code></strong>：</p><ul><li>平滑地关闭 <code>ExecutorService</code>。</li><li>它不会立即停止正在执行的任务，也不会接受新任务。</li><li>已提交但尚未执行的任务会继续执行。</li><li>当所有任务执行完成后，线程池才会真正关闭。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">fixedThreadPool.shutdown();</span><br></pre></td></tr></table></figure></li><li><p><strong><code>shutdownNow()</code></strong>：</p><ul><li>尝试立即停止所有正在执行的任务。</li><li>它会中断正在执行的任务，并返回一个尚未执行的任务列表。</li><li>它不会等待任务完成。</li><li>这是一个“尽力而为”的操作，不保证所有任务都会立即停止。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Runnable&gt; unexecutedTasks = cachedThreadPool.shutdownNow();</span><br><span class="line">System.out.println(<span class="string">&quot;Unexecuted tasks: &quot;</span> + unexecutedTasks.size());</span><br></pre></td></tr></table></figure></li><li><p><strong><code>awaitTermination(long timeout, TimeUnit unit)</code></strong>：</p><ul><li>在调用 <code>shutdown()</code> 后，可以使用此方法等待线程池终止。</li><li>它会阻塞当前线程，直到所有任务执行完成，或者达到指定的超时时间，或者当前线程被中断。</li><li>通常和 <code>shutdown()</code> 配合使用，以确保在所有任务完成后再进行后续操作。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">fixedThreadPool.shutdown();</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (!fixedThreadPool.awaitTermination(<span class="number">60</span>, TimeUnit.SECONDS)) &#123;</span><br><span class="line">        <span class="comment">// 如果在指定时间内未能终止，可以再次尝试 shutdownNow() 或者记录日志</span></span><br><span class="line">        System.err.println(<span class="string">&quot;Thread pool did not terminate in the specified time.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">    Thread.currentThread().interrupt();</span><br><span class="line">    System.err.println(<span class="string">&quot;Waiting for thread pool termination was interrupted.&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><h3 id="executorservice-在-android-中的最佳实践"><a class="markdownIt-Anchor" href="#executorservice-在-android-中的最佳实践"></a> ExecutorService 在 Android 中的最佳实践</h3><h4 id="示例场景"><a class="markdownIt-Anchor" href="#示例场景"></a> <strong>示例场景</strong></h4><p>假设我们有一个 <code>TextView</code> 需要每 5 秒显示一次从“网络”或“数据库”获取的模拟数据。</p><p><strong>1. 准备你的 Android 项目</strong></p><ul><li><p><strong><code>activity_main.xml</code> (布局文件)</strong>：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">LinearLayout</span> <span class="attr">xmlns:android</span>=<span class="string">&quot;http://schemas.android.com/apk/res/android&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_width</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:layout_height</span>=<span class="string">&quot;match_parent&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:orientation</span>=<span class="string">&quot;vertical&quot;</span></span></span><br><span class="line"><span class="tag">    <span class="attr">android:gravity</span>=<span class="string">&quot;center&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">TextView</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:id</span>=<span class="string">&quot;@+id/dataTextView&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_width</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:layout_height</span>=<span class="string">&quot;wrap_content&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:text</span>=<span class="string">&quot;等待数据刷新...&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:textSize</span>=<span class="string">&quot;24sp&quot;</span></span></span><br><span class="line"><span class="tag">        <span class="attr">android:textColor</span>=<span class="string">&quot;@android:color/black&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">LinearLayout</span>&gt;</span></span><br></pre></td></tr></table></figure></li><li><p><strong><code>MyApp.java</code> (Application 类，用于管理全局的 ExecutorService)</strong>： （沿用之前我们讨论过的 Application 类，确保 ExecutorService 是单例且在应用生命周期内正确管理）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// MyApp.java (保持不变，已包含在之前的回答中)</span></span><br><span class="line"><span class="keyword">import</span> android.app.Application;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.ExecutorService;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.Executors;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.TimeUnit;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyApp</span> <span class="keyword">extends</span> <span class="title class_">Application</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">volatile</span> ExecutorService backgroundExecutor;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">CPU_COUNT</span> <span class="operator">=</span> Runtime.getRuntime().availableProcessors();</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">int</span> <span class="variable">CORE_POOL_SIZE</span> <span class="operator">=</span> CPU_COUNT * <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate();</span><br><span class="line">        <span class="comment">// 创建线程池</span></span><br><span class="line">        backgroundExecutor = Executors.newFixedThreadPool(CORE_POOL_SIZE);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> ExecutorService <span class="title function_">getBackgroundExecutor</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (backgroundExecutor == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (MyApp.class) &#123;</span><br><span class="line">                <span class="keyword">if</span> (backgroundExecutor == <span class="literal">null</span>) &#123;</span><br><span class="line">                    backgroundExecutor = Executors.newFixedThreadPool(CORE_POOL_SIZE);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> backgroundExecutor;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onTerminate</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onTerminate();</span><br><span class="line">        <span class="keyword">if</span> (backgroundExecutor != <span class="literal">null</span> &amp;&amp; !backgroundExecutor.isShutdown()) &#123;</span><br><span class="line">            backgroundExecutor.shutdown();</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (!backgroundExecutor.awaitTermination(<span class="number">60</span>, TimeUnit.SECONDS)) &#123;</span><br><span class="line">                    backgroundExecutor.shutdownNow();</span><br><span class="line">                    <span class="keyword">if</span> (!backgroundExecutor.awaitTermination(<span class="number">60</span>, TimeUnit.SECONDS)) &#123;</span><br><span class="line">                        System.err.println(<span class="string">&quot;ExecutorService did not terminate after forced shutdown.&quot;</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException ie) &#123;</span><br><span class="line">                backgroundExecutor.shutdownNow();</span><br><span class="line">                Thread.currentThread().interrupt();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><p><strong>2. <code>MainActivity.java</code> (核心逻辑)</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> android.os.Bundle;</span><br><span class="line"><span class="keyword">import</span> android.os.Handler;</span><br><span class="line"><span class="keyword">import</span> android.os.Looper;</span><br><span class="line"><span class="keyword">import</span> android.widget.TextView;</span><br><span class="line"><span class="keyword">import</span> androidx.appcompat.app.AppCompatActivity;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.text.SimpleDateFormat;</span><br><span class="line"><span class="keyword">import</span> java.util.Date;</span><br><span class="line"><span class="keyword">import</span> java.util.Locale;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.ExecutorService;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MainActivity</span> <span class="keyword">extends</span> <span class="title class_">AppCompatActivity</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> TextView dataTextView;</span><br><span class="line">    <span class="keyword">private</span> Handler uiHandler; <span class="comment">// 用于将结果发布到主线程</span></span><br><span class="line">    <span class="keyword">private</span> ExecutorService backgroundExecutor; <span class="comment">// 获取后台线程池</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">REFRESH_INTERVAL_MS</span> <span class="operator">=</span> <span class="number">5000</span>; <span class="comment">// 刷新间隔：5 秒</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 这个 Runnable 封装了“获取数据”和“调度下一次刷新”的逻辑。</span></span><br><span class="line">    <span class="comment">// 它首先将数据获取任务提交给 ExecutorService，然后等待 ExecutorService 完成并更新 UI，</span></span><br><span class="line">    <span class="comment">// 最后再通过 Handler 调度自身的下一次执行。</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> <span class="type">Runnable</span> <span class="variable">refreshDataAndScheduleNext</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">            <span class="comment">// 将数据获取任务提交给 ExecutorService</span></span><br><span class="line">            backgroundExecutor.execute(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">                <span class="meta">@Override</span></span><br><span class="line">                <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                    <span class="comment">// --- 1. 后台线程执行耗时的数据获取操作 ---</span></span><br><span class="line">                    <span class="type">String</span> <span class="variable">fetchedData</span> <span class="operator">=</span> fetchDataInBackground();</span><br><span class="line">                    System.out.println(<span class="string">&quot;后台获取数据完成: &quot;</span> + fetchedData);</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// --- 2. 通过 Handler 将结果发布到主线程更新 UI ---</span></span><br><span class="line">                    uiHandler.post(<span class="keyword">new</span> <span class="title class_">Runnable</span>() &#123;</span><br><span class="line">                        <span class="meta">@Override</span></span><br><span class="line">                        <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">run</span><span class="params">()</span> &#123;</span><br><span class="line">                            dataTextView.setText(fetchedData);</span><br><span class="line">                            System.out.println(<span class="string">&quot;UI已更新: &quot;</span> + fetchedData);</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// --- 3. 再次调度此 Runnable，实现定时循环 ---</span></span><br><span class="line">            <span class="comment">// 每次任务完成后，通过 Handler 再次 postDelayed 自身，实现周期性执行。</span></span><br><span class="line">            <span class="comment">// 注意：这里是主线程的Handler在调度下一个后台任务的启动。</span></span><br><span class="line">            uiHandler.postDelayed(<span class="built_in">this</span>, REFRESH_INTERVAL_MS);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onCreate</span><span class="params">(Bundle savedInstanceState)</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">        dataTextView = findViewById(R.id.dataTextView);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 初始化主线程 Handler</span></span><br><span class="line">        uiHandler = <span class="keyword">new</span> <span class="title class_">Handler</span>(Looper.getMainLooper());</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 获取全局的后台线程池</span></span><br><span class="line">        backgroundExecutor = MyApp.getBackgroundExecutor();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onResume</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onResume();</span><br><span class="line">        <span class="comment">// 在 Activity 可见时启动定时刷新</span></span><br><span class="line">        <span class="comment">// 第一次调度任务，之后任务会通过自身递归实现循环</span></span><br><span class="line">        uiHandler.postDelayed(refreshDataAndScheduleNext, REFRESH_INTERVAL_MS);</span><br><span class="line">        System.out.println(<span class="string">&quot;定时UI刷新已启动.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onPause</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onPause();</span><br><span class="line">        <span class="comment">// 在 Activity 不可见时停止定时刷新，避免内存泄漏和不必要的资源消耗</span></span><br><span class="line">        uiHandler.removeCallbacks(refreshDataAndScheduleNext);</span><br><span class="line">        System.out.println(<span class="string">&quot;定时UI刷新已停止.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">onDestroy</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="built_in">super</span>.onDestroy();</span><br><span class="line">        <span class="comment">// 再次确保移除所有回调，防止内存泄漏</span></span><br><span class="line">        uiHandler.removeCallbacks(refreshDataAndScheduleNext);</span><br><span class="line">        <span class="comment">// 通常不需要在这里关闭 backgroundExecutor，因为它是 Application 级别的</span></span><br><span class="line">        <span class="comment">// 它的关闭逻辑应该在 MyApp.onTerminate() 中处理</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 模拟在后台线程中执行的数据获取操作。</span></span><br><span class="line"><span class="comment">     * 这是一个耗时操作。</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 模拟的最新数据字符串</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> String <span class="title function_">fetchDataInBackground</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 模拟网络请求或数据库查询的耗时</span></span><br><span class="line">            Thread.sleep(<span class="number">2000</span>); <span class="comment">// 假设数据获取需要2秒</span></span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            Thread.currentThread().interrupt();</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;数据获取被中断&quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 生成模拟数据，例如当前时间</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;新数据: &quot;</span> + <span class="keyword">new</span> <span class="title class_">SimpleDateFormat</span>(<span class="string">&quot;HH:mm:ss&quot;</span>, Locale.getDefault()).format(<span class="keyword">new</span> <span class="title class_">Date</span>());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="工作流程解释"><a class="markdownIt-Anchor" href="#工作流程解释"></a> 工作流程解释：</h4><ol><li><p><strong><code>MyApp.java</code> (<code>ExecutorService</code> 的创建和管理)</strong>:</p><ul><li>在 <code>Application</code> 类的 <code>onCreate()</code> 中创建并初始化一个全局的 <code>ExecutorService</code>（如 <code>FixedThreadPool</code>）。这确保了整个应用只使用一个线程池实例，方便管理和资源复用。</li><li>提供一个静态方法 <code>getBackgroundExecutor()</code> 让其他组件可以获取到这个线程池实例。</li><li>在 <code>onTerminate()</code> 中，优雅地关闭 <code>ExecutorService</code>，释放所有线程资源。</li></ul></li><li><p><strong><code>MainActivity.java</code> (<code>Handler</code> 和调度逻辑)</strong>:</p><ul><li><p><strong><code>uiHandler = new Handler(Looper.getMainLooper());</code></strong>: 在 <code>onCreate()</code> 中初始化 <code>Handler</code>，并明确它关联到主线程的 Looper，这意味着通过此 <code>Handler</code> 发布的所有任务都将在主线程中执行。</p></li><li><p><strong><code>backgroundExecutor = MyApp.getBackgroundExecutor();</code></strong>: 获取全局的后台线程池实例。</p></li><li><p><code>refreshDataAndScheduleNext</code> (Runnable) :</p><ul><li><p>这是一个关键的   <code>Runnable  </code>当它被执行时，它会做两件事：</p><ol><li><strong>将数据获取任务提交到 <code>backgroundExecutor</code></strong>：<code>backgroundExecutor.execute(new Runnable() &#123; ... &#125;);</code>。这个内部的 <code>Runnable</code> 负责执行 <code>fetchDataInBackground()</code> 这个耗时操作。</li><li><strong>在数据获取完成后，通过 <code>uiHandler.post()</code> 将结果发布到主线程</strong>：<code>uiHandler.post(new Runnable() &#123; ... &#125;);</code>。这确保了 <code>TextView.setText()</code>（UI 操作）是在主线程上安全进行的。</li><li><strong>重新调度自身</strong>：<code>uiHandler.postDelayed(this, REFRESH_INTERVAL_MS);</code>。在当前循环的 UI 更新操作（或数据获取任务的提交）完成后，它会再次通过主线程的 <code>Handler</code> 调度自身，使其在 <code>REFRESH_INTERVAL_MS</code> 毫秒后再次执行，从而形成一个定时刷新的循环。</li></ol></li></ul></li><li><p><code>onResume()</code> 和 <code>onPause()</code> :</p><ul><li>在 <code>onResume()</code> 中，调用 <code>uiHandler.postDelayed(refreshDataAndScheduleNext, REFRESH_INTERVAL_MS);</code> 来启动第一次定时刷新。</li><li>在 <code>onPause()</code> 中，调用 <code>uiHandler.removeCallbacks(refreshDataAndScheduleNext);</code> 来停止定时刷新。这至关重要，它可以防止内存泄漏（因为 <code>Runnable</code> 持有对 Activity 的引用）和不必要的后台活动。</li></ul></li></ul></li></ol><p>这种组合方式实现了：</p><ul><li><strong>UI 响应性</strong>：所有耗时操作都在 <code>ExecutorService</code> 提供的后台线程中执行，主线程始终保持流畅，避免 ANR。</li><li><strong>UI 安全更新</strong>：通过 <code>Handler</code> 的 <code>post()</code> 方法，确保所有 UI 更新都回到主线程执行。</li><li><strong>资源高效利用</strong>：<code>ExecutorService</code> 通过线程池管理线程，避免了频繁创建和销毁线程的开销。</li><li><strong>定时调度</strong>：<code>Handler</code> 的 <code>postDelayed()</code> 提供了简单可靠的定时调度机制。</li><li><strong>生命周期管理</strong>：结合 Activity 的 <code>onResume()</code> 和 <code>onPause()</code> 来启动和停止刷新，避免内存泄漏和资源浪费。</li></ul><h3 id="注意事项"><a class="markdownIt-Anchor" href="#注意事项"></a> 注意事项</h3><ol><li><p><strong>合理设置线程池大小</strong><br />太小会导致任务排队，太大可能造成资源浪费甚至 OOM。</p></li><li><p><strong>记得调用 <code>shutdown()</code></strong><br />否则线程池不会自动退出，可能导致程序无法正常结束。</p></li><li><p><strong>异常处理要小心</strong><br />如果任务中抛出未捕获的异常，可能导致任务失败而无声无息。</p></li><li><p><strong>避免死锁</strong><br />不要在任务中调用 <code>future.get()</code> 而导致线程阻塞。</p></li></ol><hr /><h3 id="android-中的应用"><a class="markdownIt-Anchor" href="#android-中的应用"></a> Android 中的应用</h3><p>在 Android 开发中，<code>ExecutorService</code> 常用于：</p><ul><li>异步加载图片或数据（替代 AsyncTask）</li><li>执行数据库操作（Room、SQLite）</li><li>多文件下载、上传</li><li>定时刷新 UI 数据（结合 Handler）</li></ul><hr /><h3 id="总结"><a class="markdownIt-Anchor" href="#总结"></a> 总结</h3><p><code>ExecutorService</code> 是 Android 开发中处理并发任务的强大工具。通过合理使用线程池，可以有效管理线程资源，提高应用程序的性能、响应速度和稳定性。理解不同类型的 <code>ExecutorService</code> 及其使用场景，以及如何正确地提交、取消和关闭任务，是编写高效、健壮 Android 应用的关键。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;在 Android 开发中，我们经常需要在后台执行耗时操作，以避免阻塞主线程（UI 线程），从而导致应用无响应（ANR）。传统的做法是使用 &lt;code&gt;Thread&lt;/code&gt; 类，但 &lt;code&gt;Thread&lt;/code&gt; 的管理开销较大，包括线程的创建、销毁和调度。当</summary>
      
    
    
    
    
    <category term="Android" scheme="https://11111-beep.github.io/tags/Android/"/>
    
  </entry>
  
  <entry>
    <title>关于接口的一些使用技巧与方法</title>
    <link href="https://11111-beep.github.io/2025/05/10/%E5%85%B3%E4%BA%8E%E6%8E%A5%E5%8F%A3%E7%9A%84%E4%B8%80%E4%BA%9B%E4%BD%BF%E7%94%A8%E6%8A%80%E5%B7%A7%E4%B8%8E%E6%96%B9%E6%B3%95/"/>
    <id>https://11111-beep.github.io/2025/05/10/%E5%85%B3%E4%BA%8E%E6%8E%A5%E5%8F%A3%E7%9A%84%E4%B8%80%E4%BA%9B%E4%BD%BF%E7%94%A8%E6%8A%80%E5%B7%A7%E4%B8%8E%E6%96%B9%E6%B3%95/</id>
    <published>2025-05-10T08:34:52.000Z</published>
    <updated>2025-12-14T08:37:12.700Z</updated>
    
    <content type="html"><![CDATA[<p>作为一个 Android 开发初学者，每当别人提到“接口”这个词时，我内心总会毫不在意地想：“接口？不就是 implements 一下就完事了吗？”然而，真正开始做项目后才发现，原来接口的使用远不止定义几个方法那么简单。很多关于接口的应用技巧和设计思想，在实际开发中常常被我忽略。你是否也曾遇到过类似的情况呢？让我们一起来深入探讨一下吧！</p><h3 id="一通过接口使用实现类的对象"><a class="markdownIt-Anchor" href="#一通过接口使用实现类的对象"></a> 一.通过接口使用实现类的对象</h3><p>通过接口使用实现类的对象体现的是多态的理念,他的核心是面向接口编程.实现类必须实现接口中定义的所有抽象方法（除非该类是抽象类）.</p><ol><li><p><strong>基本语法结构</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">接口名 变量名 = <span class="keyword">new</span> 实现类构造器();</span><br><span class="line">List&lt;String&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">list.add(<span class="string">&quot;小明&quot;</span>);</span><br></pre></td></tr></table></figure><p>这行代码的意思是：</p><ul><li><strong>左边：接口 List</strong> —— 表示你“只关心行为”（比如添加、删除、查找等列表操作）.</li><li><strong>右边：实现类 ArrayList</strong> —— 提供具体的行为实现.</li><li><strong>变量名 list</strong> —— 是你手中握住的“遥控器”,通过变量名你可以使用对应的方法.</li></ul><p>这就是通过接口来使用实现类对象的经典方式.</p></li><li><p><strong>设计思想</strong></p><p><strong>编程时面向接口，而非面向实现。</strong></p><p><strong>松耦合</strong>：更容易替换实现类</p><p><strong>可扩展</strong>：可以随时替换为 <code>LinkedList</code>、<code>CopyOnWriteArrayList</code> 等实现</p><p><strong>易测试</strong>：Mock 或 stub 方便替换实现</p><p><strong>更清晰的 API 设计</strong>：只暴露该暴露的能力</p></li><li><p><strong>自定义接口</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 定义接口</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">makeSound</span><span class="params">()</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 实现类</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Dog</span> <span class="keyword">implements</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">makeSound</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;汪汪！&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 使用接口引用实现类对象</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Test</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Animal</span> <span class="variable">a</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Dog</span>();  <span class="comment">// 接口引用指向实现类</span></span><br><span class="line">        a.makeSound();         <span class="comment">// 输出：汪汪！</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>虽然我们拿的是 <code>Animal</code> 类型的遥控器，但背后控制的是 <code>Dog</code> 这台机器。</p></li></ol><hr /><h3 id="二接口作为参数或返回值"><a class="markdownIt-Anchor" href="#二接口作为参数或返回值"></a> 二.接口作为参数或返回值</h3><p>“接口作为参数或返回值”&quot;是 Android 和 Java/Kotlin 编程中非常关键的一项技能，尤其是在 <strong>回调机制、事件处理、模块解耦</strong> 等场景中应用极广。</p><h4 id="核心概念"><a class="markdownIt-Anchor" href="#核心概念"></a> 核心概念：</h4><table><thead><tr><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><strong>作为参数</strong></td><td>把某个接口实例传入函数中，用来回调或配置行为</td></tr><tr><td><strong>作为返回值</strong></td><td>函数返回一个接口实例，可以由调用者接收并使用</td></tr></tbody></table><h4 id="一-接口作为参数监听按钮点击后执行某个回调逻辑"><a class="markdownIt-Anchor" href="#一-接口作为参数监听按钮点击后执行某个回调逻辑"></a> 一、接口作为参数（监听按钮点击后执行某个回调逻辑）</h4><h5 id="定义一个接口"><a class="markdownIt-Anchor" href="#定义一个接口"></a> 定义一个接口：</h5>   <figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">OnConfirmListener</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">onConfirm</span><span class="params">()</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="函数接受这个接口作为参数"><a class="markdownIt-Anchor" href="#函数接受这个接口作为参数"></a> 函数接受这个接口作为参数：</h5>   <figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">showDialog</span><span class="params">(listener: <span class="type">OnConfirmListener</span>)</span></span> &#123;</span><br><span class="line">    <span class="comment">// 模拟用户点击确认按钮</span></span><br><span class="line">    println(<span class="string">&quot;用户点击了确认按钮&quot;</span>)</span><br><span class="line">    listener.onConfirm()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong><code>listener.onConfirm()</code></strong>: 这是关键所在。<code>showDialog</code> 函数通过其接收到的 <code>listener</code> 参数（类型是接口），调用了接口中定义的 <code>onConfirm()</code> 方法。此时，<code>showDialog</code> 函数并不知道 <code>listener</code> 参数具体是哪个类的实例，它只知道这个实例有一个 <code>onConfirm()</code> 方法可以被调用。这正是<strong>多态</strong>的体现。</p><h5 id="使用接口对象传递行为"><a class="markdownIt-Anchor" href="#使用接口对象传递行为"></a> 使用接口对象传递行为：</h5>   <figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">showDialog(<span class="keyword">object</span> : OnConfirmListener &#123;</span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onConfirm</span><span class="params">()</span></span> &#123;</span><br><span class="line">        println(<span class="string">&quot;执行确认操作，比如提交表单&quot;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p><strong><code>showDialog(...)</code></strong>: 这里调用了 <code>showDialog</code> 函数。</p><p><strong><code>object : OnConfirmListener &#123; ... &#125;</code></strong>: 这是 Kotlin 中的<strong>匿名对象 (Anonymous Object)</strong>。它是一个临时的、只使用一次的类实例，并且直接实现了 <code>OnConfirmListener</code> 接口。</p><p><strong><code>override fun onConfirm() &#123; ... &#125;</code></strong>: 在匿名对象内部，我们提供了 <code>onConfirm()</code> 方法的具体实现，也就是当确认事件发生时，我们希望执行的逻辑（例如，“执行确认操作，比如提交表单”）。</p><p><strong>目的</strong>: 通过这种方式，我们将一个“行为”（<code>onConfirm()</code> 的实现）作为参数传递给了 <code>showDialog</code> 函数。<code>showDialog</code> 在合适的时机（模拟用户点击确认后）会“回调”这个行为。</p><h5 id="kotlin-中还可以使用-lambda-简化如果是-sam-接口"><a class="markdownIt-Anchor" href="#kotlin-中还可以使用-lambda-简化如果是-sam-接口"></a> Kotlin 中还可以使用 Lambda 简化（如果是 SAM 接口）：</h5>   <figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">showDialog</span><span class="params">(onConfirm: () -&gt; <span class="type">Unit</span>)</span></span> &#123;</span><br><span class="line">    println(<span class="string">&quot;用户点击了确认按钮&quot;</span>)</span><br><span class="line">    onConfirm()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">showDialog &#123;</span><br><span class="line">    println(<span class="string">&quot;Lambda方式的确认逻辑&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong><code>fun showDialog(onConfirm: () -&gt; Unit)</code></strong>: 这是 Kotlin 的一个强大特性：如果一个接口只包含一个抽象方法（称为 <strong>SAM - Single Abstract Method 接口</strong>），Kotlin 允许你使用 <strong>Lambda 表达式</strong> 来简化它的实现。这里的 <code>onConfirm: () -&gt; Unit</code> 表示 <code>onConfirm</code> 是一个函数类型参数，它接受零个参数并返回 <code>Unit</code>（相当于 Java 中的 <code>void</code>）。</p><p><strong><code>onConfirm()</code></strong>: 调用传入的 Lambda 表达式。</p><p><strong><code>showDialog &#123; println(&quot;Lambda方式的确认逻辑&quot;) &#125;</code></strong>: 这是 Lambda 表达式的调用方式。它提供了一种更简洁、更具函数式编程风格的方式来实现回调。当函数类型参数是最后一个参数时，Kotlin 允许将 Lambda 放在圆括号外部（尾随 Lambda）。</p><hr /><h4 id="二-接口作为返回值你希望从某个类中获取策略或处理逻辑"><a class="markdownIt-Anchor" href="#二-接口作为返回值你希望从某个类中获取策略或处理逻辑"></a> 二、接口作为返回值(你希望从某个类中获取“策略”或“处理逻辑”)</h4><h5 id="定义接口"><a class="markdownIt-Anchor" href="#定义接口"></a> 定义接口：</h5>   <figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">DataProcessor</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">process</span><span class="params">(<span class="keyword">data</span>: <span class="type">String</span>)</span></span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="函数返回接口对象"><a class="markdownIt-Anchor" href="#函数返回接口对象"></a> 函数返回接口对象：</h5>   <figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fun</span> <span class="title">getProcessor</span><span class="params">()</span></span>: DataProcessor &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">object</span> : DataProcessor &#123;</span><br><span class="line">        <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">process</span><span class="params">(<span class="keyword">data</span>: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">            println(<span class="string">&quot;处理数据：<span class="variable">$data</span>&quot;</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong><code>fun getProcessor(): DataProcessor</code></strong>: 定义了一个名为 <code>getProcessor</code> 的函数，它声明返回一个 <code>DataProcessor</code> 类型的对象。</p><p><strong><code>return object : DataProcessor &#123; ... &#125;</code></strong>: 这里，<code>getProcessor</code> 函数返回了一个<strong>匿名对象</strong>，这个匿名对象实现了 <code>DataProcessor</code> 接口。</p><p><strong><code>override fun process(data: String) &#123; println(&quot;处理数据：$data&quot;) &#125;</code></strong>: 这是匿名对象内部对 <code>process</code> 方法的具体实现。这个实现就是 <code>getProcessor</code> 函数所提供的“处理逻辑”。</p><h5 id="调用方使用返回的接口对象"><a class="markdownIt-Anchor" href="#调用方使用返回的接口对象"></a> 调用方使用返回的接口对象：</h5>   <figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">val</span> processor = getProcessor()</span><br><span class="line">processor.process(<span class="string">&quot;Hello from interface!&quot;</span>)</span><br></pre></td></tr></table></figure><p><strong><code>val processor = getProcessor()</code></strong>: 调用 <code>getProcessor()</code> 函数，并将返回的 <code>DataProcessor</code> 接口对象赋值给 <code>processor</code> 变量。注意，<code>processor</code> 变量的静态类型是 <code>DataProcessor</code> 接口类型。</p><p><strong><code>processor.process(&quot;Hello from interface!&quot;)</code></strong>: 通过 <code>processor</code> 接口变量，调用了它所引用的实现类对象中的 <code>process</code> 方法。此时，执行的是 <code>getProcessor</code> 函数内部匿名对象里定义的 <code>process</code> 逻辑。</p><hr /><h3 id="三接口的默认方法与函数式接口"><a class="markdownIt-Anchor" href="#三接口的默认方法与函数式接口"></a> 三.接口的默认方法与函数式接口</h3><p>你问到了 Java 接口中两个非常现代且实用的特性：<strong>默认方法（default methods）</strong> 与 <strong>函数式接口（functional interfaces）</strong>。这两个概念如同接口的“双翼”，一个重在扩展性，一个重在简洁性，帮助接口在面向对象与函数式编程之间架起桥梁。</p><hr /><h4 id="一-接口的默认方法default-method"><a class="markdownIt-Anchor" href="#一-接口的默认方法default-method"></a> 一、接口的默认方法（Default Method）</h4><h5 id="定义"><a class="markdownIt-Anchor" href="#定义"></a> 定义</h5><p>Java 8 开始，接口中允许定义带有默认实现的方法，用 <code>default</code> 关键字修饰。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">makeSound</span><span class="params">()</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 默认方法：可以被实现类继承或重写</span></span><br><span class="line">    <span class="keyword">default</span> <span class="keyword">void</span> <span class="title function_">breathe</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;呼吸空气...&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="示例"><a class="markdownIt-Anchor" href="#示例"></a> 示例</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Dog</span> <span class="keyword">implements</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">makeSound</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;汪汪！&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Animal</span> <span class="variable">a</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Dog</span>();</span><br><span class="line">        a.makeSound();   <span class="comment">// 汪汪！</span></span><br><span class="line">        a.breathe();     <span class="comment">// 呼吸空气...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h4 id="二-函数式接口functional-interface"><a class="markdownIt-Anchor" href="#二-函数式接口functional-interface"></a> 二、函数式接口（Functional Interface）</h4><h5 id="定义-2"><a class="markdownIt-Anchor" href="#定义-2"></a> 定义</h5><p>函数式接口是<strong>只包含一个抽象方法</strong>的接口，适合用于 Lambda 表达式。</p><p>使用 <code>@FunctionalInterface</code> 注解可明确声明该意图：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">Calculator</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="title function_">calc</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span>; <span class="comment">// 只允许一个抽象方法</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">default</span> <span class="keyword">void</span> <span class="title function_">printResult</span><span class="params">(<span class="type">int</span> result)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;结果是：&quot;</span> + result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="示例使用-lambda-表达式"><a class="markdownIt-Anchor" href="#示例使用-lambda-表达式"></a> 示例：使用 Lambda 表达式</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 使用 Lambda 表达式实现函数式接口</span></span><br><span class="line">        <span class="type">Calculator</span> <span class="variable">add</span> <span class="operator">=</span> (a, b) -&gt; a + b;</span><br><span class="line">        <span class="type">Calculator</span> <span class="variable">mul</span> <span class="operator">=</span> (a, b) -&gt; a * b;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">r1</span> <span class="operator">=</span> add.calc(<span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line">        <span class="type">int</span> <span class="variable">r2</span> <span class="operator">=</span> mul.calc(<span class="number">4</span>, <span class="number">5</span>);</span><br><span class="line"></span><br><span class="line">        add.printResult(r1); <span class="comment">// 结果是：5</span></span><br><span class="line">        mul.printResult(r2); <span class="comment">// 结果是：20</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><table><thead><tr><th>特性</th><th>接口默认方法</th><th>函数式接口（Functional Interface）</th><th>接口多态（通过接口使用实现类的对象）</th><th>接口作为参数或返回值</th></tr></thead><tbody><tr><td>出现版本</td><td>Java 8</td><td>Java 8</td><td>Java 基础特性</td><td>Java 基础特性</td></tr><tr><td>方法数量</td><td>可有多个方法（含默认实现）</td><td>只能有一个抽象方法</td><td>接口中定义的所有抽象方法</td><td>接口中定义的抽象方法（可含默认方法）</td></tr><tr><td>使用关键字</td><td><code>default</code></td><td><code>@FunctionalInterface</code>（建议加）</td><td><code>interface</code>、<code>implements</code></td><td><code>interface</code> + 参数/返回类型为接口</td></tr><tr><td>可否有默认方法</td><td>✅ 可以</td><td>✅ 可以</td><td>✅ 可以（Java 8 起）</td><td>✅ 可以（Java 8 起）</td></tr><tr><td>使用优势</td><td>向后兼容，减少重复逻辑</td><td>适配 Lambda，简洁表达函数逻辑</td><td>解耦结构、支持多态、便于扩展与维护</td><td>增强灵活性、支持回调、适配不同实现类</td></tr><tr><td>使用场景</td><td>接口升级、代码复用</td><td>Lambda 表达式、方法引用、回调、流式操作等</td><td>多态调用、策略模式、面向接口设计、依赖注入等</td><td>回调机制、策略模式、工厂方法、依赖注入等</td></tr><tr><td>表现形式</td><td>在接口中用 <code>default</code> 修饰方法</td><td>用 Lambda 表达式实现：<code>(a, b) -&gt; a + b</code></td><td><code>接口名 obj = new 实现类();</code></td><td><code>void func(接口名 obj)</code> / <code>接口名 getObj()</code></td></tr></tbody></table><hr /><p>通过上面的内容，我们一起回顾了在 Android 实际项目中常见的三个接口使用方式和一些实用技巧。接口虽然简单，但灵活运用起来却大有学问。希望这些内容能在你今后的开发过程中带来启发和帮助！</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;作为一个 Android 开发初学者，每当别人提到“接口”这个词时，我内心总会毫不在意地想：“接口？不就是 implements 一下就完事了吗？”然而，真正开始做项目后才发现，原来接口的使用远不止定义几个方法那么简单。很多关于接口的应用技巧和设计思想，在实际开发中常常被我</summary>
      
    
    
    
    
    <category term="Android" scheme="https://11111-beep.github.io/tags/Android/"/>
    
  </entry>
  
  <entry>
    <title>Android中调用接口和回调的区别</title>
    <link href="https://11111-beep.github.io/2025/05/05/Android%E4%B8%AD%E8%B0%83%E7%94%A8%E6%8E%A5%E5%8F%A3%E5%92%8C%E5%9B%9E%E8%B0%83%E7%9A%84%E5%8C%BA%E5%88%AB/"/>
    <id>https://11111-beep.github.io/2025/05/05/Android%E4%B8%AD%E8%B0%83%E7%94%A8%E6%8E%A5%E5%8F%A3%E5%92%8C%E5%9B%9E%E8%B0%83%E7%9A%84%E5%8C%BA%E5%88%AB/</id>
    <published>2025-05-05T08:14:17.000Z</published>
    <updated>2025-12-14T08:31:09.858Z</updated>
    
    <content type="html"><![CDATA[<p>很多Android开发小白刚开始都不明白书上调用接口和回调的区别,简单来说“调用接口”是一个非常宽泛的术语，它可以指很多情况，而“回调”是一种<strong>特定类型的</strong>接口调用，它发生在特定的设计模式和场景下。</p><p>让我们来详细区分一下：</p><h2 id="什么是调用接口"><a class="markdownIt-Anchor" href="#什么是调用接口"></a> 什么是&quot;调用接口&quot;</h2><p>在Android开发中,&quot;调用接口&quot;通常包括<strong>两种情况</strong>:</p><ol><li><p><strong>调用某个类实现的接口方法</strong>:当一个类A实现了接口B时,你可以通过接口B的引用来调用A中实现的方法.这也体现出了面向对象里面的多态的特点.<br /><strong>例子:</strong><br /><code>List</code>是一个接口,<code>ArrayList</code>实现了该<code>Llst</code>.当你写<code>List&lt;String&gt;myList = new ArrayList&lt;&gt;()</code>;然后调用<code>myList.add(&quot;item&quot;)</code>;时,你就是在通过<code>List</code>调用<code>ArrayList</code>的<code>add</code>方法.</p></li><li><p><strong>调用某个API(Application Programming Interface)</strong>:这里的&quot;接口&quot;是指一套预定义的函数,方法或协议,允许不同软件组件之间进行交互.<br /><strong>例子</strong>:<br />调用Android SDK提供的<code>findViewById()</code>方法,调用某个Web API(<a href="http://xn--api-eo8e.example.com/users">如api.example.com/users</a>).</p></li></ol><hr /><h2 id="什么是回调"><a class="markdownIt-Anchor" href="#什么是回调"></a> 什么是&quot;回调&quot;?</h2><p>回调是一种设计模式,它基于&quot;将一个函数或对象作为参数传递给另一个函数或方法,然后在某个特定事件发生时,被调用的函数或方法再回头来调用这个参数(即回调函数或回调对象中的方法)&quot;的原理.<br /><strong>简而言之类似于C++算法里面在main()函数外面定义的全局函数,我们可以在main()函数里面需要的地方对其进行调用</strong><br />回调的<strong>核心特征</strong>是:</p><ul><li>被动调用:回调的方法不是你主动在代码流中立即调用,而是由另一个组件(通常是异步操作的执行者,事件源或第三方库)在某个特定时机(比如任务完成,事件发生)&quot;回过头来&quot;调用.</li><li>通知机制:它主要用于通知某个组件特定的状态或结果.</li><li>控制反转:你把一部分控制权交给了另一个组件,让它在适当的时候调用你的代码.</li></ul><hr /><h2 id="什么时候调用接口是回调"><a class="markdownIt-Anchor" href="#什么时候调用接口是回调"></a> 什么时候&quot;调用接口&quot;是&quot;回调&quot;</h2><p>当你通过接口引用,并且这个接口是专门设计用来<em>在异步操作完成或特定事件发生时通知你的</em>,那么这种调用就大概率是回调.<br />我列举三个回调的例子:</p><ol><li><strong>Fragment 与 Activity 通信的 <code>OnDataPassListener</code>：</strong><ul><li><code>MainActivity</code> 实现了 <code>MyFragment.OnDataPassListener</code> 接口.</li><li><code>MyFragment</code> 持有 <code>listener</code> 引用（类型是 <code>OnDataPassListener</code>）.</li><li>当 <code>MyFragment</code> 中的按钮被点击时，它调用 <code>listener.onDataPass(...)</code>.</li><li><code>MyFragment</code> 在事件发生时，通过 <code>OnDataPassListener</code> 接口“回调”了 <code>MainActivity</code> 中的 <code>onDataPass</code> 方法.</li></ul></li><li><strong>网络请求完成后的 <code>OnNetworkRequestListener</code>：</strong><ul><li>你定义了一个接口 <code>OnNetworkRequestListener</code>，其中有 <code>onSuccess</code> 和 <code>onFailure</code> 方法.</li><li>你将这个接口的实现传递给 <code>NetworkManager</code>.</li><li>当网络请求在后台线程完成（成功或失败）时，<code>NetworkManager</code> 会“回过头来”调用你实现的 <code>onSuccess</code> 或 <code>onFailure</code> 方法.</li><li>它通知你在异步操作结束后发生了什么。</li></ul></li><li><strong>UI 控件的监听器<code> Listeners</code>：</strong><ul><li><code>button.setOnClickListener &#123; ... &#125;</code>.</li><li>你传递了一个匿名函数（实现了 <code>OnClickListener</code> 接口的方法）给 <code>Button</code>.</li><li>当用户点击按钮时，<code>Button</code> 内部的代码会“回调”你提供的 <code>onClick</code> 方法.</li><li>它通知你用户发生了什么交互.</li></ul></li></ol><hr /><h2 id="回调的代码示例图片下载完成时的通知回调"><a class="markdownIt-Anchor" href="#回调的代码示例图片下载完成时的通知回调"></a> 回调的代码示例(图片下载完成时的通知回调)</h2><h3 id="1定义回调接口"><a class="markdownIt-Anchor" href="#1定义回调接口"></a> 1.定义回调接口</h3><p>首先,我们定义一个接口,用于在图片下载完成时通知我们.</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">interfce ImageDownloadListener &#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> imageUrl 下载的图片URL</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> imagePath 图片保存的本地路径</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">   <span class="function"><span class="keyword">fun</span> <span class="title">onImageDownloaded</span><span class="params">(imageUrl: <span class="type">String</span>, imagePath: <span class="type">String</span>)</span></span> <span class="comment">// 图片下载成功时调用</span></span><br><span class="line">  </span><br><span class="line">   <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> imageUrl 尝试下载的图片URL</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> errorMessage 错误信息</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">   <span class="function"><span class="keyword">fun</span> <span class="title">onDownloadFailed</span><span class="params">(imageUri: <span class="type">String</span>, errorMessage: <span class="type">String</span>)</span></span> <span class="comment">// 图片下载失败时调用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong><code>ImageDownloadListener</code></strong>：这是一个 Kotlin 接口.</li><li><strong><code>onImageDownloaded</code></strong> 和 <strong><code>onDownloadFailed</code></strong>：这是接口中定义的两个方法。任何实现这个接口的类都需要提供这两个方法的具体实现。它们是“回调”方法.</li></ul><hr /><h3 id="实现图片下载器后台任务"><a class="markdownIt-Anchor" href="#实现图片下载器后台任务"></a> 实现图片下载器(后台任务)</h3><p>接着,我们创建一个模拟的<code>ImageDownloader</code>类.他会在内部启动一个新线程来模拟耗时的下载操作,并在操作完成后通过回调通知结果.</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">ImageDownloader</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">var</span> listener: ImageDownloadListener? = <span class="literal">null</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 设置下载监听器</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> listener 实现 ImageDownloadListener 接口的对象</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">setDownloadListener</span><span class="params">(listener: <span class="type">ImageDownloadListener</span>)</span></span> &#123;</span><br><span class="line">        <span class="keyword">this</span>.listener = listener</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 模拟下载图片</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> imageUrl 要下载的图片URL</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">downloadImage</span><span class="params">(imageUrl: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 在新的线程中执行下载操作，避免阻塞主线程</span></span><br><span class="line">        Thread &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 模拟网络延迟和下载过程</span></span><br><span class="line">                Thread.sleep(<span class="number">3000</span>) <span class="comment">// 假设下载需要3秒</span></span><br><span class="line">                <span class="keyword">val</span> localPath = <span class="string">&quot;/data/data/your.package.name/files/downloaded_image.png&quot;</span> <span class="comment">// 模拟本地路径</span></span><br><span class="line"></span><br><span class="line">                <span class="comment">// 模拟下载成功或失败的随机性</span></span><br><span class="line">                <span class="keyword">val</span> isSuccess = (<span class="number">0.</span><span class="number">.1</span>).random() == <span class="number">1</span> <span class="comment">// 50%的成功率</span></span><br><span class="line"></span><br><span class="line">                <span class="comment">// 下载完成后，通过 Handler 切换回主线程，然后调用回调方法</span></span><br><span class="line">                Handler(Looper.getMainLooper()).post &#123;</span><br><span class="line">                    <span class="keyword">if</span> (isSuccess) &#123;</span><br><span class="line">                        listener?.onImageDownloaded(imageUrl, localPath)</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        listener?.onDownloadFailed(imageUrl, <span class="string">&quot;模拟：下载失败，服务器无响应&quot;</span>)</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (e: InterruptedException) &#123;</span><br><span class="line">                <span class="comment">// 线程中断异常处理</span></span><br><span class="line">                Handler(Looper.getMainLooper()).post &#123;</span><br><span class="line">                    listener?.onDownloadFailed(imageUrl, <span class="string">&quot;下载被中断: <span class="subst">$&#123;e.message&#125;</span>&quot;</span>)</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (e: Exception) &#123;</span><br><span class="line">                <span class="comment">// 其他异常处理</span></span><br><span class="line">                Handler(Looper.getMainLooper()).post &#123;</span><br><span class="line">                    listener?.onDownloadFailed(imageUrl, <span class="string">&quot;下载发生未知错误: <span class="subst">$&#123;e.message&#125;</span>&quot;</span>)</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;.start() <span class="comment">// 启动新线程</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 在不再需要时，清除监听器引用，防止内存泄漏</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">fun</span> <span class="title">clearListener</span><span class="params">()</span></span> &#123;</span><br><span class="line">        listener = <span class="literal">null</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong><code>private var listener: ImageDownloadListener? = null</code></strong>：这是下载器内部保存的一个可空引用：究竟“结果要发给谁”，就靠它。在任何时刻，下载器可以通过<code> listener</code> 调用回调方法。初始时，它为空（还没注册监听者），只有后面调用 <code>setDownloadListener(...) </code>后，才会持有一个真正实现了回调接口的对象引用.</li><li><strong><code>setDownloadListener(listener: ImageDownloadListener)</code></strong>：这个方法允许我们“注册”一个实现了 <code>ImageDownloadListener</code> 接口的对象。<code>ImageDownloader</code> 会持有这个对象的引用.</li><li><strong><code>downloadImage(imageUrl: String)</code></strong>：这个方法启动了一个新的 <code>Thread</code> 来模拟耗时的下载过程（<code>Thread.sleep(3000)</code>）.</li><li><strong><code>Handler(Looper.getMainLooper()).post &#123; ... &#125;</code></strong>：因为 <code>onImageDownloaded</code> 和 <code>onDownloadFailed</code> 方法通常会用于更新 UI，而 UI 更新必须在主线程进行。后台线程不能直接更新 UI。<code>Handler</code> 负责将 Runnable发送到主线程的消息队列中执行.</li><li><strong><code>listener?.onImageDownloaded(...)</code> 与 <code>listener?.onDownloadFailed(...)</code></strong>：在下载任务完成（或失败）后，<code>ImageDownloader</code> 通过之前设置的 <code>listener</code> 引用，<strong>回过头来调用</strong>了这些方法。这就是<strong>回调</strong>的发生点.</li></ul><hr /><h3 id="在activity中接收回调并更新ui"><a class="markdownIt-Anchor" href="#在activity中接收回调并更新ui"></a> 在Activity中接收回调并更新UI</h3><p>最后,我们在<code>MainActivity</code>中使用<code>Imageloader</code>,并实现<code>ImageDownloadListener</code>接口来接收下载结果,然后更新UI.</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MainActivity</span> : <span class="type">AppCompatActivity</span>(), ImageDownloadListener &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> downloadButton: Button</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> statusTextView: TextView</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">lateinit</span> <span class="keyword">var</span> downloadedImageView: ImageView <span class="comment">// 假设是显示图片，这里只更新文本</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">val</span> imageDownloader = ImageDownloader() <span class="comment">// 创建下载器实例</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onCreate</span><span class="params">(savedInstanceState: <span class="type">Bundle</span>?)</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onCreate(savedInstanceState)</span><br><span class="line">        setContentView(R.layout.activity_main) <span class="comment">// 你的布局文件</span></span><br><span class="line"></span><br><span class="line">        downloadButton = findViewById(R.id.downloadButton)</span><br><span class="line">        statusTextView = findViewById(R.id.statusTextView)</span><br><span class="line">        downloadedImageView = findViewById(R.id.downloadedImageView) <span class="comment">// 如果你真的要显示图片，需要图片加载库</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 关键一步：将 Activity 自己注册为 ImageDownloader 的回调监听器</span></span><br><span class="line">        imageDownloader.setDownloadListener(<span class="keyword">this</span>)</span><br><span class="line"></span><br><span class="line">        downloadButton.setOnClickListener &#123;</span><br><span class="line">            <span class="keyword">val</span> imageUrl = <span class="string">&quot;https://example.com/some_image.jpg&quot;</span> <span class="comment">// 模拟一个图片URL</span></span><br><span class="line">            statusTextView.text = <span class="string">&quot;正在下载图片：<span class="variable">$imageUrl</span> ...&quot;</span></span><br><span class="line">            downloadButton.isEnabled = <span class="literal">false</span> <span class="comment">// 下载中禁用按钮</span></span><br><span class="line">            imageDownloader.downloadImage(imageUrl) <span class="comment">// 开始下载</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// --- 实现 ImageDownloadListener 接口的回调方法 ---</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onImageDownloaded</span><span class="params">(imageUrl: <span class="type">String</span>, imagePath: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 下载成功回调：此方法在主线程执行，可以直接更新UI</span></span><br><span class="line">        statusTextView.text = <span class="string">&quot;图片下载成功！\nURL: <span class="variable">$imageUrl</span>\n路径: <span class="variable">$imagePath</span>&quot;</span></span><br><span class="line">        downloadButton.isEnabled = <span class="literal">true</span> <span class="comment">// 启用按钮</span></span><br><span class="line">        Toast.makeText(<span class="keyword">this</span>, <span class="string">&quot;图片下载成功！&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">        <span class="comment">// 实际应用中，你可能在这里加载图片到 downloadedImageView</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDownloadFailed</span><span class="params">(imageUrl: <span class="type">String</span>, errorMessage: <span class="type">String</span>)</span></span> &#123;</span><br><span class="line">        <span class="comment">// 下载失败回调：此方法也在主线程执行，可以直接更新UI</span></span><br><span class="line">        statusTextView.text = <span class="string">&quot;图片下载失败！\nURL: <span class="variable">$imageUrl</span>\n错误: <span class="variable">$errorMessage</span>&quot;</span></span><br><span class="line">        downloadButton.isEnabled = <span class="literal">true</span> <span class="comment">// 启用按钮</span></span><br><span class="line">        Toast.makeText(<span class="keyword">this</span>, <span class="string">&quot;图片下载失败！&quot;</span>, Toast.LENGTH_LONG).show()</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">fun</span> <span class="title">onDestroy</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.onDestroy()</span><br><span class="line">        <span class="comment">// 在 Activity 销毁时，清除回调引用，防止内存泄漏</span></span><br><span class="line">        imageDownloader.clearListener()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong><code>class MainActivity : AppCompatActivity(), ImageDownloadListener&#123;&#125;</code></strong>：继承回调接口,用于实现<code>onImageDownloaded</code>与<code>onDownloadFailed</code>的方法重写.</li><li><strong><code>private val imageDownloader = ImageDownloader()</code></strong>：创建了上面代码的下载器实例,使我们可以调用它的方法实现下载操作.</li><li><strong><code>imageDownloader.setDownloadListener(this))</code></strong>：将 该Activity 自己注册为 <code>ImageDownloader</code> 的回调监听器,使其监听下载过程来进行UI变化.</li></ul><hr /><h3 id="回调机制究竟如何实现信使与消息传递的秘密"><a class="markdownIt-Anchor" href="#回调机制究竟如何实现信使与消息传递的秘密"></a> 回调机制究竟如何实现？“信使”与“消息传递”的秘密</h3><p><strong>注册监听者：建立“信使”关系</strong></p><ul><li>当我们在 <code>MainActivity.onCreate()</code> 调用 <code>imageDownloader.setDownloadListener(this)</code> 时，其实是告诉下载器：“以后你要想把结果告诉我，就给我打电话（调用我的回调方法）”.</li><li>本质上，<code>listener = this</code>，<code>listener</code> 保存了一个对 Activity 的引用（前提是 Activity 实现了 <code>ImageDownloadListener</code>）。这一步就像是“邮差把地址记牢了”.</li></ul><p><strong>后台线程完成任务：执笔写信</strong></p><ul><li><p><code>downloadImage(imageUrl)</code> 会在新线程里模拟下载。当下载过程完成或因异常终止时，下载结果就像信被写好：它包含成功/失败标志、<code>imageUrl</code>、<code>localPath</code>（或错误信息）.</p></li><li><p>但是，后台线程不能直接更新 UI；如果直接在后台线程里调用 <code>listener?.onImageDownloaded(...)</code>，会抛出 “只能在主线程更新 UI” 的异常。因此，我们借助：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Handler(Looper.getMainLooper()).post &#123; … &#125;</span><br></pre></td></tr></table></figure><p>这行代码的作用是：</p><ol><li>拿到主线程的 <code>Looper</code>（消息循环）.</li><li>创建一个 <code>Handler</code>，把想要执行的“回调逻辑”封装到一个 Runnable 里，丢到主线程的消息队列.</li><li>什么时候？当主线程空闲时，就会从队列中取出这个 Runnable 执行。那时再调用 <code>listener?.onImageDownloaded(...)</code>、<code>listener?.onDownloadFailed(...)</code>，既是在主线程，就能安心更新 UI 了.</li></ol></li></ul><p><strong>回调方法真正被触发：Activity “接到信”</strong></p><ul><li>比如下载成功：<code>listener?.onImageDownloaded(imageUrl, localPath)</code> 被执行，此时 <code>listener</code> 指向 <code>MainActivity</code> 的一个实例，而且正好执行在主线程里。Android 会转身调用 <code>MainActivity.onImageDownloaded(...)</code>，然后我们在这个方法里优雅地更新 UI：<ul><li>改写状态文本 <code>statusTextView.text = &quot;...下载成功...&quot;</code></li><li>重新启用按钮，让用户开心点一下重试</li><li>弹个 Toast，提醒喜讯</li></ul></li><li>下载失败时则执行 <code>onDownloadFailed(imageUrl, errorMsg)</code>，带上了“为什么失败”的缘由，让用户不至于一脸茫然.</li></ul><p><strong>回调机制的魅力：解耦、可扩展、灵活</strong></p><ul><li><strong>解耦</strong>：<code>ImageDownloader</code> 只负责“模仿下载并返回结果”，而不关心“后续如何处理”。后续逻辑全交给实现回调接口的类.</li><li><strong>可扩展</strong>：假设你今天有多个地方都要下载图片：Activity A、Activity B、甚至一个 Service、Fragment、或者自定义组件，只要实现了 <code>ImageDownloadListener</code>，就可以随时“接收”下载结果。下载器不需要为不同场景重写，只需“注册监听者”即可.</li><li><strong>灵活</strong>：如果未来想要在下载失败时做更复杂的逻辑（比如上报日志、弹对话框、触发重试），只要在 <code>onDownloadFailed</code> 里添代码就行，不影响下载器本身。反之亦然.</li></ul><p><strong>实现过程</strong></p><ul><li><strong>定义接口</strong> → 设计“回调契约”，刻画出“成功”和“失败”两种信使内容.</li><li><strong>创建下载器</strong> → 内部保存一个接口引用（<code>listener</code>），相当于暗暗握住“回报电话”.</li><li><strong>注册监听者</strong> → 在 <code>Activity</code> 中通过 <code>imageDownloader.setDownloadListener(this)</code>，告诉下载器“以后请将结果拨打我的号码”.</li><li><strong>后台线程执笔</strong> → 模拟网络延迟、抛出异常、决定成功/失败，写好“信件”。</li><li><strong><code>Handler</code> 切回主线程</strong> → 将“回报信”放进主线程的消息队列，保证安全、合法地更新 UI。</li><li><strong>触发回调</strong> → <code>listener?.onImageDownloaded(...)</code> 或 <code>listener?.onDownloadFailed(...)</code> 调用，最终被派发到 <code>MainActivity.onImageDownloaded</code> 或 <code>onDownloadFailed</code> 中；写下“下载成功”或“下载失败”，让用户在屏幕上读到结果.</li><li><strong>生命周期清理</strong> → 当 Activity 销毁，“回报电话”要及时挂断，通过 <code>imageDownloader.clearListener()</code> 避免无效引用，防止内存泄漏.</li></ul><hr /><h2 id="总结"><a class="markdownIt-Anchor" href="#总结"></a> 总结</h2><ul><li><strong>调用接口</strong>是一个广义的概念，指执行接口中定义的方法。</li><li><strong>回调</strong>是<strong>调用接口的一种特定形式</strong>，发生在当一个组件（通常是异步操作或事件源）在某个特定时刻回过头来执行你预先提供（作为参数传递）的代码时。它强调的是<strong>事件驱动和控制反转</strong>。</li></ul><p>所以，可以说“回调是调用接口的一种应用，但调用接口不总是回调”。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;很多Android开发小白刚开始都不明白书上调用接口和回调的区别,简单来说“调用接口”是一个非常宽泛的术语，它可以指很多情况，而“回调”是一种&lt;strong&gt;特定类型的&lt;/strong&gt;接口调用，它发生在特定的设计模式和场景下。&lt;/p&gt;
&lt;p&gt;让我们来详细区分一下：&lt;/p&gt;</summary>
      
    
    
    
    
    <category term="Android" scheme="https://11111-beep.github.io/tags/Android/"/>
    
  </entry>
  
  <entry>
    <title>利用 PermissionX 库来简化权限请求</title>
    <link href="https://11111-beep.github.io/2025/04/14/%E5%88%A9%E7%94%A8-PermissionX-%E5%BA%93%E6%9D%A5%E7%AE%80%E5%8C%96%E6%9D%83%E9%99%90%E8%AF%B7%E6%B1%82/"/>
    <id>https://11111-beep.github.io/2025/04/14/%E5%88%A9%E7%94%A8-PermissionX-%E5%BA%93%E6%9D%A5%E7%AE%80%E5%8C%96%E6%9D%83%E9%99%90%E8%AF%B7%E6%B1%82/</id>
    <published>2025-04-14T08:07:05.000Z</published>
    <updated>2025-12-14T08:30:10.550Z</updated>
    
    <content type="html"><![CDATA[<h2 id="permissionx简介没维护推荐xxpermissions"><a class="markdownIt-Anchor" href="#permissionx简介没维护推荐xxpermissions"></a> PermissionX简介(没维护,推荐XXPermissions))</h2><p>PermissionX是一个开源的Android库,用于简化运行时权限的请求流程,由&lt;&lt;第一行代码&gt;&gt;作者郭霖开发,适用于Android 6.0及以上系统中的动态权限问题.</p><p>项目地址：<a href="https://github.com/guolindev/PermissionX">https://github.com/guolindev/PermissionX</a></p><hr /><p><img src="/2025/04/14/利用-PermissionX-库来简化权限请求/cd85eefb6a8d4aaf945f8fc3ac31de24.png"  width="300" /><img src="/2025/04/14/利用-PermissionX-库来简化权限请求/ca105aaf536f47b0b606c0ac66acaf6b.png"  width="300" /></p><h2 id="permissionx的主要特点"><a class="markdownIt-Anchor" href="#permissionx的主要特点"></a> PermissionX的主要特点</h2><ul><li>支持同时申请多个权限</li><li>支持权限说明弹窗（用户拒绝后解释原因）</li><li>支持跳转设置页（用户永久拒绝权限时引导跳转）</li><li>简单易用，链式调用，支持 Java 和 Kotlin</li><li>自动处理敏感权限如：定位、存储、相机、电话等</li></ul><hr /><h2 id="使用方法"><a class="markdownIt-Anchor" href="#使用方法"></a> 使用方法</h2><p>1.添加依赖</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">dependencies &#123;</span><br><span class="line">   implementation (&quot;com.guolindev.permissionx:permissionx:1.7.1&quot;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>2.初始化权限的方法</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="function"><span class="keyword">fun</span> <span class="title">requestPermissions</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="comment">// 创建一个可变的权限列表</span></span><br><span class="line">        <span class="keyword">val</span> permissions = mutableListOf(</span><br><span class="line">            Manifest.permission.CAMERA, <span class="comment">// 相机权限，用于拍照或录像</span></span><br><span class="line">            Manifest.permission.READ_EXTERNAL_STORAGE, <span class="comment">// 读取外部存储权限，用于访问设备上的文件</span></span><br><span class="line">            Manifest.permission.POST_NOTIFICATIONS, <span class="comment">// 发布通知权限，用于在 Android 13 及以上版本发送通知</span></span><br><span class="line">            Manifest.permission.SCHEDULE_EXACT_ALARM <span class="comment">// 安排精确闹钟权限，用于设置精确的闹钟或提醒</span></span><br><span class="line">        )</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 对于 Android 13 (API 33) 及以上版本，需要请求具体的图片和视频权限</span></span><br><span class="line">        <span class="keyword">if</span> (Build.VERSION.SDK_INT &gt;= Build.VERSION_CODES.TIRAMISU) &#123;</span><br><span class="line">            <span class="comment">// Android 13 引入了更细粒度的媒体权限</span></span><br><span class="line">            permissions.add(Manifest.permission.READ_MEDIA_IMAGES) <span class="comment">// 读取图片权限</span></span><br><span class="line">            permissions.add(Manifest.permission.READ_MEDIA_VIDEO) <span class="comment">// 读取视频权限</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 使用 PermissionX 库来处理权限请求</span></span><br><span class="line">        PermissionX.<span class="keyword">init</span>(<span class="keyword">this</span>) <span class="comment">// 初始化 PermissionX，传入当前的 Context</span></span><br><span class="line">            .permissions(permissions) <span class="comment">// 设置需要请求的权限列表</span></span><br><span class="line">            .onExplainRequestReason &#123; scope, deniedList -&gt;</span><br><span class="line">                <span class="comment">// 当用户拒绝了某些权限但应用需要解释原因时触发此回调</span></span><br><span class="line">                scope.showRequestReasonDialog(</span><br><span class="line">                    deniedList, <span class="comment">// 被拒绝的权限列表</span></span><br><span class="line">                    <span class="string">&quot;为了应用能够正常工作，请授予以下权限&quot;</span>, </span><br><span class="line">                    <span class="string">&quot;确定&quot;</span>, </span><br><span class="line">                    <span class="string">&quot;取消&quot;</span> </span><br><span class="line">                )</span><br><span class="line">            &#125;</span><br><span class="line">            .onForwardToSettings &#123; scope, deniedList -&gt;</span><br><span class="line">                <span class="comment">// 当用户永久拒绝了某些权限（不再询问）时触发此回调，引导用户前往设置手动授予</span></span><br><span class="line">                scope.showForwardToSettingsDialog(</span><br><span class="line">                    deniedList, <span class="comment">// 被永久拒绝的权限列表</span></span><br><span class="line">                    <span class="string">&quot;您需要在设置中手动授予以下权限&quot;</span>,</span><br><span class="line">                    <span class="string">&quot;前往设置&quot;</span>, </span><br><span class="line">                    <span class="string">&quot;取消&quot;</span> </span><br><span class="line">                )</span><br><span class="line">            &#125;</span><br><span class="line">            .request &#123; allGranted, grantedList, deniedList -&gt;</span><br><span class="line">                <span class="comment">// 权限请求结果回调</span></span><br><span class="line">                <span class="comment">// allGranted: Boolean，表示是否所有权限都已授予</span></span><br><span class="line">                <span class="comment">// grantedList: List&lt;String&gt;，已授予的权限列表</span></span><br><span class="line">                <span class="comment">// deniedList: List&lt;String&gt;，被拒绝的权限列表</span></span><br><span class="line">                <span class="keyword">if</span> (allGranted) &#123;</span><br><span class="line">                    Toast.makeText(<span class="keyword">this</span>, <span class="string">&quot;所有权限已授予&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    Toast.makeText(<span class="keyword">this</span>, <span class="string">&quot;以下权限被拒绝：<span class="variable">$deniedList</span>&quot;</span>, Toast.LENGTH_SHORT).show()</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="总结"><a class="markdownIt-Anchor" href="#总结"></a> 总结</h2><table><thead><tr><th><strong>优点</strong></th><th><strong>说明</strong></th></tr></thead><tbody><tr><td>简洁</td><td>链式调用,省去大量模版代码</td></tr><tr><td>全面</td><td>支持所有常用权限</td></tr><tr><td>易用</td><td>提供引导窗口,提升用户体验</td></tr><tr><td>跨语言</td><td>支持Java和Kotlin</td></tr></tbody></table>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;permissionx简介没维护推荐xxpermissions&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#permissionx简介没维护推荐xxpermissions&quot;&gt;&lt;/a&gt; PermissionX简介(没维护,推荐XXPer</summary>
      
    
    
    
    
    <category term="Android" scheme="https://11111-beep.github.io/tags/Android/"/>
    
  </entry>
  
  <entry>
    <title>常见cmd命令</title>
    <link href="https://11111-beep.github.io/2024/12/04/%E6%97%A0%E4%BA%86/"/>
    <id>https://11111-beep.github.io/2024/12/04/%E6%97%A0%E4%BA%86/</id>
    <published>2024-12-04T11:45:55.000Z</published>
    <updated>2025-12-14T07:27:09.672Z</updated>
    
    <content type="html"><![CDATA[<ol><li>盘符名称+冒号 ：盘符切换</li><li>dir：查看当前路径下的内容</li><li>cd目录：进入单级目录（tab可以自动补全）</li><li>cd…：回退到上一级目录</li><li>cd 目录1\目录2\ …：进入多级目录</li><li>cd \：回退到盘级目录</li><li>cls ：清屏</li><li>exit：退出命令提示符窗口cd</li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;ol&gt;
&lt;li&gt;盘符名称+冒号 ：盘符切换&lt;/li&gt;
&lt;li&gt;dir：查看当前路径下的内容&lt;/li&gt;
&lt;li&gt;cd目录：进入单级目录（tab可以自动补全）&lt;/li&gt;
&lt;li&gt;cd…：回退到上一级目录&lt;/li&gt;
&lt;li&gt;cd 目录1&#92;目录2&#92; …：进入多级目录&lt;/li&gt;
&lt;li&gt;</summary>
      
    
    
    
    
    <category term="cmd" scheme="https://11111-beep.github.io/tags/cmd/"/>
    
  </entry>
  
  <entry>
    <title>新生赛</title>
    <link href="https://11111-beep.github.io/2024/12/03/%E6%96%B0%E7%94%9F%E8%B5%9B/"/>
    <id>https://11111-beep.github.io/2024/12/03/%E6%96%B0%E7%94%9F%E8%B5%9B/</id>
    <published>2024-12-03T12:46:44.000Z</published>
    <updated>2025-12-14T07:27:34.164Z</updated>
    
    <content type="html"><![CDATA[<h3 id="新生赛打完咯21名一般般吧a出五题不过并查集没a出来将会是我一生的痛本来还有机会拼罚时冲第一的~~"><a class="markdownIt-Anchor" href="#新生赛打完咯21名一般般吧a出五题不过并查集没a出来将会是我一生的痛本来还有机会拼罚时冲第一的~~"></a> 新生赛打完咯，21名一般般吧，a出五题，不过并查集没a出来将会是我一生的痛，本来还有机会拼罚时冲第一的~~</h3><img src="/2024/12/03/%E6%96%B0%E7%94%9F%E8%B5%9B/d6a582b3536cab8dc8476110799f6ac3.png" class="" title="d6a582b3536cab8dc8476110799f6ac3"><hr /><h3 id="并查集模版题"><a class="markdownIt-Anchor" href="#并查集模版题"></a> 并查集模版题</h3><p>题目链接：<a href="https://ac.nowcoder.com/acm/contest/97783/K">K-城市连接_2024年湖南工业大学第十二届程序设计竞赛新生赛 (nowcoder.com)</a></p><p>代码：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> IOS ios::sync_with_stdio(0),cin.tie(0),cout.tie(0)</span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt;PII;</span><br><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">2e5</span><span class="number">+10</span>;</span><br><span class="line"><span class="type">int</span> fa[N];</span><br><span class="line"><span class="type">bool</span> mark[N];</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">init</span><span class="params">(<span class="type">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)fa[i]=i;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">if</span>(x==fa[x])<span class="keyword">return</span> x;</span><br><span class="line"><span class="keyword">else</span>&#123;</span><br><span class="line">fa[x]=<span class="built_in">find</span>(fa[x]);</span><br><span class="line"><span class="keyword">return</span> fa[x];</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">unionn</span><span class="params">(<span class="type">int</span> i,<span class="type">int</span> j)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="type">int</span> i_fa=<span class="built_in">find</span>(i);</span><br><span class="line"><span class="type">int</span> j_fa=<span class="built_in">find</span>(j);</span><br><span class="line">fa[i_fa]=j_fa;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">IOS;</span><br><span class="line"><span class="type">int</span> n,m,x,y,q;</span><br><span class="line">cin&gt;&gt;n&gt;&gt;q;</span><br><span class="line"><span class="built_in">init</span>(n);</span><br><span class="line"><span class="keyword">while</span>(q--)&#123;</span><br><span class="line">cin&gt;&gt;m&gt;&gt;x&gt;&gt;y;</span><br><span class="line"><span class="keyword">if</span>(m==<span class="number">1</span>)<span class="built_in">unionn</span>(x,y);</span><br><span class="line"><span class="keyword">else</span>&#123;</span><br><span class="line"><span class="keyword">if</span>(<span class="built_in">find</span>(x)==<span class="built_in">find</span>(y))cout&lt;&lt;<span class="string">&quot;Y&quot;</span>&lt;&lt;endl;</span><br><span class="line">    <span class="keyword">else</span> cout&lt;&lt;<span class="string">&quot;N&quot;</span>&lt;&lt;endl;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="不过进移动组了还是非常开心的不学算法了哈哈哈"><a class="markdownIt-Anchor" href="#不过进移动组了还是非常开心的不学算法了哈哈哈"></a> 不过进移动组了还是非常开心的，不学算法了，哈哈哈！</h2>]]></content>
    
    
      
      
    <summary type="html">&lt;h3 id=&quot;新生赛打完咯21名一般般吧a出五题不过并查集没a出来将会是我一生的痛本来还有机会拼罚时冲第一的~~&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#新生赛打完咯21名一般般吧a出五题不过并查集没a出来将会是我一生的痛本来还有机会拼罚时冲第</summary>
      
    
    
    
    
    <category term="算法" scheme="https://11111-beep.github.io/tags/%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>三面错题与反思</title>
    <link href="https://11111-beep.github.io/2024/11/11/%E4%B8%89%E9%9D%A2%E9%94%99%E9%A2%98%E4%B8%8E%E5%8F%8D%E6%80%9D/"/>
    <id>https://11111-beep.github.io/2024/11/11/%E4%B8%89%E9%9D%A2%E9%94%99%E9%A2%98%E4%B8%8E%E5%8F%8D%E6%80%9D/</id>
    <published>2024-11-11T13:33:28.000Z</published>
    <updated>2025-12-14T07:28:37.913Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1dp的运用"><a class="markdownIt-Anchor" href="#1dp的运用"></a> 1.dp的运用</h2><p>题目链接：<a href="https://ac.nowcoder.com/acm/contest/93900/E">E-四叠半的电影_2024HUT实验室三面 (nowcoder.com)</a></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> IOS ios::sync_with_stdio(0),cin.tie(0),cout.tie(0)</span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt;PII;</span><br><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">2e5</span><span class="number">+10</span>;</span><br><span class="line"><span class="type">int</span> a[N];</span><br><span class="line"><span class="type">bool</span> mark[N];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">IOS;</span><br><span class="line"><span class="type">int</span> n,k;</span><br><span class="line">cin&gt;&gt;n&gt;&gt;k;</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">1</span>;i&lt;=n;i++)cin&gt;&gt;a[i];</span><br><span class="line"><span class="type">int</span> dp[n<span class="number">+1</span>][k<span class="number">+1</span>];</span><br><span class="line"><span class="built_in">memset</span>(dp,<span class="number">0x7f</span>,<span class="built_in">sizeof</span>(dp));</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;=n;i++)dp[i][<span class="number">0</span>]=<span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">2</span>;i&lt;=n<span class="number">-1</span>;i++)&#123;</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;k;j++)&#123;</span><br><span class="line">dp[i][j]=<span class="built_in">min</span>(dp[i<span class="number">-1</span>][j],dp[i<span class="number">-2</span>][j<span class="number">-1</span>]+a[i]);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">cout&lt;&lt;dp[n<span class="number">-1</span>][k<span class="number">-1</span>];</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr /><h2 id="2滑动窗口"><a class="markdownIt-Anchor" href="#2滑动窗口"></a> 2.滑动窗口</h2><p>题目链接：<a href="https://ac.nowcoder.com/acm/contest/93900/J">J-青青子衿悠悠我心_2024HUT实验室三面 (nowcoder.com)</a></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> IOS ios::sync_with_stdio(0),cin.tie(0),cout.tie(0)</span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt;PII;</span><br><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">2e5</span><span class="number">+10</span>;</span><br><span class="line"><span class="comment">//int a[N],s[N];</span></span><br><span class="line"><span class="type">bool</span> mark[N];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">IOS;</span><br><span class="line">    <span class="type">int</span> t;</span><br><span class="line">    cin&gt;&gt;t;</span><br><span class="line">    <span class="keyword">while</span>(t--)&#123;</span><br><span class="line">        <span class="type">int</span> n,m;</span><br><span class="line">        cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line">        vector&lt;<span class="type">int</span>&gt;<span class="built_in">a</span>(n);</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;n;i++)cin&gt;&gt;a[i];</span><br><span class="line">        <span class="built_in">sort</span>(a.<span class="built_in">begin</span>(),a.<span class="built_in">end</span>());</span><br><span class="line">        <span class="type">int</span> maxx=<span class="number">-1</span>;</span><br><span class="line">        <span class="type">int</span> l=<span class="number">0</span>,sum=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> r=<span class="number">0</span>;r&lt;n;r++)&#123;</span><br><span class="line">            sum+=a[r];</span><br><span class="line">            <span class="keyword">while</span>(a[r]-a[l]&gt;<span class="number">1</span>||sum&gt;m)&#123;</span><br><span class="line">                sum-=a[l];</span><br><span class="line">                l++;</span><br><span class="line">            &#125;</span><br><span class="line">            maxx=<span class="built_in">max</span>(maxx,sum);</span><br><span class="line">        &#125;</span><br><span class="line">        cout&lt;&lt;maxx&lt;&lt;endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><hr /><h2 id="3unordered_set自动排序的set运用"><a class="markdownIt-Anchor" href="#3unordered_set自动排序的set运用"></a> 3.unordered_set(自动排序的set运用)</h2><p>题目链接：<a href="https://ac.nowcoder.com/acm/contest/93900/C">C-奶龙の考验_2024HUT实验室三面 (nowcoder.com)</a></p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> IOS ios::sync_with_stdio(0),cin.tie(0),cout.tie(0)</span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> pair&lt;<span class="type">int</span>,<span class="type">int</span>&gt;PII;</span><br><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">2e5</span><span class="number">+10</span>;</span><br><span class="line"><span class="type">bool</span> mark[N];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">IOS;</span><br><span class="line">unordered_set&lt;ll&gt;se;</span><br><span class="line">ll n,a;</span><br><span class="line"><span class="type">double</span> sum = <span class="number">0</span>;</span><br><span class="line">cin&gt;&gt;n;</span><br><span class="line"><span class="keyword">while</span>(n--)&#123;</span><br><span class="line">cin&gt;&gt;a;</span><br><span class="line">se.<span class="built_in">insert</span>(a);</span><br><span class="line">&#125;</span><br><span class="line">ll len=se.<span class="built_in">size</span>();</span><br><span class="line"><span class="keyword">while</span>(len)</span><br><span class="line">&#123;</span><br><span class="line">sum+=<span class="number">1.0</span>/len;</span><br><span class="line">len--;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%.8lf&quot;</span>,sum);</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="4反思"><a class="markdownIt-Anchor" href="#4反思"></a> 4.反思</h2><h4 id="这次比赛感觉比的不太好蛮多其实不难的题目当时并没有做出来刚开始就去磕难题去了结果到最后也没做出来-也是导致我做题做得慢罚时高的主要原因怎么说呢还得慢慢提升自己学会沉下心来稳住心态新生赛继续加油吧一定要进移动组啊"><a class="markdownIt-Anchor" href="#这次比赛感觉比的不太好蛮多其实不难的题目当时并没有做出来刚开始就去磕难题去了结果到最后也没做出来-也是导致我做题做得慢罚时高的主要原因怎么说呢还得慢慢提升自己学会沉下心来稳住心态新生赛继续加油吧一定要进移动组啊"></a> 这次比赛感觉比的不太好，蛮多其实不难的题目当时并没有做出来，刚开始就去磕难题去了，结果到最后也没做出来。也是导致我做题做得慢，罚时高的主要原因，怎么说呢，还得慢慢提升自己，学会沉下心来，稳住心态，新生赛继续加油吧！(一定要进移动组啊)</h4><hr /><img src="/2024/11/11/%E4%B8%89%E9%9D%A2%E9%94%99%E9%A2%98%E4%B8%8E%E5%8F%8D%E6%80%9D/9150B8EA3AC5C02593EE66EFCD3793C5.png" class="" title="9150B8EA3AC5C02593EE66EFCD3793C5">]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1dp的运用&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#1dp的运用&quot;&gt;&lt;/a&gt; 1.dp的运用&lt;/h2&gt;
&lt;p&gt;题目链接：&lt;a href=&quot;https://ac.nowcoder.com/acm/contest/93900/E&quot;&gt;</summary>
      
    
    
    
    
    <category term="算法" scheme="https://11111-beep.github.io/tags/%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>青媒日常(一)</title>
    <link href="https://11111-beep.github.io/2024/11/10/%E9%9D%92%E5%AA%92%E6%97%A5%E5%B8%B8-%E4%B8%80/"/>
    <id>https://11111-beep.github.io/2024/11/10/%E9%9D%92%E5%AA%92%E6%97%A5%E5%B8%B8-%E4%B8%80/</id>
    <published>2024-11-10T03:28:22.000Z</published>
    <updated>2025-12-14T07:28:19.820Z</updated>
    
    <content type="html"><![CDATA[<p>​     进入青少年媒体中心的这些天让我体验到了充实而独特的生活。作为刚进入大学的新生，我决定加入青媒，是希望能在这里学习到一些新的技能、提升自己的表达能力，同时也能更好地记录和传播校园中的故事。青媒的工作涵盖了摄影、写作和编辑等多种内容，让我能够在短时间内全面接触到媒体工作的方方面面。</p><p>​     尽管自己之前对摄影了解不多，甚至没有专业的设备，只能依靠手机拍摄，但在马学长和谭学姐的帮助下，我逐渐学到了一些拍摄的技巧。他们教会了我，摄影并不仅仅是拍下一张照片那么简单，而是一门用镜头讲故事的艺术。通过他们的讲解，我了解到手机拍摄其实也能拍出很好的效果，关键在于如何去观察、构图和捕捉光线。比如拍摄时要学会寻找自然光源，用光影来增强画面的层次感；有时还可以选择低角度拍摄，以突出主题的视觉效果。记得有一次我在活动中尝试学长学姐教的方法，最终拍出了一张自己很满意的照片。这种成就感让我对摄影产生了浓厚的兴趣，感觉手机镜头可以捕捉到很多平时不曾注意的细节。</p><p>​      除了摄影，我的主要任务是撰写推文。和拍摄一样，写推文也需要一定的技巧。推文的内容通常简洁明了，但要做到既能准确传达信息，又能吸引读者的注意力，对我来说是个不小的挑战。编辑组的前辈们建议我在写作时尽量精简文字，避免使用过于复杂的句式，同时也可以加入一些具体的场景描述来增强推文的生动性。我开始尝试将自己的情感带入到推文中，比如通过描述活动中的小细节，让读者感受到现场的氛围。这种带有温度的表达方式让我慢慢体会到，推文不仅仅是信息的传达，更是情感的传递。</p><p>​     刚开始时，我有些担心自己能否胜任这份工作，毕竟自己以前很少写这种公开的文字表达。但随着推文发布的增多，我逐渐积累了些许经验，也更加自信了。每当看到自己的推文发布出来，获得同学们的关注和评论时，我都会感到一种成就感。这让我意识到，写作并不仅仅是一个文字的堆砌过程，而是将内心想法传递给他人的桥梁。</p><p>​      在青媒的日子里，我学会了如何更好地管理时间。由于有时会同时面临多个任务，这让我开始养成合理安排工作的习惯。比如在学习之余，我会提前规划好拍摄和写作的时间，保证每一项工作都能按时完成。随着任务的增多，我的抗压能力也逐渐提高，发现自己在高强度的学习和工作环境下也能保持较好的状态。这种自律让我不仅能够高效地完成青媒的工作，同时也能很好地兼顾学业。</p><p>​      青少媒的工作让我逐渐成长，尤其是在写作和拍摄方面有了很大的提升。感谢马学长和谭学姐在摄影上的悉心指导，也感谢编辑组前辈们给我的建议。在未来的日子里，我会继续努力提升自己，用心去记录和表达。相信在青媒的成长过程中，我将会收获更多难忘的经验，成为一个更成熟的自己。</p><hr /><p>​                                                                                                                                                         -2024.11.10</p><p>​                                                                                                                                                                -何琪旺</p><h2 id="我的推文"><a class="markdownIt-Anchor" href="#我的推文"></a> 我的推文</h2><img src="/2024/11/10/%E9%9D%92%E5%AA%92%E6%97%A5%E5%B8%B8-%E4%B8%80/%E6%8E%A8%E6%96%87.jpg" class="" title="推文"><hr /><h2 id="我发的官q"><a class="markdownIt-Anchor" href="#我发的官q"></a> 我发的官Q</h2><img src="/2024/11/10/%E9%9D%92%E5%AA%92%E6%97%A5%E5%B8%B8-%E4%B8%80/%E5%AE%98Q.jpg" class="" title="官Q"><hr /><h2 id="我的摄影"><a class="markdownIt-Anchor" href="#我的摄影"></a> 我的摄影</h2><img src="/2024/11/10/%E9%9D%92%E5%AA%92%E6%97%A5%E5%B8%B8-%E4%B8%80/%E6%8B%8D%E6%91%84.jpg" class="" title="拍摄"><hr /><h2 id="推文链接"><a class="markdownIt-Anchor" href="#推文链接"></a> 推文链接</h2><p>喜欢的画家 <a href="https://c.xiumi.us/board/v5/6WKGJ/575002872?share_depth=1">https://c.xiumi.us/board/v5/6WKGJ/575002872?share_depth=1</a></p><p>上海堡垒 <a href="https://r.xiumi.us/board/v5/6WKGJ/574745999?share_depth=1">https://r.xiumi.us/board/v5/6WKGJ/574745999?share_depth=1</a></p><p>军训 <a href="https://b.xiumi.us/board/v5/6WKGJ/575526936?share_depth=2">https://b.xiumi.us/board/v5/6WKGJ/575526936?share_depth=2</a></p><p>青春三下乡 <a href="https://b.xiumi.us/board/v5/6WKGJ/575344046?share_depth=1">https://b.xiumi.us/board/v5/6WKGJ/575344046?share_depth=1</a></p><p>喜欢的歌 <a href="https://a.xiumi.us/board/v5/6WKGJ/574466645?share_depth=1">https://a.xiumi.us/board/v5/6WKGJ/574466645?share_depth=1</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;​     进入青少年媒体中心的这些天让我体验到了充实而独特的生活。作为刚进入大学的新生，我决定加入青媒，是希望能在这里学习到一些新的技能、提升自己的表达能力，同时也能更好地记录和传播校园中的故事。青媒的工作涵盖了摄影、写作和编辑等多种内容，让我能够在短时间内全面接触到媒体</summary>
      
    
    
    
    
    <category term="青媒" scheme="https://11111-beep.github.io/tags/%E9%9D%92%E5%AA%92/"/>
    
  </entry>
  
  <entry>
    <title>帅照</title>
    <link href="https://11111-beep.github.io/2024/11/04/%E5%B8%85%E7%85%A7/"/>
    <id>https://11111-beep.github.io/2024/11/04/%E5%B8%85%E7%85%A7/</id>
    <published>2024-11-04T02:04:18.000Z</published>
    <updated>2025-12-14T07:28:32.725Z</updated>
    
    <content type="html"><![CDATA[<img src="/2024/11/04/%E5%B8%85%E7%85%A7/%E6%9C%AC%E4%BA%BA%E4%B8%8B%E8%BE%88%E5%AD%90%E9%95%BF%E8%BF%99%E6%A0%B7.jpg" class="" title="本人下辈子长这样"><h2 id="我下辈子长这样"><a class="markdownIt-Anchor" href="#我下辈子长这样"></a> 我下辈子长这样</h2><hr /><img src="/2024/11/04/%E5%B8%85%E7%85%A7/%E5%84%BF%E5%AD%90.jpg" class="" title="儿子"><h2 id="儿子"><a class="markdownIt-Anchor" href="#儿子"></a> 儿子</h2><hr /><img src="/2024/11/04/%E5%B8%85%E7%85%A7/%E8%8D%89%E5%8C%85.jpg" class="" title="草包"><h2 id="草包"><a class="markdownIt-Anchor" href="#草包"></a> 草包</h2><hr /><img src="/2024/11/04/%E5%B8%85%E7%85%A7/%E9%BE%99%E5%9D%A4.jpg" class="" title="龙坤"><h2 id="龙坤"><a class="markdownIt-Anchor" href="#龙坤"></a> 龙坤</h2><hr /><img src="/2024/11/04/%E5%B8%85%E7%85%A7/%E5%BD%AD%E5%B0%91.jpg" class="" title="彭少"><h2 id="彭少"><a class="markdownIt-Anchor" href="#彭少"></a> 彭少</h2><hr /><img src="/2024/11/04/%E5%B8%85%E7%85%A7/%E5%B7%A6%E7%A5%9E.jpg" class="" title="左神"><h2 id="古希腊掌管c算法的神左鸿中"><a class="markdownIt-Anchor" href="#古希腊掌管c算法的神左鸿中"></a> 古希腊掌管c++算法的神——左鸿中</h2>]]></content>
    
    
      
      
    <summary type="html">&lt;img src=&quot;/2024/11/04/%E5%B8%85%E7%85%A7/%E6%9C%AC%E4%BA%BA%E4%B8%8B%E8%BE%88%E5%AD%90%E9%95%BF%E8%BF%99%E6%A0%B7.jpg&quot; class=&quot;&quot; title=&quot;本人下辈子</summary>
      
    
    
    
    
    <category term="帅照" scheme="https://11111-beep.github.io/tags/%E5%B8%85%E7%85%A7/"/>
    
  </entry>
  
  <entry>
    <title>军训场V题-双指针（Codeforces Round</title>
    <link href="https://11111-beep.github.io/2024/11/04/%E5%86%9B%E8%AE%AD%E5%9C%BAV%E9%A2%98-%E5%8F%8C%E6%8C%87%E9%92%88%EF%BC%88Codeforces-Round-898-Div-4-F-Money-Trees-%E9%A2%98%E8%A7%A3%EF%BC%89/"/>
    <id>https://11111-beep.github.io/2024/11/04/%E5%86%9B%E8%AE%AD%E5%9C%BAV%E9%A2%98-%E5%8F%8C%E6%8C%87%E9%92%88%EF%BC%88Codeforces-Round-898-Div-4-F-Money-Trees-%E9%A2%98%E8%A7%A3%EF%BC%89/</id>
    <published>2024-11-04T00:37:50.000Z</published>
    <updated>2025-12-14T08:26:15.993Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1自我介绍"><a class="markdownIt-Anchor" href="#1自我介绍"></a> 1.自我介绍</h2><p>作为一个刚敲代码没有多久的的<mark>小蒟蒻</mark>，也是第一次发博客😂,有一些没说好的地方希望各位大佬谅解，如果存在错误的地方也希望大家可以指出，那么话不多说，<mark>action!</mark>🌟</p><hr /><h2 id="2题目"><a class="markdownIt-Anchor" href="#2题目"></a> 2.题目</h2><p><a href="https://vjudge.net/contest/655848#problem/V">军训训练场 - Virtual Judge </a><br /><a href="https://codeforces.com/contest/1873/problem/F">原题目</a></p><img src="/2024/11/04/%E5%86%9B%E8%AE%AD%E5%9C%BAV%E9%A2%98-%E5%8F%8C%E6%8C%87%E9%92%88%EF%BC%88Codeforces-Round-898-Div-4-F-Money-Trees-%E9%A2%98%E8%A7%A3%EF%BC%89/1%20.png" class=""><img src="/2024/11/04/%E5%86%9B%E8%AE%AD%E5%9C%BAV%E9%A2%98-%E5%8F%8C%E6%8C%87%E9%92%88%EF%BC%88Codeforces-Round-898-Div-4-F-Money-Trees-%E9%A2%98%E8%A7%A3%EF%BC%89/2.png" class=""><h2 id="3分析题目"><a class="markdownIt-Anchor" href="#3分析题目"></a> 3.分析题目</h2><p>根据观察题目我们不难发现Luca所能得到的子数组的数得满足<mark>两个条件</mark>：</p><p>1.高度h[i]能被下一个高度h[i]整除（最后一个数因为后面没有数了，得舍弃）。</p><p>2.子数组里的所有a[i]相加不能超过k。</p><p>这思路不就来了吗，对数组里的数进行两个条件的判断，如果满足条件子数组个数每次加一，否则子数组个数固定，下一个数返回循环重新判断，直接暴力做。(<mark>代码乱写的，但大概意思就是这样</mark>)</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span>(h[j]%h[j<span class="number">+1</span>]==<span class="number">0</span>)&#123; <span class="comment">//第一个条件</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> k=i;k&lt;=j;k++)&#123; <span class="comment">//i为子数组的起始下标</span></span><br><span class="line">sum+=a[k];</span><br><span class="line"><span class="keyword">if</span>(sum&lt;=m)&#123; <span class="comment">//第二个条件</span></span><br><span class="line">cnt++; <span class="comment">//条件满足后,子数组个数加一</span></span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">         b[l]=cnt; <span class="comment">//建立一个数组存储每一个子数组的个数</span></span><br><span class="line">         l++;</span><br><span class="line">         cnt=<span class="number">0</span>; <span class="comment">//不要忘记个数清零</span></span><br><span class="line">      &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当然我们写起来肯定比想起来要难，反正我是没暴力出来😹,就算暴力出来也可能会超时，哎，只能去请教<mark>圈圈学长</mark>了！</p><hr /><h2 id="4题目方法-双指针"><a class="markdownIt-Anchor" href="#4题目方法-双指针"></a> 4.题目方法-双指针</h2><p>其实我们思考一下这题就类似于一种滑动窗口，就像一根辣条，我们每次切中间的一段吃，看那一段切的最长，我们要求的就是那一段的长度，这就需要动态处理区间-<mark>双指针</mark>！</p><img src="/2024/11/04/%E5%86%9B%E8%AE%AD%E5%9C%BAV%E9%A2%98-%E5%8F%8C%E6%8C%87%E9%92%88%EF%BC%88Codeforces-Round-898-Div-4-F-Money-Trees-%E9%A2%98%E8%A7%A3%EF%BC%89/3.jpeg" class=""><h3 id="双指针算法"><a class="markdownIt-Anchor" href="#双指针算法"></a> 双指针算法</h3><p>双指针算法是一种常用的算法技巧，主要用于处理数组和链表等线性结构。它通过使用两个指针（索引位置）来遍历数据结构，从而达到减少时间复杂度的目的。双指针算法有多种变体，包括快慢指针、对撞指针和滑动窗口等。</p><img src="/2024/11/04/%E5%86%9B%E8%AE%AD%E5%9C%BAV%E9%A2%98-%E5%8F%8C%E6%8C%87%E9%92%88%EF%BC%88Codeforces-Round-898-Div-4-F-Money-Trees-%E9%A2%98%E8%A7%A3%EF%BC%89/4.png" class=""><p>下面是我从网上找的一段与我们题目类似<mark>快慢双指针</mark>的题目与代码：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string a; <span class="comment">//定义一个字符串a</span></span><br><span class="line">    <span class="built_in">getline</span>(cin,a); <span class="comment">//忽略空格读入所有字符</span></span><br><span class="line">    <span class="type">int</span> n=a.<span class="built_in">size</span>(); <span class="comment">//n为a字符串的长度</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;n i++) <span class="comment">//字符串下标都是从0开始</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> j=i; <span class="comment">//j为快字符，i为慢字符</span></span><br><span class="line">        <span class="keyword">while</span>(a[j]!=<span class="string">&#x27; &#x27;</span>) j++; <span class="comment">//如果没读到空格，j继续向前移动</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> k=i; k&lt;j;k++) cout&lt;&lt;a[k]; <span class="comment">//把i到j中间的字符输出</span></span><br><span class="line">        cout&lt;&lt;endl; <span class="comment">//记得换行</span></span><br><span class="line">        i=j; <span class="comment">//循环体执行完后for()中的i才i++即，下一次开始时i就到了上一次空格(位置j)的下一位了 </span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>我尽力了，如果大家还没搞懂的话可以<a href="https://blog.csdn.net/qq_54773252/article/details/122836179?ops_request_misc=%7B%22request%5Fid%22%3A%228E6F2263-C83C-4AD8-BF36-F2EB9BF139D3%22%2C%22scm%22%3A%2220140713.130102334..%22%7D&amp;request_id=8E6F2263-C83C-4AD8-BF36-F2EB9BF139D3&amp;biz_id=0&amp;utm_medium=distribute.pc_search_result.none-task-blog-2~all~top_positive~default-1-122836179-null-null.142%5Ev100%5Econtrol&amp;utm_term=%E5%8F%8C%E6%8C%87%E9%92%88&amp;spm=1018.2226.3001.4187">双指针算法-CSDN博客</a>看看。</p><hr /><h2 id="5题目讲解"><a class="markdownIt-Anchor" href="#5题目讲解"></a> 5.题目讲解</h2><p>从题目着手，我们一个一个条件来。</p><p><mark>1.高度h[i]能被下一个高度h[i]整除（最后一个数因为后面没有数了，得舍弃）。</mark></p><p><mark>2.子数组里的所有a[i]相加不能超过k。</mark></p><p>首先我们先判断第一个条件。</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span>(j<span class="number">+1</span>&lt;n&amp;&amp;h[j]%h[j<span class="number">+1</span>]==<span class="number">0</span>) j++;</span><br></pre></td></tr></table></figure><p>我们可以用一个while循环判断数组有多少满足第一个条件的数，j为满足第一个条件的数的个数，至于j+1&lt;n是因为数组的长度是n并且数组最后一个数我们得舍弃。</p><hr /><p>其次，我们我们继续从j个数里面筛选出满足条件二的数的个数</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> sum=<span class="number">0</span>; <span class="comment">//sum为总数的大小</span></span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> k=i,last=i;k&lt;=j;k++) <span class="comment">//k为快指针下标，last为慢指针下标，i为起始下标（初始值），j为终止下标（结束值）</span></span><br><span class="line">&#123; </span><br><span class="line">sum+=a[k];</span><br><span class="line"><span class="keyword">while</span>(sum&gt;m) <span class="comment">//如果我们加的数字超过了k值(我定义的m是题目中的k值)</span></span><br><span class="line">&#123; </span><br><span class="line">sum-=a[last]; <span class="comment">//sum每次减去慢指针下标的数，直到总数小于k值为止</span></span><br><span class="line">last++; <span class="comment">//快指针向右移动，缩小慢指针与快指针的距离</span></span><br><span class="line">   &#125;</span><br><span class="line">ans=<span class="built_in">max</span>(ans,k-last<span class="number">+1</span>); <span class="comment">//ans每次循环更新为最长子数组的长度,k-last+1就是两个指针之间的距离</span></span><br><span class="line">&#125;</span><br><span class="line">i=j; <span class="comment">//因为前面的数已经判断过了，下次指针从j后面一个数开始判断</span></span><br></pre></td></tr></table></figure><p><mark>我从示例一中的第一个样例分析一下</mark></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">t:5 m:12</span><br><span class="line">a:3 2 4 1 8//注意看这一组</span><br><span class="line">h:4 4 2 4 1 </span><br></pre></td></tr></table></figure><hr /><p>我们最开始让i为0(第一个数4的下标为0，第二个数4为1，第三个数2为2，以此类推)，j为0（从0开始计数），前面的<mark>4,4,2</mark>是满足条件一的，所以j=3.</p><p>1.第一次循环sum+a[0]=3,不大于m，进入下一个循环。</p><p>快指针 <u>3</u> 2 4</p><p>慢指针 <u>3</u> 2 4</p><p>2.第二次循环sum+a[1]=5,不大于m，进入下一个循环。</p><p>快指针 3 <u>2</u> 4</p><p>慢指针 <u>3</u> 2 4</p><p>3.第三次循环sum+a[2]=9,不大于m,结束循环。</p><p>快指针 3 2 <u>4</u></p><p>慢指针 <u>3</u> 2 4</p><p><mark>ans=2-0+1=3</mark></p><p>所以输出为3。</p><p><mark>示例一中的第二个样例也差不多</mark></p><p><mark>注意</mark>：但如果进入了while循环，慢指针就向右移动了，反正退出while循环后也是使其满足条件二，再进入max函数。</p><hr /><h2 id="6ac代码"><a class="markdownIt-Anchor" href="#6ac代码"></a> 6.AC代码</h2><p>累死我了，给大家看看我的AC代码😂</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> IOS ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)</span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">typedef</span> <span class="type">long</span> <span class="type">long</span> ll;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> N=<span class="number">2e5</span><span class="number">+10</span>;</span><br><span class="line"><span class="type">int</span> a[N],h[N];</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    IOS;</span><br><span class="line"><span class="type">int</span> t,n,m;</span><br><span class="line">cin&gt;&gt;t;</span><br><span class="line"><span class="keyword">while</span>(t--)&#123;</span><br><span class="line">cin&gt;&gt;n&gt;&gt;m;</span><br><span class="line"><span class="type">int</span> ans=<span class="number">0</span>,cnt=<span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;n;i++)&#123;</span><br><span class="line">cin&gt;&gt;a[i];</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">0</span>;j&lt;n;j++)&#123;</span><br><span class="line">cin&gt;&gt;h[j];</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;n;i++)&#123;</span><br><span class="line"><span class="type">int</span> j=i;</span><br><span class="line"><span class="keyword">while</span>(j<span class="number">+1</span>&lt;n&amp;&amp;h[j]%h[j<span class="number">+1</span>]==<span class="number">0</span>)j++;</span><br><span class="line"><span class="type">int</span> sum=<span class="number">0</span>;</span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> k=i,last=i;k&lt;=j;k++)&#123;</span><br><span class="line">sum+=a[k];</span><br><span class="line"><span class="keyword">while</span>(sum&gt;m)&#123;</span><br><span class="line">sum-=a[last];</span><br><span class="line">last++;</span><br><span class="line">&#125;</span><br><span class="line">ans=<span class="built_in">max</span>(ans,k-last<span class="number">+1</span>);</span><br><span class="line">&#125;</span><br><span class="line">i=j;</span><br><span class="line">&#125;</span><br><span class="line">cout&lt;&lt;ans&lt;&lt;endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>哎，大差不差吧。</p><p>其实敲代码真的很累，但需要大家一起坚持，在此我很感谢圈圈学长，苏希无学姐，肖明阳学长等人长期以来给予我的帮助，让我可以坚持下来，最后☆*: .｡. o(≧▽≦)o .｡.:*☆😘</p><p>​</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1自我介绍&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#1自我介绍&quot;&gt;&lt;/a&gt; 1.自我介绍&lt;/h2&gt;
&lt;p&gt;作为一个刚敲代码没有多久的的&lt;mark&gt;小蒟蒻&lt;/mark&gt;，也是第一次发博客😂,有一些没说好的地方希望各位大佬谅解，如果</summary>
      
    
    
    
    
    <category term="算法" scheme="https://11111-beep.github.io/tags/%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>Markdown基本语法</title>
    <link href="https://11111-beep.github.io/2024/11/03/Markdown%E7%AE%80%E8%A6%81%E8%AF%AD%E6%B3%95/"/>
    <id>https://11111-beep.github.io/2024/11/03/Markdown%E7%AE%80%E8%A6%81%E8%AF%AD%E6%B3%95/</id>
    <published>2024-11-03T15:49:18.000Z</published>
    <updated>2025-12-14T07:28:11.819Z</updated>
    
    <content type="html"><![CDATA[<h1 id="markdown基本语法"><a class="markdownIt-Anchor" href="#markdown基本语法"></a> Markdown基本语法</h1><h2 id="标题"><a class="markdownIt-Anchor" href="#标题"></a> 标题</h2><p>## 二级标题</p><p>### 三级标题</p><p>#### 四级标题</p><p>##### 五级标题</p><p>###### 六级标题</p><h2 id="引用"><a class="markdownIt-Anchor" href="#引用"></a> 引用</h2><p>&gt; 这是一段引用</p><p>&gt; <a href="https://www.bilibili.com/video/BV1JA411h7Gw/?spm_id_from=333.337.search-card.all.click&amp;vd_source=1db826af20ac643dce96190fd8c9f0e3">https://www.bilibili.com/video/BV1JA411h7Gw/?spm_id_from=333.337.search-card.all.click&amp;vd_source=1db826af20ac643dce96190fd8c9f0e3</a> bilibili net</p><h2 id="有序列表"><a class="markdownIt-Anchor" href="#有序列表"></a> 有序列表：</h2><p>把大象关进冰箱需要3步：</p><ol><li><p>打开冰箱</p></li><li><p>把大象放进冰箱</p></li><li><p>关上冰箱</p></li></ol><h2 id="无序列表"><a class="markdownIt-Anchor" href="#无序列表"></a> 无序列表</h2><p>把大象关进冰箱需要3步：</p><p>- 打开冰箱</p><p>- 把大象放进冰箱</p><p>- 关上冰箱</p><h2 id="任务列表"><a class="markdownIt-Anchor" href="#任务列表"></a> 任务列表</h2><p>今天学习任务列表:</p><p>- [ ] 今天</p><p>- [x] 今天</p><p>- [x] 今天</p><h2 id="代码块"><a class="markdownIt-Anchor" href="#代码块"></a> 代码块</h2><p>```c</p><p>int main(){</p><p>​    return 0;</p><p>}</p><p>```</p><h2 id="数学公式"><a class="markdownIt-Anchor" href="#数学公式"></a> 数学公式</h2><p class="katex-block"><span class="katex-display"><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML" display="block"><semantics><mrow><mfrac><mrow><mi mathvariant="normal">∂</mi><mi>f</mi></mrow><mrow><mi mathvariant="normal">∂</mi><mi>x</mi></mrow></mfrac><mo>=</mo><mn>2</mn><msqrt><mi>a</mi></msqrt><mi>x</mi></mrow><annotation encoding="application/x-tex">\frac{\partial f}{\partial x} = 2\sqrt{a}x</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:2.0574em;vertical-align:-0.686em;"></span><span class="mord"><span class="mopen nulldelimiter"></span><span class="mfrac"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:1.3714em;"><span style="top:-2.314em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord" style="margin-right:0.05556em;">∂</span><span class="mord mathnormal">x</span></span></span><span style="top:-3.23em;"><span class="pstrut" style="height:3em;"></span><span class="frac-line" style="border-bottom-width:0.04em;"></span></span><span style="top:-3.677em;"><span class="pstrut" style="height:3em;"></span><span class="mord"><span class="mord" style="margin-right:0.05556em;">∂</span><span class="mord mathnormal" style="margin-right:0.10764em;">f</span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.686em;"><span></span></span></span></span></span><span class="mclose nulldelimiter"></span></span><span class="mspace" style="margin-right:0.2778em;"></span><span class="mrel">=</span><span class="mspace" style="margin-right:0.2778em;"></span></span><span class="base"><span class="strut" style="height:1.04em;vertical-align:-0.1908em;"></span><span class="mord">2</span><span class="mord sqrt"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.8492em;"><span class="svg-align" style="top:-3em;"><span class="pstrut" style="height:3em;"></span><span class="mord" style="padding-left:0.833em;"><span class="mord mathnormal">a</span></span></span><span style="top:-2.8092em;"><span class="pstrut" style="height:3em;"></span><span class="hide-tail" style="min-width:0.853em;height:1.08em;"><svg xmlns="http://www.w3.org/2000/svg" width="400em" height="1.08em" viewBox="0 0 400000 1080" preserveAspectRatio="xMinYMin slice"><path d="M95,702c-2.7,0,-7.17,-2.7,-13.5,-8c-5.8,-5.3,-9.5,-10,-9.5,-14c0,-2,0.3,-3.3,1,-4c1.3,-2.7,23.83,-20.7,67.5,-54c44.2,-33.3,65.8,-50.3,66.5,-51c1.3,-1.3,3,-2,5,-2c4.7,0,8.7,3.3,12,10s173,378,173,378c0.7,0,35.3,-71,104,-213c68.7,-142,137.5,-285,206.5,-429c69,-144,104.5,-217.7,106.5,-221l0 -0c5.3,-9.3,12,-14,20,-14H400000v40H845.2724s-225.272,467,-225.272,467s-235,486,-235,486c-2.7,4.7,-9,7,-19,7c-6,0,-10,-1,-12,-3s-194,-422,-194,-422s-65,47,-65,47zM834 80h400000v40h-400000z"/></svg></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.1908em;"><span></span></span></span></span></span><span class="mord mathnormal">x</span></span></span></span></span></p><h2 id="行内数学公式"><a class="markdownIt-Anchor" href="#行内数学公式"></a> 行内数学公式</h2><p><span class="katex"><span class="katex-mathml"><math xmlns="http://www.w3.org/1998/Math/MathML"><semantics><mrow><mo stretchy="false">⌈</mo><mfrac><mi>n</mi><mn>2</mn></mfrac><mo stretchy="false">⌉</mo></mrow><annotation encoding="application/x-tex">\lceil \frac{n}{2} \rceil</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:1.095em;vertical-align:-0.345em;"></span><span class="mopen">⌈</span><span class="mord"><span class="mopen nulldelimiter"></span><span class="mfrac"><span class="vlist-t vlist-t2"><span class="vlist-r"><span class="vlist" style="height:0.6954em;"><span style="top:-2.655em;"><span class="pstrut" style="height:3em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mtight">2</span></span></span></span><span style="top:-3.23em;"><span class="pstrut" style="height:3em;"></span><span class="frac-line" style="border-bottom-width:0.04em;"></span></span><span style="top:-3.394em;"><span class="pstrut" style="height:3em;"></span><span class="sizing reset-size6 size3 mtight"><span class="mord mtight"><span class="mord mathnormal mtight">n</span></span></span></span></span><span class="vlist-s">​</span></span><span class="vlist-r"><span class="vlist" style="height:0.345em;"><span></span></span></span></span></span><span class="mclose nulldelimiter"></span></span><span class="mclose">⌉</span></span></span></span></p><h2 id="表格"><a class="markdownIt-Anchor" href="#表格"></a> 表格</h2><p>|姓名|年龄|成绩|</p><p>|:—|—😐:—😐</p><p>|张三|19|19|</p><p>|李四|19|19|</p><h2 id="脚注"><a class="markdownIt-Anchor" href="#脚注"></a> 脚注</h2><p>一键三连<sup class="footnote-ref"><a href="#fn1" id="fnref1">[1]</a></sup></p><h2 id="横线"><a class="markdownIt-Anchor" href="#横线"></a> 横线</h2><p>-–</p><h2 id="链接"><a class="markdownIt-Anchor" href="#链接"></a> 链接</h2><p><a href="baidu.com" title="一个垃圾的搜索引擎">百度</a></p><h2 id="引用链接"><a class="markdownIt-Anchor" href="#引用链接"></a> 引用链接</h2><p><a href="baidu.com" title="一个垃圾的搜索引擎">百度</a></p><p><a href="Google.com" title="一个还可以的搜索引擎">谷歌</a></p><h2 id="链接设置到标题"><a class="markdownIt-Anchor" href="#链接设置到标题"></a> 链接设置到标题</h2><p>请参考<a href="#%E4%BA%94%E7%BA%A7%E6%A0%87%E9%A2%98">标题</a></p><h2 id="url"><a class="markdownIt-Anchor" href="#url"></a> URL</h2><p><a href="http://www.baidu.com">http://www.baidu.com</a></p><h2 id="图片"><a class="markdownIt-Anchor" href="#图片"></a> 图片</h2><p><img src="https://www.baidu.com/img/PCtm_d9c8750bed0b3c7d089fa7d55720d6cf.png" alt="百度" title="百度搜索" /></p><p>如果是本地地址那么可以写<strong>本地图片的相对地址或者绝对地址</strong></p><p>![本地图片](c:\Users\Public\Pictures\Sample Pictures\八仙花.jpg “百度搜索”)</p><p><strong>本地地址未成功</strong></p><h2 id="斜体"><a class="markdownIt-Anchor" href="#斜体"></a> 斜体</h2><p><em>斜体</em></p><h2 id="加粗"><a class="markdownIt-Anchor" href="#加粗"></a> 加粗</h2><p><strong>加粗</strong></p><h2 id="行内代码"><a class="markdownIt-Anchor" href="#行内代码"></a> 行内代码</h2><p><code>行内代码</code></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">printf()</span><br></pre></td></tr></table></figure><h2 id="下划线"><a class="markdownIt-Anchor" href="#下划线"></a> 下划线</h2><p><u>下划线</u></p><h2 id="标准的表情符号"><a class="markdownIt-Anchor" href="#标准的表情符号"></a> 标准的表情符号</h2><p>😄</p><p>&gt;<a href="https://unicode.org/emoji/charts/full-emoji-list.html">https://unicode.org/emoji/charts/full-emoji-list.html</a></p><h2 id="下标"><a class="markdownIt-Anchor" href="#下标"></a> 下标</h2><p>H<sub>2</sub>O</p><h2 id="上标"><a class="markdownIt-Anchor" href="#上标"></a> 上标</h2><p>X<sup>2</sup></p><h2 id="高亮文字"><a class="markdownIt-Anchor" href="#高亮文字"></a> 高亮文字</h2><p><mark>这是一段高亮文字</mark></p><h2 id="视频"><a class="markdownIt-Anchor" href="#视频"></a> 视频</h2><iframe src="//player.bilibili.com/player.html?aid=327623069&bvid=BV1JA411h7Gw&cid=171385214&page=1" scrolling="no" border="0" frameborder="no" framespacing="0" allowfullscreen="true"> </iframe><hr class="footnotes-sep" /><section class="footnotes"><ol class="footnotes-list"><li id="fn1" class="footnote-item"><p>点赞，投币，收藏。 <a href="#fnref1" class="footnote-backref">↩︎</a></p></li></ol></section>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;markdown基本语法&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href=&quot;#markdown基本语法&quot;&gt;&lt;/a&gt; Markdown基本语法&lt;/h1&gt;
&lt;h2 id=&quot;标题&quot;&gt;&lt;a class=&quot;markdownIt-Anchor&quot; href</summary>
      
    
    
    
    
    <category term="Markdown" scheme="https://11111-beep.github.io/tags/Markdown/"/>
    
  </entry>
  
</feed>
